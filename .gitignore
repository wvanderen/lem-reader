# Dependencies
node_modules

# Build output
dist
dist-ssr

# Local env / secrets
*.local

# OS
.DS_Store

# Test artifacts
playwright-report
test-results
coverage
.calibration-tmp/
.perf-tmp/

# Local real-PDF calibration corpus (D11-04) — licensing/size keep real PDFs
# local; only DERIVED evidence commits (tests/unit/server/pdf-calibration/).
# The README is the directory's committed convention card.
corpus/pdf/*
!corpus/pdf/README.md

# Local real-EPUB calibration corpus (D12-12, the D11-04 mirror) —
# licensing/size keep real books local; only DERIVED evidence commits
# (tests/unit/server/epub-calibration/). README is the convention card.
corpus/epub/*
!corpus/epub/README.md
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root

# Cloudflare Wrangler local state (workerd cache, build artifacts)
.wrangler/

# Vercel CLI local state (project link written on first deploy — 260821-k6z;
# the .vercel/ dir holds per-machine org/project bindings, never committable)
.vercel/
