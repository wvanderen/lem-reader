---
phase: 10-annotation-review-panel
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - tests/e2e/review-panel/listing.spec.ts
  - tests/e2e/review-panel/tri-state.spec.ts
  - tests/e2e/review-panel/empty-states.spec.ts
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "Every seeded highlight appears in the panel grouped under its article (h2 section titles), with note previews on noted rows, visible dates, and rows within sections ordered by the active sort — newest-first under default Date, position-ascending under Position/Article (RECV-01.b, D10-04/D10-06, D10-08)"
    - "Filter controls compose with AND semantics live: tag chips narrow to the tagged article's rows, the article select narrows to one article, the confidence select narrows by tri-state — and combinations narrow further (D10-08)"
    - "Sort select reorders sections/rows: Date default (newest first), Article (title alpha), Position (library order) — observable in the DOM order (D10-08)"
    - "Ambiguous rows show their badge, orphan rows appear in the trailing 'Highlights without an article' group, confident rows are badge-less, the legend line is present, and confidence=All never hides unresolved rows (RECV-01.e, D10-05/D10-07)"
    - "Empty states are distinct: a wiped library shows the no-highlights-yet copy; a filter combination matching nothing shows the no-match copy — both inside the .status live region (RECV-01.g, D10-10)"
  artifacts:
    - path: "tests/e2e/review-panel/listing.spec.ts"
      provides: "Cross-article listing + filter AND + sort coverage (RECV-01.b + RECV-01.d e2e smoke)"
      min_lines: 80
    - path: "tests/e2e/review-panel/tri-state.spec.ts"
      provides: "Honest tri-state surfacing coverage (RECV-01.e)"
      min_lines: 60
    - path: "tests/e2e/review-panel/empty-states.spec.ts"
      provides: "Both empty-state branches (RECV-01.g)"
      min_lines: 40
  key_links:
    - from: "tests/e2e/review-panel/listing.spec.ts"
      to: "tests/e2e/portability/_portability.ts"
      via: "seeds via seedRows/makeArticle/confidentHighlightOn/highlightRow — helpers reused, never forked"
      pattern: "seedRows"
    - from: "tests/e2e/review-panel/tri-state.spec.ts"
      to: "src/routes/review/ReviewView.tsx"
      via: "asserts rendered badge/orphan-section DOM produced by deriveReviewSections"
      pattern: "Highlights without an article"
---

<objective>
Prove the review surface behavior in real browsers (RECV-01.b/.d/.e/.g): cross-article listing with metadata, live filter AND-composition and sort reordering, honest tri-state surfacing with the orphan tail, and both empty states through the .status live region.

Purpose: The deep logic is unit-proven by 10-01; this plan proves the user-visible contract of the surface shipped by 10-02 — the grouped list really shows everything (never silently hidden), the controls really compose, and the empty states really distinguish the two cases. Specs reuse the portability seeding helpers wholesale.

Output: Three fully-strengthened e2e specs (listing, tri-state, empty-states) green across chromium/firefox/webkit.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md
@.planning/phases/10-annotation-review-panel/10-02-SUMMARY.md

Seeding/analog sources:
@tests/e2e/portability/_portability.ts
@tests/e2e/annotations/_fixtures.ts
@src/routes/review/ReviewView.tsx
@src/routes/review/reviewFilter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: listing.spec.ts — cross-article listing + filter AND + sort</name>
  <files>tests/e2e/review-panel/listing.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/listing.spec.ts (the 10-01 sentinel to replace)
  - tests/e2e/portability/_portability.ts (seedRows L191–225, makeArticle L232–255 incl. tags/paragraph overrides, confidentHighlightOn L261–276, highlightRow L279–293 — the noteRow/store shapes for seeding a NoteRecord row)
  - tests/e2e/annotations/_fixtures.ts (wipeDatabase L44, BASE L33)
  - src/routes/review/ReviewView.tsx (the shipped markup: section h2 structure, row anatomy classes, select labels, TagFilter chips)
  - src/ingestion/library/LibraryView.tsx (the .library-list > li direct-child selector lesson from STATE 08-05 if counting li elements — count rows via a review-specific selector, not generic li)
  </read_first>
  <action>
  Replace the sentinel. Shared setup: beforeEach wipeDatabase; a seed helper building a two-article corpus through seedRows, seeded in this library order — article A first, article B second — with article A (title alpha-earlier, e.g. "Alpha…", tag "essay", 2 confident highlights at different positions with explicitly seeded createdAt: the earlier-position highlight older, the later-position highlight newer — both still older than article B's — so within-section date order deliberately differs from position order; one highlight carries a NoteRecord) and article B (title alpha-later, e.g. "Zeta…", no tags, 1 confident highlight whose createdAt is the newest in the corpus) — all via makeArticle/confidentHighlightOn/highlightRow so anchors are verified-confident at seed time. Tests (query by role/name — h2 section headings, buttons, comboboxes):
  1. Listing completeness + metadata: goto BASE + "/#/review" → h2 for each article title present; every highlight's quote excerpt text visible within its article's section; the noted row shows its note preview text; each row shows a date; rows within a section follow the DEFAULT Date contract, newest-first (assert the newer article-A excerpt precedes the older one — the position-order assertion is NOT made here; it belongs to the Position-sort step of test 5, whose contract is position).
  2. Tag filter: click the "essay" tag chip → only article A's section and rows remain; article B's title absent.
  3. Article select: choose article B in the article combobox → only article B rows remain (also proves the select is populated with both titles).
  4. Confidence filter compose: select confidence "Confident" → all rows remain (all seeds are confident — documents that confident is a first-class option); combining tag "essay" + confidence "Ambiguous" yields the no-match empty copy (bridge to empty-states, but assert at least that zero rows render).
  5. Sort default + reordering: with default sort Date, article B's section precedes article A's (B's single highlight is the corpus-newest and 10-01 orders sections by newest entry createdAt — note B's alpha-later title makes this the opposite of alpha order); switch sort to "Article" → the order flips: article A ("Alpha…") now precedes B; switch sort to "Position" → sections follow the seeded library order (A first, B second — differs from the Date ordering, so a Date-fallback regression fails here) and within article A's section the two rows now render position-ascending (earlier-position excerpt first — the position-order assertion lives under this sort, never under the default Date sort).
  Selector discipline: locate rows via their quote text (getByText) or the review-row button role; never index-based nth where order isn't the assertion target.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/listing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - Spec covers: completeness (every seeded highlight visible under its article h2), note preview presence, newest-first rows within a section under default Date sort and position-ascending rows under Position sort, tag narrowing, article-select narrowing, confidence option behavior, and sort flips between Date/Article/Position with seeds chosen so section date-order ≠ alpha-order ≠ library order and within-section date-order ≠ position-order
  - Seeding uses only seedRows/makeArticle/confidentHighlightOn/highlightRow imports — no forked seeding code
  - npx playwright test tests/e2e/review-panel/listing.spec.ts exits 0 across chromium/firefox/webkit
  </acceptance_criteria>
  <done>RECV-01.b proven plus the e2e smoke of RECV-01.d: the grouped list is complete with metadata, filters AND-compose live, and all three sorts observably reorder the DOM.</done>
</task>

<task type="auto">
  <name>Task 2: tri-state.spec.ts + empty-states.spec.ts — honest surfacing and empty branches</name>
  <files>tests/e2e/review-panel/tri-state.spec.ts, tests/e2e/review-panel/empty-states.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/tri-state.spec.ts and tests/e2e/review-panel/empty-states.spec.ts (the 10-01 sentinels to replace)
  - tests/e2e/portability/_portability.ts (the two tri-state knobs: orphan via highlightRow with articleId "ghost-article"; ambiguous via an article whose body duplicates the anchor sentence — research Code Examples)
  - src/routes/review/ReviewView.tsx (badge class names/aria structure for ambiguous vs orphan rows; the legend line text; the orphan section h2 "Highlights without an article")
  - src/portability/markdown.ts (UNMATCHED_SECTION_HEADING L129 — the exact heading vocabulary the spec must assert)
  - tests/e2e/annotations/_fixtures.ts (wipeDatabase)
  </read_first>
  <action>
  tri-state.spec.ts — replace the sentinel. Setup: seed one article carrying (a) a confident highlight on unique text, (b) an ambiguous highlight whose quote sentence appears twice in the article body, plus (c) an orphan highlightRow pointing at articleId "ghost-article". Tests:
  1. Honest badges: the ambiguous row renders its badge element; the confident row renders none; the legend line ("No badge means anchored confidently" wording) is visible under the filter row.
  2. Orphan tail: the trailing section heading "Highlights without an article" is present, its group contains the ghost-article row with the quote text visible (D10-03: orphan rows show the quote itself), and the orphan row exposes no enabled jump affordance.
  3. Never silently hidden: with confidence "All" selected (the default), both the ambiguous and orphan rows are visible; selecting confidence "Orphan" narrows to only the orphan group; selecting "Ambiguous" narrows to only the ambiguous row.
  4. Ambiguous rows are not jumpable: the ambiguous row's jump control is disabled or absent (mirror however ReviewView rendered it — assert the reader cannot activate it).
  empty-states.spec.ts — replace the sentinel. Tests:
  1. No highlights at all: wipeDatabase, seed ONLY articles via seedRows (zero highlight rows), goto #/review → the .status region contains the "No highlights yet" copy (assert the distinct first sentence; exact full string per ReviewView).
  2. Filters-zero: seed the Task-1-style corpus, apply a filter combination that matches nothing (e.g. tag chip for a tag no seeded article carries is impossible — instead use article select of article B + confidence "Orphan") → zero rows and the "No highlights match these filters" copy in .status, while the library is demonstrably non-empty (assert some article title was visible before filtering).
  3. Live-region parity: both empty-state texts render inside the element carrying role="status" (assert the region's text content), proving SR announcement parity (D10-10).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/tri-state.spec.ts tests/e2e/review-panel/empty-states.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - tri-state.spec asserts: ambiguous badge present + confident badge absent + legend visible; orphan group heading is exactly "Highlights without an article" with its row present and not jumpable; confidence=All shows unresolved rows; Orphan/Ambiguous filters narrow correctly
  - empty-states.spec asserts both distinct copies inside the role="status" region, with the no-match case proven against a non-empty library
  - Both specs exit 0 across chromium/firefox/webkit
  </acceptance_criteria>
  <done>RECV-01.e and RECV-01.g proven in real browsers: tri-state is honest and never silently hidden, the orphan tail is first-class, and both empty states are distinct and live-region announced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded rows → assertions | Specs inject Dexie rows directly (including foreign-shaped orphan/ambiguous cases) — validates the panel against hostile-ish data shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04a | Tampering (information integrity) | orphan/ambiguous seeds | mitigate | Specs prove the panel surfaces (never hides) rows whose anchors are broken — the SC#4 "never silently hidden" guarantee becomes regression-locked |
| T-10-04b | Tampering | npm installs | accept | Zero packages installed; specs-only plan |
</threat_model>

<verification>
- npx playwright test tests/e2e/review-panel/listing.spec.ts tests/e2e/review-panel/tri-state.spec.ts tests/e2e/review-panel/empty-states.spec.ts — all green, 3 engines
- npm run test:unit -- --run — untouched, stays green (specs-only plan)
</verification>

<success_criteria>
- RECV-01.b (listing + metadata), RECV-01.d (filter AND + three sorts, e2e smoke over the 10-01 unit core), RECV-01.e (tri-state honest), RECV-01.g (empty states) all proven in real browsers
- Seeding reuses _portability.ts helpers wholesale — no forked seed code
- No source changes in this plan (specs-only — surface fixes, if any assertion reveals a 10-02 defect, are minimal corrections inside ReviewView/reviewFilter with a SUMMARY note per Rule 1)
</success_criteria>

## Artifacts this plan produces

- Strengthened `tests/e2e/review-panel/listing.spec.ts` (completeness, metadata, filter AND-composition, sort flips — no new source symbols)
- Strengthened `tests/e2e/review-panel/tri-state.spec.ts` (badge/orphan-tail/never-hidden matrix)
- Strengthened `tests/e2e/review-panel/empty-states.spec.ts` (both .status branches)

<output>
Create `.planning/phases/10-annotation-review-panel/10-04-SUMMARY.md` when done
</output>
