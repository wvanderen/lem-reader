---
phase: 10-annotation-review-panel
plan: 06
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified:
  - tests/e2e/review-panel/jump-bidirectional.spec.ts
  - tests/e2e/a11y.spec.ts
  - tests/e2e/forced-colors.spec.ts
  - tests/e2e/reduced-motion.spec.ts
  - tests/e2e/panel-keyboard.spec.ts
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "The full SC#2 loop works from the panel itself: click a confident row on #/review → article opens on the highlight with the mark focused → browser Back returns to #/review — in BOTH reading modes"
    - "#/review passes the axe gate with zero serious/critical WCAG 2.2 AA violations (heading-order and list guards included) on a seeded non-empty panel"
    - "The existing edge-condition specs (forced-colors, reduced-motion, panel-keyboard) cover the #/review route wherever they enumerate routes — strengthen-only, no existing assertion removed"
    - "The FULL npm run test suite exits 0 in ONE invocation — no subsets, no --grep, no engine skip — with pass/fail counts recorded honestly in 10-06-OUTPUT.md (the 04-11/09-07 anti-pattern guard)"
  artifacts:
    - path: "tests/e2e/review-panel/jump-bidirectional.spec.ts"
      provides: "Panel-row → article → Back loop (RECV-01.c closing half)"
    - path: "tests/e2e/a11y.spec.ts"
      provides: "#/review axe case with heading-order/list guards"
    - path: "tests/e2e/forced-colors.spec.ts"
      provides: "#/review route case if route-enumerating (else byte-unchanged)"
    - path: "tests/e2e/reduced-motion.spec.ts"
      provides: "#/review route case if route-enumerating (else byte-unchanged)"
    - path: "tests/e2e/panel-keyboard.spec.ts"
      provides: "#/review keyboard reachability case if route-enumerating (else byte-unchanged)"
  key_links:
    - from: "tests/e2e/review-panel/jump-bidirectional.spec.ts"
      to: "src/routes/review/ReviewView.tsx + src/routes/ArticleView.tsx"
      via: "row-button click pushes #/article/<id>/h/<hid>; on-mount jump focuses the mark; page.goBack() returns to #/review"
      pattern: "goBack"
    - from: "tests/e2e/a11y.spec.ts"
      to: "#/review"
      via: "AxeBuilder analyze on the seeded review route with the same WCAG_TAGS + seriousViolations filter as sibling cases"
      pattern: "#/review"
---

<objective>
Close RECV-01 end-to-end (per the 04-02/09-01 split precedent, the requirement flips Complete only here): prove the bidirectional loop from the panel row itself, pass the accessibility and edge-condition gates on the new route, and run the honest full-suite phase gate.

Purpose: SC#2's bidirectionality is a browser-history property — this plan proves it through the real UI path (row click → jump → Back), runs the shipped axe/forced-colors/reduced-motion/keyboard bars against the new surface, and locks the whole phase with the full-suite discipline every phase since 04-11 uses (executor runs it, records counts honestly, never trusts a prior SUMMARY).

Output: Completed bidirectional spec, a11y/edge route coverage, 10-06-OUTPUT.md full-suite record.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md
@.planning/phases/10-annotation-review-panel/10-03-SUMMARY.md
@.planning/phases/10-annotation-review-panel/10-04-SUMMARY.md
@.planning/phases/10-annotation-review-panel/10-05-SUMMARY.md

Gate precedents:
@tests/e2e/a11y.spec.ts
@tests/e2e/forced-colors.spec.ts
@tests/e2e/reduced-motion.spec.ts
@tests/e2e/panel-keyboard.spec.ts
@tests/e2e/review-panel/jump-bidirectional.spec.ts
@tests/e2e/portability/_portability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bidirectional loop from the panel row (both reading modes)</name>
  <files>tests/e2e/review-panel/jump-bidirectional.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/jump-bidirectional.spec.ts (10-03's deep-link tests — EXTEND, keep every existing test green)
  - src/routes/review/ReviewView.tsx (row jump button accessible name/structure — click it as a reader would)
  - tests/e2e/annotations/navigate-back.spec.ts (the retry-assert focus shape)
  - tests/e2e/portability/_portability.ts (seedRows/makeArticle/confidentHighlightOn)
  - tests/e2e/annotations/_fixtures.ts (wipeDatabase; the settings-seed pattern for readingMode "scrolling" used by 10-03)
  </read_first>
  <action>
  EXTEND the spec (strengthen-only — all 10-03 tests remain byte-stable). Add two loop tests, one per reading mode:
  1. Paginated loop: seed an article with a confident highlight; goto BASE + "/#/review"; locate the confident row's jump button (by role/name) and CLICK it (a real reader click — this is the assertion 10-03 could not make, since the panel did not exist yet); assert arrival via the retrying focus check (document.activeElement is the mark) and the stripped URL; then page.goBack() → h1 "Review highlights" visible again (Back returned to #/review — SC#2 bidirectional).
  2. Scrolling loop: identical flow with the readingMode "scrolling" settings seed.
  3. Focus-restore observation: after goBack() to #/review, assert the panel is operable (e.g. the filter combobox or the first row button is focusable/visible) — do NOT assert focus lands on the origin row (engine-variable; the manual-only check in 10-VALIDATION.md covers the feel).
  Pitfall 9 (research): after Back the panel remounts scrolled to top — that is the accepted documented limitation; do NOT add scroll-restoration assertions or machinery.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - Spec gains two loop tests (paginated + scrolling) driving the jump from the panel row button itself and returning via page.goBack() to the #/review h1
  - All 10-03 deep-link tests still pass unchanged
  - No assertion on origin-row focus after Back (documented engine variance); panel operability is asserted instead
  - npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts exits 0 across chromium/firefox/webkit
  </acceptance_criteria>
  <done>RECV-01.c fully closed: bidirectional jump proven through the real UI path in both reading modes across all three engines.</done>
</task>

<task type="auto">
  <name>Task 2: a11y gate on #/review + edge-spec route coverage (strengthen-only)</name>
  <files>tests/e2e/a11y.spec.ts, tests/e2e/forced-colors.spec.ts, tests/e2e/reduced-motion.spec.ts, tests/e2e/panel-keyboard.spec.ts</files>
  <read_first>
  - tests/e2e/a11y.spec.ts (route-enum pattern L36–44: AxeBuilder withTags WCAG_TAGS, seriousViolations filter, the heading-order + list not.toContain guards; BASE L16)
  - tests/e2e/forced-colors.spec.ts, tests/e2e/reduced-motion.spec.ts, tests/e2e/panel-keyboard.spec.ts (read each: IF it enumerates routes, extend with #/review; if it is fixture-scoped only, leave byte-unchanged and note that in the SUMMARY — research Wave-0 gap 4 says "extend if selectors scoped")
  - tests/e2e/portability/_portability.ts (seeding for a non-empty panel: article + confident highlight + note so axe sees real row structure)
  - docs/ACCEPTANCE-PROTOCOL.md (manual SR protocol context — the two manual-only rows in 10-VALIDATION.md remain for /gsd-verify-work, NOT automated here)
  </read_first>
  <action>
  1. tests/e2e/a11y.spec.ts: add a "#/review" case cloned from the fixture-list case — wipeDatabase, seed one article + one confident highlight + one note, goto BASE + "/#/review", AxeBuilder analyze with the same WCAG_TAGS, expect seriousViolations empty AND ids not.toContain "heading-order" and not.toContain "list" (the Pitfall-8 h1/semantic-list guards; one h1 + grouped ul structure must pass them).
  2. forced-colors / reduced-motion / panel-keyboard specs: read each; if the spec enumerates routes or applies per-view, add the #/review case in that spec's own idiom (forced-colors: badges/rows legible under emulation; reduced-motion: trivially satisfied — the review block ships no transition/animation properties, assert the page renders and is operable; panel-keyboard: Tab reaches filter controls → sort select → rows → entry-button back out). If a spec is strictly fixture-article-scoped and cannot host a review case, leave it byte-unchanged and record the scoping decision in the SUMMARY.
  3. Strengthen-only discipline: no existing assertion in any of these four specs is removed or weakened.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/a11y.spec.ts tests/e2e/forced-colors.spec.ts tests/e2e/reduced-motion.spec.ts tests/e2e/panel-keyboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - a11y.spec.ts contains a #/review case asserting zero serious/critical violations plus the heading-order and list guards on a seeded non-empty panel
  - Each of the three edge specs either gained a #/review case in its own idiom or is byte-unchanged with the scoping rationale recorded in the SUMMARY
  - git diff shows no removed assertions in the four specs (strengthen-only)
  - All four specs exit 0 across chromium/firefox/webkit
  </acceptance_criteria>
  <done>The new route is held to the shipped accessibility and edge-condition bars — automated coverage locked; the two manual-only SR checks remain queued for /gsd-verify-work per 10-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite phase gate + OUTPUT record</name>
  <files>tests/e2e/review-panel/jump-bidirectional.spec.ts</files>
  <read_first>
  - .planning/phases/09-*/09-07-OUTPUT.md (the permanent-record format: literal command, per-suite + per-engine counts, literal exit code, anti-pattern attestation)
  - .planning/STATE.md sections on the 04-11/09-07 honest-suite precedent (the "269/0 misreport" history and its guard)
  - package.json (scripts: test = test:unit --run && test:e2e)
  </read_first>
  <action>
  Run the FULL suite in ONE invocation: npm run test (unit + e2e across chromium/firefox/webkit + the throttled perf project). No subsets, no --grep, no engine skip, no watch flags. YOU run it — do not trust any prior SUMMARY's counts. Record honestly: total passed / failed / skipped per suite (unit vs e2e) and the literal exit code; the 13 documented intentional skips are expected (the 09-07 baseline) — any NEW skip or failure is a finding to fix before this task completes, not a footnote. If failures surface, fix them within phase scope (review-panel code or its regressions) and re-run the full suite; genuinely unrelated pre-existing failures get logged to .planning/phases/10-annotation-review-panel/deferred-items.md per the 08-05 scope-boundary rule — but the gate is not green while any fail > 0 remains, so treat everything as fix-first. Write .planning/phases/10-annotation-review-panel/10-06-OUTPUT.md as the permanent record: the literal command line, the counts, the exit code, and the anti-pattern attestation (executor ran the suite; fail counts recorded honestly rather than omitted). The jump-bidirectional.spec.ts files_modified entry reflects any stabilization this gate demanded (load-race fixes follow the 09-07 precedent); if no source/spec change was needed, record that outcome in the OUTPUT instead.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
  - npm run test exits 0 in one invocation; recorded counts show failed = 0 and skipped limited to the documented intentional set (≤ 13, matching the 09-07 baseline)
  - 10-06-OUTPUT.md exists containing the literal command, per-suite counts, exit code, and the anti-pattern attestation
  - Any failures encountered on the way were fixed and re-run to green (or, if truly out of scope, logged to deferred-items.md AND surfaced in the SUMMARY as a Rule-4 item — but the suite itself must still exit 0 for this task to pass)
  </acceptance_criteria>
  <done>The phase gate is green the honest way: one invocation, zero failures, counts on the record — RECV-01's end-to-end proof is complete and the requirement can flip to Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full suite → phase verdict | The gate's honesty is itself the security property: misreported counts are the documented failure mode this task defends against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06a | Repudiation (process integrity) | full-suite gate | mitigate | Executor runs npm run test itself in ONE invocation; literal counts + exit code recorded in 10-06-OUTPUT.md; no subset/grep/engine-skip; fail counts written honestly (04-11/09-07 anti-pattern guard) |
| T-10-06b | Tampering (a11y regression) | #/review surface | mitigate | axe gate with heading-order/list guards + the three edge-spec route cases lock the shipped a11y bar against the new route |
| T-10-06c | Tampering | npm installs | accept | Zero packages installed |
</threat_model>

<verification>
- npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts — loop + deep-link matrix green, 3 engines
- npx playwright test tests/e2e/a11y.spec.ts tests/e2e/forced-colors.spec.ts tests/e2e/reduced-motion.spec.ts tests/e2e/panel-keyboard.spec.ts — gates green, 3 engines
- npm run test — full suite, one invocation, exit 0, honest record in 10-06-OUTPUT.md
</verification>

<success_criteria>
- RECV-01 fully proven end-to-end: listing, bidirectional jump (both modes), filter/sort, honest tri-state, in-place curation — the requirement flips to Complete with this plan (04-02/09-01 split precedent)
- Zero serious/critical axe violations on #/review; edge specs cover the route wherever scoped
- Full suite exits 0 with an honest OUTPUT record; no regressions in the 44-requirement v1.0 substrate or the v2.0 phases 7–9
</success_criteria>

## Artifacts this plan produces

- Two bidirectional loop tests (paginated + scrolling, row-click driven) appended to `tests/e2e/review-panel/jump-bidirectional.spec.ts`
- `#/review` axe case in `tests/e2e/a11y.spec.ts`; `#/review` cases in `forced-colors` / `reduced-motion` / `panel-keyboard` specs where route-scoped
- `.planning/phases/10-annotation-review-panel/10-06-OUTPUT.md` — the permanent full-suite record (literal command, counts, exit code, attestation)

<output>
Create `.planning/phases/10-annotation-review-panel/10-06-SUMMARY.md` when done
</output>
