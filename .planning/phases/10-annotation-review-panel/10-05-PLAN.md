---
phase: 10-annotation-review-panel
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/routes/review/ReviewNoteDialog.tsx
  - src/routes/review/DeleteHighlightConfirm.tsx
  - src/routes/review/ReviewView.tsx
  - tests/e2e/review-panel/curate.spec.ts
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "Reader can edit a note in place from any row (including an orphan's note — notes are keyed to highlightId, no article needed) and the edit persists (D10-11)"
    - "Empty note text deletes the NoteRecord (D5-10 empty-text policy owned by the dialog); every close path — Done, Esc, delete — routes through ONE commit/cleanup path so no keystrokes are lost (Pitfall 7)"
    - "Reader can delete a highlight via a native <dialog> alertdialog confirmation whose copy states the attached note is removed with it; the destructive write fires ONLY in the Proceed button onClick (Pitfall 8) and [data-initial-focus] sits on the NON-destructive button (D10-12)"
    - "After a delete or note edit commits, the panel re-derives from Dexie without a reload (refreshKey bump — Pitfall 6) and the result announces via .status in calm voice ('Highlight removed.')"
    - "deleteHighlight is the ONE call for highlight+note removal (its Dexie transaction cascade-deletes notes — no second call); saveNote/deleteNote appear only inside ReviewNoteDialog"
  artifacts:
    - path: "src/routes/review/ReviewNoteDialog.tsx"
      provides: "Structural clone of NotePopover bound to props (not useHighlightOverlay)"
      exports: ["ReviewNoteDialog"]
      min_lines: 100
    - path: "src/routes/review/DeleteHighlightConfirm.tsx"
      provides: "Structural clone of RemoveConfirm calling deleteHighlight"
      exports: ["DeleteHighlightConfirm"]
      min_lines: 100
    - path: "src/routes/review/ReviewView.tsx"
      provides: "Per-row Edit note / Remove affordances + dialog wiring + refreshKey bump + .status announcements"
    - path: "tests/e2e/review-panel/curate.spec.ts"
      provides: "Edit/delete/cascade-copy/announcement/re-derivation coverage (RECV-01.f)"
  key_links:
    - from: "src/routes/review/DeleteHighlightConfirm.tsx"
      to: "src/persistence/highlightsStore.ts"
      via: "deleteHighlight(highlightId) lives ONLY in the destructive button onClick — single grep-verifiable call site"
      pattern: "deleteHighlight"
    - from: "src/routes/review/ReviewNoteDialog.tsx"
      to: "src/persistence/notesStore.ts"
      via: "saveNote / deleteNote in the Done/close commit path only"
      pattern: "saveNote|deleteNote"
    - from: "src/routes/review/ReviewView.tsx"
      to: "src/routes/review/ReviewNoteDialog.tsx + DeleteHighlightConfirm.tsx"
      via: "row affordances open the dialogs; onDone/onConfirm bump refreshKey and write .status"
      pattern: "setRefreshKey"
---

<objective>
Ship in-place curation (RECV-01.f / SC#4): edit any note and delete any highlight directly from the review panel, with the codebase's structural-clone dialog discipline, cascade-honest copy, live-region announcements, and stale-free re-derivation.

Purpose: D10-11 and D10-12 lock the curation UX to shipped patterns — NotePopover's dialog shape and RemoveConfirm's destructive-confirm discipline are cloned (never shared-abstracted, per the 08-04/09-05 Pitfall 8 lineage) because the originals are bound to contexts the panel doesn't have. deleteHighlight's existing transaction makes highlight+note removal one atomic call.

Output: ReviewNoteDialog.tsx, DeleteHighlightConfirm.tsx, ReviewView curation wiring, curate.spec.ts.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md
@.planning/phases/10-annotation-review-panel/10-02-SUMMARY.md

Clone sources (verbatim excerpts in 10-PATTERNS.md):
@src/reader/annotations/NotePopover.tsx
@src/ingestion/library/RemoveConfirm.tsx
@src/persistence/notesStore.ts
@src/persistence/highlightsStore.ts
@src/routes/review/ReviewView.tsx
@src/content/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewNoteDialog — structural clone of NotePopover for the panel context</name>
  <files>src/routes/review/ReviewNoteDialog.tsx</files>
  <read_first>
  - src/reader/annotations/NotePopover.tsx (the clone source: open/close sync effect L99–131 with triggerRef capture + showModal + textarea focus/select; close listener L139–149 routing every close path through one cleanup; textarea as React text child L271–283 — NEVER raw HTML)
  - src/persistence/notesStore.ts (saveNote L46 upsert, deleteNote L56 — signatures and NoteRecord expectations)
  - src/content/schema.ts (NoteRecord shape: highlightId, articleId, text, updatedAt — build rows via its schema-validated constructor or parse)
  - src/ingestion/library/RemoveConfirm.tsx (the dialog focus-discipline family shape)
  </read_first>
  <action>
  Create src/routes/review/ReviewNoteDialog.tsx exporting function ReviewNoteDialog. Props: { open: boolean; highlightId: string; articleId: string; existing: NoteRecord | null; onDone: () => void } — takes its data as PROPS and calls the stores directly; it does NOT import useHighlightOverlay (the per-article provider the panel lives outside of — the entire reason this is a clone, per Pattern 3).
  Structure cloned from NotePopover: a <dialog> with ref; the open/close sync effect (capture document.activeElement into triggerRef BEFORE showModal; showModal; focus+select the textarea on open; dlg.close() when open flips false); the close-listener effect restoring triggerRef.current?.focus() on every close. Commit semantics per research Pitfall 7's simple option: local textarea state initialized from existing?.text ?? ""; ONE commit function invoked from BOTH the Done button onClick AND the dialog close listener (every close path — Done/Esc — routes through the single commit): if text is empty → await deleteNote(highlightId) (D5-10 empty-text policy); else → await saveNote({ highlightId, articleId, text, updatedAt: new Date().toISOString() }) built to the NoteRecord schema shape; then call onDone(). Wrap the commit in try/catch that still calls onDone (a failed write must not strand the dialog — the refresh re-derives from Dexie and reveals the truth, the RemoveConfirm catch discipline). saveNote and deleteNote appear ONLY inside this component's commit path (grep-verifiable). Editable for orphans is automatic — notes are keyed to highlightId and articleId is carried on the highlight record even when its article is gone (verify against the seeded orphan shape; the NoteRecord schema fields must be satisfied from the highlight row itself for orphan rows).
  Markup: dialog with aria-labelledby heading (calm copy naming the action — exact strings agent discretion per CONTEXT), the textarea (React text child discipline, rows=3, placeholder mirroring NotePopover's tone), and a Done button carrying [data-initial-focus] (the non-destructive default — Enter confirms the edit, never deletes).
  </action>
  <verify>
    <automated>npm run build && npx eslint src/routes/review/ReviewNoteDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/review/ReviewNoteDialog.tsx contains `export function ReviewNoteDialog(`, imports saveNote and deleteNote from ../../persistence/notesStore, and contains NO useHighlightOverlay import
  - The single commit path handles empty-text → deleteNote and non-empty → saveNote, and is invoked from both the Done onClick and the close listener
  - The open path captures triggerRef BEFORE showModal and the close listener restores focus to triggerRef.current
  - [data-initial-focus] sits on the Done button; textarea value renders as a React text child (no dangerouslySetInnerHTML anywhere in the file)
  - npm run build exits 0
  </acceptance_criteria>
  <done>The note-edit dialog exists as a self-contained clone with the settled dialog/focus/commit discipline, callable from any row including orphans.</done>
</task>

<task type="auto">
  <name>Task 2: DeleteHighlightConfirm — structural clone of RemoveConfirm + ReviewView curation wiring</name>
  <files>src/routes/review/DeleteHighlightConfirm.tsx, src/routes/review/ReviewView.tsx</files>
  <read_first>
  - src/ingestion/library/RemoveConfirm.tsx (the clone source in full: props interface L24–35, open/close sync + [data-initial-focus] focus L50–73, the destructive handler L87–102, alertdialog markup L109–149)
  - src/persistence/highlightsStore.ts (deleteHighlight L105–110 — the transaction cascade-deletes highlight AND its notes: ONE call, one rollback unit)
  - src/routes/review/ReviewView.tsx (the 10-02 surface being wired: row anatomy, refreshKey state, .status region)
  - src/routes/review/ReviewNoteDialog.tsx (shipped by Task 1 — wire it)
  </read_first>
  <action>
  1. Create src/routes/review/DeleteHighlightConfirm.tsx exporting function DeleteHighlightConfirm. Props: { open: boolean; highlightId: string; excerpt: string; onConfirm: () => void; onCancel: () => void } (excerpt is informational context for the copy). Clone RemoveConfirm structurally: dialog role="alertdialog" aria-modal="true" aria-labelledby aria-describedby; open/close sync effect with triggerRef capture → showModal → focus [data-initial-focus]; close listener restoring trigger focus. Destructive handler onDestructiveClick: try await deleteHighlight(highlightId) catch (still proceed to close so the reader is never stranded — the row's presence/absence after refresh is the truth) then onConfirm(). deleteHighlight appears ONLY in this handler (single grep-verifiable call site in the review feature). Copy states the cascade honestly per D10-12: removing the highlight also removes the note attached to it (calm voice; exact string agent discretion — name the consequence, mirror RemoveConfirm's "will also be removed" register). [data-initial-focus] sits on the CANCEL button (non-destructive default — accidental Enter cannot delete; Pitfall 8).
  2. Wire ReviewView: each row gains two affordances — "Edit note" (opens ReviewNoteDialog for that row's highlight; passes the row's note as existing, or null) and "Remove highlight" (opens DeleteHighlightConfirm with the row's excerpt as context). Row state: noteTarget / removeTarget (the ReviewEntry under action, null when closed). onDone from the note dialog and onConfirm from the delete confirm both: clear the target, bump refreshKey (Pitfall 6 — no stale rows), and write a calm announcement into .status — delete: "Highlight removed." (D10-12 exact copy); note edit: a short calm confirmation (agent discretion, e.g. "Note saved."). onCancel closes only. Buttons get accessible names including the highlight context where feasible (aria-label carrying the quote excerpt prefix) so screen-reader rows are distinguishable.
  3. No bulk actions, no export button, no search-within-quotes (deferred items — must NOT appear).
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/review/DeleteHighlightConfirm.tsx contains `export function DeleteHighlightConfirm(`, role="alertdialog", a destructive handler awaiting deleteHighlight, and NO dexieLibrarySource import
  - The cascade consequence (note removed with the highlight) is stated in the dialog body copy; [data-initial-focus] is on the cancel button
  - src/routes/review/ReviewView.tsx renders Edit note + Remove affordances per row, wires both dialogs, and its onDone/onConfirm handlers call setRefreshKey and announce via the .status region ("Highlight removed." present verbatim)
  - Repo grep: deleteHighlight call sites in the review feature resolve ONLY to DeleteHighlightConfirm; saveNote/deleteNote only to ReviewNoteDialog
  - npm run build exits 0; unit suite green
  </acceptance_criteria>
  <done>Curation is fully wired: every row (orphans included) is note-editable and highlight-deletable in place, with honest cascade copy, safe focus defaults, announcements, and immediate re-derivation.</done>
</task>

<task type="auto">
  <name>Task 3: curate.spec.ts — edit/delete/announce/re-derive coverage</name>
  <files>tests/e2e/review-panel/curate.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/curate.spec.ts (the 10-01 sentinel to replace)
  - tests/e2e/portability/_portability.ts (seedRows/makeArticle/confidentHighlightOn/highlightRow + how to seed a NoteRecord row alongside)
  - tests/e2e/annotations/_fixtures.ts (wipeDatabase)
  - src/routes/review/ReviewView.tsx + ReviewNoteDialog.tsx + DeleteHighlightConfirm.tsx (shipped markup: button names, dialog labels, .status copy)
  - tests/e2e/library/remove-cascade.spec.ts or the Phase 8 remove e2e (the confirm-dialog interaction precedent — proceed/cancel button querying)
  </read_first>
  <action>
  Replace the sentinel. Setup: beforeEach wipeDatabase; seed one article with a confident highlight carrying a NoteRecord, plus one orphan highlight (articleId "ghost-article") — orphans are the D10-11 edge. Tests:
  1. Edit note in place: open #/review, activate the confident row's Edit note → dialog opens with existing text focused; replace the text; activate Done → dialog closes, the row's note preview shows the new text WITHOUT page reload (refreshKey re-derivation), and a reload still shows it (persistence proof).
  2. Add note to a note-less row: open Edit note on the orphan row (D10-11 — no article needed), type text, Done → orphan row gains the note preview; reload persists.
  3. Empty-text deletes the note (D5-10): open Edit note on a noted row, clear the textarea, Done → the note preview is gone from the row without reload.
  4. Esc does not lose edits (Pitfall 7): open Edit note, type new text, press Escape → the commit path still ran (note preview updated) — asserts every close path routes through one cleanup.
  5. Delete with confirm: activate Remove highlight → alertdialog visible with copy naming the note consequence; initial focus is on the CANCEL button (assert document.activeElement); Cancel → dialog closes, row remains.
  6. Delete proceeds: re-open, activate the confirm/Proceed control → dialog closes, the row (and its note preview) is gone without reload, and the .status region announces "Highlight removed."; a reload keeps it gone (Dexie truth).
  Selector discipline: dialogs via getByRole("alertdialog") / getByRole("dialog"); buttons by accessible name; .status text via the role="status" region.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/curate.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - Spec covers: edit-in-place + persistence, orphan note add, empty-text note delete, Esc-close commit, cancel-keeps-row + initial-focus-on-cancel, proceed removes row + note + announces "Highlight removed." in the status region, no-reload re-derivation throughout
  - No page.reload() used as the mechanism to see updates mid-test (reload only as persistence double-check)
  - npx playwright test tests/e2e/review-panel/curate.spec.ts exits 0 across chromium/firefox/webkit (18 cells)
  </acceptance_criteria>
  <done>RECV-01.f proven in real browsers: calm, honest, atomic in-place curation with safe focus defaults and stale-free UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored text → dialog content | Note text and quote excerpts (possibly from imported bundles) render inside both dialogs |
| destructive writes → Dexie | deleteHighlight / deleteNote / saveNote are the only mutation paths the panel opens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05a | Tampering (stored XSS) | dialog text rendering | mitigate | Textarea value + excerpt rendered as React text children; react/no-danger enforced; no raw HTML paths |
| T-10-05b | Tampering (accidental destructive fire) | DeleteHighlightConfirm | mitigate | [data-initial-focus] on the CANCEL button; deleteHighlight ONLY inside the destructive button onClick (never catch blocks/effects); alertdialog semantics give the reader an explicit confirm step |
| T-10-05c | Repudiation (surprise data loss) | note cascade on delete | mitigate | Dialog copy explicitly states the attached note is removed with the highlight; deleteHighlight's Dexie transaction makes highlight+note removal atomic (no orphaned half-state) |
| T-10-05d | Tampering | npm installs | accept | Zero packages installed |
</threat_model>

<verification>
- npm run build + npm run test:unit -- --run — type-safe, no regressions
- npx playwright test tests/e2e/review-panel/curate.spec.ts — curation matrix, 3 engines
- Repo grep discipline: single call sites for deleteHighlight (DeleteHighlightConfirm), saveNote/deleteNote (ReviewNoteDialog) within the review feature
- npm run lint + npm run lint:no-danger — clean
</verification>

<success_criteria>
- RECV-01.f proven: edit note in place (incl. orphan notes), delete highlight with honest cascade copy, .status announcements, refreshKey re-derivation without reload
- Both dialogs are structural clones (no shared abstraction, no context-bound imports) — the 08-04/09-05 Pitfall 8 lineage holds
- No bulk actions / export-from-panel / quote-search features appear (deferred list honored)
</success_criteria>

## Artifacts this plan produces

- `ReviewNoteDialog` component — new export of `src/routes/review/ReviewNoteDialog.tsx` (new file; props: open, highlightId, articleId, existing, onDone)
- `DeleteHighlightConfirm` component — new export of `src/routes/review/DeleteHighlightConfirm.tsx` (new file; props: open, highlightId, excerpt, onConfirm, onCancel)
- ReviewView additions: noteTarget/removeTarget row state, Edit note + Remove highlight affordances, .status curation announcements ("Highlight removed."), refreshKey wiring
- Strengthened `tests/e2e/review-panel/curate.spec.ts`

<output>
Create `.planning/phases/10-annotation-review-panel/10-05-SUMMARY.md` when done
</output>
