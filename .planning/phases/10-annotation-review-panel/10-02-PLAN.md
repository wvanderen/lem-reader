---
phase: 10-annotation-review-panel
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/routes/review/ReviewView.tsx
  - src/App.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/app.css
  - tests/component/App.test.tsx
  - tests/e2e/review-panel/route-entry.spec.ts
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "#/review renders the ReviewView with its own h1, a .status live region (role=status aria-live=polite aria-atomic), and skip-link parity via main#main (D10-01, one-h1-per-page)"
    - "A 'Review highlights' button in the LibraryView header cluster navigates to #/review — the only Phase-10 entry point (D10-02)"
    - "parseHash maps #/article/<id>/h/<hid> to the article view carrying jumpHighlightId, #/review to the review view, unknown #/ routes to the list, and never swaps views for fragment-only hashes — the Gap 3 guard stays byte-unchanged (D10-01, D10-03)"
    - "The list renders grouped-by-article sections (title + subtle source host) with rows showing quote excerpt, note preview when present, short date, and a tri-state badge ONLY on ambiguous/orphan rows, plus the 'no badge' legend line (D10-04, D10-05, D10-06, D10-07)"
    - "Confident rows jump via whole-row button to #/article/<id>/h/<hid>; orphan and unresolved rows render no jump affordance (D10-03)"
    - "Empty states are distinct and honest via .status: no highlights at all vs filters matched zero (D10-10)"
    - "Filter row = TagFilter chips + article select + confidence select (All/Confident/Ambiguous/Orphan), AND-composed; sort select defaults to Date (D10-08)"
  artifacts:
    - path: "src/routes/review/ReviewView.tsx"
      provides: "The #/review route view (LibraryView twin: load effect, filter row, grouped list, orphan tail, empty states)"
      exports: ["ReviewView"]
      min_lines: 200
    - path: "src/App.tsx"
      provides: "Three-view hash router + /h/ deep-link grammar"
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "Review highlights entry button in header cluster"
    - path: "src/app.css"
      provides: "Additive .review-* selectors (tokens-only, no transition/animation properties)"
  key_links:
    - from: "src/App.tsx"
      to: "src/routes/review/ReviewView.tsx"
      via: "view swap renders ReviewView when view.name === review"
      pattern: "ReviewView"
    - from: "src/routes/review/ReviewView.tsx"
      to: "src/routes/review/reviewFilter.ts"
      via: "pure derived sections in render body — no effect chains (D10-09)"
      pattern: "deriveReviewSections"
    - from: "src/routes/review/ReviewView.tsx"
      to: "src/ingestion/library/TagFilter.tsx"
      via: "tag chips reused as-is (D10-08)"
      pattern: "TagFilter"
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "#/review"
      via: "Review highlights button sets window.location.hash = #/review"
      pattern: "Review highlights"
---

<objective>
Ship the review panel surface (RECV-01): the third hash route #/review rendering the cross-article ReviewView, the extended route grammar with the /h/<highlightId> deep-link capture, and the LibraryView entry point.

Purpose: D10-01 makes the panel a dedicated route (not a modal) so browser-back works for free; D10-04–D10-07 fix the grouped list vocabulary (the interactive twin of the Phase 9 highlights Markdown export); D10-10 keeps empty states honest. This plan composes the 10-01 derivation module with the shipped load/filter/live-region patterns — no new logic.

Output: src/routes/review/ReviewView.tsx, App.tsx three-view router, LibraryView entry button, additive app.css block, extended App.test.tsx, strengthened route-entry e2e.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md
@.planning/phases/10-annotation-review-panel/10-01-SUMMARY.md

Analog sources (verbatim excerpts in 10-PATTERNS.md):
@src/ingestion/library/LibraryView.tsx
@src/ingestion/library/TagFilter.tsx
@src/reader/annotations/AnnotationsDrawer.tsx
@src/portability/markdown.ts
@src/App.tsx
@src/routes/ArticleView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewView route component + additive app.css block</name>
  <files>src/routes/review/ReviewView.tsx, src/app.css</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (the route-view twin: cancelled-flag Promise.all load effect keyed on refreshKey L66–97, .status live region L112–123, filter row wiring, empty-state branches L104–138)
  - src/routes/review/reviewFilter.ts (deriveReviewSections + types shipped by 10-01)
  - src/reader/annotations/AnnotationsDrawer.tsx (row anatomy: drawer-entry button pattern, quote excerpt, note preview truncation limits, disabled-when-unresolved rule L184–189)
  - src/ingestion/library/TagFilter.tsx (props: tags, activeTag, onSelect — reuse as-is)
  - src/portability/markdown.ts (UNMATCHED_SECTION_HEADING L129 "Highlights without an article")
  - src/routes/ArticleView.tsx L106–115 (formatDate — mirror with dateStyle "short")
  - src/persistence/highlightsStore.ts and src/persistence/notesStore.ts (loadAllHighlights / loadAllNotes signatures)
  - src/ingestion/library/tagsStore.ts (loadAllTags)
  - src/content/repository.ts (listArticles)
  - src/app.css (the .library-* / .status / dialog sections named in 10-PATTERNS.md — additive rule at the L1760 section comment)
  </read_first>
  <action>
  1. Create src/routes/review/ReviewView.tsx exporting function ReviewView() with no props. State: status ("loading" | "ready" | "error"), articles, highlights, notes, allTags, filters ({ tag: string | null; articleId: string | null; confidence: ConfidenceFilter } defaulting to null/null/"all"), sort (ReviewSort, default "date" per D10-08), refreshKey.
  2. Load effect keyed on [refreshKey]: cancelled-flag + Promise.all([listArticles(), loadAllHighlights(), loadAllNotes(), loadAllTags()]) — on rejection set status "error" with calm DOC-06 copy through .status (mirror LibraryView's error branch wording discipline). NO new store code.
  3. Derived in the render body (D10-09 — pure call, no effect chains): const derivation = deriveReviewSections(articles, highlights, notes, filters, sort).
  4. Markup mirrors LibraryView's page shape: main#main (skip-link parity), one h1 "Review highlights" (one-h1-per-page), then the .status div (role=status aria-live=polite aria-atomic=true) carrying loading/error text AND both D10-10 empty-state copies: when highlights.length === 0 and status "ready" → "No highlights yet. Highlights you make while reading appear here."; when highlights.length > 0 but derivation.sections and derivation.orphanEntries are both empty → "No highlights match these filters."
  5. Filter row (D10-08): TagFilter with tags=allTags activeTag=filters.tag onSelect updating filters; an article select (label + options from loaded articles ordered by provenance.title, plus an "All articles" default option) bound to filters.articleId; a confidence select labeled for screen readers with options All / Confident / Ambiguous / Orphan bound to filters.confidence; a sort select with options Date / Article / Position bound to sort (Date default). Legend line under the filter row: "No badge means anchored confidently." (D10-07).
  6. Sections: for each ReviewSection an h2 with the article provenance.title and a subtle source-host suffix when the article carries source metadata (derive the host from provenance sourceUrl when present — reuse how SourceBadge or the markdown export surfaces it; fixture articles show no host). Rows inside a ul: each li carries a whole-row jump button (class review-row, the AnnotationsDrawer drawer-entry pattern) enabled ONLY for entries with status "confident" AND an article — onClick sets window.location.hash to the template-built hash for that article id and highlight id (D10-03; ambiguous/orphan rows render the button disabled with aria-disabled, mirroring AnnotationsDrawer L184–189). Row content: quote excerpt in blockquote styling (drawer truncation limits), note preview line when entry.note exists (truncated), short date via new Intl.DateTimeFormat(navigator.language, { dateStyle: "short" }) with try/catch fallback to the raw ISO string (ArticleView L106–115 shape), and a badge span ONLY when status is "ambiguous" or "orphan" (D10-07) with text "Uncertain anchor" / "Article missing" (calm copy — exact strings are agent discretion but must be distinct and announced as row content).
  7. Orphan tail (D10-05): after article sections, when derivation.orphanEntries is non-empty, an h2 titled exactly "Highlights without an article" (the markdown.ts UNMATCHED_SECTION_HEADING vocabulary) followed by the same row anatomy — no jump affordance at all on orphan rows (curatable, filterable via confidence=orphan; first-class rows).
  8. Curation affordances are NOT in this task — Plan 10-05 adds edit-note and delete buttons plus dialog wiring to this component.
  9. src/app.css: one additive block of .review-* class hooks (review-header, review-filter-row, review-legend, review-section list/rows, review-quote, review-note-preview, review-badge, review-badge-ambiguous, review-badge-orphan, review-date). Tokens-only (every color via var(--…) — the Phase 9 audit bar: no new hex literals outside :root). NO transition or animation properties anywhere in the new block (global reduced-motion gate). TagFilter chips reuse existing .tag-chip styles untouched.
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/review/ReviewView.tsx contains `export function ReviewView(`, imports deriveReviewSections from ./review/reviewFilter, TagFilter from ../../ingestion/library/TagFilter, loadAllHighlights/loadAllNotes/listArticles/loadAllTags
  - ReviewView contains role="status" with aria-live="polite" and aria-atomic="true", an h1, and section h2s; the orphan group heading text is "Highlights without an article"
  - The confident-row jump handler assigns window.location.hash from article id + highlight record id (template-built); ambiguous/orphan rows are not jumpable
  - src/app.css new selectors all start with .review- and reference only var(--…) colors; the new block contains no transition: or animation: declarations (verify: grep -n "transition\|animation" over the added lines returns nothing)
  - npm run build exits 0 (tsc clean); full unit suite green
  </acceptance_criteria>
  <done>The #/review surface exists as a component: whole-library load, pure derivation, grouped sections with orphan tail, badge discipline, honest empty states, filter/sort controls — ready for the router to mount it.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx three-view router + parseHash tests + LibraryView entry button + route-entry e2e</name>
  <files>src/App.tsx, tests/component/App.test.tsx, src/ingestion/library/LibraryView.tsx, tests/e2e/review-panel/route-entry.spec.ts</files>
  <read_first>
  - src/App.tsx (View union L32, parseHash L34–37, Gap 3 guard onHash L146–150 — must stay byte-unchanged, [view] reset effect L157–160, view swap L192–202, parseHash export L215)
  - tests/component/App.test.tsx (existing parseHash describe L51–73 — strengthen-only: every existing case stays)
  - src/ingestion/library/LibraryView.tsx (header cluster where IngestControl mounts L108–110; the window.location.hash = "#/" fallback precedent L177)
  - tests/e2e/review-panel/route-entry.spec.ts (the 10-01 sentinel to replace)
  - tests/e2e/annotations/_fixtures.ts (BASE, wipeDatabase)
  </read_first>
  <action>
  1. src/App.tsx (extend, don't rewrite — per D10-01/D10-03 and research Pattern 1): extend the View union to { name: "list" } | { name: "article"; id: string; jumpHighlightId?: string } | { name: "review" }. Extend parseHash: first exec /^#\/article\/([a-z0-9-]+)(?:\/h\/([^/]+))?$/ on window.location.hash — on match return { name: "article", id: m[1], jumpHighlightId: m[2] } (the [^/]+ capture is deliberate: highlight ids from imported bundles are foreign-controlled strings, used only for Array.find/getElementById lookups, never innerHTML — research Pitfall 4); then an exact equality check for "#/review" returning { name: "review" }; else { name: "list" } (byte-stable fallback). The onHash Gap 3 guard (fragment-only hashes must not swap views) stays byte-unchanged. View swap gains the review branch rendering ReviewView — order the ternary/branches: list → review → article, and pass NO new props to ArticleView yet (jumpHighlightId consumption ships in Plan 10-03; passing it now would be a tsc error against ArticleView's current props). The [view] reset effect is untouched (its firing on review↔article swaps is desirable — drawer/count reset; panel state re-derives from Dexie on mount by design).
  2. tests/component/App.test.tsx — extend the parseHash describe, strengthen-only (every existing case remains and stays green). New cases: hash "#/article/a-one/h/hl-123" → { name: "article", id: "a-one", jumpHighlightId: "hl-123" }; hash "#/article/a-one/h/" (trailing slash, empty id) → { name: "list" } (documents that the optional group fails and falls through); hash "#/review" → { name: "review" }; hash "#/review/x" → { name: "list" } (unknown). Keep the existing plain-article, empty-hash, bare-#/ , unknown-route, and Gap 3 fragment cases exactly as they are.
  3. src/ingestion/library/LibraryView.tsx (D10-02 — minimal diff, nothing else in this file changes): add a "Review highlights" button in the header cluster alongside IngestControl, using the same button styling tokens as existing header controls (reuse the class used by sibling header buttons), onClick sets window.location.hash = "#/review".
  4. Strengthen tests/e2e/review-panel/route-entry.spec.ts (replace the sentinel, keep the file): beforeEach wipeDatabase; test (a) goto BASE + click the "Review highlights" button → h1 "Review highlights" visible and URL hash is #/review; test (b) direct goto BASE#/review → same h1 (deep-linkable); test (c) goto BASE#/review, then browser-back → library h1 "Saved articles" returns (history discipline); test (d) the LibraryView still shows its own h1 after the route swap back (view swap does not duplicate mains); test (e) header gating on the third view (research Pitfall 10 / Open Question 3 resolution): on #/review the Header annotations-trigger does not render — App.tsx passes articleMounted={view.name === "article"} so the Header L98 conditional is false outside the article view — assert page.getByRole("button", { name: /Highlights and notes/ }) has count 0 (the trigger is conditionally rendered, not CSS-hidden; regex name because the aria-label appends a formatted count when highlights exist). Header.tsx itself stays byte-stable — this test PINS existing gating, it does not drive changes. Selector discipline: query by role/name (getByRole("button", { name: "Review highlights" }), getByRole("heading", { level: 1 })).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/component/App.test.tsx && npx playwright test tests/e2e/review-panel/route-entry.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/App.tsx parseHash matches the /h/ suffix form first, then #/review, then falls to list; the Gap 3 guard block (hash !== "" && !hash.startsWith("#/")) is byte-unchanged from before this task
  - App.tsx renders ReviewView for the review view and passes no jumpHighlightId prop to ArticleView yet
  - tests/component/App.test.tsx contains new cases for the /h/ grammar (with and without a trailing empty id), #/review, and #/review/x → list, and all pre-existing parseHash cases still pass (strengthen-only)
  - LibraryView renders a "Review highlights" button whose handler assigns the #/review hash; no other LibraryView behavior changed
  - route-entry.spec includes the #/review header-gating test: the annotations-trigger (name matching /Highlights and notes/) has count 0 on #/review, pinning the articleMounted gating with Header.tsx byte-stable
  - npx playwright test tests/e2e/review-panel/route-entry.spec.ts exits 0 across chromium/firefox/webkit (15 cells)
  </acceptance_criteria>
  <done>#/review is a real route with a working entry point and grammar-tested parser; browser-back from #/review returns to the library; every pre-existing route behavior is regression-proven; Header chrome stays article-gated on the third view (byte-stable Header, pinned by test (e)).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → route parser | #/article/<id>/h/<highlightId> is a public input surface; highlightId may be any hand-typed or foreign-bundle string |
| stored text → DOM | highlight quotes and note text (possibly imported) render inside review rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02a | Tampering | parseHash /h/ capture | mitigate | Regex-anchored grammar; [^/]+ capture is lookup-only (Array.find + getElementById prefix concat in 10-03); never innerHTML; unknown forms fall to list |
| T-10-02b | Tampering (stored XSS) | ReviewView row rendering | mitigate | All quote/note/title text as React text children (escaping by default); react/no-danger + lint:no-danger enforced; no dangerouslySetInnerHTML anywhere |
| T-10-02c | Tampering | hash assignment in jump/button handlers | mitigate | Hash values template-built from validated record ids only; hashchange consumers re-parse through the same regex |
| T-10-02d | Spoofing/Tampering | npm installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- npm run test:unit -- --run tests/component/App.test.tsx — grammar matrix green, existing cases green
- npx playwright test tests/e2e/review-panel/route-entry.spec.ts — entry/route/back/header-gating across 3 engines
- npm run build — tsc clean with the three-view union
- npm run test:unit -- --run — no unit regressions
</verification>

<success_criteria>
- RECV-01.a proven: #/review route swap + LibraryView entry button (e2e, 3 engines)
- RECV-01.h proven: parseHash /h/ + #/review + unknown→list + Gap 3 fragment cases (component tests, strengthen-only)
- ReviewView renders grouped sections, orphan tail, badge-only-when-unresolved discipline, legend, honest empty states, filter/sort controls (visually asserted in 10-04 specs)
- No Header.tsx changes (research Pitfall 10; the annotations-trigger gating on #/review is now e2e-asserted per the Open Question 3 resolution); Gap 3 guard byte-unchanged
</success_criteria>

## Artifacts this plan produces

- `ReviewView` component — new export of `src/routes/review/ReviewView.tsx` (new file)
- `View` union third alternative `{ name: "review" }` + `jumpHighlightId?: string` field on the article variant; extended `parseHash` regex — `src/App.tsx`
- "Review highlights" header-cluster button + `#/review` hash navigation — `src/ingestion/library/LibraryView.tsx`
- `.review-*` CSS class family (review-header, review-filter-row, review-legend, review-section, review-row, review-quote, review-note-preview, review-badge, review-badge-ambiguous, review-badge-orphan, review-date) — `src/app.css` additive block
- New parseHash test cases in `tests/component/App.test.tsx`; strengthened `tests/e2e/review-panel/route-entry.spec.ts`

<output>
Create `.planning/phases/10-annotation-review-panel/10-02-SUMMARY.md` when done
</output>
