---
phase: 05-durable-highlights-and-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/normalizeText.ts
  - src/content/schema.ts
  - src/persistence/db.ts
  - src/persistence/highlightsStore.ts
  - src/persistence/notesStore.ts
  - src/annotations/resolution.ts
  - src/annotations/capture.ts
  - src/annotations/overlap.ts
  - src/annotations/highlightRanges.ts
  - tests/unit/annotations/resolve-quote-selector.test.ts
  - tests/unit/annotations/capture-offset-mapping.test.ts
  - tests/unit/annotations/selector-roundtrip.test.ts
  - tests/unit/annotations/overlap.test.ts
  - tests/unit/annotations/highlight-schema.test.ts
  - tests/unit/annotations/highlights-store-error.test.ts
autonomous: true
requirements: [ANNO-05, ANNO-06, ANNO-07, STATE-03, STATE-04]
user_setup: []

must_haves:
  truths:
    - "resolveQuoteSelector re-resolves a stored TextQuoteSelector to the SAME TextPositionSelector on the same revision (the ANNO-05/ANNO-06 round-trip invariant)"
    - "resolveQuoteSelector returns 'ambiguous' when the exact text appears N>1 times after prefix/suffix disambiguation, and 'orphan' when zero exact + no confident prefix/suffix fallback (ANNO-07 tri-state contract)"
    - "captureSelection converts an ephemeral DOM Range into a TextPositionSelector over the D-05 grapheme substrate, accounting for whitespace-collapse divergence, and rejects multi-block / empty / ineligible selections with a typed reason"
    - "HighlightRecord and NoteRecord are Zod-validated at the persistence boundary; a corrupt record is rejected, not silently coerced (STATE-04)"
    - "highlightsStore loads all highlights for an articleId across revisions via the [articleId+revision] compound index and never throws to the reader (STATE-05)"
    - "deleteHighlight atomically cascade-deletes a highlight and its note via a Dexie transaction (D5-12)"
  artifacts:
    - path: "src/content/normalizeText.ts"
      provides: "resolveQuoteSelector implementation (fills the Phase 1 stub at L149-152)"
      contains: "export function resolveQuoteSelector"
    - path: "src/annotations/resolution.ts"
      provides: "Pure re-anchoring helpers (findAllOccurrences, matchesContext) used by resolveQuoteSelector"
    - path: "src/annotations/capture.ts"
      provides: "captureSelection(article, readingRoot) -> CaptureResult + DOM-offset→grapheme-offset mapping"
      exports: ["captureSelection", "CaptureResult"]
    - path: "src/annotations/overlap.ts"
      provides: "rangesOverlap disjoint-range check (D5-13)"
      exports: ["rangesOverlap"]
    - path: "src/annotations/highlightRanges.ts"
      provides: "Block↔highlight intersection + run-slice computation reusing splitParagraphRuns"
    - path: "src/content/schema.ts"
      provides: "HighlightRecordSchema, NoteRecordSchema, TextPositionSelectorSchema, TextQuoteSelectorSchema"
      contains: "HighlightRecordSchema"
    - path: "src/persistence/highlightsStore.ts"
      provides: "loadHighlights, saveHighlight, deleteHighlight (mirrors locationStore.ts)"
      exports: ["loadHighlights", "saveHighlight", "deleteHighlight", "HighlightsLoadResult"]
    - path: "src/persistence/notesStore.ts"
      provides: "loadNote, saveNote (mirrors locationStore.ts/settingsStore.ts)"
      exports: ["loadNote", "saveNote"]
    - path: "tests/unit/annotations/resolve-quote-selector.test.ts"
      provides: "ANNO-07 tri-state proof across simulated content edits"
    - path: "tests/unit/annotations/capture-offset-mapping.test.ts"
      provides: "DOM Range → grapheme offset mapping with whitespace-collapse correction"
    - path: "tests/unit/annotations/selector-roundtrip.test.ts"
      provides: "offset → deriveQuoteSelector → resolveQuoteSelector → offset === original"
    - path: "tests/unit/annotations/overlap.test.ts"
      provides: "D5-13 disjoint-range rejection"
    - path: "tests/unit/annotations/highlight-schema.test.ts"
      provides: "STATE-04 Zod boundary validation + corrupt-record rejection"
    - path: "tests/unit/annotations/highlights-store-error.test.ts"
      provides: "STATE-05 classifyStorageError routing"
  key_links:
    - from: "src/content/normalizeText.ts"
      to: "src/annotations/resolution.ts"
      via: "resolveQuoteSelector delegates substring/context matching to pure helpers in resolution.ts"
      pattern: "import.*from.*annotations/resolution"
    - from: "src/persistence/highlightsStore.ts"
      to: "src/content/schema.ts"
      via: "HighlightRecordSchema.safeParse on every read row"
      pattern: "HighlightRecordSchema\\.safeParse"
    - from: "src/persistence/highlightsStore.ts"
      to: "src/persistence/db.ts"
      via: "db.highlights.where('[articleId+revision]').between(...) compound-index range query"
      pattern: "\\[articleId\\+revision\\]"
    - from: "src/annotations/capture.ts"
      to: "src/content/normalizeText.ts"
      via: "imports normalizeRunText/graphemeClusters/blockNormalizedText (Pattern 5 — never fork normalization)"
      pattern: "from.*content/normalizeText"
---

## Phase Goal

**As a** reader, **I want to** create, revisit, and manage local highlights and notes, **so that** they remain attached to their intended normalized text across every view change.

> This plan (05-01) delivers the **annotation anchor engine + persistence seams + Wave 0 unit tests** — the pure-logic foundation every UI slice in Plans 05-02..05-05 builds on. It proves the ANNO-05/06 round-trip invariant (`offset → quote → offset`) and the ANNO-07 tri-state contract (`confident | ambiguous | orphan`) as pure functions, plus the STATE-03/04/05 persistence boundary. No UI ships here; the "happy path" is a green unit-test suite.

<objective>
Implement the durable-anchor domain layer: fill the stubbed `resolveQuoteSelector` (D5-02 exact-first / prefix-suffix-disambiguate / orphan-fallback algorithm), build `captureSelection` (DOM Range → D-05 grapheme offset with whitespace-collapse correction), the disjoint-range check, the block↔highlight intersection math, the `HighlightRecord`/`NoteRecord` Zod schemas, and the Dexie persistence seams. Add the 6 Wave 0 unit-test files proving the round-trip invariant, the tri-state resolution, capture offset mapping, overlap rejection, schema boundary, and STATE-05 error routing.

Purpose: Every UI slice (capture toolbar, `<mark>` overlay, note popover, drawer, navigate-back, ambiguous/orphan surfacing) consumes these pure functions + persistence seams. Proving them first — in jsdom, fast — means the Playwright-heavy UI validation in Plan 05-05 isolates rendering/interaction correctness from anchor-math correctness.
Output: 4 new pure-logic modules under `src/annotations/`, 2 new persistence seams, 3 modified files (normalizeText/schema/db), 6 unit-test files.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durable-highlights-and-notes/05-CONTEXT.md
@.planning/phases/05-durable-highlights-and-notes/05-RESEARCH.md
@.planning/phases/05-durable-highlights-and-notes/05-PATTERNS.md
@src/content/normalizeText.ts
@src/content/schema.ts
@src/persistence/db.ts
@src/persistence/locationStore.ts
@src/pagination/anchor.ts
@src/pagination/splitBlock.ts
@src/pagination/fragmentRenderer.tsx
@tests/unit/selectors.test.ts
@tests/unit/restoreLocation.test.ts
@tests/unit/storageFallback.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor engine — resolveQuoteSelector + capture + overlap + highlightRanges + pure-logic unit tests</name>
  <files>
src/content/normalizeText.ts,
src/annotations/resolution.ts,
src/annotations/capture.ts,
src/annotations/overlap.ts,
src/annotations/highlightRanges.ts,
tests/unit/annotations/resolve-quote-selector.test.ts,
tests/unit/annotations/capture-offset-mapping.test.ts,
tests/unit/annotations/selector-roundtrip.test.ts,
tests/unit/annotations/overlap.test.ts
  </files>
  <behavior>
    - resolveQuoteSelector: same-revision unique-exact match returns the original TextPositionSelector (start/end byte-identical).
    - resolveQuoteSelector: exact text appearing N>1 times after prefix+suffix disambiguation still N>1 → returns "ambiguous".
    - resolveQuoteSelector: zero exact matches + zero confident prefix/suffix fallback → returns "orphan".
    - resolveQuoteSelector: zero exact + exactly one prefix/suffix candidate near the positionHint → returns a confident TextPositionSelector (low-certainty path; renders as normal per Open Question #3).
    - captureSelection: a DOM Range inside a single eligible block maps to the correct intra-block grapheme offsets even when DOM textContent has whitespace that normalizeRunText would collapse (the load-bearing whitespace-correction case).
    - captureSelection: multi-block selection → { ok:false, reason:"multi-block" }; empty/collapsed → { ok:false, reason:"empty" }; selection rooted in an ineligible block → { ok:false, reason:"ineligible" }.
    - rangesOverlap: [10,20) vs [20,30) → false (end-exclusive, touch is not overlap); [10,20) vs [15,25) → true.
    - highlightRanges: a highlight at intra-block [5,12) over a paragraph's InlineRun[] slices into before/highlighted/after runs preserving inline marks (a link straddling the boundary becomes two link runs).
    - selector-roundtrip: for each fixture in a small synthetic set, deriveQuoteSelector(article, p) → resolveQuoteSelector(article, q, p) === p (the ANNO-05 invariant).
  </behavior>
  <read_first>
src/content/normalizeText.ts (the file being modified — read in full; the deriveQuoteSelector sibling at L133-147 and the stubbed contract at L149-152 are the in-file analog),
src/pagination/anchor.ts (read in full — the pure-logic discipline template: header comment citing locked decisions, "Pure domain logic — no DOM, no React, no side effects," REUSE-DO-NOT-FORK warning),
src/reader/restoreLocation.ts (read in full — capture.ts is the REVERSE direction; reuse its normalizeElText/findScrollTarget offset-mapping discipline and its TreeWalker SHOW_TEXT walk),
src/pagination/splitBlock.ts (read splitParagraphRuns L186-221 — highlightRanges reuses it verbatim to slice runs at highlight boundaries; do NOT reimplement run slicing),
src/pagination/fragmentRenderer.tsx (read resolveBlockSlice L98-131 — the range-intersection math overlap.ts + highlightRanges.ts mirror),
tests/unit/selectors.test.ts (read in full — copy its parseArticle helper + baseArticle fixture + describe/it structure verbatim for the new selector tests),
tests/unit/restoreLocation.test.ts (read its makeBlock HTMLElement-stub factory — copy verbatim for capture-offset-mapping.test.ts; jsdom is sufficient for the pure mapping logic),
05-RESEARCH.md §Pattern 1 (capture), §Pattern 2 (resolveQuoteSelector), §Code Examples "Selector Round-Trip" and "Cross-Fragment Highlight Slicing",
05-PATTERNS.md §`src/annotations/capture.ts`, §`src/annotations/resolution.ts`, §`src/annotations/overlap.ts`, §`src/annotations/highlightRanges.ts`, §`src/content/normalizeText.ts`
  </read_first>
  <action>
Implement the D5-02 re-anchoring algorithm as a PURE function. In `src/content/normalizeText.ts`, replace the stubbed comment block at L149-152 with an exported `resolveQuoteSelector(article: CanonicalArticle, selector: TextQuoteSelector, positionHint?: TextPositionSelector): TextPositionSelector | "ambiguous" | "orphan"`. Per D5-02: (1) find ALL exact-substring occurrences of `selector.exact` in `normalizeText(article)` via grapheme-cluster slicing (use the already-imported `graphemeClusters` + `normalizeText`); (2) unique exact → return `{ start, end }`; (3) N>1 exact → filter by `matchesContext` (prefix precedes + suffix follows each candidate, trimmed to the stored context radius) → still N>1 → return `"ambiguous"` (the positionHint is a DISPLAY tie-breaker only, never a silent re-attach — ANNO-07); (4) zero exact → prefix+suffix-only window scan, unique candidate near positionHint → return confident position, else `"orphan"`. NEVER return a position silently when the contract says ambiguous/orphan (Pitfall 7). Delegate substring-search + context-matching helpers to `src/annotations/resolution.ts` (`findAllOccurrences`, `matchesContext`) so normalizeText.ts stays focused; re-export `resolveQuoteSelector` from normalizeText.ts (the contract signature stays there per the Phase 1 stub). Cite D5-01/D5-02 in the header comment, mirror anchor.ts's "Pure domain logic — no DOM, no React, no side effects. jsdom-safe." discipline.

Create `src/annotations/capture.ts` with `export type CaptureResult = | { ok: true; blockIndex: number; position: TextPositionSelector } | { ok: false; reason: "empty" | "multi-block" | "ineligible" | "measurement-body" }` and `export function captureSelection(article: CanonicalArticle, readingRoot: HTMLElement): CaptureResult` per D5-06/D5-07/D5-08. The function reads `window.getSelection()`, bails on collapsed/empty (reason "empty"), finds the `[data-block-index]` ancestor of BOTH range endpoints via a `findBlockAncestor(node, root)` TreeWalker (different ancestors → reason "multi-block"; ancestor missing → "ineligible"), reads the `data-block-index` attribute → `blockIndex`, checks `isEligibleBlock(block)` for the broad set per D5-07 (paragraph, heading, blockquote children, list-item children, figure caption, code-block source, footnote-reference marker — exhaustive over block.kind, no default per Pattern F), then maps the DOM Range to intra-block grapheme offsets. The mapping (the load-bearing detail, Pitfall 1): walk the block's text nodes in document order (TreeWalker SHOW_TEXT) building a cumulative raw-char offset, simultaneously walk `blockNormalizedText(block)` grapheme clusters (reuse `graphemeClusters`, `normalizeRunText`, `blockNormalizedText` from normalizeText.ts — Pattern 5, NEVER fork, NEVER use `selection.toString()` whose whitespace serialization varies by engine per Pitfall 2), aligning non-whitespace characters and advancing the raw pointer without advancing the normalized pointer where `normalizeRunText` would collapse `[\t\n\f\r ]+` to a single space or trim. Add the block's article-global start offset (accumulate per-block `graphemeClusters(blockNormalizedText(block), lang).length` + `BLOCK_SEPARATOR.length` up to blockIndex — mirror `pageStartGlobalOffset` accumulation in anchor.ts). Return `{ ok: true, blockIndex, position: { start: blockGlobalStart + intraStart, end: blockGlobalStart + intraEnd } }`.

Create `src/annotations/overlap.ts` with `export function rangesOverlap(a: TextPositionSelector, b: TextPositionSelector): boolean` returning `Math.max(a.start, b.start) < Math.min(a.end, b.end)` (end-exclusive; touching ranges do NOT overlap). Pure function, no DOM. Per D5-13.

Create `src/annotations/highlightRanges.ts` exporting `sliceRunsForHighlights(runs: InlineRun[], blockGlobalStart: number, highlights: { id: string; position: TextPositionSelector; hasNote: boolean }[], lang: string)` which computes, for each highlight intersecting this block's article-global range, the intra-block [start,end) intersection, sorts intersections by start, and uses `splitParagraphRuns` (imported from `src/pagination/splitBlock.ts` — Pitfall 4, do NOT reimplement run slicing) at each boundary to produce a list of `{ runs: InlineRun[]; highlightId: string | null; hasNote: boolean }` slices the renderer (Plan 05-02) wraps in `<mark>`. Reuse the intersection math from fragmentRenderer.tsx's resolveBlockSlice pattern. Header comment cites D5-16 (cross-fragment intersection) + Pattern 7 (reuse splitParagraphRuns).

Write the 4 unit-test files FIRST (RED), then implement to GREEN (test-first since tdd_mode-style discipline benefits this pure I/O logic). `tests/unit/annotations/resolve-quote-selector.test.ts` copies `tests/unit/selectors.test.ts`'s `parseArticle` + `baseArticle` fixture structure; covers: unique-exact confident, N>1-after-disambiguation ambiguous, zero-exact orphan, zero-exact-prefix/suffix-unique confident, positionHint-ignored-when-unique-exact. `tests/unit/annotations/capture-offset-mapping.test.ts` copies `tests/unit/restoreLocation.test.ts`'s `makeBlock(text, kind)` HTMLElement-stub factory (jsdom provides textContent + dataset); covers single-block valid capture with whitespace-collapse correction, multi-block rejection, empty rejection, ineligible rejection. `tests/unit/annotations/selector-roundtrip.test.ts` loops a small synthetic article set asserting `resolveQuoteSelector(article, deriveQuoteSelector(article, p), p)` deep-equals `p` for several positions per article (the ANNO-05 invariant). `tests/unit/annotations/overlap.test.ts` covers disjoint/touching/partial-overlap/nested-rejected cases. All tests are jsdom-safe (pure logic only — NO layout, NO Playwright here per STACK.md).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/annotations/resolve-quote-selector.test.ts tests/unit/annotations/capture-offset-mapping.test.ts tests/unit/annotations/selector-roundtrip.test.ts tests/unit/annotations/overlap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function resolveQuoteSelector" src/content/normalizeText.ts` returns 1 (stub filled, not left as a comment).
    - `grep -c "resolveQuoteSelector" src/content/normalizeText.ts` ≥ 2 (export + re-export or usage).
    - `grep -c "never fork\|REUSE\|do not fork\|Never fork" src/annotations/capture.ts` ≥ 1 (Pattern 5 warning present).
    - `grep -c "splitParagraphRuns" src/annotations/highlightRanges.ts` ≥ 1 (run slicing reused, not reimplemented).
    - `grep -c "selection.toString" src/annotations/capture.ts` returns 0 (Pitfall 2 — never anchor on engine-dependent serialization).
    - `grep -v '^ *//' src/annotations/capture.ts | grep -c "default:"` returns 0 (Pattern F — no default swallow in block-kind eligibility).
    - `npm run test:unit -- --run tests/unit/annotations/resolve-quote-selector.test.ts tests/unit/annotations/capture-offset-mapping.test.ts tests/unit/annotations/selector-roundtrip.test.ts tests/unit/annotations/overlap.test.ts` exits 0 with all assertions passing.
    - The selector-roundtrip test asserts `(resolved as TextPositionSelector).start === originalPosition.start && .end === .end` for same-revision (ANNO-05 invariant).
    - The resolve-quote-selector test asserts the function returns the string literal "ambiguous" and "orphan" in the respective cases (ANNO-07 tri-state).
  </acceptance_criteria>
  <done>
resolveQuoteSelector implements the full D5-02 algorithm; captureSelection maps DOM Ranges to grapheme offsets with whitespace correction and typed invalid-reasons; rangesOverlap enforces D5-13; sliceRunsForHighlights reuses splitParagraphRuns; all 4 unit-test files green. No UI yet — this plan's "happy path" is the green pure-logic suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence boundary — HighlightRecord/NoteRecord Zod schemas + db.ts type fix + highlightsStore/notesStore + STATE-04/05 tests</name>
  <files>
src/content/schema.ts,
src/persistence/db.ts,
src/persistence/highlightsStore.ts,
src/persistence/notesStore.ts,
tests/unit/annotations/highlight-schema.test.ts,
tests/unit/annotations/highlights-store-error.test.ts
  </files>
  <behavior>
    - HighlightRecordSchema.safeParse accepts a well-formed record {schemaVersion:1, id, articleId, revision, position:{start,end}, quote:{prefix,exact,suffix}, createdAt} and rejects: end<=start, missing fields, non-datetime createdAt, non-integer revision.
    - NoteRecordSchema.safeParse accepts {schemaVersion:1, id, highlightId, text, updatedAt} and rejects empty/missing fields; text is z.string() (never HTML-parsed — Pitfall 8).
    - loadHighlights(articleId) returns {ok:true, highlights:[...]} on success, {ok:false, reason:"unavailable"|"corrupt"|"unupgradeable"} on Dexie error, and DROPS individual corrupt rows (a single bad row does not block the rest) per RESEARCH §Pattern 3.
    - loadHighlights uses the [articleId+revision] compound index range query between([articleId,0],[articleId,MAX_SAFE_INTEGER]) — NOT a plain articleId index (Pitfall 6).
    - saveHighlight upserts via db.highlights.put.
    - deleteHighlight runs a Dexie transaction deleting the highlight AND its note(s) atomically (Pitfall 10, D5-12).
    - loadNote(highlightId)/saveNote(note) mirror the locationStore/settingsStore single-record pattern.
  </behavior>
  <read_first>
src/content/schema.ts (the file being modified — read LocationRecordSchema at L245-252 as the verbatim in-file analog for the new schemas; note schemaVersion:z.literal(1), articleId regex /^[a-z0-9-]+$/, savedAt:z.string().datetime()),
src/persistence/db.ts (the file being modified — read in full; the highlights/notes placeholder Table<> annotations at L43-47 are what get fixed; the LocationRecordRow interface at L25-31 is the row-type analog; the v1 version block at L51-62 is byte-unchanged per Pitfall 9),
src/persistence/locationStore.ts (read in full — the CLOSEST analog; copy its LocationLoadResult discriminated union, safeParse-on-read, classifyStorageError routing, and saveLocation put pattern),
src/persistence/settingsStore.ts (sibling single-record seam — read for loadNote/saveNote pattern),
src/persistence/errors.ts (read in full — classifyStorageError returns "unavailable"|"corrupt"|"unupgradeable"; reuse verbatim, do NOT add new error surfaces),
tests/unit/locationSchema.test.ts and tests/unit/settingsSchema.test.ts (Zod-boundary test analogs — copy structure for highlight-schema.test.ts),
tests/unit/storageFallback.test.ts (read in full — copy its vi.mock("../../src/persistence/db",...) factory + vi.mocked(db.settings.get) pattern for highlights-store-error.test.ts, mocking db.highlights/db.notes instead),
05-RESEARCH.md §Pattern 3 (Persistence Schema — NO version bump), §Pitfall 6 (compound-key query), §Pitfall 9 (never edit shipped version block), §Pitfall 10 (cascade-delete transaction),
05-PATTERNS.md §`src/persistence/highlightsStore.ts`, §`src/persistence/notesStore.ts`, §`src/content/schema.ts`, §`src/persistence/db.ts`
  </read_first>
  <action>
In `src/content/schema.ts`, add FOUR exported schemas next to LocationRecordSchema (single source of truth, STATE-04). Per D5-03 (persist BOTH position and quote — position = O(1) primary anchor for same-revision; quote = recovery substrate for cross-revision), `HighlightRecordSchema` carries BOTH selectors: `TextPositionSelectorSchema` = z.object({ start: z.number().int().min(0), end: z.number().int().min(0) }).refine(s => s.end > s.start, { message: "end must be > start" }) (start-inclusive/end-exclusive per the existing interface at normalizeText.ts L117-120); `TextQuoteSelectorSchema` = z.object({ prefix: z.string(), exact: z.string().min(1), suffix: z.string() }) (default context radius = 32 grapheme clusters per D5-03, carried from Phase 1's deriveQuoteSelector); `HighlightRecordSchema` = z.object({ schemaVersion: z.literal(1), id: z.string(), articleId: z.string().regex(/^[a-z0-9-]+$/) (reuse the LocationRecord articleId regex), revision: z.number().int().min(1), position: TextPositionSelectorSchema, quote: TextQuoteSelectorSchema, createdAt: z.string().datetime() }) and `export type HighlightRecord = z.infer<typeof HighlightRecordSchema>`; `NoteRecordSchema` = z.object({ schemaVersion: z.literal(1), id: z.string(), highlightId: z.string(), text: z.string() (NEVER HTML — Pitfall 8; React escapes text children, react/no-danger ESLint rule forbids dangerouslySetInnerHTML), updatedAt: z.string().datetime() }) and `export type NoteRecord = z.infer<typeof NoteRecordSchema>`. ID generation uses crypto.randomUUID() at the call site (Open Question #2 recommendation) — not inside the schema.

In `src/persistence/db.ts`, replace ONLY the placeholder `Table<>` annotations on highlights (L43-46) and notes (L47) with real row types. Define `export interface HighlightRecordRow { schemaVersion: 1; id: string; articleId: string; revision: number; position: { start: number; end: number }; quote: { prefix: string; exact: string; suffix: string }; createdAt: string }` and `export interface NoteRecordRow { schemaVersion: 1; id: string; highlightId: string; text: string; updatedAt: string }` next to LocationRecordRow (L25-31). Change the annotations to `highlights!: Table<HighlightRecordRow, string>` and `notes!: Table<NoteRecordRow, string>`. DO NOT edit the v1 `this.version(1).stores({...})` block (L51-62) or the v2 block (L71-75) — Pitfall 9, byte-unchanged; the v1 declarations `highlights: "id, [articleId+revision]"` and `notes: "id, highlightId"` are already sufficient (NO version bump per RESEARCH §Pattern 3 finding). Runtime behavior is unaffected (Dexie resolves stores by name from version declarations, not TS types — mirrors the Phase 02-02 Table<> definite-assignment precedent in STATE.md).

Create `src/persistence/highlightsStore.ts` mirroring locationStore.ts structure verbatim. Export `HighlightsLoadResult = | { ok: true; highlights: HighlightRecord[] } | { ok: false; reason: "unavailable" | "corrupt" | "unupgradeable" }`. `loadHighlights(articleId)` uses the compound-index range query `db.highlights.where("[articleId+revision]").between([articleId, 0], [articleId, Number.MAX_SAFE_INTEGER]).toArray()` (D5-01 cross-revision lookup; Pitfall 6 — array-form range query, NOT a plain articleId field query), then loops `HighlightRecordSchema.safeParse(row)` pushing successes and DROPPING failures (defensive — a single corrupt row must not block all highlights, unlike locationStore's single-record corrupt→WipeConfirm). Catches errors via `classifyStorageError(e)` from `./errors` (never throws — STATE-05). `saveHighlight(h: HighlightRecord)` does `await db.highlights.put(h)` (throws propagate to caller → STATE-05 routing). `deleteHighlight(highlightId: string)` runs `await db.transaction("rw", db.highlights, db.notes, async () => { await db.highlights.delete(highlightId); await db.notes.where("highlightId").equals(highlightId).delete(); })` (Pitfall 10 atomic cascade-delete, D5-12 "a highlight and its note are removed together").

Create `src/persistence/notesStore.ts` mirroring settingsStore.ts/locationStore.ts. `loadNote(highlightId)` does `db.notes.where("highlightId").equals(highlightId).first()` then `NoteRecordSchema.safeParse` (returns NoteRecord | null). `saveNote(note)` does `await db.notes.put(note)`. No cascade-delete here — `deleteHighlight` owns the transaction (Pitfall 10). Empty-text note = caller deletes/never-creates the NoteRecord (D5-10 — handled in Plan 05-03's popover; this store just persists what it's given).

Write the 2 unit-test files. `tests/unit/annotations/highlight-schema.test.ts` copies locationSchema.test.ts structure: asserts valid records parse, corrupt records (end<=start, missing createdAt, non-integer revision, empty exact) are rejected (STATE-04). `tests/unit/annotations/highlights-store-error.test.ts` copies storageFallback.test.ts: `vi.mock("../../src/persistence/db", ...)` factory mocking `db.highlights`/`db.notes`, asserts loadHighlights returns the right reason for Dexie errors classified by classifyStorageError (unavailable/corrupt/unupgradeable), asserts a single corrupt row is dropped while valid rows pass, and asserts deleteHighlight calls both db.highlights.delete and db.notes.where().equals().delete() inside a transaction (Pitfall 10).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/annotations/highlight-schema.test.ts tests/unit/annotations/highlights-store-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HighlightRecordSchema\|NoteRecordSchema" src/content/schema.ts` ≥ 4 (both schemas + both inferred types).
    - `grep -c "TextPositionSelectorSchema\|TextQuoteSelectorSchema" src/content/schema.ts` ≥ 2.
    - `grep -c "dangerouslySetInnerHTML\|z.html\|z.string().html" src/content/schema.ts` returns 0 (Pitfall 8 — no HTML parsing).
    - `grep -c "version(3)\|version(2)" src/persistence/db.ts` returns exactly 1 (only the existing v2; NO v3 added — Pitfall 9 honored).
    - `grep -c 'between(\[articleId' src/persistence/highlightsStore.ts` ≥ 1 (Pitfall 6 compound-index range query).
    - `grep -c "db.transaction" src/persistence/highlightsStore.ts` ≥ 1 (Pitfall 10 cascade-delete transaction).
    - `grep -c "classifyStorageError" src/persistence/highlightsStore.ts` ≥ 1 (STATE-05 routing).
    - `npm run test:unit -- --run tests/unit/annotations/highlight-schema.test.ts tests/unit/annotations/highlights-store-error.test.ts` exits 0.
    - The store-error test asserts a corrupt row is DROPPED (not thrown) while valid rows are returned (defensive load).
  </acceptance_criteria>
  <done>
HighlightRecord/NoteRecord schemas + nested selector schemas live in schema.ts; db.ts Table<> annotations fixed with NO version bump; highlightsStore loadHighlights/saveHighlight/deleteHighlight + notesStore loadNote/saveNote mirror locationStore.ts with compound-index query + cascade-delete transaction; STATE-04 boundary + STATE-05 routing tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → in-memory | Highlight/Note rows read from Dexie cross into React state; Zod safeParse is the gate (STATE-04) |
| DOM Selection → persisted anchor | Ephemeral browser Range converted to durable grapheme offsets; whitespace-collapse mapping is the correctness boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | resolveQuoteSelector | mitigate | Pure function over normalizeText; never silently re-attaches — returns explicit "ambiguous"/"orphan" per D5-02/ANNO-07 (Pitfall 7) |
| T-05-02 | Tampering | capture.ts offset mapping | mitigate | Walks DOM text nodes + aligns to normalized graphemes; NEVER uses selection.toString() (engine-dependent whitespace — Pitfall 2); reuses normalizeText (Pattern 5, no fork) |
| T-05-03 | Tampering | HighlightRecord/NoteRecord persistence | mitigate | Zod safeParse on every Dexie read (STATE-04); corrupt rows dropped/never coerced; text is z.string() never HTML (Pitfall 8) |
| T-05-04 | Denial of Service | highlightsStore/notesStore | mitigate | classifyStorageError routes all Dexie failures to STATE-05; never throws to reader (D2-13) |
| T-05-05 | Tampering | DOM clobbering via highlight id | mitigate | Highlight IDs are crypto.randomUUID() at the call site; `<mark id="hl-{id}">` uses hl- prefix (no collision with fn-N footnote ids) |
| T-05-SC | Tampering | npm/pip installs | accept | Zero new packages this plan (research Package Legitimacy Audit is a no-op); gate N/A |
</threat_model>

<verification>
`npm run test:unit -- --run tests/unit/annotations/` exits 0 — all 6 Wave 0 unit-test files green. `npm run build` (tsc + vite build) succeeds — the db.ts Table<> type fix compiles, resolveQuoteSelector fills the stub with no type errors, the new src/annotations/* modules typecheck. No e2e yet (Plan 05-05 owns the corpus matrix).
</verification>

<success_criteria>
- resolveQuoteSelector implements D5-02 exact-first/disambiguate/orphan and returns the tri-state (ANNO-07 pure logic).
- The selector round-trip `offset → deriveQuoteSelector → resolveQuoteSelector → offset` is proven equal for same-revision (ANNO-05/ANNO-06 invariant).
- captureSelection maps DOM Ranges to D-05 grapheme offsets with whitespace-collapse correction and typed invalid reasons (ANNO-01 capture substrate).
- HighlightRecord/NoteRecord Zod schemas enforce STATE-04; db.ts type fix ships with NO Dexie version bump (Pitfall 9).
- highlightsStore compound-index cross-revision lookup + cascade-delete transaction work; STATE-05 routing verified.
- All 6 Wave 0 unit tests green; `npm run build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/05-durable-highlights-and-notes/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 05-01)

**Pure-logic functions (src/annotations/ + normalizeText.ts):**
- `resolveQuoteSelector(article, selector, positionHint?)` — fills the Phase 1 stub; D5-02 tri-state algorithm
- `findAllOccurrences(text, needle)`, `matchesContext(text, start, len, prefix, suffix)` — src/annotations/resolution.ts
- `captureSelection(article, readingRoot)` → `CaptureResult` — src/annotations/capture.ts
- `rangesOverlap(a, b)` — src/annotations/overlap.ts
- `sliceRunsForHighlights(runs, blockGlobalStart, highlights, lang)` — src/annotations/highlightRanges.ts

**Persistence (src/persistence/ + schema.ts + db.ts):**
- `HighlightRecordSchema`, `NoteRecordSchema`, `TextPositionSelectorSchema`, `TextQuoteSelectorSchema` + inferred types — schema.ts
- `HighlightRecordRow`, `NoteRecordRow` interfaces — db.ts (Table<> annotations fixed; NO version bump)
- `loadHighlights(articleId)`, `saveHighlight(h)`, `deleteHighlight(id)`, `HighlightsLoadResult` — highlightsStore.ts
- `loadNote(highlightId)`, `saveNote(note)` — notesStore.ts

**Tests:** 6 files under tests/unit/annotations/ (resolve-quote-selector, capture-offset-mapping, selector-roundtrip, overlap, highlight-schema, highlights-store-error)
