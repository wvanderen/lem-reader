---
phase: 05-durable-highlights-and-notes
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/reader/annotations/NotePopover.tsx
  - src/reader/annotations/AnnotationsDrawer.tsx
  - src/reader/Header.tsx
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/unit/annotations/note-popover-confirm.test.ts
autonomous: true
requirements: [ANNO-02, ANNO-03, ANNO-04]
user_setup: []

must_haves:
  truths:
    - "Reader can attach a text note to a highlight via the inline popover (textarea, debounced save, no Save button) — 'Highlight + note' (or N) creates + opens the popover with a focused empty textarea (ANNO-02, D5-10)"
    - "Reader can open a header-mounted annotations drawer listing all highlights+notes for THIS article in reading order, each tappable to jump back to its passage (ANNO-03/04, D5-09)"
    - "Reader can delete a highlight via a two-step confirm (Delete → confirm prompt → Delete/Keep) with non-destructive default focus on Keep; the highlight + its note are removed together and '.status' announces 'Highlight deleted.' (ANNO-03, D5-12, STATE-03)"
    - "Navigate-back closes the drawer, resolves the highlight's grapheme offset to its block via data-block-index, turns to the page (paginated) or scrollIntoView (scrolling), and focuses the <mark> (ANNO-04, D5-11)"
    - "Note text is rendered as a React text child (escaped) — never raw HTML (Pitfall 8)"
  artifacts:
    - path: "src/reader/annotations/NotePopover.tsx"
      provides: "Popover API (popover=manual) note editor + two-step delete confirm mirroring WipeConfirm + debounced save"
      exports: ["NotePopover"]
    - path: "src/reader/annotations/AnnotationsDrawer.tsx"
      provides: "Native <dialog> slide-over list of highlights+notes in reading order + empty-state + ambiguous/orphan flag slots"
      exports: ["AnnotationsDrawer"]
    - path: "src/reader/Header.tsx"
      provides: "annotations-trigger button inline-start of ModeToggle with count badge"
    - path: "src/routes/ArticleView.tsx"
      provides: "NotePopover + AnnotationsDrawer mount + navigate-back landing (commitTurn/scrollIntoView + focus <mark>)"
    - path: "src/app.css"
      provides: ".highlight-popover, .annotations-drawer, .drawer-*, .annotations-trigger styles"
  key_links:
    - from: "src/reader/annotations/NotePopover.tsx"
      to: "src/reader/annotations/useAnnotationState.ts"
      via: "debounced updateNote + deleteHighlight (flush on Done/Escape)"
      pattern: "updateNote|deleteHighlight|scheduleNoteSave|flushNoteSave"
    - from: "src/reader/annotations/AnnotationsDrawer.tsx"
      to: "src/routes/ArticleView.tsx"
      via: "onNavigate(highlightId) callback → ArticleView runs D5-11 (commitTurn/scrollIntoView + focus <mark>)"
      pattern: "onNavigate|commitTurn|scrollIntoView"
    - from: "src/reader/Header.tsx"
      to: "src/reader/annotations/AnnotationsDrawer.tsx"
      via: "annotations-trigger aria-expanded controls drawer open state"
      pattern: "annotations-trigger|aria-expanded"
---

## Phase Goal

**As a** reader, **I want to** create, revisit, and manage local highlights and notes, **so that** they remain attached to their intended normalized text across every view change.

> This plan (05-03) delivers the **notes + management slice**: attach/edit notes (ANNO-02), browse in a drawer + navigate back (ANNO-03/04), delete with confirm (ANNO-03/STATE-03). It layers onto the Plan 05-02 capture→render slice — the popover opens on `<mark>` activation or "Highlight + note", the drawer mounts in the header, and navigate-back reuses the Phase 4 anchor machinery in reverse.

<objective>
Build the three management surfaces: (1) `NotePopover` (Popover API `popover="manual"`) — textarea with debounced save (mirrors SettingsContext D2-03), two-step delete confirm (mirrors WipeConfirm Pitfall 8), focus management; (2) `AnnotationsDrawer` (native `<dialog>` reusing `.settings-panel` geometry) — reading-order `<ol>`, empty-state, ambiguous/orphan flag slots (data wired in Plan 05-04); (3) `Header` annotations-trigger button + `ArticleView` navigate-back landing (D5-11: grapheme offset → block via data-block-index → commitTurn paginated / scrollIntoView scrolling → focus `<mark>`). Complete the `updateNote` debounced-save in `useAnnotationState`. Add the `.status` announces ("Highlight saved." / "Note saved." / "Highlight deleted.").

Purpose: ANNO-02 (notes), ANNO-03 (view/edit/delete), ANNO-04 (navigate-back). The popover + drawer reuse proven Phase 2 patterns (native `<dialog>`, debounced save, WipeConfirm) — the genuinely new work is the D5-11 navigate-back running the D4-10/D4-11 anchor machinery in reverse.
Output: 2 new UI components, 1 header modification, 1 route modification, 1 CSS modification, 1 component test.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durable-highlights-and-notes/05-CONTEXT.md
@.planning/phases/05-durable-highlights-and-notes/05-RESEARCH.md
@.planning/phases/05-durable-highlights-and-notes/05-PATTERNS.md
@.planning/phases/05-durable-highlights-and-notes/05-UI-SPEC.md
@.planning/phases/05-durable-highlights-and-notes/05-01-SUMMARY.md
@.planning/phases/05-durable-highlights-and-notes/05-02-SUMMARY.md
@src/reader/SettingsPanel.tsx
@src/reader/WipeConfirm.tsx
@src/reader/Header.tsx
@src/settings/SettingsContext.tsx
@src/routes/ArticleView.tsx
@src/pagination/anchor.ts
@src/reader/restoreLocation.ts
@src/persistence/notesStore.ts
@src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotePopover (Popover API editor + debounced save + WipeConfirm-style delete confirm) + Header annotations-trigger + useAnnotationState.updateNote completion</name>
  <files>
src/reader/annotations/NotePopover.tsx,
src/reader/Header.tsx,
src/reader/annotations/useAnnotationState.ts,
src/app.css
  </files>
  <read_first>
src/reader/SettingsPanel.tsx (read in full — the native <dialog>/showModal lifecycle + focus-restore pattern L42-79 is the popover lifecycle/focus template; the explicit-focus-after-showModal WebKit fix applies analogously to showPopover),
src/reader/WipeConfirm.tsx (read in full — the two-step confirm + [data-initial-focus] on the non-destructive button L85-102 + --destructive border L120-143 is the delete-confirm template to copy EXACTLY per Pitfall 8),
src/settings/SettingsContext.tsx (read L113-165 — scheduleSave + flushSave + dual-event-flush (visibilitychange-hidden + pagehide); updateNote's debounced save mirrors this verbatim per D2-03),
src/reader/annotations/useAnnotationState.ts (the file being modified — read the updateNote stub from Plan 05-02; replace with the full debounced save),
src/reader/Header.tsx (the file being modified — read the gear-button L62-75 + .header-controls group L60; the annotations-trigger is a sibling inline-start of ModeToggle per UI-SPEC §Interaction 30),
src/app.css (the file being modified — read .settings-panel geometry rules ~L420-436 for the popover/drawer reference; the :root + [data-theme] token blocks + global forced-colors gate already carry --highlight from Plan 05-02),
src/persistence/notesStore.ts (the Plan 05-01 seam — loadNote/saveNote are the save targets),
05-RESEARCH.md §Pattern 5 (three overlay mechanisms rationale — popover=manual for the note editor), §Pattern 6 (debounced note save mirrors SettingsContext), §Pitfall 8 (note XSS),
05-PATTERNS.md §`src/reader/annotations/NotePopover.tsx`, §`src/reader/Header.tsx`, §`src/reader/annotations/useAnnotationState.ts`,
05-UI-SPEC.md §Interaction 29 (popover lifecycle/focus/contents), §Interaction 32 (delete confirm + announces), §Copywriting (popover + announce copy), §Component Inventory (.highlight-popover hooks)
  </read_first>
  <action>
Create `src/reader/annotations/NotePopover.tsx` per UI-SPEC §Interaction 29. A `<div popover="manual" id="highlight-popover" class="highlight-popover" role="dialog" aria-label="Highlight note">` (Popover API — NOT <dialog>/showModal which is too heavy per UI-SPEC §Design System rationale; manual mode so typing doesn't light-dismiss). Props: `{ openFor: { highlightId: string; excerpt: string } | null; noteText: string; unresolved?: boolean; onClose: () => void; onSaveNote: (id, text) => void; onDelete: (id) => Promise<void> }`. Contents: (1) visually-hidden `<label>` "Highlighted text:" + read-only `<p class="highlight-popover-excerpt">` (italic, --ink-soft, excerpt truncated ~200 chars); (2) visually-hidden `<label htmlFor>` "Note" + `<textarea>` (Body role, selected body stack, placeholder "Add a note (optional)"); (3) actions row: `<button>Done</button>` + `<button class="destructive">Delete</button>` (--destructive border). React state controls `showPopover()`/`hidePopover()` via an effect on `openFor`. Debounced save (D2-03 pattern): on each textarea change, `scheduleNoteSave(nextText)` (debounced ~800ms-1s mirroring settings cadence) stashing pendingRef + saveTimer refs (copy SettingsContext L113-129 structure); on `Done`/`Escape`, `flushNoteSave()` fires a final write before close so no edit is lost. Add the dual-event flush (visibilitychange-hidden + pagehide — bfcache-safe; the deprecated session-end events are FORBIDDEN per Plan 05-02's useAnnotationState header). Empty textarea = no NoteRecord (the flush deletes the note if text emptied). Two-step delete (D5-12, mirror WipeConfirm.tsx L85-102 + L120-143 EXACTLY): step 1 `Delete` replaces the popover body with the confirm prompt "Delete this highlight?" + excerpt + two buttons; step 2 confirm `Delete` (--destructive border + text, hover inverts to fill --destructive / text --surface) calls `onDelete(highlightId)` then closes; cancel `Keep` (neutral --hairline border) returns to edit view. **Non-destructive default focus (Pitfall 8):** on step 1, focus moves to the `Keep` button via `[data-initial-focus]` — an accidental Enter cannot delete. Focus management: on open via "Highlight + note" (N) → focus the empty textarea; on open via activating an existing `<mark>` → focus the textarea with existing text selected; on Done/Escape → focus back to the `<mark>` (predictable restore — A11Y-02, mirrors the gear-button trigger-restore). The excerpt + note render as React text children (`<p>{excerpt}</p>`, `<textarea value={text}/>`) — NEVER dangerouslySetInnerHTML (Pitfall 8; react/no-danger enforces).

Complete `updateNote(highlightId, text)` in `src/reader/annotations/useAnnotationState.ts` (replace the Plan 05-02 stub): implement `scheduleNoteSave`/`flushNoteSave` (copy SettingsContext L113-165 scheduleSave + flushSave + dual-event-flush verbatim, swapping settingsStore.saveSettings → notesStore.saveNote, and deleting the NoteRecord when text is empty). On flush commit, announce "Note saved." once (debounced — not per keystroke) via the onStatusAnnounce callback. Update the ResolvedHighlight.note in state optimistically; route save errors via classifyStorageError → onStorageError → existing StorageBanner (D2-13, no new surface).

Modify `src/reader/Header.tsx`: add an annotations-trigger `<button type="button" class="annotations-trigger" aria-label="Highlights and notes{, {N}}" aria-expanded={drawerOpen} aria-haspopup="dialog">` inline-start of the ModeToggle in `.header-controls` (so the group reads `[annotations] [mode] [gear]` per UI-SPEC §Interaction 30). Mirror the gear-button geometry EXACTLY: 44×44 hit area, transparent background, `--ink-soft` default, `--accent` ONLY when `[aria-expanded="true"]`. Inline-SVG highlighter glyph (`aria-hidden="true"`; the name lives in aria-label). Count badge `<span class="annotations-trigger-badge" aria-hidden="true">{N}</span>` via `Intl.NumberFormat` when N>0 (count also in the aria-label — no visually-hidden duplicate). The trigger appears ONLY when an article is mounted (hidden on the fixture list — use the existing article-mounted prop/guard Header already receives). The header does NOT grow (the three controls share 44×44 geometry; `.header-controls` gap stays `--space-sm`). Wire `onClick` → toggles drawer open state owned by ArticleView (passed down or via the HighlightOverlay context).

Modify `src/app.css`: add `.highlight-popover { background: var(--surface-raised); border: 1px solid var(--hairline); border-radius: 8px; max-width: 380px; width: calc(100vw - 2 * var(--space-md)); padding: var(--space-lg); display: flex; flex-direction: column; gap: var(--space-md); }` + `.highlight-popover-excerpt { font-style: italic; color: var(--ink-soft); }` + `.highlight-popover-confirm { /* confirm-prompt layout */ }` + the destructive/keep button rules (mirror `.wipe-confirm-destructive`/`.wipe-confirm-cancel` geometry exactly per Pitfall 8). Add `.annotations-trigger { /* mirror .gear-button geometry */ }` + `.annotations-trigger[aria-expanded="true"] { color: var(--accent); }` + `.annotations-trigger-badge { /* superscript, --ink-soft */ }`. NO transition/animation property (A11Y-06 — the Popover API's own transition is disabled by the global reduced-motion gate). Add the `.status` announce copy is data, not CSS (the existing `.status` rule L257-270 already styles it).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/annotations/note-popover-confirm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'popover="manual"\|popover={"manual"}' src/reader/annotations/NotePopover.tsx` ≥ 1 (Popover API mechanism per UI-SPEC §Design System).
    - `grep -c "showPopover\|hidePopover" src/reader/annotations/NotePopover.tsx` ≥ 2 (Popover API lifecycle).
    - `grep -c "data-initial-focus" src/reader/annotations/NotePopover.tsx` ≥ 1 (Pitfall 8 non-destructive default focus on Keep).
    - `grep -c "destructive" src/reader/annotations/NotePopover.tsx` ≥ 1 (delete button uses the destructive border).
    - `grep -c "scheduleNoteSave\|flushNoteSave" src/reader/annotations/useAnnotationState.ts` ≥ 2 (debounced save + flush).
    - `grep -c "visibilitychange\|pagehide" src/reader/annotations/useAnnotationState.ts` ≥ 2 (dual-event flush per D2-03).
    - `grep -c "dangerouslySetInnerHTML" src/reader/annotations/NotePopover.tsx` returns 0 (Pitfall 8).
    - `grep -c "annotations-trigger" src/reader/Header.tsx` ≥ 1.
    - `grep -c "aria-expanded" src/reader/Header.tsx` ≥ 1.
    - `grep -cE "transition|animation" src/app.css` shows NO new transition/animation on .highlight-popover or .annotations-trigger selectors (A11Y-06).
    - `npm run test:unit -- --run tests/unit/annotations/note-popover-confirm.test.ts` exits 0.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
NotePopover opens via <mark> activation or N, edits the note with debounced save + dual-event flush, runs the two-step delete confirm with non-destructive default focus; useAnnotationState.updateNote persists via notesStore.saveNote (deleting on empty); Header hosts the annotations-trigger with count badge; CSS for popover + trigger landed. Component test green; build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: AnnotationsDrawer (native <dialog> reading-order list + empty-state) + ArticleView navigate-back landing (D5-11) + drawer CSS</name>
  <files>
src/reader/annotations/AnnotationsDrawer.tsx,
src/routes/ArticleView.tsx,
src/app.css
  </files>
  <read_first>
src/reader/SettingsPanel.tsx (read in full — the drawer is a sibling <dialog class="annotations-drawer"> reusing .settings-panel sheet geometry + showModal lifecycle + focus-restore L42-79 VERBATIM per D5-09; copy the open/close effect + handleClose close-listener + triggerRef.current?.focus() restore),
src/routes/ArticleView.tsx (the file being modified — read the mode-aware branch, the PaginatedSurface commitTurn imperative handle usage, findScrollTarget/scrollIntoView in scrolling mode, and the queryBlocks [data-block-index] helper at L87-91; navigate-back reuses all three),
src/pagination/anchor.ts (read fragmentContainingOffset + pageStartGlobalOffset — D5-11 navigate-back resolves the highlight's grapheme offset → page index via fragmentContainingOffset, then commitTurn),
src/reader/restoreLocation.ts (read findScrollTarget + computeTopVisibleOffset — the scrolling-mode navigate-back target; reuse, do not fork),
src/reader/annotations/HighlightOverlay.tsx (the Plan 05-02 provider — the drawer reads highlights + deleteHighlight + openPopoverFor from the context),
src/app.css (the file being modified — read .settings-panel geometry ~L420-436; the drawer reuses it verbatim under .annotations-drawer),
05-RESEARCH.md §Pattern 5 (drawer = native <dialog> reusing D2-01), §"Don't Hand-Roll" (navigate-back reuses D4-10/D4-11 machinery in reverse),
05-PATTERNS.md §`src/reader/annotations/AnnotationsDrawer.tsx`, §`src/routes/ArticleView.tsx`,
05-UI-SPEC.md §Interaction 30 (drawer contents/ordering/empty-state), §Interaction 31 (navigate-back), §Component Inventory (.annotations-drawer, .drawer-* hooks)
  </read_first>
  <action>
Create `src/reader/annotations/AnnotationsDrawer.tsx` per UI-SPEC §Interaction 30. A `<dialog class="annotations-drawer">` shown via `showModal()` — copy SettingsPanel.tsx L42-79 lifecycle VERBATIM (capture `document.activeElement` on open → `triggerRef.current` → `dlg.showModal()` → focus first focusable; close listener restores focus to the trigger). Reuse `.settings-panel` sheet geometry (near-full-width <640px; ~400px ≥640px; `::backdrop` covers the article; free focus-trap + Esc + inert backdrop). Props: `{ open: boolean; onClose: () => void; onNavigate: (highlightId: string) => void; onEditNote: (highlightId: string) => void; onDeleteRequest: (highlightId: string) => void }`. Reads `highlights: ResolvedHighlight[]` from `useHighlightOverlay()`. Contents: header row (`<h2>` "Highlights and notes" via `aria-labelledby` for the dialog's accessible name + × close button mirroring settings-panel close). Body: if highlights.length === 0, render the empty-state card `<div class="status drawer-empty">` with heading "No highlights yet" + body "Select any text in the article to highlight it. You can add a note to any highlight, and come back here to find them." (UI-SPEC §Copywriting). Else render `<ol class="drawer-list">` of `<li>` entries in READING ORDER (sort by `resolvedPosition?.start ?? record.position.start` ascending — D5-09 canonical order; no sort/filter for MVP per UI-SPEC §Interaction 30). Each entry: a `<button type="button" class="drawer-entry">` (the whole-row jump affordance — A11Y-07 large hit area) with `aria-label={"Go to highlight: {excerpt}{; unresolved note}"}` containing the excerpt (`<span class="drawer-entry-excerpt">`, Body role, truncated ~120 chars) + optional note (`<span class="drawer-entry-note">`, italic, --ink-soft, truncated ~200 chars). Sibling actions row `<span class="drawer-entry-actions">` with `<button>Edit note</button>` + `<button>Delete</button>` (quiet text buttons OUTSIDE the jump button so they don't trigger navigation). Ambiguous/orphan entries (status !== "confident", data arrives fully in Plan 05-04 but the slot is built here): jump button `disabled`, a `<p class="drawer-entry-flag">` "Couldn't find a unique match" / "Couldn't relocate this highlight" + body "The passage may have changed…", Edit disabled, Delete enabled (D5-04 — delete always available). The drawer-trigger count badge (Plan 05-03 Task 1 Header) reflects total (resolved + unresolved).

Modify `src/routes/ArticleView.tsx`: (a) mount `<AnnotationsDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} onNavigate={handleNavigateBack} onEditNote={openPopoverFor} onDeleteRequest={openPopoverFor} />` as a sibling of the article body (inside the HighlightOverlayProvider). (b) Implement `handleNavigateBack(highlightId)`: look up the ResolvedHighlight, read its `resolvedPosition` (confident) — `setDrawerOpen(false)`; resolve the grapheme offset to its block via `data-block-index` (find the block whose article-global range contains the offset — reuse the `pageStartGlobalOffset`/blockGraphemeLength accumulation from anchor.ts, or walk `[data-block-index]` elements); then branch on mode: PAGINATED → `const pageIdx = fragmentContainingOffset(pages, offset, article); commitTurn(pageIdx);` (reuse the PaginatedSurface imperative handle + anchor.ts — D4-10/D4-11 machinery in reverse), SCROLLING → `findScrollTarget(articleEl, queryBlocks(articleEl), offset)` + `el.scrollIntoView({ block: "center" })` (reuse Phase 2 findScrollTarget EXACTLY — do not fork); then `requestAnimationFrame(() => document.getElementById("hl-"+highlightId)?.focus())` to focus the `<mark>` after the turn/scroll commits (D4-07 focus precedent). Ambiguous/orphan entries never call handleNavigateBack (their jump button is disabled — ANNO-07). (c) Wire the drawer-open state to the Header annotations-trigger `aria-expanded` (via props or context — Header already receives article-mounted state).

Modify `src/app.css`: add `.annotations-drawer { /* reuse .settings-panel sheet geometry verbatim */ }` (copy the `.settings-panel` width/inset/backdrop rules under the new class so the drawer's internal layout can differ but the sheet behavior is identical) + `.drawer-list { list-style: none; margin: 0; padding: 0; }` + `.drawer-entry { display: block; width: 100%; text-align: start; padding: var(--space-md); border: none; background: transparent; color: inherit; cursor: pointer; }` + `.drawer-entry:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 2px; }` + `li + li .drawer-entry { border-top: 1px solid var(--hairline); }` (calm list rhythm) + `.drawer-entry-excerpt { /* Body role */ }` + `.drawer-entry-note { font-style: italic; color: var(--ink-soft); }` + `.drawer-entry-flag { color: var(--ink-soft); font-weight: 600; }` + `.drawer-entry-actions { display: flex; gap: var(--space-sm); justify-content: flex-end; }` + `.drawer-empty { /* reuse .status card geometry */ padding: var(--space-lg); }`. NO transition/animation (A11Y-06 — the global gate disables the slide).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/annotations/note-popover-confirm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<dialog\|class=\"annotations-drawer\"" src/reader/annotations/AnnotationsDrawer.tsx` ≥ 1 (native <dialog> per D5-09).
    - `grep -c "showModal" src/reader/annotations/AnnotationsDrawer.tsx` ≥ 1.
    - `grep -c "triggerRef.current" src/reader/annotations/AnnotationsDrawer.tsx` ≥ 2 (focus-restore pattern copied from SettingsPanel — Pitfall 1).
    - `grep -c "drawer-entry" src/reader/annotations/AnnotationsDrawer.tsx` ≥ 1.
    - `grep -c "drawer-empty\|No highlights yet" src/reader/annotations/AnnotationsDrawer.tsx` ≥ 1 (empty-state per UI-SPEC).
    - `grep -c "fragmentContainingOffset\|commitTurn\|scrollIntoView\|findScrollTarget" src/routes/ArticleView.tsx` ≥ 2 (D5-11 navigate-back reuses Phase 4/2 anchor machinery).
    - `grep -c "getElementById(.hl-\|getElementById(\"hl-" src/routes/ArticleView.tsx` ≥ 1 (focus the <mark> after navigate-back).
    - `grep -c "dangerouslySetInnerHTML" src/reader/annotations/AnnotationsDrawer.tsx` returns 0 (Pitfall 8 — excerpts/notes are React text children).
    - `grep -cE "transition|animation" src/app.css` shows NO new transition/animation on .annotations-drawer or .drawer-* selectors (A11Y-06).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
AnnotationsDrawer renders a reading-order list (or empty-state) in a native <dialog> reusing .settings-panel geometry + focus-restore; ArticleView handleNavigateBack runs D5-11 (resolve offset → fragmentContainingOffset/commitTurn paginated OR findScrollTarget/scrollIntoView scrolling → focus the <mark>); drawer CSS landed. Build succeeds; the popover-confirm component test still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader-authored note text → rendered | Note textarea input crosses into React state + IndexedDB + rendered <p>/<textarea>; React escaping + z.string() are the gate |
| Drawer entry → navigation | A drawer tap triggers page-turn/scroll + focus; ambiguous/orphan entries must NOT navigate (disabled jump button) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | NotePopover note rendering | mitigate | React text children (`<textarea value={text}/>`); z.string() (no HTML); react/no-danger enforced (Pitfall 8) |
| T-05-11 | Repudiation / DoS | accidental highlight deletion | mitigate | Two-step confirm mirroring WipeConfirm; [data-initial-focus] on Keep; --destructive border conveys consequence (D5-12/Pitfall 8) |
| T-05-12 | DoS | note save data loss on tab close | mitigate | Debounced save + dual-event flush (visibilitychange-hidden + pagehide); final flush on Done/Escape (D2-03) |
| T-05-13 | Tampering / Information | navigate-back to wrong passage | mitigate | Ambiguous/orphan entries have disabled jump buttons (never navigate); resolved entries use the confident position only (ANNO-07) |
</threat_model>

<verification>
`npm run test:unit -- --run tests/unit/annotations/` green (all prior + note-popover-confirm). `npm run build` succeeds. `npm run lint` clean. A developer can manually exercise the full note/drawer/navigate/delete flow in `npm run dev`, but the automated corpus-matrix proof is Plan 05-05.
</verification>

<success_criteria>
- Reader attaches/edits a note via the popover; debounced save + dual-event flush; "Note saved." announces once (ANNO-02).
- Reader opens the drawer from the header, sees highlights+notes in reading order (or the empty-state), jumps back to a passage in BOTH modes (ANNO-03/04, D5-11).
- Reader deletes a highlight via two-step confirm with non-destructive default focus; highlight+note removed together; "Highlight deleted." announces (ANNO-03, D5-12, STATE-03).
- Note text + excerpts render as React text children (no XSS surface).
- Drawer reuses .settings-panel geometry + focus-restore; navigate-back reuses Phase 4/2 anchor machinery (no fork).
</success_criteria>

<output>
Create `.planning/phases/05-durable-highlights-and-notes/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 05-03)

**Components (src/reader/annotations/):**
- `NotePopover` — Popover API (`popover="manual"`) note editor + two-step delete confirm (WipeConfirm pattern) + debounced save
- `AnnotationsDrawer` — native `<dialog>` slide-over reading-order list + empty-state + ambiguous/orphan flag slots

**Modified files:**
- `Header.tsx` — `.annotations-trigger` button inline-start of ModeToggle + count badge
- `ArticleView.tsx` — mounts NotePopover + AnnotationsDrawer; `handleNavigateBack` (D5-11: fragmentContainingOffset/commitTurn OR findScrollTarget/scrollIntoView + focus `<mark>`)
- `useAnnotationState.ts` — `updateNote` completed (scheduleNoteSave/flushNoteSave + dual-event flush + "Note saved." announce)
- `app.css` — `.highlight-popover`, `.highlight-popover-*`, `.annotations-trigger`, `.annotations-trigger-badge`, `.annotations-drawer`, `.drawer-*` styles

**Test:** tests/unit/annotations/note-popover-confirm.test.ts (RTL: two-step confirm non-destructive default focus, debounced save wiring)
