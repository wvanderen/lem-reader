---
phase: 18-reader-orientation
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - src/routes/ArticleView.tsx
  - src/reader/useScrollSave.ts
  - src/reader/RestorationMarker.tsx
  - tests/unit/RestorationMarker.test.tsx
  - tests/e2e/persistence.spec.ts
  - tests/e2e/toc/restoration-cue.spec.ts
  - src/reader/ResumeBanner.tsx
  - src/app.css
  - tests/e2e/chrome/mobile-first-page-chrome.spec.ts
  - tests/e2e/library/reading-views.spec.ts
autonomous: true
requirements: [ORNT-06]
must_haves:
  truths:
    - "A pure-paginated reader's location persists on page turns (debounced anchor-change save + dual flush) — same LocationRecord shape, no schema change"
    - "A paginated reopen lands on the saved page (readiness-gated restore), not page 1 — the deferred paginated-restore gap closes"
    - "Reopening an article with a saved location shows a passive marker AT the restored spot plus the polite announce — the cue IS the location (D18-05)"
    - "The marker fires on reopen-restore ONLY — never on first open without a location, never on TOC jumps, never on Highlights deep-links (D18-08)"
    - "The marker never shifts content, blocks page turns, intercepts pointer events, or requires dismissal; it fades at ~4s and clears instantly under reduced motion (D18-07)"
    - "The retired banner surface is fully gone across all four surfaces — component, wiring, CSS, and both e2e assertion sites (D18-06)"
  artifacts:
    - path: "src/reader/RestorationMarker.tsx"
      provides: "transient passive position marker (both modes) + polite announce"
    - path: "tests/unit/RestorationMarker.test.tsx"
      provides: "lifecycle + announce + passivity RTL suite (fake timers)"
    - path: "tests/e2e/toc/restoration-cue.spec.ts"
      provides: "marker lifecycle e2e (scrolling cells here; paginated + matrix cells in Plan 18-04)"
    - path: "src/reader/useScrollSave.ts"
      provides: "paginated anchor-change save scheduling reusing the debounced save + dual-flush discipline"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/pagination/anchor.ts"
      via: "paginated reopen restore: fragmentContainingOffset(pages, loc.graphemeOffset, article) → surfaceRef.turnToPage"
      pattern: "fragmentContainingOffset"
    - from: "src/routes/ArticleView.tsx"
      to: "src/reader/useScrollSave.ts"
      via: "handleAnchorChange per-turn offsets feed the debounced saveLocation scheduler (single call-site family preserved)"
      pattern: "saveLocation"
    - from: "src/reader/RestorationMarker.tsx"
      to: "src/routes/ArticleView.tsx"
      via: "parent mounts the marker ONLY when a reopen-restore genuinely landed; marker resolves its attach target on the visible surface"
      pattern: "restoration-marker"
---

<objective>
Ship restoration honesty (ORNT-06): close the two load-bearing paginated
gaps (no location saves on page turns; deferred paginated reopen-restore),
replace the retiring banner with the passive transient RestorationMarker, and
sweep the banner retirement across all four surfaces.

Purpose: ORNT-06 is unqualified by mode and paginated is the DEFAULT mode —
without the gap closures the paginated marker can never honestly fire
(RESEARCH Pitfalls 1-2; UI-SPEC §Auto-Resolved #8 sanctions both closures
in-phase). The marker is the "cue IS the location" reshape (D18-05/06/07/08).

Output: paginated save/restore wiring + RestorationMarker + marker CSS +
retirement sweep (component delete, wiring removal, CSS block removal, 2 e2e
site replacements).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-reader-orientation/18-CONTEXT.md
@.planning/phases/18-reader-orientation/18-RESEARCH.md
@.planning/phases/18-reader-orientation/18-PATTERNS.md
@.planning/phases/18-reader-orientation/18-UI-SPEC.md
@.planning/phases/18-reader-orientation/18-02-SUMMARY.md

@src/routes/ArticleView.tsx
@src/reader/useScrollSave.ts
@src/persistence/locationStore.ts
@src/pagination/anchor.ts
@src/reader/ResumeBanner.tsx
@src/reader/PaginatedSurface.tsx
@src/reader/restoreLocation.ts
@src/app.css
@tests/e2e/persistence.spec.ts
@tests/e2e/chrome/mobile-first-page-chrome.spec.ts
@tests/e2e/library/reading-views.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paginated location saves + readiness-gated paginated reopen-restore</name>
  <files>src/routes/ArticleView.tsx, src/reader/useScrollSave.ts, tests/e2e/persistence.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§ArticleView seams 2 + 3: deep-link readiness-gate excerpt L1343-1455 with RETRY_CAP_MS 5000; useScrollSave scheduleSave excerpt L124-133 + dual-flush L171-184; the restore-effect jumpPendingRef guard note L1472)
  - .planning/phases/18-reader-orientation/18-RESEARCH.md (§Common Pitfalls 1 + 2 — the two gaps; §Open Questions 1-2 with the sanctioned in-phase recommendation)
  - src/routes/ArticleView.tsx (restore effect L1462-1542; deep-link effect L1343-1455 — the readiness template; handleAnchorChange ~L507 and its PaginatedSurface onAnchorChange wiring ~L2134; jumpPendingRef guard; surfaceRef; currentAnchorOffsetRef threading ~L2133)
  - src/reader/useScrollSave.ts (SAVE_DEBOUNCE_MS 1200; scheduleSave; saveLocation call; visibilitychange-hidden + pagehide dual flush)
  - src/pagination/anchor.ts (fragmentContainingOffset signature + semantics)
  - src/reader/PaginatedSurface.tsx (handle: getPages/turnToPage)
  - src/persistence/locationStore.ts (saveLocation + LocationRecord shape)
  - tests/e2e/persistence.spec.ts (the option-(b) deferral block ~L55-70 — retires with wiring per UI-SPEC §Regression Targets)
  </read_first>
  <action>
  Close both gaps with existing machinery — import, never reimplement:

  1. SAVES (Pitfall 2): extend src/reader/useScrollSave.ts so the paginated
     path shares its discipline — export a scheduler (or accept an external
     offset source) that handleAnchorChange's per-turn offset feeds: same
     debounced saveLocation persistence (SAVE_DEBOUNCE_MS 1200), same
     visibilitychange-hidden + pagehide dual flush, same LocationRecord
     fields {schemaVersion, articleId, revision, graphemeOffset, savedAt}.
     The saveLocation call-site FAMILY stays singular (in useScrollSave) —
     no new direct saveLocation calls elsewhere. No Dexie schema/version
     change (Pitfall 9). In ArticleView, wire handleAnchorChange to schedule
     the save with the freshest anchor offset.
  2. RESTORE (Pitfall 1): in the restore effect, add the paginated branch
     using the deep-link readiness template VERBATIM in shape: bounded rAF
     retry (RETRY_CAP_MS 5000) until the first pagination commit
     (surfaceRef.current?.getPages() non-empty), then pageIdx =
     fragmentContainingOffset(pages, loc.graphemeOffset, article) and
     surfaceRef.current.turnToPage(pageIdx). Preserve the existing
     jumpPendingRef guard (deep-link jump wins; restore stays suppressed on
     deep-link arrivals). The scrolling branch is UNCHANGED — byte-stable.
     StrictMode idempotent via the shipped one-shot previous-value
     comparison pattern.
  3. tests/e2e/persistence.spec.ts: retire the option-(b) deferral comment +
     deferral assertion; add the paginated restore cell — seed a
     LocationRecord, open the article in paginated mode, assert the reader
     lands on the page containing the saved offset (not page 1). Changed
     assertions carry a D18-06/UI-SPEC-#8 citation comment. Also add the
     save-on-turn cell: turn pages, reload, assert the reopened page is the
     last-turned page's offset range.
  </action>
  <acceptance_criteria>
  - src/reader/useScrollSave.ts still contains the ONLY saveLocation call family (rg saveLocation src/ shows call(s) only in useScrollSave.ts)
  - ArticleView.tsx paginated restore contains fragmentContainingOffset + turnToPage within a bounded rAF retry (RETRY_CAP_MS cap present; no unbounded loop)
  - The jumpPendingRef guard still short-circuits the restore effect (deep-link precedence preserved)
  - No Dexie version/store declaration changes (git diff on the db schema module is empty)
  - tests/e2e/persistence.spec.ts paginated restore + save-on-turn cells pass; the deferral comment is gone
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/persistence.spec.ts && npx vitest run --project unit</automated>
  </verify>
  <done>Pure-paginated readers persist on turns and paginated reopens land on the saved page — both RESEARCH-verified gaps closed with existing machinery; scrolling behavior byte-stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RestorationMarker component + restore-effect swap + marker CSS + tests</name>
  <files>src/reader/RestorationMarker.tsx, src/routes/ArticleView.tsx, src/app.css, tests/unit/RestorationMarker.test.tsx, tests/e2e/toc/restoration-cue.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Component Inventory RestorationMarker — anatomy, both-mode attachment, lifecycle to the millisecond; §Interaction Contract rules 11-12; §Color — the narrowly-scoped accent consumption)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§RestorationMarker.tsx — announce discipline verbatim L33-47 + transient lifecycle L1594-1611; §Shared Patterns 2/5)
  - src/reader/ResumeBanner.tsx (the announce pattern to carry forward: role=status, aria-live=polite, aria-atomic=true, visually-hidden region — copy L33-47 discipline)
  - src/routes/ArticleView.tsx (restore effect — the swap site; the retired banner's state flag ~L243, auto-dismiss listeners ~L1594-1611, both handlers ~L1697-1715, render mount; queryBlocks; surfaceRef)
  - src/reader/restoreLocation.ts (offset → visible-block resolution for marker attach)
  - src/app.css (reduced-motion global gate L84-92; file-end placement rule; the page-fragment geometry the paginated marker lives within)
  - tests/e2e/chrome/mobile-first-page-chrome.spec.ts (the seed-location + reload + assert-restore harness shape L59-91)
  </read_first>
  <behavior>
  - Test: on mount, the component renders the visually-hidden role=status region containing exactly the announce copy "Returned to where you left off."
  - Test: the marker bar element is present with pointer-events none and no interactive descendants (no button, no a, no role=button anywhere inside).
  - Test: with fake timers, the bar gains the fading class at 3400ms and the component is unmounted by 4000ms.
  - Test: the lifecycle uses CSS transition classes only — no requestAnimationFrame-driven style writes in the component source.
  </behavior>
  <action>
  1. Create src/reader/RestorationMarker.tsx (UI-SPEC anatomy): a 4px solid
     var(--accent) bar (class restoration-marker), absolutely positioned
     (zero layout impact), border-radius 2px, pointer-events none.
     Scrolling mode: attached at the restored block — parent passes the
     restored offset; the marker resolves the attach element via the
     restoreLocation machinery + [data-block-index] on the VISIBLE surface
     (exclude the .article-body-measurement clone — Pitfall 7), sits at
     inset-inline-start calc(-1 * var(--space-md)) in the gutter, height =
     the block's rendered height. Paginated mode: attached to the restored
     PAGE — resolve the page fragment containing the restored offset via
     fragmentContainingOffset; 4px bar on that page fragment's inline-start
     edge, page height (D18-05's restored page edge). The announce region:
     visually-hidden div role="status" aria-live="polite" aria-atomic="true"
     with text "Returned to where you left off." (verbatim — carried forward
     from the retiring banner per D18-05). Lifecycle (D18-07): at 3400ms add
     class is-fading (a 600ms CSS opacity transition); at 4000ms unmount;
     timers cleaned up. ZERO JS animation code — the fade is a CSS
     transition the global reduced-motion gate kills (Pitfall 8); under
     reduced motion the bar clears instantly. No dismissal interaction
     exists.
  2. ArticleView swap: replace the retired banner wiring with marker mount
     state — the marker mounts ONLY when a reopen-restore genuinely landed
     (a saved LocationRecord existed AND the restore resolved this mount;
     never on first open with no location, never on TOC jumps, never on
     Highlights deep-links — D18-08; the jumpPendingRef guard from Task 1
     already suppresses deep-link arrivals). One-shot previous-value
     comparison (StrictMode idempotent).
  3. app.css at FILE END: .restoration-marker (the 4px accent bar geometry
     per mode, absolute positioning, pointer-events none, radius 2px),
     .restoration-marker.is-fading { opacity transition 600ms }, forced-
     colors mapping to CanvasText (shape-based survival). This is the ONE
     new sanctioned accent consumption (UI-SPEC §Color reserved-for list).
  4. tests/unit/RestorationMarker.test.tsx (RTL + vi.useFakeTimers) covering
     the four behavior bullets. Write RED → implement GREEN.
  5. tests/e2e/toc/restoration-cue.spec.ts — scrolling-mode cells on the
     mobile-first harness shape (raw IndexedDB location put → reload →
     assert): marker bar visible near the restored block; announce text
     present in the polite region; presence-then-absence with generous
     timeouts (fade window ~4s); first open with NO seeded location → no
     marker and no announce (marker honesty, rule 11); content not shifted
     (marker is overlay-only — article geometry byte-equal before/after
     mount). Paginated + 3-engine matrix cells land in Plan 18-04.
  </action>
  <acceptance_criteria>
  - src/reader/RestorationMarker.tsx contains role="status", aria-live="polite", aria-atomic="true", and the announce string "Returned to where you left off."
  - The marker source contains pointerEvents: "none" (or CSS pointer-events: none) and contains no rAF-driven style writes
  - Marker mount is gated on genuine restore-landing in ArticleView (no mount on first-open-no-location; jumpPending deep-link arrivals mount nothing)
  - tests/unit/RestorationMarker.test.tsx exits 0 (announce, passivity, 3400ms/4000ms lifecycle)
  - tests/e2e/toc/restoration-cue.spec.ts scrolling cells exit 0 including the first-open silence cell
  </acceptance_criteria>
  <verify>
  <automated>npx vitest run tests/unit/RestorationMarker.test.tsx && npx playwright test tests/e2e/toc/restoration-cue.spec.ts</automated>
  </verify>
  <done>The passive marker ships: cue at the restored location in both modes, honest gating (reopen-restore only), transient with reduced-motion-safe clear, zero interference — unit + scrolling e2e proven.</done>
</task>

<task type="auto">
  <name>Task 3: Banner retirement sweep across all four surfaces</name>
  <files>src/reader/ResumeBanner.tsx, src/routes/ArticleView.tsx, src/app.css, tests/e2e/chrome/mobile-first-page-chrome.spec.ts, tests/e2e/library/reading-views.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Component Inventory RETIRED block; §Copywriting Contract RETIRED strings; §Regression Targets deliberate-retirement paragraph)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§Shared Pattern 8 — the inventoried 4-surface retirement + acceptance grep)
  - src/reader/ResumeBanner.tsx (the file being deleted — confirm the announce copy extraction happened in Task 2 before deleting)
  - src/routes/ArticleView.tsx (post-Task-2 state — any remaining retired-banner references)
  - src/app.css (the two retired banner rule blocks: base styles ~L1009-1101 and the narrow-tuning block ~L3650-3692 — delete both as one inventoried edit)
  - tests/e2e/chrome/mobile-first-page-chrome.spec.ts (assertion site ~L79 + the geometry checks ~L84-91 that reference the retired banner's box — re-target the marker element)
  - tests/e2e/library/reading-views.spec.ts (assertion site ~L1017-1020 — replace with marker assertions; KEEP the restore-beats-h1 invariant: article h1 not focused)
  </read_first>
  <action>
  Complete the D18-06 retirement inventory (Task 2 already replaced the
  wiring; this task sweeps the remainder):

  1. Delete src/reader/ResumeBanner.tsx (the component file) — its announce
     discipline already survives verbatim inside RestorationMarker.
  2. Confirm zero remaining wiring references in ArticleView (the state
     flag, auto-dismiss listeners, both click handlers, and the render mount
     were swapped in Task 2 — remove any stragglers the swap left, including
     imports).
  3. Delete BOTH retired banner rule blocks from app.css (the base block and
     the narrow-tuning block identified in read_first) as one inventoried
     deletion — no orphaned selectors, no orphaned custom-property uses.
  4. Replace the two e2e assertion sites with marker assertions, each
     carrying a D18-06 citation comment in the spec: in
     mobile-first-page-chrome.spec.ts, the restore-surface assertion now
     targets the restoration marker + announce, and the adjacent geometry
     checks re-target the marker element's box; in reading-views.spec.ts,
     the restore-surface assertion becomes marker assertions while the
     restore-beats-h1 invariant (article h1 not focused) is preserved
     unchanged.
  5. Gate: after the sweep, the three retired copy strings enumerated in
     18-UI-SPEC §Copywriting Contract (RETIRED strings list) plus the
     retired CSS class family return ZERO hits across src/ and tests/.
     The announce copy that survives inside the marker is the one sanctioned
     carry-forward (D18-05).
  </action>
  <acceptance_criteria>
  - src/reader/ResumeBanner.tsx does not exist on disk
  - rg -q 'resume-banner|Resume reading|Start from top' over src/ and tests/ finds nothing (command in verify exits accordingly)
  - app.css contains neither retired banner rule block (both deleted whole)
  - Both modified specs pass with D18-06 citation comments present at the changed assertions
  - reading-views.spec.ts still asserts the article h1 is not focused after restore (invariant preserved)
  - The announce string "Returned to where you left off." still exists exactly once in src/ (inside RestorationMarker)
  </acceptance_criteria>
  <verify>
  <automated>! rg -q 'resume-banner|Resume reading|Start from top' src tests && npx playwright test tests/e2e/chrome/mobile-first-page-chrome.spec.ts tests/e2e/library/reading-views.spec.ts</automated>
  </verify>
  <done>The banner is fully retired across component, wiring, CSS, and both spec sites — grep-clean, marker assertions in place, announce copy surviving exactly where D18-05 puts it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted LocationRecord → restore behavior | existing validated store rows drive new paginated restore wiring |
| restore state → UI claim | the marker asserts "you are at the restored spot" — a false claim is the threat (honesty constraint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering (dishonest cue — marker claims a restore that didn't happen) | RestorationMarker gating | mitigate | Mount gated on genuine restore-landing (saved record existed AND resolved); first-open silence + deep-link suppression asserted in e2e (rule 11); paginated marker enabled only after Task 1's real restore wiring |
| T-18-07 | Tampering (location record integrity) | paginated save path | mitigate | Same LocationRecord shape through the existing Zod-validated saveLocation store; no new write surface; debounced + dual-flush prevents partial/rapid-turn corruption |
| T-18-08 | DoS-adjacent (restore loop / unbounded retry) | readiness gate | mitigate | Bounded rAF retry with RETRY_CAP_MS 5000 (deep-link template); failure degrades to page-1 landing with NO marker (honest absence) |
| T-18-09 | Repudiation adjacent (motion discipline bypass) | marker fade | mitigate | CSS transition only — the global prefers-reduced-motion gate kills it (Pitfall 8); unit test asserts no rAF style writes |
| T-18-SC | Tampering (package installs) | package.json | accept | Zero installs this phase (18-RESEARCH §Package Legitimacy Audit: none required) |
</threat_model>

## Artifacts this phase produces (Plan 18-03)

- `src/reader/RestorationMarker.tsx` — new component: transient passive restoration marker
- `tests/unit/RestorationMarker.test.tsx` — lifecycle/passivity RTL suite
- `tests/e2e/toc/restoration-cue.spec.ts` — new spec (scrolling cells; paginated + matrix cells in 18-04)
- Paginated save scheduler export in `src/reader/useScrollSave.ts` (name per implementation; feeds handleAnchorChange offsets into the debounced save)
- Readiness-gated paginated restore branch in `src/routes/ArticleView.tsx`
- Marker mount state + CSS classes `.restoration-marker`, `.restoration-marker.is-fading`
- DELETED: `src/reader/ResumeBanner.tsx`; both retired banner CSS blocks; 2 spec assertion sites replaced

<verification>
- `npx playwright test tests/e2e/persistence.spec.ts` exit 0 (paginated save + restore cells; deferral retired)
- `npx vitest run tests/unit/RestorationMarker.test.tsx && npx playwright test tests/e2e/toc/restoration-cue.spec.ts` exit 0
- Retirement grep gate clean; both banner-site specs green
- `npx vitest run --project unit` green overall
</verification>

<success_criteria>
- ORNT-06 closed end-to-end: reopen communicates restored location passively in BOTH modes — no content shift, no blocked page turns, no dismissal, transient with reduced-motion-safe clear
- Both RESEARCH-verified paginated gaps closed with existing machinery (D5-11/anchor/restoreLocation imports — no forks)
- Banner fully retired (D18-06) with grep-clean proof; strengthen-only outside the sanctioned retirement sites
</success_criteria>

<output>
Create `.planning/phases/18-reader-orientation/18-03-SUMMARY.md` when done
</output>
