---
phase: 18-reader-orientation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/toc.ts
  - tests/unit/toc.test.ts
  - src/reader/sectionSpy.ts
  - src/reader/SectionAnnouncer.tsx
autonomous: true
requirements: [ORNT-04]
must_haves:
  truths:
    - "deriveToc returns a synthetic Top entry first (offset 0) plus exactly one entry per h2-h6 heading block, each carrying its article-global D-05 grapheme offset from blockStartOffsets"
    - "Skipped heading levels (e.g. h2 followed by h5) produce deeper nesting depth with zero invented intermediate entries (D18-10)"
    - "Duplicate heading texts pass through as identical-text entries with no disambiguation suffixes (D18-11)"
    - "A headingless article derives to exactly one entry — the Top entry (D18-13 foundation)"
    - "SectionAnnouncer's announce behavior is byte-identical after the sectionSpy extraction — section-announce.spec.ts passes with zero spec diff (Pitfall 5)"
  artifacts:
    - path: "src/content/toc.ts"
      provides: "pure TOC structure derivation over the canonical article"
      exports: ["deriveToc", "TocEntry"]
    - path: "src/reader/sectionSpy.ts"
      provides: "selector-parameterized scroll-spy detection extracted from SectionAnnouncer"
    - path: "tests/unit/toc.test.ts"
      provides: "derivation invariant suite (skips, duplicates, offsets, headingless, h1 exclusion)"
  key_links:
    - from: "src/content/toc.ts"
      to: "src/content/normalizeText.ts"
      via: "articleGraphemeIndex(article).blockStartOffsets O(1) destination lookup — same substrate restore/pagination speak"
      pattern: "articleGraphemeIndex"
    - from: "src/reader/SectionAnnouncer.tsx"
      to: "src/reader/sectionSpy.ts"
      via: "announcer consumes the extracted detection with its exact current selector h2, h3, h4 — announce contract byte-stable"
      pattern: "sectionSpy"
---

<objective>
Ship the two pure foundation modules every Phase 18 surface consumes: the TOC
structure derivation (`src/content/toc.ts`) and the shared scroll-spy
extraction (`src/reader/sectionSpy.ts`).

Purpose: ORNT-04's semantic honesty (levels preserved, skips tolerated,
duplicates as-is) and D18-12's aria-current (shared detection, not a fork)
both need ONE derivation point and ONE detection implementation before any UI
lands. Zero persistence, zero DOM, zero React in `toc.ts` (Pitfall 9 — no
Dexie changes this phase).

Output: `deriveToc`/`TocEntry` + unit invariant suite; `sectionSpy.ts`
extraction with SectionAnnouncer refactored to consume it byte-stably.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-reader-orientation/18-CONTEXT.md
@.planning/phases/18-reader-orientation/18-RESEARCH.md
@.planning/phases/18-reader-orientation/18-PATTERNS.md
@.planning/phases/18-reader-orientation/18-UI-SPEC.md

@src/content/normalizeText.ts
@src/content/schema.ts
@src/pagination/anchor.ts
@src/reader/SectionAnnouncer.tsx
@tests/e2e/section-announce.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveToc pure module + invariant unit suite</name>
  <files>src/content/toc.ts, tests/unit/toc.test.ts</files>
  <read_first>
  - src/pagination/anchor.ts (module anatomy: contract-citing header comment, articleGraphemeIndex import, O(1) blockStartOffsets lookup in pageStartGlobalOffset L63-77 — copy this lookup shape)
  - src/content/normalizeText.ts (articleGraphemeIndex + blockStartOffsets, L137-195 — the D-05 substrate; import, never reimplement)
  - src/content/schema.ts (HeadingBlock L58-69: kind/level 1-6/content runs — NO ids exist on headings; CanonicalArticle; bookId L242)
  - tests/unit/normalizeText.test.ts (the pure-module Vitest shape: ArticleSchema.parse helper + baseArticle literal)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§src/content/toc.ts assignment + excerpts)
  </read_first>
  <behavior>
  - Test: article with headings h2,h3,h2 → deriveToc returns 4 entries: Top first (text "Top of article", offset 0, depth 0), then 3 heading entries whose offsets equal articleGraphemeIndex(article).blockStartOffsets at each heading's block index.
  - Test: skipped level (h2 followed directly by h5) → the h5 entry has depth 2 with NO intermediate entry invented; total entries = Top + 2.
  - Test: two headings with identical text → two entries with identical text fields, no suffixes, no prefixes (D18-11).
  - Test: article whose only heading is level 1 → deriveToc returns exactly [Top] (h1 is provenance-rendered, never a TOC entry).
  - Test: article with zero heading blocks of any level → deriveToc returns exactly [Top] (headingless foundation for D18-13).
  - Test: heading levels are preserved verbatim on entries (an h5 stays level 5, an h6 stays level 6 — ORNT-04).
  - Test: h5 and h6 headings appear as entries (the corpus gap RESEARCH flagged — no fixture ships them, unit must cover).
  </behavior>
  <action>
  Create src/content/toc.ts as a pure domain module mirroring anchor.ts anatomy:
  contract-citing header comment, imports from ../content/normalizeText and
  ../content/types, no DOM/React/side effects, jsdom-safe.

  Export interface TocEntry with fields: text (string — heading runs joined,
  duplicates AS-IS per D18-11), offset (number — article-global D-05 grapheme
  offset of the heading block's start), blockIndex (number — top-level block
  index for [data-block-index] DOM resolution), level (2|3|4|5|6 — source
  heading level preserved verbatim per ORNT-04), depth (number — nesting
  depth among entries, 0 = shallowest).

  Export function deriveToc(article: CanonicalArticle): TocEntry[]:
  1. First emit the synthetic Top entry per D18-09: text "Top of article",
     offset 0, blockIndex -1 (sentinel — the Top destination targets the
     article h1, not a body block), depth 0, level 2.
  2. Then walk article.blocks once; for each block with kind "heading" AND
     level !== 1: text = block.content.map((r) => r.text).join(""), offset =
     articleGraphemeIndex(article).blockStartOffsets[blockIndex] (the O(1)
     prefix-sum lookup, same pattern as pageStartGlobalOffset in anchor.ts),
     level from the block.
  3. depth from a level stack (push/pop like computeDepth in RESEARCH Pattern
     3): popped when a heading of shallower-or-equal level arrives; skipped
     levels (h2→h5) deepen depth with NO invented intermediate entries
     (D18-10). The Top entry's depth is 0 and the first body entry is depth 0
     among body entries — the renderer derives ul nesting from depth
     transitions.

  Write tests/unit/toc.test.ts FIRST (RED), then implement until GREEN.
  Follow the normalizeText.test.ts shape: parseArticle helper via
  ArticleSchema.parse, one baseArticle literal, table-driven cases covering
  every <behavior> bullet.
  </action>
  <acceptance_criteria>
  - src/content/toc.ts contains `export function deriveToc(` and `export interface TocEntry`
  - src/content/toc.ts imports articleGraphemeIndex from ./normalizeText (no forked offset math — no manual grapheme accumulation loops)
  - tests/unit/toc.test.ts exits 0 with cases covering: Top-first, skips-nest-without-invention, duplicates-as-is, h1 exclusion, headingless → Top-only, level preservation, h5/h6 presence
  - `npx vitest run tests/unit/toc.test.ts` exit 0
  </acceptance_criteria>
  <verify>
  <automated>npx vitest run tests/unit/toc.test.ts</automated>
  </verify>
  <done>deriveToc + TocEntry ship pure and unit-proven: Top entry first at offset 0, one entry per h2-h6 heading with blockStartOffsets destinations, skips nest deeper without invention, duplicates identical, h1 excluded, headingless → Top-only.</done>
</task>

<task type="auto">
  <name>Task 2: sectionSpy extraction — SectionAnnouncer consumes shared detection byte-stably</name>
  <files>src/reader/sectionSpy.ts, src/reader/SectionAnnouncer.tsx</files>
  <read_first>
  - src/reader/SectionAnnouncer.tsx (the extraction source: detection effect L48-113, HEADER_PX=48 L34, ANNOUNCE_DEBOUNCE_MS=250 L37, announce string L76, status region L115-124 — every constant and the cleanup shape L104-112)
  - tests/e2e/section-announce.spec.ts (the byte-stable contract — this spec MUST pass unchanged; Pitfall 5's warning sign is any diff here)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§src/reader/sectionSpy.ts — verbatim extraction excerpt + contract)
  </read_first>
  <action>
  Extract SectionAnnouncer's detection into src/reader/sectionSpy.ts with a
  heading-selector PARAMETER (Pitfall 5 — this is the whole point):

  1. New module exports a detection hook (shape: useSectionSpy or an
     equivalent create/watch function — match the file's natural style)
     taking: the article element, a heading selector string, and a callback
     invoked with the current heading ELEMENT (not just text — the TOC maps
     the element's data-block-index to its aria-current entry later).
  2. Carry over VERBATIM from SectionAnnouncer L48-113: the detect() rule
     (most-recently-passed heading with getBoundingClientRect().top <
     HEADER_PX + 8), IntersectionObserver with rootMargin
     "-48px 0px -60% 0px" + threshold [0], the rAF-throttled passive window
     scroll fallback, the 250ms debounce, and FULL cleanup (obs.disconnect,
     removeEventListener, cancelAnimationFrame, clear debounce timer).
  3. Rewrite SectionAnnouncer.tsx to consume the extraction with its EXACT
     current selector "h2, h3, h4". The announce string (Section: {text}.),
     copy, timing, and visible behavior stay byte-identical — the spec diff
     must be zero. Only the detection wiring is replaced by the shared call.
  4. No behavior change for the announcer's empty-headings early return.

  Do NOT add the TOC's h2-h6 selector anywhere in this task — the
  parameterization is what ships; TocPanel (Plan 18-02) passes
  "h2, h3, h4, h5, h6".
  </action>
  <acceptance_criteria>
  - src/reader/sectionSpy.ts exists and its detection accepts a heading-selector parameter (no hardcoded "h2, h3, h4" inside the shared module)
  - src/reader/SectionAnnouncer.tsx no longer contains inline IntersectionObserver wiring — it consumes the shared module with selector "h2, h3, h4"
  - tests/e2e/section-announce.spec.ts is byte-unchanged (git diff shows no modification to this spec) and passes
  - The shared module's callback surfaces the heading element (so data-block-index mapping is possible downstream)
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/section-announce.spec.ts && npx vitest run tests/unit/toc.test.ts</automated>
  </verify>
  <done>One scroll-spy implementation, two future parameterizations: SectionAnnouncer behavior byte-stable (spec green, zero diff), selector parameterization shipped for the TOC's aria-current (D18-12 reuse, not a fork).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical article → derived TOC data | Zod-validated blocks (ArticleSchema.parse at ingest) flow into a new derived-data module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering (XSS via heading text) | src/content/toc.ts | mitigate | Pure data module — no DOM, no HTML construction; TocEntry.text is the same run text the body renders; the render site (Plan 18-02) renders it as React text children only (react/no-danger enforced) |
| T-18-02 | Tampering (DOM clobbering via invented ids) | src/content/toc.ts | mitigate | No string ids invented — schema carries none; destinations are numeric blockIndex + offset only (footnote-id/Pitfall-4 discipline) |
| T-18-SC | Tampering (npm/pip/cargo installs) | package.json | accept | Zero package installs this phase — 18-RESEARCH §Package Legitimacy Audit: none required; browser platform + existing modules only |
</threat_model>

## Artifacts this phase produces (Plan 18-01)

- `src/content/toc.ts` — new module: `deriveToc(article: CanonicalArticle): TocEntry[]`, `TocEntry` interface `{ text, offset, blockIndex, level: 2|3|4|5|6, depth }`
- `src/reader/sectionSpy.ts` — new module: selector-parameterized scroll-spy detection (hook/function consumed by SectionAnnouncer and later TocPanel)
- `tests/unit/toc.test.ts` — derivation invariant suite

<verification>
- `npx vitest run tests/unit/toc.test.ts` exit 0 (RED→GREEN committed atomically)
- `npx playwright test tests/e2e/section-announce.spec.ts` exit 0 with zero spec diff (Pitfall 5 gate)
- Unit suite overall green: `npx vitest run --project unit` (no regressions from the SectionAnnouncer refactor)
</verification>

<success_criteria>
- deriveToc ships pure with Top-first + blockStartOffsets destinations (ORNT-04 foundation, D18-09/10/11 encoded and unit-proven)
- sectionSpy extraction is behavior-invisible: announcer contract byte-stable
- No Dexie/schema/persistence changes (Pitfall 9)
</success_criteria>

<output>
Create `.planning/phases/18-reader-orientation/18-01-SUMMARY.md` when done
</output>
