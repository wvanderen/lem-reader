---
phase: 18-reader-orientation
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - tests/e2e/toc/_corpus.ts
  - tests/e2e/toc/toc-navigation.spec.ts
  - tests/e2e/toc/toc-geometry.spec.ts
  - tests/e2e/toc/restoration-cue.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ORNT-01, ORNT-03, ORNT-04, ORNT-05, ORNT-06]
must_haves:
  truths:
    - "Every ORNT behavior is proven in real browsers across chromium, firefox, and webkit — including skipped levels, duplicates, h5/h6, headingless articles, and an EPUB chapter (ORNT-04, D18-14)"
    - "The same TOC entry lands on the same structural destination in both reading modes, proven cross-engine (ORNT-03)"
    - "At 320px and 400% zoom: opening/closing the TOC traps no focus, obscures nothing inertly, and changes no logical location; the 5-button header neither wraps nor overflows; collapsed destinations un-clip on focus (ORNT-05)"
    - "The restoration marker is proven transient, passive, announce-carrying, and honest in BOTH modes cross-engine (ORNT-06)"
    - "The full test suite passes in one honest invocation — npm run test exit 0 with literal counts recorded (the 04-11/09-07 lessons)"
  artifacts:
    - path: "tests/e2e/toc/_corpus.ts"
      provides: "seeded TOC corpus (h2→h4 skip, duplicate texts, h5/h6, EPUB chapter) via seedArticleRows discipline"
    - path: "tests/e2e/toc/toc-geometry.spec.ts"
      provides: "320px/400% no-trap, no-shift, staged-collapse, touch-target, axe cells joined to the D6-09 invariant"
  key_links:
    - from: "tests/e2e/toc/toc-geometry.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "assertEdgeInvariant (D6-09) applied to the open-panel state"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/toc/toc-navigation.spec.ts"
      to: "tests/e2e/library/reading-views.spec.ts"
      via: "seedArticleRows seeding discipline copied from L506-535"
      pattern: "seedArticleRows"
---

<objective>
Prove Phase 18 across the full 3-engine matrix and close the phase with the
honest full-suite gate.

Purpose: geometry, focus, scroll, and restore truth belongs to Playwright
across chromium/firefox/webkit (STACK rule — jsdom is not authoritative for
layout). The corpus gap RESEARCH flagged (no fixture ships h4-h6, skips, or
duplicates) closes here; ORNT-01/03/04/05/06 flip complete on this evidence.

Output: seeded TOC corpus + extended navigation/restoration specs + new
geometry spec + REQUIREMENTS.md checkboxes + the recorded honest gate.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-reader-orientation/18-CONTEXT.md
@.planning/phases/18-reader-orientation/18-RESEARCH.md
@.planning/phases/18-reader-orientation/18-PATTERNS.md
@.planning/phases/18-reader-orientation/18-UI-SPEC.md
@.planning/phases/18-reader-orientation/18-01-SUMMARY.md
@.planning/phases/18-reader-orientation/18-02-SUMMARY.md
@.planning/phases/18-reader-orientation/18-03-SUMMARY.md

@tests/e2e/toc/toc-navigation.spec.ts
@tests/e2e/toc/restoration-cue.spec.ts
@tests/e2e/chrome/tag-popover.spec.ts
@tests/e2e/library/reading-views.spec.ts
@tests/e2e/_edge-invariant.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded TOC corpus + navigation matrix extension (skips, duplicates, h5/h6, chapter, aria-current)</name>
  <files>tests/e2e/toc/_corpus.ts, tests/e2e/toc/toc-navigation.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-RESEARCH.md (§Validation Architecture Wave 0 gaps — the corpus list + test map per requirement)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§toc-navigation.spec.ts assignment — harness conventions; §reading-views.spec seedArticleRows L506-535 excerpt)
  - tests/e2e/library/reading-views.spec.ts (seedArticleRows implementation L506-535 — ArticleSchema.parse in Node → raw IndexedDB puts; chapter rows carry denormalized bookId)
  - tests/e2e/toc/toc-navigation.spec.ts (the core cells from Plan 18-02 — extend, never rewrite)
  - tests/e2e/chrome/tag-popover.spec.ts (prepareFreshPage + readiness sentinel + zero-fixed-sleeps + webkit focus-exception conventions)
  - src/fixtures/index.ts (confirm which existing fixture is headingless for the D18-13 cell — RESEARCH names essay-long-form)
  </read_first>
  <action>
  1. Create tests/e2e/toc/_corpus.ts with seedArticleRows-discipline helpers
     (ArticleSchema.parse in Node → raw IndexedDB puts; NEVER deleteDatabase
     — clear-rows only) seeding: (a) an article with an h2→h4 skip (skipped
     levels); (b) an article with two identical heading texts (duplicates);
     (c) an article with h5 and h6 headings present; (d) one EPUB chapter
     row with denormalized bookId (D18-14 — chapters are articles). Each
     seeded article is long enough to require real scrolling/pagination for
     jump assertions.
  2. EXTEND tests/e2e/toc/toc-navigation.spec.ts (strengthen-only — the
     Plan 18-02 core cells stay byte-stable) with corpus-driven cells,
     asserting on the DOM the reader actually renders:
     - Skips: the h4 under an h2 renders inside a deeper nested ul with NO
       intermediate li (assert list-structure depth, not visual indent).
     - Duplicates: two entries with identical accessible names, both
       navigable, each landing on its own heading (position disambiguates —
       D18-11).
     - Levels: h5/h6 entries exist and preserve their heading level in the
       rendered article (h5 renders as h5 — ORNT-04).
     - Chapter: the chapter article gets the identical trigger + panel
       machinery from its own heading hierarchy (D18-14).
     - Headingless: on the headingless fixture, the panel shows the Top
       entry + the note "This article has no headings." and the trigger is
       present (never peekaboo — D18-13).
     - aria-current: follows scroll in scrolling mode and page turns in
       paginated mode (D18-12); above the first heading the Top entry
       carries it.
     - Enter activation on a corpus entry jumps without re-routing (hash
       unchanged; article not remounted — Pitfall 4).
     - Both-modes equivalence on the skip corpus (ORNT-03): the same entry
       focuses the same heading text after toggling modes.
  3. All cells run on the project's 3-engine matrix (chromium/firefox/
     webkit); zero fixed sleeps (expect/expect.poll only); webkit keeps the
     documented focus-restore exception shape where the tag-popover
     precedent applies.
  </action>
  <acceptance_criteria>
  - tests/e2e/toc/_corpus.ts exists and seeds all four corpus shapes (skip, duplicates, h5/h6, chapter with bookId) via ArticleSchema-validated rows
  - toc-navigation.spec.ts contains cells asserting nested-ul depth for the skip, identical accessible names for duplicates, the headingless note string, chapter TOC parity, and aria-current following both scroll and page turns
  - The Plan 18-02 core cells are byte-stable (git diff shows additive changes only)
  - `npx playwright test tests/e2e/toc/toc-navigation.spec.ts` exit 0 on all three engines
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/toc/toc-navigation.spec.ts</automated>
  </verify>
  <done>The corpus gap is closed and ORNT-04's full honesty matrix — skips, duplicates, deep levels, headingless, chapters — plus aria-current and mode equivalence are proven in real browsers on all three engines.</done>
</task>

<task type="auto">
  <name>Task 2: toc-geometry spec (320px/400%, no-trap, no-shift, staged collapse, axe) + paginated restoration cells</name>
  <files>tests/e2e/toc/toc-geometry.spec.ts, tests/e2e/toc/restoration-cue.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Layout Contract mandatory-e2e paragraph for the staged collapse; §Interaction Contract rules 1, 5, 12; §Regression Targets 320px suites row)
  - tests/e2e/_edge-invariant.ts (assertEdgeInvariant — the D6-09 helper; note the visible-blocks [data-block-index] :not(.article-body-measurement) scoping)
  - tests/e2e/reflow.spec.ts, tests/e2e/high-zoom.spec.ts, tests/e2e/touch-targets.spec.ts (the edge suites the new spec joins; the high-zoom viewport pattern setViewportSize 320x800 as the load-bearing reflow assertion per STATE D6-10)
  - tests/e2e/toc/restoration-cue.spec.ts (the scrolling cells from Plan 18-03 — extend with paginated + matrix cells)
  - tests/e2e/persistence.spec.ts (Task 18-03 seeding shape for paginated location records)
  </read_first>
  <action>
  1. Create tests/e2e/toc/toc-geometry.spec.ts joining the existing edge
     matrix (same harness conventions; assertEdgeInvariant applied to the
     OPEN-panel state):
     - 320x640 and 400% zoom (320x800 viewport) in Reader: with the panel
       open AND closed, assertEdgeInvariant holds (keyboard content in both
       modes + required functions + no overflow).
     - No trap: Tab from the panel's last entry reaches page content (rail
       geometry AND full-width sheet geometry); Esc closes from anywhere in
       the panel; the page behind the open sheet remains keyboard-escapable
       (D18-04).
     - No location change: opening/closing the panel leaves the scroll
       offset (scrolling) / current page index (paginated) byte-stable; no
       repagination diagnostic fires on open (Pitfall 7's warning sign).
     - 5-button header at 320px: the controls row neither wraps nor
       overflows the viewport nor silently overlaps (.header-start
       flex-shrink honesty); all five article-scoped buttons ≥44px.
     - Staged collapse: below 420px in Reader the shell-nav links are
       clipped but still keyboard-reachable (Tab reaches them) and a focused
       collapsed link becomes visible (the :focus-visible un-clip).
     - Axe on the open panel: AxeBuilder with the project's WCAG tags,
       zero serious/critical findings (the tag-popover spec precedent).
  2. EXTEND tests/e2e/toc/restoration-cue.spec.ts (strengthen-only) with
     the paginated cells + cross-engine presence: seed location → turn/read
     in paginated mode → reload → lands on the saved page with the marker at
     that page's inline-start edge + the announce present; marker
     presence-then-absence with generous fade-window timeouts; no
     interactive element inside the marker (no dismissal exists); page
     turns still work while the marker is visible (pointer-events none);
     content geometry unchanged by marker mount (no shift — ORNT-06);
     reduced-motion emulation cell: the marker clears without transition
     (instant state change).
  </action>
  <acceptance_criteria>
  - tests/e2e/toc/toc-geometry.spec.ts exists, imports assertEdgeInvariant from ../_edge-invariant, and contains cells for: no-trap (Tab escape at both geometries), no-location-change (both modes), 5-button ≥44px no-wrap/no-overflow, collapsed-link keyboard reachability + focus un-clip, and an AxeBuilder zero-serious/critical pass
  - restoration-cue.spec.ts contains paginated marker-at-page-edge cells, a no-dismissal assertion, a page-turns-unblocked assertion, and a reduced-motion clear cell
  - Both specs exit 0 on chromium, firefox, and webkit
  - No fixed sleeps introduced in either file
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/toc/toc-geometry.spec.ts tests/e2e/toc/restoration-cue.spec.ts</automated>
  </verify>
  <done>ORNT-05's calm-geometry contract and ORNT-06's both-mode marker behavior are proven at the edges — 320px, 400% zoom, sheet geometry, staged collapse — on all three engines with the shared D6-09 invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite gate + requirements completion</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
  - .planning/REQUIREMENTS.md (§Reader Orientation ORNT-01/03/04/05/06 rows + §Traceability Phase 18 rows — the checkboxes this task flips)
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Regression Targets — the byte-stable list + deliberate retirements this gate audits against)
  - git log/diff of tests/e2e since phase start (the strengthen-only audit scope)
  </read_first>
  <action>
  1. Strengthen-only audit: review the phase's full diff on tests/e2e — the
     ONLY legitimate modifications outside tests/e2e/toc/ are the two
     banner-retirement sites (D18-06-cited) and the persistence.spec
     option-(b) retirement (UI-SPEC-cited). Anything else touching
     byte-stable anchors (pagination/annotations/library specs) is a
     violation to fix before the gate.
  2. Honest gate: run the FULL suite in ONE invocation — npm run test (unit
     + e2e across chromium/firefox/webkit + throttled perf). Record the
     literal pass/fail/skip counts in the SUMMARY (the 04-11 misreport and
     09-07 honest-gate lessons: run it yourself, record fail=0 honestly; a
     red first run is recorded, fixed atomically, and re-run — never
     masked). The gate is green only at exit 0.
  3. Flip .planning/REQUIREMENTS.md: mark ORNT-01, ORNT-03, ORNT-04,
     ORNT-05, ORNT-06 checked in §Reader Orientation and Complete in the
     §Traceability table rows for Phase 18 — each backed by this phase's
     spec evidence (navigation/geometry/restoration-cue/persistence specs).
  </action>
  <acceptance_criteria>
  - `npm run test` exits 0 in one invocation; SUMMARY records literal counts (e.g. "X passed / 0 failed / Y skipped")
  - The strengthen-only audit finds no spec-anchor modifications beyond the three sanctioned retirement sites
  - .planning/REQUIREMENTS.md shows ORNT-01, ORNT-03, ORNT-04, ORNT-05, ORNT-06 checked and their Traceability rows marked Complete
  </acceptance_criteria>
  <verify>
  <automated>npm run test</automated>
  </verify>
  <done>Phase 18 closes honestly: full suite green in one recorded invocation, strengthen-only discipline audited, and all five ORNT requirements flipped complete on real-browser evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test corpus → IndexedDB | seeded article rows cross into browser storage (ArticleSchema.parse in Node guards row shape) |
| e2e assertions → CI gate | the honest gate's integrity depends on running the full suite, not subsets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering (corpus row injection) | tests/e2e/toc/_corpus.ts | mitigate | All seeded rows pass ArticleSchema.parse in Node before raw puts (the seedArticleRows discipline — same validation as production reads) |
| T-18-11 | Repudiation (gate integrity — subset/misreported runs) | honest full-suite gate | mitigate | Single full `npm run test` invocation; literal counts recorded in SUMMARY; strengthen-only audited via git diff (the 04-11/09-07 lessons are the precedent) |
| T-18-12 | Tampering (a11y regression through the new overlay) | geometry + axe cells | mitigate | assertEdgeInvariant on open/closed panel states + AxeBuilder zero serious/critical + no-trap Tab cells across all three engines |
| T-18-SC | Tampering (package installs) | package.json | accept | Zero installs this phase (18-RESEARCH §Package Legitimacy Audit: none required) |
</threat_model>

## Artifacts this phase produces (Plan 18-04)

- `tests/e2e/toc/_corpus.ts` — seeded TOC corpus helpers (skip/duplicates/h5-h6/chapter shapes)
- `tests/e2e/toc/toc-geometry.spec.ts` — new edge-matrix spec
- Extended cells in `tests/e2e/toc/toc-navigation.spec.ts` and `tests/e2e/toc/restoration-cue.spec.ts`
- `.planning/REQUIREMENTS.md` — ORNT-01/03/04/05/06 flipped Complete

<verification>
- `npx playwright test tests/e2e/toc/` exit 0 on chromium, firefox, webkit (all three phase specs)
- `npm run test` exit 0 — the honest full-suite gate with recorded counts
- REQUIREMENTS.md Phase 18 rows complete
</verification>

<success_criteria>
- All five ORNT requirements proven in real browsers on all three engines and flipped complete
- The corpus gap (no fixture with skips/duplicates/h4-h6) closed with schema-valid seeds
- Strengthen-only discipline holds: zero byte-stable-anchor modifications beyond the three sanctioned retirement sites
- Honest gate green in one invocation with literal counts in the record
</success_criteria>

<output>
Create `.planning/phases/18-reader-orientation/18-04-SUMMARY.md` when done
</output>
