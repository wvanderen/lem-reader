---
phase: 18-reader-orientation
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/reader/TocPanel.tsx
  - tests/unit/TocPanel.test.tsx
  - src/reader/Header.tsx
  - src/App.tsx
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/e2e/toc/toc-navigation.spec.ts
autonomous: true
requirements: [ORNT-01, ORNT-03, ORNT-04, ORNT-05]
must_haves:
  truths:
    - "Reader can open a labeled table of contents from the shell header on any article, headingless or not — the trigger never plays peekaboo (D18-02, D18-13)"
    - "Activating an entry closes the panel and moves keyboard focus to the destination heading with a visible focus cue, in scrolling mode (scroll) and paginated mode (page turn) through the same canonical offset (D18-03, ORNT-03)"
    - "The same TOC entry lands on the same structural destination in both reading modes — destinations are D-05 grapheme offsets, never page numbers or DOM identity (ORNT-03)"
    - "Skipped levels nest deeper without invented entries; duplicate headings are identical-text links; the list is a semantic nested ul of links (ORNT-04, D18-10, D18-11)"
    - "The current section's entry carries aria-current=true plus weight/stroke emphasis, following the shared sectionSpy (D18-12)"
    - "Opening or closing the panel never traps focus, never scrolls the page, never turns a page, never fires repagination — at rail and full-width-sheet geometry (D18-01, D18-04, ORNT-05)"
    - "At ≤420px in Reader the header keeps one 48px row with all five 44px article-scoped buttons; collapsed shell-nav links stay keyboard-reachable and un-clip on :focus-visible"
  artifacts:
    - path: "src/reader/TocPanel.tsx"
      provides: "non-modal popover=manual TOC panel (nested ul, Top entry, aria-current, internal scroll, headingless note)"
    - path: "tests/unit/TocPanel.test.tsx"
      provides: "RTL component suite (render semantics, aria-current, headingless note, Esc routing)"
    - path: "tests/e2e/toc/toc-navigation.spec.ts"
      provides: "core open/jump/focus e2e cells (both modes) — extended by Plan 18-04"
    - path: "src/app.css"
      provides: ".toc-panel/.toc-trigger/.toc-list styles + ≤420px staged shell-nav collapse (additive, file-end placement)"
  key_links:
    - from: "src/reader/TocPanel.tsx"
      to: "src/content/toc.ts"
      via: "renders deriveToc(article) entries as the nested list"
      pattern: "deriveToc"
    - from: "src/reader/TocPanel.tsx"
      to: "src/reader/sectionSpy.ts"
      via: "consumes shared detection with selector 'h2, h3, h4, h5, h6' → aria-current entry mapping"
      pattern: "sectionSpy|useSectionSpy"
    - from: "src/routes/ArticleView.tsx"
      to: "src/pagination/anchor.ts"
      via: "paginated jump: fragmentContainingOffset(pages, offset, article) → surfaceRef.turnToPage (D5-11 tail)"
      pattern: "fragmentContainingOffset"
    - from: "src/routes/ArticleView.tsx"
      to: "src/reader/restoreLocation.ts"
      via: "scrolling jump: findScrollTarget(article, queryBlocks(el), offset) → scrollIntoView (D5-11 tail)"
      pattern: "findScrollTarget"
    - from: "src/reader/Header.tsx"
      to: "src/reader/TocPanel.tsx"
      via: "controlled open state — trigger aria-expanded; ONE toggle-event close seam with focus-restore (Pattern 1)"
      pattern: "toggle"
---

<objective>
Ship the reader-facing TOC: the non-modal TocPanel (popover=manual), the 5th
article-scoped header trigger, the mode-aware jump handler, and the ≤420px
staged header collapse.

Purpose: ORNT-01 (open + jump), ORNT-03 (canonical destinations both modes),
ORNT-04 (semantic list semantics), ORNT-05 (calm geometry) — the panel is the
first non-dialog overlay in the codebase (D18-01 deliberately breaks the
5-dialog precedent), so its focus/Esc contract is documented explicitly in
every task.

Output: TocPanel.tsx + Header/App/ArticleView wiring + panel/trigger/collapse
CSS + core navigation e2e cells.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-reader-orientation/18-CONTEXT.md
@.planning/phases/18-reader-orientation/18-RESEARCH.md
@.planning/phases/18-reader-orientation/18-PATTERNS.md
@.planning/phases/18-reader-orientation/18-UI-SPEC.md
@.planning/phases/18-reader-orientation/18-01-SUMMARY.md

@src/routes/ArticleView.tsx
@src/reader/Header.tsx
@src/reader/SectionAnnouncer.tsx
@src/reader/PaginatedSurface.tsx
@src/reader/restoreLocation.ts
@src/pagination/anchor.ts
@src/app.css
@tests/e2e/chrome/tag-popover.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TocPanel component + panel CSS + RTL component suite</name>
  <files>src/reader/TocPanel.tsx, tests/unit/TocPanel.test.tsx, src/app.css</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Component Inventory TocPanel — anatomy verbatim; §Layout Contract — rail/sheet geometry; §Copywriting Contract — the five chrome strings; §Interaction Contract rules 1-9)
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§TocPanel.tsx — tag-popover seam excerpt L338-375, entry markup, manual-Esc snippet)
  - src/routes/ArticleView.tsx (tag popover controlled seam L333-375 — the mechanical template being adapted)
  - src/content/toc.ts (from Plan 18-01 — TocEntry shape consumed)
  - src/reader/SectionAnnouncer.tsx (how the announcer consumes sectionSpy — mirror the call shape)
  - src/app.css (tag-popover/.status surface classes; the ≤639px wordmark collapse declarations L3725+ the sheet siblings; file-end placement rule)
  - tests/unit/annotations/note-popover-confirm.test.tsx (RTL component-test shape in this repo)
  </read_first>
  <behavior>
  - Test: renders an h2 "Contents" title and a nav with aria-label "Table of contents" (ORNT-01 labeled contract).
  - Test: first entry is a link with text "Top of article"; body entries render heading text as-is — a fixture with duplicate heading texts renders two identical-text links (D18-11).
  - Test: depth transitions render nested ul structure — an h2→h5 skip produces a deeper nested list with no intermediate li (D18-10).
  - Test: exactly one entry carries aria-current with token "true" (never the token shell-nav uses for other-page links); when no body heading is current, the Top entry carries it.
  - Test: headingless article (entries = [Top]) renders the note "This article has no headings." below the title (D18-13).
  - Test: entry activation calls the onActivate callback with the entry (click and Enter) and calls preventDefault (link semantics without router side effects).
  - Test: Escape keydown calls hidePopover on the panel element (manual popover gets no native Esc — the only hand-rolled key handling).
  </behavior>
  <action>
  Create src/reader/TocPanel.tsx per UI-SPEC §Component Inventory:

  1. Container: div with popover="manual" and class toc-panel. NO
     role="dialog", NO aria-modal, NO aria-haspopup anywhere on the surface
     (Pitfall 3 — the NotePopover VoiceOver blocker history forbids claiming
     dialog semantics on a non-modal surface). Inside: h2 class toc-title
     with text "Contents", then nav aria-label "Table of contents" wrapping
     the list.
  2. Entries: derive entries via deriveToc(article); render a nested ul from
     depth transitions (deeper entry opens a child ul inside the parent li;
     a ul-level indent class per depth). Each entry is a real a element with
     fragment-only href built as "#toc-" + blockIndex (Top uses its
     sentinel), aria-current="true" only on the current entry, text as React
     text children (auto-escaped), onClick preventDefault + onActivate(entry).
     Entries NEVER truncate (no ellipsis, no line clamp — hidden destination
     text would be silent garbage).
  3. aria-current mapping: consume sectionSpy with selector
     "h2, h3, h4, h5, h6" scoped to the visible article element; map the
     current heading element's data-block-index to the matching entry; above
     the first heading the Top entry is current (D18-12).
  4. Open behavior: on open, focus moves to the aria-current entry (or the
     first entry); the panel scrolls internally to bring that entry toward
     vertical center using offsetTop/scrollTop arithmetic on the panel
     element — the element method that scrolls ancestors is FORBIDDEN here
     (D18-15: no page-level scroll side effects from opening).
  5. Esc: keydown handler on the panel routes Escape through
     panelRef.current.hidePopover() so the single toggle seam fires (the
     parent owns the seam + focus restore).
  6. Headingless state: entries [Top] only + the note
     "This article has no headings." in class toc-empty (D18-13).

  app.css additions placed at FILE END (the cascade lesson — earlier
  same-specificity rules lose): .toc-panel (position fixed via popover top
  layer; width min(320px, 100vw - 2*var(--space-md)); inset-inline-end
  var(--space-md); top calc(48px + var(--space-xs)); max-height
  calc(100dvh - 48px - 2*var(--space-xs)); overflow-y auto; background
  var(--surface-raised); 1px solid var(--hairline) border; border-radius 4px;
  padding var(--space-md)/var(--space-lg) rhythm); media max-width 639px →
  .toc-panel { inset-inline: 0 } (full-width sheet — still non-inert);
  .toc-list entries (min-height var(--touch); font-size 14px; weight 400;
  nested ul indent var(--space-md) per depth); the aria-current entry state
  (font-weight 600 + text-decoration-thickness 2px — emphasis by weight and
  stroke, never color alone; forced-colors safe); .toc-empty note
  (var(--ink-soft), 14px). Panel open/close is an instant state change —
  ZERO new transition/animation properties (A11Y-06; UI-SPEC rule 15). No new
  custom properties anywhere.

  Write tests/unit/TocPanel.test.tsx (RTL) covering every behavior bullet;
  jsdom is sanctioned for this component-glue level (STACK rule — layout
  truth stays in Playwright, Plan 18-04).
  </action>
  <acceptance_criteria>
  - src/reader/TocPanel.tsx contains `popover="manual"` on the container and the string `Table of contents` (nav aria-label) and `Contents` (visible h2)
  - TocPanel.tsx does NOT contain role="dialog" or aria-modal or aria-haspopup
  - aria-current uses the token "true" (grep the component: no aria-current="page")
  - The panel open-scroll code contains offsetTop/scrollTop arithmetic and contains no ancestor-scrolling element method
  - tests/unit/TocPanel.test.tsx exits 0 covering all seven behavior bullets
  - app.css gains .toc-panel rules referencing only existing custom properties (no new custom property declarations introduced)
  </acceptance_criteria>
  <verify>
  <automated>npx vitest run tests/unit/TocPanel.test.tsx</automated>
  </verify>
  <done>TocPanel renders the labeled, semantically-nested, aria-current-tracking non-modal panel with honest headingless state; CSS consumes existing tokens only; component suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Header trigger + App/ArticleView wiring + mode-aware jump + core e2e cells</name>
  <files>src/reader/Header.tsx, src/App.tsx, src/routes/ArticleView.tsx, tests/e2e/toc/toc-navigation.spec.ts</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-PATTERNS.md (§Header.tsx — tags-trigger block L177-188 to mirror; §ArticleView.tsx — D5-11 jump tail L1732-1776 + deep-link readiness L1343-1455 + tag seam; prop-trio note)
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Component Inventory Header/ArticleView extensions; §Interaction Contract rules 2-8, 14, 17-18; §Layout Contract trigger anatomy)
  - src/reader/Header.tsx (tags-trigger anatomy + the articleScoped/articleMounted gate + header icon glyphs L301-318; the tagsOpen/onToggleTags prop declarations L73-81)
  - src/App.tsx (how tagsOpen state is owned and threaded to Header + ArticleView — mirror for tocOpen)
  - src/routes/ArticleView.tsx (tag popover seam L333-375; handleNavigateBack L1732-1776 — the jump tail to copy; queryBlocks selector; surfaceRef; articleH1Ref)
  - src/reader/PaginatedSurface.tsx (PaginatedSurfaceHandle: turnToPage/getPages L147-178)
  - src/pagination/anchor.ts (fragmentContainingOffset)
  - src/reader/restoreLocation.ts (findScrollTarget)
  - tests/e2e/chrome/tag-popover.spec.ts (harness conventions wholesale: prepareFreshPage, seed helpers, readiness sentinel, focus-restore webkit exception, zero fixed sleeps)
  </read_first>
  <action>
  1. Header.tsx — 5th article-scoped trigger, FIRST in the group so Reader
     shows [contents][tags][annotations][mode][gear] (D18-02): button
     type="button" className "toc-trigger", aria-label "Table of contents",
     aria-expanded={tocOpen}, onClick onToggleToc, inline-SVG glyph (20x20,
     viewBox 24, strokeWidth 1.75, aria-hidden, focusable false — standard
     three-line list/contents glyph copying the TagIcon shape). Anatomy
     mirrors the tags trigger byte-for-byte: min-height/min-width
     var(--touch), padding var(--space-sm), transparent background + 1px
     transparent border, border-radius 4px, var(--ink-soft) rest,
     var(--accent) on hover and when aria-expanded="true", global
     :focus-visible ring. NO aria-haspopup="dialog" on this trigger (the
     panel is not a dialog — Pitfall 3). New props tocOpen/onToggleToc
     mirror tagsOpen/onToggleTags. Render data-destination on the
     .app-header element from the existing destination prop (styling hook for
     Task 3).
  2. App.tsx — own tocOpen state + onToggleToc callback; thread to Header
     (trigger props) and ArticleView (panel state), mirroring the tags
     plumbing ownership exactly.
  3. ArticleView.tsx — mount TocPanel + the controlled seam (Pattern 1,
     adapted from the tag popover): tocPanelRef + tocTriggerRef; effect syncs
     tocOpen to showPopover()/hidePopover() via :popover-open checks,
     capturing the trigger element BEFORE showPopover; ONE toggle-event
     listener is the ONLY close path — trigger toggle, Esc, entry
     activation, and (sheet mode) outside pointerdown all route through
     hidePopover so state-reset + focus-restore-to-trigger fire exactly once
     (no second focus-restore path). Manual Esc: keydown on the open panel.
     Outside-click policy is width-scoped (UI-SPEC rule 4): at ≥640px NO
     outside dismissal (persistent companion); at ≤639px pointerdown outside
     panel+trigger closes. View change (Back to library, shell nav, another
     article) closes the panel through the seam (rule 18). StrictMode-safe
     via previous-value comparison (rule 17).
  4. Jump handler handleTocJump(entry): close the panel through the seam;
     then the D5-11 tail VERBATIM from handleNavigateBack — paginated:
     pages = surfaceRef.current?.getPages(), pageIdx =
     fragmentContainingOffset(pages, entry.offset, article),
     surfaceRef.current.turnToPage(pageIdx); scrolling: findScrollTarget(
     article, queryBlocks(articleRef.current), entry.offset)?.scrollIntoView(
     { block: "start" }). Then focus the destination: resolve the element
     via [data-block-index] selector on the VISIBLE reading surface (exclude
     the .article-body-measurement clone — Pitfall 7), set tabIndex -1 on it,
     focus via requestAnimationFrame AND window.setTimeout 120ms double-call
     (the D4-07 firefox-settle guard). Top entry (blockIndex sentinel):
     offset 0 → scroll-to-top / page 1, focus lands on the article h1 via
     the existing articleH1Ref (D14-03 precedent). Activation interception:
     preventDefault on click and Enter — the hash router NEVER re-parses;
     never assign location.hash (Pitfall 4); parseHash + fragment guard stay
     byte-stable.
  5. tests/e2e/toc/toc-navigation.spec.ts — core cells (corpus extension
     comes in Plan 18-04; strengthen-only there): copy the tag-popover.spec
     harness wholesale (prepareFreshPage, article seeding via the
     portability helpers, the waitForFunction readiness sentinel over
     .page-fragment / .article-body:not(.article-body-measurement)
     [data-block-index], zero fixed sleeps). Cells: (a) trigger opens the
     panel — h2 "Contents" + nav aria-label visible, trigger aria-expanded
     true; (b) Esc closes and focus returns to the trigger (chromium/firefox
     poll activeElement; webkit asserts the documented weaker
     not-trapped-in-closed-surface exception); (c) entry click closes the
     panel, jumps, and focus lands on the destination heading (both reading
     modes — toggle mode between cells); (d) Enter activation jumps without
     re-routing (hash unchanged, article did not remount); (e) the same
     entry lands on the same heading in both modes (ORNT-03 equivalence —
     assert the focused heading's text matches across mode toggles); (f)
     open-close-open leaves scroll offset (scrolling) / current page
     (paginated) unchanged (ORNT-05).
  </action>
  <acceptance_criteria>
  - Header.tsx contains a button with aria-label "Table of contents" and aria-expanded, gated by the same articleMounted condition as the tags trigger, rendered before the tags trigger in the controls group
  - Header.tsx toc-trigger carries no aria-haspopup attribute
  - ArticleView.tsx contains exactly ONE toggle-event listener for the TOC panel and the fragmentContainingOffset + findScrollTarget + turnToPage jump calls (D5-11 tail reused, not reimplemented — grep both imports)
  - ArticleView.tsx contains no document.getElementById-based TOC destination focus (resolution is [data-block-index]-based; no invented DOM ids)
  - The TOC jump path never assigns location.hash (grep: no location.hash assignment added in this plan's diff)
  - tests/e2e/toc/toc-navigation.spec.ts exits 0 on chromium with cells (a)-(f); zero fixed sleeps in the file
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/toc/toc-navigation.spec.ts</automated>
  </verify>
  <done>Reader opens the labeled TOC from the header on any article, jumps in both modes through canonical offsets with focus-on-heading, Esc/outside-sheet/entry close all restore focus through one seam, and the core e2e cells prove it in a real browser.</done>
</task>

<task type="auto">
  <name>Task 3: ≤420px staged shell-nav collapse + 5-button geometry audit</name>
  <files>src/app.css</files>
  <read_first>
  - .planning/phases/18-reader-orientation/18-UI-SPEC.md (§Layout Contract — "The ≤639px five-button geometry decision" SANCTIONED block verbatim: staged collapse, rejected alternatives, 420px rationale, mandatory e2e list)
  - src/app.css (the measured 320px budget comment L3705-3723; the ≤639px wordmark collapse declarations L3725-3751 — the clip treatment to copy; the .header-start flex-shrink honesty rule; the reduced-motion gate L84-92)
  - tests/e2e/touch-targets.spec.ts, tests/e2e/reflow.spec.ts, tests/e2e/high-zoom.spec.ts (the suites the 5-button row must now survive)
  </read_first>
  <action>
  Add the sanctioned staged destination collapse to app.css at FILE END:

  1. Media block max-width 420px scoped to
     .app-header[data-destination="reader"] .shell-nav a — apply the SAME
     visually-hidden clip declarations the ≤639px wordmark collapse uses
     (position absolute; 1px box; clip rect) read from the existing block.
     The clipping treatment is the wordmark mechanism verbatim — NEVER the
     display property that removes elements from the a11y tree (links stay
     in tab order and the a11y tree; D18-02's row is arithmetically
     impossible below ~378px otherwise).
  2. Strengthen-only companion rule (applies to the collapsed wordmark
     too): :focus-visible on those clipped links un-clips them (restores
     visible box) so a focused destination is never an invisible focus
     target — closes the latent Phase 15 gap in passing (UI-SPEC §Layout).
  3. Preserve every load-bearing constant: single 48px row (no wrap, no
     height change at any breakpoint — main.paginated-main calc, banner
     offsets, headerPx 48, SectionAnnouncer sentinel all depend on it); all
     five article-scoped buttons keep 44px minimum targets;
     .header-start keeps flex-shrink: 0 (deficits stay visible, never
     silent overlap — the 15-04 honesty fix).
  4. No other header changes: gaps/padding stay as tuned (the ladder is
     exhausted; the collapse IS the relief).

  Then run the three geometry suites — they now exercise the 5-button row at
  320px/400%. Full geometry/no-trap/un-clip e2e cells land in Plan 18-04's
  toc-geometry.spec (this task ships the CSS; those specs prove it).
  </action>
  <acceptance_criteria>
  - app.css contains a max-width: 420px media block scoped to .app-header[data-destination="reader"] shell-nav links applying the visually-hidden clip declarations
  - The collapse uses clip-based hiding — the added block contains no rule removing the links from layout/a11y tree via the display property
  - A :focus-visible un-clip rule exists covering the collapsed shell-nav links (and the collapsed wordmark)
  - The 48px header height rule is untouched (git diff shows no height change to .app-header at any breakpoint)
  - tests/e2e/touch-targets.spec.ts, tests/e2e/reflow.spec.ts, tests/e2e/high-zoom.spec.ts all exit 0 with the 5-button row present
  </acceptance_criteria>
  <verify>
  <automated>npx playwright test tests/e2e/touch-targets.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts</automated>
  </verify>
  <done>The 5-button header fits 320px in Reader via the sanctioned staged collapse — one 48px row, five 44px targets, collapsed destinations reachable and focus-visible — and the existing 320px suites stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived TOC data → DOM | TocEntry text (already Zod-validated at ingest) renders through a new UI surface |
| user activation → router | TOC link activations cross into hash-router territory (must stay intercepted) |
| overlay → reading surface | the panel overlays content that must stay interactive and un-resized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering (XSS via heading text) | TocPanel.tsx | mitigate | Entry text renders as React text children only; no dangerouslySetInnerHTML (react/no-danger enforced repo-wide); content sanitized at ingest (DOMPurify pipeline, ING-07) |
| T-18-02 | Tampering (DOM clobbering) | ArticleView jump handler | mitigate | No invented string ids; destination resolution via numeric data-block-index attribute only |
| T-18-03 | Tampering (router confusion via hrefs) | TocPanel entries | mitigate | All activations intercepted (preventDefault click + Enter); fragment-only hrefs; parseHash + fragment guard byte-stable; never location.hash assignment; e2e cell (d) proves no remount |
| T-18-04 | Repudiation/Elevation adjacent (focus hijacking) | TocPanel + seam | mitigate | Non-modal by construction (popover=manual, no trap possible); one toggle seam; Tab-escape asserted in real browsers (Plan 18-04 geometry cells) |
| T-18-05 | Tampering (repagination side channel) | panel geometry | mitigate | Panel is fixed-position overlay only — never resizes the article column (Pitfall 7); e2e cell (f) asserts location/repagination stability on open/close |
| T-18-SC | Tampering (package installs) | package.json | accept | Zero installs this phase (18-RESEARCH §Package Legitimacy Audit: none required) |
</threat_model>

## Artifacts this phase produces (Plan 18-02)

- `src/reader/TocPanel.tsx` — new component: non-modal popover=manual TOC panel
- `tests/unit/TocPanel.test.tsx` — RTL component suite
- `tests/e2e/toc/toc-navigation.spec.ts` — new spec (core cells; extended in 18-04)
- `handleTocJump` handler in `src/routes/ArticleView.tsx`
- `tocOpen` / `onToggleToc` props threaded App → Header → ArticleView
- CSS classes: `.toc-panel`, `.toc-title`, `.toc-list`, `.toc-empty`, `.toc-trigger`, `.toc-destination` (focus rule), `.is-current`-equivalent aria-current entry styling, `.app-header[data-destination]` collapse rules

<verification>
- `npx vitest run tests/unit/TocPanel.test.tsx` exit 0
- `npx playwright test tests/e2e/toc/toc-navigation.spec.ts` exit 0 (chromium default project)
- `npx playwright test tests/e2e/touch-targets.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts` exit 0
- `npx vitest run --project unit` green (no unit regressions)
</verification>

<success_criteria>
- ORNT-01: labeled TOC opens from the header on any article (headingless included, D18-13)
- ORNT-03: same entry → same destination in both modes via canonical offsets; zero persisted page numbers/DOM ids
- ORNT-04: nested semantic list, levels preserved, skips nest without invention, duplicates as-is
- ORNT-05 (panel half): open/close changes nothing logical; no trap at rail or sheet geometry
- D18-01/02/03/04/09/10/11/12/13/15 all implemented; byte-stable surfaces (parseHash, fragment guard, announcer, 48px constants) untouched
</success_criteria>

<output>
Create `.planning/phases/18-reader-orientation/18-02-SUMMARY.md` when done
</output>
