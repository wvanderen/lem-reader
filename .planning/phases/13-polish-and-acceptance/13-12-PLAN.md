---
phase: 13-polish-and-acceptance
plan: 12
type: execute
wave: 7
depends_on: ["13-11"]
files_modified:
  - docs/ACCEPTANCE-PROTOCOL.md
  - tests/e2e/annotations/toolbar-keydownless-focus.spec.ts
  - tests/e2e/annotations/toolbar-tab-path.spec.ts
autonomous: true
gap_closure: true
requirements: [ACPT-05]

must_haves:
  truths:
    - "ACCEPTANCE-PROTOCOL.md (v1.1) Flow C2 instructs NVDA (Firefox) users to enter focus mode (NVDA+Space) BEFORE pressing Tab, C1's expected outcome names the 'Highlight actions available.' mount announcement as the toolbar-appeared cue, and C3's NVDA note is re-anchored on the focus-mode state instead of the false browse-mode pass-through claim"
    - "The e2e suite pins the NVDA browse-mode boundary: a keydown-less programmatic focus jump to the Previous page chevron (the exact DOM sequence NVDA browse mode produces for Tab) delivers ZERO keydowns to the page and, in firefox and webkit, collapses the selection and unmounts the toolbar; in chromium the selection survives and the toolbar stays mounted"
    - "After a keydown-less collapse, re-selecting and pressing a REAL Tab (the focus-mode condition) routes focus onto the toolbar's Highlight button — with activeElement starting on the chevron inside the article subtree — and Enter creates mark.highlight[data-highlight-id] with the 'Highlight saved.' announcement: the documented NVDA recovery journey"
    - "toolbar-tab-path.spec.ts no longer asserts the false equivalence between Playwright's real Tab keydown and what NVDA browse mode delivers to Firefox, and cross-references the keydown-less boundary spec"
    - "ZERO production source changes ship (decision G7-D1); ACPT-05 remains pending the human NVDA re-run per D13-06/D13-07"
  artifacts:
    - path: "docs/ACCEPTANCE-PROTOCOL.md"
      provides: "Flow C corrected for NVDA browse-mode Tab consumption + focus-mode instruction + C1 announcement cue"
      contains: "NVDA+Space"
    - path: "tests/e2e/annotations/toolbar-keydownless-focus.spec.ts"
      provides: "The browse-mode boundary pin + the focus-mode recovery spec (the automation gap that hid G7's recurrence)"
      min_lines: 90
    - path: "tests/e2e/annotations/toolbar-tab-path.spec.ts"
      provides: "Corrected comment modeling real Tab as the focus-mode/sighted-keyboard condition"
  key_links:
    - from: "tests/e2e/annotations/toolbar-keydownless-focus.spec.ts"
      to: "src/reader/PaginatedSurface.tsx"
      via: "keydown-less focus() on button.page-turn-previous (aria-label 'Previous page') — the exact control NVDA browse mode landed the tester on (G7 report)"
      pattern: "page-turn-previous"
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: ".planning/debug/g7-nvda-tab-bypass-selection-toolbar.md"
      via: "Flow C2 focus-mode instruction = the live-verified Phase A path (real Tab keydown routes + toolbar survives)"
      pattern: "NVDA\\+Space"
    - from: "tests/e2e/annotations/toolbar-keydownless-focus.spec.ts"
      to: "tests/e2e/annotations/_fixtures.ts"
      via: "reuses openArticle/selectRangeInBlock/findFirstBlockWithText/announcementRegion wholesale (no forked harness)"
      pattern: "selectRangeInBlock"
---

<objective>
Close UAT gap G7 (13-UAT.md, status: diagnosed): the ACPT-05 Flow C failure on the G6-fixed build where NVDA+Firefox browse mode consumes the Tab keydown itself and moves DOM focus to the Previous page control, bypassing 13-11's keydown-only Tab routing and unmounting the toolbar via the Gecko selection collapse.

Purpose: Make the documented NVDA Flow C sequence true and re-runnable without reintroducing any known harm — the ACPT-05 gate (D13-06/D13-07) is blocked solely on this gap.
Output: (1) ACCEPTANCE-PROTOCOL.md v1.1 with the corrected Flow C (NVDA focus-mode instruction at C2, mount-announcement cue at C1, re-anchored C3 note); (2) a keydown-less-focus e2e spec pinning the browse-mode boundary and the focus-mode recovery path; (3) the false comment correction in toolbar-tab-path.spec.ts. Zero production source changes.

## Decision G7-D1 (made in planning, as the gap's missing-item 2 delegates)

**Chosen: option (b) — accept focus-mode-only SR reachability under NVDA and document it in the protocol. Rejected: option (a) toolbar focus-on-appear (debounced).** The executor MUST NOT add focus-on-appear, auto-focus, or any src/ change; the product behavior shipped by 13-11 is final for this gap.

Justification (recorded here so it is not re-litigated):
1. **(a) reintroduces a recorded harm.** 13-11's key decision explicitly rejected focus-on-appear because Gecko/WebKit collapse the document selection synchronously inside focus() — so on Firefox and Safari EVERY settled selection (mouse AND keyboard) would lose its visible selection highlight once the debounce fires. The visible selection is the primary "what am I about to highlight" feedback; destroying it attacks the product's calm-predictability core promise.
2. **(a) is a race it can still lose.** The tester's actual pacing was Tab immediately after selecting. When NVDA's Tab beats the debounce, NVDA moves focus to the Previous page chevron itself, Gecko collapses the selection inside that move, the selectionchange collapsed branch clears the toolbar state, and the pending focus-on-appear cancels on the cleared state — G7's observable byte-for-byte. A debounce narrows the race window; it cannot close it.
3. **(a) harms SR UX on another axis.** Unsolicited focus moves during selection reading/shaping are a recognized screen-reader disorientation pattern; the debounce is a heuristic that can fire mid-intent.
4. **(b) is deterministic and already verified.** The G7 debug session's live firefox Phase A experiment proved the current build routes a real Tab keydown onto the toolbar's Highlight button with the toolbar surviving — exactly the focus-mode condition. NVDA+Space is one keystroke SR users perform constantly.
5. **(b) is the platform convention.** SR users switch to focus mode to operate interactive widgets (the universal rich-toolbar convention); NVDA's own auto-switch cannot help here because a plain-button toolbar does not "require" focus mode, which is why the explicit C2 instruction is load-bearing. Browse mode to read/select (C1), focus mode to operate the widget (C2/C3) is the coherent split.
6. **Zero product risk.** Pointer path, sighted-keyboard path, Chromium engine behavior, and the VoiceOver path remain byte-unchanged.

Boundary honesty: under G7-D1 a browse-mode Tab still yields the G7 observable (focus on Previous page, toolbar unmounts). That is now a DOCUMENTED boundary pinned by the new spec, and it is recoverable (re-select, NVDA+Space, Tab). The gap's truth is achieved via the documented focus-mode sequence — the closure shape the missing list itself sanctions ("or accept focus-mode-only reachability and document it").

## Missing-item coverage map

- Item 1 (protocol fix: Flow C2 NVDA focus mode) → Task 2
- Item 2 (product fix choice, decided + justified above) → Decision G7-D1 (this objective)
- Item 3 (keydown-less e2e + false comment fix) → Task 1
- Item 4 (re-run note: confirm the C1 mount announcement; NVDA re-run of Flow C) → Task 2 adds the C1 audible cue to the protocol; plan-level verification records the re-run trigger. ACPT-05 flips only when the tester's re-run lands in 13-VERIFICATION.md per D13-06/D13-07 (13-11 precedent).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
@.planning/phases/13-polish-and-acceptance/13-11-SUMMARY.md
@.planning/debug/g7-nvda-tab-bypass-selection-toolbar.md
@.planning/debug/flowc-selection-toolbar-nvda.md
@docs/ACCEPTANCE-PROTOCOL.md
@src/routes/ArticleView.tsx
@src/reader/annotations/SelectionToolbar.tsx
@src/reader/PaginatedSurface.tsx
@tests/e2e/annotations/toolbar-tab-path.spec.ts
@tests/e2e/annotations/_fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keydown-less-focus boundary + recovery e2e spec; correct the false Tab comment</name>
  <files>tests/e2e/annotations/toolbar-keydownless-focus.spec.ts, tests/e2e/annotations/toolbar-tab-path.spec.ts</files>
  <behavior>
    - Test 1 (browse-mode emulation, per-engine boundary): from a live programmatic selection + mounted toolbar, a keydown-less programmatic focus to the Previous page chevron delivers 0 keydowns; firefox + webkit: selection collapsed, toolbar count 0; chromium: selection alive, toolbar still connected.
    - Test 2 (recovery = documented focus-mode path): after the keydown-less collapse, a fresh selection + ONE real Tab (activeElement starting on the chevron inside the article subtree) lands focus on the Highlight button with the toolbar connected; Enter creates mark.highlight[data-highlight-id] + "Highlight saved." announcement + toolbar count 0.
  </behavior>
  <action>
    Create tests/e2e/annotations/toolbar-keydownless-focus.spec.ts (UAT G7 item 3, first half). Reuse the _fixtures.ts helpers wholesale (wipeDatabase, openArticle, FIXTURES, findFirstBlockWithText, selectRangeInBlock, announcementRegion) — exactly as toolbar-tab-path.spec.ts does; FIXTURE = FIXTURES[0] (essay-long-form), paginated default mode, no switchMode. File header comment states the G7 provenance: NVDA browse mode binds Tab as its own navigation gesture and never delivers a Tab keydown to the page — it moves DOM focus itself via accessibility APIs (official NVDA user guide; .planning/debug/g7-nvda-tab-bypass-selection-toolbar.md Phase B reproduced this exact DOM sequence live in firefox with zero keydowns); per decision G7-D1 this spec PINS the documented boundary (browse mode is out of reachability contract; focus mode is in — pinned by toolbar-tab-path.spec.ts's real-Tab tests).

    Test 1 — "browse-mode emulation pins the per-engine keydown-less boundary": openArticle → findFirstBlockWithText(page, 24) not -1 → selectRangeInBlock(page, blockIdx, 0, 18) truthy → expect .selection-toolbar visible + getByRole("toolbar", { name: "Highlight actions" }) visible. Then, in ONE page.evaluate, install a window-scoped keydown counter with a capture-phase window listener (counter starts at 0 at install time — install AFTER all setup keyboard activity). In a SECOND page.evaluate (still zero keyboard input), focus the Previous page chevron: query button.page-turn-previous (rendered by PaginatedSurface, aria-label "Previous page" — the exact control NVDA landed the tester on). NO page.keyboard call may occur between counter install and the final assertion — that keydown-less-ness IS the G7 signature. waitForTimeout(300) for the rAF-throttled selectionchange + React unmount, then ONE atomic evaluate returning the four-fact state: keydown count, activeElement-is-the-chevron, window.getSelection().isCollapsed, .selection-toolbar count. Per-engine expectations as an engine-keyed constant: firefox and webkit → keydowns 0, focus on chevron, collapsed true, toolbar 0 (the G7 observable — the documented browse-mode boundary); chromium → keydowns 0, focus on chevron, collapsed false, toolbar 1 (Gecko/WebKit collapse on focus moves; chromium keeps the live selection so no dismissal fires — comment that chromium is engine-matrix completeness only; the SR acceptance pairings are NVDA+Firefox and VO+Safari).

    Test 2 — "recovery: the documented focus-mode path works after a browse-mode collapse": identical pre-state and keydown-less chevron focus (the browse-mode mistake); settle; assert toolbar count 0. Re-select via a fresh selectRangeInBlock call (C1 again — focus stays on the chevron; selection changes never move focus); expect toolbar visible again. Then press ONE real Tab via page.keyboard (models NVDA focus mode after NVDA+Space — the keydown-reaches-page condition; note in a comment that activeElement is the chevron INSIDE the article subtree, so the 13-11 routing guard's articleNode.contains clause is the engaging branch, modeling the real NVDA recovery pre-state). waitForTimeout(150), then the same atomic evaluate used by toolbar-tab-path.spec.ts: Highlight button isFocus true AND closest .selection-toolbar isConnected true (the containment-hold survival). Press Enter; expect mark.highlight[data-highlight-id] first visible; announcementRegion(page) contains /Highlight saved/i; toolbar count 0. No engine split (mirrors toolbar-tab-path test 1, green on all three; the chevron pre-state is the only delta).

    This is a boundary-PINNNG spec, not TDD: decision G7-D1 ships zero product changes, so BOTH tests must be GREEN on the current build immediately — they assert today's diagnosed behavior as the documented contract. If any assertion fails on the current build, the G7 diagnosis itself is contradicted: stop and surface as Rule 4 (do NOT weaken the assertions to force green).

    Then correct the false comment in tests/e2e/annotations/toolbar-tab-path.spec.ts (UAT G7 item 3, second half): the two-line comment immediately above the page.keyboard.press("Tab") call in assertTabReachAndEnterActivate (currently L70-71) asserts a false equivalence between Playwright's real trusted Tab keydown and what NVDA browse mode delivers to Firefox. Replace those two lines with the true model: a real Tab keydown models the condition where a key actually reaches the page — the sighted keyboard, or NVda focus mode (NVDA+Space), which passes keys through; NVDA browse mode consumes Tab as its own gesture and moves focus itself with zero page keydowns; that boundary is pinned by the sibling toolbar-keydownless-focus.spec.ts (G7; see .planning/debug/g7-nvda-tab-bypass-selection-toolbar.md). Also append one line to the file-header comment block (after the existing G6 pointers) recording that G7 refined what the real Tab models — keep every other header line byte-unchanged. Change NOTHING else in the file: no assertion, helper, or test-body edits.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/toolbar-keydownless-focus.spec.ts tests/e2e/annotations/toolbar-tab-path.spec.ts && ! rg -q "exact key NVDA browse mode" tests/e2e/annotations/toolbar-tab-path.spec.ts && rg -c "toolbar-keydownless-focus" tests/e2e/annotations/toolbar-tab-path.spec.ts</automated>
  </verify>
  <done>New spec green on chromium/firefox/webkit with the per-engine boundary facts (0 keydowns both tests' emulations; firefox/webkit collapse+unmount, chromium survives) and the chevron-pre-state recovery completing C2+C3; the false browse-mode-passthrough claim is gone from toolbar-tab-path.spec.ts; the sibling cross-reference present; zero production source files touched.</done>
</task>

<task type="auto">
  <name>Task 2: ACCEPTANCE-PROTOCOL.md Flow C NVDA focus-mode correction (v1.0 → v1.1)</name>
  <files>docs/ACCEPTANCE-PROTOCOL.md</files>
  <action>
    Correct Flow C per the G7 fix direction (UAT G7 item 1 + item 4's C1 cue; decision G7-D1). Follow the doc's own versioning discipline (final section: bump the Version field when flows are edited).

    1. Header table: Version 1.0 → 1.1. Touch no other header field (the run-date/tester blanks stay blank — they are filled at execution).

    2. Flow C callout (the "Primary SR path" block, currently ending with the G6-era sentence saying the flow may be exercised exactly as documented): amend that trailing sentence to state the reachability precondition — the single-Tab reachability holds whenever a real Tab keydown reaches the page (sighted keyboard, or NVDA focus mode); NVDA browse mode binds Tab as its own navigation gesture and moves focus itself via accessibility APIs WITHOUT delivering any keydown to the page (official NVDA user guide), so under NVDA the tester enters focus mode at C2. Reference .planning/debug/g7-nvda-tab-bypass-selection-toolbar.md. Keep the callout's existing H/N-consumption content intact.

    3. C1 row, Expected outcome column: append one sentence — a polite live-region announcement ("Highlight actions available.") confirms the toolbar has appeared once the selection settles. This is the mount cue the G7 re-run must listen for after C1 (UAT item 4's confirmation).

    4. C2 row, Keyboard sequence column: insert the NVDA focus-mode instruction BEFORE the Tab — for NVDA (Firefox) press NVDA+Space to enter focus mode first, THEN Tab; sighted keyboard users Tab directly. Expected outcome column unchanged (role=toolbar, accessible name "Highlight actions", "Highlight" + "Highlight + note" buttons); optionally note the tester hears NVDA's focus-mode toggle confirmation before the Tab.

    5. C3 row: replace the parenthetical that claims browse mode hands Enter through — the same wrong model the G7 session disproved for Tab-adjacent claims. Re-anchor on the C2 state: NVDA (Firefox): Enter (you are in focus mode from C2, where keys pass through); NVDA+Space also activates. Keep the VoiceOver VO+Space alternative intact.

    6. Diff discipline (the 13-11 precedent, which verified its protocol diff confined to Flow C): changes confined to the header Version field and Flow C (callout + C1/C2/C3 rows). Every other flow and section stays byte-unchanged — verify with git diff before committing.
  </action>
  <verify>
    <automated>rg -c "NVDA\\+Space" docs/ACCEPTANCE-PROTOCOL.md && rg -n "Version\\|\\*\\*1\\.1" docs/ACCEPTANCE-PROTOCOL.md | head -2 && git diff --stat && git diff docs/ACCEPTANCE-PROTOCOL.md | grep -c "^[+-]" </automated>
  </verify>
  <done>Protocol is v1.1; Flow C2 directs NVDA users through NVDA+Space before Tab (the live-verified Phase A sequence); C1 names the "Highlight actions available." mount cue; C3's NVDA note is focus-mode-anchored with the browse-mode pass-through claim gone; git diff confined to the Version line + Flow C; NVDA+Space appears at least twice (C2 + C3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs + test-only change; zero runtime code, zero dependencies, zero data paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12-01 | Tampering | e2e specs (test code) | accept | Test files run in the sandboxed Playwright harness against fixtures; no production input surface |
| T-13-12-02 | Information Disclosure | ACCEPTANCE-PROTOCOL.md | accept | Public-facing manual test instructions; no secrets, credentials, or personal data |
| T-13-12-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (no package.json / lockfile changes — verify in the gates) |
</threat_model>

<verification>
1. Task gates (run per-task above): new boundary/recovery spec + corrected tab-path spec green on all 3 engines; false comment gone; protocol greps pass.
2. Regression net: npx playwright test tests/e2e/annotations/ — the full annotations directory (168 cells + the new spec's cells) green on chromium/firefox/webkit, zero pre-existing failures introduced.
3. Unit sanity: npm run test:unit -- --run — green (no src/ changes shipped, so any failure signals an environmental regression, not this plan).
4. Zero-product-change proof: git diff --name-only across the plan's commits shows ONLY the three files_modified paths; in particular src/ and package.json/lockfiles are byte-unchanged (decision G7-D1 compliance + T-13-12-SC).
5. Protocol diff discipline: git diff docs/ACCEPTANCE-PROTOCOL.md confined to the Version header line + Flow C.
6. Re-run readiness (record in the SUMMARY, UAT G7 item 4): ACPT-05 stays Pending; the tester's NVDA+Firefox re-run of Flow C on the v1.1 protocol must confirm (a) "Highlight actions available." is heard after C1 (toolbar mounted pre-Tab) and (b) NVDA+Space then Tab lands focus on the "Highlight actions" toolbar / its Highlight button, and Enter creates the mark. Results land in 13-VERIFICATION.md §1.3/§1.4; zero blocker/major across the full protocol flips ACPT-05 (D13-06/D13-07) — the 13-11 precedent: this plan does NOT flip it.
</verification>

<success_criteria>
- UAT G7's four missing items are all addressed: protocol fix shipped (v1.1), the product-fix choice made and justified (G7-D1 in the objective, zero src/ edits), the keydown-less spec + comment fix landed, and the re-run prerequisites recorded.
- The e2e suite now covers BOTH sides of the NVDA Tab boundary: real-keydown (focus mode, toolbar-tab-path.spec.ts) and keydown-less (browse mode, the new spec) — the automation blind spot that hid G7's recurrence is closed with an honest boundary pin, not a forced-green assertion.
- ACCEPTANCE-PROTOCOL.md Flow C is executable as written by an NVDA+Firefox tester with no ambiguity about mode switching.
- The annotations e2e directory and unit suite are green; no file outside files_modified changed.
</success_criteria>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-12-SUMMARY.md` when done.
The SUMMARY must record decision G7-D1 and its justification (so the re-run reviewer and any future focus-on-appear proposal encounters the recorded rejection), the boundary-spec semantics, and the ACPT-05 re-run prerequisites (D13-06/D13-07 — flip only on the tester's 13-VERIFICATION.md results).
</output>
