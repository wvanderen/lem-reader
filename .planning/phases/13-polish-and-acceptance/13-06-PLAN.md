---
phase: 13-polish-and-acceptance
plan: 06
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05"]
files_modified:
  - tests/e2e/portability/core-flow-spine.spec.ts
autonomous: true
requirements: [ACPT-06]
must_haves:
  truths:
    - "A reader completes the v2.0 core flow — ingest a .md file, read it, create a highlight, export the library, re-import on a second machine — entirely through the real UI on chromium, firefox, and webkit"
    - "Nothing is lost: exported-then-reimported articles/highlights/notes/positions/preferences rows are byte-equal at the raw IndexedDB level, every highlight re-resolves confident, and the reimported article opens, paginates, annotates, and restores location identically (D13-09)"
    - "The full npm run test suite exits 0 in one invocation with honest counts recorded (D13-10)"
  artifacts:
    - path: "tests/e2e/portability/core-flow-spine.spec.ts"
      provides: "ACPT-06 core-flow spine e2e extending the 09-06 two-context harness"
      min_lines: 120
  key_links:
    - from: "tests/e2e/portability/core-flow-spine.spec.ts"
      to: "tests/e2e/portability/_portability.ts"
      via: "reuses prepareFreshPage, readRow/readAllRows/countRows raw IndexedDB truth, openSettings/bundleInput/settingsStatus helpers — extension, not rebuild"
      pattern: "_portability"
    - from: "tests/e2e/portability/core-flow-spine.spec.ts"
      to: "tests/e2e/library/markdown-upload.spec.ts + tests/e2e/annotations/"
      via: "reuses the proven threshold-clearing .md payload and the real-UI selection→toolbar→mark highlight helpers"
      pattern: "MARKDOWN_WITH_FRONTMATTER"
  prohibitions:
    - statement: "No production source changes in Task 1 (spec-only; the shipped pipeline is the boundary under test)"
      status: resolved
      verification: "git status during Task 1 shows changes only under tests/ (or the payload export line in markdown-upload.spec.ts)"
    - statement: "No fixed sleeps for load-bearing assertions (expect.poll end-condition polling only — Pitfall 8)"
      status: resolved
      verification: "rg -c 'waitForTimeout' tests/e2e/portability/core-flow-spine.spec.ts returns 0"
    - statement: "No engine subsetting, no spec skipping, no grep-filtered suite invocation in the gate (honest-suite discipline)"
      status: resolved
      verification: "the gate task's recorded command is exactly npm run test; every intermediate red run is recorded in the OUTPUT"
---

<objective>
The ACPT-06 phase gate: one deterministic .md-anchored core-flow spine (D13-08 — planner confirms .md: network-free, deterministic, threshold-clearing payload proven) driven entirely through the real UI across the 3-engine matrix, extending the 09-06 two-context round-trip harness (ingest → read → highlight → export → transfer → re-import) with the D13-09 no-content-loss bar (byte-equal raw rows, confident re-resolution, identical open/paginate/annotate/restore) — followed by the honest full-suite gate: npm run test exits 0 in one invocation with fail counts recorded (D13-10), closing the phase and the milestone's engineering gate.

Purpose: prove "the reading engine cannot tell an ingested article from a fixture" end-to-end across engines, and end v2.0 on the locked honest-suite discipline.
Output: core-flow-spine.spec.ts; honest full-suite record in the plan SUMMARY (12-08 OUTPUT format).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core-flow spine e2e (ACPT-06, SC#4)</name>
  <files>tests/e2e/portability/core-flow-spine.spec.ts</files>
  <read_first>
  - tests/e2e/portability/_portability.ts (prepareFreshPage; readRow/readAllRows/countRows raw IndexedDB truth helpers; openSettings/bundleInput/settingsStatus UI helpers)
  - tests/e2e/portability/round-trip.spec.ts (two-context machine A/B shape L85-107; confidentHighlightOn; the download-capture → unzip → re-import transfer flow; books spec ~L296 — excluded here per D13-08)
  - tests/e2e/library/markdown-upload.spec.ts (MARKDOWN_WITH_FRONTMATTER payload proven to clear ING-06 thresholds + the 5-offset anchor gate; setInputFiles on input#ingest-file; "Add file" button; waitForURL /#\/article\/md-/)
  - tests/e2e/annotations/capture-highlight.spec.ts + tests/e2e/annotations/_fixtures.ts (real-UI selection → toolbar → mark.highlight → "Highlight saved." announcement)
  - tests/e2e/pagination/ + tests/e2e/persistence.spec.ts (settle/restore assertion helpers to reuse for the reimported article)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pattern 4 + Pitfall 9 — payload must clear gates by construction)
  </read_first>
  <action>
    Create tests/e2e/portability/core-flow-spine.spec.ts with plain test() declarations (playwright.config.ts runs plain tests on chromium/firefox/webkit — no project filtering). Machine A (browser.newContext + prepareFreshPage): upload the proven .md payload through IngestControl (setInputFiles on the ingest file input; activate the add-file control; wait for the md- article route), read the article (pagination settles), create one highlight via the real selection UI (annotation helpers → toolbar → highlight → saved announcement), then export the whole-library bundle through the Settings UI (openSettings → export → captured download). Transfer in Node per the round-trip.spec.ts flow (unzip the captured bundle). Machine B (fresh context + prepareFreshPage): import the bundle through the Settings import UI including the ImportPreviewDialog Proceed step. Assert the D13-09 bar: (1) raw IndexedDB rows byte-equal between the exported set and the reimported set across the five row kinds (articles, highlights, notes, locations, settings — readRow/readAllRows comparisons; books excluded per D13-08); (2) every reimported highlight re-resolves confident via the shipped resolver helper (tri-state surfacing intact — none silently dropped); (3) the reimported article opens from the library, pagination settles, a NEW highlight can be created on it (annotates), and reading position restores after reload (reuse the persistence/restore helpers — no forks). Reuse ONLY shipped helpers and payloads (REUSE-DO-NOT-FORK): import MARKDOWN_WITH_FRONTMATTER from markdown-upload.spec.ts (add an export keyword there if it is not exported — that is the one allowed non-spec edit) rather than copying the payload. expect.poll for every end condition; no fixed sleeps.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/core-flow-spine.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/portability/core-flow-spine.spec.ts exits 0 on chromium, firefox, and webkit
    - Byte-equality assertions cover all five row kinds via the _portability raw-row helpers; the confident-resolution assertion uses the shipped resolver helper; open/paginate/annotate/restore assertions reuse existing helpers
    - The spine is UI-driven end-to-end (no direct db writes for the flow steps themselves — seeding/wipe via prepareFreshPage is the only harness-level storage access on machine B's import path)
    - rg -c 'waitForTimeout' tests/e2e/portability/core-flow-spine.spec.ts returns 0
    - git status during this task shows no changes outside tests/ (the payload export keyword in markdown-upload.spec.ts excepted)
  </acceptance_criteria>
  <done>The v2.0 core flow proven across the matrix with the 09-06-extended no-content-loss bar.</done>
</task>

<task type="auto">
  <name>Task 2: Honest full-suite phase gate (D13-10)</name>
  <files>.planning/phases/13-polish-and-acceptance/13-06-OUTPUT.md</files>
  <read_first>
  - .planning/phases/12-epub-intake/12-08-OUTPUT.md (the honest-suite record format to copy)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pitfall 8 — the two known flake classes: webkit dev-server goto timeouts under full-suite parallelism; firefox rAF-throttled scroll/announce settle)
  - package.json (test script = unit run + e2e in one invocation)
  </read_first>
  <action>
    Run npm run test in ONE invocation. Create .planning/phases/13-polish-and-acceptance/13-06-OUTPUT.md recording honestly: exit code, unit counts (passed/failed/skipped), e2e counts (passed/failed/skipped per engine), and timestamp — the 12-08 record format. If exit is non-zero: triage per the 12-08 protocol — real bugs and races get fixed with atomic commits (prefer expect.poll strengthening over sleeps for the two known flake classes), then re-run the FULL suite; environmental non-reproducers may receive the "green in isolation + later run" note only after a genuine reproduction attempt. The gate passes only when a full invocation exits 0. No engine subsetting, no spec skipping, no filtered suite invocations — the recorded command history must show only plain npm run test runs. Every intermediate red run stays in the record.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - The final npm run test invocation exits 0
    - 13-06-OUTPUT.md exists with exit code, unit counts, e2e per-engine counts, timestamp, and (if any) the full honest history of intermediate red runs with their fixes
    - The OUTPUT records no skipped-spec or single-engine workaround invocations
  </acceptance_criteria>
  <done>v2.0 ends on an honest green full suite with the permanent OUTPUT record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .md fixture bytes → /api/ingest middleware → pipeline | Spine fixture content crosses the ingest boundary (unchanged shipped validation is the control under test) |
| bundle zip bytes → import validation | Machine-B import consumes a transferred archive through the shipped six-refusal pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | Spine fixture regressing the ingest security boundary | mitigate | Reuse the proven threshold-clearing payload unchanged; the shipped Zod-at-boundary + CommonMark-escape pipeline (D8-16) is exercised, not modified; no production changes accepted in this plan |
| T-13-09 | Repudiation | Gate honesty (claiming green without a full run) | mitigate | OUTPUT record format requires the literal command history + counts; any red intermediate run must appear; checker compares recorded counts against the final invocation |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; any dependency addition is out of scope |
</threat_model>

<verification>
- npx playwright test tests/e2e/portability/core-flow-spine.spec.ts (3 engines)
- npm run test — single invocation, exit 0, counts recorded in 13-06-OUTPUT.md
</verification>

<success_criteria>
- ACPT-06 SC#4 proven: the UI-driven core flow completes without content loss across chromium/firefox/webkit at the D13-09 byte-equality bar
- Honest full-suite gate: npm run test exit 0, fail counts recorded, permanent OUTPUT record
</success_criteria>

<artifacts_produced>
- tests/e2e/portability/core-flow-spine.spec.ts — new spec (no production symbols)
- .planning/phases/13-polish-and-acceptance/13-06-OUTPUT.md — new record document
- (possible one-line) export keyword added to MARKDOWN_WITH_FRONTMATTER in tests/e2e/library/markdown-upload.spec.ts
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-06-SUMMARY.md` when done
</output>
