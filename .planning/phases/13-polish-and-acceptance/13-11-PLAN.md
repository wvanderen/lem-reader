---
phase: 13-polish-and-acceptance
plan: 11
type: execute
wave: 6
depends_on: []
files_modified:
  - src/routes/ArticleView.tsx
  - src/reader/annotations/SelectionToolbar.tsx
  - tests/e2e/annotations/toolbar-tab-path.spec.ts
  - tests/e2e/annotations/keyboard-shortcuts.spec.ts
  - docs/ACCEPTANCE-PROTOCOL.md
autonomous: true
gap_closure: true
requirements: [ACPT-05]
must_haves:
  truths:
    - "After a text selection exists in the reader, a single Tab press moves focus onto the selection toolbar's Highlight button in Chromium, Firefox, AND WebKit (G6 truth; ACCEPTANCE-PROTOCOL Flow C2)"
    - "The selection toolbar stays mounted while it contains document.activeElement, surviving the Gecko/WebKit focus-induced selection collapse"
    - "Enter on the focused Highlight button creates mark.highlight[data-highlight-id] and announces Highlight saved. via the existing role=status region, in all three engines and BOTH reading modes (paginated + scrolling)"
    - "Tabbing past the toolbar's last button dismisses it (containment releases; no wedged toolbar)"
    - "The pointer path is unchanged: a selection followed by a toolbar button click still creates the highlight, and toolbar appearance never steals focus (no auto-focus on appear)"
  artifacts:
    - path: "src/routes/ArticleView.tsx"
      provides: "Focus-containment dismissal guard + Tab routing to the toolbar + saved-range restore feeding the ONE existing creation path"
      contains: "selection-toolbar"
    - path: "src/reader/annotations/SelectionToolbar.tsx"
      provides: "Polite announce-on-appear live region (buttons variant) + corrected keyboard-path comment"
      contains: "Highlight actions available"
    - path: "tests/e2e/annotations/toolbar-tab-path.spec.ts"
      provides: "3-engine Tab-reachability + Enter-activation regression spec, both reading modes"
      contains: "Highlight actions"
    - path: "tests/e2e/annotations/keyboard-shortcuts.spec.ts"
      provides: "The formerly-skipped Tab-activation assertion, now real"
    - path: "docs/ACCEPTANCE-PROTOCOL.md"
      provides: "NVDA (Firefox) selection + activation gestures documented in Flow C1/C3"
      contains: "NVDA (Firefox)"
  key_links:
    - from: "src/routes/ArticleView.tsx (window keydown Tab branch)"
      to: ".selection-toolbar first button"
      via: "preventDefault + focus() — Tab from the reading context lands directly in the toolbar"
      pattern: "\\.selection-toolbar button"
    - from: "src/routes/ArticleView.tsx (handleHighlightShortcut saved-range restore)"
      to: "HighlightOverlayProvider createHighlightFromSelection"
      via: "restored live Range re-enters the unchanged capture pipeline — ONE creation path, single-block rule re-validated on restore"
      pattern: "createHighlightFromSelection"
    - from: "tests/e2e/annotations/toolbar-tab-path.spec.ts"
      to: ".selection-toolbar + mark.highlight DOM contract"
      via: "real Tab presses + Enter activation asserting toolbar survival across the focus move and mark creation"
      pattern: "mark\\.highlight\\[data-highlight-id\\]"
---

<objective>
Close gap G6 (major, diagnosed): the selection toolbar is keyboard-unreachable in Firefox and WebKit because those engines collapse the document selection synchronously when DOM focus moves, and the toolbar lifecycle (ArticleView's selectionchange listener, collapsed branch L713-716) unmounts it before focus can arrive. This plan makes ACCEPTANCE-PROTOCOL Flow C2 (Tab reaches the toolbar) + C3 (Enter creates the highlight) true in all three engines and both reading modes, adds the automation gap that hid the defect (a real Tab-walk e2e spec in firefox + webkit), and documents the NVDA gestures in Flow C1/C3 so the D13-06 ACPT-05 re-run is unambiguous.

Purpose: ACPT-05 (NVDA+Firefox acceptance, SC#3) failed at Flow C — a major finding under D13-06 fix-then-re-run. The fix must land before the tester's re-run; the protocol must document the NVDA selection/activation gestures the re-run depends on.

Output: Fixed toolbar lifecycle (focus containment + Tab routing + saved-range activation + announce-on-appear), a 3-engine keyboard-path e2e spec (both modes), the strengthened keyboard-shortcuts assertion, and NVDA gesture documentation. The ACPT-05 re-run itself is the tester's, outside this plan.

Evidence base (read it): .planning/debug/flowc-selection-toolbar-nvda.md — the verified root cause + 3-engine experiments. Do not re-litigate the diagnosis; implement its fix direction.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
@.planning/debug/flowc-selection-toolbar-nvda.md
@src/routes/ArticleView.tsx
@src/reader/annotations/SelectionToolbar.tsx
@tests/e2e/annotations/_fixtures.ts
@tests/e2e/annotations/keyboard-shortcuts.spec.ts
@docs/ACCEPTANCE-PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Tab-walk e2e spec reproducing G6 (new toolbar-tab-path.spec.ts + strengthen the skipped keyboard-shortcuts assertion)</name>
  <files>tests/e2e/annotations/toolbar-tab-path.spec.ts, tests/e2e/annotations/keyboard-shortcuts.spec.ts</files>
  <action>
Create tests/e2e/annotations/toolbar-tab-path.spec.ts — the automation gap that hid G6 (UAT artifact 4). Reuse the annotations harness imports from ./_fixtures (wipeDatabase, openArticle, selectRangeInBlock, findFirstBlockWithText, announcementRegion, switchMode, modeToggle, visibleBlock) with FIXTURE = FIXTURES[0] (essay-long-form). Header comment cites G6 + the debug session and states the contract being locked: one Tab from the reading context reaches the toolbar in ALL engines; the toolbar survives the focus move (Gecko/WebKit collapse); Enter creates the mark.

Tests (all run on the standard 3-engine matrix — no project filter):
1. "Tab reaches the toolbar and Enter creates a highlight (paginated mode)": openArticle (paginated default) → findFirstBlockWithText(page, 24) → selectRangeInBlock(page, idx, 0, 18) → expect .selection-toolbar visible with role toolbar + accessible name Highlight actions → page.keyboard.press("Tab") (REAL Tab — this is the assertion that failed under NVDA+Firefox) → atomically (single evaluate against the button locator) assert document.activeElement is the toolbar's Highlight button (exact:true) AND the toolbar is still connected to the DOM (the Gecko/WebKit collapse regression assertion — pre-fix the toolbar unmounts here) → press Enter → expect mark.highlight[data-highlight-id] visible, announcementRegion contains Highlight saved, and .selection-toolbar count 0.
2. Same flow in scrolling mode: switchMode(page) first (modeToggle/m per the helper), re-find a visible block in the new surface, then the identical Tab→focus→Enter→mark+announce+dismiss sequence. Both modes covered per the G6 constraint.
3. "Tabbing past the toolbar dismisses it": select → Tab (focus lands on Highlight) → Tab twice more (Highlight + note → out of the toolbar) → expect .selection-toolbar count 0 and activeElement NOT inside a .selection-toolbar (containment releases; no wedged toolbar).

Then STRENGTHEN tests/e2e/annotations/keyboard-shortcuts.spec.ts L140-167 (strengthen-only — keep the existing programmatic-focus assertion and file header): replace the comment claiming Tab-activation is not assertable (it names the exact collapse this plan fixes — that prose must go) and extend the test body: after the existing isFocused check, press Tab, assert document.activeElement is the Highlight button, press Enter, expect mark.highlight first visible + announcementRegion contains Highlight saved + toolbar count 0. Reference toolbar-tab-path.spec.ts as the full-coverage spec in the updated comment.

Do NOT use selectViaKeyboard (dormant helper; real-arrow selection from a focused block is engine-divergent per the spec header comment at L36-38). selectRangeInBlock + real Tab presses is exactly the reproduction method the debug session validated (programmatic selections reproduce the collapse because it is focus-driven, not selection-origin-driven).

This task ends RED by design: pre-fix, tests 1-2 fail in firefox + webkit at the toolbar-survival/focus assertion (unmount on collapse) and ALSO fail in chromium at the single-Tab-lands assertion (the toolbar is last in DOM order — the experiment needed Tab #10). Record the observed per-engine failure output in the summary as RED evidence; do not "fix" the spec to pass.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/toolbar-tab-path.spec.ts --project=firefox 2>&1 | tail -20</automated>
  </verify>
  <done>Spec exists with the three tests; firefox project run shows failures at the Tab-focus/toolbar-survival assertions (RED evidence recorded); keyboard-shortcuts.spec.ts L140-167 carries the real Tab-activation assertion with the stale non-assertability comment removed and existing assertions intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — focus-containment lifecycle, Tab routing, saved-range activation, announce-on-appear</name>
  <files>src/routes/ArticleView.tsx, src/reader/annotations/SelectionToolbar.tsx</files>
  <behavior>
    - Test (toolbar-tab-path #1/#2): after a selection, one Tab press → document.activeElement is the toolbar Highlight button AND the toolbar is still connected, in chromium/firefox/webkit, both reading modes
    - Test (toolbar-tab-path #3): Tab out of the toolbar's last button → toolbar unmounts
    - Test (toolbar-tab-path #1/#2): Enter on the focused Highlight button → mark.highlight[data-highlight-id] + Highlight saved. announcement + toolbar dismissed
    - Existing capture-highlight.spec.ts (mouse clicks), keyboard-shortcuts.spec.ts (H/N/M), cross-fragment/navigate-back/note specs stay green — no pointer-path or Chromium regression
  </behavior>
  <action>
All changes scoped to the two files; ZERO changes to HighlightOverlay.tsx, highlight/highlight selectors, or the mark DOM contract. Gap reason: Gecko/WebKit collapse the selection synchronously on any DOM focus move (verified inside the focus() call itself — see debug session firefox-iso evidence), so a purely selectionchange-driven lifecycle is keyboard-hostile there.

In src/routes/ArticleView.tsx:

1. Saved-range ref: add lastValidRangeRef = useRef&lt;Range | null&gt;(null) beside the selectionRect state (L275-281). In the selectionchange rAF callback, at the valid branch where setSelectionRect(range.getBoundingClientRect()) runs (L746), also store range.cloneRange() into the ref. Clear the ref in every branch that clears selectionRect/captureResult (collapsed L713-716, outside-article L728-729, measurement-body L742-743, the post-activation clear L609-610, and the article-change reset L968-969). The clone is the ONLY persisted selection state; captureResult/selectionRect continue to drive display as today.

2. Focus-containment guard: in the collapsed-selection branch of the selectionchange listener (L713-716), BEFORE clearing state, check whether the toolbar currently owns focus: document.activeElement is an Element inside .selection-toolbar (activeElement.closest(".selection-toolbar") is the one-line check). If so, return without clearing — the toolbar stays mounted with its last captureResult/selectionRect while it contains document.activeElement. Leave the outside-article and measurement-body branches unchanged (a collapsed selection while toolbar-focused only ever routes through the collapsed branch). This is debugger fix direction (a)/(c) — the standard floating-toolbar focus-containment pattern.

3. Tab routing: in the existing window keydown listener (L622-671, already guarded by isFormField and scoped to article+articleEl mounts), add a branch for plain Tab (event.key === "Tab", NOT shiftKey — never touch Shift+Tab). Fire only when ALL hold: captureResult?.ok === true (never route into a hint-only toolbar — no buttons to focus), selectionRect is non-null, and document.activeElement is document.body OR is contained by articleRef.current (the reading context), and activeElement is NOT already inside .selection-toolbar (let focus move naturally within/past the toolbar). When it fires: event.preventDefault(), then focus the toolbar's first button — query .selection-toolbar button within the mounted view and call .focus() on it. Consequence per engine: chromium keeps the live selection (activation proceeds normally); firefox/webkit collapse it synchronously inside focus() → the containment guard from step 2 keeps the toolbar mounted. This is what makes protocol C2 (Tab to it) a single deterministic Tab instead of a walk past every article focusable (the toolbar sits near the END of DOM order — L1976-1990). Guard rationale to record in the comment: dialogs/popovers (NotePopover, tag popover, drawer) are never intercepted because their focus targets are outside articleRef and are not body.

4. Saved-range activation restore: in handleHighlightShortcut (L590-618), after reading window.getSelection(), if the live selection is collapsed/absent BUT document.activeElement is inside .selection-toolbar AND lastValidRangeRef.current is non-null, restore the selection: sel.removeAllRanges(); sel.addRange(lastValidRangeRef.current) — then proceed into the existing flow unchanged (the collapsed-bail now sees a live selection). The restore guard (activeElement inside the toolbar) means H/N pressed with focus anywhere else can never resurrect a stale selection — the pre-fix keyboard-shortcut behavior is preserved exactly. The restored Range re-enters createHighlightFromSelection → captureCurrentSelection, so the single-block rule (D5-05/D5-06), overlap check (D5-13), measurement-body guard, and grapheme capture all re-validate against the live DOM — ONE creation path, zero forked validation (05-05 discipline). A stale/detached Range fails containment inside capture and returns a typed not-ok → calm bail (no crash path).

5. Do NOT auto-focus the toolbar on appear (explicitly rejected in the debug analysis): Gecko/WebKit would collapse the selection — and visually destroy the reader's selection highlight — on every mouse selection. Pointer users keep today's behavior; Tab is the keyboard/SR route.

In src/reader/annotations/SelectionToolbar.tsx:

6. Announce-on-appear (gap missing item 1, second half): add a visually-hidden polite live region as a child of the toolbar root — a p with className "visually-hidden" and role "status" (the repo's established announce pattern; class exists in app.css L154). The region is present whenever the toolbar renders (live regions must pre-exist to announce reliably); its text is set in a useEffect ONLY on the transition into the buttons variant (captureResult?.ok true, previous render was not buttons — guard with a prev ref, mirroring the measuredSize prev-guard discipline at L104-119): copy exactly "Highlight actions available." Never announce on rect-only updates (selection reshaping) or the hint variants (out of gap scope); never clear the CRUD announcement region (annotationAnnouncement is a separate region and stays untouched — D5-12 copy unchanged).

7. Replace the stale keyboard-path comment block (L22-24 — "the keyboard path is the H/N shortcuts ... Tab still reaches the toolbar buttons as a fallback"): the toolbar IS the primary SR path (Phase 6 protocol rewrite + this fix). The new comment states: toolbar reachable by a single Tab from the reading context in all engines; lifecycle keeps it mounted while it contains focus; H/N remain the sighted keyboard convenience (SRs consume bare letters); cite G6 + the debug session.

8. Geometry, position computation, role=toolbar + aria-label Highlight actions, the two buttons' names, hint copy, and the no-transition reduced-motion discipline stay byte-unchanged (G5 geometry lessons and firstPageReservedPx contracts untouched — this fix never touches CSS or pagination).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/toolbar-tab-path.spec.ts tests/e2e/annotations/keyboard-shortcuts.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All toolbar-tab-path tests pass on chromium, firefox, and webkit projects (the Task-1 RED is now GREEN); keyboard-shortcuts.spec.ts fully green including the strengthened test; zero provider/mark-contract changes.</done>
</task>

<task type="auto">
  <name>Task 3: ACCEPTANCE-PROTOCOL Flow C — document the NVDA (Firefox) selection and activation gestures</name>
  <files>docs/ACCEPTANCE-PROTOCOL.md</files>
  <action>
UAT artifact 3: Flow C documents VoiceOver gestures only (C1: VO+Enter … VO+Enter; C3: VO+Space) with no NVDA guidance — the ACPT-05 re-run pairing IS NVDA+Firefox. Edit ONLY the Flow C table rows (L186-191) — no other flows, no pass-criteria rewording, no claims about re-run results:

C1 row: extend the selection-gesture parenthetical so it reads for all three reader types — sighted keyboard Shift+Right arrow (existing text), VoiceOver VO+Enter … arrows … VO+Enter (existing), and add "NVDA (Firefox): browse-mode Shift+arrows (Firefox-native selection)" — the gesture named in the G6 diagnosis. Keep the D5-05/D5-06 single-block expected outcome sentence unchanged.

C2 row: no gesture changes needed (Tab is engine-native and now actually works everywhere after Task 2); leave the row byte-unchanged unless a stray VoiceOver-only aside exists — there is none.

C3 row: extend the activation parenthetical — after the existing VoiceOver VO+Space, add "NVDA (Firefox): Enter (browse mode passes Enter through; NVDA+Space also activates)" — grounded in the debug session's NVDA key-passthrough model. Expected outcome (mark.highlight[data-highlight-id] + role=status confirmation) unchanged.

Also update the Flow C intro callout (L176-184) with one added sentence after the tester-confirmed VoiceOver sentence, factual and forward-looking only: the toolbar is now keyboard-reachable via a single Tab from the reading context in all supported engines (G6 fix), so the re-run may exercise C2 exactly as documented. Do not record any finding, verdict, or re-run date — the re-run record belongs to 13-VERIFICATION.md and is the tester's.
  </action>
  <verify>
    <automated>rg -c "NVDA \\(Firefox\\)" docs/ACCEPTANCE-PROTOCOL.md</automated>
  </verify>
  <done>Flow C1 and C3 rows each carry the NVDA (Firefox) gesture note (grep count >= 2); C2 row and all other flows byte-unchanged (git diff confined to the Flow C section); no re-run results claimed anywhere in the doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user selection/DOM events → annotation creation | ephemeral Selection/Range state feeding the capture pipeline (existing boundary, unchanged) |
| keyboard events → toolbar focus routing | new Tab-interception listener on window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11-01 | Tampering | saved-range restore path (handleHighlightShortcut) | mitigate | the restored Range is re-validated end-to-end by the unchanged captureCurrentSelection pipeline (single-block D5-05/D5-06, overlap D5-13, measurement-body, article containment) — no capture data is trusted from persisted state; restore is gated on toolbar focus |
| T-13-11-02 | Denial of Service | Tab-interception keydown listener | mitigate | guarded to plain Tab + valid capture + focus in the reading context/body; never fires in form fields (isFormField), dialogs, or Shift+Tab; e2e test 3 proves Tab still exits and dismisses |
| T-13-11-03 | Information Disclosure | announce-on-appear live region | accept | static copy only; no user content echoed; reuses the audited visually-hidden role=status pattern |
| T-13-11-SC | Tampering | package installs | accept | zero new packages; zero dependency changes in this plan |
</threat_model>

<verification>
1. Task-level: toolbar-tab-path.spec.ts + keyboard-shortcuts.spec.ts green on all three engine projects (npx playwright test with both spec paths).
2. Regression net for the G6 constraints (no Chromium regression, mark DOM contract, role=status confirmation, pointer path): npx playwright test tests/e2e/annotations/ 2>&1 | tail -15 — the full annotations directory green across chromium/firefox/webkit.
3. Unit suite untouched-but-honest: npm run test:unit -- --run exits 0 (no production units were modified, but run it — Rule 2 honesty).
4. Protocol diff discipline: git diff docs/ACCEPTANCE-PROTOCOL.md confined to Flow C rows + the one callout sentence.
</verification>

<success_criteria>
- G6 truth closed at the automation level: single-Tab toolbar reachability + Enter activation (mark + polite confirmation) proven in chromium, firefox, AND webkit, in BOTH reading modes
- The automation gap that hid the defect is permanently closed (real Tab-walk spec; the keyboard-shortcuts skip converted to a live assertion)
- No regression: annotations e2e directory fully green; single-block rule, mark.highlight[data-highlight-id] contract, and Highlight saved. role=status announcement all byte-preserved through the unchanged creation path
- NVDA (Firefox) gestures documented in Flow C1/C3 so the tester's ACPT-05 re-run (D13-06, outside this plan) is unambiguous
- ACPT-05 itself is NOT flipped by this plan — it flips only when the user's re-run results land in 13-VERIFICATION.md §1.3/§1.4 with zero blocker/major (D13-07)
</success_criteria>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-11-SUMMARY.md` when done (requirements-completed: [] — ACPT-05 stays Pending until the tester's re-run per D13-06/D13-07; note in the summary that the re-run prerequisite has landed).
</output>
