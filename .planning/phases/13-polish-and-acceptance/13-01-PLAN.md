---
phase: 13-polish-and-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/settings/settingsMirror.ts
  - src/settings/SettingsContext.tsx
  - tests/unit/settings/mirror.test.ts
  - tests/e2e/polish/cold-load-no-snap.spec.ts
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "A reader who cold-loads with persisted non-default settings (theme dark, font sans, size 22, readingMode scrolling) sees those settings from the first paint — no default-to-persisted flip exists anywhere in the documentElement mutation timeline"
    - "The scrolling surface is present from the first ArticleView paint when the persisted readingMode is scrolling (no paginated-first snap)"
    - "A wiped reader cold-loads to defaults — the mirror key does not survive the wipe (no zombie preferences)"
    - "A corrupt, absent, or quota-blocked mirror fails silent to defaults — never a crash, never recovery UI"
  artifacts:
    - path: "src/settings/settingsMirror.ts"
      provides: "Single mirror seam: read/write/clear of the localStorage settings mirror"
      exports: ["readSettingsMirror", "writeSettingsMirror", "clearSettingsMirror", "SETTINGS_MIRROR_KEY"]
      min_lines: 30
    - path: "index.html"
      provides: "Inline pre-React paint-hint script (D13-01)"
      contains: "lem-settings-mirror-v1"
    - path: "tests/unit/settings/mirror.test.ts"
      provides: "Mirror seam unit tests + inline-script/token sync check"
      min_lines: 60
    - path: "tests/e2e/polish/cold-load-no-snap.spec.ts"
      provides: "SC#1 cold-load no-snap e2e across 3 engines"
      min_lines: 80
  key_links:
    - from: "index.html inline script"
      to: "localStorage SETTINGS_MIRROR_KEY"
      via: "defensive read + setProperty/dataset writes on documentElement before React mounts"
      pattern: "setProperty"
    - from: "src/settings/SettingsContext.tsx"
      to: "src/settings/settingsMirror.ts"
      via: "lazy-init state from mirror; mirror write rides scheduleSave/flushSave; resetLocalData clears"
      pattern: "readSettingsMirror"
    - from: "index.html inline script token copies"
      to: "src/settings/tokens.ts"
      via: "sync-check unit test asserts inline FONT_STACKS/SPACING_PRESETS values equal the module maps (A2)"
      pattern: "FONT_STACKS"
  prohibitions:
    - statement: "Mirror write/read failures are never routed through classifyStorageError or setStorageState (Pitfall 4)"
      status: resolved
      verification: "unit test: localStorage.setItem throwing during a settings save leaves storageState ok and does not crash"
    - statement: "The inline script contains no markup-string or HTML-injection APIs (innerHTML, insertAdjacentHTML, outerHTML)"
      status: resolved
      verification: "grep -c 'innerHTML\\|insertAdjacentHTML\\|outerHTML' index.html returns 0"
    - statement: "No new packages installed this plan"
      status: resolved
      verification: "package.json dependency arrays unchanged in this plan's commits"
---

<objective>
Kill the first-paint settings flash (POLISH-01, D13-01/D13-02/D13-03): a localStorage mirror of the persisted ReaderSettings plus an inline pre-React script in index.html paints mode/theme/typography tokens before React mounts, SettingsContext lazy-inits from the mirror so the first React render matches, Dexie stays the source of truth and self-corrects a stale mirror, and a Playwright cold-load no-snap test proves zero mode/theme/token flip on first paint.

Purpose: "The jumping around of content on load is really bad right now" — the user's felt pain; the mirror covers ALL settings (D13-02), not just the mode flip.
Output: settingsMirror.ts seam, index.html inline script, SettingsContext lazy-init/write/clear wiring, mirror unit tests, cold-load-no-snap e2e.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror seam module (settingsMirror.ts) + unit tests</name>
  <files>src/settings/settingsMirror.ts, tests/unit/settings/mirror.test.ts</files>
  <read_first>
  - src/persistence/settingsStore.ts (seam-module + Zod-at-read conventions — the analog, per 13-PATTERNS)
  - src/settings/defaults.ts (DEFAULT_SETTINGS record shape)
  - src/content/schema.ts (ReaderSettingsSchema)
  - .planning/phases/13-polish-and-acceptance/13-PATTERNS.md (§settingsMirror.ts assignment)
  </read_first>
  <behavior>
    - Test: readSettingsMirror returns null when the key is absent (first run paints defaults)
    - Test: writeSettingsMirror then readSettingsMirror round-trips a full ReaderSettings record (theme "dark", font "sans", size 22, readingMode "scrolling")
    - Test: readSettingsMirror returns null on a stored value that fails ReaderSettingsSchema (corrupt JSON, wrong value shapes, unknown schemaVersion)
    - Test: readSettingsMirror returns null (does not throw) when localStorage.getItem throws
    - Test: writeSettingsMirror is a silent no-op (does not throw) when localStorage.setItem throws (quota exceeded)
    - Test: clearSettingsMirror removes the key; clearing an absent key is a no-op
  </behavior>
  <action>
    Create src/settings/settingsMirror.ts following the settingsStore.ts seam conventions: opening header comment citing D13-01/D13-02 (mirror is a best-effort paint hint; Dexie via loadSettings stays the sole source of truth; the mirror never routes recovery UI per D13-03). Export const SETTINGS_MIRROR_KEY with value "lem-settings-mirror-v1". Export readSettingsMirror(): ReaderSettings | null — reads the key from window.localStorage, runs ReaderSettingsSchema.safeParse, returns the parsed settings or null on absent/invalid/throwing (null-on-doubt discipline copied from settingsStore's never-silently-coerce read). Export writeSettingsMirror(s: ReaderSettings): void — JSON-serializes the FULL record (all settings in one key per D13-02) and writes it; the entire body is wrapped so any failure is a silent no-op (Pitfall 4 — never classified, never surfaced). Export clearSettingsMirror(): void — removes the key, same silent-failure discipline. This module is the single seam: no other src/ module touches localStorage for settings. Create tests/unit/settings/mirror.test.ts (new tests/unit/settings/ dir; .test.ts extension per tests/unit/pagination convention) covering the six behaviors above, stubbing a throwing localStorage via Object.defineProperty on window/localStorage getter or a replaced global per vitest jsdom conventions.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings/mirror.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/settings/settingsMirror.ts contains "export function readSettingsMirror(", "export function writeSettingsMirror(", "export function clearSettingsMirror(", and SETTINGS_MIRROR_KEY = "lem-settings-mirror-v1"
    - npx vitest run tests/unit/settings/mirror.test.ts exits 0 with all six behaviors as separate test cases
    - rg -l "lem-settings-mirror-v1" src/ returns exactly src/settings/settingsMirror.ts (single seam; index.html joins in Task 2)
    - The module imports ReaderSettingsSchema from src/content/schema.ts (no re-declared shape)
  </acceptance_criteria>
  <done>Mirror seam ships with green unit tests proving null-on-doubt reads and no-op-on-failure writes.</done>
</task>

<task type="auto">
  <name>Task 2: Inline pre-React script + SettingsContext lazy-init/write/clear seams + token sync test</name>
  <files>index.html, src/settings/SettingsContext.tsx, tests/unit/settings/mirror.test.ts</files>
  <read_first>
  - index.html (current 12-line shell — placement point)
  - src/settings/applyTheme.ts (the EXACT set of writes the script replicates: data-theme + --font-body, --font-size, --line-height, --letter-spacing, --word-spacing, --measure)
  - src/settings/tokens.ts (FONT_STACKS, SPACING_PRESETS — the value maps duplicated inline)
  - src/settings/SettingsContext.tsx (L61 initializer, L74-76 mount applyTheme, L81-105 hydrate effect, L113-129 scheduleSave, L132-146 flushSave, L194-207 resetLocalData)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pattern 1 — the two-part remedy; Pitfalls 1-4)
  </read_first>
  <action>
    (a) index.html: add a synchronous inline script BEFORE the /src/main.tsx module script. Whole body defensive: try/catch everything, guarded JSON parse, absent or non-object value returns early so the CSS literal fallbacks paint (18px/1.6/0 — the 02-04 cascade contract). On a valid mirror it writes the SAME tokens applyTheme writes: documentElement dataset theme, then the six custom properties --font-body, --font-size, --line-height, --letter-spacing, --word-spacing, --measure, resolving font/spacing through inline value-map copies of FONT_STACKS and SPACING_PRESETS keyed by the record's font/spacing values (the script cannot import tokens.ts); an unknown key skips that single write; size/measure are numeric with px/ch units. Only style-property writes and dataset assignment — never markup-string or HTML-injection APIs (T-02-02 parity; T-13-03). (b) SettingsContext: change the L61 initializer to lazy-init from the mirror (readSettingsMirror() nullish-coalescing DEFAULT_SETTINGS) so the mount-time applyTheme effect writes byte-identical values to the inline script (Pitfall 2 — React must not re-introduce the flash). Mirror writes ride the existing save path: in the scheduleSave timer callback and in flushSave, call writeSettingsMirror with the same ReaderSettings value being persisted to Dexie, before the Dexie call; all failure handling lives inside the seam (silent no-op). On successful loadSettings hydration, when the Dexie-parsed settings differ from what the mirror carries, write the corrected mirror immediately (stale self-correct in the same session — RESEARCH Open Question 2 recommendation). resetLocalData gains clearSettingsMirror() next to its existing in-memory reset — the single post-wipe seam (Pitfall 1: localStorage survives db.delete()). Do NOT touch loadSettings/classifyStorageError semantics (Pitfall 3 — STATE-05 paths never read the mirror). (c) Append a describe block to tests/unit/settings/mirror.test.ts: read index.html from disk, extract the inline script, and assert its FONT_STACKS/SPACING_PRESETS value copies are equal to the tokens.ts maps (the A2 drift guard), and assert the script appears before the module script tag.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings/mirror.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains the inline script tag before the /src/main.tsx module script reference, and grep -c 'innerHTML\|insertAdjacentHTML\|outerHTML' index.html returns 0
    - src/settings/SettingsContext.tsx: the useState initializer references readSettingsMirror; scheduleSave and flushSave bodies each contain writeSettingsMirror; resetLocalData contains clearSettingsMirror
    - The sync-check describe asserts inline token copies equal tokens.ts maps for every FONT_STACKS key and SPACING_PRESETS key
    - npx vitest run tests/unit/settings/mirror.test.ts exits 0 and npx tsc --noEmit exits 0
    - git diff shows src/persistence/settingsStore.ts and src/settings/applyTheme.ts byte-unchanged
  </acceptance_criteria>
  <done>Both halves of the FOUC remedy wired: pre-React paint hint + React lazy-init; mirror write/clear ride existing seams; sync drift guard green.</done>
</task>

<task type="auto">
  <name>Task 3: Cold-load no-snap e2e (SC#1)</name>
  <files>tests/e2e/polish/cold-load-no-snap.spec.ts</files>
  <read_first>
  - tests/e2e/progress.spec.ts (wipe discipline, computed-style reads, expect.poll settle pattern)
  - tests/e2e/portability/_portability.ts (seedRows — raw Dexie settings-row seeding)
  - tests/e2e/portability/round-trip.spec.ts (L111-127 non-default prefs seeding precedent: font sans, size 22, theme dark)
  - tests/e2e/annotations/_fixtures.ts (openArticle helper)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (§Code Examples — no-snap MutationObserver mechanism)
  </read_first>
  <action>
    New spec tests/e2e/polish/cold-load-no-snap.spec.ts, plain test() declarations so playwright.config.ts runs it on chromium/firefox/webkit. Mechanism: page.addInitScript seeds the mirror key (localStorage set of the serialized non-default record) AND installs a MutationObserver on documentElement (attributeFilter data-theme and style) that appends {timestamp, dataTheme, fontSizeToken} records into a window array from navigation start. Seed the Dexie truth with the SAME values (raw settings-store put of the reader-prefs row: theme dark, font sans, size 22, readingMode scrolling) via the seedRows/evaluate precedent, and open a fixture article. Assert after hydration settle (expect.poll for a stable record tail plus the scrolling surface): (1) the FIRST recorded mutation already carries the persisted data-theme and --font-size (inline script worked); (2) NO record in the entire timeline shows a data-theme or --font-size different from the persisted values (no default-to-persisted flip — Pitfall 2 catch); (3) the scrolling article body is present from the first article paint and the paginated-surface class never appears (mode lazy-init worked). Second test (wipe): seed persisted settings, drive the WipeConfirm destructive path per the existing settings/wipe e2e precedent, reload — defaults paint (records show default theme) and localStorage mirror read evaluates to null (Pitfall 1 catch). expect.poll for every end condition; zero fixed sleeps.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/polish/cold-load-no-snap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/polish/cold-load-no-snap.spec.ts exits 0 on chromium, firefox, and webkit
    - The spec asserts all three no-snap clauses as separate expectations: first-record-carries-persisted-tokens, no-flip-anywhere-in-timeline, scrolling-surface-from-first-paint
    - The wipe test asserts the mirror key reads back null after the destructive flow
    - The spec contains no waitForTimeout call (rg -c 'waitForTimeout' on the file returns 0)
  </acceptance_criteria>
  <done>SC#1 proven in real browsers: persisted settings paint first with zero flip; wipe leaves no zombie mirror.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → inline script → CSS | Hand-edited/corrupt mirror values are untrusted input parsed pre-React |
| localStorage → SettingsContext lazy-init | Mirror values become initial React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Mirror poisoning via hand-edited localStorage | mitigate | ReaderSettingsSchema.safeParse at the read seam, null-on-doubt; script coerces through enum/number token maps before any value reaches setProperty — worst case a bad-looking theme, never script execution |
| T-13-02 | DoS (local) | localStorage quota/security exceptions crashing settings save | mitigate | Mirror writes are try/catch no-ops inside the seam; unit test proves setStorageState unreachable from a mirror write failure |
| T-13-03 | Tampering | Inline script string handling (index.html) | mitigate | Script writes only via style-property and dataset APIs; acceptance grep proves no markup-string APIs present |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (research-locked); any dependency addition in a commit is out of scope and rejectable at review |
</threat_model>

<verification>
- Unit: npx vitest run tests/unit/settings/mirror.test.ts (seam behaviors + token sync guard)
- e2e: npx playwright test tests/e2e/polish/cold-load-no-snap.spec.ts (3 engines)
- Regression: npx playwright test tests/e2e/typography-live-apply.spec.ts tests/e2e/persistence.spec.ts (settings apply/persist surfaces untouched semantically)
</verification>

<success_criteria>
- POLISH-01's SC#1 test passes: cold load with persisted non-default settings shows zero mode/theme/token flip across the mutation timeline, on all three engines
- Wipe clears both truths; corrupt mirror fails silent to defaults
- No regression in settings/persistence suites; no new packages
</success_criteria>

<artifacts_produced>
- src/settings/settingsMirror.ts — new module: SETTINGS_MIRROR_KEY constant, readSettingsMirror(), writeSettingsMirror(), clearSettingsMirror()
- index.html — new inline pre-React mirror script (unnamed IIFE; no exports)
- src/settings/SettingsContext.tsx — modified: lazy-init initializer, mirror writes in scheduleSave/flushSave, hydrate-diff self-correct, clearSettingsMirror in resetLocalData
- tests/unit/settings/mirror.test.ts — new (mirror behaviors + index.html/token sync check)
- tests/e2e/polish/cold-load-no-snap.spec.ts — new (SC#1)
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-01-SUMMARY.md` when done
</output>
