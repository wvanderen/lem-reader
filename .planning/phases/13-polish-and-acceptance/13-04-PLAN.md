---
phase: 13-polish-and-acceptance
plan: 04
type: execute
wave: 2
depends_on: ["13-03"]
files_modified:
  - src/reader/BackToLibrary.tsx
  - src/App.tsx
  - src/routes/ArticleView.tsx
  - src/routes/review/ReviewView.tsx
  - src/app.css
  - tests/e2e/chrome/back-nav.spec.ts
  - tests/e2e/chrome/header-geometry.spec.ts
autonomous: true
requirements: [POLISH-03, POLISH-05]
must_haves:
  truths:
    - "A reader in an article sees a slim header — title and essential controls only — with no internal header scrolling at 360×640"
    - "A reader edits tags and sees byline/source at a calm metadata spot at the article top, rendered once — not repeated on every paginated page"
    - "A reader activates 'Back to library' from an article or the review panel and lands in the library; a fresh deep-link never exits the app; the control is keyboard-operable"
    - "Pagination and initial-focus behavior are unregressed (corpus + open-every-fixture specs green)"
  artifacts:
    - path: "src/reader/BackToLibrary.tsx"
      provides: "Shared back affordance with history.back + #/ fallback (D13-15)"
      exports: ["BackToLibrary"]
      min_lines: 20
    - path: "tests/e2e/chrome/back-nav.spec.ts"
      provides: "Back-nav e2e: in-app return, deep-link fallback, review panel, keyboard"
      min_lines: 60
    - path: "tests/e2e/chrome/header-geometry.spec.ts"
      provides: "Header geometry spec: no internal scroll at 360×640; render-once metadata spot"
      min_lines: 50
  key_links:
    - from: "src/App.tsx"
      to: "src/reader/BackToLibrary.tsx"
      via: "hasAppHistory flag set on the first in-app hashchange after mount, passed as a prop to ArticleView and ReviewView"
      pattern: "hasAppHistory"
    - from: "src/routes/ArticleView.tsx"
      to: "src/reader/BackToLibrary.tsx + src/reader/TagEntry.tsx"
      via: "BackToLibrary mounts at the article header start; TagEntry moves to the ArticleView-owned article-top metadata spot rendered once, outside the pinned header row and outside the pagination block stream"
      pattern: "BackToLibrary"
  prohibitions:
    - statement: "The 09-07 geometry cap rule (.article-body.paginated-surface grid minmax(auto, 25%) header row) stays byte-unchanged"
      status: resolved
      verification: "git diff shows no changed line in the .article-body.paginated-surface grid rule block"
    - statement: "The metadata spot never mounts inside the grid header row (no page-viewport height oscillation between turns → no ResizeObserver re-measure loop)"
      status: resolved
      verification: "code review of mount point + pagination corpus specs green after turns"
    - statement: "TagEntry keeps its inert-at-mount discipline at the new home (no auto-focus attribute, no mount-time focus call — Pitfall 8-5/6)"
      status: resolved
      verification: "rg -c 'autoFocus|\\.focus\\(' src/reader/TagEntry.tsx returns 0 and open-every-fixture initial-focus assertions pass"
    - statement: "The 12-06 chapter chrome (fixed positioning outside grid flow) is unchanged"
      status: resolved
      verification: "git diff shows no changes to chapter-nav positioning rules or ChapterNav component"
    - statement: "No new CSS motion properties in this plan's app.css additions"
      status: resolved
      verification: "git diff added-lines contain no transition/animation property"
---

<objective>
The remaining two chrome-polish items (D13-12 widened scope): slim the article header by moving TagEntry, byline/source meta, and the per-article export button out of the pinned per-page header into an ArticleView-owned article-top metadata spot rendered once (POLISH-03, D13-13), and add the standardized keyboard-reachable "Back to library" affordance with history.back plus #/ fallback on the article and review views (POLISH-05, D13-15) — honoring the 09-07 geometry lesson (header growth broke pagination) at every step.

Purpose: "In page mode that header will be on every page — needs to be pretty minimal" and "There's no real navigation… we need standardized nav."
Output: BackToLibrary component, App history flag, slimmed header + render-once metadata spot, two chrome e2e specs, pagination/focus regressions green.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BackToLibrary component + App history flag + mounts + back-nav spec</name>
  <files>src/reader/BackToLibrary.tsx, src/App.tsx, src/routes/ArticleView.tsx, src/routes/review/ReviewView.tsx, src/app.css, tests/e2e/chrome/back-nav.spec.ts</files>
  <read_first>
  - src/App.tsx (parseHash L51-60 — unknown #/ deep links map to the list; hashchange handler L170-177 Gap-3 guard — only #-prefixed hashes route, so assigning location.hash "#/" is always a valid route-to-library)
  - src/ingestion/library/LibraryView.tsx (L202-210 quiet-button precedent reusing .article-export-highlights tokens)
  - src/routes/review/ReviewView.tsx (L320-323 review header mount point)
  - src/routes/ArticleView.tsx (article header block L1528-1579 — Task 2 slims it; this task prepends the affordance)
  - src/reader/PageIndicator.tsx (minimal-component discipline: header comment citing decisions, single responsibility)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pitfall 7 — history.back exiting the app on deep links; §Code Examples back-affordance sketch)
  </read_first>
  <action>
    Create src/reader/BackToLibrary.tsx: opening header comment citing D13-15; props { hasAppHistory: boolean }; a native button with type "button", className "back-to-library", and calm label copy "Back to library"; onClick calls history.back() when hasAppHistory is true, otherwise assigns location.hash the library route "#/" (the fallback that keeps a fresh deep-link tab inside the app — Pitfall 7). In src/App.tsx, track in-app navigation with a flag that flips true the first time the hashchange handler routes an in-app hash AFTER initial mount (the initial load does not count — a fresh deep-link tab has no prior in-app entry; a reload mid-article also does not fake in-app history); pass hasAppHistory down to ArticleView and ReviewView as a prop. Mount BackToLibrary at the very start of the article header (before the title cluster) and at the start of the review header — identical anatomy in both places. Add quiet-button CSS in app.css mirroring the .article-export-highlights token discipline; native button = keyboard-reachable by construction; zero motion properties. New spec tests/e2e/chrome/back-nav.spec.ts (plain test(), 3 engines): (a) library → open an article → activate Back to library → library view visible at hash "#/"; (b) fresh context, direct goto of a fixture article deep link → activate → location.hash is "#/" and the library view is visible (assert the page did NOT navigate away from the app origin); (c) "#/review" → activate Back to library → library; (d) keyboard: the control is reachable via Tab from the article (role button, accessible name "Back to library") and activates with Enter. Cover both mount points (article + review).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/back-nav.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/reader/BackToLibrary.tsx exists, exports BackToLibrary, contains history.back and the "#/" fallback assignment
    - src/App.tsx sets the hasAppHistory flag on first post-mount in-app hashchange and passes it to both views; ArticleView and ReviewView render BackToLibrary at their header starts
    - tests/e2e/chrome/back-nav.spec.ts exits 0 on chromium, firefox, and webkit covering cases a-d including the deep-link no-exit assertion
  </acceptance_criteria>
  <done>One calm back affordance, same anatomy on every view, deep-link safe, keyboard operable.</done>
</task>

<task type="auto">
  <name>Task 2: Header slim + article-top metadata spot + geometry spec</name>
  <files>src/routes/ArticleView.tsx, src/app.css, tests/e2e/chrome/header-geometry.spec.ts</files>
  <read_first>
  - src/routes/ArticleView.tsx (header block L1528-1579: h1, .meta byline, book-context line, source link, TagEntry at L1563-1566, Export-highlights button at L1573; the L356-362 effectiveMode machinery — MUST stay untouched)
  - src/reader/TagEntry.tsx (inert-at-mount discipline L7-22 — carries verbatim to the new home)
  - src/app.css (L1012-1044 the 09-07 .article-body.paginated-surface grid cap — byte-unchanged; ~L2854 chapter-nav fixed-chrome precedent)
  - src/reader/PaginatedSurface.tsx (surface + page viewport structure — where the metadata spot may NOT perturb measurement)
  - tests/e2e/open-every-fixture.spec.ts (initial-focus assertions that must stay green)
  - tests/e2e/pagination/ (corpus specs that must stay green — PAGE-03/04 geometry bar)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pitfalls 5-6; Open Question 3 — metadata-spot anatomy)
  </read_first>
  <action>
    Move OUT of the pinned per-page header: the TagEntry form, the byline/source .meta cluster (book-context line semantics per 12-06 D12-08 preserved), and the per-article Export-highlights button. The pinned header becomes: BackToLibrary (Task 1) + the title (h1) + essential controls only (mode toggle, settings trigger; PageIndicator placement unchanged). Create an ArticleView-owned article-top metadata spot rendered EXACTLY ONCE in the DOM holding byline, source link when present, book-context line, TagEntry, and the Export-highlights button. Placement contract: in scrolling mode it is ordinary flow content above the article body (scrolls away naturally). In paginated mode it renders only while the reader is at article start (first page) — mounted OUTSIDE the grid header row (so the page-viewport row height never oscillates between turns — that would re-fire ResizeObserver re-measurement, the 04-06/04-08 loop class) and OUTSIDE the pagination block stream (never enters page-capacity math; if its height pushes page-1 content, the post-render overflow guard re-split is the documented safety net, and initial pagination-settle is explicitly out of scope per D13-03). Validate the geometry empirically against the acceptance bar; if the first-page-only mount measurably breaks a pagination corpus spec, fall back to keeping the spot mounted at article start above the surface with visibility tied to being at article start — the bar (render-once semantics + green corpus + no header scroll at 360×640) is the contract, the mechanism within these constraints is the executor's to finalize. TagEntry itself stays byte-identical (component untouched; only its mount point moves). CSS: single-line slimmed header anatomy at reference viewport; token-only; zero motion. New spec tests/e2e/chrome/header-geometry.spec.ts: at viewport 360×640 in paginated mode on a corpus fixture — assert the header element reports scrollHeight ≤ clientHeight (no internal scrolling); assert exactly ONE TagEntry instance in the document, located at the article-top spot, and the per-page header contains no tags form and no export button; turn to page 2 and assert the metadata spot is no longer visible and the TagEntry count stays 1. Then run the pagination corpus specs and open-every-fixture as the regression gate.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/header-geometry.spec.ts tests/e2e/pagination/ tests/e2e/open-every-fixture.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/chrome/header-geometry.spec.ts exits 0 on chromium, firefox, and webkit: no internal header scrolling at 360×640; single TagEntry instance; metadata spot not visible after the first turn
    - npx playwright test tests/e2e/pagination/ and tests/e2e/open-every-fixture.spec.ts exit 0 (geometry + initial focus unregressed)
    - The .article-body.paginated-surface grid rule (minmax(auto, 25%) cap) is byte-unchanged in the git diff
    - rg -c 'autoFocus|\.focus\(' src/reader/TagEntry.tsx returns 0 (inert discipline carried)
    - The effectiveMode/sessionModeOverride lines in ArticleView are byte-unchanged (T-04-15 preserved)
  </acceptance_criteria>
  <done>Header is title + essential controls; tags/metadata render once at article top; pagination geometry and focus contracts unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Navigation is hash-only within the app origin; no external URLs are constructed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | history.back()/hash assignment | accept | Fallback assigns only the literal "#/" route (parseHash maps it to the library list); no attacker-influenced URL reaches navigation |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; any dependency addition is out of scope |
</threat_model>

<verification>
- npx playwright test tests/e2e/chrome/back-nav.spec.ts tests/e2e/chrome/header-geometry.spec.ts
- Regression: npx playwright test tests/e2e/pagination/ tests/e2e/open-every-fixture.spec.ts
</verification>

<success_criteria>
- POLISH-03: slimmed header passes the 360×640 no-internal-scroll bar; TagEntry renders once at the article-top spot; corpus + focus specs green
- POLISH-05: back affordance proven on both views across 3 engines incl. the deep-link no-exit case and keyboard activation
</success_criteria>

<artifacts_produced>
- src/reader/BackToLibrary.tsx — new component: BackToLibrary export ({ hasAppHistory } props)
- src/App.tsx — modified: hasAppHistory flag + prop pass-through
- src/routes/ArticleView.tsx — modified: slimmed pinned header; new article-top metadata spot; hasAppHistory prop; BackToLibrary mount
- src/routes/review/ReviewView.tsx — modified: hasAppHistory prop; BackToLibrary mount
- src/app.css — modified: back-to-library button styles; slimmed header anatomy; metadata-spot styles
- tests/e2e/chrome/back-nav.spec.ts, tests/e2e/chrome/header-geometry.spec.ts — new
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-04-SUMMARY.md` when done
</output>
