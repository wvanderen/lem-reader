---
phase: 13-polish-and-acceptance
plan: 08
type: execute
wave: 4
depends_on: []
files_modified:
  - src/ingestion/IngestControl.tsx
  - tests/e2e/library/upload-queue.spec.ts
autonomous: true
gap_closure: true
requirements: [ING-03]

must_haves:
  truths:
    - "A queued file pick can be removed before upload via a visible 'Remove file' control next to the picker"
    - "After a completed upload that stays on the library (the EPUB book path), the file input is empty and the Add file button is disabled again — without a page refresh"
    - "After every refusal (over-cap, unreadable, dedupe), the pick is cleared so re-picking the same file re-fires the picker — the 09-05 import-input reset discipline applied to the intake control"
    - "Removing a queued pick never submits the form and is impossible while an upload is in flight"
  artifacts:
    - path: "src/ingestion/IngestControl.tsx"
      provides: "resetFilePick helper + Remove file affordance + terminal-outcome resets"
      contains: "resetFilePick"
    - path: "tests/e2e/library/upload-queue.spec.ts"
      provides: "queue-removal + post-success reset + refusal-reset e2e contract (user bar)"
      min_lines: 60
  key_links:
    - from: "tests/e2e/library/upload-queue.spec.ts"
      to: "src/ingestion/IngestControl.tsx"
      via: "setInputFiles on input#ingest-file, click Remove file, assert input value + Add file disabled state"
      pattern: "ingest-remove-file"
    - from: "src/ingestion/IngestControl.tsx"
      to: "src/reader/SettingsPanel.tsx"
      via: "reset discipline precedent — file-input value cleared on refusals AND terminal outcomes so same-file retry never no-ops"
      pattern: "value = \"\""
  prohibitions:
    - statement: "No fixed sleeps in the new spec — every end condition uses expect.poll / waitForFunction quiescence (Pitfall 8 discipline)"
      status: resolved
      verification: "a search for the Playwright hard-wait helper name in upload-queue.spec.ts returns zero matches"
    - statement: "No new CSS file edits — the Remove file button reuses the existing cross-surface quiet-button class (the 13-03 Review-highlights reuse precedent)"
      status: resolved
      verification: "git diff over the plan's commits touches no .css file"
---

<objective>
Close gap G2 from the Phase 13 user review (13-UAT.md § Gaps, 13-VERIFICATION.md § Gaps Found): the intake upload control has no way to remove a queued file, and the queued pick persists after a completed upload until the page is refreshed.

Root cause: `handleFileSubmit` in src/ingestion/IngestControl.tsx never clears `fileInputRef` after a terminal outcome, and the `hasFile` mirror state has no clear path other than a fresh mount. The 09-05 import flow already solved this exact class (SettingsPanel resets the import input's value on refusals AND on Proceed/Cancel so same-file retry never silently no-ops) — this plan applies that precedent to the intake control.

Purpose: the upload control behaves like a finished queue — pick, inspect, remove, upload, and the control resets itself.
Output: resetFilePick helper wired to every terminal outcome + a Remove file button + a new e2e contract.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: resetFilePick helper + Remove file affordance + terminal-outcome resets in IngestControl (G2)</name>
  <files>src/ingestion/IngestControl.tsx</files>
  <read_first>
  - src/ingestion/IngestControl.tsx (whole file — handleFileSubmit's every exit path: the three extension-aware over-cap early returns, the book dedupe refusal return, the article dedupe refusal return, both success paths, and the catch; the hasFile state + fileInputRef at lines 130–137)
  - src/reader/SettingsPanel.tsx (lines 271–350 — the 09-05 reset precedent: import input value cleared on refusal AND on Proceed/Cancel so re-picking the same file re-fires onChange)
  - src/app.css (the .article-export-highlights rule around line 2458 — the cross-surface quiet-button class the new button reuses; and the .ingest-control form layout around lines 749–840)
  </read_first>
  <action>
  In src/ingestion/IngestControl.tsx:

  1. Add a `resetFilePick` function near the file state: set `fileInputRef.current.value` to the empty string when the ref is non-null, then `setHasFile(false)`. This is the single reset seam (one helper, every caller — no scattered clears).

  2. Render a Remove file button inside the upload form, after the file input and before the Add file button, only when `hasFile` is true: `type="button"` (a remove action must NEVER submit the form), `className="article-export-highlights ingest-remove-file"` (the second class is the behavioral/test hook; the first reuses the existing generic quiet-button tokens — the 13-03 cross-surface reuse precedent used for the Review-highlights button; zero CSS additions), `disabled={submitting}`, text `Remove file`, `onClick={resetFilePick}`.

  3. Call `resetFilePick()` at EVERY terminal outcome of `handleFileSubmit`: the three over-cap early returns (pdf-too-large, epub-too-large, response-too-large) — after setting the calm copy; the book dedupe refusal return (already-in-library); the article dedupe refusal return; the EPUB success path (after the success copy is set — this is the path the user saw: the control stays mounted on the library with a stale queued file); the single-article success path (before the hash navigation — harmless there but keeps the contract uniform); and the final catch. Update the form's explanatory comment to record the reset discipline without naming greppable helper tokens unrelated to this surface.

  Do not change the url/paste forms, mapReasonToCopy, the extension-aware cap logic, or the .status live region semantics. The dedupe-refuse and cap logic stay byte-identical — only the reset calls are added on the exits.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/ingestion/IngestControl.tsx contains a `resetFilePick` function that clears the file input value and sets hasFile false
  - a `Remove file` button exists with `type="button"`, class `article-export-highlights ingest-remove-file`, rendered only when hasFile is true, disabled while submitting
  - `rg -c 'resetFilePick' src/ingestion/IngestControl.tsx` returns at least 8 (1 definition + Remove onClick + 3 over-cap returns + 2 dedupe returns + 2 success paths + catch)
  - `git diff --name-only` over the task commit touches no file other than src/ingestion/IngestControl.tsx
  - tsc exits 0; the pinned copy unit tests stay green (mapReasonToCopy byte-stable)
  </acceptance_criteria>
  <done>Queued picks are removable pre-submit, and every terminal outcome leaves the picker empty with Add file disabled — no refresh needed, same-file retry always re-fires.</done>
</task>

<task type="auto">
  <name>Task 2: upload-queue e2e spec — remove affordance, post-success reset, refusal reset + same-file retry (G2 contract)</name>
  <files>tests/e2e/library/upload-queue.spec.ts</files>
  <read_first>
  - tests/e2e/epub-intake.spec.ts (the uploadEpub helper at lines 83–90 — setInputFiles on input#ingest-file + Add file click; the validBookEpub3 / corruptNotEpub fixture imports from ../unit/server/epub-fixtures; the ingestStatus live-region locator helper)
  - tests/e2e/library/markdown-upload.spec.ts (the library-open + ingest harness conventions for the library suite)
  - src/ingestion/IngestControl.tsx (as modified by Task 1 — the button hook class and reset behavior under test)
  </read_first>
  <action>
  Create tests/e2e/library/upload-queue.spec.ts with three tests (plain test(), 3-engine matrix inherited; before-each database wipe + library open following the epub-intake.spec.ts conventions; image-route fulfillment not needed — no image fixtures):

  1. "Remove file clears a queued pick before upload" — pick a small .md buffer via setInputFiles on input#ingest-file; assert the Add file button is enabled; click the Remove file button (locate via the ingest-remove-file hook class); assert the input's value reads empty via an evaluate on the locator, and the Add file button is disabled again; re-pick the SAME name+buffer and assert Add file is enabled again (proves onChange re-fired — the reset cleared the picker, not just the visual).

  2. "a completed book upload resets the picker without a page refresh" — reuse the validBookEpub3 fixture and the uploadEpub helper shape from epub-intake.spec.ts; wait for the calm success copy "Book added to your library." in the ingest status region; then assert input#ingest-file value is empty AND Add file is disabled — with NO reload (this is the exact user-reported path: the EPUB success stays on the library view).

  3. "a refusal clears the pick so re-picking the same file re-fires the picker" — pick the corruptNotEpub fixture named .epub, click Add file, wait for the calm unreadable copy; assert the input value is empty and Add file disabled; re-pick the same name+buffer, assert Add file becomes enabled (reset made the second pick count), submit, and assert the calm copy appears again.

  End conditions use expect/expect.poll only — zero fixed sleeps (the suite-wide Pitfall 8 discipline). Read the input value through `locator.evaluate` returning the raw string (a set picker reads as a fake-path prefixed filename; a cleared picker reads as the empty string).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/library/upload-queue.spec.ts tests/e2e/library/markdown-upload.spec.ts tests/e2e/pdf-intake.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/library/upload-queue.spec.ts exists with three tests covering: remove-before-upload, epub-success reset without refresh, refusal reset + same-file re-pick
  - the spec imports validBookEpub3 and corruptNotEpub from ../unit/server/epub-fixtures (REUSE-DO-NOT-FORK — no new fixture generators)
  - a search for the Playwright hard-wait helper name in the new spec returns zero matches
  - `npx playwright test tests/e2e/library/upload-queue.spec.ts` exits 0 across chromium/firefox/webkit
  - markdown-upload + pdf-intake regression suites exit 0 (existing upload flows untouched by the reset discipline)
  </acceptance_criteria>
  <done>The G2 user bar is pinned in a real-browser contract: removable queue, self-resetting control after completion, and refusal-safe same-file retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→file picker | unchanged trust boundary — the picker already feeds the extension-aware cap + server re-validation; this plan adds only client-side state hygiene |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-01 | DoS | handleFileSubmit cap branches | accept | Cap logic byte-unchanged and still refusing before any read; reset calls are added after the refusal copy, never replacing the guard |
| T-13-08-02 | Tampering | Remove file button | accept | type="button" (cannot submit); disabled during flight; no data path — clears local picker state only |
| T-13-08-SC | Tampering | package installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- `npx playwright test tests/e2e/library/` — full library suite green including the new upload-queue contract
- `npx playwright test tests/e2e/epub-intake.spec.ts` — book upload flows green alongside the new reset behavior
- `npx vitest run` — unit suite green (IngestControl copy maps untouched)
</verification>

<success_criteria>
- G2 closed at the user bar: remove/clear affordance exists, post-completion state resets without refresh, refusals clear the pick (09-05 discipline)
- No intake cap/dedupe/refusal behavior weakened — only exits gained resets
</success_criteria>

## Artifacts this phase produces

- Function `resetFilePick` (local to src/ingestion/IngestControl.tsx)
- Button hook class `ingest-remove-file` (rendered on the Remove file button; no CSS rule — rides the existing .article-export-highlights tokens)
- e2e spec tests/e2e/library/upload-queue.spec.ts (3 tests × 3 engines)
- No new exports, packages, schema, or store changes

<output>
Create `.planning/phases/13-polish-and-acceptance/13-08-SUMMARY.md` when done
</output>
