---
phase: 13-polish-and-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pagination/progress.ts
  - src/reader/PaginatedSurface.tsx
  - src/reader/ProgressHairline.tsx
  - tests/unit/pagination/progress-formula.test.ts
  - tests/e2e/polish/first-paint-progress.spec.ts
autonomous: true
requirements: [POLISH-02]
must_haves:
  truths:
    - "A reader who opens a one-page article sees the progress hairline near empty (≈0%), not full"
    - "A reader on page 1 of a multi-page article sees progress near 0; each turn grows it monotonically; the last page reads below 100%"
    - "Scrolling-mode progress behavior is unchanged (already position-anchored)"
    - "The hairline stays a zero-motion, aria-hidden decorative bar; the PageIndicator still reads N of M"
  artifacts:
    - path: "src/pagination/progress.ts"
      provides: "Pure offset-anchored progress ratio over the D-05 coordinate system"
      exports: ["paginatedProgressRatio"]
      min_lines: 15
    - path: "tests/unit/pagination/progress-formula.test.ts"
      provides: "Boundary-value unit tests for the ratio"
      min_lines: 40
    - path: "tests/e2e/polish/first-paint-progress.spec.ts"
      provides: "SC#2 boundary e2e (1-page and multi-page) across 3 engines"
      min_lines: 60
  key_links:
    - from: "src/pagination/progress.ts"
      to: "src/pagination/anchor.ts + src/content/normalizeText.ts"
      via: "composes pageStartGlobalOffset and graphemeLength (REUSE-DO-NOT-FORK — no new offset accumulation walks)"
      pattern: "pageStartGlobalOffset"
    - from: "src/reader/PaginatedSurface.tsx"
      to: "src/pagination/progress.ts"
      via: "ProgressHairline progress prop replaces the page N/M prop at the former L585 call site"
      pattern: "paginatedProgressRatio"
    - from: "src/reader/PaginatedSurface.tsx"
      to: "src/reader/PageIndicator.tsx"
      via: "N-of-M indicator call site untouched"
      pattern: "PageIndicator"
  prohibitions:
    - statement: "ProgressHairline gains no transition/animation property, no text node, and stays aria-hidden"
      status: resolved
      verification: "tests/e2e/progress.spec.ts invariant tests stay green byte-unchanged (strengthen-only)"
    - statement: "PageIndicator.tsx is byte-unchanged (D-05: page numbers are never persistent identity; N-of-M display stays)"
      status: resolved
      verification: "git diff --stat src/reader/PageIndicator.tsx is empty for this plan's commits"
    - statement: "No progress value is persisted anywhere (no Dexie/store/schema changes)"
      status: resolved
      verification: "git diff shows src/persistence/ untouched by this plan"
---

<objective>
Replace PaginatedSurface's page-count ratio (current/total — the bug: 1/1 shows 100% on open, 1/2 shows 50% at start) with the roadmap-prescribed offset-anchored formula over the D-05 grapheme coordinate system (POLISH-02): ratio = pageStartGlobalOffset / graphemeLength, boundary-tested in unit tests and proven on the real paginated surface in a 3-engine e2e.

Purpose: progress-bar semantics must reflect actual position in the text, matching the library rows' existing D8-11 ratio and the restore coordinate system.
Output: pure progress.ts helper, ratio-path ProgressHairline, boundary unit tests, first-paint-progress e2e.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure offset-anchored ratio helper + boundary unit tests</name>
  <files>src/pagination/progress.ts, tests/unit/pagination/progress-formula.test.ts</files>
  <read_first>
  - src/pagination/anchor.ts (pageStartGlobalOffset L59-72 — purity contract at L14-21; also blockGraphemeLength)
  - src/content/normalizeText.ts (graphemeLength L109-111)
  - src/pagination/types.ts (PageFragment type)
  - src/content/schema.ts (CanonicalArticle type)
  - tests/unit/pagination/fragmentOrder.test.ts (unit-structure analog in the same dir)
  </read_first>
  <behavior>
    - Test: a one-page article's only fragment yields ratio 0 (SC#2: 1-page article no longer reads 100% on open)
    - Test: the first fragment of a multi-fragment article yields ratio 0 (progresses from the start)
    - Test: ratios across successive fragments are monotonically non-decreasing, and the last fragment's ratio is strictly below 1 when the last page has content
    - Test: an article whose graphemeLength is 0 yields ratio 0 (defensive empty)
    - Test: a start offset beyond total clamps to 1 (defensive upper bound; ProgressHairline also clamps)
  </behavior>
  <action>
    Create src/pagination/progress.ts: opening header comment citing POLISH-02 + the D-05 coordinate contract (derived per layout, never persisted). Export function paginatedProgressRatio(article: CanonicalArticle, fragment: PageFragment): number — computes pageStartGlobalOffset(article, fragment) divided by graphemeLength(article); when total is 0 return 0; otherwise clamp the quotient into [0, 1] via Math.min/Math.max. The function composes ONLY the two shipped helpers (pageStartGlobalOffset from src/pagination/anchor.ts, graphemeLength from src/content/normalizeText.ts) — no new offset accumulation walk over blocks (REUSE-DO-NOT-FORK; the 04-09 offset-drift class is the risk being avoided). No DOM, no React imports (anchor.ts purity note). Create tests/unit/pagination/progress-formula.test.ts (same dir, .test.ts convention) covering the five behaviors above with small hand-built canonical articles (2-3 paragraph blocks; reuse test-article construction patterns from tests/unit/pagination/ neighbors rather than new factories where practical).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/progress-formula.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/pagination/progress.ts contains "export function paginatedProgressRatio(" and imports pageStartGlobalOffset from anchor.ts and graphemeLength from normalizeText.ts
    - progress.ts contains no React import and no DOM access (rg -c 'react|document\\.' src/pagination/progress.ts returns 0)
    - npx vitest run tests/unit/pagination/progress-formula.test.ts exits 0 with the five boundary behaviors as named test cases
  </acceptance_criteria>
  <done>Pure ratio helper ships with the full SC#2 boundary table encoded and green.</done>
</task>

<task type="auto">
  <name>Task 2: PaginatedSurface ratio path + ProgressHairline page-prop removal</name>
  <files>src/reader/PaginatedSurface.tsx, src/reader/ProgressHairline.tsx</files>
  <read_first>
  - src/reader/PaginatedSurface.tsx (L474 onAnchorChange effect, L551-556 imperative handle — both already use pageStartGlobalOffset; L585-586 the ProgressHairline page prop + PageIndicator call)
  - src/reader/ProgressHairline.tsx (the page branch to delete; the ratio path to keep byte-identical)
  - src/reader/PageIndicator.tsx (untouched sibling)
  - tests/e2e/progress.spec.ts (invariants that must stay green byte-unchanged: aria-hidden, transition none, origin)
  </read_first>
  <action>
    In PaginatedSurface, replace the ProgressHairline call that passes the page object with progress={paginatedProgressRatio(article, pages[currentPageIdx])} (import from ../pagination/progress; memoize per committed page the way LibraryRow memoizes its grapheme total — the article/pages are already in refs used by the neighboring pageStartGlobalOffset call sites). In ProgressHairline, delete the page-variant branch from the component and remove the page field from ProgressHairlineProps; the ratio path (clamp of progress into [0,1] feeding scaleX with transformOrigin left) stays byte-identical; the wrapper stays aria-hidden decorative with no text node and no motion. PageIndicator's call site and component stay byte-unchanged. PaginatedSurface is the only former user of the page prop — removing it must be proven by the type check. Do not touch scrolling-mode progress (ArticleView's scroll ratio path) — it is already position-anchored.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/e2e/progress.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/reader/ProgressHairline.tsx props type contains no page field (rg -c 'page\\??:' src/reader/ProgressHairline.tsx returns 0) and the component clamps progress into [0,1]
    - src/reader/PaginatedSurface.tsx ProgressHairline call passes progress via paginatedProgressRatio; the PageIndicator call is unchanged
    - npx tsc --noEmit exits 0 (proves no residual page-prop caller anywhere)
    - tests/e2e/progress.spec.ts exits 0 with zero modifications to that spec file (git diff empty for it)
  </acceptance_criteria>
  <done>The paginated hairline is offset-anchored; the presentational contract (aria-hidden, zero motion, origin left) is untouched; PageIndicator untouched.</done>
</task>

<task type="auto">
  <name>Task 3: First-paint progress boundary e2e (SC#2)</name>
  <files>tests/e2e/polish/first-paint-progress.spec.ts</files>
  <read_first>
  - tests/e2e/progress.spec.ts (L81-131 computed-transform matrix parsing + expect.poll settle discipline)
  - tests/e2e/annotations/_fixtures.ts (openArticle helper)
  - tests/e2e/portability/_portability.ts (seedRows — seeding a short canonical article directly into Dexie)
  - src/fixtures/index.ts (corpus fixture ids for the multi-page case)
  - tests/e2e/pagination/ (page-turn helper precedents)
  </read_first>
  <action>
    New spec tests/e2e/polish/first-paint-progress.spec.ts, plain test() declarations (inherits chromium/firefox/webkit). Case A (one-page article): seed a short schema-valid canonical article (2-3 short paragraph blocks) via the seedRows precedent, open it in paginated mode, wait for pagination settle (expect.poll), sanity-assert PageIndicator shows 1 of 1, then read the hairline fill's computed transform matrix per the progress.spec.ts parsing pattern and assert scaleX is below 0.1 on open (was 1.0 before the fix). Case B (multi-page): open a corpus fixture that paginates to at least 2 pages; assert page-1 scaleX is below 0.1; turn to page 2 and assert scaleX strictly greater than page 1's; step to the last page and assert scaleX remains below 1.0; sampled turns are monotonically non-decreasing. All end conditions via expect.poll; zero fixed sleeps (Pitfall 8 — firefox rAF settle is a known flake class).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/polish/first-paint-progress.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/polish/first-paint-progress.spec.ts exits 0 on chromium, firefox, and webkit
    - Case A asserts 1-page open scaleX below 0.1; Case B asserts near-zero start, strictly-growing turn, last-page below 1.0
    - The spec contains no waitForTimeout call (rg -c 'waitForTimeout' on the file returns 0)
  </acceptance_criteria>
  <done>SC#2 proven on the real paginated surface across engines: 1-page is not 100% on open; multi-page progresses from the start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Ratio is derived from already-validated canonical articles; presentational output only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | paginatedProgressRatio inputs | accept | Inputs are Zod-validated article records already inside the trust boundary; output is a clamped number consumed by an aria-hidden decorative bar — no untrusted input crosses |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; any dependency addition is out of scope |
</threat_model>

<verification>
- Unit: npx vitest run tests/unit/pagination/progress-formula.test.ts
- e2e: npx playwright test tests/e2e/polish/first-paint-progress.spec.ts
- Strengthen-only regression: npx playwright test tests/e2e/progress.spec.ts (byte-unchanged, all green)
</verification>

<success_criteria>
- POLISH-02's SC#2 boundary behavior proven in unit tests and 3-engine e2e
- No hairline contract drift (no motion, no text, aria-hidden; PageIndicator unchanged)
- No persistence/schema changes
</success_criteria>

<artifacts_produced>
- src/pagination/progress.ts — new module: paginatedProgressRatio()
- src/reader/ProgressHairline.tsx — modified: page prop removed from ProgressHairlineProps; ratio-only path
- src/reader/PaginatedSurface.tsx — modified: passes progress via paginatedProgressRatio
- tests/unit/pagination/progress-formula.test.ts — new
- tests/e2e/polish/first-paint-progress.spec.ts — new
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-02-SUMMARY.md` when done
</output>
