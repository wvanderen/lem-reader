---
phase: 21-integrated-refinement-and-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - tests/e2e/chrome/tag-menu-geometry.spec.ts
  - tests/e2e/chrome/tag-popover.spec.ts
autonomous: true
requirements: [POLISH-08]
must_haves:
  truths:
    - "The tag menu opens directly below its trigger button separated by exactly the xs-space gap, at any viewport the app supports"
    - "The menu stays fully inside the viewport at 240px width via flip fallbacks — no manual clamp math"
    - "The menu re-resolves under its trigger on viewport resize with zero JS reposition listeners"
    - "Any close path (Esc, light-dismiss, toggle) returns focus to the trigger, asserted honestly per engine (webkit divergence documented)"
    - "The popover=auto interaction contract (top layer, light-dismiss, Esc) and the role/aria-label shapes are unchanged (D21-05)"
  artifacts:
    - path: "src/app.css"
      provides: ".tags-trigger anchor-name + re-anchored .tag-popover block (D21-05)"
      contains: "position-anchor"
    - path: "tests/e2e/chrome/tag-menu-geometry.spec.ts"
      provides: "Cross-engine geometry proof: adjacency, resize-follow, viewport-keep, focus-restore"
      min_lines: 80
  key_links:
    - from: ".tags-trigger (Header)"
      to: ".tag-popover (ArticleView mount)"
      via: "anchor-name/position-anchor pair — document-scoped, so the DOM split is a non-issue"
      pattern: "anchor-name: --tags-trigger"
    - from: "tests/e2e/chrome/tag-menu-geometry.spec.ts"
      to: "src/routes/ArticleView.tsx toggle seam"
      via: "open/close through the real UI; focus-restore asserted on the shipped toggle-event seam (no new code)"
      pattern: "tags-trigger"
  prohibitions:
    - statement: "No JS resize/scroll reposition listeners for the tag menu — the browser owns geometry (probe-verified on all 3 engines)"
      status: enforced
      verification: "git diff shows no listener additions in Header.tsx/ArticleView.tsx for tag geometry; geometry spec passes with JS listeners absent"
    - statement: "The old header-anchored inset declarations are deleted, not zeroed alongside position-area (they fight anchor resolution)"
      status: enforced
      verification: "sed -n '/^\\.tag-popover {/,/^}/p' src/app.css contains neither the old top calc nor the old inset-inline-end declaration"
    - statement: "TocPanel (popover=manual), native dialog surfaces, and TagEntry internals are untouched (locked contracts)"
      status: enforced
      verification: "git diff shows no changes to src/reader/TocPanel.tsx, src/reader/TagEntry.tsx, or dialog components"
    - statement: "Zero motion properties added — static repositioning only"
      status: enforced
      verification: "reduced-motion spec green; the .tag-popover block declares no transition/animation"
---

<objective>
Tag-menu anchoring (POLISH-08, D21-05): the tag popover hugs its trigger —
adjacent on open, viewport-kept, geometry-following, focus-restoring — using
CSS anchor positioning (probe-verified on this project's pinned Playwright
1.61.1 chromium/firefox/webkit; RESEARCH Pattern 1), inside the unchanged
popover=auto interaction contract.

Purpose: The current .tag-popover is position:fixed under the header's
inline-end cluster — anchored to the header visually, not the trigger, so it
detaches at some geometries. This plan corrects the anchoring with ~6 CSS
declarations and proves it cross-engine.
Output: re-anchored CSS block + new cross-engine geometry spec.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS anchor positioning replaces the header-fixed block</name>
  <files>src/app.css</files>
  <read_first>
  - src/app.css (.tags-trigger styles ~L1926-1941; .tag-popover block ~L1943-1963 including its comment block L1943-1948)
  - src/routes/ArticleView.tsx (popover mount L2615-2629 + toggle/focus-restore seam L385-419 — context only, no change)
  - src/reader/Header.tsx (the trigger button — context only, no JSX change expected)
  - 21-RESEARCH.md §Pattern 1 (probe evidence + replacement shape) + §Pitfall 2 (UA popover styles fight author positioning)
  - 21-UI-SPEC.md §1 Refinement Contract (the locked clause table)
  - 21-PATTERNS.md "POLISH-08 wave" (current-state CSS + replacement block)
  </read_first>
  <action>
  Replace the .tag-popover positioning wholesale per D21-05:
  1. Add anchor-name: --tags-trigger to the existing .tags-trigger rule.
  2. Rewrite the .tag-popover declarations: keep position: fixed; add position-anchor: --tags-trigger; position-area: block-end span-inline-end; position-try-fallbacks: flip-block, flip-inline; inset: auto (kills UA popover insets — MDN conflict note); margin: var(--space-xs) (the calm 4px gap). KEEP unchanged: the width cap min(420px, …), padding var(--space-md), background var(--surface-raised), border 1px solid var(--hairline), border-radius 4px.
  3. DELETE the two old header-anchored inset declarations (the top calc and the inset-inline-end) entirely — do not zero them (Pitfall 2).
  4. Update the block's comment: cite D21-05, the retained popover=auto contract (top layer + light-dismiss + Esc), the 2026-08-31 three-engine probe evidence, and the zero-motion note (reduced-motion trivially satisfied).
  No JSX changes; no other CSS blocks touched.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/tag-popover.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/app.css .tags-trigger rule contains "anchor-name: --tags-trigger"
  - The .tag-popover block contains "position-anchor: --tags-trigger", "position-area: block-end span-inline-end", "position-try-fallbacks: flip-block, flip-inline", "inset: auto", and "margin: var(--space-xs)"
  - sed -n '/^\.tag-popover {/,/^}/p' src/app.css | grep -c 'top: calc' returns 0 and the same region contains no inset-inline-end declaration
  - The block still contains "min(420px" and "var(--surface-raised)"
  - The comment block cites D21-05
  - tests/e2e/chrome/tag-popover.spec.ts green on chromium (existing close-path/axe contract holds)
  </acceptance_criteria>
  <done>Popover geometry resolves against the trigger via native anchor positioning on all three engines; old insets gone; interaction contract byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-engine geometry spec — adjacency, resize-follow, viewport-keep, focus-restore</name>
  <files>tests/e2e/chrome/tag-menu-geometry.spec.ts, tests/e2e/chrome/tag-popover.spec.ts</files>
  <read_first>
  - tests/e2e/chrome/tag-popover.spec.ts (harness imports L22-32/60-64, focus-restore with webkit divergence L108-132, boundingBox precedent ~L198-205, zero-fixed-sleeps convention L19-21)
  - tests/e2e/toc/toc-geometry.spec.ts (geometry-assertion precedent)
  - src/routes/ArticleView.tsx (toggle seam L385-419 — the focus-restore mechanism being asserted)
  - 21-PATTERNS.md "New test files → tag-menu-geometry" section (cells + webkit divergence pattern)
  - 21-RESEARCH.md §Pattern 1 behavioral probe (the exact geometries the probe verified)
  </read_first>
  <action>
  Author tests/e2e/chrome/tag-menu-geometry.spec.ts copying the tag-popover.spec.ts harness (prepareFreshPage from tests/e2e/portability/_portability, BASE localhost:5173, expect/expect.poll/waitForFunction waits only — zero fixed sleeps). Cells (POLISH-08 / D21-05):
  1. Adjacency: open the popover via the real trigger click; assert via boundingBox that the popover's top edge sits at/below the trigger's bottom edge within the 4px gap tolerance and the popover's inline span aligns to the trigger's inline-end area (not the header corner).
  2. Resize-follow: with the popover open at 1280x800, page.setViewportSize to 360x640; assert the popover re-resolves adjacent below the trigger (probe behavior: follows with zero JS listeners).
  3. Viewport-keep: at 240x600 the popover box lies fully within the viewport (fallbacks flip).
  4. Esc close + focus-restore: focus the trigger, open, press Escape; assert focus returns to the trigger on chromium/firefox; on webkit assert the documented weaker shape (focus not trapped in the closed surface) — reuse the expectFocusOnTrigger engine-divergence pattern verbatim in spirit (D18-04 honesty precedent).
  5. Light-dismiss: click outside; assert the popover closes and focus restores (same engine-honest shapes).
  Run all three engines. If tag-popover.spec.ts pinned any old header-geometry literals, update only those cells with a D21-05 citation (strengthen-only otherwise).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/tag-menu-geometry.spec.ts tests/e2e/chrome/tag-popover.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/chrome/tag-menu-geometry.spec.ts exists and its adjacency cell compares .tag-popover and .tags-trigger boundingBox y-coordinates
  - It contains a resize cell (setViewportSize) and a 240-width viewport-keep cell asserting all box edges within the viewport
  - It contains a webkit-divergent focus branch (grep 'webkit') and no geometry test.skip (geometry is cross-engine per the probe)
  - The three-engine run exits 0 for both specs
  </acceptance_criteria>
  <done>POLISH-08 proven in real browsers: the menu hugs its trigger at every tested geometry, stays in-viewport at 240px, and restores focus predictably per engine.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- `.tags-trigger` anchor-name declaration + re-anchored `.tag-popover` block — src/app.css (first anchor-positioned element in the codebase)
- tests/e2e/chrome/tag-menu-geometry.spec.ts (NEW spec file)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Geometry-only CSS change; no input surface, no data flow, no user content enters style properties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | anchor-name CSS injection | accept | Anchor names are author-authored dashed idents in authored CSS; no user content reaches style properties (RESEARCH Security Domain) |
| T-21-05 | Denial of Service | popover off-screen/overlap | mitigate | position-try-fallbacks flip-block/flip-inline + the 240px viewport-keep e2e cell prove in-viewport geometry |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (browser-platform CSS only) |
</threat_model>

<verification>
- New geometry spec green on chromium/firefox/webkit
- Existing tag-popover.spec.ts contract green (close paths, axe)
- No JSX changes; no listener code; no motion properties
</verification>

<success_criteria>
- POLISH-08 proven: adjacency + viewport-keep + geometry-following + focus-restore, 3 engines, popover=auto contract byte-stable
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-02-SUMMARY.md` when done
</output>
