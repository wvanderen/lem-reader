---
phase: 21-integrated-refinement-and-acceptance
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md
  - .planning/phases/21-integrated-refinement-and-acceptance/deferred-items.md
autonomous: true
requirements: [POLISH-11]
must_haves:
  truths:
    - "An impeccable-informed audit has scored every primary user-facing surface (Library views/filters/Add dialog, Highlights, Reader both modes + TOC + annotations chrome, Settings/dialogs, error/refusal states) across the five dimensions (D21-08)"
    - "Every finding carries a P0-P3 severity mapped to blocker/major/minor, with location, impact, standard violated, and recommendation"
    - "Every blocker/major (P0/P1) finding is fixed in-phase with all suites green (D21-10)"
    - "Every minor/cosmetic (P2/P3) finding is logged in the phase deferred-items.md with severity"
    - "No remediation replaced native semantics or weakened reduced-motion, forced-colors, zoom, or screen-reader behavior — the audits of the corrected POLISH-08/09/10 state included (D21-09)"
  artifacts:
    - path: ".planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md"
      provides: "The durable audit report: Anti-Patterns verdict opening, 5 dimension scores, P0-P3 findings table, per-finding remediation status, remediation gate"
      min_lines: 60
    - path: ".planning/phases/21-integrated-refinement-and-acceptance/deferred-items.md"
      provides: "Minor/cosmetic findings ledger with severity (D21-10)"
  key_links:
    - from: "21-AUDIT-FINDINGS.md"
      to: "21-VERIFICATION.md (phase verification)"
      via: "the findings doc is referenced from the phase verification ledger (UI-SPEC §4)"
      pattern: "AUDIT-FINDINGS"
  prohibitions:
    - statement: "Remediation never replaces native semantics and never weakens reduced-motion, forced-colors, zoom, or SR behavior (D21-08; RESEARCH Pitfall 6)"
      status: enforced
      verification: "remediation diffs touch no roles/semantics/motion properties; a11y + edge specs re-run green after remediation"
    - statement: "No remediation introduces dangerous HTML (XSS/mXSS surface)"
      status: enforced
      verification: "node scripts/check-no-danger.js exits clean; npm run test:unit green"
    - statement: "The audit runs only after the POLISH-08/09/10 fixes land — it validates the corrected state (D21-09)"
      status: enforced
      verification: "plan depends_on 21-01/02/03; findings reference the corrected geometries"
    - statement: "No new features ship under audit cover — this phase corrects and proves only"
      status: enforced
      verification: "remediation diffs are geometry/style/token/doc changes only"
---

<objective>
Impeccable-informed audit (POLISH-11, D21-08/D21-09/D21-10): run the
impeccable skill's audit methodology over ALL primary user-facing surfaces of
the corrected state, severity-map its findings (P0→blocker, P1→major,
P2/P3→minor), fix every blocker/major in-phase under the hard a11y
constraint, and log minors to the phase deferred-items ledger.

Purpose: The v2.1 milestone framed itself as an Impeccable-informed
refinement pass; this is the pass — a disciplined visual/interaction audit of
the final UI (including the three corrections themselves), with remediation
that cannot trade a cosmetic win for an accessibility loss.
Output: 21-AUDIT-FINDINGS.md + in-phase remediation + deferred-items ledger.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UI-SPEC.md
@.planning/phases/15-application-shell-and-destinations/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the five-dimension audit and author the findings report</name>
  <files>.planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md</files>
  <read_first>
  - The impeccable skill at ~/.agents/skills/impeccable/SKILL.md (load via the skill tool — D21-08 names it the mandated methodology)
  - 21-RESEARCH.md §Pattern 4 (dimensions, severity ladder, mapping onto D21-10)
  - 21-UI-SPEC.md §4 Audit Contract (coverage list, hard constraint, findings-artifact shape)
  - src/app.css (tokens :root, theme blocks, the surfaces' style blocks)
  - .planning/phases/15-application-shell-and-destinations/deferred-items.md (the known-debt ledger the audit inherits context from)
  </read_first>
  <action>
  1. Load the impeccable skill; follow its audit approach over every D21-08 surface: Library (views, filters, Add dialog), Highlights, Reader (scrolling + paginated, TOC, annotations chrome), Settings/dialogs, and error/refusal states. Use the running dev server for live-browser inspection where the skill's method calls for it; code-level inspection otherwise. Audit the CORRECTED state — the re-anchored tag menu (21-02), the truthful measure range (21-01), and the Highlights glyph/conformance (21-03) are in scope as audited surfaces.
  2. Score the five dimensions 0-4 (Accessibility; Performance; Theming; Responsive; Anti-Patterns) and collect findings tagged P0 (blocking) / P1 (major) / P2 (minor) / P3 (polish), each with location, impact, standard violated, recommendation.
  3. Author .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md: opening section = the skill's Anti-Patterns verdict (does this look AI-generated?); the five dimension scores; the findings table with a severity-mapping legend (P0→blocker, P1→major — fix in-phase per D21-10; P2/P3→minor — log); a per-finding remediation status column; and the remediation gate text (native semantics preserved; reduced-motion/forced-colors/zoom/SR never weakened; all suites stay green — RESEARCH Pitfall 6).
  No code changes in this task.
  </action>
  <verify>
    <automated>test -s .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md && grep -c 'Anti-Patterns' .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md && grep -c 'D21-10' .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
  - 21-AUDIT-FINDINGS.md exists, is non-empty, and contains the Anti-Patterns verdict section, all five dimension headings (Accessibility, Performance, Theming, Responsive, Anti-Patterns), the P0-P3 severity legend citing D21-10, and the remediation-gate text
  - Every finding row has location + severity + remediation status
  - Coverage: the report names every D21-08 surface (Library views/filters/Add dialog, Highlights, Reader scrolling + paginated + TOC + annotations chrome, Settings/dialogs, error/refusal states)
  - npm run test:unit -- --run exits 0 (audit changed no code)
  </acceptance_criteria>
  <done>Durable findings report authored from a real audit run over all surfaces of the corrected state, severity-mapped per D21-10.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate blocker/major findings; log minors; prove suites hold</name>
  <files>.planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md, .planning/phases/21-integrated-refinement-and-acceptance/deferred-items.md</files>
  <read_first>
  - .planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md (Task 1's output — the remediation worklist)
  - src/app.css + the specific source files each P0/P1 finding locates
  - 21-UI-SPEC.md §6 byte-stability ledger (what remediation may not touch)
  - .planning/phases/20-safe-local-image-fidelity/deferred-items.md (the ledger format precedent)
  </read_first>
  <action>
  1. Fix every P0/P1 finding in-phase (D21-10). Remediation is geometry/style/token/doc work: conform-or-cite token usage, spacing rhythm, visual-hierarchy corrections. Any proposed remediation that would alter roles, semantics, or motion properties is a DEFECT — redesign the fix or surface it in the SUMMARY for human decision instead of weakening a11y.
  2. Log every P2/P3 finding to .planning/phases/21-integrated-refinement-and-acceptance/deferred-items.md with severity + location + one-line recommendation (the milestone closes honestly with logged minors).
  3. Update the per-finding remediation status column in 21-AUDIT-FINDINGS.md (fixed / logged-minor) so zero blocker/major rows remain open.
  4. Re-run the regression net: full unit suite + the a11y/edge specs covering the remediated surfaces (strengthen-only — no assertion removed).
  </action>
  <verify>
    <automated>npm run test:unit -- --run && npx playwright test tests/e2e/a11y.spec.ts tests/e2e/forced-colors.spec.ts tests/e2e/reduced-motion.spec.ts && node scripts/check-no-danger.js</automated>
  </verify>
  <acceptance_criteria>
  - 21-AUDIT-FINDINGS.md shows no open P0/P1 rows (every blocker/major status is fixed)
  - deferred-items.md exists in the phase dir listing each P2/P3 finding with severity
  - Remediation diffs introduce no dangerous HTML (check-no-danger script clean) and touch no role/semantics/motion properties
  - npm run test:unit -- --run exits 0 and the targeted a11y/edge specs exit 0
  </acceptance_criteria>
  <done>Zero blocker/major audit findings remain; minors ledgered with severity; every suite that guards the hard a11y constraint is green after remediation.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- `.planning/phases/21-integrated-refinement-and-acceptance/21-AUDIT-FINDINGS.md` (NEW phase artifact — the POLISH-11 findings report)
- `.planning/phases/21-integrated-refinement-and-acceptance/deferred-items.md` (NEW phase ledger — minor findings)
- Remediation edits to existing source files as findings dictate (no new modules expected; any new symbol must be listed in the SUMMARY)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit remediation → shipped DOM | Style/token changes land in surfaces readers and AT consume; the constraint is they must not alter the semantic contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering / Elevation | audit remediation | mitigate | Remediation gate: no dangerouslySetInnerHTML (check-no-danger script), native semantics preserved, full suites green (RESEARCH Security Domain row 2) |
| T-21-09 | Denial of Service | remediation weakening a11y | mitigate | Hard constraint written into the findings doc; a11y + forced-colors + reduced-motion specs re-run green; diffs touching roles/semantics/motion = defect |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (skill methodology only) |
</threat_model>

<verification>
- Findings doc structural checks pass; no open blocker/major rows after remediation
- Unit + a11y/edge suites green post-remediation
- Minors ledgered in deferred-items.md
</verification>

<success_criteria>
- POLISH-11 closed: audit executed on the corrected state across all surfaces, blocker/major findings resolved in-phase, minors logged, a11y floor proven intact
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-04-SUMMARY.md` when done
</output>
