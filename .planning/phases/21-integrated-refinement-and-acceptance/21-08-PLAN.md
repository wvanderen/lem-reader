---
phase: 21-integrated-refinement-and-acceptance
plan: 08
type: execute
wave: 2
depends_on: ["21-07"]
files_modified:
  - tests/e2e/toc/toc-navigation.spec.ts
  - vite.config.ts
autonomous: true
gap_closure: true
requirements: [ACPT-08]
must_haves:
  truths:
    - "toc-navigation (d)/(m) Enter-activation cells pass deterministically on chromium/firefox/webkit — openToc synchronizes with the panel's open-focus settle before any entry.focus(), so the race is closed by construction"
    - "The full honest gate (lint + unit + e2e across the 3 engine projects + chromium-throttled) exits 0 in ONE recorded invocation on a FRESH dev server, with only the documented intentional skip set — including 21-07's new regression spec"
    - "The stale long-lived dev server (the ~190% CPU PID 1899 class) is terminated before the gate, and its CPU accumulation is investigated with findings recorded; any mitigation landed is evidence-backed, not speculative"
  artifacts:
    - path: "tests/e2e/toc/toc-navigation.spec.ts"
      provides: "hardened openToc — awaits the open-focus settle (additions only; click cells unaffected)"
      contains: "toc-panel"
    - path: "vite.config.ts"
      provides: "watcher mitigation ONLY if the investigation produces evidence (conditional artifact — byte-stable otherwise)"
  key_links:
    - from: "tests/e2e/toc/toc-navigation.spec.ts"
      to: "src/reader/TocPanel.tsx"
      via: "openToc polls until document.activeElement is an anchor inside .toc-panel — synchronizing with TocPanel's open-focus rAF (L168-184) instead of racing it"
      pattern: "activeElement"
---

<objective>
Close UAT Test 7 (major): re-establish the green acceptance gate by (a) hardening the openToc helper against the diagnosed open-focus race (strengthen-only — the app's TocPanel behavior is correct per UI-SPEC rules 9-10/D18-15), (b) investigating and mitigating the reused dev server's ~190% CPU accumulation, and (c) re-running the full honest gate on a FRESH dev server per the D18/19-VALIDATION protocol, with 21-07's regression spec in the suite.

Purpose: The seven webkit failures were two modes with one environmental trigger — six page.goto load-timeouts (harness starvation on a starved reused Vite dev server, all isolation-green) plus one test-level focus race in openToc (awaits panel visibility but never the panel's open-focus rAF, so under a starved renderer the aria-current yank fires after the test's entry.focus()). ACPT-08's automated matrix evidence (Phase 21 success criterion 5) is not green until the gate re-runs clean.

Output: Hardened openToc, an evidence-backed dev-server finding (± minimal mitigation), and the fresh-server gate ledger. Full diagnosis: .planning/debug/webkit-e2e-timeouts-toc-null.md.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UAT.md

# Gap evidence (read first)
@.planning/debug/webkit-e2e-timeouts-toc-null.md

# Gate-ledger + starvation-classification precedent (20-07 bounded-workers control)
@.planning/phases/21-integrated-refinement-and-acceptance/21-06-SUMMARY.md

# Files under change (single read each)
@tests/e2e/toc/toc-navigation.spec.ts
@playwright.config.ts
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden openToc — await the panel's open-focus settle (strengthen-only)</name>
  <files>tests/e2e/toc/toc-navigation.spec.ts</files>
  <action>
STRENGTHEN-ONLY: this task adds synchronization; it removes or weakens zero existing assertions, and it does NOT touch src/reader/TocPanel.tsx (the app behavior is correct per UI-SPEC rules 9-10/D18-15 — the diagnosis conclusion; the optional defensive app tweak stays out of scope unless the race somehow persists, in which case surface it rather than silently expanding scope).

In the openToc helper (L132-138): after the existing .toc-panel toBeVisible assertion, add a bounded open-focus settle — poll (expect.poll or waitForFunction, ~3000ms timeout) until document.activeElement is an anchor (tagName A) contained by the .toc-panel element. That rAF is TocPanel's open effect focusing the aria-current entry (deterministically "Top of article" in paginated mode — sectionSpy lags via MutationObserver + 250ms debounce); awaiting it means the test's entry.focus() in cells (d)/(m) can no longer be yanked afterward, closing the race by construction instead of by frame-timing luck. Comment the WHY at the helper: the starved-renderer race from .planning/debug/webkit-e2e-timeouts-toc-null.md (UAT test 7).

Scope notes:
- The settle applies to every openToc caller in this spec — click cells are immune (they re-target focus with an actual click) and settle harmlessly; (d)/(m) Enter-activation cells are the beneficiaries.
- Leave the separate openToc copy in tests/e2e/toc/toc-geometry.spec.ts byte-unchanged: audit it first — it drives no entry.focus()+Enter activation, so it has no race to close.
- Keep the (d)/(m) test bodies themselves unchanged — the fix belongs in the shared helper.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/toc/toc-navigation.spec.ts && git diff -U0 -- tests/e2e/toc/toc-navigation.spec.ts | grep -c '^-[^-]' | grep -x '0'</automated>
  </verify>
  <done>All 15 toc-navigation tests green on chromium/firefox/webkit (including (d) at :320 and (m)); the git diff for the spec shows zero removed lines (pure addition — the strengthen-only bar). Geometry spec untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Kill the stale dev server + investigate the ~190% CPU accumulation</name>
  <files>vite.config.ts</files>
  <action>
1. Terminate the stale server: kill whatever owns :5173 (`lsof -ti :5173 | xargs kill`; expected to be the diagnosis-session vite, PID 1899 — 7h11m CPU over 3h49m elapsed ≈190% average). Confirm the port is free. This is also the fresh-server precondition for Task 3.

2. Investigate the CPU accumulation (bounded — ~20 minutes of active work, honest findings over speculation). Leading hypothesis: the dev server's file watcher reacting to sustained churn INSIDE the project root during 16.5m full-matrix runs — Playwright writes test-results/ (and error-context dumps) continuously, git operations churn, .planning edits land — each change re-statting/re-invalidating modules for a server nobody is reloading. Procedure:
   a. Read vite.config.ts (watch/ignore config if any) and check .gitignore for the generated dirs that churn during runs.
   b. Start a fresh `npm run dev`, record baseline CPU (ps -o pid,etime,cputime for the vite process), run a representative e2e subset (e.g. `npx playwright test tests/e2e/toc tests/e2e/library/focused-add.spec.ts --project=webkit`) while sampling cputime every ~30s, and compare accumulated CPU against elapsed — repeat with test-results/ outputDir redirected outside the repo (PLAYWRIGHT outputDir flag or a temp-dir run) to isolate watcher churn as the variable.
   c. If watcher churn is confirmed or strongly indicated: land the MINIMAL mitigation — server.watch.ignored additions in vite.config.ts for the generated dirs (test-results/, playwright-report/, and any other evidenced churn source), with a comment citing the 190% incident + the debug-session path. Do not ignore anything else; do not touch any other config key.
   d. If the evidence is inconclusive or points elsewhere: change NOTHING and record the findings (sampling data + the leading remaining hypothesis) honestly in the SUMMARY — an unproven config change would be worse than a documented open question.
  </action>
  <verify>
    <automated>test -z "$(lsof -ti :5173)" && npx tsc --noEmit && npx playwright test tests/e2e/toc/restoration-cue.spec.ts</automated>
  </verify>
  <done>Port 5173 free of the stale server; investigation findings (with sampling numbers) recorded in the SUMMARY §Dev-server CPU findings; vite.config.ts either carries the evidenced watch.ignored mitigation (tsc clean, dev server boots, a smoke spec green on the fresh server) or is byte-unchanged with the inconclusive finding documented.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh-server honest acceptance gate — the D18/19-VALIDATION re-run</name>
  <files></files>
  <action>
The acceptance-gate re-run closing UAT Test 7. No production files change; the ledger lives in this plan's SUMMARY.

1. Precondition check: `uptime` (machine load) and confirm :5173 is free — if anything reclaimed the port, kill it; the FIRST invocation must boot its own fresh server (reuseExistingServer only reuses when a server exists; an empty port guarantees fresh).
2. Run the gate in the 21-06 ledger discipline, every invocation recorded with true exit code + counts + classification:
   a. `npm run lint` — expect exit 0.
   b. `npm run test:unit -- --run` — expect the documented unit pass set (0 failed, the ~13 intentional skips).
   c. `npm run test:e2e -- --workers=2` — the 20-07/21-06 bounded-workers control (recorded green precedent; workers:3 config default remains untouched — the flag is invocation-level contention control). Expect 0 failed / only the documented intentional skips (the 16-skip set + any engine-boundary skips), INCLUDING 21-07's page-turn-focus-handoff spec and the hardened toc-navigation cells.
3. Starvation protocol (only if a red appears): any webkit moving-tail goto-timeout set → isolation re-run of exactly the failed specs on a fresh server (`--project=webkit`) before classifying, per the D18 lesson (webkit-only + isolation-green = environment); if machine load is the trigger, the --workers=2 control is already applied — record and re-run once after load decays. Never weaken an assertion to make a red green.
4. Record the full ledger (invocation, command, exit, pass/fail/skip counts, classification) in the SUMMARY §Gate Ledger, mirroring 21-06's format.
  </action>
  <verify>
    <automated>npm run lint && npm run test:unit -- --run && npm run test:e2e -- --workers=2</automated>
  </verify>
  <done>The full gate exits 0 across one recorded invocation sequence on a fresh dev server: lint 0, unit 0-failed, e2e 0-failed with only the documented skip set — the six goto-timeout cells and toc (d) all green inside the full matrix; every invocation (including any classified red + isolation re-run) honestly ledgered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app under test | Playwright drives the real UI; no new input surface |
| dev-tooling config → build pipeline | vite.config.ts watch.ignored (conditional) alters dev-server file watching only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08-01 | Tampering | vite.config.ts server.watch.ignored | mitigate | mitigation may only ADD ignore entries for evidenced generated dirs (test-results/, playwright-report/); never ignore src/, config, or any source path — task action constrains it; tsc + boot + smoke spec verify |
| T-21-08-02 | Denial of Service | full-matrix gate under machine load | accept | bounded-workers control + documented starvation classification protocol; no assertion weakened to force green |
| T-21-08-SC | Tampering | package installs | accept | no installs in this plan — zero new supply-chain surface |
</threat_model>

<verification>
- toc-navigation.spec.ts green × 3 engines with a pure-addition diff (strengthen-only verified).
- Stale dev server gone; CPU findings recorded with sampling evidence; mitigation only where evidenced.
- Fresh-server full gate: lint exit 0 + unit 0-failed + e2e 0-failed (--workers=2) in one recorded sequence, 21-07's regression spec included; ledger in the SUMMARY.
</verification>

<success_criteria>
UAT Test 7's truth is restored: "the six-spec edge matrix across the four destinations is green with no weakened assertions" — re-established by the full-suite green gate on a fresh server (a superset of the six-spec matrix), with the TOC race closed at the helper level and the environmental trigger understood and recorded.
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-08-SUMMARY.md` when done
</output>
