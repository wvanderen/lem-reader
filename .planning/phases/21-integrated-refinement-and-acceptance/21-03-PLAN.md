---
phase: 21-integrated-refinement-and-acceptance
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - src/routes/review/ReviewView.tsx
  - src/app.css
  - tests/e2e/review-panel/jump-bidirectional.spec.ts
  - tests/e2e/review-panel/listing.spec.ts
autonomous: true
requirements: [POLISH-10]
must_haves:
  truths:
    - "Every jump-capable (confident) Highlights row carries a visible open-in-reader glyph so the destination is understandable at a glance (D21-06)"
    - "The glyph never appears on orphan-tail rows or disabled/unresolved rows; ambiguous/orphan disabled rules are unchanged"
    - "The row's accessible name (Go to highlight template), role, and disabled state are byte-stable — the glyph is decorative"
    - "Highlights layout measurements derive from the same shared tokens as Library: row padding at the primary lg register, section headings at the shared 22px register"
    - "Every intentional deviation from Library anatomy carries a citation comment (POLISH-07 discipline, D21-07)"
  artifacts:
    - path: "src/routes/review/ReviewView.tsx"
      provides: "Module-local jump glyph component rendered inside confident row buttons"
      contains: "viewBox"
    - path: "src/app.css"
      provides: ".review-* token conformance (padding lg, h2 conform, citation comments, one hover selector)"
      contains: "D21-07"
  key_links:
    - from: "src/routes/review/ReviewView.tsx (row button)"
      to: "article context"
      via: "existing whole-row hash jump — unchanged; glyph is the visible affordance of the same action"
      pattern: "#/article/"
    - from: ".review-row:hover"
      to: "glyph color"
      via: "one additive tokens-only selector: ink-soft at rest, accent on row hover"
      pattern: "var\\(--accent\\)"
  prohibitions:
    - statement: "Row semantics, the aria-label template, ambiguous/orphan disabled rules, and the orphan-tail shape are unchanged (D21-06)"
      status: enforced
      verification: "review-panel specs assert role/name/state byte-stable; git diff shows no change to the aria-label construction or disabled logic"
    - statement: "No literal shared grid template — Highlights keeps its single-column row anatomy (D21-07)"
      status: enforced
      verification: ".review-section-list keeps single-column layout; deviations carry citation comments"
    - statement: "No new color tokens; no motion properties (zero-transition hover, color only)"
      status: enforced
      verification: "hover selector uses existing tokens only; grep the review block for transition/animation returns 0"
    - statement: ".review-select 16px register stays (D15-03 cited intentional difference — do not fix)"
      status: enforced
      verification: "existing citation comment intact; review-select font-size unchanged"
---

<objective>
Highlights cohesion (POLISH-10, D21-06/D21-07): the Highlights destination
respects the shared layout grammar through token conformance (not a literal
shared grid) and makes article-context navigation understandable at a glance
with a quiet inline-SVG glyph on every confident row — row semantics,
disabled rules, and the SR contract untouched.

Purpose: Review rows currently read as opaque cards — the whole-row jump has
no visible affordance — and several .review-* rules drift from the shared
token registers Library uses. This plan conforms-or-cites each measurement
(POLISH-07 discipline) and adds the visible affordance.
Output: glyph + conformance CSS + extended review-panel proof.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visible row affordance glyph + .review-* token conformance</name>
  <files>src/routes/review/ReviewView.tsx, src/app.css</files>
  <read_first>
  - src/routes/review/ReviewView.tsx (row button L242-259, aria-label construction, ambiguous/orphan disabled rules L224-233, orphan-tail shape)
  - src/ingestion/library/LibraryRow.tsx (EditIcon L202-227 + TrashIcon L168-200 — the icon anatomy to clone)
  - src/app.css (.review-* block ~L3036-3243; .library-row/.book-row padding ~L2066 + ~L3260; .review-select citation comment L3079-3082; .continue-reading-strip h2 — confirms no override = 22px shared register)
  - 21-UI-SPEC.md §3 conformance ledger (prescriptive calls) + Typography/Spacing phase deltas
  - 21-PATTERNS.md "POLISH-10 wave" (icon anatomy contract + citation-comment format)
  </read_first>
  <action>
  1. Glyph (D21-06): add a module-local glyph component in ReviewView.tsx (e.g. JumpToArticleIcon) cloning the project anatomy exactly — 20x20 box, viewBox 0 0 24 24, fill none, stroke currentColor, stroke-width 1.75, round caps/joins, aria-hidden true, focusable false, doc comment citing the LibraryRow precedent — shaped as a north-east open-in-context arrow. Render it inside the confident (jump-capable) row button at the row foot, inline-end of the date line (an SVG is non-interactive content — legal inside the button; the .review-row-actions cluster is untouched). Never render it on orphan-tail rows or disabled/unresolved rows. The aria-label template stays the whole accessible name.
  2. Conformance (D21-07) per the UI-SPEC §3 ledger: conform .review-row padding to var(--space-lg) (primary row register); DELETE the .review-section h2 font-size override so it inherits the global 22px/600/1.3 register (resolves RESEARCH A5 as conform); add citation comments in the established format for (a) the single-column .review-section-list anatomy vs Library's responsive grid and (b) the absence of @media re-grid rules (1100px cap + main#main insets carry narrow widths); keep .review-select 16px and its existing D15-03 comment untouched.
  3. One additive hover selector: the glyph inherits ink-soft at rest and takes var(--accent) on .review-row:hover — tokens only, zero motion properties, matching the tags-trigger/library-row-remove rest/hover grammar.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/review-filter.test.ts && npx playwright test tests/e2e/review-panel/listing.spec.ts tests/e2e/review-panel/tri-state.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - ReviewView.tsx contains the glyph component with aria-hidden and focusable false attributes and viewBox 0 0 24 24; it renders conditionally only when the row is jump-capable
  - The row button's aria-label construction and disabled logic are byte-stable (git diff shows no change to those lines)
  - src/app.css .review-row block contains "padding: var(--space-lg)"
  - The review block contains no h2 font-size override: grep -A3 '\.review-section h2' src/app.css returns no font-size declaration line
  - grep -c 'D21-07' src/app.css is at least 2 (the two new citation comments)
  - The hover selector references var(--accent); the review block declares no transition or animation properties
  - tests/e2e/review-panel/listing.spec.ts and tri-state.spec.ts green (existing anatomy proof holds)
  </acceptance_criteria>
  <done>Confident rows show the quiet glyph; every measurement conforms or carries a citation; SR contract and disabled rules byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Review-panel e2e extension — glyph visibility + conformance cells</name>
  <files>tests/e2e/review-panel/jump-bidirectional.spec.ts, tests/e2e/review-panel/listing.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/jump-bidirectional.spec.ts (harness discipline L34-45: wipeDatabase + app load before seedRows; rows seeded through ArticleSchema + shipped derive/resolve machinery; expect(async…).toPass retry-asserts)
  - tests/e2e/review-panel/tri-state.spec.ts (ambiguous/orphan disabled-rule assertions — must stay green byte-stable)
  - tests/e2e/review-panel/listing.spec.ts (row anatomy assertions)
  - 21-PATTERNS.md review-panel extension section
  </read_first>
  <action>
  Extend the review-panel specs (strengthen-only — no existing assertion removed):
  1. Glyph cells (D21-06): in jump-bidirectional (or the fittest sibling), assert each confident row button contains the visible glyph (svg descendant) and the row's accessible name still matches the Go to highlight template via getByRole with name regex; assert orphan-tail rows and disabled/unresolved rows contain no glyph and keep aria-disabled/disabled state.
  2. Conformance cells (D21-07): in listing.spec.ts assert .review-section h2 computes the shared 22px register and .review-row computes the lg padding (24px) — computed-style assertions, no pixel drift tolerance beyond sub-pixel.
  3. Jump behavior: the existing bidirectional jump assertions stay and re-run green (glyph addition must not perturb activation).
  Run the full review-panel directory on all three engines.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel</automated>
  </verify>
  <acceptance_criteria>
  - jump-bidirectional.spec.ts contains a glyph-presence assertion scoped to confident rows and a glyph-absence assertion for orphan/disabled rows
  - listing.spec.ts contains computed-style assertions for the 22px section heading register and the lg row padding
  - The full tests/e2e/review-panel run exits 0 on chromium, firefox, and webkit
  - git diff shows tri-state.spec.ts and curate.spec.ts untouched (their disabled-rule proof is the regression net)
  </acceptance_criteria>
  <done>POLISH-10 proven: visible affordance on exactly the jump-capable rows, shared-register measurements, navigation semantics intact, 3 engines green.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- `JumpToArticleIcon` (module-local glyph component) — src/routes/review/ReviewView.tsx
- `.review-row` lg padding + `.review-section h2` register conform + 2 citation comments + 1 hover selector — src/app.css
- glyph/conformance e2e cells — tests/e2e/review-panel/jump-bidirectional.spec.ts, listing.spec.ts

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Decorative SVG inside an existing native button; no data flow, no input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Elevation of Privilege | glyph as interactive element | mitigate | Glyph is aria-hidden + focusable false inside the existing button — never a separate control; e2e asserts the single row-button role/name |
| T-21-07 | Tampering | conformance edits | accept | CSS token swaps only; no selector broadens beyond the review block; forced-colors safe by construction (currentColor stroke) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed |
</threat_model>

<verification>
- review-panel suite green on 3 engines (glyph + conformance + jump + tri-state)
- No role/name/state changes; no motion properties; citation comments present
</verification>

<success_criteria>
- POLISH-10 proven: token-conformant Highlights with a visible, decorative, anatomy-consistent jump affordance
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-03-SUMMARY.md` when done
</output>
