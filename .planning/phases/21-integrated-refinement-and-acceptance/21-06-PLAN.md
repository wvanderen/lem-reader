---
phase: 21-integrated-refinement-and-acceptance
plan: 06
type: execute
wave: 5
depends_on: ["21-04", "21-05"]
files_modified:
  - tests/e2e/_edge-invariant.ts
  - tests/e2e/forced-colors.spec.ts
  - tests/e2e/reduced-motion.spec.ts
  - tests/e2e/reflow.spec.ts
  - tests/e2e/high-zoom.spec.ts
  - tests/e2e/touch-targets.spec.ts
  - tests/e2e/panel-keyboard.spec.ts
  - tests/e2e/library/focused-add.spec.ts
  - docs/ACCEPTANCE-PROTOCOL.md
autonomous: true
requirements: [ACPT-08]
user_setup:
  - service: "NVDA + Firefox on Windows hardware"
    why: "ACPT-08 screen-reader arm — locked human-run on real hardware (D13-06/D13-07); off-machine per RESEARCH Environment Availability; user-scheduled, results land in the verification ledger"
  - service: "VoiceOver + Safari on macOS"
    why: "ACPT-08 screen-reader arm + D21-12 one-time real-Safari image-flow verification (save, offline reopen, export, import with images) folded into the VO session plus one sighted pass; evidence recorded in the protocol results sheet"
must_haves:
  truths:
    - "The automated matrix arms — keyboard, reduced-motion, forced-colors, 320px reflow, 400% zoom, touch targets — pass across all four destinations (Library, Highlights, Add dialog, Reader) on chromium, firefox, and webkit via the extended D6-09 edge-invariant machinery"
    - "ACCEPTANCE-PROTOCOL.md is v1.3: capability flows cover each v2.1 capability once (library views/filters, Add dialog, metadata edit, TOC navigation, cross-block highlight + review, images) authored as keyboard sequence + role + accessible name + state outcomes, on top of the existing reader/settings flows and exploratory charters (D21-14)"
    - "The Phase 16 Add-dialog manual-SR deferral is retired by the v1.3 Add-dialog flow"
    - "npm run lint AND npm run test exit 0 in the phase honest gate, with every invocation recorded (fresh dev server, workers-contention discipline)"
    - "ACPT-08 flips only when the human NVDA+Firefox AND VoiceOver+Safari runs land zero blocker/major on protocol v1.3 (D13-06/D13-07) — not in this plan"
  artifacts:
    - path: "tests/e2e/_edge-invariant.ts"
      provides: "Destination-agnostic wrapper asserting the destination-neutral invariant clauses ((b) required functions reachable + (c) no overflow) at non-reader destinations, (a) stays reader-scoped"
      exports: ["assertDestinationInvariant"]
    - path: "docs/ACCEPTANCE-PROTOCOL.md"
      provides: "Protocol v1.3 — capability flows + results-record location updated to the Phase 21 verification artifact"
      contains: "1.3"
  key_links:
    - from: "forced-colors/reduced-motion/reflow/high-zoom/touch-targets specs"
      to: "tests/e2e/_edge-invariant.ts"
      via: "destination cells call the shared wrapper — extend, never fork (D6-09)"
      pattern: "assertDestinationInvariant"
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: "21-VERIFICATION.md"
      via: "results-record location points at the Phase 21 verification artifact; the D21-12 Safari image evidence lands in the protocol results sheet with a pointer"
      pattern: "21-VERIFICATION"
  prohibitions:
    - statement: "Strengthen-only: no existing edge-spec assertion is removed or weakened to make new destination cells pass (D6-12)"
      status: enforced
      verification: "existing describe/test blocks byte-stable in the five edge specs; new cells are additive"
    - statement: "assertEdgeInvariant is extended, never forked — one helper file owns the invariant"
      status: enforced
      verification: "no new invariant helper module; the wrapper lives in tests/e2e/_edge-invariant.ts and reuses clause logic"
    - statement: "Protocol v1.3 outcomes are authored as role + accessible name + state — never verbatim SR phrasing as a gate (Pitfall 7)"
      status: enforced
      verification: "each new flow table's expected-outcome column names roles/accessible names, not SR utterances"
    - statement: "ACPT-08 does not flip in-plan and no REQUIREMENTS.md row is edited by this plan — the flip belongs to verify-work at zero-blocker/major"
      status: enforced
      verification: "git diff shows .planning/REQUIREMENTS.md unchanged by this plan"
    - statement: "The existing protocol flows and exploratory charters stay intact (growth, not rewrite)"
      status: enforced
      verification: "the 6 existing flows + 5 charters present and unmodified in v1.3"
---

<objective>
ACPT-08 acceptance matrix (D21-14 + carried flip policy): extend the existing
D6-09 edge-invariant machinery across the four destinations (Library,
Highlights, Add, Reader) for the automated arms (keyboard, reduced-motion,
forced-colors, 320px reflow, 400% zoom, touch targets); grow
ACCEPTANCE-PROTOCOL.md v1.2 → v1.3 with capability flows (retiring the Phase
16 Add-dialog SR deferral) and the D21-12 Safari image-verification slot; and
run the phase honest gate (lint + full test suite, every invocation recorded)
that closes the milestone's automated bars.

Purpose: The matrix is the milestone's proof instrument. Its automated arms
must extend shipped machinery (never fork), its human arms are locked to
real-hardware protocol runs, and the phase gate must be honest — red runs
recorded, webkit starvation classified, never silently green.
Output: destination edge cells + protocol v1.3 + the phase gate record.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UI-SPEC.md
@docs/ACCEPTANCE-PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Destination-agnostic edge-invariant wrapper + four-destination matrix cells</name>
  <files>tests/e2e/_edge-invariant.ts, tests/e2e/forced-colors.spec.ts, tests/e2e/reduced-motion.spec.ts, tests/e2e/reflow.spec.ts, tests/e2e/high-zoom.spec.ts, tests/e2e/touch-targets.spec.ts, tests/e2e/panel-keyboard.spec.ts</files>
  <read_first>
  - tests/e2e/_edge-invariant.ts (assertEdgeInvariant L90 + its argument type at L53 — the (a)/(b)/(c) clause structure; (a) is article-scoped, (b)/(c) destination-neutral)
  - tests/e2e/high-zoom.spec.ts (extension loop pattern L41-69: beforeEach wipe + setViewportSize + per-fixture describe)
  - tests/e2e/forced-colors.spec.ts, tests/e2e/reduced-motion.spec.ts, tests/e2e/reflow.spec.ts, tests/e2e/touch-targets.spec.ts (each file being extended)
  - tests/e2e/panel-keyboard.spec.ts (keyboard arm substrate) and tests/e2e/library/focused-add.spec.ts (ADD-04 Add-dialog focus discipline cells)
  - 21-RESEARCH.md §Pattern 6 + Open Question 2 (wrapper vs per-destination recommendation)
  - 21-PATTERNS.md edge-spec extension section
  </read_first>
  <action>
  1. Add a destination-agnostic wrapper (e.g. assertDestinationInvariant(page, opts)) to tests/e2e/_edge-invariant.ts asserting the destination-neutral clauses — required functions reachable + no overflow — for non-reader destinations; the article clause stays scoped inside the existing reader helper. Extend the shared module; do not fork it, do not remove or relax any existing assertion.
  2. Extend each edge spec (forced-colors, reduced-motion, reflow, high-zoom, touch-targets) with additive destination cells covering Library (list + views/filters), Highlights, and Add dialog (opened), alongside the existing Reader cells — navigating to each destination via the real UI (shell-nav), seeding through the existing harness discipline (wipeDatabase + app load before seed), reusing each spec's existing condition emulation (forced-colors emulation, reduced-motion, 320px viewport, zoom geometry, target measurement).
  3. Keyboard arm: extend panel-keyboard.spec.ts (+ the ADD-04 Add-dialog focus cells in focused-add.spec.ts only additively) so Esc + focus-restore and full keyboard operability are asserted on the Add dialog and Highlights surfaces — engine focus divergence asserted honestly per engine (D18-04 precedent).
  4. Run the extended matrix on all three engines.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/forced-colors.spec.ts tests/e2e/reduced-motion.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts tests/e2e/touch-targets.spec.ts tests/e2e/panel-keyboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/_edge-invariant.ts exports assertDestinationInvariant and still exports assertEdgeInvariant (grep both)
  - Each of the five edge specs contains destination cells for the review/highlights destination and the library destination (grep per file), additive to the existing reader cells
  - Existing describe/test blocks in the five edge specs are byte-stable (strengthen-only; git diff shows additions, no removals)
  - The three-engine run of the six specs exits 0
  </acceptance_criteria>
  <done>Automated matrix arms pass across all four destinations on chromium/firefox/webkit through the one shared invariant module.</done>
</task>

<task type="auto">
  <name>Task 2: ACCEPTANCE-PROTOCOL v1.3 — capability flows + Safari image slot</name>
  <files>docs/ACCEPTANCE-PROTOCOL.md</files>
  <read_first>
  - docs/ACCEPTANCE-PROTOCOL.md (read IN FULL: §1 Matrix L42, §2 Authoring rules L90, §3 Flows L113-311 incl. Flow A table format L140-146, §4 Charters L313, §5 Severity L369, §6 Results L410, §7 Re-run L432)
  - 21-RESEARCH.md §Pattern 6 + §Pitfall 7 (role + accessible name + state discipline)
  - 21-CONTEXT.md D21-12/D21-14 (growth scope + Safari fold-in)
  - .planning/phases/16-organized-library-and-focused-add-flow/16-CONTEXT.md (the manual-SR deferral being retired)
  </read_first>
  <action>
  Grow v1.2 → v1.3 in the existing structure (D21-14):
  1. Bump the header Version to 1.3; keep the D6-08 versioned re-run contract intact.
  2. Add capability flows in the exact Flow format (Goal + numbered keyboard-sequence table + Pass criterion), one per v2.1 capability: library views/filters; Add dialog (retiring the Phase 16 manual-SR deferral — cite it); metadata edit; TOC navigation; cross-block highlight + review; images (save → offline reopen → view). Target ~8-10 scripted flows per SR engine total (existing 6 + new capability flows). Every expected outcome is role + accessible name + state — phrasing notes informational-only, severity minor at most (Pitfall 7).
  3. Fold the D21-12 one-time real-Safari image-flow verification into the VoiceOver+Safari session + one sighted pass: save → reopen offline → export → import with images; record evidence in the protocol results sheet with a pointer from 21-VERIFICATION.md (RESEARCH OQ3 recommendation).
  4. Update the results-record location from 06-VERIFICATION.md to the Phase 21 verification artifact (21-VERIFICATION.md); add blank v1.3 results sheets for both pairings in the existing format.
  5. Keep the 5 exploratory charters and the zero-blocker/major severity policy text unchanged.
  </action>
  <verify>
    <automated>grep -c '1\.3' docs/ACCEPTANCE-PROTOCOL.md && grep -c 'Add dialog' docs/ACCEPTANCE-PROTOCOL.md && grep -c '21-VERIFICATION' docs/ACCEPTANCE-PROTOCOL.md && grep -c 'table of contents\|TOC' docs/ACCEPTANCE-PROTOCOL.md</automated>
  </verify>
  <acceptance_criteria>
  - docs/ACCEPTANCE-PROTOCOL.md header shows version 1.3
  - New capability flows present for all six capabilities (library views/filters, Add dialog, metadata edit, TOC navigation, cross-block highlight + review, images) in the Flow table format
  - The Add-dialog flow cites the retired Phase 16 deferral
  - The results-record location references 21-VERIFICATION.md; the D21-12 Safari image slot appears in the VO+Safari session section
  - The 6 existing flows and 5 exploratory charters remain present and unmodified
  - Every new flow table's expected-outcome column expresses role/name/state (no SR-utterance gates)
  </acceptance_criteria>
  <done>Protocol v1.3 is the durable instrument for the human runs: capability-complete, Pitfall-7-disciplined, Safari image verification slotted, results sheets ready.</done>
</task>

<task type="auto">
  <name>Task 3: Phase honest gate — lint + full suite, every invocation recorded</name>
  <files></files>
  <read_first>
  - 21-RESEARCH.md §Pitfall 5 (webkit starvation discipline) + §Validation Architecture (phase-gate shape)
  - playwright.config.ts (webServer + workers:3 contention control)
  - package.json (test/lint scripts)
  </read_first>
  <action>
  Run the milestone-close gates honestly:
  1. Start a FRESH dev server (never a reused stale one — Pitfall 5).
  2. Run npm run lint (D21-15 milestone condition) — expect exit 0.
  3. Run npm run test (unit --run + e2e across chromium/firefox/webkit + throttled) — expect exit 0.
  4. Record EVERY invocation in the SUMMARY with exit codes — red runs included, never silently green; if late webkit specs fail but re-run green in isolation, classify as environment starvation per the documented 18-04/15-04/20-07 lesson (fresh server + workers:3) and record both runs.
  5. In the SUMMARY, declare the human SR sessions that remain (user_setup): NVDA+Firefox (Windows, off-machine) and VoiceOver+Safari (macOS, incl. the D21-12 sighted image pass) on protocol v1.3 — ACPT-08 flips only at their zero-blocker/major results (D13-06/D13-07), via verify-work, not in-plan.
  </action>
  <verify>
    <automated>npm run lint && npm run test</automated>
    <human-check>
    After this plan: run protocol v1.3 on NVDA+Firefox (Windows hardware) and VoiceOver+Safari (macOS, folding the D21-12 sighted image pass: save an article with images, reopen offline, export, import, verify images). Record results in the protocol v1.3 results sheets; ACPT-08 flips only when both runs land zero blocker/major.
    </human-check>
  </verify>
  <acceptance_criteria>
  - npm run lint exits 0
  - npm run test exits 0 in one invocation (or with honestly-recorded environment-classified re-runs)
  - The SUMMARY lists every gate invocation with its exit code
  - The SUMMARY declares both human SR sessions with their flip condition
  </acceptance_criteria>
  <done>The milestone's automated bars are closed honestly: lint green, full suite green, invocation ledger complete, human SR runs teed up for verify-work.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- `assertDestinationInvariant` (exported wrapper) — tests/e2e/_edge-invariant.ts
- Four-destination edge cells across five edge specs + panel-keyboard extension (new test cells)
- ACCEPTANCE-PROTOCOL.md v1.3 (capability flows, Safari image slot, v1.3 results sheets)
- Phase gate invocation ledger — 21-06-SUMMARY.md

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol doc → human acceptance | The protocol's expected outcomes gate real SR runs; drifting into verbatim phrasing would make acceptance flaky and gamed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Tampering | protocol v1.3 authoring | mitigate | Role + accessible name + state discipline enforced in flow tables (Pitfall 7); severity policy text unchanged |
| T-21-13 | Denial of Service | gate contention (false reds) | mitigate | Fresh dev server + pinned workers:3 + honest invocation recording with starvation classification (Pitfall 5) |
| T-21-14 | Repudiation | gate record | mitigate | Every invocation recorded in the SUMMARY (red included) — the project's standing honest-gate discipline |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed |
</threat_model>

<verification>
- Extended edge matrix green on 3 engines across 4 destinations
- Protocol v1.3 structurally verified (version, flows, results location)
- Phase gate: npm run lint + npm run test exit 0, ledger complete
- Human-check declared for both SR sessions (ACPT-08 flip condition)
</verification>

<success_criteria>
- ACPT-08's automated arms pass on the full matrix; the v1.3 instrument is ready; the milestone's honest gates are green and recorded
- The ACPT-08 requirement flip is correctly left to the human zero-blocker runs (D13-06/D13-07)
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-06-SUMMARY.md` when done
</output>
