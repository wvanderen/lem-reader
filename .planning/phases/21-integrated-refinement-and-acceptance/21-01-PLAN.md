---
phase: 21-integrated-refinement-and-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings/tokens.ts
  - src/settings/legacyMeasure.ts
  - src/content/schema.ts
  - src/persistence/settingsStore.ts
  - src/settings/settingsMirror.ts
  - src/portability/ExportImportService.ts
  - index.html
  - tests/unit/settings/measure-clamp.test.ts
  - tests/unit/settingsSchema.test.ts
  - tests/unit/settings/mirror.test.ts
  - tests/unit/storageFallback.test.ts
  - tests/component/SettingsContext.test.tsx
  - tests/e2e/typography-live-apply.spec.ts
  - tests/e2e/polish/cold-load-no-snap.spec.ts
  - tests/e2e/polish/first-paint-mode-surface.spec.ts
  - tests/e2e/pagination/fixtures-matrix.ts
autonomous: true
requirements: [POLISH-09]
must_haves:
  truths:
    - "At the slider far-right endpoint, the reading surface content width equals a 64-zero-advance ruler of the body font within 1px, in scrolling AND paginated mode"
    - "The slider exposes min 40 / max 64 / step 6 identically visually and programmatically (aria-valuemin, aria-valuemax, aria-valuenow), and the readout reads Reading width 64 ch at far-right"
    - "A persisted settings row carrying the legacy maximum measure loads calmly as 64 with every other field intact — never the corrupt path, never WipeConfirm (D21-03)"
    - "A garbage measure value still fails parse and surfaces the existing corrupt path (STATE-04 never-silently-coerce)"
    - "An exported v2.1-era bundle whose preferences block carries the legacy maximum re-imports calmly"
    - "First paint matches hydration — no transient legacy-width flash from the index.html paint hint"
  artifacts:
    - path: "src/settings/legacyMeasure.ts"
      provides: "LEGACY_MEASURE bounded map + clampLegacyMeasure pre-parse normalizer (D21-03)"
      exports: ["clampLegacyMeasure", "LEGACY_MEASURE"]
      contains: "72"
    - path: "src/settings/tokens.ts"
      provides: "MEASURE_STEPS five-step uniform-6 range (D21-01/D21-02)"
      contains: "40, 46, 52, 58, 64"
    - path: "tests/unit/settings/measure-clamp.test.ts"
      provides: "Three-seam clamp unit proof + paint-hint sync-check"
  key_links:
    - from: "src/persistence/settingsStore.ts"
      to: "src/settings/legacyMeasure.ts"
      via: "loadSettings wraps the raw row with clampLegacyMeasure before ReaderSettingsSchema.safeParse"
      pattern: "safeParse\\(clampLegacyMeasure\\("
    - from: "src/settings/settingsMirror.ts"
      to: "src/settings/legacyMeasure.ts"
      via: "readSettingsMirror clamps the parsed JSON before safeParse"
      pattern: "clampLegacyMeasure"
    - from: "src/portability/ExportImportService.ts"
      to: "src/settings/legacyMeasure.ts"
      via: "import seam clamps the raw preferences block before ExportBundleSchema.safeParse"
      pattern: "clampLegacyMeasure"
    - from: "index.html"
      to: "src/settings/tokens.ts"
      via: "paint-hint inline legacy map mirrors the module map so first paint matches hydration"
      pattern: "--measure"
  prohibitions:
    - statement: "The legacy map contains exactly one entry (the known 72 value mapping to 64); arbitrary out-of-range values are never coerced"
      status: enforced
      verification: "measure-clamp.test.ts asserts garbage values (71, string, null) still fail parse per seam contract"
    - statement: "The clamp is silent — no migration prompt, no new user-facing copy (D21-03)"
      status: enforced
      verification: "No new string literals in settings surfaces; corrupt-path copy byte-stable"
    - statement: "The settings readout gains no endpoint labels (D21-04 keeps the inline shape)"
      status: enforced
      verification: "SettingsPanel.tsx readout template byte-stable"
    - statement: "The 5 documented webkit Blob→IDB skip sites stay byte-stable (D21-11)"
      status: enforced
      verification: "git diff shows zero changes to the 5 enumerated skip sites"
---

<objective>
Truthful reading width (POLISH-09, D21-01/D21-02/D21-03/D21-04): the slider's
far-right endpoint is a truthful 64ch — the 72 step is removed, the range
extends downward to a uniform-step-6 five-step set, every stored legacy-72
setting clamps calmly to 64 at every settings-entry seam, and both the visual
surface and the programmatic attributes agree at every step in both reading
modes.

Purpose: The current far-right step lies (displays 72 but the surface never
delivers it). This plan makes 64 mean 64 — shown value, aria attributes, and
the rendered column all agree — without routing a single legacy reader to the
WipeConfirm corrupt path (RESEARCH Pitfall 1, THE phase trap).
Output: clamp module + evolved tokens/schema + four seam edits + unit and e2e
truth proof.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Truthful steps + bounded legacy clamp at every settings seam</name>
  <files>src/settings/legacyMeasure.ts, src/settings/tokens.ts, src/content/schema.ts, src/persistence/settingsStore.ts, src/settings/settingsMirror.ts, src/portability/ExportImportService.ts, index.html, tests/unit/settings/measure-clamp.test.ts, tests/unit/settingsSchema.test.ts, tests/unit/settings/mirror.test.ts, tests/unit/storageFallback.test.ts, tests/component/SettingsContext.test.tsx</files>
  <read_first>
  - src/settings/tokens.ts (MEASURE_STEPS L30-32 + SIZE_STEPS sibling discipline)
  - src/content/schema.ts (measure union L360-365 + readingMode .default() boundary-migration precedent L368-372)
  - src/persistence/settingsStore.ts (loadSettings safeParse seam L54-78)
  - src/settings/settingsMirror.ts (readSettingsMirror L39-51)
  - src/portability/ExportImportService.ts (import parse choke point ~L282-309) and src/portability/bundle.ts (preferences block embedding ReaderSettingsSchema ~L106)
  - index.html (paint-hint script L23-27 marker comments + measure write L83-85)
  - tests/unit/settings/mirror.test.ts (seam-test structure + NON_DEFAULT fixture L30-38 + any paint-hint sync-check)
  - tests/unit/settingsSchema.test.ts (valid/invalid union tables, pinned rows L80 + L249)
  - tests/unit/storageFallback.test.ts (L49 pinned cell), tests/component/SettingsContext.test.tsx (L144 pinned cell)
  - 21-RESEARCH.md §Pattern 2 + §Pitfall 1; 21-PATTERNS.md "POLISH-09 wave" (full clamp sketches + seam line cites)
  </read_first>
  <behavior>
  - clampLegacyMeasure on a record whose measure is the legacy 72 returns the same record with measure 64 and every other field identical
  - clampLegacyMeasure on a record whose measure is 71, or a string, or null, or a record without measure returns the input unchanged (so downstream safeParse still fails → corrupt path per STATE-04)
  - settingsStore.loadSettings on a stored-72 Dexie row returns ok:true with measure 64; on a garbage-measure row returns ok:false reason corrupt
  - readSettingsMirror on a painted-72 mirror returns parsed settings at 64 (not null); on garbage returns null
  - import path: a bundle preferences block carrying the legacy value parses through ExportBundleSchema after clamping; garbage still refuses
  - index.html paint-hint inline map matches the module LEGACY_MEASURE map (sync-check in the same discipline as the existing marker-comment test)
  </behavior>
  <action>
  Write tests/unit/settings/measure-clamp.test.ts FIRST (RED), then implement (GREEN) per D21-01/D21-02/D21-03:
  1. Create src/settings/legacyMeasure.ts exporting LEGACY_MEASURE (a frozen single-entry record mapping the known legacy maximum 72 to 64 — exactly one key, never more) and clampLegacyMeasure(raw: unknown): unknown following the 21-PATTERNS.md sketch (object-with-measure check, spread-copy on map hit, input returned untouched otherwise). Header comment cites D21-03 + STATE-04 (only the enumerated legacy value bypasses failure; actual corruption still surfaces).
  2. src/settings/tokens.ts: evolve MEASURE_STEPS to the five-step uniform-6 array [40, 46, 52, 58, 64]; update the trailing comment (default 64 is now index 4 per D21-01/D21-02). SIZE_STEPS untouched.
  3. src/content/schema.ts: in the measure union, remove the 72 literal and add literals for the two new lower steps 40 and 46, keeping 52/58/64. This edit MUST land in the same commit as the seam clamps (Pitfall 1: alone it routes stored-72 readers to WipeConfirm).
  4. Clamp seams — wrap the raw value with clampLegacyMeasure BEFORE each safeParse: (a) settingsStore.loadSettings on the Dexie row value; (b) settingsMirror.readSettingsMirror on the JSON.parse result; (c) ExportImportService at the import seam, applied to the raw preferences block ahead of ExportBundleSchema.safeParse so a v2.1-era bundle carrying the legacy value re-imports calmly. Add a one-line comment citing D21-03 at each seam.
  5. index.html paint hint: add the same single-entry legacy map inline (this script cannot import modules — inline copy like FONT_STACKS) so the --measure write never paints the legacy width; update the marker comments if the sync-check unit test anchors on them.
  6. Update the four pinned unit/component cells (each with a D21-01/D21-02 citation comment): settingsSchema.test.ts valid-union row + fixture (the legacy value becomes an INVALID-union row), mirror.test.ts NON_DEFAULT measure, storageFallback.test.ts L49, SettingsContext.test.tsx L144 — move each to a valid step (64 or 58).
  Do NOT touch SettingsPanel.tsx (min/max/step/aria all derive from MEASURE_STEPS array ends — verify-only) or the readout shape (D21-04).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/settings/measure-clamp.test.ts tests/unit/settingsSchema.test.ts tests/unit/settings/mirror.test.ts tests/unit/storageFallback.test.ts && npm run test:unit -- --run tests/component/SettingsContext.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - src/settings/legacyMeasure.ts contains "export function clampLegacyMeasure(" and its map literal contains exactly one entry (the 72 key)
  - src/settings/tokens.ts contains "40, 46, 52, 58, 64"
  - src/content/schema.ts contains "z.literal(40)" and "z.literal(46)"; the measure union region contains no 72 literal: sed -n '/measure: z\.union/,/^          \]),/p' src/content/schema.ts | grep -v '^\s*//' | grep -c 'literal(72)' returns 0
  - src/persistence/settingsStore.ts, src/settings/settingsMirror.ts, and src/portability/ExportImportService.ts each contain "clampLegacyMeasure("
  - tests/unit/settings/measure-clamp.test.ts exits 0 and includes a case where a garbage measure value (e.g. 71) still routes to the corrupt/null contract
  - git diff shows tests/unit/portability/zip-slip.test.ts and the 5 webkit skip sites (tests/e2e/imagery/offline-reopen.spec.ts, tests/e2e/portability/round-trip.spec.ts, tests/e2e/portability/import-preview.spec.ts, tests/e2e/epub-intake.spec.ts, tests/e2e/ingestion/happy-path.spec.ts) unchanged
  </acceptance_criteria>
  <done>Five-step truthful range live; the legacy value loads calmly at 64 through Dexie, mirror, and import seams; garbage still surfaces corrupt; paint hint matches hydration; all targeted unit + component suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Truthful-64 e2e proof + legitimately-updating pinned e2e cells</name>
  <files>tests/e2e/typography-live-apply.spec.ts, tests/e2e/polish/cold-load-no-snap.spec.ts, tests/e2e/polish/first-paint-mode-surface.spec.ts, tests/e2e/pagination/fixtures-matrix.ts</files>
  <read_first>
  - tests/e2e/typography-live-apply.spec.ts (slider-driven live-apply substrate — the file being extended)
  - tests/e2e/polish/cold-load-no-snap.spec.ts (L46 seeded measure cell)
  - tests/e2e/polish/first-paint-mode-surface.spec.ts (L57 seeded measure cell)
  - tests/e2e/pagination/fixtures-matrix.ts (L79 typography-matrix cell)
  - src/reader/SettingsPanel.tsx (L455-477 slider derivation — verify-only)
  - src/app.css (.article-body max-width L263; .article-body.paginated-surface width L1100)
  - 21-RESEARCH.md §Pitfall 4 + "Truthful-64 verification cell" code example; 21-PATTERNS.md truthful-measure test section
  </read_first>
  <action>
  1. Extend tests/e2e/typography-live-apply.spec.ts with a truthful-measure describe (D21-01/D21-04): drive the slider to far-right via the real UI, then assert — (a) ruler truth: append a hidden probe span of 64 zero characters (position absolute, visibility hidden, white-space pre) inside .article-body and expect its boundingBox width to equal the surface content width within 1px, in scrolling mode; (b) switch to paginated mode via the mode toggle and repeat the ruler assertion against .article-body.paginated-surface; (c) programmatic agreement: slider aria-valuemax is 64 and aria-valuenow is 64 at far-right; (d) readout agreement: the settings readout shows Reading width 64 ch. Add one far-left cell: aria-valuemin is 40 and the computed --measure resolves to 40ch (D21-02 floor — the 320px-reflow/400%-zoom matrix cells of Plan 06 empirically confirm usability). NEVER verify by counting prose characters — ch is the zero-glyph advance (Pitfall 4).
  2. Update the three pinned e2e cells with a D21-01/02 citation (strengthen-only for everything else): the seeded legacy-max measure in cold-load-no-snap.spec.ts, first-paint-mode-surface.spec.ts, and the fixtures-matrix.ts typography cell each move to a valid step (64 or 58). If any pagination spec consuming the matrix pins page-count/geometry literals for that cell, update those literals with the same citation; all other assertions stay byte-stable.
  3. Run the targeted specs on all three engines.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/typography-live-apply.spec.ts tests/e2e/polish/cold-load-no-snap.spec.ts tests/e2e/polish/first-paint-mode-surface.spec.ts && npx playwright test tests/e2e/pagination</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/typography-live-apply.spec.ts contains a ruler probe building a 64-zero string (grep 'repeat(64)') with a toBeLessThanOrEqual(1) tolerance assertion, executed against BOTH .article-body and the paginated surface
  - tests/e2e/typography-live-apply.spec.ts asserts aria-valuemax 64 and the 64 ch readout
  - grep -c 'measure: 72' tests/e2e/polish/cold-load-no-snap.spec.ts tests/e2e/polish/first-paint-mode-surface.spec.ts tests/e2e/pagination/fixtures-matrix.ts returns 0 for each file
  - The three-engine targeted run exits 0 (chromium, firefox, webkit)
  </acceptance_criteria>
  <done>64 means 64 proven in real browsers on both reading surfaces; the pinned cells the decision legitimately changes are updated with citations; every untouched assertion byte-stable and green.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- `clampLegacyMeasure` (exported function) + `LEGACY_MEASURE` (exported frozen map) — src/settings/legacyMeasure.ts (NEW)
- `MEASURE_STEPS` evolved to the five-step range — src/settings/tokens.ts
- measure schema union literals 40/46 added, 72 removed — src/content/schema.ts
- tests/unit/settings/measure-clamp.test.ts (NEW)
- truthful-measure describe in tests/e2e/typography-live-apply.spec.ts (new cells)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings → schema | IndexedDB row / localStorage mirror / imported bundle preferences cross into app state via ReaderSettingsSchema.safeParse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | clamp seams (settingsStore/mirror/import) | mitigate | Bounded map: only the enumerated legacy value maps; all other invalid values still fail parse and classify (STATE-04/V5 hold); unit tests lock both directions |
| T-21-02 | Tampering | schema union | mitigate | Never widen the union for tolerance — the union stays an enumeration; the clamp is a pre-parse legacy map only |
| T-21-03 | Information Disclosure | settings data | accept | Local-first data unchanged; no new data flows, no PII surface |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan (RESEARCH Package Legitimacy Audit: none checked — nothing installs) |
</threat_model>

<verification>
- Unit: measure-clamp + settingsSchema + mirror + storageFallback + SettingsContext suites green
- E2e: typography-live-apply truthful cells + both polish specs + pagination matrix green on chromium/firefox/webkit
- No WipeConfirm surface for a stored legacy value (unit-asserted, not just inspected)
</verification>

<success_criteria>
- POLISH-09 proven: truthful 64 at far-right, visual + programmatic agreement, both modes, 3 engines
- Legacy readers never see the corrupt path; genuine corruption still does
- Strengthen-only held: only the enumerated pinned cells changed, each with a D21 citation
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-01-SUMMARY.md` when done
</output>
