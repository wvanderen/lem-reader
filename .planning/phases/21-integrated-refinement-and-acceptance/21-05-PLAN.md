---
phase: 21-integrated-refinement-and-acceptance
plan: 05
type: execute
wave: 4
depends_on: ["21-04"]
files_modified:
  - tests/e2e/portability/v21-core-flow-spine.spec.ts
  - src/portability/zipSlip.ts
autonomous: true
requirements: [ACPT-07]
must_haves:
  truths:
    - "One continuous v2.1 journey — seed → organize via views/filters → add content → edit metadata → navigate by TOC → create a cross-block highlight → review it → export with images → wipe → import → byte-equal restoration — passes as a single unbroken test on chromium, firefox, and webkit (D21-13)"
    - "Raw IndexedDB rows (now including asset rows on chromium/firefox) are byte-equal after export → wipe → import (09-06 two-context machinery)"
    - "The re-imported cross-block highlight re-resolves confident through the shipped resolver and its mark renders"
    - "Webkit's image-save cells carry documented engine skips citing the Phase 20 deferred-items ledger; chromium + firefox prove every affected flow (D21-11)"
    - "npm run lint exits 0 with the three zipSlip errors fixed behavior-identically (D21-15)"
  artifacts:
    - path: "tests/e2e/portability/v21-core-flow-spine.spec.ts"
      provides: "The ACPT-07 integrated spine proof (two-context journey, asset-row byte equality)"
      min_lines: 150
  key_links:
    - from: "tests/e2e/portability/v21-core-flow-spine.spec.ts"
      to: "tests/e2e/portability/_portability.ts"
      via: "prepareFreshPage / readRow / readAllRows / seedRows / makeArticle / confidentHighlightOn / buildBundleZip / readBundleJson — reuse, never fork"
      pattern: "_portability"
    - from: "tests/e2e/portability/v21-core-flow-spine.spec.ts"
      to: "tests/e2e/annotations/_fixtures.ts"
      via: "selectRangeBetweenBlocks drives the cross-block highlight arm"
      pattern: "selectRangeBetweenBlocks"
  prohibitions:
    - statement: "The 5 documented webkit Blob→IDB skip sites stay byte-stable; the spine adds its OWN skips in the same documented pattern (D21-11)"
      status: enforced
      verification: "git diff shows the 5 existing sites unchanged; the spine's new skips cite deferred-items.md"
    - statement: "The ACPT-06 template spec (core-flow-spine.spec.ts) stays byte-stable — the v2.1 spine is a NEW file"
      status: enforced
      verification: "git diff shows tests/e2e/portability/core-flow-spine.spec.ts unchanged"
    - statement: "zipSlip edits are behavior-identical; the regression nets stay byte-stable"
      status: enforced
      verification: "tests/unit/portability/zip-slip.test.ts + tests/e2e/portability/zip-slip-regression.spec.ts untouched and green"
    - statement: "No fixed sleeps; helpers imported from non-spec modules only (never from .spec files)"
      status: enforced
      verification: "spec contains no waitForTimeout; imports reference _portability/_fixtures/add-dialog/markdown-payload modules"
---

<objective>
ACPT-07 via ONE INTEGRATED SPINE SPEC (D21-13): a single continuous end-to-end
journey proving the complete v2.1 core flow — organize library, add content,
edit metadata, navigate by TOC, create a cross-block highlight, review it,
export/import with controlled images — without loss across chromium, firefox,
and webkit (webkit image-save cells riding the five documented D21-11 skips).
Plus D21-15: close the three pre-existing zipSlip lint errors so the
milestone's lint gate can go green.

Purpose: "Without loss" is only honest as one unbroken journey over real
machinery — segment proofs can silently drop hand-offs. The ACPT-06 spine is
the shipped template; this is its v2.1 twin.
Output: tests/e2e/portability/v21-core-flow-spine.spec.ts + lint-clean zipSlip.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-CONTEXT.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-RESEARCH.md
@.planning/phases/21-integrated-refinement-and-acceptance/21-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the v2.1 core-flow spine spec (one unbroken journey, two contexts)</name>
  <files>tests/e2e/portability/v21-core-flow-spine.spec.ts</files>
  <read_first>
  - tests/e2e/portability/core-flow-spine.spec.ts (the ACPT-06 template — read IN FULL: two-context skeleton L101-118, paginationDev reader L84-99, import-from-non-spec-helper comment L49-63)
  - tests/e2e/portability/_portability.ts (prepareFreshPage L50, readRow L114, readAllRows L164, seedRows L212, makeArticle L278, confidentHighlightOn L307, buildBundleZip L356, readBundleJson L372)
  - tests/e2e/annotations/_fixtures.ts (selectRangeBetweenBlocks L619, openArticle, switchMode, wipeDatabase, countHighlightsInDexie, announcementRegion)
  - tests/e2e/library/add-dialog.ts (openAddDialog L26, pickSource L39) and tests/e2e/library/markdown-payload.ts (MARKDOWN_WITH_FRONTMATTER)
  - tests/e2e/library/reading-views.spec.ts + tests/e2e/library/search-tag-filter.spec.ts (organize-arm machinery)
  - tests/e2e/library/metadata-edit.spec.ts (metadata-edit precedent) and tests/e2e/toc/toc-navigation.spec.ts (TOC machinery)
  - .planning/phases/20-safe-local-image-fidelity/deferred-items.md (the webkit Blob→IDB ledger the skips cite)
  - 21-RESEARCH.md §Pattern 5 (journey arms, images-arm reality, skip pattern)
  </read_first>
  <action>
  Author tests/e2e/portability/v21-core-flow-spine.spec.ts as ONE test (D21-13), test.setTimeout 90-120s per engine, two isolated browser contexts (machineA/machineB):
  1. Seed machineA (fixtures via seedRows + a REAL ingestion through the Add dialog). The images arm requires ingestion that writes Blob asset rows — fixture-registry images do not travel through export (RESEARCH A4); an EPUB-with-images or file upload through the Add dialog is the network-free path. Pick the fittest shipped payload from add-dialog.ts/markdown-payload.ts machinery.
  2. Journey arms, each driven through the real UI: organize via views/filters (reading-views + search-tag-filter machinery) → add content (openAddDialog/pickSource) → edit metadata (metadata-edit precedent) → navigate by TOC (toc machinery) → create a CROSS-BLOCK highlight via selectRangeBetweenBlocks → review it (route to Highlights, assert the row, jump back to the article, mark renders) → export with images (Settings download capture + Node-side readBundleJson).
  3. Wipe machineB-side, then import the bundle through the real ImportPreviewDialog in machineB's context; assert byte-equal restoration via readAllRows/readRow raw IndexedDB row equality across row kinds — articles, highlights, notes, locations, settings, AND asset rows on chromium/firefox; assert the re-imported cross-block highlight re-resolves confident and renders its mark; assert pagination page-count identity (paginationDev reader) and position restore per the ACPT-06 bar.
  4. Webkit: the image-save cells (ingestion writing Blob rows, asset-row equality) carry test.skip in the documented pattern citing .planning/phases/20-safe-local-image-fidelity/deferred-items.md (D21-11) — chromium + firefox prove every affected flow. All non-image cells run on webkit.
  Zero fixed sleeps; imports from non-spec modules only.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/v21-core-flow-spine.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/portability/v21-core-flow-spine.spec.ts exists, contains exactly one journey test with test.setTimeout, calls browser.newContext() for two machines, and imports selectRangeBetweenBlocks from tests/e2e/annotations/_fixtures
  - It asserts raw-row equality including asset rows on chromium/firefox and contains a webkit test.skip citing deferred-items
  - It contains no waitForTimeout call
  - git diff shows tests/e2e/portability/core-flow-spine.spec.ts and the 5 documented webkit skip sites unchanged
  - The three-engine run exits 0 with the documented webkit skips reported as skipped (not failed)
  </acceptance_criteria>
  <done>ACPT-07 proven as one unbroken no-loss journey across chromium/firefox/webkit, with the webkit image boundary recorded as reduced-gate honesty.</done>
</task>

<task type="auto">
  <name>Task 2: D21-15 zipSlip lint closure — three errors, behavior-identical</name>
  <files>src/portability/zipSlip.ts</files>
  <read_first>
  - src/portability/zipSlip.ts (the three error sites: two control-character regex guards at ~L34 and ~L76, one redundant-escape character class at ~L77)
  - tests/unit/portability/zip-slip.test.ts and tests/e2e/portability/zip-slip-regression.spec.ts (the regression nets — must stay byte-stable and green)
  - 21-RESEARCH.md §Code Examples "zipSlip lint fixes" (the two fix shapes)
  </read_first>
  <action>
  Fix the three pre-existing lint errors (D21-15):
  1. At the two control-character guard sites: add an eslint-disable-next-line no-control-regex comment with a justification line (the control-character escapes ARE the guard's payload — detecting control characters in archive entry names is its function; this is not an accident). One disable per site, directly above each offending line.
  2. At the redundant-escape site: remove the unneeded forward-slash escape inside the character class (a forward-slash requires no escaping in a class) — behavior-identical, no disable comment.
  3. Verify with npx eslint on the file (zero errors) and keep both regression nets byte-stable.
  </action>
  <verify>
    <automated>npx eslint src/portability/zipSlip.ts && npm run test:unit -- --run tests/unit/portability/zip-slip.test.ts && npx playwright test tests/e2e/portability/zip-slip-regression.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - npx eslint src/portability/zipSlip.ts exits 0 with no output
  - The file contains exactly two eslint-disable-next-line no-control-regex comments, each with a justification
  - npm run lint exits 0 repo-wide (the D21-15 milestone-close condition)
  - git diff shows zip-slip.test.ts and zip-slip-regression.spec.ts unchanged; both suites green
  </acceptance_criteria>
  <done>The Phase 9 lint debt is closed: milestone can gate on npm run lint green with the zip-slip guards' behavior provably unchanged.</done>
</task>

</tasks>

## Artifacts this phase-plan produces

- tests/e2e/portability/v21-core-flow-spine.spec.ts (NEW spec — the ACPT-07 instrument)
- Two scoped eslint-disable comments + one escape removal in src/portability/zipSlip.ts (no new symbols)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported bundle → storage | The spine exercises the existing validateBundle/zipSlip defenses end-to-end; no new parsing code ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering | zipSlip L77 escape removal | mitigate | Behavior-identical edit proven by the byte-stable unit + e2e regression nets (RESEARCH V5 row) |
| T-21-11 | Tampering | disable comments masking real violations | mitigate | Disables are line-scoped to the two guards whose function IS control-character detection, each justified in-line; no file-level or broad disables |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed |
</threat_model>

<verification>
- Spine green on 3 engines (webkit image cells skipped-documented)
- zip-slip regression nets green and byte-stable; npm run lint exits 0
</verification>

<success_criteria>
- ACPT-07 closed: the v2.1 core flow proven without loss as one journey on the full engine matrix
- D21-15 closed: lint gate green
</success_criteria>

<output>
Create `.planning/phases/21-integrated-refinement-and-acceptance/21-05-SUMMARY.md` when done
</output>
