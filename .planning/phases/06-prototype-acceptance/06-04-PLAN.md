---
phase: 06-prototype-acceptance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ACCEPTANCE-PROTOCOL.md
autonomous: true
requirements: [ACPT-02]
user_setup: []

must_haves:
  truths:
    - "A versioned ACCEPTANCE-PROTOCOL.md exists in the repo as the durable, re-runnable manual screen-reader acceptance artifact (D6-08)"
    - "The protocol documents the SR matrix = NVDA+Firefox (Windows) + VoiceOver+Safari (macOS), with JAWS recorded as a v1.x coverage boundary, NOT a Phase 6 gate (D6-05)"
    - "The protocol is a hybrid: a scripted checklist for core flows (each step with expected outcome authored as role + accessible name, NOT verbatim SR phrasing) PLUS an exploratory charter for edge scenarios (D6-06, Pitfall 7)"
    - "The protocol states a zero-blocker/major pass policy with a blocker/major/minor severity rubric; minor SR-output quirks are recorded, not blocking (D6-07)"
    - "The protocol is flagged 're-run on material reader-surface changes' (D6-08)"
  artifacts:
    - path: "docs/ACCEPTANCE-PROTOCOL.md"
      provides: "Versioned manual SR + keyboard acceptance protocol — SR matrix, scripted core-flow checklist (role+name expected outcomes), exploratory charter, severity rubric, re-run flag"
      min_lines: 120
  key_links:
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: "tests/e2e/panel-keyboard.spec.ts"
      via: "cross-references the automated keyboard substrate (focus trap/restore, dialog role/name) the manual protocol layers SR verification onto"
      pattern: "panel-keyboard"
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: "tests/e2e/section-announce.spec.ts"
      via: "cross-references the A11Y-08 status-announce automated substrate"
      pattern: "section-announce"
---

<objective>
Author the versioned `docs/ACCEPTANCE-PROTOCOL.md` — the durable, re-runnable manual screen-reader + keyboard acceptance instrument for ACPT-02.

This is the ACPT-02 instrument vertical slice. STACK.md mandates that `@axe-core/playwright` "catches only automatable issues; retain manual keyboard and screen-reader checks" — this protocol is the direct fulfillment of that directive alongside the automated keyboard substrate (`panel-keyboard.spec.ts`, `section-announce.spec.ts`) already shipped in Phase 2. The protocol is authored entirely from the locked decisions D6-05/D6-06/D6-07/D6-08. Plan 06-06 (Wave 3) EXECUTES this protocol on real hardware and records results in 06-VERIFICATION.md.

Purpose: A single canonical artifact future releases can re-run (D6-08), so acceptance is repeatable rather than ad-hoc. It records the SR matrix, the scripted core flows with deterministic expected outcomes, the exploratory charter for real-world usability, and the zero-blocker severity policy.
Output: `docs/ACCEPTANCE-PROTOCOL.md`.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md
@.planning/phases/06-prototype-acceptance/06-PATTERNS.md

# Automated keyboard/SR substrate this protocol layers manual verification onto
@tests/e2e/panel-keyboard.spec.ts
@tests/e2e/section-announce.spec.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/ACCEPTANCE-PROTOCOL.md (versioned manual SR + keyboard protocol)</name>
  <files>docs/ACCEPTANCE-PROTOCOL.md</files>
  <read_first>
    - .planning/phases/06-prototype-acceptance/06-CONTEXT.md (D6-05 SR matrix, D6-06 hybrid protocol shape, D6-07 zero-blocker policy, D6-08 versioned + re-run contract — the locked authority for this doc's content)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (§ Pattern 3 SR-announcement role+name authoring rule + Pitfall 7 verbatim-output anti-pattern + § D6-06/D6-07 resolutions enumerating the 6 scripted flows + severity definitions)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ docs/ACCEPTANCE-PROTOCOL.md — no direct analog; borrow AGENTS.md GSD-managed md structure + fingerprint.json rationale discipline)
    - tests/e2e/panel-keyboard.spec.ts (the automated keyboard substrate: dialog role + accessible name "Reading settings", focus trap/restore — cross-reference so the protocol layers SR verification onto verified automated behavior)
    - tests/e2e/section-announce.spec.ts (the A11Y-08 status-announce automated substrate)
    - AGENTS.md (the repo's process-doc markdown discipline to mirror for header + sectioning)
  </read_first>
  <action>
    Create `docs/ACCEPTANCE-PROTOCOL.md` (the repo has no docs/ dir yet — create it). Author the document from the locked decisions. Required sections:

    1. VERSIONED HEADER (D6-08) — a version line, a generated/last-run date field, and a prominent flag: "Re-run on material reader-surface changes." State that this is the durable, re-runnable SR + keyboard acceptance instrument for the Lem Reader prototype (engineering acceptance per PROJECT.md, NOT a user study).

    2. SR + KEYBOARD MATRIX (D6-05) — NVDA on Firefox (Windows) and VoiceOver on Safari (macOS). Explicitly record JAWS as a v1.x stretch/coverage boundary (licensed, costly to run on dev hardware), NOT a Phase 6 gate. Note the 3 Playwright engines (chromium/firefox/webkit) already cover the AUTOMATED cross-engine surface for ACPT-01.

    3. SCRIPTED CORE-FLOW CHECKLIST (D6-06) — six core flows, each step with: exact keyboard sequence AND expected outcome authored as ROLE + ACCESSIBLE NAME + STATE (programmatically verifiable), NOT verbatim SR phrasing (Pitfall 7 — SR output varies across versions/voices/settings; verbatim phrasing flakes). The six flows: (a) open article → read end-to-end; (b) switch reading mode (M); (c) create a highlight (select → toolbar → Highlight); (d) view + edit + delete a highlight + note (drawer); (e) navigate from a saved annotation back to its passage (D5-11); (f) adjust settings (typography/theme/measure). Cross-reference the automated substrate (panel-keyboard.spec.ts asserts dialog role + "Reading settings" name; section-announce.spec.ts asserts A11Y-08 status announce) so the manual run layers SR verification onto verified automated behavior.

    4. EXPLORATORY CHARTER (D6-06) — goal-based edge scenarios for both SR pairings: e.g. "complete the full reading + annotation loop using only the screen reader; note anything confusing, lost, or unreachable"; "navigate every fixture end-to-end in both modes"; "trigger a pagination fallback and confirm the reader is oriented." Charter catches real-world usability the scripted checklist misses.

    5. SEVERITY RUBRIC (D6-07) — blocker / major / minor definitions. State the PASS POLICY explicitly: a pass = ZERO blocker AND ZERO major issues; every documented flow is completable using only SR/keyboard and no content or required function is lost or unreachable. Minor SR-output quirks (announcement phrasing differences across SR versions/settings) are RECORDED with severity, NOT blocking. Default boundary case: confusing-but-completable announcement = minor unless the reader cannot complete the step or loses content/function (then major/blocker).

    Author as GSD-managed markdown mirroring AGENTS.md header/section discipline. Reference 06-VERIFICATION.md as where run results are recorded.
  </action>
  <verify>
    <automated>test -f docs/ACCEPTANCE-PROTOCOL.md && grep -iq 'NVDA' docs/ACCEPTANCE-PROTOCOL.md && grep -iq 'VoiceOver' docs/ACCEPTANCE-PROTOCOL.md && grep -iq 'zero.blocker\|zero blocker' docs/ACCEPTANCE-PROTOCOL.md && grep -iq 're-run' docs/ACCEPTANCE-PROTOCOL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/ACCEPTANCE-PROTOCOL.md exists (the docs/ directory is created)
    - The doc mentions NVDA and VoiceOver (the D6-05 SR matrix) — grep -iq confirms both
    - The doc records JAWS as a v1.x coverage boundary (NOT a Phase 6 gate) — grep confirms JAWS
    - The doc contains BOTH a scripted checklist (6 core flows) AND an exploratory charter — the hybrid D6-06 shape
    - The scripted-flow expected outcomes are authored as role + accessible name (cross-reference panel-keyboard.spec.ts), NOT verbatim SR phrasing — a note explains the role+name authoring rule (Pitfall 7)
    - The doc states the zero-blocker/major pass policy (D6-07) with blocker/major/minor definitions — grep confirms "zero blocker" or equivalent
    - The doc carries a versioned header with a "re-run on material reader-surface changes" flag (D6-08) — grep confirms re-run
  </acceptance_criteria>
  <done>docs/ACCEPTANCE-PROTOCOL.md exists as the versioned, re-runnable manual SR + keyboard acceptance instrument, authored entirely from D6-05/D6-06/D6-07/D6-08, cross-referencing the automated keyboard substrate.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-04)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| Versioned acceptance protocol | `docs/ACCEPTANCE-PROTOCOL.md` | manual process doc — SR matrix (NVDA+Firefox, VoiceOver+Safari), 6 scripted core flows (role+name outcomes), exploratory charter, severity rubric, re-run flag |

No production code is modified. No new packages. The new `docs/` directory is created. Plan 06-06 executes this protocol on real hardware and records results.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only artifact; no code, no boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | SR-flow gap hides an inaccessible function | mitigate | The hybrid protocol (scripted checklist + exploratory charter) on two SR ecosystems (NVDA+Firefox, VoiceOver+Safari) covers the automatable-axe-blind surface; the zero-blocker policy ensures no inaccessible function passes acceptance |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-04 installs ZERO packages; no install task |
</threat_model>

<verification>
- docs/ACCEPTANCE-PROTOCOL.md exists and passes all acceptance-criterion greps
- The doc is well-formed markdown (no broken structure)
</verification>

<success_criteria>
- ACPT-02 has its durable instrument: a versioned, re-runnable manual SR + keyboard protocol authored from the locked decisions, ready for Plan 06-06 to execute on NVDA+Firefox + VoiceOver+Safari
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-04-SUMMARY.md` when done
</output>
