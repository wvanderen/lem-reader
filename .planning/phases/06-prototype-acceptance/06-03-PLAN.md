---
phase: 06-prototype-acceptance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/perf/perf.harness.spec.ts
  - tests/e2e/perf/budget.compare.ts
  - tests/e2e/perf/budget.json
  - playwright.config.ts
  - package.json
autonomous: false
requirements: [ACPT-04]
user_setup: []

must_haves:
  truths:
    - "Cold and warm repagination on the worst-case fixtures (essay-long-form text + list-reference/technical-post structure) is measured at p95 across desktop + throttled-mobile-chromium profiles × 3 engines (D6-01, D6-02)"
    - "An explicit cold + warm wall-clock budget contract exists in budget.json, with numeric thresholds set at measured p95 + headroom and APPROVED by the user before they are enforced (D6-01)"
    - "The fallback scrolling commit lands within the SAME warm budget as a normal repagination — one gate, no separate looser fallback threshold (D6-03)"
    - "`npm run perf` runs the perf harness and a Node compare gate that exits 1 on regression (mirrors the calibration CI gate exactly) and exits 0 when measurements are within the committed budget (D6-04)"
    - "The throttled-mobile Playwright project is chromium-gated (CDP CPU/network throttle is chromium-only) and scoped via testMatch so it does NOT multiply the full suite (Pitfall 5)"
  artifacts:
    - path: "tests/e2e/perf/perf.harness.spec.ts"
      provides: "ACPT-04 perf measurement harness — measures cold/warm repagination wall-clock per fixture × profile × engine; writes per-engine temp files in afterAll (mirrors calibration.harness.spec.ts)"
      min_lines: 80
    - path: "tests/e2e/perf/budget.compare.ts"
      provides: "ACPT-04 Node CI gate — loads per-engine temp results, refuses empty (exit 2), computes p95, diffs against budget.json, process.exit(1) on regression (mirrors fingerprint.compare.ts)"
      contains: "process.exit(1)"
    - path: "tests/e2e/perf/budget.json"
      provides: "Committed budget contract (schemaVersion, toleranceBound/headroom, rationale, per-fixture/profile/engine/phase thresholds) — committed as PLACEHOLDER on Wave 1; numbers locked after measurement + user approval"
      contains: "schemaVersion"
    - path: "playwright.config.ts"
      provides: "Extended project matrix — adds chromium-throttled-mobile project (CDP CPU+network throttle), testMatch-scoped to perf harness only"
    - path: "package.json"
      provides: "Adds the perf script mirroring calibrate: playwright test perf.harness && node tests/e2e/perf/budget.compare.ts"
      contains: "perf"
  key_links:
    - from: "tests/e2e/perf/budget.compare.ts"
      to: "tests/e2e/calibration/fingerprint.compare.ts"
      via: "structural mirror — per-engine temp-file load → refuse-empty (exit 2) → aggregate → write committed artifact → diff → process.exit(1) on regression"
      pattern: "process\\.exit\\(1\\)"
    - from: "tests/e2e/perf/perf.harness.spec.ts"
      to: "src/measurement/useMeasurement.ts"
      via: "reads DEV-only window.__lemLastTrustedConstraints writes (cold = first write, warm = subsequent) — NO production timing probes added"
      pattern: "__lemLastTrustedConstraints"
    - from: "package.json (perf script)"
      to: "tests/e2e/perf/budget.compare.ts"
      via: "npm run perf = playwright test perf.harness && node budget.compare.ts — mirrors npm run calibrate"
      pattern: "perf"
---

<objective>
Build the ACPT-04 performance harness + CI regression gate, then MEASURE FIRST and lock the budget only after the user approves measured p95 + headroom thresholds (D6-01).

This is the ACPT-04 vertical slice. It produces the measurement harness, the regression gate (mirroring Phase 3's calibration CI gate exactly), the throttled-mobile chromium project, the `npm run perf` script, and — via a blocking checkpoint — the user-approved budget contract. The numeric thresholds are deliberately NOT pre-filled: D6-01 locks the contract (cold + warm wall-clock per repagination; fallback shares warm budget) but defers the NUMBERS to executor measurement + user approval.

Purpose: PROJECT.md's Performance constraint — "Repagination must feel responsive and remain stable after fonts settle" — has no explicit budget until this plan ships one. Phase 3's measure-first discipline (D3-02 derived calibration tolerance empirically) is the precedent: the product does not guess at its own performance envelope.
Output: `tests/e2e/perf/perf.harness.spec.ts`, `tests/e2e/perf/budget.compare.ts`, `tests/e2e/perf/budget.json`, extended `playwright.config.ts`, extended `package.json`, and a user-approved budget.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md
@.planning/phases/06-prototype-acceptance/06-PATTERNS.md

# The CI-gate precedent to mirror EXACTLY
@tests/e2e/calibration/calibration.harness.spec.ts
@tests/e2e/calibration/fingerprint.compare.ts
@calibration/fingerprint.json
# The measurement seam (DEV hooks already present — no new instrumentation)
@src/measurement/useMeasurement.ts
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Perf harness + regression gate + config wiring + placeholder budget</name>
  <files>tests/e2e/perf/perf.harness.spec.ts, tests/e2e/perf/budget.compare.ts, tests/e2e/perf/budget.json, playwright.config.ts, package.json</files>
  <read_first>
    - tests/e2e/calibration/calibration.harness.spec.ts (the EXACT structural template: module-scope engineResults accumulator, per-engine test, afterAll writes .calibration-tmp/<browserName>.json — mirror this for .perf-tmp/)
    - tests/e2e/calibration/fingerprint.compare.ts (the EXACT Node-gate template: loadTempResults → refuse-empty exit 2 → aggregate → writeFileSync committed artifact → diff → process.exit(1) on regression → exit 0 — mirror this skeleton, swap eligibility-regression for p95-exceeds-budget)
    - calibration/fingerprint.json (the committed-artifact schema to mirror: schemaVersion, generatedAt, toleranceBound → headroom tolerance, rationale, engines shape)
    - src/measurement/useMeasurement.ts (confirm window.__lemLastTrustedConstraints is written on every trusted commit at ~L132-136 under import.meta.env.DEV; this is the cold/warm observation seam — cold = first write, warm = subsequent writes around a re-trigger)
    - playwright.config.ts (the existing 3-engine projects array to EXTEND)
    - package.json (the existing calibrate script at L14 to mirror)
    - src/fixtures/index.ts (confirm the corpus ids; worst-case targets per finding 3 = essay-long-form + list-reference/technical-post, NOT figure-heavy)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (findings 3/4/5 + Pitfalls 4/5 + § Pattern 1 + § Code Examples D6-04)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ perf.harness.spec.ts, § budget.compare.ts, § budget.json, § playwright.config.ts, § package.json assignments)
  </read_first>
  <action>
    Build the five files. Mirror the calibration CI-gate pattern EXACTLY (RESEARCH § Don't Hand-Roll — forking it adds risk for no gain).

    1) `tests/e2e/perf/perf.harness.spec.ts` — mirror calibration.harness.spec.ts structure: a module-scope `engineResults` accumulator per worker; a `test(...)` that measures cold + warm repagination wall-clock for the WORST-CASE fixtures (essay-long-form for text, list-reference + technical-post for structure — NOT figure-heavy, which is stubbed to 1×1 SVG and has the smallest normalized text per finding 3). Measure cold = first `window.__lemLastTrustedConstraints` write (page open → first trusted commit) and warm = subsequent writes around a re-trigger (e.g. a viewport or typography re-measure). Read the DEV hook via `page.evaluate(() => window.__lemLastTrustedConstraints)` — do NOT add timing probes to ArticleView.tsx or engine.ts (D3-04 invisible-by-default; the hooks already exist per finding 5). Accumulate samples shaped { fixture, profile: "desktop"|"throttled-mobile", engine, phase: "cold"|"warm", wallClockMs }. In `test.afterAll`, if engineResults is non-empty, mkdirSync a `.perf-tmp` dir and writeFileSync `<browserName>.json`. Apply the V7 pageerror guard (collect pageerrors, the harness must not surface reader-visible errors).

    2) `tests/e2e/perf/budget.compare.ts` — mirror fingerprint.compare.ts skeleton EXACTLY: `loadTempResults()` from `.perf-tmp/` (return [] if dir missing); if empty → `console.error` + `process.exit(2)` (refuse to overwrite a real budget with empty data); compute p95 per (fixture, profile, engine, phase); load committed `budget.json`; if no committed budget yet (placeholder/first run) → log and `process.exit(0)` (the first-run-no-budget path mirrors fingerprint.compare.ts); else diff measured p95 against budget → if any exceeds budget beyond the headroom tolerance → `console.error` regression list + `process.exit(1)` (THE CI GATE); else `process.exit(0)`. Call `main()` at the bottom.

    3) `tests/e2e/perf/budget.json` — commit as a PLACEHOLDER mirroring fingerprint.json schema (schemaVersion: 1, generatedAt, toleranceBound: { headroomPct: ... } expressed as a contract shape, rationale: "budget not yet measured — thresholds locked after D6-01 measurement + user approval", engines: {}). Do NOT invent numeric thresholds (D6-01 / Pitfall 4 — the numbers come from Task 2's measurement + the user's approval).

    4) `playwright.config.ts` — EXTEND the projects array with a `chromium-throttled-mobile` project. CRITICAL: scope it via `testMatch: /perf\.harness/` (or a perf-only testDir) so ONLY the perf harness runs under it — otherwise the full `npm run test` suite multiplies (every test re-runs under the throttled project). The project applies chromium-only CPU + network throttle via CDP (`context.newCDPSession(page)` → `Emulation.setCPUThrottlingRate` + `Network.emulateNetworkConditions`) — VERIFY these CDP commands are available in Playwright 1.61.1 (research assumption A1); if unavailable, apply throttle inside the harness via `page.context().newCDPSession(page)`. Gate the project to chromium only (firefox/webkit silently ignore CDP throttle → misleadingly-fast numbers, Pitfall 5). Keep the existing chromium/firefox/webkit desktop projects unchanged. Use the recommended 4× CPU + Slow 3G starting point (research Open Question 1) and document the choice.

    5) `package.json` — add `"perf": "playwright test perf.harness && node tests/e2e/perf/budget.compare.ts"` mirroring the `calibrate` script at L14 EXACTLY. Do NOT wire `perf` into the default `test` script (it is a separate CI/release gate, like calibrate is). No new dependencies.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node tests/e2e/perf/budget.compare.ts; echo "compare-exit=$?"; grep -q '"perf"' package.json && grep -q 'chromium-throttled-mobile' playwright.config.ts && grep -q 'testMatch' playwright.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/perf/perf.harness.spec.ts, budget.compare.ts, budget.json all exist
    - budget.compare.ts contains process.exit(1) (the regression gate) AND process.exit(2) (the refuse-empty guard) — grep confirms both
    - budget.json is a placeholder with NO locked numeric thresholds (rationale states measurement pending) — grep for a rationale string indicating D6-01 measurement pending
    - playwright.config.ts contains a `chromium-throttled-mobile` project with a `testMatch` scoping it to perf specs only (grep for chromium-throttled-mobile AND testMatch in the same config)
    - package.json contains a `"perf"` script mirroring calibrate (grep for perf + budget.compare.ts)
    - `node tests/e2e/perf/budget.compare.ts` exits 2 when .perf-tmp is empty (refuse-empty guard), and the harness is NOT multiplied into the full suite (the throttled project is testMatch-scoped)
    - No timing probes are added to src/ (grep src/ for any new perf-measurement code returns nothing — only the pre-existing DEV hooks are used)
  </acceptance_criteria>
  <done>The perf harness, regression gate, placeholder budget, chromium-throttled-mobile project (testMatch-scoped), and npm run perf script all exist and type-check; budget.compare.ts enforces exit 1/2/0 correctly; no production instrumentation added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D6-01 measure-first budget checkpoint (run harness → propose p95+headroom → user approves)</name>
  <action>Measure-first budget approval gate per D6-01. The executor runs `npm run perf`, reads the printed p95 cold/warm tables (per fixture × profile × engine × phase), measures the fallback commit timing to confirm it fits the warm budget (D6-03), and PROPOSES cold + warm thresholds at measured p95 + headroom for the user to approve. This is a blocking checkpoint — PAUSE for user approval before Task 3 commits the locked budget.json. Do not invent numbers; the budget is grounded in observed p95.</action>
  <what-built>
    The ACPT-04 perf harness + Node regression gate (mirroring the calibration CI gate) + chromium-throttled-mobile Playwright project + `npm run perf` script. budget.json is committed as a PLACEHOLDER with no numeric thresholds.
  </what-built>
  <how-to-verify>
    This is the D6-01 MEASURE-FIRST checkpoint. The executor must:
    1. Run `npm run perf` end-to-end (this runs the harness across the worst-case fixtures × desktop + throttled-mobile-chromium × 3 engines, writes .perf-tmp/<engine>.json, then budget.compare.ts merges → computes p95).
    2. Read the p95 cold + warm wall-clock tables the compare script prints (per fixture × profile × engine × phase). Also measure the fallback scrolling commit timing to confirm it fits within the warm budget (D6-03).
    3. PROPOSE a budget at measured p95 + headroom for cold and warm (and confirm fallback shares the warm budget — one gate, D6-03). Present the proposed thresholds with the measured p95 evidence + the chosen headroom rationale (mirrors how Phase 3 derived the calibration tolerance empirically, D3-02).
    4. PAUSE for the user to APPROVE the proposed thresholds.
    After approval, resume so Task 3 can commit the locked budget.json and verify the gate passes.
    Expected outcome: a user-approved set of cold + warm wall-clock budgets grounded in observed p95, committed to budget.json.
  </how-to-verify>
  <resume-signal>Approve the proposed p95+headroom budget thresholds (or request adjustments). Type "approved" with the numbers to lock, or describe desired changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit locked budget + verify the CI gate passes</name>
  <files>tests/e2e/perf/budget.json</files>
  <read_first>
    - tests/e2e/perf/budget.json (the placeholder committed in Task 1)
    - tests/e2e/perf/budget.compare.ts (confirm the diff + exit-code logic)
    - calibration/fingerprint.json (the rationale/generatedAt discipline to mirror in the now-real budget)
  </read_first>
  <action>
    Apply the user-approved cold + warm thresholds to `tests/e2e/perf/budget.json`: fill the per-fixture/profile/engine/phase wallClockMs thresholds at the approved p95 + headroom values, set generatedAt to the measurement timestamp, and write a rationale citing the measured p95 evidence + the D6-01 measure-first methodology + D6-03 (fallback shares the warm budget). Mirror the fingerprint.json rationale discipline. Then run `npm run perf` end-to-end and confirm it exits 0 (measurements within the now-committed budget). Confirm the fallback-path measurement is within the warm budget (D6-03 — one gate).
  </action>
  <verify>
    <automated>npm run perf; echo "perf-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/perf/budget.json contains user-approved numeric thresholds (grep confirms non-empty engines/thresholds with real wallClockMs numbers, not a placeholder rationale)
    - `npm run perf` exits 0 (the harness measures + the compare gate passes against the committed budget)
    - The committed rationale cites D6-01 (measure-first) + D6-03 (fallback shares warm budget)
  </acceptance_criteria>
  <done>budget.json is locked with user-approved p95+headroom thresholds; `npm run perf` exits 0 against the committed budget; the measure-first checkpoint (D6-01) is complete.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-03)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| Perf measurement harness | `tests/e2e/perf/perf.harness.spec.ts` | Playwright harness — cold/warm wall-clock per fixture × profile × engine, afterAll temp-file write |
| Regression CI gate | `tests/e2e/perf/budget.compare.ts` | Node CLI — loadTempResults → refuse-empty (exit 2) → p95 → diff → process.exit(1) on regression |
| Budget contract | `tests/e2e/perf/budget.json` | committed JSON — placeholder on Wave 1, locked with user-approved numbers in Task 3 |
| Throttled-mobile project | `playwright.config.ts` (extended) | `chromium-throttled-mobile` project, testMatch-scoped, CDP CPU+network throttle |
| Perf script | `package.json` (extended) | `npm run perf` mirroring `npm run calibrate` |

No production code is modified (the DEV hooks `__lemLastTrustedConstraints` / `__lemDiagnosticBus` are pre-existing and DEV-gated). No new packages.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-tier harness + Node gate + config; no production code path or new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | regressed repagination blocks reading | mitigate | The CI gate (budget.compare.ts exit 1 on regression) prevents silent perf regressions between releases; the warm budget covers the "stable after fonts settle" dimension (PROJECT.md Performance constraint); fallback-shares-warm-budget (D6-03) ensures the fallback path is also non-blocking |
| T-06-05 | Tampering | throttled-mobile project multiplies the suite / misleadingly-fast firefox/webkit numbers | mitigate | testMatch-scoping confines the throttled project to perf specs only; chromium-only throttle gate prevents firefox/webkit false confidence (Pitfall 5) |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-03 installs ZERO packages; no install task |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `node tests/e2e/perf/budget.compare.ts` exits 2 on empty .perf-tmp (refuse-empty guard verified)
- `npm run perf` exits 0 after Task 3 commits the approved budget (CI gate passes)
- The throttled-mobile project does not appear in plain `npx playwright test --list` for non-perf specs (testMatch scoping verified)
- The full `npm run test` suite remains green after this plan — verified at Wave merge by Plan 06-06
</verification>

<success_criteria>
- ACPT-04 has an explicit, measured, user-approved cold + warm budget (D6-01 honored — measure-first, not guessed)
- The CI gate enforces it on regression (D6-04), mirroring the calibration gate exactly
- Fallback shares the warm budget — one gate (D6-03)
- The throttled-mobile profile is chromium-gated and does not inflate the full suite
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-03-SUMMARY.md` when done (record the measured p95 tables + the approved budget thresholds as evidence)
</output>
