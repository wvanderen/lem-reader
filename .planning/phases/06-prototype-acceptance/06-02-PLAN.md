---
phase: 06-prototype-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/acceptance/core-reading-flow.spec.ts
autonomous: true
requirements: [ACPT-01]
user_setup: []

must_haves:
  truths:
    - "A reader can complete the full core reading flow (open → read through → switch mode → restore location → create + navigate a highlight) on every one of the 6 corpus fixtures in current Chromium, Firefox, and WebKit without content loss or blocked navigation (D6-13)"
    - "The consolidated spec proves the END-TO-END flow as one acceptance contract per fixture × engine, without duplicating the per-feature PAGE-01/ANNO-01 isolation specs (Pitfall 6)"
  artifacts:
    - path: "tests/e2e/acceptance/core-reading-flow.spec.ts"
      provides: "ACPT-01 consolidated end-to-end core-reading-flow acceptance spec across the 6-fixture corpus × 3 engines"
      min_lines: 90
  key_links:
    - from: "tests/e2e/acceptance/core-reading-flow.spec.ts"
      to: "tests/e2e/annotations/_fixtures.ts"
      via: "imports openArticle, switchMode, selectRangeInBlock, findFirstBlockWithText, announcementRegion, modeToggle, wipeDatabase — reuses existing harness, does not re-derive selectors"
      pattern: "from.*annotations/_fixtures"
    - from: "tests/e2e/acceptance/core-reading-flow.spec.ts"
      to: "tests/e2e/pagination/fixtures-matrix.ts"
      via: "imports FIXTURES — the 6-fixture corpus enumeration (D3-09), one representative typography"
      pattern: "FIXTURES"
---

<objective>
Deliver the ACPT-01 acceptance proof: a consolidated end-to-end core-reading-flow spec across the 6-fixture corpus × 3 engines.

This is the ACPT-01 vertical slice. The substrate (DOC-01 fixture mount, PAGE-01 mode-switch, ANNO-01 highlight capture, STATE-01 location restore) is all built and verified by Phases 1–5. D6-13 requires proving the COMPLETE flow as one acceptance contract per fixture × engine — not re-proving each feature in isolation. This plan adds a single sibling spec (per the D6-13 resolution: a SIBLING, not an extension of open-every-fixture.spec.ts which stays the DOC-01 mount smoke).

Purpose: ACPT-01 is the headline acceptance requirement — "readers can complete the core reading flow without content loss or blocked navigation." A consolidated spec is the durable acceptance lens that catches cross-feature regressions the per-feature specs cannot.
Output: `tests/e2e/acceptance/core-reading-flow.spec.ts`.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md
@.planning/phases/06-prototype-acceptance/06-PATTERNS.md

# The harness + corpus to reuse wholesale
@tests/e2e/annotations/_fixtures.ts
@tests/e2e/pagination/fixtures-matrix.ts
# The mount smoke this spec SIBLINGS (not replaces)
@tests/e2e/open-every-fixture.spec.ts
# Per-feature proof patterns to reuse (not duplicate isolation logic)
@tests/e2e/pagination/mode-switch-anchor.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACPT-01 consolidated core-reading-flow spec</name>
  <files>tests/e2e/acceptance/core-reading-flow.spec.ts</files>
  <read_first>
    - tests/e2e/annotations/_fixtures.ts (the reusable harness: openArticle, switchMode, selectRangeInBlock, findFirstBlockWithText, announcementRegion, modeToggle, drawerTrigger, wipeDatabase, FIXTURES re-export — reuse VERBATIM, do not re-derive selectors)
    - tests/e2e/pagination/fixtures-matrix.ts (FIXTURES array = the 6-fixture corpus, D3-09; VIEWPORTS; one representative typography for this flow spec — RESEARCH Open Question 2 recommends ONE representative typography, NOT the full 54-cell CORPUS_MATRIX, because typography-stress is already PAGE-03's job)
    - tests/e2e/open-every-fixture.spec.ts (the DOC-01 mount smoke this spec SIBLINGS — iterate fixtures, hash route, h1 sentinel — D6-13 resolved sibling-not-extension)
    - tests/e2e/pagination/mode-switch-anchor.spec.ts (the waitForPagination DEV-hook sentinel pattern: waitForFunction on window.__lemPagination + 600ms settle, and the M-toggle aria-label round-trip assertion)
    - tests/e2e/annotations/capture-highlight.spec.ts (the highlight-creation path: selectRangeInBlock → .selection-toolbar → Highlight button → mark.highlight[data-highlight-id] + announcementRegion contains /Highlight saved/i)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (§ D6-13 resolution + Pitfall 6 — do not duplicate per-feature isolation logic)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ core-reading-flow.spec.ts assignment)
  </read_first>
  <action>
    Create the ACPT-01 consolidated acceptance spec as a SIBLING of open-every-fixture.spec.ts (which stays the DOC-01 mount smoke). Per D6-13, iterate the 6 FIXTURES from fixtures-matrix.ts at ONE representative typography (D-07 default serif/18/64/comfortable) × 6 fixtures × 3 engines = 18 runs (RESEARCH Open Question 2 — do NOT iterate the full CORPUS_MATRIX; typography-stress is PAGE-03's job).

    For each fixture, assert the complete end-to-end flow as ONE acceptance contract:

    1. OPEN — reuse `openArticle(page, fixture.id)` (which navigates the hash route `${BASE}/#/article/<id>`, waits for the h1 visible + the __lemPagination DEV hook + 600ms settle).
    2. READ THROUGH — assert `page.getByRole("article")` visible; assert `page.locator("[data-block-index]").count()` is greater than zero (every block represented; no content loss). Navigate forward through pages (paginated mode is default) via the Space/ArrowRight path or chevron, asserting the page indicator advances — reuse the existing turn controls, do NOT re-prove PAGE-02 in isolation.
    3. SWITCH MODE — reuse `switchMode(page)` (drives the M shortcut per D4-06; asserts the mode-toggle aria-label flips to "Reading mode: scrolling"); assert article + blocks still present in scrolling mode (D4-10 mode-switch anchor — same logical passage).
    4. RESTORE LOCATION — reload the page; assert the resume banner / restored location behavior (STATE-01 substrate); assert article re-mounts without content loss.
    5. CREATE + NAVIGATE HIGHLIGHT — reuse `selectRangeInBlock` + the `.selection-toolbar` → `Highlight` button path → assert `mark.highlight[data-highlight-id]` appears + `announcementRegion` contains /Highlight saved/i (ANNO-01). Then open the annotations drawer (drawerTrigger), activate the navigate-back action, and assert focus lands on the mark (ANNO-04 / D5-11 navigate-back).

    Reuse `wipeDatabase(page)` + the image-stub route from annotations/_fixtures.ts in a beforeEach. This is acceptance CONSOLIDATION using existing selectors and helpers — per Pitfall 6, do NOT re-implement mode-switch/location-restore/highlight-capture logic; assert the flow as one contract. The per-feature PAGE-01/ANNO-01/STATE-01 specs remain authoritative for isolation.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "core-reading-flow" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/acceptance/core-reading-flow.spec.ts exists and `npm run test:e2e -- --grep "core-reading-flow"` exits 0 across chromium, firefox, and webkit
    - The spec iterates the 6 FIXTURES from fixtures-matrix.ts (grep for a `for (const ... of FIXTURES)` loop or equivalent enumeration over all 6)
    - The spec imports the reusable harness from annotations/_fixtures.ts (grep for `from.*annotations/_fixtures`)
    - The spec asserts the full flow: open, read-through (blocks present), switch mode (M + aria-label flip), restore (reload), create highlight (mark.highlight[data-highlight-id] + announcement), navigate back (drawer → focus on mark)
    - The spec uses ONE representative typography (not the full CORPUS_MATRIX) — grep confirms no CORPUS_MATRIX iteration
    - open-every-fixture.spec.ts is NOT modified (this is a sibling, per D6-13)
  </acceptance_criteria>
  <done>core-reading-flow.spec.ts is green across all 3 engines on all 6 fixtures, proving the complete core reading + annotation flow as one acceptance contract without content loss or blocked navigation, reusing the existing harness without duplicating per-feature isolation specs.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-02)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| ACPT-01 consolidated flow spec | `tests/e2e/acceptance/core-reading-flow.spec.ts` | Playwright e2e spec — 6 fixtures × 3 engines, full open→read→switch→restore→highlight+navigate loop |

No production code is modified. No new packages. The new `tests/e2e/acceptance/` directory is created. open-every-fixture.spec.ts stays the DOC-01 mount smoke (untouched).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test-only spec reusing the existing substrate; no production code or new boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | malformed fixture breaks the flow | mitigate (re-verify) | The consolidated spec iterates all 6 fixtures; any malformed fixture (Zod ArticleSchema.parse at module load, Phase 1) would surface as a flow failure here — re-verifies the existing V5 boundary over the full corpus |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-02 installs ZERO packages; no install task |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run test:e2e -- --grep "core-reading-flow"` exits 0 across chromium, firefox, webkit (18 runs: 6 fixtures × 3 engines)
- The full `npm run test` suite remains green after this plan — verified at Wave merge by Plan 06-06
</verification>

<success_criteria>
- ACPT-01 is proven: the complete core reading + annotation flow completes on every corpus fixture in all 3 engines without content loss or blocked navigation
- The spec is a consolidation lens (reuses existing harness/selectors), not a duplication of per-feature isolation specs (Pitfall 6 honored)
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-02-SUMMARY.md` when done
</output>
