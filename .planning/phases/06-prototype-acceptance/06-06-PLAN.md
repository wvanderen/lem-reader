---
phase: 06-prototype-acceptance
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified:
  - .planning/phases/06-prototype-acceptance/06-VERIFICATION.md
autonomous: false
requirements: [ACPT-02]
user_setup:
  - service: "screen-reader acceptance hardware"
    why: "ACPT-02 manual SR matrix requires NVDA on Firefox (Windows) and VoiceOver on Safari (macOS) — automating SR output is brittle and version-coupled (STACK.md: axe catches only automatable issues)"
    env_vars: []
    dashboard_config:
      - task: "Run the NVDA+Firefox scripted checklist + exploratory charter"
        location: "Windows machine/VM with NVDA + Firefox installed (research assumption A4 — confirm availability; if NVDA unavailable, record as a coverage boundary and run VoiceOver+Safari as a reduced gate)"
      - task: "Run the VoiceOver+Safari scripted checklist + exploratory charter"
        location: "macOS (VoiceOver is built-in; Safari is built-in)"

must_haves:
  truths:
    - "The manual SR protocol (docs/ACCEPTANCE-PROTOCOL.md) has been EXECUTED on NVDA+Firefox and VoiceOver+Safari with ZERO blocker and ZERO major findings; minor SR-output quirks are recorded but do not block acceptance (D6-07 backstop)"
    - "The full `npm run test` suite exits 0 with honest pass/fail counts (no subset, no grep, no engine-skip — the Plan 04-11/05-05 precedent), covering unit + e2e across chromium/firefox/webkit"
    - "06-VERIFICATION.md records ACPT-01..04 results: corpus flow, SR protocol run, edge-condition matrix, and the user-approved perf budget sign-off (D6-01/D6-04)"
    - "ACPT-01..04 are flippable to Complete in REQUIREMENTS.md / ROADMAP.md after this gate passes (the v1 milestone's final requirement set)"
  artifacts:
    - path: ".planning/phases/06-prototype-acceptance/06-VERIFICATION.md"
      provides: "Phase 6 acceptance evidence ledger — ACPT-01 corpus-flow results, ACPT-02 manual SR run record (zero-blocker), ACPT-03 edge-condition matrix results, ACPT-04 measured p95 + user-approved budget sign-off + full-suite green exit"
      min_lines: 80
  key_links:
    - from: ".planning/phases/06-prototype-acceptance/06-VERIFICATION.md"
      to: "docs/ACCEPTANCE-PROTOCOL.md"
      via: "records the manual SR protocol execution results (NVDA+Firefox, VoiceOver+Safari, zero-blocker)"
      pattern: "ACCEPTANCE-PROTOCOL"
    - from: ".planning/phases/06-prototype-acceptance/06-VERIFICATION.md"
      to: "tests/e2e/perf/budget.json"
      via: "records the ACPT-04 measured p95 + the user-approved budget sign-off (D6-01)"
      pattern: "budget"
---

<objective>
Execute the ACPT-02 manual screen-reader protocol on real hardware and consolidate all four ACPT results into the phase evidence ledger (06-VERIFICATION.md), then run the honest full-suite acceptance gate.

This is the phase's final vertical slice. Plans 06-01..06-05 produced the automated proof (edge gaps, consolidated corpus flow, perf harness + budget, audited specs) and the SR protocol instrument (Plan 06-04). This plan EXECUTES the manual SR protocol (the one thing automation cannot replace — STACK.md) and records the complete acceptance evidence so ACPT-01..04 can flip to Complete.

Purpose: Acceptance means a real reader can rely on the prototype. The automated specs prove the cross-engine + edge + perf contract; the manual SR run proves the screen-reader experience; 06-VERIFICATION.md is the durable record that ties them together (the same honest-full-suite discipline as Plans 04-11/05-05).
Output: `06-VERIFICATION.md`, plus the executed protocol results.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md

# The protocol instrument produced by Plan 06-04 (EXECUTE this)
@docs/ACCEPTANCE-PROTOCOL.md
# Prior-plan summaries (the evidence to consolidate)
@.planning/phases/06-prototype-acceptance/06-01-SUMMARY.md
@.planning/phases/06-prototype-acceptance/06-02-SUMMARY.md
@.planning/phases/06-prototype-acceptance/06-03-SUMMARY.md
@.planning/phases/06-prototype-acceptance/06-04-SUMMARY.md
@.planning/phases/06-prototype-acceptance/06-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute the ACPT-02 manual SR protocol on NVDA+Firefox + VoiceOver+Safari</name>
  <action>Execute docs/ACCEPTANCE-PROTOCOL.md on real hardware — the ACPT-02 manual gate (D6-05/D6-06/D6-07) that automation cannot replace (STACK.md: axe catches only automatable issues). Run the scripted core-flow checklist (6 flows, role+accessible-name expected outcomes) + the exploratory charter on BOTH NVDA+Firefox (Windows) and VoiceOver+Safari (macOS). Classify findings by the severity rubric; a PASS = zero blocker AND zero major (D6-07). This is a blocking human checkpoint — the executor cannot run screen readers. If NVDA+Windows is unavailable (research assumption A4), record it as a coverage boundary and run VoiceOver+Safari as a reduced gate.</action>
  <what-built>
    The complete automated acceptance substrate (Plans 06-01..06-05): the consolidated ACPT-01 corpus flow, the two NEW ACPT-03 edge specs (high-zoom, font-failure) + four audited edge specs, the ACPT-04 perf harness + CI gate + user-approved budget, and the versioned docs/ACCEPTANCE-PROTOCOL.md (Plan 06-04). The executor cannot run screen readers — this checkpoint is the manual SR matrix the automated suite cannot replace.
  </what-built>
  <how-to-verify>
    Execute docs/ACCEPTANCE-PROTOCOL.md on real hardware — this is the ACPT-02 manual gate (D6-05/D6-06/D6-07):
    1. NVDA on Firefox (Windows): run the 6 scripted core-flow checklist steps (open → read → switch mode → highlight+note CRUD → navigate-back → settings), verifying each expected outcome (role + accessible name). Then run the exploratory charter (complete the full reading + annotation loop using only the screen reader; note anything confusing, lost, or unreachable).
    2. VoiceOver on Safari (macOS): repeat the same scripted checklist + exploratory charter.
    3. Classify every finding by the severity rubric (blocker/major/minor). A PASS = ZERO blocker AND ZERO major (D6-07). Minor SR-output quirks are recorded, not blocking.
    4. Hardware note (research assumption A4): if NVDA+Windows is unavailable, record it as a coverage boundary in 06-VERIFICATION.md and run VoiceOver+Safari as a REDUCED gate (one SR ecosystem instead of two) — defensible but flagged.
    Expected outcome: zero blocker/major findings across both SR pairings, with any minor quirks recorded for the deferred-items list.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded findings (or describe any blocker/major issue that must be fixed before acceptance). If NVDA was unavailable, state so.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Honest full-suite gate + 06-VERIFICATION.md evidence ledger</name>
  <files>.planning/phases/06-prototype-acceptance/06-VERIFICATION.md</files>
  <read_first>
    - docs/ACCEPTANCE-PROTOCOL.md (the executed protocol — record its results)
    - .planning/phases/06-prototype-acceptance/06-01-SUMMARY.md through 06-05-SUMMARY.md (consolidate the automated evidence: corpus flow, edge gaps + audits, perf budget, protocol doc)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (§ Validation Architecture — the 4 success-criteria → validation-ownership table to mirror in the VERIFICATION sections)
    - .planning/REQUIREMENTS.md (ACPT-01..04 definitions + Traceability table — these flip to Complete after this gate)
    - .planning/ROADMAP.md (Phase 6 success criteria 1–4 — the VERIFICATION sections mirror these)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ 06-VERIFICATION.md — required content list)
  </read_first>
  <action>
    First, run the honest full-suite acceptance gate: execute `npm run test` (unit + e2e across chromium/firefox/webkit) in ONE invocation — no subset, no --grep, no engine-skip — and record BOTH the pass AND fail counts honestly (the Plan 04-11/05-05 precedent; fail must be 0). Capture the literal command, per-suite + per-engine counts, and the literal exit code.

    Then author `.planning/phases/06-prototype-acceptance/06-VERIFICATION.md` recording all four ACPT results, mirroring the 4 ROADMAP success criteria and the RESEARCH validation-ownership table:

    1. ACPT-01 result — the `core-reading-flow.spec.ts` results (6 fixtures × 3 engines), citing the Plan 06-02 SUMMARY; assert no content loss or blocked navigation.
    2. ACPT-02 result — the manual SR protocol run record (NVDA+Firefox + VoiceOver+Safari), the zero-blocker finding, the deferred/minor-quirks list, and (if applicable) the NVDA-availability coverage boundary. Reference docs/ACCEPTANCE-PROTOCOL.md.
    3. ACPT-03 result — the high-zoom + font-failure (NEW, Plan 06-01) + audited forced-colors/reduced-motion/reflow/touch-targets (Plan 06-05) results; assert the shared invariant holds across the full condition matrix.
    4. ACPT-04 result — the measured p95 cold/warm tables, the user-approved budget sign-off (from Plan 06-03's checkpoint), the throttled-mobile chromium gate, and the full-device-matrix manual sign-off note (D6-04).
    5. Honest full-suite execution — the literal `npm run test` command, per-suite + per-engine counts, exit 0, and an anti-pattern-guard attestation (the executor ran the suite itself; did not trust any prior SUMMARY; recorded fail=0 honestly rather than omitting it — the 04-11/05-05 discipline).

    Author as GSD-managed markdown. This is the durable acceptance record future releases reference.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -25; echo "test-exit=${PIPESTATUS[0]}"; test -f .planning/phases/06-prototype-acceptance/06-VERIFICATION.md && grep -iq 'ACPT-01' .planning/phases/06-prototype-acceptance/06-VERIFICATION.md && grep -iq 'ACPT-04' .planning/phases/06-prototype-acceptance/06-VERIFICATION.md && grep -iq 'zero.blocker\|zero blocker' .planning/phases/06-prototype-acceptance/06-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 with fail=0 recorded honestly in 06-VERIFICATION.md (per-suite + per-engine counts present; no subset/grep/engine-skip)
    - .planning/phases/06-prototype-acceptance/06-VERIFICATION.md exists and records all four ACPT results (grep confirms ACPT-01, ACPT-02, ACPT-03, ACPT-04 sections)
    - The VERIFICATION references docs/ACCEPTANCE-PROTOCOL.md and records the zero-blocker SR finding (grep confirms ACCEPTANCE-PROTOCOL and "zero blocker")
    - The VERIFICATION records the ACPT-04 measured p95 + user-approved budget sign-off (grep confirms budget)
    - The VERIFICATION includes the anti-pattern-guard attestation (executor ran the full suite itself; fail=0 honest)
  </acceptance_criteria>
  <done>06-VERIFICATION.md is the complete phase-6 acceptance evidence ledger: full `npm run test` exit 0 with honest counts, ACPT-01..04 results consolidated, manual SR zero-blocker recorded, perf budget sign-off recorded. ACPT-01..04 are ready to flip to Complete.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-06)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| Phase acceptance evidence ledger | `.planning/phases/06-prototype-acceptance/06-VERIFICATION.md` | evidence record — ACPT-01..04 results, honest full-suite exit 0, SR zero-blocker, perf budget sign-off |

No production code is modified. No new packages. This plan consumes the protocol doc (Plan 06-04) + all prior SUMMARYs and produces the final evidence record.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Evidence-ledger + manual-run record; no production code or new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Repudiation | acceptance claimed without honest evidence | mitigate | 06-VERIFICATION.md records the literal full-suite command + per-engine counts + exit code + an anti-pattern-guard attestation (the 04-11/05-05 honest-counts discipline); the manual SR run is a blocking checkpoint with recorded findings |
| T-06-09 | Spoofing | hidden SR-inaccessible function passes acceptance | mitigate | the manual SR protocol (D6-06 hybrid: scripted + exploratory) on two SR ecosystems is the automated-axe-blind backstop; zero-blocker policy (D6-07) ensures none pass |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-06 installs ZERO packages; no install task |
</threat_model>

<verification>
- `npm run test` exits 0 (full suite, unit + e2e, 3 engines, fail=0)
- 06-VERIFICATION.md exists with all four ACPT sections + the honest-counts record
- The manual SR checkpoint produced a zero-blocker (or a documented coverage boundary) finding
</verification>

<success_criteria>
- ACPT-02 is proven: the manual SR protocol executes on NVDA+Firefox + VoiceOver+Safari with zero blocker/major findings
- The full suite is honestly green (fail=0)
- 06-VERIFICATION.md consolidates all four ACPT results + the perf budget sign-off
- The v1 milestone's final requirement set (ACPT-01..04) is ready to flip to Complete
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-06-SUMMARY.md` when done
</output>
