---
phase: 06-prototype-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/_edge-invariant.ts
  - tests/e2e/high-zoom.spec.ts
  - tests/e2e/font-failure.spec.ts
autonomous: true
requirements: [ACPT-03]
user_setup: []

must_haves:
  truths:
    - "Under 400% zoom + 320 CSS px reflow, every fixture's full content is reachable via keyboard in BOTH reading modes across chromium, firefox, and webkit (D6-09a, D6-10)"
    - "Under font block / delay / swap, the reader is never blocked: content stays readable in fallback fonts, last-valid-view is retained (no blank flash), and the stale-epoch guard drops stale results (D6-11, V7)"
    - "No layout overflow clips or overlaps content under high-zoom or font-failure across all 6 fixtures × 3 engines (D6-09c)"
    - "Required functions (settings, mode toggle, annotation shortcuts) remain reachable under every edge condition (D6-09b)"
  artifacts:
    - path: "tests/e2e/_edge-invariant.ts"
      provides: "Shared edge-condition invariant helper (D6-09): exports assertEdgeInvariant(page, {fixture, condition}) asserting (a) full content reachable via keyboard in both modes, (b) required functions reachable, (c) no overflow clips content"
      exports: ["assertEdgeInvariant"]
    - path: "tests/e2e/high-zoom.spec.ts"
      provides: "ACPT-03 high-zoom gap spec — 400% + 320 CSS px reflow across 6 fixtures × 3 engines applying the shared invariant"
      min_lines: 60
    - path: "tests/e2e/font-failure.spec.ts"
      provides: "ACPT-03 font-failure gap spec — block/delay/swap modes via injected @font-face + page.route interception applying the shared invariant + V7 no-error guard"
      min_lines: 80
  key_links:
    - from: "tests/e2e/high-zoom.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant } — applied per fixture × condition"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/font-failure.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant } — applied per block/delay/swap mode"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/font-failure.spec.ts"
      to: "page.addStyleTag + page.route"
      via: "@font-face injection MUST run before route interception so document.fonts.ready becomes genuinely pending (the vacuous-pass trap)"
      pattern: "addStyleTag.*font-face|page\\.route"
---

<objective>
Close the two genuine ACPT-03 coverage gaps (high-zoom and font-failure) and ship the shared edge-condition invariant helper (D6-09) that every edge spec applies uniformly.

This is the foundation vertical slice of ACPT-03: it produces the reusable invariant assertion (`_edge-invariant.ts`) plus the two NEW gap specs that previously did not exist. Plan 06-05 (Wave 2) audits the four existing edge specs against the same invariant.

Purpose: Prove the single accessibility bar ("the reader loses nothing") holds under high-zoom (400% + 320 CSS px reflow) and late/failed font loading (block / delay / swap) — the two edge conditions no spec currently exercises end-to-end.
Output: `tests/e2e/_edge-invariant.ts` (helper), `tests/e2e/high-zoom.spec.ts`, `tests/e2e/font-failure.spec.ts`.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md
@.planning/phases/06-prototype-acceptance/06-PATTERNS.md

# Reusable harness the gap specs import
@tests/e2e/annotations/_fixtures.ts
# Existing edge + measurement specs to mirror
@tests/e2e/reflow.spec.ts
@tests/e2e/forced-colors.spec.ts
@tests/e2e/measurement/stale-drop.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared edge-condition invariant helper (_edge-invariant.ts)</name>
  <files>tests/e2e/_edge-invariant.ts</files>
  <read_first>
    - tests/e2e/annotations/_fixtures.ts (helper-module shape: type-only imports + Page→void exports; also exports openArticle, switchMode, modeToggle, drawerTrigger, announcementRegion, wipeDatabase to re-export)
    - tests/e2e/reflow.spec.ts (the load-bearing overflow check at ~L22-49 — the (c) assertion to lift verbatim: document.body + .article-body scrollWidth ≤ clientWidth + 1)
    - src/routes/ArticleView.tsx (confirm [data-block-index] selector is the 1:1 block↔element mapping; confirm mode toggle + settings button accessible names)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ _edge-invariant.ts assignment + § D6-09 example)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (§ Pattern 2 + § Code Examples D6-09)
  </read_first>
  <action>
    Create a NON-spec helper module (leading underscore so Playwright's testDir glob does not pick it up — mirrors the `_fixtures.ts` convention). Encode the D6-09 invariant as a single exported async function `assertEdgeInvariant(page, { fixture, condition })` that asserts all three clauses:

    (a) Full content reachable via keyboard in BOTH reading modes — assert `page.getByRole("article")` is visible; assert `page.locator("[data-block-index]").count()` is greater than zero (every block represented); Tab through the article and confirm content remains present. Then drive the M shortcut to toggle mode (reuse `switchMode` semantics from annotations/_fixtures.ts) and re-assert article visible + blocks present in the other mode.

    (b) Required functions reachable — assert the settings button is visible (`getByRole("button", { name: /Reading settings/i })`), and that the mode toggle control is present. (H/N annotation shortcuts are exercised interactively in the ACPT-01 consolidated spec; here assert the controls exist and are focusable.)

    (c) No layout overflow clips or overlaps content — lift the overflow check verbatim from reflow.spec.ts: read `document.body.scrollWidth` / `clientWidth` and `.article-body` scrollWidth/clientWidth via page.evaluate, assert scrollWidth ≤ clientWidth + 1 (1px sub-pixel tolerance) for BOTH body and article-body when the article-body element exists.

    Re-export the reusable selectors/helpers from annotations/_fixtures.ts (FIXTURES, openArticle, switchMode, modeToggle, drawerTrigger, announcementRegion, wipeDatabase) so all edge specs import from one place. Use type-only imports for Page/Locator. No DOM emulator logic — this is a Playwright Page assertion helper only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --list 2>&1 | grep -v '_edge-invariant' | head -1 >/dev/null; test -f tests/e2e/_edge-invariant.ts && grep -q 'export async function assertEdgeInvariant' tests/e2e/_edge-invariant.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/_edge-invariant.ts exists and is NOT discovered as a spec (filename leading underscore; `npx playwright test --list` does not enumerate it as a test file)
    - The file exports `assertEdgeInvariant` (an async function taking a Page and a {fixture, condition} object)
    - The helper asserts all three D6-09 clauses: (a) keyboard-reachable content in both modes, (b) required-function controls present, (c) body + article-body scrollWidth ≤ clientWidth + 1
    - `npx tsc --noEmit` exits 0 (no type errors)
    - The helper re-exports FIXTURES + the reusable harness functions from annotations/_fixtures.ts
  </acceptance_criteria>
  <done>_edge-invariant.ts exists, type-checks, exports assertEdgeInvariant implementing all three D6-09 clauses, and is not treated as a Playwright spec.</done>
</task>

<task type="auto">
  <name>Task 2: High-zoom gap spec (400% + 320 CSS px reflow)</name>
  <files>tests/e2e/high-zoom.spec.ts</files>
  <read_first>
    - tests/e2e/_edge-invariant.ts (the helper created in Task 1 — import assertEdgeInvariant + FIXTURES)
    - tests/e2e/reflow.spec.ts (the setViewportSize({width:320,height:800}) beforeEach + overflow pattern — the load-bearing reflow primitive)
    - tests/e2e/forced-colors.spec.ts (the emulateMedia beforeEach pattern)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (findings 1 + 2 + Pitfalls 2/3 — setViewportSize is load-bearing; document.body.style.zoom is secondary/engine-variable; deviceScaleFactor is DPR NOT zoom)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ high-zoom.spec.ts assignment)
  </read_first>
  <action>
    Create the ACPT-03 high-zoom gap spec. D6-10 locks 400% zoom + 320 CSS px reflow. Per research finding 1, Playwright has NO native browser-zoom API, so the LOAD-BEARING reflow assertion is `page.setViewportSize({ width: 320, height: 800 })` (cross-engine, already used by reflow.spec.ts). `document.body.style.zoom = "4"` is a SECONDARY, engine-aware check (chromium yes, firefox 126+, webkit partial) — apply it via page.evaluate but assert only "no content is LOST" rather than exact layout; document the engine variance in a spec comment. Do NOT use `deviceScaleFactor` — it is DPR, not CSS zoom, and does not trigger reflow (Pitfall 2).

    Iterate ALL 6 FIXTURES (not just one) since D6-09 applies the invariant uniformly. For each fixture: set the 320×800 viewport, navigate to the article hash route, settle pagination, then call `assertEdgeInvariant(page, { fixture, condition: "high-zoom-400" })`. Reuse `wipeDatabase` + the image-stub route from annotations/_fixtures.ts in a beforeEach (the canonical harness). Drive both reading modes inside the invariant helper.

    Add a focused test asserting that at 320px width the body has no horizontal overflow (the (c) clause proven directly at the WCAG 1.4.10 reflow target). Run across chromium, firefox, webkit (the locked 3-engine matrix picks it up automatically).
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "high-zoom" --workers=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/high-zoom.spec.ts exists and `npm run test:e2e -- --grep "high-zoom"` exits 0 across chromium, firefox, and webkit
    - The spec iterates all 6 fixtures from FIXTURES (not a single fixture)
    - The load-bearing reflow mechanism is `page.setViewportSize({ width: 320, height: 800 })` (grep for `setViewportSize` with width 320)
    - The spec does NOT rely on `deviceScaleFactor` for zoom (grep confirms no deviceScaleFactor-based zoom assertion)
    - The spec imports and calls `assertEdgeInvariant` from _edge-invariant.ts
    - A spec comment documents that document.body.style.zoom is a secondary engine-variable check and setViewportSize is load-bearing
  </acceptance_criteria>
  <done>high-zoom.spec.ts is green across all 3 engines on all 6 fixtures, asserting the shared invariant at 400% + 320 CSS px reflow per D6-10, with setViewportSize as the cross-engine load-bearing assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Font-failure gap spec (block / delay / swap via injected @font-face)</name>
  <files>tests/e2e/font-failure.spec.ts</files>
  <read_first>
    - tests/e2e/_edge-invariant.ts (import assertEdgeInvariant)
    - tests/e2e/measurement/stale-drop.spec.ts (the DEV-hook observation point: waitForFunction on window.__lemLastTrustedConstraints + the V7 pageerror guard `page.on('pageerror')` then `expect(pageErrors).toEqual([])` + rapid-trigger race shape)
    - tests/e2e/measurement/last-valid-view.spec.ts (last-valid-view retention assertion shape)
    - tests/e2e/open-every-fixture.spec.ts (the image-stub page.route beforeEach harness)
    - src/measurement/useMeasurement.ts (confirm __lemLastTrustedConstraints is written on every trusted commit at ~L132-136 under import.meta.env.DEV)
    - src/settings/tokens.ts (CONFIRM the no-web-fonts finding: FONT_STACKS are OS-installed cascades — no @font-face, no woff URL exists to intercept)
    - .planning/phases/06-prototype-acceptance/06-RESEARCH.md (finding 2 + Pitfall 1 + § Code Examples D6-11)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ font-failure.spec.ts assignment + the CRITICAL GOTCHA)
  </read_first>
  <action>
    Create the ACPT-03 font-failure gap spec implementing D6-11 (block / delay / swap). CRITICAL — Lem Reader loads ZERO web fonts (verified in src/settings/tokens.ts FONT_STACKS). The harness MUST inject a web font FIRST via `page.addStyleTag({ content: "@font-face { font-family: \"TestInjectedFont\"; src: url(\"/test-injected-font.woff2\") format(\"woff2\"); } .article-body, .page-fragment { font-family: \"TestInjectedFont\", var(--font-body); }" })`, THEN `page.route("**/test-injected-font.woff2", handler)` to intercept the injected URL. The addStyleTag must run around navigation so `document.fonts.ready` becomes genuinely pending — calling page.route on the unmodified app intercepts NOTHING and is a vacuous-pass trap (Pitfall 1). Verify the font is actually requested via `page.on('request')`.

    Three test modes on a representative fixture (essay-long-form — text worst case):

    (a) BLOCK — `route.abort()` the injected font; assert h1 + first paragraph stay visible (last-valid-view, no blank flash — PAGE-06), assert `document.fonts.ready` still resolves (blocked font → fallback), apply assertEdgeInvariant, and assert V7 (collect pageerrors, expect empty).

    (b) DELAY — `route.continue()` after a 1500ms setTimeout; assert content visible during the delay (last-valid-view), then assert `__lemLastTrustedConstraints` UPDATES after fonts.ready resolves (re-commit) — reuse the stale-drop.spec.ts waitForFunction + evaluate(__lemLastTrustedConstraints) observation pattern.

    (c) SWAP — fulfill with one font then re-route mid-read; assert the stale-epoch guard drops the stale result (PAGE-07) so only the newer trusted constraints commit — reuse the stale-drop.spec.ts rapid-trigger race shape + __lemLastTrustedConstraints observation.

    Reuse wipeDatabase + image-stub from annotations/_fixtures.ts in a beforeEach. Apply assertEdgeInvariant in each mode (the shared invariant holds under font failure). Run across chromium, firefox, webkit.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "font-failure" --workers=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/font-failure.spec.ts exists and `npm run test:e2e -- --grep "font-failure"` exits 0 across chromium, firefox, and webkit
    - The spec defines all three modes: BLOCK (route.abort), DELAY (route.continue after setTimeout), SWAP (re-route mid-read) — grep for route.abort, route.continue, and a swap/re-route sequence
    - The addStyleTag @font-face injection runs BEFORE the page.route interception (grep for addStyleTag containing font-family TestInjectedFont AND page.route on the injected woff2 URL)
    - The spec does NOT call page.route on a woff2 glob without first injecting the font (no vacuous-pass pattern)
    - DELAY and SWAP modes observe __lemLastTrustedConstraints (grep for __lemLastTrustedConstraints)
    - Each mode asserts V7 (pageerror guard) and calls assertEdgeInvariant
  </acceptance_criteria>
  <done>font-failure.spec.ts is green across all 3 engines with all three modes (block/delay/swap) exercising the real font gate (D3-06), last-valid-view (PAGE-06), and stale-drop (PAGE-07), each asserting the shared invariant + V7 no-error guard.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-01)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| Shared edge-invariant helper | `tests/e2e/_edge-invariant.ts` | exported `assertEdgeInvariant(page, {fixture, condition})` + re-exported harness selectors |
| High-zoom gap spec | `tests/e2e/high-zoom.spec.ts` | Playwright e2e spec (6 fixtures × 3 engines, 400% + 320px) |
| Font-failure gap spec | `tests/e2e/font-failure.spec.ts` | Playwright e2e spec (block/delay/swap via injected @font-face + page.route, 3 engines) |

No production code is modified. No new packages. The injected `@font-face` in font-failure.spec.ts is test-tier only (page.addStyleTag) and never present in production builds (D3-04 invisible-by-default holds).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-harness → app DOM | font-failure.spec.ts injects a `@font-face` via page.addStyleTag and intercepts the request via page.route — test-tier only, never shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | font-failure injected font URL | accept | The injected @font-face + woff2 URL is a test-only fixture; it is never present in production builds (import.meta.env.DEV-adjacent test harness; vite build strips nothing here because it is in a Playwright spec, not src/) |
| T-06-02 | Denial of Service | measurement/font failure blocks reader | mitigate | D6-11 spec directly verifies V7: measurement never throws to the reader; assertEdgeInvariant + the pageerror guard prove last-valid-view retention + no blank flash |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-01 installs ZERO packages (RESEARCH §Package Legitimacy Audit — all deps pre-existing); no install task, no legitimacy gate needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run test:e2e -- --grep "high-zoom"` exits 0 across chromium, firefox, webkit
- `npm run test:e2e -- --grep "font-failure"` exits 0 across chromium, firefox, webkit
- _edge-invariant.ts is not enumerated as a spec by `npx playwright test --list`
- The full `npm run test` suite remains green after this plan (no regressions to existing specs) — verified at the Wave merge by Plan 06-06
</verification>

<success_criteria>
- The two genuine ACPT-03 coverage gaps (high-zoom, font-failure) are closed with real-browser, cross-engine specs
- The shared D6-09 invariant is encoded once as `assertEdgeInvariant` and applied by both new specs
- The font-failure harness correctly exercises the real font pipeline (injection-before-interception; no vacuous pass)
- High-zoom uses setViewportSize({width:320}) as the load-bearing reflow assertion (research finding 1 honored)
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-01-SUMMARY.md` when done
</output>
