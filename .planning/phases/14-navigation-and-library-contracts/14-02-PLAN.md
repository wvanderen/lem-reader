---
phase: 14-navigation-and-library-contracts
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/App.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/app.css
  - tests/component/App.test.tsx
autonomous: true
requirements: [NAV-04, LIB-07, LIB-08]
must_haves:
  truths:
    - "Reader can switch among All, Unread, In progress, and Finished via real shareable URLs (#/, #/unread, #/in-progress, #/finished) whose membership follows the readingState policy (D14-12, LIB-07)"
    - "A view switch replaces the history entry — URL updates, no hashchange fires, Back returns to the previous destination, never an intermediate view (D14-13/D14-14)"
    - "Exactly one switcher link carries aria-current=page inside a labeled nav landmark; the library h1 text stays 'Saved articles' across all views (D14-22/D14-25, D14-04)"
    - "Counts in the switcher labels derive from the same policy functions as row membership, and a book counts as ONE item (D14-23/D14-24 — LIB-08)"
    - "Each view shows its own calm empty state exactly when its membership is zero (D14-26)"
    - "In-app arrival at the library and every view switch focus the 'Saved articles' h1; cold loads and reloads never move focus (D14-01/D14-03/D14-15)"
    - "The library destination sets document.title to 'Saved articles — Lem Reader', constant across views (D14-02/D14-25)"
  artifacts:
    - path: "src/App.tsx"
      provides: "View-segment grammar + replaceState switch wiring + warm-mount threading"
      exports: ["LibraryViewName"]
      contains: "replaceState"
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "ViewSwitcher nav + per-view filtering + counts + empty states + focus/title effects"
      contains: "view-switcher"
    - path: "src/app.css"
      provides: ".view-switcher styles (1100px measure, 44px links, weight+underline current-state)"
      contains: "view-switcher"
  key_links:
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "src/ingestion/library/readingState.ts"
      via: "per-view membership filter + countByState counts in the same render body (D14-20/23/24)"
      pattern: "articleReadingState|bookReadingState|countByState"
    - from: "src/ingestion/library/LibraryView.tsx (switcher onClick)"
      to: "src/App.tsx (switchLibraryView)"
      via: "onSwitchView prop — replaceState + direct setView (no event fires; Pitfall 2)"
      pattern: "onSwitchView"
    - from: "src/App.tsx"
      to: "src/ingestion/library/LibraryView.tsx"
      via: "view + onSwitchView + warmMount props (the hasAppHistory threading precedent)"
      pattern: "warmMount"
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "src/ingestion/library/pageMeta.ts"
      via: "setDocumentTitle('Saved articles') on mount"
      pattern: "setDocumentTitle"
---

<objective>
Make reading-state views real hash routes with truthful switcher UI: parseHash
gains view segments (D14-12/D14-16), the switcher is a nav of real links with
aria-current and counts (D14-22/D14-23), view switches use replaceState + a
direct router update (D14-13), and LibraryView gains per-view filtering,
counts, empty states (D14-26), the library title, and the uniform h1-focus
rule at both trigger points (D14-01/D14-15) with cold-load immunity
(D14-03/D14-17).

Purpose: NAV-04's history behavior + LIB-07's switchable views + LIB-08's
structural count/membership/empty agreement.
Output: Router grammar extension, ViewSwitcher, per-view LibraryView, new CSS
— all on existing surfaces, every byte-stable anchor preserved.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-and-library-contracts/14-CONTEXT.md
@.planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md
@.planning/phases/14-navigation-and-library-contracts/14-PATTERNS.md
@.planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md
@.planning/phases/14-navigation-and-library-contracts/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseHash view-segment grammar + LibraryViewName + strengthen-only App.test.tsx extension</name>
  <files>src/App.tsx, tests/component/App.test.tsx</files>
  <read_first>
  - src/App.tsx (parseHash L43-61 — extend in place, match order preserved; onHash + fragment guard L175-196 — byte-unchanged; View union L38-41; exports L276-277)
  - tests/component/App.test.tsx (parseHash describe L51-102, the beforeEach hash-reset L43-49, fragment-guard describe L104-141 which stays untouched)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Pattern 2 (grammar sketch + D14-16 fallback discipline)
  </read_first>
  <behavior>
  - Test: '#/unread' → { name: "list", view: "unread" }
  - Test: '#/in-progress' → { name: "list", view: "in-progress" }
  - Test: '#/finished' → { name: "list", view: "finished" }
  - Test: '#/' and unknown '#/…' segments (e.g. '#/unknown-view') → { name: "list", view: "all" } (D14-16)
  - Test (existing, realigned): empty hash / bare '#/' / unrecognized-route cases now assert the list shape WITH view "all" — the assertion gains a field, the grammar contract they encode is preserved and extended
  - Test (existing, byte-stable): article, /h/, and '#/review' cases unchanged
  </behavior>
  <action>
  In App.tsx: export type LibraryViewName = the literal union "all" |
  "unread" | "in-progress" | "finished". The View union's list alternative
  becomes { name: "list"; view: LibraryViewName } (D14-12). Extend parseHash
  IN PLACE with match order preserved: the article /h/ regex FIRST
  (byte-stable), then the exact '#/review' equality, then NEW literal
  equality comparisons — hash === '#/unread' returns view unread,
  '#/in-progress' returns in-progress, '#/finished' returns finished — and
  the final fallback returns { name: "list", view: "all" } (D14-16: unknown
  #/ segments fall back to All, mirroring the existing unknown-route → list
  discipline; no new error surface). Literal allowlist only — segments are
  compared against constants, never interpreted (14-RESEARCH V5). The onHash
  listener + fragment guard stay byte-unchanged. Export LibraryViewName
  alongside the existing View/parseHash exports. In App.test.tsx: update the
  three list-fallback cases to include view "all" (necessary realignment of
  the extended shape — the 13-06 stale-expectation precedent; record the
  deviation in the commit body), keep every other existing case
  byte-stable, and ADD the new view-segment cases in the same style inside
  the parseHash describe.
  </action>
  <verify>
    <automated>npx vitest run tests/component/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - npx vitest run tests/component/App.test.tsx exits 0
  - src/App.tsx contains `export type LibraryViewName` and literal comparisons against "#/unread", "#/in-progress", "#/finished"
  - In parseHash, the article regex check and the "#/review" check both precede the view-segment comparisons; the fallback returns view "all"
  - The fragment-guard describe (a '#fn-1' hashchange does NOT unmount…) is untouched (git diff shows zero changes inside it)
  </acceptance_criteria>
  <done>Grammar extended and unit-pinned; unknown segments fall back to All; fragment guard and destination grammar byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: replaceState switch wiring + ViewSwitcher nav + per-view filtering + counts + empty states + CSS</name>
  <files>src/App.tsx, src/ingestion/library/LibraryView.tsx, src/app.css</files>
  <read_first>
  - src/App.tsx (AppInner L142-266: hasAppHistory state L173, onHash L185-196, render threading L247-263 — the threading precedent to copy)
  - src/ingestion/library/LibraryView.tsx (load effect L104-154, book/article partition L162-196, visibleItems/visibleBooks filtering, render L198-303 incl. the empty-state block L258-266)
  - src/ingestion/library/libraryFilter.ts (the pure-filter composition the state filter composes with)
  - src/ingestion/library/readingState.ts (Plan 14-01 API: articleReadingState/bookReadingState/countByState)
  - src/ingestion/library/BookRow.tsx L66-75 (totalsById memo precedent)
  - src/app.css (.library-search / .tag-filter measure rules — the shared-measure pattern to mirror)
  - .planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md (§Component Inventory ViewSwitcher, §Copywriting switcher + empty-state tables, §Layout Contract, §Interaction 5/8/9/10/13)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Pattern 5 + Pitfall 2/4/5
  </read_first>
  <action>
  App.tsx — add switchLibraryView(next: LibraryViewName): a calm early
  return when the current view is already the list with the same view name
  (same-view click is a no-op); otherwise history.replaceState(null, "",
  href) where href is one of the four template constants "#/", "#/unread",
  "#/in-progress", "#/finished" (same-origin by construction), IMMEDIATELY
  followed by the direct setView(parseHash()) call — replaceState fires no
  hashchange and no popstate, so the direct router update is load-bearing
  (D14-13; Pitfall 2 — URL/DOM desync is THE failure mode). Never call
  setHasAppHistory inside it: a view switch is not destination navigation
  and hasAppHistory is not flipped by design (document in a comment;
  Pitfall 4). In the render, thread three new props to LibraryView: view
  (the active LibraryViewName of the list state), onSwitchView
  (switchLibraryView), and warmMount (the hasAppHistory value — false on
  cold loads and reloads by construction, D14-03). Destinations keep push
  semantics: every existing window.location.hash assignment stays
  byte-unchanged.
  LibraryView.tsx — the props interface gains view: LibraryViewName;
  onSwitchView: (next: LibraryViewName) => void; warmMount: boolean. Build
  ONE totalsById Map (useMemo keyed on items identity — the BookRow L66-75
  precedent; per article graphemeClusters(normalizeText(article),
  article.lang).length) and thread totalsById.get as the policy lookup
  (avoids per-row recompute — the 260819-tld lesson). Derive per-item
  states in the render body: standalone articles via
  articleReadingState(locationsByArticle.get(id), totalsById.get(id) ?? 0);
  books via bookReadingState(book, allLocations, totalsById.get). Filter
  both halves by state === active view (view "all" skips the state filter)
  BEFORE the existing filterLibrary/filterBooks query/tag composition
  (filters narrow within the selected view — LIB-09 interplay keeps
  existing behavior). Render the switcher as the FIRST child of
  section.library-section-list, above LibrarySearch: a nav with className
  "view-switcher" and aria-label "Library views" (D14-04/D14-22 — labeled
  because chapter-nav and a Phase 15 app-shell nav exist) containing four
  real links in order — href "#/" text "All", "#/unread" text "Unread",
  "#/in-progress" text "In progress" (sentence case, lowercase p per
  UI-SPEC voice rule), "#/finished" text "Finished". Exactly one link
  carries aria-current="page" (the active view; undefined on the others).
  Counts live in the accessible names ONLY at status ready — link text
  becomes e.g. "Unread (3)" from countByState plus the All count =
  standalone count + book count (D14-23/D14-24); while loading, bare labels
  with no parenthetical (never a zero lie — UI-SPEC loading honesty). Every
  link onClick guards modified clicks: early return when e.defaultPrevented,
  e.button !== 0, or metaKey/ctrlKey/shiftKey/altKey held; otherwise
  e.preventDefault() + onSwitchView(next) — middle/cmd-click falls through
  to native fragment navigation (push + hashchange handled by the existing
  onHash path). Empty states (D14-26): when status is ready AND the view's
  membership count is zero, render the per-view copy INSTEAD of the ul —
  All keeps the byte-stable pair "Your library is empty" / "Paste a URL or
  upload a file to begin."; Unread gets "Nothing unread" / "Everything in
  your library has been started."; In progress gets "Nothing in progress" /
  "Open something unread — it will show up here."; Finished gets "Nothing
  finished yet" / "Read to the end and finished items will collect here."
  When membership is non-empty but query/tag filters hide every row, the ul
  still renders with zero children (a filtered-out view is not an empty
  view; the previous filtered-empty copy render is intentionally
  superseded — nothing pins that copy, verified by repo grep). app.css —
  add a .view-switcher rule block: max-width 1100px + margin var(--space-lg)
  auto 0 (the 13-07 shared-measure precedent), display flex, flex-wrap
  wrap, gap var(--space-sm) (mirrors .tag-filter); link rules: display
  inline-flex, align-items center, min-height var(--touch), padding-inline
  var(--space-xs), font-family var(--font-ui), font-size 14px, line-height
  1.45; a current-state rule scoped to the aria-current attribute inside
  .view-switcher sets font-weight 600 and text-decoration-thickness 2px and
  NOTHING else (never color alone — forced-colors safety, UI-SPEC
  Interaction 13). No color overrides (the global a rule rides), zero
  transition/animation properties (Interaction 12), no new design tokens.
  </action>
  <verify>
    <automated>npx vitest run tests/component/App.test.tsx && npx playwright test tests/e2e/ingestion/happy-path.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/ingestion/happy-path.spec.ts passes UNMODIFIED on chromium (h1, .status, .library-list anchors byte-stable — D14-25)
  - src/ingestion/library/LibraryView.tsx contains `className="view-switcher"`, `aria-label="Library views"`, `aria-current`, and imports articleReadingState/bookReadingState/countByState from "./readingState"
  - src/App.tsx contains a replaceState call and a direct setView(parseHash()) call in the same switch handler; the handler does NOT reference setHasAppHistory
  - src/app.css contains a ".view-switcher" rule (1100px measure, var(--touch) links) and a current-state rule carrying font-weight 600 + text-decoration-thickness 2px
  - `rg -n "location.hash = \"#/(unread|in-progress|finished)\"" src/` returns zero matches (the switcher never pushes view URLs)
  - counts render only when status === "ready" (loading shows bare labels — no parenthetical)
  </acceptance_criteria>
  <done>Views are real routes with a truthful switcher; counts/membership/empty states all derive from the policy module in one render body; history semantics are replace-not-push; all byte-stable anchors green unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: LibraryView title + h1 focus effects (mount warm-gated + view-switch keyed)</name>
  <files>src/ingestion/library/LibraryView.tsx</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (post-Task-2 state: the h1 in the library-header, the new view/warmMount props)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Pattern 1 + Pitfall 3 (the LibraryView-does-NOT-remount fact) + Pitfall 9 (StrictMode)
  - src/ingestion/library/pageMeta.ts (setDocumentTitle)
  </read_first>
  <action>
  The h1 gains tabIndex={-1} and a ref (h1Ref) — text "Saved articles" and
  the level stay byte-stable (D14-25; gains ONLY the attribute + ref). TWO
  effects, per Pitfall 3 (view switches never remount LibraryView, so a
  mount effect alone cannot carry them): (1) a mount effect with empty deps
  that calls setDocumentTitle("Saved articles") — the library title,
  constant across views (D14-02/D14-25) — and then focuses the h1 ONLY when
  the warmMount prop is true (D14-03: cold deep-links and reloads keep
  natural browser focus; D14-08: return-to-library uses the uniform h1
  rule); (2) a view-switch effect keyed on the view prop with a first-run
  skip ref (the first run belongs to the mount effect) that focuses the h1
  on every subsequent view change (D14-15 — one uniform rule, second
  trigger point). focus() is called WITHOUT preventScroll — its default
  scroll-into-view delivers the reset-to-list-top behavior because the h1
  sits at content top (UI-SPEC Interaction 10; no scrollTo choreography).
  Neither effect has a cleanup function — focusing twice is idempotent and
  StrictMode-safe (Pitfall 9). No live-region announcement is added — the
  focused h1 IS the announcement (D14-09).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/ingestion/happy-path.spec.ts tests/e2e/library/search-tag-filter.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - src/ingestion/library/LibraryView.tsx: the h1 element carries tabIndex={-1} and a ref, and its rendered text is exactly "Saved articles"
  - a mount effect calls setDocumentTitle("Saved articles") and gates h1 focus on warmMount; a separate effect keyed on the view prop with first-run skip focuses the h1 on view changes
  - neither effect returns a cleanup function
  - the count of aria-live attributes in LibraryView.tsx is unchanged from pre-phase (no new live region — D14-09)
  - happy-path.spec + search-tag-filter.spec green on chromium (regression; the real 3-engine focus proof is Plan 14-04's documented scope)
  </acceptance_criteria>
  <done>The uniform announcement rule lands at both trigger points with cold-load immunity; the library title is truthful; jsdom-invisible focus timing is explicitly deferred to the Playwright plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser hash → router | untrusted URL fragment enters parseHash |
| switcher click → history | user gesture drives same-origin replaceState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering (URL injection) | parseHash view segments | mitigate | closed literal allowlist (=== against four constants); unknown → All fallback, values never interpolated into DOM/URLs beyond the same constants (T-10-02c discipline) |
| T-14-04 | Spoofing (history hijack) | switchLibraryView replaceState | mitigate | same-origin template-built URLs only (replaceState throws on cross-origin); destinations keep push semantics |
| T-14-05 | Spoofing (focus phishing) | h1 focus on swap | accept | focus moves only to the truthful incoming-destination heading; cold loads never move focus (D14-03) |
| T-14-SC | Tampering (supply chain) | npm installs | accept | zero packages installed this plan |
</threat_model>

<verification>
- npx vitest run tests/component/App.test.tsx — exit 0 (grammar + strengthened cases)
- npx playwright test tests/e2e/ingestion/happy-path.spec.ts tests/e2e/library/search-tag-filter.spec.ts tests/e2e/library/progress-recent.spec.ts --project=chromium — exit 0, all three specs unmodified
- rg -n "FINISHED_THRESHOLD" src/App.tsx src/ingestion/library/LibraryView.tsx — zero matches (views never touch the constant directly; only the policy module does)
</verification>

<success_criteria>
- Views switchable by URL and by switcher across chromium (3-engine proof in 14-04)
- URL ↔ DOM agreement after every switch path (direct click, Back, reload, middle-click)
- Counts, rows, and empty states derive from one policy call-site set — agreement structural (D14-23/24)
- Byte-stable anchors (<h1>Saved articles</h1>, .status, .library-list, fragment guard, back-nav) proven unchanged
</success_criteria>

## Artifacts this phase produces (Plan 14-02)

- `src/App.tsx` — NEW export `LibraryViewName` (type union); `View` list alternative gains the `view` field; NEW internal `switchLibraryView` handler (replaceState + direct setView); LibraryView render call gains `view`/`onSwitchView`/`warmMount` props
- `src/ingestion/library/LibraryView.tsx` — NEW props (`view`, `onSwitchView`, `warmMount`); NEW ViewSwitcher nav block (`.view-switcher`, aria-label "Library views"); NEW totalsById memo, per-view state filtering, countByState counts, per-view empty states; h1 gains tabIndex + ref + the two focus effects + title effect
- `src/app.css` — NEW `.view-switcher` rule block + aria-current current-state rule
- `tests/component/App.test.tsx` — parseHash describe extended (view-segment cases); list-fallback cases realigned to the extended shape

<output>
Create `.planning/phases/14-navigation-and-library-contracts/14-02-SUMMARY.md` when done
</output>
