---
phase: 14-navigation-and-library-contracts
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - tests/e2e/library/reading-views.spec.ts
  - tests/e2e/chrome/back-nav.spec.ts
  - tests/e2e/a11y.spec.ts
autonomous: true
requirements: [NAV-04, LIB-07, LIB-08]
must_haves:
  truths:
    - "Counts in switcher labels, rendered row counts, and per-view empty states agree with the imported policy module's expected values across chromium, firefox, AND webkit (LIB-08 — structural agreement, not copy-checked)"
    - "Reading-state views are reachable by URL, survive reload, and unknown #/ segments fall back to All (D14-12/D14-16/D14-17 — LIB-07)"
    - "Focus identity, history semantics (replace-not-push), and URL↔DOM agreement hold in all three engines (NAV-04)"
    - "The honesty rows hold in real browsers: a 39-of-40-chapter book and a missing-chapter-row book render In progress, never Finished (D14-19/D14-21 — held-out checks against seeded raw IndexedDB rows)"
    - "The honest full suite exits 0 with every byte-stable anchor spec proven unmodified"
  artifacts:
    - path: "tests/e2e/library/reading-views.spec.ts"
      provides: "The 3-engine views/counts/empty/focus/title/history matrix + seedBook/seedArticleRows helpers"
      contains: "readingState"
    - path: "tests/e2e/chrome/back-nav.spec.ts"
      provides: "Strengthen-only view-route Back-semantics case"
  key_links:
    - from: "tests/e2e/library/reading-views.spec.ts"
      to: "src/ingestion/library/readingState.ts"
      via: "expected membership/counts computed in Node from the SAME functions the app renders (LIB-08 structural lock)"
      pattern: "from \"../../../src/ingestion/library/readingState\""
    - from: "tests/e2e/library/reading-views.spec.ts"
      to: "src/persistence/db.ts (store names)"
      via: "seedBook/seedArticleRows/seedLocation raw IndexedDB puts into the v5 articles/books/location stores"
      pattern: "objectStore\\(\"books\"\\)"
---

<objective>
Prove Phase 14 in real browsers: a new 3-engine Playwright matrix
(reading-views.spec.ts) covering view routes, count/row/empty agreement
against the imported policy module, focus identity (cold vs warm vs
deep-link vs restore), per-destination titles (incl. EPUB chapter + error +
review + overlay stability), and replaceState history semantics; a strengthen-only
back-nav extension for view-route Back behavior; and the honest full-suite
phase gate (npm run test exit 0, counts recorded, byte-stable anchors
audited unmodified).

Purpose: NAV-04/LIB-07/LIB-08 browser truth — focus, history, and scroll are
Playwright-owned per STACK.md ("DOM emulators are not authoritative",
14-RESEARCH Pitfall 7).
Output: One new spec, one strengthened spec, an a11y coverage check, and the
phase gate record.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-and-library-contracts/14-CONTEXT.md
@.planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md
@.planning/phases/14-navigation-and-library-contracts/14-PATTERNS.md
@.planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md
@.planning/phases/14-navigation-and-library-contracts/14-VALIDATION.md
@.planning/phases/14-navigation-and-library-contracts/14-01-SUMMARY.md
@.planning/phases/14-navigation-and-library-contracts/14-02-SUMMARY.md
@.planning/phases/14-navigation-and-library-contracts/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reading-views.spec.ts — harness + views/counts/empty/membership agreement matrix</name>
  <files>tests/e2e/library/reading-views.spec.ts</files>
  <read_first>
  - tests/e2e/library/progress-recent.spec.ts (beforeEach L145-188 to clone: image route stub + clear-rows discipline; seedLocation L61-100; openLibrary L120-143 — seed BEFORE open or reload; the integer-truncation trap comment L238-241)
  - src/persistence/db.ts L189-198 (v5 store names: articles "id, revision, source, addedAt, *tags, bookId"; books "id, title, *tags"; location "[articleId+revision]")
  - src/ingestion/library/readingState.ts (the module imported for expected values)
  - tests/unit/library/book-progress.test.ts (BookSchema-valid row builders to mirror for seed payloads)
  - src/content/normalizeText.ts exports (normalizeText + graphemeClusters — compute per-article totals in Node for textLengthOf parity with the app)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Code Examples (seeding + agreement assertion shapes)
  </read_first>
  <action>
  Create tests/e2e/library/reading-views.spec.ts with BASE =
  http://localhost:5173 (the progress-recent convention). Clone the
  progress-recent beforeEach wholesale (image stub; goto BASE; wait for the
  "Saved articles" h1; raw IndexedDB clear-rows) with ONE change: ADD
  "books" to the cleared store list (the list predates the v5 store).
  Clear-rows, never deleteDatabase (the webkit race). Helpers: clone
  seedLocation verbatim; NEW seedBook writing a BookSchema-valid row
  (build the plain object in Node via BookSchema.parse — id, title, authors,
  tags, chapterArticleIds, addedAt, publishedDate, originalFileHash — then
  page.evaluate a raw put into the books store); NEW seedArticleRows writing
  ArticleSchema-valid article rows where chapter rows carry BOTH
  ingestionMeta.bookId AND the top-level bookId (the 12-03 v5 index
  contract). Schema-building in Node guarantees the store-seam Zod read
  never drops a seeded row. Corpus (on top of the six bundled fixture
  articles, which are always present and unread on a cleared location
  store): standalone — one unread (no location), one in-progress
  (graphemeOffset a mid fraction of its total), one finished
  (graphemeOffset = the article's FULL total — never a floored threshold
  multiple, the documented trap); books — one with every chapter finished
  (each chapter seeded at its own full total), one with N-1 of N chapters
  finished (e.g. 3 of 4 — the D14-19 honesty row), one with no locations
  (unread), and one whose chapterArticleIds includes an id with NO seeded
  article row while all present chapters are finished (the D14-21
  missing-row honesty row). Agreement assertions: import articleReadingState
  + bookReadingState from ../../../src/ingestion/library/readingState and
  compute expected per-view counts in Node over the seed corpus PLUS the
  bundled fixtures (per-article totals computed with normalizeText +
  graphemeClusters over the same row objects — textLengthOf parity). For
  each of the four view URLs (#/, #/unread, #/in-progress, #/finished):
  assert the switcher link's accessible name matches the expected count
  exactly (getByRole link with name like "Unread (7)"), assert
  .library-list > li count equals the expected rows for that view (a book
  row is ONE li — D14-24), and assert the per-view empty-state heading
  appears exactly when the expected count is zero. Additional route cases:
  unknown segment #/bogus-view lands on All with aria-current on the All
  link (D14-16); page.reload() on #/finished stays Finished with
  aria-current on the Finished link (D14-17). Use plain test() blocks —
  the 3-engine matrix inherits by default.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/library/reading-views.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - npx playwright test tests/e2e/library/reading-views.spec.ts --project=chromium exits 0; the full spec is green on chromium+firefox+webkit at wave merge
  - the spec imports from "../../../src/ingestion/library/readingState" (agreement is structural) and from src/content/normalizeText
  - the beforeEach clears the books store; every Finished seed uses graphemeOffset equal to the article's full total (no floored-threshold seeds anywhere in the file)
  - the 39-of-40-style and missing-chapter-row books assert IN PROGRESS membership (visible under #/in-progress, absent from #/finished) — the honesty rows are held-out checks, not inferred from unit tests
  </acceptance_criteria>
  <done>LIB-07/LIB-08 browser truth pinned: routes, counts, rows, and empty states agree with the one policy module across all three engines.</done>
</task>

<task type="auto">
  <name>Task 2: focus/title/history matrix + back-nav strengthen-only extension</name>
  <files>tests/e2e/library/reading-views.spec.ts, tests/e2e/chrome/back-nav.spec.ts</files>
  <read_first>
  - tests/e2e/library/reading-views.spec.ts (Task 1 harness + corpus)
  - tests/e2e/chrome/back-nav.spec.ts (the backToLibrary assertion shape L94-96 + its helpers — clone, extend, never weaken)
  - tests/e2e/review-panel/jump-bidirectional.spec.ts (the existing #/article/<id>/h/<hl> deep-link seeding to reuse)
  - src/reader/Header.tsx (settings trigger accessible name "Reading settings" — for the overlay-stability case)
  - src/ingestion/library/LibraryView.tsx L216-224 (the "Review highlights" header button — the in-app #/review entry for the review-destination case)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Code Examples (verified assertion APIs: toBeFocused auto-retry, toHaveTitle, toHaveURL)
  </read_first>
  <action>
  Add to reading-views.spec.ts (each as its own test, using the Task 1
  corpus): COLD LOAD — goto BASE + "#/finished": the h1 is NOT focused
  (D14-03/D14-17), toHaveTitle matches /Saved articles — Lem Reader/, and
  aria-current sits on the Finished link. IN-APP SWAP — from #/, activate an
  Open article link: the article h1 becomes focused (toBeFocused) and
  toHaveTitle matches the article title + suffix; page.goBack(): the library
  h1 is focused (warm remount — D14-08/D14-15) and the title returns to
  /Saved articles — Lem Reader/. VIEW SWITCH — click the Unread link:
  toHaveURL matches /#\/unread$/, aria-current moves to Unread, the library
  h1 is focused (D14-15), and the title is unchanged (constant across
  views). HISTORY — from #/ click Unread then Finished (two switches), open
  an article, page.goBack(): the app lands on /#\/finished/ — Back returned
  to the ORIGINATING view, never an intermediate view entry (D14-13/D14-14).
  DEEP LINK — reuse the jump-bidirectional seeding to open
  #/article/<id>/h/<hl>: the hl-<id> mark is focused and the article h1 is
  NOT (D14-05). RESTORE BEATS H1 — in-app open of the seeded mid-article
  standalone article: the article h1 is NOT focused and the resume banner is
  visible (D14-10). OVERLAY STABILITY — open the settings panel via the
  header button (accessible name "Reading settings") and close it:
  toHaveTitle is unchanged before/after (D14-11). ERROR PARITY — from an
  in-app surface, navigate the hash to #/article/<nonexistent-id>: the error
  h1 is focused and toHaveTitle matches /Couldn't open this article — Lem
  Reader/ (D14-06). EPUB CHAPTER TITLE — open a seeded chapter: toHaveTitle
   matches /<chapter title> — <book title> — Lem Reader/ (D14-07). REVIEW
   DESTINATION — from #/, activate the library header's Review highlights
   button (getByRole button — the in-app #/review entry, LibraryView L216-224;
   its hash assignment pushes + fires hashchange, so the ReviewView mount is
   warm): toHaveTitle matches /Review highlights — Lem Reader/ and the review
   h1 is focused (toBeFocused) (D14-01/D14-02 — the review destination's title
   and warm h1 focus are otherwise regression-untested, since 14-03 Task 1's
   verify is a rendering-regression run only). In
   tests/e2e/chrome/back-nav.spec.ts add ONE strengthen-only test block
  cloning the existing assertion shape: library #/ → switch to a view via
  the switcher → open an article → BackToLibrary/back → the library is
  visible at the switched-to view URL (never an intermediate view). Every
  existing back-nav case stays byte-stable — additions only.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/library/reading-views.spec.ts tests/e2e/chrome/back-nav.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - both specs exit 0 on chromium; green on chromium+firefox+webkit at wave merge (toBeFocused auto-retry absorbs engine focus-settle timing)
  - reading-views.spec.ts contains assertions for: cold-load no-focus, warm article h1 focus, view-switch h1 focus + URL replace, Back-to-originating-view, deep-link mark focus, restore-beats-h1, overlay title stability, error focus + title, EPUB chapter title, review h1 focus + title
  - git diff tests/e2e/chrome/back-nav.spec.ts shows ONLY added lines (zero modified/deleted existing lines — strengthen-only)
  </acceptance_criteria>
  <done>NAV-04's focus/title/history contract is proven in real browsers on all three engines.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite gate + a11y coverage check + byte-stable anchor audit</name>
  <files>tests/e2e/a11y.spec.ts</files>
  <read_first>
  - .planning/phases/14-navigation-and-library-contracts/14-VALIDATION.md (sampling contract + manual-only table)
  - tests/e2e/a11y.spec.ts (which surfaces the axe scans cover)
  - package.json (test scripts)
  </read_first>
  <action>
  First verify a11y coverage: the new view-switcher nav lives on the library
  surface — confirm a11y.spec's axe scans include the library view, and
  extend the spec strengthen-only ONLY if the library surface is outside the
  scanned set (the expectation is no change needed — 14-RESEARCH A6). Then
  run the FULL npm run test in ONE invocation — the honest phase gate: exit
  0 required; record the exact passed/failed/skipped counts (unit + e2e
  breakdown) in 14-04-SUMMARY.md. Never subset, grep-filter, or skip an
  engine to claim green; if any failure appears, record it honestly and
  surface it per the gap process (the 04-11 / 09-07 discipline). Finally the
  anchor audit: git diff review proving tests/e2e/ingestion/happy-path.spec.ts
  and tests/e2e/library/progress-recent.spec.ts have ZERO changes, that
  back-nav.spec.ts changed only by the added view-route case, and that the
  library h1 markup change is tabIndex+ref only. Note the manual boundary in
  the SUMMARY: SR announcement quality (D14-09 voice) is the documented
  manual/deferred instrument (ACPT-08, Phase 21) — this phase proves the
  automatable substrate (focus identity landed on the h1).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
  - npm run test exits 0 in one invocation; exact counts recorded in 14-04-SUMMARY.md
  - tests/e2e/a11y.spec.ts green across chromium/firefox/webkit with the .view-switcher nav present in a scanned library surface (extended only if coverage was genuinely absent — the diff then shows additions only)
  - git diff shows zero changes to happy-path.spec.ts and progress-recent.spec.ts
  - the SUMMARY records the D14-09 manual boundary and the deferred-items note if any pre-existing failure surfaced
  </acceptance_criteria>
  <done>Phase 14's honest gate is green with counts on record; every byte-stable anchor proven untouched; a11y coverage confirmed for the new landmark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test seeding → local IndexedDB | spec-written rows enter the app's read path (local-only, no production surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering (false-green tests) | honest gate | mitigate | full npm run test in one invocation; counts recorded; no subset/engine-skip; strengthen-only diffs audited via git |
| T-14-09 | Tampering (seed rows) | seedBook/seedArticleRows | accept | test-local raw IndexedDB writes built from Zod-parsed payloads; cleared per-test; no production code path touched |
| T-14-SC | Tampering (supply chain) | npm installs | accept | zero packages installed this plan |
</threat_model>

<verification>
- npx playwright test tests/e2e/library/reading-views.spec.ts tests/e2e/chrome/back-nav.spec.ts --project=chromium — exit 0 (full 3-engine at merge)
- npx playwright test tests/e2e/a11y.spec.ts — exit 0
- npm run test — exit 0 with counts recorded (the honest gate)
</verification>

<success_criteria>
- LIB-08 agreement structurally locked in 3 engines (counts + rows + empty states vs the imported policy)
- NAV-04 focus/title/history matrix green in 3 engines, including the honesty rows (D14-19/D14-21) as held-out seeded checks
- Honest full-suite gate exit 0; byte-stable anchor specs byte-identical; strengthen-only diffs only
</success_criteria>

## Artifacts this phase produces (Plan 14-04)

- `tests/e2e/library/reading-views.spec.ts` — NEW spec: beforeEach harness (incl. books-store clearing), helpers `seedBook` / `seedArticleRows` (+ cloned `seedLocation`), the views/counts/empty agreement matrix, and the focus/title/history matrix
- `tests/e2e/chrome/back-nav.spec.ts` — strengthened with one view-route Back-semantics case (additions only)
- `tests/e2e/a11y.spec.ts` — extended ONLY if the library surface was outside the axe scans (expected: no change)
- `14-04-SUMMARY.md` — the honest-gate count record + anchor audit + manual-boundary note

<output>
Create `.planning/phases/14-navigation-and-library-contracts/14-04-SUMMARY.md` when done
</output>
