---
phase: 14-navigation-and-library-contracts
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/routes/ArticleView.tsx
  - src/routes/review/ReviewView.tsx
  - tests/component/ArticleView.test.tsx
autonomous: true
requirements: [NAV-04]
must_haves:
  truths:
    - "Each destination sets a truthful document.title: article '<provenance.title> — Lem Reader', EPUB chapter '<chapter title> — <book.title> — Lem Reader', review 'Review highlights — Lem Reader', error 'Couldn't open this article — Lem Reader' (D14-02/D14-06/D14-07)"
    - "An in-app swap into a fresh article with nothing to restore focuses its h1; cold loads and reloads never move focus (D14-01/D14-03)"
    - "A deep link to #/article/<id>/h/<hl> keeps focus on the highlight mark, never the h1 (D14-05)"
    - "A resumed article never yanks focus to the h1 — the saved-location restore wins (D14-10)"
    - "The error state gets title + h1 focus parity on in-app arrival (D14-06)"
    - "Overlays never touch the title (D14-11 — zero overlay title code by construction)"
  artifacts:
    - path: "src/routes/ArticleView.tsx"
      provides: "Title effect keyed [article, chapterContext, status] + h1-default focus in the restore fall-through + error h1 focus parity"
      contains: "setDocumentTitle"
    - path: "src/routes/review/ReviewView.tsx"
      provides: "Review title + warm-gated mount h1 focus"
      contains: "setDocumentTitle"
    - path: "tests/component/ArticleView.test.tsx"
      provides: "jsdom-level title + focus-wiring coverage (Pitfall 7 boundary respected)"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/ingestion/library/pageMeta.ts"
      via: "setDocumentTitle for article/EPUB-chapter/error forms"
      pattern: "setDocumentTitle"
    - from: "src/routes/review/ReviewView.tsx"
      to: "src/ingestion/library/pageMeta.ts"
      via: "setDocumentTitle('Review highlights')"
      pattern: "setDocumentTitle"
    - from: "src/routes/ArticleView.tsx (restore-effect fall-through)"
      to: "article h1 (tabIndex=-1 + ref)"
      via: "h1-default focus when no jump pending + no saved location + warm mount (D14-05/D14-10 layering at ONE decision point)"
      pattern: "articleH1Ref\\.current\\?\\.focus"
    - from: "src/routes/ArticleView.tsx (status effect)"
      to: "error h1 (tabIndex=-1 + ref)"
      via: "error focus parity when status === error and warm (D14-06)"
      pattern: "errorH1Ref\\.current\\?\\.focus"
---

<objective>
Give the article and review destinations truthful titles (D14-02, incl. the
EPUB chapter D14-07 and error D14-06 forms) and the uniform route-change h1
focus rule (D14-01) with the full most-specific-wins layering inside
ArticleView: deep-link jump focus > saved-location restore > h1 default
(D14-05/D14-10), cold-load immunity (D14-03), and error parity (D14-06).
ReviewView gets title + warm-gated mount focus.

Purpose: NAV-04's title + focus half (history/views are Plan 14-02).
Output: Two view components extended; zero App.tsx changes (hasAppHistory is
already threaded to both and doubles as the warm-mount signal).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-and-library-contracts/14-CONTEXT.md
@.planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md
@.planning/phases/14-navigation-and-library-contracts/14-PATTERNS.md
@.planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md
@.planning/phases/14-navigation-and-library-contracts/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewView — title effect + warm-gated mount h1 focus</name>
  <files>src/routes/review/ReviewView.tsx</files>
  <read_first>
  - src/routes/review/ReviewView.tsx (truncate helper L70-80, load effect L278-301, header + h1 L320-329, the hasAppHistory prop threading from App.tsx L250)
  - src/ingestion/library/pageMeta.ts (Plan 14-01 helper)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Pattern 1 (per-view ownership rationale)
  </read_first>
  <action>
  The h1 ("Review highlights", L328 — text byte-stable) gains tabIndex={-1}
  and a ref. ONE mount effect (empty deps, no cleanup — Pitfall 9 idempotent
  focus): call setDocumentTitle("Review highlights") (D14-02 — the helper
  appends the suffix), then focus the h1 ONLY when the hasAppHistory prop is
  true (D14-01/D14-03 — the already-threaded flag doubles as the per-mount
  warm signal: false on cold loads and reloads by construction). No live
  region, no announcement code (D14-09). Overlays in this view (none today;
  dialogs are ArticleView/global) touch nothing.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/review/ReviewView.tsx: the h1 element carries tabIndex={-1} + a ref; its text remains exactly "Review highlights"
  - a mount effect calls setDocumentTitle("Review highlights") and gates h1 focus on the hasAppHistory prop
  - tests/e2e/review-panel suite green on chromium (regression — ReviewView rendering unchanged)
  </acceptance_criteria>
  <done>Review destination has title + focus parity with the uniform rule; jsdom-invisible timing deferred to Plan 14-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ArticleView — per-destination title effect (article / EPUB chapter / error)</name>
  <files>src/routes/ArticleView.tsx, tests/component/ArticleView.test.tsx</files>
  <read_first>
  - src/routes/ArticleView.tsx (chapterContext state + tolerant load L1195-1250, chapter context line render L1797-1812, error branch h1 L1679-1693, article h1 L1959)
  - src/ingestion/library/pageMeta.ts (setDocumentTitle + the 64-char combined-portion truncation)
  - tests/component/ArticleView.test.tsx (renderWithProvider harness, openArticle mock, stubArticle shape)
  - .planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md §Copywriting (title table — em-dash separators, no trailing period in titles)
  </read_first>
  <behavior>
  - Test (jsdom): standalone article resolves → document.title === "<provenance.title> — Lem Reader"
  - Test (jsdom): openArticle rejects → status error branch renders → document.title === "Couldn't open this article — Lem Reader" (no trailing period before the suffix; the visible h1 keeps its own period)
  - Test (jsdom): while article is null and status is loading, document.title is NOT written by ArticleView (the previous/static title stands — transient state)
  </behavior>
  <action>
  Add ONE title effect keyed [article, chapterContext, status] (Pitfall 8 —
  every truth change rewrites; the async chapterContext lookup upgrades the
  title when it resolves): when status is the error state →
  setDocumentTitle("Couldn't open this article") (D14-06; apostrophe and no
  trailing period — the helper appends the suffix); when article is null
  (loading) → no write at all (transient); when article AND chapterContext
  are both truthy → setDocumentTitle with the combined content template
  article.provenance.title + " — " + chapterContext.book.title (D14-07 —
  the helper truncates the COMBINED chapter—book portion at 64 and appends
  the suffix, yielding "Chapter — Book — Lem Reader"); otherwise (article,
  null chapterContext — standalone article or missing/corrupt book row) →
  setDocumentTitle(article.provenance.title). The effect writes only — no
  cleanup, no restore-on-unmount (every destination sets its own title;
  D14-11: overlays have zero title code by construction).
  </action>
  <verify>
    <automated>npx vitest run tests/component/ArticleView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - tests/component/ArticleView.test.tsx exits 0 with the three new title cases; the standalone + error cases pin exact full strings including the " — Lem Reader" suffix
  - the title effect's dependency array is exactly [article, chapterContext, status]
  - src/routes/ArticleView.tsx contains setDocumentTitle calls for all three forms (error literal, combined chapter—book template, plain provenance title)
  </acceptance_criteria>
  <done>Article + chapter + error titles are truthful and upgrade-safe; the EPUB form is additionally proven in a real browser in Plan 14-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ArticleView — focus layering (deep-link jump > restore > h1 default; error parity)</name>
  <files>src/routes/ArticleView.tsx, tests/component/ArticleView.test.tsx</files>
  <read_first>
  - src/routes/ArticleView.tsx (jump effect + jumpPendingRef claim discipline L1255-1394; restore effect + jumpPendingRef suppression L1396-1456 — the fall-through sites; error h1 L1679-1693; article h1 L1959; ArticleViewProps hasAppHistory)
  - .planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md §Pitfall 1 (focus fights — the one-decision-point rule) + §Pitfall 9 (no cleanup)
  - tests/component/ArticleView.test.tsx (harness + the title cases from Task 2)
  </read_first>
  <behavior>
  - Test (jsdom): hasAppHistory true + openArticle resolves + no jump param + no restorable location (loadLocation falls through) → the article h1 is document.activeElement (D14-01)
  - Test (jsdom): hasAppHistory false under the same conditions → the h1 is NOT focused (D14-03 cold-load immunity)
  - Test (jsdom): status error + hasAppHistory true → the error h1 is document.activeElement (D14-06)
  - Deep-link-wins (D14-05) and restore-wins (D14-10) assertions are browser-scope (timing/rAF machinery) — pinned in Plan 14-04 Task 2, noted here as the documented boundary
  </behavior>
  <action>
  Add tabIndex={-1} + refs to BOTH h1s: the article h1 (L1959, articleH1Ref)
  and the error-branch h1 (L1687, errorH1Ref) — text and levels byte-stable.
  The h1-default composes INSIDE the existing restore effect at its no-restore
  fall-through (the branch where the location result is not ok OR
  result.location is null): when the hasAppHistory prop is true, call
  articleH1Ref.current?.focus() (D14-01 — fresh-article default). The
  jumpPendingRef early-return ABOVE that point stays byte-unchanged — a
  pending deep-link jump can never reach the h1 default (D14-05). A
  successful restore keeps its existing scroll/banner behavior and NEVER
  focuses the h1 — the restored position is the orientation (D14-10, never
  fight the restore scroll). Add ONE small effect keyed on status: when
  status is the error state and hasAppHistory is true, call
  errorH1Ref.current?.focus() (D14-06 — a truthful destination). No cleanup
  on any focus path (focusing twice is idempotent — Pitfall 9). NEVER add a
  third competing mount effect for the h1 (Pitfall 1 — the decision lives
  at one point inside the existing effects).
  </action>
  <verify>
    <automated>npx vitest run tests/component/ArticleView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - tests/component/ArticleView.test.tsx exits 0 with the three focus-wiring cases (warm h1 default, cold no-focus, error h1 focus)
  - both the article h1 and the error h1 carry tabIndex={-1} + refs in src/routes/ArticleView.tsx
  - the h1-default focus call site lives inside the restore effect's no-restore fall-through (after the jumpPendingRef early return), not in a new mount effect
  - no focus-related effect in this task returns a cleanup function
  </acceptance_criteria>
  <done>Most-specific-wins layering holds at one decision point; error states get parity; the deep-link/restore ordering proofs run in real browsers in Plan 14-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| foreign provenance/chapter titles → document.title | imported bundle + web-extracted strings reach a DOM-global write |
| async truth → title/focus | slow loads decide which destination truth is announced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (XSS via title) | title effect strings | mitigate | pageMeta text-only assignment + 64-char truncation (Plan 14-01); foreign titles cannot execute (MDN/WHATWG text-only title model) |
| T-14-06 | Spoofing (tab/title mismatch) | error + loading states | mitigate | loading writes NO title (never a stale lie); error titles say error exactly (D14-06 honesty) |
| T-14-07 | Denial of service (focus fights disorient SR users) | three converging focus pipelines | mitigate | one decision point inside the existing effects (Pitfall 1); no third competing effect; cold loads never move focus (D14-03) |
| T-14-SC | Tampering (supply chain) | npm installs | accept | zero packages installed this plan |
</threat_model>

<verification>
- npx vitest run tests/component/ArticleView.test.tsx — exit 0 (title + focus wiring)
- npx playwright test tests/e2e/review-panel --project=chromium — exit 0 (review regression)
- Deep-link/restore/error ordering + EPUB chapter title proven in real browsers by Plan 14-04 Task 2 (documented boundary, per 14-VALIDATION Manual-Only + e2e map)
</verification>

<success_criteria>
- All four title forms exact per UI-SPEC (article, chapter, review, error)
- h1 focus fires exactly on warm fresh-article arrival and warm error arrival; never cold, never over a jump, never over a restore
- Byte-stable h1 texts unchanged ("Review highlights", "Couldn't open this article.", article provenance title)
- No App.tsx changes, no store changes, no overlay title/focus code
</success_criteria>

## Artifacts this phase produces (Plan 14-03)

- `src/routes/ArticleView.tsx` — NEW title effect keyed [article, chapterContext, status]; NEW refs `articleH1Ref` + `errorH1Ref` on the two h1s (both gain tabIndex={-1}); NEW h1-default focus branch inside the restore effect's fall-through; NEW status-keyed error-focus effect
- `src/routes/review/ReviewView.tsx` — NEW h1 ref + tabIndex={-1}; NEW mount effect (setDocumentTitle + warm-gated focus)
- `tests/component/ArticleView.test.tsx` — extended with title + focus-wiring cases

<output>
Create `.planning/phases/14-navigation-and-library-contracts/14-03-SUMMARY.md` when done
</output>
