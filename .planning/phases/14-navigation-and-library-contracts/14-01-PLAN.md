---
phase: 14-navigation-and-library-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/library/readingState.ts
  - src/ingestion/library/pageMeta.ts
  - src/ingestion/library/bookProgress.ts
  - src/ingestion/library/LibraryRow.tsx
  - src/ingestion/library/BookRow.tsx
  - src/ingestion/library/ContinueReadingStrip.tsx
  - tests/unit/library/reading-state.test.ts
  - tests/unit/library/page-meta.test.ts
autonomous: true
requirements: [LIB-07]
must_haves:
  truths:
    - "articleReadingState derives unread for an article with no LocationRecord, in-progress for any opened location below the finished threshold, and finished at or above it (D14-18, D14-19)"
    - "bookReadingState reports a 39-of-40-chapter book and a book with a missing chapter row as in-progress, never finished (D14-19, D14-21)"
    - "LibraryRow, BookRow, and ContinueReadingStrip render byte-identical output after swapping their derivation onto the policy module (D14-20)"
    - "setDocumentTitle writes '<content> — Lem Reader' with the content portion truncated at 64 characters (D14-02 foundation)"
  artifacts:
    - path: "src/ingestion/library/readingState.ts"
      provides: "The ONE pure reading-state policy module (D14-20)"
      exports: ["ReadingState", "articleReadingState", "bookReadingState", "countByState"]
      contains: "FINISHED_THRESHOLD"
    - path: "src/ingestion/library/pageMeta.ts"
      provides: "Per-destination document.title helper (D14-02 foundation)"
      exports: ["TITLE_SUFFIX", "truncateTitle", "setDocumentTitle"]
    - path: "tests/unit/library/reading-state.test.ts"
      provides: "LIB-07 policy truth table (12 boundary rows)"
    - path: "tests/unit/library/page-meta.test.ts"
      provides: "Title string builder + truncation boundaries"
  key_links:
    - from: "src/ingestion/library/readingState.ts"
      to: "src/ingestion/library/ContinueReadingStrip.tsx"
      via: "FINISHED_THRESHOLD import — the single source, never a numeric fork"
      pattern: "import \\{ FINISHED_THRESHOLD \\} from \"\\./ContinueReadingStrip\""
    - from: "src/ingestion/library/readingState.ts"
      to: "src/ingestion/library/bookProgress.ts"
      via: "deriveBookProgress === 1 + resolveResumeChapterId === null wrap (D14-19/D14-21)"
      pattern: "from \"\\./bookProgress\""
    - from: "src/ingestion/library/LibraryRow.tsx"
      to: "src/ingestion/library/readingState.ts"
      via: "FINISHED_RATIO fork deleted; isFinished routes through articleReadingState (D14-20)"
      pattern: "from \"\\./readingState\""
    - from: "src/ingestion/library/ContinueReadingStrip.tsx"
      to: "src/ingestion/library/readingState.ts"
      via: "membership gates become !== in-progress checks (behavior identical)"
      pattern: "articleReadingState|bookReadingState"
---

<objective>
Ship the two pure foundation modules of Phase 14 — the reading-state policy
module `readingState.ts` (D14-20: ONE derivation owned by ONE module) and the
title helper `pageMeta.ts` (D14-02 foundation) — and unify the three existing
consumers (LibraryRow, BookRow, ContinueReadingStrip) onto the policy with
byte-identical rendered output. The LibraryRow FINISHED_RATIO fork (known tech
debt) is deleted here.

Purpose: LIB-07's "one documented progress policy" and LIB-08's "counts cannot
disagree with membership" both require a single derivation point before any
UI work. Zero UI change, zero store change (Pitfall 9 — policy derives from
existing rows).
Output: 2 new pure modules, 2 new unit suites, 1 one-line export in
bookProgress.ts, 3 behavior-identical consumer edits.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-and-library-contracts/14-CONTEXT.md
@.planning/phases/14-navigation-and-library-contracts/14-RESEARCH.md
@.planning/phases/14-navigation-and-library-contracts/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: readingState.ts policy module + 12-row truth-table unit suite</name>
  <files>src/ingestion/library/readingState.ts, src/ingestion/library/bookProgress.ts, tests/unit/library/reading-state.test.ts</files>
  <read_first>
  - src/ingestion/library/bookProgress.ts (the exact module shape to mirror: header/store-seam comment discipline, deriveBookProgress L72-91, resolveResumeChapterId L102-118, private latestLocationByArticle L43-54)
  - src/ingestion/library/ContinueReadingStrip.tsx (FINISHED_THRESHOLD export L56 — the single source to import; membership filters L121-168 for the semantics being unified)
  - tests/unit/library/book-progress.test.ts (fixture style to clone: BookSchema.parse/LocationRecordSchema.parse builders, lengthsOf identity lookup, boundary-named truth-table cases)
  </read_first>
  <behavior>
  - Test: no location → unread (D14-18)
  - Test: location at offset 0 with total > 0 → in-progress (D14-18 "opened = started")
  - Test: offset = total → finished
  - Test: offset exactly at the FINISHED_THRESHOLD boundary (total 100, offset 98) → finished (>= semantics)
  - Test: one below the boundary (total 100, offset 97) → in-progress
  - Test: opened zero-length article (total 0, positive offset) → finished — preserves current LibraryRow/strip behavior; the formula stays verbatim so the edge is byte-stable (document in the test comment)
  - Test: book with no chapter locations → unread
  - Test: book with some chapters opened, none finished → in-progress
  - Test: book 39/40 chapters finished → in-progress (D14-19)
  - Test: book whose chapterArticleIds includes an id with no article row (textLengthOf returns undefined) and all present chapters finished → in-progress (D14-21)
  - Test: book with all chapters individually finished → finished
  - Test: countByState over a mixed corpus returns counts that sum to standalone + book totals, one book counting once (D14-24)
  </behavior>
  <action>
  Create the test file first (RED), then the module (GREEN). readingState.ts
  mirrors bookProgress.ts exactly (no React, no Dexie, caller-supplied
  lookups; header comment cites D14-18/D14-19/D14-20/D14-21). Exports:
  type ReadingState as the literal union unread | in-progress | finished;
  articleReadingState(location: LocationRecord | undefined, total: number)
  — no location returns unread, otherwise the ratio is computed with the
  formula verbatim (Math.min(1, location.graphemeOffset / total)) and
  compared with >= against FINISHED_THRESHOLD imported from
  ./ContinueReadingStrip (NEVER a numeric literal — any literal threshold in
  this file is a fork of D8-12); bookReadingState(book, locations,
  textLengthOf) — resolveResumeChapterId(book, locations) === null returns
  unread, otherwise deriveBookProgress(book, locations, textLengthOf) === 1
  returns finished, else in-progress (wrap the existing functions, never
  re-implement their algebra — D14-19/D14-21 fall out of deriveBookProgress's
  own denominator discipline); countByState(standalone: array of
  id/location?/total entries, books, locations, textLengthOf) folding the two
  functions into a Record&lt;ReadingState, number&gt; where each book counts
  once. In bookProgress.ts add the export keyword to
  latestLocationByArticle (single-line change, no behavior change) — it
  becomes the one owner of the latest-savedAt fold (the fourth-copy
  anti-pattern from 14-RESEARCH Open Question 2 is closed by export, per the
  research recommendation). Note for the executor: the
  readingState ↔ ContinueReadingStrip import cycle is the exact
  bookProgress ↔ strip precedent already shipping (constant import is
  side-effect free — see bookProgress.ts header L9-13); do NOT restructure
  it.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/library/reading-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - npx vitest run tests/unit/library/reading-state.test.ts exits 0 with all 12 truth-table rows, including cases named for the 39/40 edge (D14-19), the missing-chapter-row edge (D14-21), the opened-zero-length edge, and both threshold-boundary rows
  - src/ingestion/library/readingState.ts contains `export type ReadingState`, `export function articleReadingState(`, `export function bookReadingState(`, `export function countByState(`
  - src/ingestion/library/readingState.ts contains `import { FINISHED_THRESHOLD } from "./ContinueReadingStrip"` and `rg -n "0\.98" src/ingestion/library/readingState.ts` returns zero matches
  - src/ingestion/library/bookProgress.ts contains `export function latestLocationByArticle(`
  </acceptance_criteria>
  <done>Policy module + fold export shipped with a green 12-row truth table; the threshold constant has exactly one source (the strip export) and the module owns zero measurement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pageMeta.ts title helper + unit suite</name>
  <files>src/ingestion/library/pageMeta.ts, tests/unit/library/page-meta.test.ts</files>
  <read_first>
  - src/routes/review/ReviewView.tsx L70-80 (the truncate/ARIA_MAX_CHARS discipline this helper generalizes)
  - .planning/phases/14-navigation-and-library-contracts/14-UI-SPEC.md (§Copywriting Contract — the exact title strings and the 64-char truncation lock; §Component Inventory pageMeta.ts entry)
  - src/ingestion/library/bookProgress.ts (pure-module header conventions to copy)
  </read_first>
  <behavior>
  - Test: setDocumentTitle("Saved articles") → document.title === "Saved articles — Lem Reader" (em dash with surrounding spaces)
  - Test: setDocumentTitle("Couldn't open this article") → document.title === "Couldn't open this article — Lem Reader"
  - Test: content of exactly 64 characters passes through untruncated
  - Test: content of 65 characters truncates to the first 64 characters plus "…" before the " — Lem Reader" suffix
  - Test: TITLE_SUFFIX is exported as "Lem Reader"
  </behavior>
  <action>
  Create pageMeta.ts as a pure helper module: export const TITLE_SUFFIX =
  "Lem Reader"; a private const TITLE_CONTENT_MAX_CHARS = 64; export
  function truncateTitle(content: string): string returning content
  unchanged when at or under the cap, else content.slice(0, 64) + "…" (the
  ReviewView truncate shape); export function setDocumentTitle(content:
  string): void performing a single plain-text assignment document.title =
  template of truncateTitle(content) + " — " + TITLE_SUFFIX. Em dash with
  one space each side is the separator (matches the D12-08 strip label
  convention per UI-SPEC). No markup, no restore-on-unmount API — every
  destination including error states sets its own title on mount (D14-06
  note in the header comment). document.title is a text-only assignment —
  no injection surface (14-RESEARCH Security Domain).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/library/page-meta.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - npx vitest run tests/unit/library/page-meta.test.ts exits 0 with the five rows above
  - src/ingestion/library/pageMeta.ts contains `export const TITLE_SUFFIX = "Lem Reader"`, `export function truncateTitle(`, `export function setDocumentTitle(`
  - the em-dash separator string appears exactly once in pageMeta.ts (inside setDocumentTitle)
  </acceptance_criteria>
  <done>Title helper shipped and pinned; Plans 14-02/14-03 consume it, never their own string building.</done>
</task>

<task type="auto">
  <name>Task 3: Consumer unification — FINISHED_RATIO fork deleted, strip + BookRow onto the policy</name>
  <files>src/ingestion/library/LibraryRow.tsx, src/ingestion/library/BookRow.tsx, src/ingestion/library/ContinueReadingStrip.tsx</files>
  <read_first>
  - src/ingestion/library/LibraryRow.tsx (FINISHED_RATIO const L35-36 + isFinished use L76-78 + the ratio/hairline math to keep verbatim)
  - src/ingestion/library/BookRow.tsx (totalsById memo L66-75, deriveBookProgress memo L91-97, isFinished L120-121)
  - src/ingestion/library/ContinueReadingStrip.tsx (article membership gate L123-137, book membership gates L143-168, FINISHED_THRESHOLD export L56 which stays untouched)
  - src/ingestion/library/readingState.ts (Task 1 output — the API being consumed)
  </read_first>
  <action>
  Behavior-identical swap per D14-20 (the current predicates ARE the
  in-progress definition — 14-RESEARCH A7; progress-recent.spec is the
  regression lock). LibraryRow.tsx: DELETE the FINISHED_RATIO const and its
  D8-12 comment entirely; isFinished becomes articleReadingState(location,
  total) === "finished"; the ratio math stays verbatim for the hairline
  (showHairline unchanged); import from ./readingState. BookRow.tsx:
  isFinished becomes bookReadingState(book, locations, (articleId) =>
  totalsById.get(articleId)) === "finished"; the deriveBookProgress memo and
  totalsById memo stay untouched (the hairline ratio still needs them);
  import from ./readingState. ContinueReadingStrip.tsx: the standalone
  article membership gate (currently the progress >= FINISHED_THRESHOLD
  early return) becomes articleReadingState(location, total) !==
  "in-progress" → return empty; the book membership gate (currently the
  resumeChapterId === null and progress >= 1 early returns) becomes
  bookReadingState(book, locations, lookup over totalsById) !==
  "in-progress" → return empty — but KEEP the resumeChapterId / ordinal /
  resumeLocation derivations because the entry construction still needs
  them; only the membership decision swaps. The FINISHED_THRESHOLD export
  at L56 is byte-unchanged (it remains THE single source). Strip surface,
  copy, and DOM stay untouched (Phase 16 owns the redesign; this phase only
  refactors membership per the CONTEXT boundary).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/library/ && npx playwright test tests/e2e/library/progress-recent.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "FINISHED_RATIO" src/` returns zero matches
  - src/ingestion/library/LibraryRow.tsx, BookRow.tsx, and ContinueReadingStrip.tsx each contain an import from "./readingState"
  - tests/e2e/library/progress-recent.spec.ts passes UNMODIFIED on chromium (zero diff to the spec — strengthen-only discipline; it is the behavior lock for the strip + marks)
  - ContinueReadingStrip.tsx still contains `export const FINISHED_THRESHOLD = 0.98;` byte-identical (single source preserved)
  </acceptance_criteria>
  <done>All three consumers derive from the one policy module; rendered output byte-identical; the standing FINISHED_RATIO fork is gone (D14-20).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted rows → pure module | LocationRecord/Book rows (some originating from imported bundles) enter the derivation as data only |
| foreign titles → document.title | (pageMeta) imported/foreign article titles reach a DOM-global string write in a later plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (XSS via title) | pageMeta.setDocumentTitle | mitigate | text-only `document.title` assignment + 64-char truncateTitle cap; no markup path (MDN/WHATWG text-only title model) |
| T-14-02 | Tampering (row data) | readingState derivations | accept | inputs are Zod-validated rows at the store seam (STATE-04); module performs arithmetic only, outputs a union literal |
| T-14-SC | Tampering (supply chain) | npm installs | accept | this plan installs ZERO packages (14-RESEARCH Package Legitimacy Audit: none installed); no checkpoint required |
</threat_model>

<verification>
- npx vitest run tests/unit/library/reading-state.test.ts tests/unit/library/page-meta.test.ts — exit 0
- npx playwright test tests/e2e/library/progress-recent.spec.ts --project=chromium — exit 0, spec unmodified
- rg -n "FINISHED_RATIO" src/ — zero matches
</verification>

<success_criteria>
- readingState.ts is the single derivation point for unread/in-progress/finished across articles AND books (D14-18/19/20/21 pinned by 12 unit rows)
- pageMeta.ts produces the UI-SPEC title convention exactly (suffix + em dash + 64-char truncation)
- LibraryRow/BookRow/ContinueReadingStrip outputs are byte-identical (progress-recent.spec green unmodified)
- No store changes, no new dependencies, no UI surface changes
</success_criteria>

## Artifacts this phase produces (Plan 14-01)

- `src/ingestion/library/readingState.ts` — NEW: exports `ReadingState` (type union), `articleReadingState(location, total)`, `bookReadingState(book, locations, textLengthOf)`, `countByState(standalone, books, locations, textLengthOf)`
- `src/ingestion/library/pageMeta.ts` — NEW: exports `TITLE_SUFFIX`, `truncateTitle(content)`, `setDocumentTitle(content)`
- `src/ingestion/library/bookProgress.ts` — `latestLocationByArticle` newly exported (was private; no behavior change)
- `tests/unit/library/reading-state.test.ts`, `tests/unit/library/page-meta.test.ts` — NEW test files
- Deleted symbol: `FINISHED_RATIO` const from LibraryRow.tsx

<output>
Create `.planning/phases/14-navigation-and-library-contracts/14-01-SUMMARY.md` when done
</output>
