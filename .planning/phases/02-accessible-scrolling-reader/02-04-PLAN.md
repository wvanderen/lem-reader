---
phase: 02-accessible-scrolling-reader
plan: 04
type: execute
gap_closure: true
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/reader/ProgressHairline.tsx
  - src/settings/applyTheme.ts
  - src/app.css
  - tests/component/SettingsContext.test.tsx
  - tests/e2e/progress.spec.ts
  - tests/e2e/typography-live-apply.spec.ts
autonomous: true
requirements: [READ-02, READ-05]
must_haves:
  truths:
    - "The progress hairline fill grows from the LEFT edge as the reader scrolls (not from the center)"
    - "Changing Text size in Settings updates the article body's computed font-size immediately (no Save step)"
    - "Changing Spacing in Settings updates the article body's line-height AND letter/word-spacing immediately"
    - "First paint still renders 18px / 1.6 line-height / zero letter- and word-spacing before applyTheme runs (no flash of unstyled text)"
    - "Font family, theme, and reading width continue to live-apply unchanged (regression-preserved)"
  artifacts:
    - path: "src/reader/ProgressHairline.tsx"
      provides: "ProgressHairline with a CSS-grammar-valid left-edge transform-origin so scaleX grows left-to-right"
      contains: "transformOrigin: \"left\""
    - path: "src/settings/applyTheme.ts"
      provides: "Routes size + line-height through custom properties consumed by body (no bare property writes that body overrides)"
      contains: "--font-size"
    - path: "src/app.css"
      provides: "body rule consumes the four typography custom properties; .progress-hairline-fill uses a valid transform-origin"
      contains: "var(--font-size, 18px)"
    - path: "tests/component/SettingsContext.test.tsx"
      provides: "Asserts the corrected custom-property token names (--font-size / --line-height) are written"
      contains: "--font-size"
    - path: "tests/e2e/progress.spec.ts"
      provides: "Extended with a computed transform-origin left-edge assertion"
      contains: "transformOrigin"
    - path: "tests/e2e/typography-live-apply.spec.ts"
      provides: "Browser-level proof that size + spacing changes reach body computed style"
      contains: "typography-live-apply"
  key_links:
    - from: "src/settings/applyTheme.ts"
      to: "src/app.css"
      via: "applyTheme writes --font-size/--line-height on <html>; the body rule (lines 127-131) consumes them via var() with literal fallbacks"
      pattern: "var\\(--font-size"
    - from: "src/reader/ProgressHairline.tsx"
      to: "src/app.css"
      via: "inline transformOrigin value must match the .progress-hairline-fill CSS rule's transform-origin (both a valid left keyword)"
      pattern: "transform-origin: left"
---

## Phase Goal (Gap Closure)

**As a** reader, **I want to** see the progress hairline fill from the inline-start edge and have my text-size and spacing choices take effect immediately, **so that** the reading surface behaves the way the settings and progress UI promise.

> Plan 04 closes two UAT-diagnosed gaps from `02-UAT.md` without re-planning the phase. Both root causes are CSS/token-plumbing regressions, NOT missing features — the fixes are surgical (one valid keyword swap + one custom-property routing correction). Zero npm installs; authored CSS + the existing React token seam only.

<objective>
Close the two failing UAT truths from `02-UAT.md ## Gaps`:

1. **Gap 1 (test 3, READ-05, minor):** the progress hairline fills from the center because `transform-origin: inline-start` is not in the `transform-origin` grammar (no logical keywords) — browsers ignore it and fall back to the initial `50% 50%`. Fix: use the physical `left` keyword in BOTH the inline style (ProgressHairline.tsx) and the `.progress-hairline-fill` CSS rule (app.css). RTL is deferred (UI-SPEC content is LTR English; a `[dir="rtl"]` override can ship later).

2. **Gap 2 (test 4, READ-02, major):** text-size and spacing settings don't apply because (a) applyTheme writes the bare `font-size` / `line-height` properties on `<html>` which the hardcoded `body { font-size: 18px; line-height: 1.6 }` rule overrides, and (b) `--letter-spacing` / `--word-spacing` are written but consumed by nothing (dead writes). Fix: route all four typography knobs through custom properties consumed by the body rule. `--font-body` and `--measure` already work and MUST NOT be touched.

Purpose: make the shipped settings + progress UI actually deliver its promised behavior so Phase 02 verification passes.
Output: corrected ProgressHairline + applyTheme + app.css body rule; updated SettingsContext component test asserting the corrected token names; extended progress.spec.ts asserting the left-edge origin; new typography-live-apply.spec.ts asserting the cascade reaches body text in a real browser.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-accessible-scrolling-reader/02-UAT.md
@.planning/phases/02-accessible-scrolling-reader/02-PATTERNS.md
@.planning/phases/02-accessible-scrolling-reader/02-VALIDATION.md
@.planning/phases/02-accessible-scrolling-reader/02-01-SUMMARY.md
@.planning/phases/02-accessible-scrolling-reader/02-03-SUMMARY.md
@src/reader/ProgressHairline.tsx
@src/settings/applyTheme.ts
@src/settings/tokens.ts
@src/reader/SettingsPanel.tsx
@src/app.css
@tests/component/SettingsContext.test.tsx
@tests/e2e/progress.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Gap 1): progress hairline fills left-to-right — valid transform-origin</name>
  <files>src/reader/ProgressHairline.tsx, src/app.css, tests/e2e/progress.spec.ts</files>
  <read_first>
    - src/reader/ProgressHairline.tsx (current inline `transformOrigin` value on line 39; the header comment block lines 1-17 explains the no-transition contract that MUST stay intact)
    - src/app.css lines 694-720 (the `.progress-hairline` + `.progress-hairline-fill` rules; line 716 holds the invalid transform-origin value; the NO-transition comment on lines 717-719 MUST remain unchanged — this fix touches ONLY the transform-origin declaration)
    - tests/e2e/progress.spec.ts (existing 4 tests: existence/aria-hidden, no-transition, scaleX near 0 at top, scaleX near 1 at bottom — this task ADDS a fifth test asserting the computed transform-origin resolves to a left-edge origin)
    - .planning/phases/02-accessible-scrolling-reader/02-UAT.md lines 86-105 (Gap 1 root_cause + missing fix)
  </read_first>
  <action>
    <!-- planner-discipline-allow: inline-start -->
    Fix the invalid transform-origin value in BOTH locations (the inline style AND the CSS rule both declare the logical keyword `inline-start`, which is not in the transform-origin grammar — its grammar is `left | center | right | top | bottom` plus lengths/percentages, with NO logical-keyword variants; browsers ignore the unknown value and fall back to the initial `50% 50%`, so the `scaleX()` transform expands from the horizontal center).

    1. In src/reader/ProgressHairline.tsx, change the inline style `transformOrigin` property from the invalid logical keyword to the physical `left` keyword (the value becomes the string literal `left`). Update the header comment (lines 7-12) so the explanation reflects the corrected rationale: the inline-start keyword is invalid for transform-origin; LTR content uses `left` so scaleX grows from the inline-start edge; a `[dir="rtl"]` override to `right` is deferred (UI-SPEC content is LTR English). Do NOT add any transition/animation property — the no-transition contract (UI-SPEC §Interaction 12, RESEARCH anti-pattern #6) is unchanged.

    2. In src/app.css, edit ONLY the `.progress-hairline-fill` rule (line 716): change the `transform-origin` declaration from the invalid logical keyword to the physical keyword `left`. Leave every other declaration in that rule block (`height`, `width`, `background`, and the NO-transition comment) byte-unchanged. Do NOT touch the unrelated `.progress-hairline` track rule (lines 698-708) or the global reduced-motion gate elsewhere in the file (that gate legitimately declares `transition: none !important` and is unrelated to this fix).

    3. In tests/e2e/progress.spec.ts, ADD a new test inside the existing `test.describe("READ-05 progress hairline", ...)` block asserting the computed transform-origin resolves to a LEFT-edge origin. Open the article (same BASE + image-stub + IDB-wipe beforeEach already in the file), then evaluate `.progress-hairline-fill` computed style: `getComputedStyle(el).transformOrigin` resolves to a string whose first token is `0px` when the origin is the left edge (because `left` computes to `0px 50%`). Assert that first token equals `0px` — this proves the scaleX transform grows from the inline-start edge, NOT from the center (where the first token would be a nonzero percentage of the element's width). Name the test descriptively, e.g. "the fill's computed transform-origin resolves to the left edge (not center) — fills left-to-right".
  </action>
  <verify>
    <automated>npx playwright test progress.spec.ts --project=chromium && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/reader/ProgressHairline.tsx inline style sets `transformOrigin` to the physical left keyword (grep -c 'transformOrigin: "left"' on the file is >= 1)
    - src/app.css `.progress-hairline-fill` rule declares `transform-origin: left` (grep -v '^/\*' app.css | grep -v '^ \*' | grep -c 'transform-origin: left' is >= 1)
    - The `.progress-hairline-fill` rule block STILL declares no transition/animation property (the existing "transition resolves to none" progress.spec.ts test stays green — no regression on UI-SPEC §Interaction 12)
    - progress.spec.ts new test asserts the fill's computed transform-origin first token is the left-edge pixel value (grep -c 'transformOrigin' tests/e2e/progress.spec.ts is >= 1)
    - `npx playwright test progress.spec.ts --project=chromium` exits 0
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>The progress hairline's `scaleX()` transform grows from the inline-start (left) edge as the reader scrolls, proven by a real-browser assertion that the computed transform-origin resolves to the left edge; the no-transition contract is preserved; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2 (Gap 2): route text-size + spacing through custom properties the body rule consumes</name>
  <files>src/settings/applyTheme.ts, src/app.css, tests/component/SettingsContext.test.tsx, tests/e2e/typography-live-apply.spec.ts</files>
  <read_first>
    - src/settings/applyTheme.ts (current writes: bare `font-size` + `line-height` on documentElement lines 20 + 22 — overridden by body; `--letter-spacing` / `--word-spacing` lines 23-24 — dead writes consumed by nothing; `--font-body` line 19 + `--measure` line 25 — these WORK, do NOT touch)
    - src/app.css lines 67-72 (the FIRST body rule — margin/background/color/font-family via var(--font-body); DO NOT touch) AND lines 127-131 (the SECOND body rule under the "Base typography" comment — hardcodes `font-size: 18px` + `line-height: 1.6` + `font-weight: 400`; THIS is the rule to edit)
    - src/settings/tokens.ts (SIZE_STEPS, SPACING_PRESETS — the value sets applyTheme writes; the SettingsPanel range guard uses SIZE_STEPS.includes / MEASURE_STEPS.includes and is correct — do NOT change)
    - src/reader/SettingsPanel.tsx (control accessible names: the size range carries `aria-label="Text size"` (role "slider"); the spacing radios are "Compact" / "Comfortable" / "Spacious" (role "radio") — these are the e2e query targets)
    - tests/component/SettingsContext.test.tsx (the `readTokens` helper line 56-64 currently reads the BARE `font-size` property; the assertions at lines 92, 136-140, 157 currently check the bare property — these MUST be updated to the corrected custom-property names because the fix intentionally stops writing the bare property)
    - tests/e2e/persistence.spec.ts (the image-stub + IDB-wipe + BASE + open-settings-panel conventions to mirror in the new spec)
    - .planning/phases/02-accessible-scrolling-reader/02-UAT.md lines 106-128 (Gap 2 root_cause + missing fix)
  </read_first>
  <action>
    Route the four typography knobs (size, line-height, letter-spacing, word-spacing) through custom properties that the body rule consumes, so the live-apply values actually reach the article text. The font-family (`--font-body`) and reading-width (`--measure`) knobs already work and MUST remain untouched.

    1. In src/settings/applyTheme.ts, change the two BARE property writes to custom-property writes: the `font-size` setProperty call becomes a `--font-size` setProperty call (same `${s.size}px` value), and the `line-height` setProperty call becomes a `--line-height` setProperty call (same `String(preset.lineHeight)` value). Leave the existing `--letter-spacing` and `--word-spacing` writes exactly as they are (they were already custom-property writes — they just had no consumer; step 2 adds the consumers). Leave `--font-body`, `--measure`, and the `data-theme` writes untouched. Update the header comment (lines 1-12) to explain the new routing: applyTheme writes custom properties on documentElement; the body rule consumes them via var() with literal first-paint fallbacks; the previously-bare font-size/line-height writes were overridden by body's hardcoded values, which is why size and the line-height half of spacing never reached the text.

    2. In src/app.css, edit ONLY the SECOND body rule (lines 127-131, under the "Base typography" comment). Replace the hardcoded `font-size: 18px;` declaration with `font-size: var(--font-size, 18px);` and the hardcoded `line-height: 1.6;` declaration with `line-height: var(--line-height, 1.6);`. ADD two new declarations to the SAME rule block: `letter-spacing: var(--letter-spacing, 0);` and `word-spacing: var(--word-spacing, 0);`. Leave `font-weight: 400` and the rule's brace structure unchanged. The literal `18px`, `1.6`, and `0` become first-paint fallbacks (before applyTheme runs on SettingsProvider mount, the custom properties are unset and the body renders the D-07 defaults). DO NOT touch the FIRST body rule (lines 67-72) — its `font-family: var(--font-body)` already works and is the model for this fix. Do NOT add anything to the `:root` block — the var() fallbacks handle first paint without redundant root declarations.

    3. In tests/component/SettingsContext.test.tsx, update the token-assertion surface to match the corrected token names. The existing `readTokens` helper (lines 56-64) reads `root.style.getPropertyValue("font-size")` (the bare property) — change it to read `root.style.getPropertyValue("--font-size")` (the custom property); rename the helper field accordingly (e.g. `fontSizeToken`). Update the three assertions that check the bare value: (a) the "applies the D-07 default tokens" test (~line 92) — assert the `--font-size` custom property equals `${DEFAULT_SETTINGS.size}px`; (b) the "update({size, measure, spacing}) writes every typography token live" test (~lines 136-140) — assert `--font-size` equals "24px" AND assert `--line-height` equals "1.8" (replace the current bare `line-height` assertion at line 138); (c) the "reset() restores the D-07 baseline" test (~line 157) — assert `--font-size` equals "18px". Add a one-line comment at the top of the file (or near readTokens) noting WHY the assertions moved: per UAT gap 2, applyTheme now writes custom properties consumed by the body rule instead of bare properties the body rule overrode. Keep EVERY OTHER assertion (data-theme, --font-body, --measure, persistence, STATE-05 routing, dual-flush) unchanged — those code paths are not touched by this fix.

    4. Create tests/e2e/typography-live-apply.spec.ts — a focused real-browser proof that the cascade reaches body text (jsdom cannot prove cascaded computed style; Pitfall 2). Mirror the persistence.spec.ts / progress.spec.ts conventions: `const BASE = "http://localhost:5173"`, a `beforeEach` that stubs remote images and wipes the `lem-reader` IndexedDB for deterministic first-run state, and a fixture with enough body text to assert against (e.g. essay-long-form). Then: open an article, open the settings panel via the "Reading settings" button, and (a) assert the DEFAULT body computed fontSize is "18px"; (b) set the Text size slider to 24 (use `page.getByRole("slider", { name: "Text size" })` then `.fill("24")`; if fill is not supported on the range in this Playwright version, focus the slider and press ArrowUp three times — 18→20→22→24 — as the documented fallback), then assert `getComputedStyle(document.body).fontSize` equals "24px"; (c) click the "Spacious" spacing radio, then assert `getComputedStyle(document.body).wordSpacing` is NOT "0px" (the spacious preset writes `0.05em`, which at 24px font resolves to a nonzero pixel value — this proves the previously-dead `--word-spacing` write is now consumed); (d) click "Compact" and assert `getComputedStyle(document.body).wordSpacing` returns to "0px". Close the panel between interactions is unnecessary — the live-apply happens while the panel is open (D2-03). Run across all three engines in the full suite, but chromium-only is sufficient for the per-task verify.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/component/SettingsContext.test.tsx && npx playwright test typography-live-apply.spec.ts --project=chromium && npm run test:unit -- --run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/settings/applyTheme.ts writes the `--font-size` custom property (grep -c '"--font-size"' on the file is >= 1) and the `--line-height` custom property (grep -c '"--line-height"' is >= 1); it does NOT write the bare `font-size` or `line-height` properties (grep -v '^//' src/settings/applyTheme.ts | grep -c 'setProperty("font-size"' is 0 AND grep -v '^//' src/settings/applyTheme.ts | grep -c 'setProperty("line-height"' is 0); `--font-body`, `--measure`, `--letter-spacing`, `--word-spacing` writes remain
    - src/app.css SECOND body rule (lines ~127-131) consumes all four custom properties: `font-size: var(--font-size, 18px)`, `line-height: var(--line-height, 1.6)`, `letter-spacing: var(--letter-spacing, 0)`, `word-spacing: var(--word-spacing, 0)` (grep -c 'var(--font-size, 18px)' src/app.css is >= 1; grep -c 'var(--line-height, 1.6)' src/app.css is >= 1)
    - src/app.css FIRST body rule (lines 67-72) is byte-unchanged (grep -c 'font-family: var(--font-body)' src/app.css is >= 1 — still works)
    - tests/component/SettingsContext.test.tsx asserts the corrected custom-property names (grep -c '"--font-size"' tests/component/SettingsContext.test.tsx is >= 1 AND grep -c '"--line-height"' tests/component/SettingsContext.test.tsx is >= 1); the data-theme / --font-body / --measure / persistence / STATE-05 assertions remain
    - tests/e2e/typography-live-apply.spec.ts exists and asserts body computed fontSize changes to 24px when the size slider moves AND wordSpacing is nonzero under the spacious preset
    - `npm run test:unit -- --run` exits 0 (full Phase 1+2 unit/component suite green — no regression)
    - `npx playwright test typography-live-apply.spec.ts --project=chromium` exits 0
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Text size and spacing changes flow through custom properties to the body rule and reach article text immediately (proven in a real browser); first-paint defaults stay 18px/1.6/zero; font-family, theme, and reading-width continue to work; SettingsContext component test asserts the corrected token names; full unit suite + tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Both fixes operate entirely within the existing reader → documentElement → CSS cascade path already validated in Plans 02-01 and 02-03. applyTheme still receives only Zod-validated enum/number values; the body rule consumes them via var() (string substitution, no selector parsing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering (V5) | applyTheme custom-property writes | mitigate | Values still derive from ReaderSettingsSchema (Zod enums/numbers); style.setProperty does not parse selectors; no new input path introduced. Existing T-02-02 mitigation unchanged. |
| T-02-12 | Tampering (V5) | body rule var() consumption | accept | Custom-property substitution is plain string replacement by the CSS engine — no script execution surface. Fallback values are hardcoded literals in authored CSS. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (locked-stack gap closure). |
</threat_model>

<verification>
- `npm run test:unit -- --run` green (full Phase 1+2 unit/component suite — SettingsContext assertions updated to the corrected token names)
- `npx playwright test progress.spec.ts typography-live-apply.spec.ts --project=chromium` green (gap 1 origin assertion + gap 2 cascade proof)
- `npx playwright test --project=chromium` green (no regression in the broader e2e suite — persistence, panel-keyboard, section-announce still pass)
- `npx tsc --noEmit` + `npm run lint` clean
- Manual re-check (end-of-phase, optional): drag the Text size slider and click each Spacing option in a browser; confirm the article body reflows immediately and the hairline grows from the left edge on scroll.
</verification>

<success_criteria>
- Progress hairline fill grows from the left edge as the reader scrolls (Gap 1 closed)
- Text size and spacing settings update the article body's computed style immediately, with no Save step (Gap 2 closed)
- First paint renders the D-07 defaults (18px / 1.6 / zero letter- and word-spacing) — no flash of unstyled or wrong-sized text
- Font family, theme, and reading width continue to live-apply (regression-preserved)
- Full unit + chromium e2e suite green; tsc + lint clean
</success_criteria>

<output>
Create `.planning/phases/02-accessible-scrolling-reader/02-04-SUMMARY.md` when done.
</output>

## Artifacts this plan produces (Plan 04 — Gap Closure)

**Reader component (src/reader/):** `ProgressHairline.tsx` (EDIT) — inline `transformOrigin` swapped to a grammar-valid left keyword.

**Settings layer (src/settings/):** `applyTheme.ts` (EDIT) — size + line-height routed through `--font-size` / `--line-height` custom properties; `--letter-spacing` / `--word-spacing` writes unchanged (now consumed).

**CSS (src/app.css):** (EDIT) — the second `body` rule consumes the four typography custom properties via var() with literal first-paint fallbacks; `.progress-hairline-fill` transform-origin corrected.

**Tests:** `tests/component/SettingsContext.test.tsx` (EDIT — token-name assertion update, with rationale note); `tests/e2e/progress.spec.ts` (EDIT — added transform-origin left-edge test); `tests/e2e/typography-live-apply.spec.ts` (NEW — browser-level cascade proof for size + spacing).
