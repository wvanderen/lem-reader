---
phase: 16-organized-library-and-focused-add-flow
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/ingestion/library/LibraryView.tsx
  - src/app.css
  - src/ingestion/AddDialog.tsx
  - src/ingestion/IngestControl.tsx
  - tests/component/IngestControl.test.tsx
  - tests/e2e/library/add-dialog.ts
  - tests/e2e/ingestion/happy-path.spec.ts
  - tests/e2e/pdf-intake.spec.ts
  - tests/e2e/epub-intake.spec.ts
  - tests/e2e/library/markdown-upload.spec.ts
  - tests/e2e/library/upload-queue.spec.ts
  - tests/e2e/library/browse-open.spec.ts
  - tests/e2e/library/remove-cascade.spec.ts
  - tests/e2e/library/search-tag-filter.spec.ts
  - tests/e2e/a11y.spec.ts
  - tests/e2e/portability/round-trip.spec.ts
  - tests/e2e/portability/core-flow-spine.spec.ts
  - tests/e2e/chrome/library-tidy.spec.ts
autonomous: true
requirements: [ADD-01, ADD-03, ADD-04]
must_haves:
  truths:
    - "An 'Add to Library' button beside the h1 in the library header opens the AddDialog modal; the shell header stays exactly two destinations (D16-02, D16-03)"
    - "The three permanently-mounted ingestion forms no longer appear on the Library page — IngestControl is retired from the codebase"
    - "The Library load-status live region (role/aria/copy: 'Opening article…' / 'Couldn't open this article') survives the add-section dissolution byte-stable (D16-03, Pitfall 7)"
    - "Article success closes the dialog then opens #/article/<id> in the reader; book success closes the dialog and the new book row appears via refreshKey with the D12-11 skip disclosure on the row (D16-12)"
    - "The eleven ingestion-driving e2e specs drive ingestion through the shared openAddDialog/pickSource helper and pass on chromium; library-tidy's structural anchors are re-homed to the post-dissolution DOM in the same commit as the DOM change"
    - "The empty All view copy points readers at the Add to Library button by words only — no second inline button (D16-04)"
  artifacts:
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "Header Add button + addOpen state + AddDialog mount + dissolved add section with surviving .status + empty-All copy swap"
      contains: "library-add-button"
    - path: "tests/e2e/library/add-dialog.ts"
      provides: "Shared openAddDialog(page) + pickSource(page, source) helper centralizing accessible names"
      exports: ["openAddDialog", "pickSource"]
    - path: "src/app.css"
      provides: "dialog.add-dialog geometry clone + .library-add-button styles; .ingest-control block retired"
      contains: "dialog.add-dialog"
    - path: "tests/e2e/chrome/library-tidy.spec.ts"
      provides: "Post-dissolution structural anchors: ordered regions, re-scoped .status locator, header-button geometry; .status contract assertions byte-identical"
      contains: "library-add-button"
  key_links:
    - from: "LibraryView Add button (library-add-button)"
      to: "AddDialog open prop"
      via: "LibraryView-local useState addOpen; button onClick setAddOpen(true); aria-haspopup dialog + aria-expanded mirror (Header gear-button precedent)"
      pattern: "setAddOpen\\(true\\)"
    - from: "AddDialog onBookAdded callback"
      to: "LibraryView refreshKey"
      via: "setRefreshKey((k) => k + 1) — RemoveConfirm onConfirm wiring precedent L700-743"
      pattern: "setRefreshKey"
    - from: "11 ingestion-driving e2e specs"
      to: "tests/e2e/library/add-dialog.ts"
      via: "import { openAddDialog, pickSource } — non-spec filename so nothing self-registers (markdown-payload.ts convention)"
      pattern: "openAddDialog"
---

<objective>
Land the focused Add workflow in the app: header-row trigger, add-section dissolution with byte-stable .status survival, AddDialog mounting, IngestControl retirement, dialog styles, library-tidy re-anchor — and migrate the eleven ingestion-driving e2e specs to drive ingestion through a shared dialog-opening helper in the SAME atomic change (Pitfall 1: never a follow-up).

Purpose: ADD-01 (focused workflow replaces permanent forms) + ADD-03 e2e layer (retry/recover paths keep working through the dialog) + ADD-04 success-navigation wiring, per D16-02, D16-03, D16-04, D16-12.
Output: Integrated LibraryView, retired IngestControl, dialog styles, shared helper, 11 migrated ingestion specs + re-anchored library-tidy.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-CONTEXT.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-RESEARCH.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-VALIDATION.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-01-SUMMARY.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-02-SUMMARY.md
@src/ingestion/library/LibraryView.tsx
@src/ingestion/AddDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header Add button, add-section dissolution, AddDialog mount, IngestControl retirement, dialog styles, empty-All copy, library-tidy re-anchor</name>
  <files>src/ingestion/library/LibraryView.tsx, src/app.css, src/ingestion/IngestControl.tsx, tests/component/IngestControl.test.tsx, src/ingestion/AddDialog.tsx, tests/e2e/chrome/library-tidy.spec.ts</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (regions only, not full-file: header row L538-549, strip section L553-555, add section + .status L556-574, dialog wiring precedent L700-743, refreshKey load effect + EMPTY_COPY constant)
  - src/ingestion/AddDialog.tsx (16-02 output — the props contract being wired)
  - src/ingestion/IngestControl.tsx (the retiring component — confirm nothing unique remains un-migrated)
  - src/app.css (dialog.library-remove-confirm block L2349-2364 to clone; .ingest-control block L808-880+ to retire; .status card rules L326-338 to KEEP; .library-header L2214-2227)
  - src/reader/Header.tsx (L222-251 — the gear-button aria-haspopup/aria-expanded trigger precedent)
  - tests/e2e/chrome/library-tidy.spec.ts (anchor regions only: ordered-regions computation L41-53, .status contract L94-100, wide/narrow boundingBox L120/137 — this spec is EDITED in this task)
  </read_first>
  <action>
  One atomic, suite-consistent change set (per D16-02, D16-03, D16-04, D16-12; Pitfall 7):

  1. LIBRARYVIEW TRIGGER: add `const [addOpen, setAddOpen] = useState(false);` (LibraryView-local — the trigger is in-page). In the `.library-header` row (L538-549), beside the h1, add `<button type="button" className="library-add-button" onClick={() => setAddOpen(true)} aria-haspopup="dialog" aria-expanded={addOpen}>Add to Library</button>` — the Header gear-button trigger shape. The shell header is untouched (D16-02).

  2. DISSOLUTION + .status SURVIVAL: delete the `.library-section-add` section wrapper and the `<IngestControl />` mount (L556-574). Re-home the load-status element — `<div className="status" role="status" aria-live="polite" aria-atomic="true">` with its exact copy branches ("Opening article…" / the couldn't-open error copy) — as a direct child of the same parent the section occupied, after the list region. The element's classes, role, aria attributes, and copy strings are byte-stable (Pitfall 7).

  3. DIALOG MOUNT: render `<AddDialog open={addOpen} onCancel={() => setAddOpen(false)} onBookAdded={() => setRefreshKey((k) => k + 1)} />` alongside the existing RemoveConfirm/BookRemoveConfirm mounts (L700-743 precedent). Article success needs no LibraryView involvement (the dialog navigates internally per D16-12). Do NOT touch librarySession capture/restore — the dialog never unmounts the Library, so restore is structurally unaffected (D15-11..14).

  4. EMPTY-ALL COPY (D16-04): change EMPTY_COPY.all.body from its current paste/upload phrasing to `Nothing saved yet. Use the Add to Library button to begin.` — heading "Your library is empty" unchanged; copy points at the button, no inline button added.

  5. RETIREMENT: delete src/ingestion/IngestControl.tsx and tests/component/IngestControl.test.tsx.

  6. APP.CSS: add `dialog.add-dialog` + `dialog.add-dialog::backdrop` cloning the `dialog.library-remove-confirm` geometry (margin:auto UA centering, padding 0, 1px var(--hairline) border, 8px radius, var(--surface-raised) background, var(--ink) color, `width: calc(100vw - var(--space-xl))`, `max-width: 560px`, `overflow: auto` — 320px-safe, high-zoom scrolls) plus `.add-dialog-inner` padding/gaps, `.add-source-picker`/`.add-source-row` layout (min-height var(--touch) on interactive rows), and `.library-add-button` (secondary-button treatment, var(--touch) min-height). Retire the `.ingest-control` block in THIS commit. The `.status` card rules stay (both regions consume them). No motion properties.

  7. LIBRARY-TIDY RE-ANCHOR (same commit as the DOM change — Pitfall 1): tests/e2e/chrome/library-tidy.spec.ts is a structural spec (it never drives ingestion — no dialog helper involved). Update it to the post-dissolution DOM: recompute the ordered-regions assertions for the retired add section (header Add button → search ordering; the re-homed .status now follows the list region, not the add controls), re-scope the load-status locator from the old add-section descendant selector to the re-homed element's new direct-child position, and move the wide/narrow geometry assertions from the retired add section's boundingBox to the header Add button vs `ul.library-list`. Rewrite the head-comment scoping note so the whole file (comments included) is free of the retired selector names. The .status role/aria/copy contract assertions stay byte-identical (Pitfall 7).
  </action>
  <acceptance_criteria>
  - src/ingestion/library/LibraryView.tsx contains `library-add-button`, `aria-haspopup="dialog"`, `aria-expanded={addOpen}`, and an `<AddDialog` mount with `onBookAdded` wired to a setRefreshKey bump
  - The `.status` load region survives with identical attributes and copy: `rg -n "Opening article" src/ingestion/library/LibraryView.tsx` matches, and the element keeps role="status" aria-live="polite" aria-atomic="true"
  - `rg -n "library-section-add|IngestControl" src/` finds nothing; src/ingestion/IngestControl.tsx and tests/component/IngestControl.test.tsx no longer exist
  - `rg -n "library-section-add|ingest-control" tests/e2e/chrome/library-tidy.spec.ts` finds nothing (comments rewritten too) while its .status role/aria-live/aria-atomic assertions remain; `npx playwright test tests/e2e/chrome/library-tidy.spec.ts --project=chromium` exits 0
  - src/app.css contains a `dialog.add-dialog` rule with `margin: auto` and `var(--surface-raised)`; `rg -n ".ingest-control" src/app.css` finds nothing
  - EMPTY_COPY.all.body equals `Nothing saved yet. Use the Add to Library button to begin.` with the heading `Your library is empty` unchanged
  - `npx vitest run tests/component` exits 0 (no dangling component-test imports)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/component && npx playwright test tests/e2e/chrome/library-tidy.spec.ts --project=chromium</automated>
  </verify>
  <done>Trigger + dissolution + mount + styles + retirement + library-tidy re-anchor landed atomically; component suite green and the re-anchored library-tidy spec green on chromium (Task 2 migrates the eleven ingestion-driving specs to the dialog).</done>
</task>

<task type="auto">
  <name>Task 2: Shared openAddDialog/pickSource helper + migrate the 11 ingestion-driving specs</name>
  <files>tests/e2e/library/add-dialog.ts, tests/e2e/ingestion/happy-path.spec.ts, tests/e2e/pdf-intake.spec.ts, tests/e2e/epub-intake.spec.ts, tests/e2e/library/markdown-upload.spec.ts, tests/e2e/library/upload-queue.spec.ts, tests/e2e/library/browse-open.spec.ts, tests/e2e/library/remove-cascade.spec.ts, tests/e2e/library/search-tag-filter.spec.ts, tests/e2e/a11y.spec.ts, tests/e2e/portability/round-trip.spec.ts, tests/e2e/portability/core-flow-spine.spec.ts</files>
  <read_first>
  - .planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md (Blast radius table — the exact driving query per spec)
  - tests/e2e/library/markdown-payload.ts (the non-spec shared-file convention: non-spec filename, never import a .spec.ts)
  - tests/e2e/ingestion/happy-path.spec.ts (describe body L86-130 only — the accessible-name driving pattern L101-102 every spec repeats)
  - tests/e2e/library/upload-queue.spec.ts (the re-pick case L120-129 + .status scoping L47 — the trickiest migration)
  - The remaining blast-radius specs (driving lines only — grep each for ingest-url/ingest-paste/ingest-file/Add pasted article/Add file to find every call site)
  </read_first>
  <action>
  Create tests/e2e/library/add-dialog.ts (non-spec filename — Playwright's testMatch never registers it; markdown-payload.ts convention):

  Export `openAddDialog(page)`: click `page.getByRole("button", { name: "Add to Library" })`, then assert `page.locator("dialog.add-dialog")` is visible. Export `pickSource(page, source: "url" | "paste" | "file")`: check the radio by accessible name Web address / Paste text / Upload file. These two functions centralize every accessible name so a future copy change is a one-file edit.

  Then migrate each of the eleven specs — insert `await openAddDialog(page);` after the Saved-articles heading wait, and `await pickSource(page, "paste" | "file");` where the spec drives a non-URL input. The input/button accessible names and ids are UNCHANGED (Add by URL / Paste HTML or text / ingest-file / Add / Add pasted article / Add file) — only the open step is new. Per-spec specifics:
  - happy-path.spec.ts: open before each URL/paste drive; book/article success assertions unchanged (article → #/article/<id>; epub → stays on Library).
  - pdf-intake / epub-intake / markdown-upload / remove-cascade / round-trip / core-flow-spine: pickSource("file") before each setInputFiles on #ingest-file (setInputFiles works on the hidden-but-mounted input).
  - upload-queue.spec.ts: pickSource("file") for the queue + re-pick cases; re-scope the ingest-status assertions from the retired control selector to `dialog.add-dialog .status`.
  - browse-open.spec.ts + search-tag-filter.spec.ts: open + pickSource("paste") before paste-text seeding.
  - a11y.spec.ts: open the dialog before the ingest-form axe scan region (the scan target becomes the open dialog surface).
  - library-tidy.spec.ts: NO edits in this task — Task 1 already re-anchored its structural assertions in the same commit as the DOM change; this spec never drives ingestion, so it takes no helper import. This task's verify re-runs it green alongside the migrated specs.
  Strengthen-only for everything not enumerated: no other assertion is removed or relaxed.
  </action>
  <acceptance_criteria>
  - tests/e2e/library/add-dialog.ts exists (non-spec filename), exports openAddDialog and pickSource, and no spec imports another spec
  - Each of the eleven listed specs imports the helper and calls openAddDialog before driving ingest inputs (grep each file for openAddDialog)
  - upload-queue's status assertions reference the dialog-scoped status (no retired control selector remains: `rg -n "ingest-control" tests/` finds nothing)
  - No accessible-name/selector churn beyond the open/pick steps and the one documented anchor update in this task (upload-queue scoping; the library-tidy structural re-anchor was Task 1) — `git diff` per spec shows additive step insertions
  - `npx playwright test tests/e2e/ingestion/happy-path.spec.ts tests/e2e/pdf-intake.spec.ts tests/e2e/epub-intake.spec.ts tests/e2e/library/ tests/e2e/portability/round-trip.spec.ts tests/e2e/portability/core-flow-spine.spec.ts tests/e2e/chrome/library-tidy.spec.ts tests/e2e/a11y.spec.ts --project=chromium` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ingestion tests/e2e/library tests/e2e/portability tests/e2e/chrome/library-tidy.spec.ts tests/e2e/a11y.spec.ts --project=chromium</automated>
  </verify>
  <done>All eleven ingestion-driving specs plus the library/portability/a11y directories (and the Task-1-re-anchored library-tidy) pass on chromium through the dialog; the forms are reachable only via the header button; the suite has no reference to the retired control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | The dialog re-uses the hardened /api/ingest + Dexie seams; no new network, storage, or validation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | IngestControl retirement leaving dead ingest code paths | mitigate | Grep gate: no IngestControl references remain in src/ or tests/ (component + test deleted in the same commit) |
| T-16-06 | Tampering (integrity) | .status live region losing its role/aria/copy contract during re-homing | mitigate | Byte-stable discipline + library-tidy anchor updates assert the region's attributes and copy verbatim (Pitfall 7) |
| T-16-08 | Elevation | Shell-header Add trigger creeping in | mitigate | D16-02 locked: the shell stays two destinations; header files untouched (acceptance: Header.tsx diff empty) |
| T-16-09 | Tampering (XSS) | New dialog CSS/markup introducing injection surface | mitigate | Markup is static JSX + controlled inputs; lint:no-danger repo guard unaffected; zero dangerouslySetInnerHTML usage |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx playwright test tests/e2e/ingestion tests/e2e/library tests/e2e/portability tests/e2e/chrome/library-tidy.spec.ts tests/e2e/a11y.spec.ts` green on all 3 engines (the wave-2 sampling gate from 16-VALIDATION)
- `rg -n "IngestControl|ingest-control" src tests` returns nothing
- `git diff src/reader/Header.tsx src/App.tsx` empty (no shell/route changes — D16-01/D16-02)
</verification>

<success_criteria>
- ADD-01: one header-row trigger opens the focused dialog; no permanent forms on the Library page; Library stays mounted (inert) behind the modal
- ADD-03 e2e layer: retry/re-pick (upload-queue), refusal visibility, dedupe refusal all proven through the dialog on chromium
- ADD-04 wiring: article/book success navigation split lands (full focus/zoom matrix proof follows in 16-04)
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- CSS hook classes `.library-add-button`, `dialog.add-dialog` (+ `::backdrop`), `.add-dialog-inner`, `.add-source-picker`, `.add-source-row` (app.css); `.ingest-control` block removed
- LibraryView: `addOpen` state, Add button, AddDialog mount, `.status` re-homed, EMPTY_COPY.all.body new string
- DELETED symbols: `IngestControl` component + its test suite
- `tests/e2e/library/add-dialog.ts` — exports `openAddDialog(page)`, `pickSource(page, source)`
- Migrated/strengthened: 11 ingestion-driving e2e specs + the library-tidy structural re-anchor (Task 1, same commit as the DOM change)
</artifacts_produced>

<output>
Create `.planning/phases/16-organized-library-and-focused-add-flow/16-03-SUMMARY.md` when done
</output>
