---
phase: 16-organized-library-and-focused-add-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/library/LibraryView.tsx
  - src/app.css
  - tests/e2e/library/search-tag-filter.spec.ts
  - tests/e2e/library/reading-views.spec.ts
autonomous: true
requirements: [LIB-09, LIB-10]
must_haves:
  truths:
    - "Filtering the selected view to zero visible rows shows a calm no-matches line with a Clear search and filters control — never the membership empty state (D16-13)"
    - "Activating Clear search and filters resets both the search query and the active tag, restoring the view's rows"
    - "A membership-empty view still shows its EMPTY_COPY heading and body unchanged — filtered-out is not an empty view (D14-26 holds)"
    - "The Continue Reading strip is visible on the All view and absent on Unread, In Progress, and Finished (D16-14)"
    - "View-switcher counts remain membership totals while a search is active (D16-16)"
  artifacts:
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "No-matches render branch beside the membership-empty branch + All-only strip gate"
      contains: "library-no-matches"
    - path: "src/app.css"
      provides: "No-matches line + clear-filters button styles on POLISH-07 tokens"
      contains: "library-clear-filters"
    - path: "tests/e2e/library/search-tag-filter.spec.ts"
      provides: "No-matches + clear-filters e2e cases"
    - path: "tests/e2e/library/reading-views.spec.ts"
      provides: "Strip view-gating + counts-pure-mid-search e2e cases"
  key_links:
    - from: "Clear search and filters button (LibraryView.tsx)"
      to: "LibraryView query/activeTag state"
      via: "onClick resets both setQuery(\"\") and setActiveTag(null)"
      pattern: "setQuery\\(\"\"\\)"
    - from: "LibraryView.tsx strip section"
      to: "ContinueReadingStrip.tsx"
      via: "rendered only when view === \"all\"; strip component byte-unchanged (D16-15)"
      pattern: "view === \"all\""
---

<objective>
Add the honest filtered-to-zero feedback layer LIB-09 requires and gate the Continue Reading strip to the All view for LIB-10. Filter composition itself is already correct (state filter runs before query/tag — Phase 14); this plan adds only the missing reader-facing feedback: a calm no-matches line distinct from membership empty states, a clear-filters affordance, and the All-only strip placement.

Purpose: LIB-09 (no contradictory filter feedback) + LIB-10 (strip complements, never duplicates/displaces) per D16-13/D16-14/D16-15/D16-16.
Output: LibraryView no-matches branch + strip gate, POLISH-07-token styles, strengthened search-tag-filter + reading-views specs.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-CONTEXT.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-RESEARCH.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-VALIDATION.md
@src/ingestion/library/libraryFilter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-matches branch + clear-filters + All-only strip gate in LibraryView</name>
  <files>src/ingestion/library/LibraryView.tsx, src/app.css</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (full — the membership-empty conditional L620-631, strip section L553-555, filter derivation L462-527, counts L490-502)
  - src/ingestion/library/libraryFilter.ts (filterLibrary/filterBooks — unchanged, composition already correct)
  - src/app.css (library-section + .status + token blocks; POLISH-07 discipline)
  - .planning/phases/16-organized-library-and-focused-add-flow/16-RESEARCH.md (Pattern 5 — no-matches sketch; Pattern 6 — strip gating)
  </read_first>
  <action>
  Two scoped edits to LibraryView.tsx plus styles (per D16-13, D16-14, D16-15, D16-16):

  1. STRIP GATE (D16-14): wrap the existing `<section className="library-section library-section-continue">` (L553-555) in `{view === "all" && (…)}`. ContinueReadingStrip.tsx stays byte-unchanged (D16-15) — the gate lives in LibraryView only.

  2. NO-MATCHES BRANCH (D16-13): in the ELSE arm of the L620-631 conditional (the arm rendering `<ul className="library-list">`), render — after the ul — a block keyed on ALL of: `status === "ready"` AND membership non-empty (`viewArticles.length > 0 || viewBooks.length > 0`) AND filtered-to-zero (`visibleItems.length === 0 && visibleBooks.length === 0`). The block is a `<p className="library-no-matches">` containing the literal text `Nothing in this view matches your filters.` followed by a `<button type="button" className="library-clear-filters">` labeled `Clear search and filters`. Its onClick resets BOTH filters: `setQuery("")` and `setActiveTag(null)` (both — simpler and honest). Do NOT touch the membership-empty branch, EMPTY_COPY values, or the counts derivation (`stateCounts`/`allCount` already derive pre-filter — D16-16 zero code change).

  3. STYLES in app.css: add `.library-no-matches` (calm body text on `var(--font-ui)` sizing, `var(--space-*)` margins consistent with library sections) and `.library-clear-filters` (quiet-button treatment reusing the existing secondary-button pattern; `min-height: var(--touch)`; global focus-visible ring applies). No motion properties. No new tokens — consume existing POLISH-07 custom properties only.
  </action>
  <acceptance_criteria>
  - src/ingestion/library/LibraryView.tsx contains `library-no-matches`, `library-clear-filters`, and the literal line copy `Nothing in this view matches your filters.`
  - The no-matches block's condition references `visibleItems` and `visibleBooks` (post-filter) AND gates on `status === "ready"` with membership non-empty via `viewArticles`/`viewBooks` — the membership-empty ternary arm (EMPTY_COPY render) is unchanged
  - `rg -n 'view === "all"' src/ingestion/library/LibraryView.tsx` matches the ContinueReadingStrip section gate
  - `git diff --stat src/ingestion/library/ContinueReadingStrip.tsx` is empty (byte-unchanged, D16-15)
  - `rg -c 'EMPTY_COPY' src/ingestion/library/LibraryView.tsx` still finds the constant with all four view entries intact
  - src/app.css contains `.library-no-matches` and `.library-clear-filters` rules consuming `var(--` tokens (no hardcoded px palette values)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/reading-views.spec.ts tests/e2e/library/progress-recent.spec.ts --project=chromium</automated>
  </verify>
  <done>No-matches branch renders only for membership-non-empty + filtered-to-zero + ready; clear-filters resets query and tag; strip renders on All only; existing specs (reading-views, progress-recent — strip assertions run on #/) stay green on chromium.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen search-tag-filter + reading-views specs (no-matches, clear-filters, strip gating, counts-pure)</name>
  <files>tests/e2e/library/search-tag-filter.spec.ts, tests/e2e/library/reading-views.spec.ts</files>
  <read_first>
  - tests/e2e/library/search-tag-filter.spec.ts (full — existing seeding + filter-driving patterns to extend)
  - tests/e2e/library/reading-views.spec.ts (full — view navigation + counts/empty-state assertions to extend)
  - src/ingestion/library/LibraryView.tsx (Task 1 result — branch conditions + class names)
  - tests/e2e/library/progress-recent.spec.ts (the All-view strip assertion precedent — L273 area)
  </read_first>
  <behavior>
  - search-tag-filter: query-only filtered-to-zero → `.library-no-matches` visible AND the membership-empty heading (EMPTY_COPY) absent; tag-only filtered-to-zero → same; combined → same
  - search-tag-filter: click "Clear search and filters" → no-matches line gone, view rows visible again, search input empty, tag chip unselected
  - search-tag-filter: membership-empty view (no seeds) with a query typed → EMPTY_COPY heading still shown, no-matches line absent (D14-26 regression guard)
  - reading-views: with an in-progress seeded article, `.library-section-continue` (or its heading) absent on `#/unread`, `#/in-progress`, `#/finished`; visible on `#/`
  - reading-views: type a query that matches nothing while on `#/` → switcher accessible names still show membership totals (e.g. an "Unread (N)" count unchanged from its pre-search value) — D16-16
  </behavior>
  <action>
  Add the behavior cases above to the two specs, strengthening only (no existing assertion removed or relaxed — the D6-12/D16 strengthen-only rule). Reuse each spec's existing seeding helpers and library-open pattern (`page.goto(BASE + "/#/…")` + `Saved articles` heading wait). For the counts-pure case, capture the switcher's accessible name before typing the query and assert it is identical after the list narrows. For strip gating, assert absence via `toBeHidden()`/count 0 on the `.library-section-continue` locator (the whole section is now conditionally mounted, so it is absent from the DOM on non-All routes).
  </action>
  <acceptance_criteria>
  - tests/e2e/library/search-tag-filter.spec.ts contains `library-no-matches` and a case asserting the clear-filters button restores rows (references `Clear search and filters`)
  - tests/e2e/library/search-tag-filter.spec.ts contains a membership-empty-with-query case asserting the EMPTY_COPY heading still renders (D14-26 guard)
  - tests/e2e/library/reading-views.spec.ts contains strip-absence assertions for unread, in-progress, and finished routes and a strip-visible assertion for `#/`
  - tests/e2e/library/reading-views.spec.ts contains a counts-pure-mid-search assertion comparing a switcher accessible name before/after a zero-match query
  - No pre-existing test name or assertion in either file was deleted (strengthen-only)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/search-tag-filter.spec.ts tests/e2e/library/reading-views.spec.ts</automated>
  </verify>
  <done>Both specs pass on chromium, firefox, and webkit with the new cases; LIB-09 feedback behavior and LIB-10 placement are proven end-to-end in all three engines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure client render branch over already-validated local state; no new input crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering (XSS) | no-matches line rendering query/tag strings | mitigate | Render query/tag-derived strings only as React text children (escaped by construction — the libraryFilter T-8-12 analysis holds); never introduce HTML injection to render the line |
| T-16-02 | Information disclosure | clear-filters resetting reader state | accept | Resets local UI filter state only; no data mutation, no persistence write |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan (RESEARCH §Package Legitimacy Audit — no new packages) |
</threat_model>

<verification>
- `npx playwright test tests/e2e/library/search-tag-filter.spec.ts tests/e2e/library/reading-views.spec.ts` green on all 3 engines
- `git diff --stat src/ingestion/library/ContinueReadingStrip.tsx` empty (D16-15)
- Membership empty states unchanged (EMPTY_COPY values byte-identical; D14-26 regression case green)
</verification>

<success_criteria>
- LIB-09: filtered-to-zero shows the distinct no-matches line + working clear-filters; membership-empty views keep their own copy; counts stay membership-pure mid-search
- LIB-10: strip on All only; strip surface byte-unchanged
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- `LibraryView` no-matches render branch (clear-filters onClick handler resetting `query` + `activeTag`)
- CSS hook classes `.library-no-matches`, `.library-clear-filters` (app.css)
- All-only strip gate (`view === "all"` conditional around `.library-section-continue`)
- Strengthened spec cases in `tests/e2e/library/search-tag-filter.spec.ts` + `tests/e2e/library/reading-views.spec.ts`
</artifacts_produced>

<output>
Create `.planning/phases/16-organized-library-and-focused-add-flow/16-01-SUMMARY.md` when done
</output>
