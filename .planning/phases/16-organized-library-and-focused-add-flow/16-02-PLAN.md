---
phase: 16-organized-library-and-focused-add-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/ingestCopy.ts
  - src/ingestion/IngestControl.tsx
  - src/ingestion/AddDialog.tsx
  - tests/component/AddDialog.test.tsx
  - tests/unit/pdf-copy.test.ts
  - tests/unit/epub-copy.test.ts
autonomous: true
requirements: [ADD-02, ADD-03]
must_haves:
  truths:
    - "mapReasonToCopy and bytesToBase64 live in src/ingestion/ingestCopy.ts byte-identically; the two byte-pinning copy tests pass with only their import paths changed"
    - "AddDialog renders a fieldset/legend 3-way picker (Web address / Paste text / Upload file) and only the selected source's input is visible (D16-05)"
    - "The dialog always opens on Web address — no last-used-source memory (D16-08)"
    - "Typed URL/paste text and a picked file survive source switches within one dialog session (D16-07)"
    - "Dedupe-refuse is a calm message only ('Already in your library.') with no save and no Open-it action (D16-09); all refusals route through mapReasonToCopy"
    - "Cancel and submit controls are disabled while a submission is in flight (D16-10)"
  artifacts:
    - path: "src/ingestion/ingestCopy.ts"
      provides: "mapReasonToCopy + bytesToBase64, extracted byte-identical"
      exports: ["mapReasonToCopy", "bytesToBase64"]
    - path: "src/ingestion/AddDialog.tsx"
      provides: "The focused Add workflow dialog component (not yet mounted in the app — Plan 16-03 mounts it)"
      exports: ["AddDialog", "AddDialogProps", "AddDialogSource"]
    - path: "tests/component/AddDialog.test.tsx"
      provides: "Component-layer coverage migrated from IngestControl.test.tsx + picker/switch/default cases"
  key_links:
    - from: "src/ingestion/AddDialog.tsx"
      to: "src/ingestion/ingestCopy.ts"
      via: "import { mapReasonToCopy, bytesToBase64 } — refusal copy + base64, no fork"
      pattern: "from \"./ingestCopy\""
    - from: "src/ingestion/AddDialog.tsx"
      to: "src/ingestion/IngestionClient.ts + src/ingestion/LibrarySource.ts + src/persistence/booksStore.ts"
      via: "the five ingest functions + IngestionError + has/save + hasBook/saveBook reused verbatim (D7-07 dedupe seams)"
      pattern: "dexieLibrarySource\\.has"
    - from: "src/ingestion/AddDialog.tsx cancel listener"
      to: "submittingRef live mirror"
      via: "cancel handler gated on submittingRef.current rewritten every render (Pitfall 3 — BookRemoveConfirm L98-104 + LibraryView L236 discipline)"
      pattern: "submittingRef\\.current"
---

<objective>
Extract the byte-pinned refusal-copy module and build the AddDialog component: a native dialog (showModal) structural clone of the BookRemoveConfirm lineage hosting IngestControl's four-state submission spine verbatim behind a controlled 3-way source picker. Component-tested in isolation; mounting into LibraryView is Plan 16-03.

Purpose: ADD-02 (every source available, only relevant inputs visible) + ADD-03 component layer (recovery without losing input, duplicates, or honest refusal reasons) per D16-01, D16-05..D16-11.
Output: src/ingestion/ingestCopy.ts, src/ingestion/AddDialog.tsx, tests/component/AddDialog.test.tsx, copy-test import updates.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-CONTEXT.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-RESEARCH.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-VALIDATION.md
@src/ingestion/library/BookRemoveConfirm.tsx
@src/reader/SettingsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract mapReasonToCopy + bytesToBase64 into src/ingestion/ingestCopy.ts</name>
  <files>src/ingestion/ingestCopy.ts, src/ingestion/IngestControl.tsx, tests/unit/pdf-copy.test.ts, tests/unit/epub-copy.test.ts</files>
  <read_first>
  - src/ingestion/IngestControl.tsx (full — mapReasonToCopy L55-97, bytesToBase64 L108-116, the reason type import, doc comments)
  - tests/unit/pdf-copy.test.ts (full — the import at L12 + EXPECTED_PDF_COPY byte-pin table)
  - tests/unit/epub-copy.test.ts (full — same structure)
  </read_first>
  <action>
  Create src/ingestion/ingestCopy.ts (RESEARCH A3 recommendation): move `mapReasonToCopy(reason: IngestionFailureReason): string` and `bytesToBase64(bytes: Uint8Array): string` out of IngestControl.tsx BYTE-IDENTICALLY — same signatures, same doc comments, same string table (the 20-reason calm DOC-06 catalog is load-bearing product surface pinned by two tests). Export BOTH (bytesToBase64 becomes exported; it was private). Then:
  1. IngestControl.tsx: delete the two local definitions, add `import { mapReasonToCopy, bytesToBase64 } from "./ingestCopy";` — the component keeps working unchanged until Plan 16-03 retires it.
  2. tests/unit/pdf-copy.test.ts + tests/unit/epub-copy.test.ts: change ONLY the import path from `../../src/ingestion/IngestControl` to `../../src/ingestion/ingestCopy`. The EXPECTED_* copy tables stay untouched — byte pins must hold.
  </action>
  <acceptance_criteria>
  - src/ingestion/ingestCopy.ts exists and exports `mapReasonToCopy` and `bytesToBase64`
  - `rg -c "mapReasonToCopy" src/ingestion/IngestControl.tsx` returns only the import line (no local definition: `rg -n "function mapReasonToCopy|function bytesToBase64" src/ingestion/IngestControl.tsx` finds nothing)
  - tests/unit/pdf-copy.test.ts and tests/unit/epub-copy.test.ts import from `../../src/ingestion/ingestCopy`; their EXPECTED_* tables are byte-unchanged (`git diff` shows import-line-only changes in those files)
  - `npx vitest run tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts tests/component/IngestControl.test.tsx` exits 0 (extraction is behavior-neutral; the still-alive IngestControl component test proves it)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts tests/component/IngestControl.test.tsx</automated>
  </verify>
  <done>ingestCopy.ts is the single home of the refusal-copy map + chunked base64; both copy byte-pins green from the new path; IngestControl still functions via imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AddDialog component (test-first) — dialog shell, 3-way picker, submission spine</name>
  <files>src/ingestion/AddDialog.tsx, tests/component/AddDialog.test.tsx</files>
  <read_first>
  - src/ingestion/IngestControl.tsx (full — the submission spine L119-351 carried near-verbatim: four-state machine, handleSubmit url/paste, handleFileSubmit extension dispatch + caps, dedupe arms, resetFilePick L148-151, status region L450-459, hasFile mirror)
  - src/ingestion/library/BookRemoveConfirm.tsx (full — the dialog shell clone: open-prop sync L63-86, close+cancel listeners L92-111)
  - src/reader/SettingsPanel.tsx (L100-179 dialog effect + L360-429 the fieldset/radio discipline)
  - src/ingestion/library/LibraryView.tsx (L236 liveContextRef — the live-ref mirror discipline)
  - tests/component/IngestControl.test.tsx (full — mock structure L25-54 + the cases to migrate)
  - .planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md (AddDialog pattern section — verbatim shapes)
  </read_first>
  <behavior>
  - Migrated from IngestControl.test.tsx (keep shape, rename suite): "Fetching article…" announced in the dialog status region while submitting; ingestUrl→has→save call ordering; dedupe-refuse shows "Already in your library." and never calls save; IngestionError reason → mapReasonToCopy calm copy; unknown error → server-error copy; no-jargon guard on refusal copy
  - Picker: dialog renders a fieldset with legend "Add from" and exactly 3 radios named Web address / Paste text / Upload file; with source=url only the URL input (label "Add by URL") is visible — paste input absent from the tree, file input present but hidden-attribute'd (Pattern 3a)
  - Switch preservation (D16-07): type into URL field → switch to Paste text → switch back → URL value intact; pick a file (fireEvent change on input#ingest-file) → switch to Web address → switch back → hasFile still true / Add file still enabled
  - Always Web address (D16-08): open → url radio checked; cancel → reopen → url radio checked again and URL field empty (fresh session state)
  - In-flight (D16-10): while status=submitting, the Cancel button and the active submit button are disabled
  - Success arms (D16-12): article success calls onCancel() then sets window.location.hash to `#/article/<id>` (stubbed setter, IngestControl.test L81-90 precedent); book success calls onCancel() then onBookAdded()
  </behavior>
  <action>
  Write tests/component/AddDialog.test.tsx FIRST (migrate the mock structure from IngestControl.test.tsx: vi.mock IngestionClient with all five functions + IngestionError class, mock dexieLibrarySource {has, save}, mock ../persistence/booksStore {hasBook, saveBook}; stub the window.location.hash setter for success navigation). Run it RED (AddDialog.tsx does not exist yet), then implement:

  src/ingestion/AddDialog.tsx — props contract (Plan 16-03 wires exactly this): `export type AddDialogSource = "url" | "paste" | "file"; export type AddDialogProps = { open: boolean; onCancel: () => void; onBookAdded: () => void; }`.

  Dialog shell — BookRemoveConfirm clone lineage, own CSS hooks (dialog.add-dialog, .add-dialog-inner, .add-source-picker, .add-source-row — never library-remove-confirm classes; three dialogs will co-mount in 16-03):
  1. Open-prop sync effect with idempotent guards; on open: capture document.activeElement into triggerRef, dlg.showModal(), then explicit focus on `[data-initial-focus]` (place it on the Web address radio) — WebKit does not auto-focus (02-01). On every false→true transition RESET session state: source to "url", urlValue to "", htmlValue to "", file pick cleared via the G2 seam (D16-08 — no memory).
  2. `close` listener restores triggerRef focus. `cancel` listener gated on a live `submittingRef` mirror (`submittingRef.current = status === "submitting"` rewritten every render — Pitfall 3): while submitting → preventDefault + return (stay open, no state churn); otherwise → preventDefault + onCancel() (routes through the React open-prop mirror — the 09-06 wedge lesson).
  3. NO dialog-method form wrapper (Chromium focus-trap interference, SettingsPanel L349-353 comment) — every control type="button" or prevented submit.

  Source picker — SettingsPanel L370-402 discipline: fieldset.add-source-picker with legend "Add from"; three label-wrapped controlled radios `name="source"` (checked from state; never uncontrolled — Firefox persistence quirk, Pitfall 9).

  Input groups — preserve the exact accessible names/ids the e2e suite drives: URL input id `ingest-url` label "Add by URL" + button "Add"; paste textarea id `ingest-paste` label "Paste HTML or text" + button "Add pasted article"; file input id `ingest-file` label "Upload a file" + button "Add file" + helper hint "Accepts .md, .html, PDF, and EPUB books". Only the selected source's group is visible; the file input stays ALWAYS MOUNTED with the `hidden` attribute when source !== "file" (Pattern 3a — FileList survives switches; fileInputRef + G2 resetFilePick seam work byte-unchanged). URL/paste inputs unmount freely (lifted state preserves their values — D16-07).

  Submission spine — carry verbatim from IngestControl L119-351: the idle/submitting/success/error machine; handleSubmit("url"|"paste") and handleFileSubmit with extension dispatch (.md/.html/.pdf/.epub), per-format caps (PDF_MAX_BYTES/EPUB_MAX_BYTES/5MB), bytesToBase64, has()/hasBook() dedupe BEFORE save/saveBook (D7-07; refusal-only per D16-09), G2 resetFilePick at every file-arm terminal outcome (D16-11 — retry = re-pick), URL/paste text NEVER cleared by an error (D16-11). D16-12 success adaptation: article success → call onCancel() FIRST, then `window.location.hash = \`#/article/${id}\``; book success → onCancel() then onBookAdded() (skip disclosure remains durable on the BookRow, D12-11 — no in-dialog copy needed). Cancel button "Cancel" (type="button", disabled while submitting); status live region `role="status" aria-live="polite" aria-atomic="true"` mirroring the IngestControl L450-459 shape.
  </action>
  <acceptance_criteria>
  - src/ingestion/AddDialog.tsx exports `AddDialog`, `AddDialogProps`, `AddDialogSource`; contains showModal, `data-initial-focus`, addEventListener for both "close" and "cancel", and `submittingRef`
  - The picker fieldset has legend "Add from" and radios with accessible names Web address / Paste text / Upload file; radios are controlled (`checked={` present)
  - The file input carries the `hidden` attribute when source !== "file" and id `ingest-file`; URL/paste inputs keep ids `ingest-url`/`ingest-paste` and labels "Add by URL"/"Paste HTML or text"
  - `rg -n "from \"./ingestCopy\"" src/ingestion/AddDialog.tsx` matches (copy + base64 imported, not forked); `rg -c "hasBook" src/ingestion/AddDialog.tsx` ≥ 1 (book dedupe seam present)
  - `rg -n "resetFilePick" src/ingestion/AddDialog.tsx` matches (G2 seam present)
  - `npx vitest run tests/component/AddDialog.test.tsx` exits 0 with all migrated + new cases
  - `rg -n "method=\"dialog\"" src/ingestion/AddDialog.tsx` finds nothing (no dialog-method form wrapper)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/component/AddDialog.test.tsx tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts</automated>
  </verify>
  <done>AddDialog passes the full migrated + new component suite: picker semantics, only-selected-input visibility, switch preservation, always-Web-address reset, in-flight control disabling, dedupe refusal-only, calm copy, success-arm callback ordering. Focus-trap/Esc/geometry deliberately NOT claimed here (jsdom-blind — Pitfall 5; proven by Plan 16-04 in real browsers).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → /api/ingest | Existing boundary, reused verbatim: IngestionClient POSTs validated payloads; typed refusals return |
| reader → IndexedDB | Existing dedupe/save seams (has/hasBook before save/saveBook) reused, not forked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering (integrity) | mapReasonToCopy move drifting copy strings | mitigate | Byte-identical move enforced by pdf-copy/epub-copy byte-pin tables (import-path-only test edits); `git diff` of EXPECTED_* tables must be empty |
| T-16-04 | Tampering (integrity) | Dedupe-refuse regression during the spine move | mitigate | Component test: dedupe arm asserts save/saveBook NOT called after has()/hasBook() returns true (migrated no-write case) |
| T-16-05 | DoS | File-arm size-cap bypass during refactor | mitigate | Extension-aware caps (PDF_MAX_BYTES/EPUB_MAX_BYTES/5MB) carried verbatim; migrated cap-refusal component cases stay green |
| T-16-06 | Tampering (XSS) | Dialog rendering refusal/echo copy | mitigate | All copy renders as React text children; lint:no-danger repo guard unaffected; zero innerHTML usage in AddDialog |
| T-16-07 | Tampering (integrity) | Duplicate submission via mid-flight dismissal | mitigate | D16-10: cancel listener preventDefault while submittingRef.current true + Cancel/submit disabled during submitting (component-asserted; 3-engine e2e proof in 16-04) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run tests/component/AddDialog.test.tsx tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts` exits 0
- `npx vitest run tests/component/IngestControl.test.tsx` still green (component alive until 16-03, now importing from ingestCopy)
- Byte-pin discipline: `git diff tests/unit/pdf-copy.test.ts tests/unit/epub-copy.test.ts` shows import-line-only changes
</verification>

<success_criteria>
- ADD-02 component layer: 3-way picker, only-selected-input visible, combined file picker with extension dispatch preserved
- ADD-03 component layer: input preserved across switches and failures, G2 retry discipline, dedupe refusal-only, calm copy byte-pinned from the new module home
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- `src/ingestion/ingestCopy.ts` — exports `mapReasonToCopy`, `bytesToBase64` (new module)
- `src/ingestion/AddDialog.tsx` — exports `AddDialog` (component), `AddDialogProps`, `AddDialogSource` (types); CSS hook classes `dialog.add-dialog`, `.add-dialog-inner`, `.add-source-picker`, `.add-source-row`; internal `submittingRef` live mirror + `resetFilePick` seam
- `tests/component/AddDialog.test.tsx` — new component suite (migrated + picker cases)
- Copy tests re-homed to the new import path (pdf-copy/epub-copy)
</artifacts_produced>

<output>
Create `.planning/phases/16-organized-library-and-focused-add-flow/16-02-SUMMARY.md` when done
</output>
