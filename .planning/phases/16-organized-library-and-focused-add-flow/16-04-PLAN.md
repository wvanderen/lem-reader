---
phase: 16-organized-library-and-focused-add-flow
plan: 04
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - tests/e2e/library/focused-add.spec.ts
  - tests/e2e/a11y.spec.ts
  - tests/e2e/reflow.spec.ts
  - tests/e2e/high-zoom.spec.ts
autonomous: true
requirements: [ADD-04, ADD-01, ADD-02]
must_haves:
  truths:
    - "Focus moves into the dialog on open (explicit initial focus on the Web address radio, WebKit included) and Tab cycles within the dialog (native trap)"
    - "Esc closes the dialog when idle and focus restores to the Add to Library trigger; Esc while a submission is in flight leaves the dialog open and still submitting (D16-10)"
    - "Reopening the dialog always shows Web address selected (D16-08); a picked file survives switching sources and back (D16-07)"
    - "Article success closes the dialog first, then opens #/article/<id> in the reader (close-then-navigate ordering, Pitfall 6)"
    - "The open dialog is operable with no horizontal overflow at 320px and at 400% zoom"
    - "An axe scan of the open dialog reports no violations; radio arrow-key navigation and visible focus work"
    - "The full suite passes: npm run test exits 0 in one invocation on a fresh dev server"
  artifacts:
    - path: "tests/e2e/library/focused-add.spec.ts"
      provides: "3-engine ADD-04 proof: focus, dismissal blocking, reopen default, switch preservation, success ordering"
    - path: "tests/e2e/a11y.spec.ts"
      provides: "Dialog-open axe scan + keyboard walkthrough cases"
    - path: "tests/e2e/reflow.spec.ts"
      provides: "Dialog-open 320px reflow case"
    - path: "tests/e2e/high-zoom.spec.ts"
      provides: "Dialog-open 400% zoom case"
  key_links:
    - from: "tests/e2e/library/focused-add.spec.ts"
      to: "tests/e2e/library/add-dialog.ts"
      via: "import { openAddDialog, pickSource } — shared helper, central accessible names"
      pattern: "openAddDialog"
    - from: "focused-add Esc-during-submit case"
      to: "/api/ingest"
      via: "page.route delayed-fulfill holds status=submitting; assert dialog stays open; then settle the request"
      pattern: "page\\.route"
---

<objective>
Prove ADD-04 in real browsers (component tests deliberately cannot — jsdom has no dialog top layer, Pitfall 5): focus management, dismissal blocking, reopen defaults, pick preservation, success-navigation ordering, and narrow-width/high-zoom behavior of the integrated AddDialog — then run the phase's honest full-suite gate.

Purpose: ADD-04 end-to-end + the phase exit gate (fresh-server honest run — the 15-04 webkit-starvation lesson).
Output: tests/e2e/library/focused-add.spec.ts, strengthened a11y/reflow/high-zoom specs, green full suite.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-CONTEXT.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-RESEARCH.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-PATTERNS.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-VALIDATION.md
@.planning/phases/16-organized-library-and-focused-add-flow/16-03-SUMMARY.md
@tests/e2e/panel-keyboard.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: focused-add.spec.ts — focus, dismissal-blocking, defaults, preservation, success ordering (3 engines)</name>
  <files>tests/e2e/library/focused-add.spec.ts</files>
  <read_first>
  - tests/e2e/panel-keyboard.spec.ts (full — the dialog focus/trap/Esc/restore e2e precedent; clone its document.activeElement assertion style)
  - tests/e2e/library/add-dialog.ts (the helper being used)
  - src/ingestion/AddDialog.tsx (the component under test — status machine, data-initial-focus target, cancel gating)
  - tests/e2e/ingestion/happy-path.spec.ts (success-path driving + any existing failure-simulation route fixtures to reuse)
  - tests/e2e/library/upload-queue.spec.ts (file-pick + refusal-simulation patterns)
  </read_first>
  <action>
  Create tests/e2e/library/focused-add.spec.ts covering, on chromium + firefox + webkit:
  1. FOCUS IN: openAddDialog → assert document.activeElement is the Web address radio (the data-initial-focus target) — the WebKit 02-01 proof.
  2. TRAP + RESTORE: Tab cycles within the open dialog (focus never lands on the library behind it); Esc when idle → dialog closed AND focus restored to the Add to Library button.
  3. REOPEN DEFAULT (D16-08): open → pickSource "file" → cancel → reopen → Web address radio checked and URL field empty.
  4. SWITCH PRESERVATION (D16-07): pickSource "file" → setInputFiles on #ingest-file → pickSource "url" → pickSource "file" → the pick is retained (Add file button enabled / retained-file affordance visible).
  5. ESC BLOCKED WHILE SUBMITTING (D16-10): hold the request with a delayed-fulfill `page.route("**/api/ingest", …)` (reuse existing route-fixture patterns for the response shape), submit a URL, wait for the submitting status text, press Escape → assert the dialog is STILL open and status still submitting; then let the route settle (fulfill the refusal/error shape) → dialog returns to error state with retry available.
  6. CLOSE-THEN-NAVIGATE (Pitfall 6): submit a successful paste/URL ingest (existing seed payloads) → dialog closes, hash becomes `#/article/<id>`, reader opens.
  7. RADIO ARROWS: with the picker focused, ArrowDown/ArrowUp move checked state among Web address / Paste text / Upload file and only the selected source's input is visible after each move.
  </action>
  <acceptance_criteria>
  - tests/e2e/library/focused-add.spec.ts exists, imports openAddDialog/pickSource from ./add-dialog, and contains cases for: initial focus on the Web address radio, Tab trap + Esc idle-close with trigger focus restore, reopen-on-Web-address, file-pick survival across source switches, Esc-blocked-while-submitting (dialog still open), article-success close-then-navigate, radio arrow-key cycling
  - The Esc-blocked case asserts BOTH dialog openness and the submitting status AFTER pressing Escape, and subsequently proves recovery when the request settles
  - `npx playwright test tests/e2e/library/focused-add.spec.ts` exits 0 on chromium, firefox, and webkit
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/focused-add.spec.ts</automated>
  </verify>
  <done>ADD-04 behaviors (focus, dismissal, defaults, preservation, ordering) proven in all three engines; D16-07/08/10/12 e2e layer closed.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen a11y (dialog-open axe scan + keyboard walkthrough), reflow + high-zoom (dialog-open states)</name>
  <files>tests/e2e/a11y.spec.ts, tests/e2e/reflow.spec.ts, tests/e2e/high-zoom.spec.ts</files>
  <read_first>
  - tests/e2e/a11y.spec.ts (full — existing AxeBuilder usage + the migrated ingest-form scan case to extend; keyboard-walkthrough pattern)
  - tests/e2e/reflow.spec.ts (full — the 320px no-horizontal-overflow invariant + how it walks surfaces)
  - tests/e2e/high-zoom.spec.ts (full — the 400% zoom harness shape and its no-content-loast assertions)
  - tests/e2e/library/add-dialog.ts (helper)
  </read_first>
  <action>
  Strengthen-only additions (no existing assertion removed):
  1. a11y.spec.ts: add a case that opens the Add dialog (openAddDialog) and runs the AxeBuilder scan against the open dialog surface — expect zero violations; extend the keyboard walkthrough pattern with the picker (arrow keys between radios as claimed in focused-add) and visible focus-ring presence on dialog controls.
  2. reflow.spec.ts: add a dialog-open case at 320px width — openAddDialog, assert no horizontal overflow of the dialog surface (scrollWidth within viewport; the dialog's own overflow:auto scrolls tall content) and that picker + submit are operable.
  3. high-zoom.spec.ts: add a dialog-open case at 400% zoom — same overflow/operability invariants for the open dialog.
  </action>
  <acceptance_criteria>
  - tests/e2e/a11y.spec.ts contains a case scanning the OPEN Add dialog with AxeBuilder expecting no violations
  - tests/e2e/reflow.spec.ts and tests/e2e/high-zoom.spec.ts each contain a dialog-open case asserting no horizontal overflow and operability of the picker/submit at 320px / 400% zoom respectively
  - No pre-existing assertion in the three specs was deleted or relaxed (strengthen-only)
  - `npx playwright test tests/e2e/a11y.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/a11y.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts</automated>
  </verify>
  <done>The open dialog passes the automatable accessibility bar and the narrow/zoom geometry matrix across all three engines.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite phase gate (fresh dev server, one invocation)</name>
  <files>.planning/phases/16-organized-library-and-focused-add-flow/16-04-SUMMARY.md</files>
  <read_first>
  - package.json (the test script composition: unit --run then e2e)
  - playwright.config.ts (webServer/reuseExistingServer behavior — the 15-04 lesson applies)
  - .planning/phases/16-organized-library-and-focused-add-flow/16-VALIDATION.md (phase-gate discipline)
  </read_first>
  <action>
  Kill any long-lived Vite dev server on :5173 first (the 15-04 webkit-starvation lesson: a reused hours-old server produces webkit-only beforeEach starvations that look like regressions). Then run the FULL suite in ONE invocation: `npm run test`. Record the honest passed/failed/skipped numbers in the summary. Triage rule if anything fails: identical-cell failure across engines = regression (fix the code); webkit-only failure that is green in isolation = harness/environment (restart the fresh server before touching specs). Do not subset, grep-filter, or engine-skip the gate run; if the first run exits 1, record it honestly, fix, and re-run to exit 0 (the 04-11/09-07 precedent).
  </action>
  <acceptance_criteria>
  - `npm run test` exits 0 in a single invocation on a fresh dev server
  - The summary records the exact passed/failed/skipped counts of the exit-0 run (and any honestly-recorded prior failing run with its fix)
  - No test was skipped, filtered, or engine-excluded to reach green
  </acceptance_criteria>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Phase 16 exits on the honest gate: full suite green in one invocation; numbers recorded; any flake triaged by the engine-consistency rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-only plan; exercises the existing hardened surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering (integrity) | Dismissal-block bypass enabling duplicate submissions | mitigate | 3-engine focused-add case: Esc during submit leaves the dialog open + submitting, recovery after settle (the D16-10 proof) |
| T-16-10 | Repudiation (honesty of the gate) | Phase gate masked by subsetting/skipping | mitigate | Honest-gate discipline: one invocation, no filters, counts recorded in the summary (04-11/09-07/15-04 precedent) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed |
</threat_model>

<verification>
- `npx playwright test tests/e2e/library/focused-add.spec.ts tests/e2e/a11y.spec.ts tests/e2e/reflow.spec.ts tests/e2e/high-zoom.spec.ts` green on all 3 engines
- `npm run test` exit 0 (fresh server, one invocation) — the phase gate
- Manual-only follow-up documented (not blocking): SR pass on the open dialog per 16-VALIDATION §Manual-Only Verifications (NVDA/VoiceOver announcement of submitting/error copy) — the full SR matrix is Phase 21's ACPT-08 scope
</verification>

<success_criteria>
- ADD-04 fully proven: focus in/out, trap, idle-Esc, blocked-Esc, defaults, preservation, success ordering, 320px/400% geometry, axe-clean — across chromium, firefox, webkit
- Phase 16 honest full-suite gate green
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- `tests/e2e/library/focused-add.spec.ts` — new 3-engine spec (focus/dismissal/defaults/preservation/ordering cases)
- Strengthened cases in `tests/e2e/a11y.spec.ts` (dialog-open axe scan + picker keyboard walkthrough), `tests/e2e/reflow.spec.ts` (dialog-open 320px), `tests/e2e/high-zoom.spec.ts` (dialog-open 400%)
- No new production symbols (test-only plan)
</artifacts_produced>

<output>
Create `.planning/phases/16-organized-library-and-focused-add-flow/16-04-SUMMARY.md` when done
</output>
