---
phase: 09-versioned-export-import
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/portability/conflicts.ts
  - tests/unit/portability/conflicts.test.ts
autonomous: true
requirements: [PORT-02]
must_haves:
  truths:
    - "The dry-run pass detects all five D9-14 conflict kinds against existing local state with skip-by-default and performs zero writes"
    - "Every incoming highlight gets an eager tri-state re-resolution across imported ∪ local-Dexie ∪ bundled-fixture articles before any write (D9-13), with per-article cluster memoization"
    - "A highlight referencing a bundled fixture article resolves against the local fixture copy — it does NOT report orphan while the fixture is present (Pitfall 4 guard)"
    - "Keep-both resolution mints new highlight/note ids and rewrites incoming notes' highlightId FKs so notes follow their highlight (Pitfall 7)"
  artifacts:
    - path: "src/portability/conflicts.ts"
      provides: "dry-run conflict detection + eager re-resolution + bulk per-kind override resolution (Pattern 7's ImportPreview interface)"
      exports: ["ConflictKind", "PerKindOverride", "ConflictSummary", "ImportPreviewData", "ResolvedImportPlan", "detectImportPreview", "resolveImportPlan"]
    - path: "tests/unit/portability/conflicts.test.ts"
      provides: "all-5-kinds detection truth + override matrix + FK-rewrite proof"
  key_links:
    - from: "src/portability/conflicts.ts"
      to: "src/ingestion/LibrarySource.ts"
      via: "dexieLibrarySource.list() for local articles (Zod-validated read path)"
      pattern: "dexieLibrarySource\\.list\\(\\)"
    - from: "src/portability/conflicts.ts"
      to: "src/persistence/{highlightsStore,notesStore,locationStore}.ts"
      via: "loadAllHighlights/loadAllNotes/loadAllLocations for local conflict comparison"
      pattern: "loadAll(Highlights|Notes|Locations)"
    - from: "src/portability/conflicts.ts"
      to: "src/content/normalizeText.ts"
      via: "resolveQuoteSelector + memoized normalizeText/graphemeClusters per article id (REUSE-DO-NOT-FORK)"
      pattern: "resolveQuoteSelector"
    - from: "src/portability/conflicts.ts"
      to: "src/fixtures/index.ts"
      via: "fixtures array as the third article-lookup source (fixtureIds rationale)"
      pattern: "from \"../fixtures\""
---

<objective>
The PORT-02 dry-run core: conflict detection against local state per the D9-14 table, the eager tri-state re-resolution pass (D9-13) with the three-source article lookup, and the pure override-resolution that produces the fully-computed ResolvedImportPlan the atomic apply (Plan 09-04) consumes.

Purpose: SC#2's "dry-run conflict preview" and "skip-by-default conflict resolution with per-entity reader overrides" (reconciled at entity-KIND level per D9-11/D9-14) live here as pure, unit-testable logic — no UI, no transaction yet.
Output: src/portability/conflicts.ts + tests/unit/portability/conflicts.test.ts.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detectImportPreview — 5-kind dry-run + eager tri-state re-resolution</name>
  <files>src/portability/conflicts.ts, tests/unit/portability/conflicts.test.ts</files>
  <read_first>
  - 09-RESEARCH.md Pattern 8 (three-source lookup + memoization) + D9-13/D9-14 in §user_constraints
  - 09-PATTERNS.md §src/portability/conflicts.ts (seen-Set PK merge analog; discriminated result conventions)
  - src/ingestion/LibrarySource.ts (list() read surface + the seen-Set merge at ~L173-190)
  - src/content/normalizeText.ts + src/annotations/resolution.ts (resolveQuoteSelector, normalizeText, graphemeClusters signatures)
  - src/persistence/settingsStore.ts (loadSettings result shape for fresh-device detection)
  - src/persistence/locationStore.ts (LocationRecord shape, compound [articleId+revision] key semantics, savedAt)
  - src/fixtures/index.ts (fixtures export)
  - src/portability/bundle.ts (ExportBundle type)
  - tests/unit/ingestion-tags.test.ts (fake-indexeddb harness + sample builder style)
  </read_first>
  <behavior>
  - local article with same id and different revision + incoming article → conflict kind article-revision
  - local article with same id AND revision but different provenance.originalHtmlHash → kind article-content-divergence
  - incoming highlight whose id exists locally → kind highlight-id; incoming note whose id exists locally → kind note-id
  - incoming location whose [articleId, revision] exists locally → kind location
  - incoming records with NO local PK match count as new (added counts per type in the preview data)
  - an incoming highlight keyed to an article present in the bundle resolves via resolveQuoteSelector and reports confident/ambiguous accordingly; keyed to a bundled fixture article id it resolves against the fixture (NOT orphan) when the fixture exists in src/fixtures; keyed to an article present nowhere → orphan
  - applyPreferencesDefault is true when no local reader-prefs row exists, false when one does
  - running detectImportPreview performs zero writes (store row counts unchanged)
  </behavior>
  <action>
  1. Create src/portability/conflicts.ts — header citing D9-11/D9-13/D9-14 + Pattern 7's ImportPreview interface. Types:
     - ConflictKind = "article-revision" | "article-content-divergence" | "highlight-id" | "note-id" | "location".
     - ConflictSummary = { kind: ConflictKind; count: number; sampleIds: string[] } (sampleIds capped at 5 for calm preview copy).
     - ImportPreviewData = { incoming: { articles: number; highlights: number; notes: number; locations: number }; added: { articles: number; highlights: number; notes: number; locations: number }; conflicts: ConflictSummary[]; resolution: { confident: number; ambiguous: number; orphan: number }; fixtureBackedHighlights: number; applyPreferencesDefault: boolean }.
  2. detectImportPreview(bundle: ExportBundle): Promise<ImportPreviewData> — PURE READS: local articles via dexieLibrarySource.list(), highlights/notes via loadAllHighlights()/loadAllNotes(), locations via loadAllLocations(), prefs presence via the settingsStore load (ok ⇒ a reader-prefs row exists). Classification per the behavior block (revision compare for articles; originalHtmlHash compare at same id+revision; id collision for highlights/notes; [articleId, revision] match for locations). Build the lookup Map with EXACTLY the Pattern 8 precedence: bundle.articles first, then local Dexie articles (only ids not already present), then fixtures from src/fixtures/index.ts (only ids not already present).
  3. Eager re-resolution (D9-13): for each incoming highlight, resolve via resolveQuoteSelector(article, highlight.quote, highlight.position) when the article is found, else status orphan. Memoize per-article derived text/clusters by article id in a local Map so each article's normalizeText/grapheme work happens once regardless of highlight count (memoization is required, not optional — RESEARCH Pattern 8). Count fixtureBackedHighlights = resolved highlights whose article came from the fixtures source.
  4. Add a module doc note: this function MUST NOT write — it is the dry-run pass; the transaction lives in Plan 09-04's applyImport.
  5. Create tests/unit/portability/conflicts.test.ts on the fake-indexeddb harness: seed local state (one article at revision 1; one same-id article scenario variants via re-seed; one highlight; one note; one location; a reader-prefs row present/absent in two cases). Build incoming bundles with ExportBundleSchema.parse covering each of the five kinds (assert the kind + count appear in conflicts), the added-count math, fixture-backed resolution (use a real fixture id from src/fixtures/index.ts with a highlight whose quote.exact matches a real passage of that fixture — reusing the fixture's own text so the resolver returns a non-orphan status), the orphan case, and a write-free assertion (count rows before/after via raw db counts).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/conflicts.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/portability/conflicts.ts exports ConflictKind, PerKindOverride (declared here for Task 2), ConflictSummary, ImportPreviewData, ResolvedImportPlan (declared here for Task 2), detectImportPreview, resolveImportPlan (added in Task 2)
  - all five conflict kinds are detectable in tests; defaults need no override to be skip (skip is the absence of writing, encoded in Task 2)
  - the fixture-backed highlight test asserts a non-orphan status while the fixture is present, and orphan when the fixture id is absent from the lookup (Pitfall 4)
  - row counts before/after detectImportPreview are equal (zero writes)
  - npm run build exits 0
  </acceptance_criteria>
  <done>The dry-run pass reports added counts, all five conflict kinds, and eager tri-state resolution counts (incl. fixture-backed) — with zero writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveImportPlan — bulk per-kind overrides to a fully-computed plan</name>
  <files>src/portability/conflicts.ts, tests/unit/portability/conflicts.test.ts</files>
  <read_first>
  - 09-RESEARCH.md Pattern 3 (apply-step contract + keep-both FK rewrite BEFORE the transaction) + D9-14 conflict table
  - 09-CONTEXT.md D9-11/D9-12/D9-14
  - src/portability/conflicts.ts (Task 1 state)
  - src/persistence/settingsStore.ts (the reader-prefs row write shape: key "reader-prefs" with a value)
  </read_first>
  <behavior>
  - default overrides (every kind "skip"): conflicted records are excluded from *ToWrite arrays and counted in skipped; new records are always written
  - article-revision + "overwrite": the incoming article is written ONLY when its revision is strictly higher than the local one (keep-higher-revision, D-06 monotonic); equal-or-lower stays local and counts as skipped
  - article-content-divergence + "overwrite": the incoming article wins (same id+revision, content replaced)
  - highlight-id/note-id + "keep-both": the incoming record is written under a freshly minted crypto.randomUUID id; the idRewrites map records old→new; every incoming note whose highlightId appears in idRewrites has its highlightId rewritten to the minted id (note follows its highlight — Pitfall 7)
  - location + "overwrite": the incoming location wins only when its savedAt is newer than the local row's (last-write-wins); older incoming stays skipped
  - "keep-both" on article/location kinds behaves as skip (documented — the dialog in 09-05 offers keep-both only for the id kinds)
  - applyPreferences true ⇒ plan.preferences = bundle.preferences; false ⇒ preferences absent and applyPreferences false
  - resolveImportPlan is pure: it touches no Dexie table (only crypto.randomUUID, which is sync platform)
  </behavior>
  <action>
  1. Extend src/portability/conflicts.ts with:
     - PerKindOverride = "skip" | "overwrite" | "keep-both"; Overrides = Record<ConflictKind, PerKindOverride>.
     - ResolvedImportPlan = { articlesToWrite: CanonicalArticle[]; highlightsToWrite: HighlightRecord[]; notesToWrite: NoteRecord[]; locationsToWrite: LocationRecord[]; preferences?: ReaderSettings; applyPreferences: boolean; idRewrites: Map<string, string>; skipped: { articles: number; highlights: number; notes: number; locations: number } }.
     - export function resolveImportPlan(bundle: ExportBundle, preview: ImportPreviewData, overrides: Overrides, applyPreferences: boolean): ResolvedImportPlan — pure; re-derives per-record decisions from the bundle against the SAME local-read results the preview used (re-read local PK sets inside this function via the same loaders — it is async-free and reads only what it is given plus re-queries are NOT allowed inside; therefore accept the local snapshot as an argument: add an optional parameter localState (the raw PK maps) OR simply re-run the same read sequence and accept that resolveImportPlan is async. Choose: make resolveImportPlan async and re-read the local PK sets exactly as detectImportPreview does — determinism note documented; a mismatch window between preview and proceed is acceptable at prototype scale and the transaction still applies atomically).
     - Keep-both FK discipline: mint ids and rewrite note highlightId values BEFORE returning the plan (the plan is fully computed data; the transaction in 09-04 contains only puts).
  2. Extend tests/unit/portability/conflicts.test.ts with the override matrix per the behavior block — one test per kind per relevant override, plus the FK-rewrite assertion (after keep-both on highlight-id, the incoming note's highlightId equals the minted id present in idRewrites values) and the keep-higher-revision and savedAt-LWW edge cases.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/conflicts.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - resolveImportPlan exported; the override matrix tests (skip default, keep-higher-revision, content overwrite, keep-both mint + FK rewrite, location LWW) all pass
  - idRewrites maps old incoming ids to minted UUIDs; rewritten note.highlightId values match the minted ids (asserted)
  - applyPreferences gates the preferences payload (asserted both ways)
  - npx vitest run tests/unit/portability exits 0
  </acceptance_criteria>
  <done>The bulk per-kind override semantics produce a fully-computed, pure ResolvedImportPlan that the atomic apply can consume with puts only.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-03)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `ConflictKind`, `PerKindOverride`, `ConflictSummary`, `ImportPreviewData`, `ResolvedImportPlan` | types | src/portability/conflicts.ts (new file) |
| `detectImportPreview` | async pure-read function | dry-run pass + eager tri-state re-resolution (D9-13) |
| `resolveImportPlan` | async pure function | D9-14 override semantics; keep-both mint + FK rewrite |
| tests/unit/portability/conflicts.test.ts | test file | 5-kind detection + override matrix |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle records → local-state comparison | untrusted incoming records are classified against trusted local rows (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-08 | Tampering (silent overwrite) | conflict resolution | mitigate | skip-by-default for every kind; never-silently-overwrite (D9-14); keep-higher-revision and savedAt-LWW are explicit reader choices under Overwrite-all — test-locked |
| T-9-09 | Tampering (orphaned FK) | keep-both id minting | mitigate | note highlightId rewritten pre-transaction (Pitfall 7); asserted in tests |
| T-9-10 | Spoofing (fixture impersonation) | 3-source lookup precedence | mitigate | bundle.articles > local > fixtures precedence means an incoming article shadows a same-id fixture; highlights resolve against the winning article — no fixture-text substitution for a real article id |
</threat_model>

<verification>
- npx vitest run tests/unit/portability exits 0
- npm run build exits 0
</verification>

<success_criteria>
- SC#2's dry-run conflict preview data exists as pure logic: five kinds, skip-by-default, honest tri-state counts including fixture-backed highlights
- The ResolvedImportPlan is fully computed data — Plan 09-04's transaction will contain Dexie puts only
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-03-SUMMARY.md` when done
</output>
