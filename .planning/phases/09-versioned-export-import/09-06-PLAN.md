---
phase: 09-versioned-export-import
plan: 06
type: execute
wave: 5
depends_on: ["09-05"]
files_modified:
  - tests/e2e/portability/round-trip.spec.ts
  - tests/e2e/portability/zip-slip-regression.spec.ts
  - tests/e2e/portability/import-preview.spec.ts
  - tests/e2e/portability/highlights-export.spec.ts
  - tests/e2e/portability/a11y.spec.ts
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03]
must_haves:
  truths:
    - "A bundle exported on machine A imports on machine B with every highlight re-resolving to confident or surfacing honestly as ambiguous/orphan — and no key matching page appears anywhere in bundle.json (SC#4)"
    - "A crafted malicious zip containing traversal entries is refused with the unsafe-entry status message and zero IndexedDB state change across all five stores (SC#2)"
    - "The preview dialog e2e proves defaults (skip everywhere), data-initial-focus on Cancel import, Esc-close with no state change, and Proceed applying with a status summary"
    - "Highlights exports (library-wide + per-article) download .md files whose content matches the locked template, and the portability surfaces pass axe + keyboard checks in all three engines"
  artifacts:
    - path: "tests/e2e/portability/round-trip.spec.ts"
      provides: "SC#4 round-trip integrity gate (two-browser-context machine A/B harness)"
    - path: "tests/e2e/portability/zip-slip-regression.spec.ts"
      provides: "SC#2 malicious-archive refusal gate with crafted buffers"
    - path: "tests/e2e/portability/import-preview.spec.ts"
      provides: "PORT-02 dialog flow gate"
    - path: "tests/e2e/portability/highlights-export.spec.ts"
      provides: "PORT-03 download content gate"
    - path: "tests/e2e/portability/a11y.spec.ts"
      provides: "axe + keyboard gate for the new surfaces"
  key_links:
    - from: "tests/e2e/portability/round-trip.spec.ts"
      to: "app UI (Settings cluster + dialog)"
      via: "real clicks through Export library bundle → download capture → file input → preview → Import"
      pattern: "waitForEvent\\(\"download\"\\)"
    - from: "tests/e2e/portability/round-trip.spec.ts"
      to: "IndexedDB (machine B)"
      via: "readRow/countRows raw indexedDB assertions — rows physically present, offsets byte-equal"
      pattern: "indexedDB\\.open"
---

<objective>
Replace the five Wave-0 sentinel specs with the real phase-exit e2e gates: SC#4 round-trip integrity (two contexts as two machines, download capture, offset equality, no page data in the bundle), SC#2 Zip Slip regression (crafted malicious zips refused, no state change), the import preview dialog flow, the highlights .md export content, and the a11y/keyboard gate — across chromium, firefox, and webkit.

Purpose: These are the real-browser truth for every PORT requirement (DOM emulators are not authoritative — locked discipline). SC#2 and SC#4 are hard phase-exit criteria.
Output: 5 fully-real e2e spec files replacing their sentinels.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: round-trip.spec.ts — SC#4 cross-machine integrity</name>
  <files>tests/e2e/portability/round-trip.spec.ts</files>
  <read_first>
  - tests/e2e/library/remove-cascade.spec.ts (FULL — the harness to clone: BASE constant, image-stub route, clear-rows-not-deleteDatabase beforeEach, readRow/countRows helpers, seedCascadeRows raw-IndexedDB seeding incl. the highlight row shape and array location keys)
  - 09-RESEARCH.md §Code Examples "SC#4 round-trip e2e harness" + Assumptions A1/A5
  - tests/e2e/portability/download-smoke.spec.ts (proven download capture)
  - src/fixtures/index.ts (fixture ids for the fixture-backed highlight)
  - 09-05-PLAN.md (the exact button names + status copy to assert against)
  </read_first>
  <action>
  1. Replace the sentinel with the real spec. Structure: one test using the browser fixture to create two contexts (machineA, machineB — separate IndexedDB origins-in-profile, the "another machine" surrogate); close both in a finally block.
  2. Machine A: goto base, clear all 5 stores (clone the remove-cascade clear helper), seed via raw IndexedDB puts (seedCascadeRows style): two valid ArticleSchema articles (a paste-style content-hash id and an md-style id), a highlight on each (valid HighlightRecord shape with position start/end + quote prefix/exact/suffix — copy real passages from the seeded article text so resolution returns confident), a note on one highlight, a location row for one article (array key), and a reader-prefs settings row. Also seed a highlight keyed to a real bundled fixture id (with a genuine passage of that fixture).
  3. Export on A: open the Settings dialog (existing settings button), click "Export library bundle", await page.waitForEvent("download"), download.path() → bundlePath.
  4. Node-side bundle inspection (spec runs in Node): read bundlePath, unzipSync it (test-side fflate import), JSON.parse bundle.json — assert schemaVersion 1, the two articles present, the fixture article absent, the fixture id in fixtureIds, preferences present, and a recursive key walk over the whole parsed object finds no key matching the pattern page (SC#4 data minimization).
  5. Machine B: goto base, clear stores, open Settings, set the import file input (locator for the hidden .zip input) via setInputFiles with bundlePath (the path form — verifies A5's path variant), assert the preview dialog (dialog.import-preview) shows the incoming counts, click Import, assert the status region contains "Imported".
  6. Machine B assertions via readRow/countRows: both articles present; both highlights present with position.start/position.end byte-equal to the seeded values (offsets survive — SC#4); the note present with matching highlightId; the location row present (array key readRow form); the reader-prefs row updated (applyPreferences default true on the fresh machine). Fixture-backed highlight: row present; then open the fixture article in the reader and assert its highlight mark element is visible (the ANNO rendering surface — reuse the mark selector from existing annotations specs).
  7. Run all three engines (default projects) — this spec must be green on chromium, firefox, and webkit.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/round-trip.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - the spec passes on chromium, firefox, and webkit (3 cells)
  - offset-equality assertions (position.start/end byte-equal pre/post) present and passing
  - the no-page-key recursive walk passes; fixture article absent from bundle.json while its id is in fixtureIds
  - the fixture-backed highlight renders a visible mark on machine B
  </acceptance_criteria>
  <done>SC#4 is proven end-to-end in real browsers: export → transfer → import → every anchor confident or honestly surfaced, offsets intact, page data absent.</done>
</task>

<task type="auto">
  <name>Task 2: zip-slip-regression.spec.ts + import-preview.spec.ts</name>
  <files>tests/e2e/portability/zip-slip-regression.spec.ts, tests/e2e/portability/import-preview.spec.ts</files>
  <read_first>
  - tests/e2e/portability/round-trip.spec.ts (Task 1 — reuse the seeding + import-flow helpers; factor shared helpers into the spec or a small local _portability.ts helper file if duplication exceeds ~40 lines)
  - 09-RESEARCH.md Assumption A5 (setInputFiles buffer form) + §Security Domain Zip Slip row
  - tests/e2e/library/remove-cascade.spec.ts (countRows helper for no-state-change assertions)
  - 09-05-PLAN.md (refusal copy strings + dialog behavior)
  </read_first>
  <action>
  1. zip-slip-regression.spec.ts: craft malicious zips in Node with fflate zipSync: variant one carries entries bundle.json + manifest.json (valid-shaped minimal content) + ../../evil.sh; variant two replaces the evil entry with the URL-encoded traversal form ..%2F..%2Fevil.sh. For each: on a cleared-store page, open Settings, setInputFiles on the .zip input with { name: "evil.zip", mimeType: "application/zip", buffer } (the buffer form — verifies A5's payload variant), assert the status region shows the unsafe-entry refusal copy ("unsafe file entry"), and assert countRows across all five stores equals the pre-pick counts (zero state change). Also assert the dialog.import-preview never becomes visible.
  2. import-preview.spec.ts: using a valid bundle produced the same way as Task 1 (or a smaller hand-built one built Node-side from ExportBundleSchema.parse + computeManifest + zipSync — building it Node-side is preferable for focus): on machine B with cleared stores plus ONE pre-seeded conflicting highlight id (to force a highlight-id conflict):
     - assert dialog.import-preview visible with the summary counts text
     - assert the Cancel import button has attribute data-initial-focus
     - press Escape → dialog closes → countRows unchanged
     - re-run the import flow, flip the highlight-id override select to Keep both, click Import → status shows the imported summary → both highlight rows exist (local + minted) — the keep-both e2e proof
     - default-skip proof: run the import flow once accepting all defaults → the conflicting highlight is NOT overwritten (its stored exact text unchanged) and the status/report reflects the skip
  3. Both specs green on all three engines.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/zip-slip-regression.spec.ts tests/e2e/portability/import-preview.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - both traversal variants refused with the unsafe-entry copy and zero store mutation across engines (6 zip-slip cells)
  - preview defaults (skip) leave the conflicting highlight byte-unchanged; keep-both yields two rows (local + minted id)
  - Esc-close restores no-change; data-initial-focus asserted on Cancel import
  </acceptance_criteria>
  <done>SC#2's malicious-archive gate and the PORT-02 dialog flow are proven in real browsers with state-level assertions.</done>
</task>

<task type="auto">
  <name>Task 3: highlights-export.spec.ts + a11y.spec.ts</name>
  <files>tests/e2e/portability/highlights-export.spec.ts, tests/e2e/portability/a11y.spec.ts</files>
  <read_first>
  - tests/e2e/a11y.spec.ts (the existing AxeBuilder pattern to mirror)
  - tests/e2e/library/remove-cascade.spec.ts (seeding helper)
  - src/portability/markdown.ts (the locked template strings to assert)
  - 09-05-PLAN.md (button names, announce copy, filename derivation)
  </read_first>
  <action>
  1. highlights-export.spec.ts: seed one Dexie article with two highlights (one with a note) + one highlight keyed to a nonexistent article id (orphan at export); per-article flow: open the article, click "Export highlights", capture the download, read the file Node-side (download.path + fs.readFile) — assert the content contains the blockquote quote lines, the citation line with the article title in italics, the Note line, and the footer counts line including the orphan count and marker; assert the suggested filename matches highlights-{sanitized title}.md. Library-wide flow: Settings → "Export all highlights" → download lem-reader-highlights.md — assert the level-1 heading, per-article level-2 sections, per-section footers, and the totals footer.
  2. a11y.spec.ts: on a page with the Settings panel open, run AxeBuilder analyze on the panel (expect zero violations); open the import flow with a valid small bundle to reveal dialog.import-preview and analyze the dialog subtree (expect zero violations); keyboard checks: Tab cycles focus within the open dialog (first/last focusable wrap inside, focus does not escape to body), Escape closes and focus returns to the trigger; axe + keyboard run on all three engines.
  3. Full portability e2e dir green on all engines; then run the complete phase-scoped sampling command.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/</automated>
  </verify>
  <acceptance_criteria>
  - downloaded .md content matches the locked template (markers, citation, Note line, footers) on all engines
  - AxeBuilder reports zero violations on the settings cluster and the preview dialog across engines
  - keyboard: focus trapped in the dialog, Esc restores; per-engine cells green
  - npx playwright test tests/e2e/portability/ exits 0 (all 6 spec files incl. download-smoke, × 3 engines)
  </acceptance_criteria>
  <done>PORT-03's export content and the a11y posture of every new surface are proven in real browsers; the phase e2e gate set is complete.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-06)

| Symbol / Path | Kind | Notes |
|---|---|---|
| tests/e2e/portability/round-trip.spec.ts | e2e gate | replaces sentinel; SC#4 |
| tests/e2e/portability/zip-slip-regression.spec.ts | e2e gate | replaces sentinel; SC#2 |
| tests/e2e/portability/import-preview.spec.ts | e2e gate | replaces sentinel; PORT-02 flow + keep-both proof |
| tests/e2e/portability/highlights-export.spec.ts | e2e gate | replaces sentinel; PORT-03 content |
| tests/e2e/portability/a11y.spec.ts | e2e gate | replaces sentinel; axe + keyboard |
| tests/e2e/portability/_portability.ts (if factored) | shared helper | seeding/import-flow reuse |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crafted hostile archives → import pipeline | the regression spec drives fully attacker-controlled zips through the real UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering + EoP | Zip Slip | mitigate (verified here) | the e2e regression is the phase-exit proof that the app-level guard fires on crafted archives with zero state change — unit truth from 09-01 now proven through the real UI |
| T-9-19 | Info Disclosure (residual) | e2e evidence | accept | e2e downloads live in Playwright temp dirs; no reader data persists in the repo |
| T-9-20 | Repudiation (test honesty) | gate execution | mitigate | executors run the specs themselves and record per-engine counts in the SUMMARY; no trusting prior green claims (Key Decision #9 posture, enforced again in 09-07) |
</threat_model>

<verification>
- npx playwright test tests/e2e/portability/ exits 0 across chromium/firefox/webkit
- Per-task sampling: npm run test:unit -- --run tests/unit/portability && npx playwright test tests/e2e/portability/ --project=chromium
</verification>

<success_criteria>
- All four ROADMAP success criteria for Phase 9 have real-browser evidence: versioned bundle export (SC#1), validated atomic import with Zip Slip refusal (SC#2), highlights Markdown export (SC#3), round-trip integrity (SC#4)
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-06-SUMMARY.md` when done
</output>
