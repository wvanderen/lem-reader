---
phase: 09-versioned-export-import
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - src/reader/ImportPreviewDialog.tsx
  - src/reader/SettingsPanel.tsx
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/unit/portability/import-preview-dialog.test.tsx
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03]
must_haves:
  truths:
    - "Reader can export the whole library bundle from the Settings panel; the download fires and the in-panel status region announces the result in calm voice"
    - "Reader can pick a bundle file, see the preview dialog with added counts, conflicts by kind, and honest ambiguous/orphan warnings, and choose bulk per-kind overrides; Proceed applies atomically, Cancel leaves IndexedDB unchanged"
    - "Reader can export all highlights as one combined .md from Settings and per-article highlights from the article view; both download and announce"
    - "When Web Crypto is unavailable (insecure context) the three data actions are disabled with a calm status message"
  artifacts:
    - path: "src/reader/ImportPreviewDialog.tsx"
      provides: "native dialog preview + confirm (structural clone of RemoveConfirm — Pitfall 8 isolation)"
      exports: ["ImportPreviewDialog"]
    - path: "src/reader/SettingsPanel.tsx"
      provides: "the Your data cluster (Export library bundle / Import bundle / Export all highlights) + import state machine + status region"
    - path: "src/routes/ArticleView.tsx"
      provides: "per-article Export highlights affordance (D9-06)"
    - path: "tests/unit/portability/import-preview-dialog.test.tsx"
      provides: "RTL component truth (counts render, initial-focus marker, override wiring)"
  key_links:
    - from: "src/reader/SettingsPanel.tsx"
      to: "src/portability/ExportImportService.ts"
      via: "buildBundleBytes / validateBundle / applyImport calls wired to the cluster buttons"
      pattern: "validateBundle|buildBundleBytes|applyImport"
    - from: "src/reader/SettingsPanel.tsx"
      to: "src/reader/ImportPreviewDialog.tsx"
      via: "preview dialog opens on validation success; applyImport fires ONLY in the dialog Proceed path"
      pattern: "<ImportPreviewDialog"
    - from: "src/routes/ArticleView.tsx"
      to: "src/portability/markdown.ts"
      via: "collectHighlightEntries + renderArticleHighlights for the per-article .md"
      pattern: "renderArticleHighlights"
---

<objective>
Wire the portability surface into the app (D9-10/D9-11/D9-12): the Settings "Your data" cluster, the ImportPreviewDialog (a structural RemoveConfirm clone with bulk per-kind overrides and the preferences apply choice), and the ArticleView per-article highlights export — with every result and refusal announcing through role=status live regions in calm DOC-06 voice.

Purpose: PORT-01/02/03 become reader-operable. The destructive write stays isolated in the Proceed handler (Pitfall 8); the library shelf stays calm (no new header chrome).
Output: 1 new component, 2 modified views, app.css styles, 1 RTL component spec.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportPreviewDialog component (structural RemoveConfirm clone)</name>
  <files>src/reader/ImportPreviewDialog.tsx, src/app.css, tests/unit/portability/import-preview-dialog.test.tsx</files>
  <read_first>
  - src/ingestion/library/RemoveConfirm.tsx (FULL — the mandated clone source: open/showModal sync effect, triggerRef capture/restore, explicit focus on [data-initial-focus], destructive handler isolation, dialog markup + class family)
  - src/reader/WipeConfirm.tsx (second precedent for the same discipline)
  - 09-PATTERNS.md §src/reader/ImportPreviewDialog.tsx (verbatim excerpts to copy)
  - 09-RESEARCH.md Pattern 6 (override toggles + defaults per kind)
  - src/portability/conflicts.ts (ImportPreviewData, ConflictKind, PerKindOverride, Overrides types)
  - src/app.css (existing dialog class patterns: library-remove-confirm family, settings-section)
  </read_first>
  <action>
  1. Create src/reader/ImportPreviewDialog.tsx — header citing D9-11 + Pitfall 8 (structural clone, NOT a shared dialog). Props: { open: boolean; preview: ImportPreviewData | null; onProceed: (overrides: Overrides, applyPreferences: boolean) => void; onCancel: () => void }. Copy the RemoveConfirm dialog discipline verbatim: useRef dialog ref; triggerRef captures document.activeElement on open and restores focus in the close listener; a useEffect syncs the open prop with showModal()/close(); after showModal, explicitly focus the [data-initial-focus] element (WebKit quirk). Markup: dialog element with className "import-preview", role "alertdialog", aria-modal "true", aria-labelledby/aria-describedby ids, an -inner wrapper, an h2 titled "Import this bundle?", body copy, and an -actions row.
  2. Body content (calm DOC-06 voice):
     - Summary sentence: "This bundle contains {A} articles, {H} highlights, {N} notes, and {L} reading positions." with the added counts when they differ from incoming (e.g., "{X} new").
     - Conflicts: when preview.conflicts is non-empty, a grouped list — per ConflictSummary a line "{count} conflicting {kind-label}" with a per-kind override control: for the article-revision, article-content-divergence, and location kinds a two-option select (Skip, the default, and Overwrite); for the highlight-id and note-id kinds a three-option select (Skip default, Keep both, Overwrite). Labels are plain words; keep-both is not offered where it has no meaning (per Plan 09-03 semantics).
     - Warnings: when resolution.ambiguous > 0 a line "{n} highlights will import as ambiguous."; when orphan > 0 "{m} highlights will import as orphan."; when fixtureBackedHighlights > 0 "{k} highlights anchor to bundled sample articles."
     - Preferences choice: a checkbox labeled "Apply imported reading preferences" defaulting to preview.applyPreferencesDefault (fresh-device detection from 09-03).
  3. Actions row: "Cancel import" button carrying the data-initial-focus attribute (the NON-destructive action) wired to onCancel; "Import" button whose onClick is the ONLY place onProceed is invoked — never in an effect, catch block, or the file-pick handler (Pitfall 8 load-bearing handler discipline).
  4. app.css: add an import-preview class family mirroring the library-remove-confirm tokens (same spacing/border/backdrop approach; authored CSS, no new dependencies).
  5. Create tests/unit/portability/import-preview-dialog.test.tsx (RTL, jsdom): render with a synthetic ImportPreviewData — assert the summary counts text is present; assert the cancel button has attribute data-initial-focus; assert the Import button is present; assert changing the highlight-id select to Keep both and submitting calls onProceed with overrides whose highlight-id value is keep-both and whose other kinds remain skip; assert the preferences checkbox initial state follows applyPreferencesDefault.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/import-preview-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - src/reader/ImportPreviewDialog.tsx exists with the dialog element, role alertdialog, class import-preview, the focus capture/restore + explicit initial-focus discipline, and onProceed invoked only from the Import button onClick
  - the cancel button carries data-initial-focus; override defaults are skip everywhere; keep-both appears only for the id kinds
  - the RTL spec passes (counts, marker, override plumbing, preferences default)
  - npm run build exits 0
  </acceptance_criteria>
  <done>The preview+confirm dialog exists as an isolated structural clone with honest counts, warnings, bulk overrides, and the isolated destructive path.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsPanel "Your data" cluster + import state machine</name>
  <files>src/reader/SettingsPanel.tsx</files>
  <read_first>
  - src/reader/SettingsPanel.tsx (FULL — fieldset/legend structure, settings-footer, the deliberate no-form-submission comment, dialog focus discipline to leave untouched)
  - src/ingestion/IngestControl.tsx (file-input + ref + status/message state machine analog, size-cap guard posture)
  - src/portability/ExportImportService.ts (buildBundleBytes, validateBundle, applyImport, ImportRefusal)
  - src/portability/conflicts.ts (detectImportPreview, resolveImportPlan, Overrides defaults)
  - src/portability/{bundle,download}.ts (BUNDLE_FILENAME, downloadBlob)
  - src/portability/markdown.ts (renderLibraryHighlights, collectHighlightEntries, orderSectionsByRecency)
  - 09-PATTERNS.md §SettingsPanel (correction: WipeConfirm mounts in App.tsx — the cluster is NEW SettingsPanel surface) + §shared .status pattern
  </read_first>
  <action>
  1. In SettingsPanel.tsx add a new fieldset with className "settings-section" and legend "Your data" (D9-10 — conceptual grouping with the wipe action; WipeConfirm itself stays where it is in App.tsx per the PATTERNS correction). All buttons type="button" (the panel avoids form submission).
  2. Three actions:
     - "Export library bundle": disable while running; call buildBundleBytes(); downloadBlob([bytes], BUNDLE_FILENAME, "application/zip"); set status text "Exported {n} articles to {BUNDLE_FILENAME}." — re-read the article count via dexieLibrarySource.list() length or thread it from the build (terminal-granularity summary per RESEARCH A6). On throw: "Export didn't complete. Nothing was exported."
     - "Export all highlights": gather dexieLibrarySource.list() + loadAllHighlights() + loadAllNotes() + the bundled fixtures array (fixtures join the article set so fixture-backed highlights resolve); collectHighlightEntries over articles ∪ fixtures; order sections with orderSectionsByRecency using loadAllLocations(); renderLibraryHighlights; downloadBlob([md], "lem-reader-highlights.md", "text/markdown"); status "Exported {n} highlights to lem-reader-highlights.md."
     - "Import bundle": a visually-hidden file input (type="file", accept=".zip", ref-based like IngestControl) triggered by the button; onChange handler runs the state machine: validating → await validateBundle(file); on refusal set the calm status copy per kind (exact strings): not-a-zip → "This file isn't a Lem Reader bundle. Nothing was imported."; unsafe-entry → "This bundle contains an unsafe file entry and can't be imported. Nothing was imported."; missing-entry → "This bundle is incomplete — {name} is missing. Nothing was imported."; newer-schema-version → "This bundle was exported by a newer Lem Reader version. Please update Lem Reader and try again."; invalid → "This bundle contains {issues.length} problems and can't be imported. Nothing was imported."; corrupted → "This bundle may be corrupted — {failedBlocks.length} record types failed integrity checking. Nothing was imported." On ok: await detectImportPreview(bundle) and open the ImportPreviewDialog with the result.
  3. Proceed path: the dialog's onProceed(overrides, applyPreferences) handler awaits resolveImportPlan(bundle, preview, overrides, applyPreferences) then applyImport(plan), then sets status "Imported {a} articles, {h} highlights, {n} notes, and {l} reading positions." (+ a trailing " {skipped total} items were skipped." when any skipped count > 0), closes the dialog, and resets the file input value so re-picking the same file re-fires onChange. On catch: "Import didn't complete. Nothing was changed." (the transaction rolled back). applyImport fires ONLY inside this Proceed handler (Pitfall 8).
  4. Status region: inside the cluster add div with className "status", role "status", aria-live "polite", aria-atomic "true" (the D2-13 pattern) carrying the messages above.
  5. Insecure-context guard (RESEARCH Pitfall 6): on panel mount compute a boolean once — globalThis.crypto?.subtle is undefined → disable all three buttons and set status "Export and import need a secure connection (HTTPS)." (mirrors StorageBanner's graceful-degradation posture; dev localhost and HTTPS hosting are secure so this is a defensive surface).
  6. app.css: minimal additions for the cluster layout if the existing settings-section styles do not already cover a button group (reuse tokens; no new CSS vocabulary beyond .status which exists).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - SettingsPanel renders a "Your data" fieldset with three buttons by accessible name "Export library bundle", "Import bundle", "Export all highlights" (verifiable in the 09-06 e2e; here: source contains the legend + three type="button" buttons)
  - the refusal copy strings above appear verbatim in the source mapped to their ImportRefusal kinds
  - the status region carries role "status" + aria-live "polite" + aria-atomic "true"
  - applyImport appears in exactly one handler (the Proceed path) — repo grep within this file shows no other invocation site
  - the insecure-context guard disables all three actions when crypto.subtle is missing
  - npm run build exits 0 and the full unit portability suite stays green
  </acceptance_criteria>
  <done>The Settings panel owns the whole-library data actions with the full import state machine, calm refusal copy, and the isolated atomic apply path.</done>
</task>

<task type="auto">
  <name>Task 3: ArticleView per-article "Export highlights" affordance</name>
  <files>src/routes/ArticleView.tsx</files>
  <read_first>
  - src/routes/ArticleView.tsx (FULL — the article header + TagEntry mount at ~L1163-1182, the visually-hidden annotation live region at ~L1101-1114, existing highlight/note loading paths via useAnnotationState or the stores)
  - src/portability/markdown.ts (collectHighlightEntries, renderArticleHighlights signatures)
  - src/portability/zipSlip.ts (sanitizeFilename)
  - src/portability/download.ts (downloadBlob)
  - 09-PATTERNS.md §ArticleView (TagEntry inert-at-mount rules to copy)
  </read_first>
  <action>
  1. Add a "Export highlights" button (type="button") to the article header, mounted after the TagEntry affordance (D9-06; exact placement beside TagEntry is the locked planner choice). Copy the TagEntry discipline: INERT at mount — no autoFocus prop, no useEffect-driven focus.
  2. onClick: load this article's highlights and notes (reuse the existing per-article loading surface already in the file's annotation state, or loadAllHighlights/loadAllNotes filtered by articleId — either is acceptable; do not fork resolution logic); collectHighlightEntries([article], filteredHighlights, filteredNotes); renderArticleHighlights; downloadBlob([md], "highlights-" + sanitizeFilename(article.provenance.title, article.id) + ".md", "text/markdown").
  3. Announce the result through a visually-hidden live region mirroring the annotationAnnouncement pattern (role "status", aria-live "polite", aria-atomic "true", class "visually-hidden"): "Exported {n} highlights for this article." — thread a new message state alongside the existing announcement state or extend it.
  4. Disable the button while an export is in flight; on throw announce "Export didn't complete." calmly.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/portability/ --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - ArticleView contains the "Export highlights" button with type="button", mounted in the header, with no autoFocus or effect-driven focus on it (inert at mount)
  - the download filename derives from sanitizeFilename(article.provenance.title, article.id) — the call is present in the handler
  - a visually-hidden role="status" region carries the export result message
  - npm run build exits 0; the chromium portability e2e scaffolds remain green
  </acceptance_criteria>
  <done>Per-article highlights export is available in the reader with a sanitized filename and a calm live-region announcement.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-05)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `ImportPreviewDialog` | React component | src/reader/ImportPreviewDialog.tsx (new file; structural RemoveConfirm clone) |
| SettingsPanel "Your data" cluster | UI surface | export/import/export-highlights actions + import state machine + status region (modified file) |
| ArticleView "Export highlights" affordance | UI surface | per-article .md export (modified file) |
| .import-preview + cluster CSS | styles | src/app.css |
| tests/unit/portability/import-preview-dialog.test.tsx | test file | RTL component truth |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader gesture → destructive bulk write | the Proceed click is the only path across this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-16 | Repudiation (destructive import without consent) | ImportPreviewDialog + Proceed handler | mitigate | alertdialog semantics; data-initial-focus on the non-destructive Cancel; applyImport ONLY in the Import button onClick (Pitfall 8) — grep-verifiable single call site |
| T-9-17 | Spoofing (confusing consent copy) | preview copy | mitigate | counts + conflicts + ambiguous/orphan warnings rendered before Proceed; calm DOC-06 voice with zero new disclosure vocabulary (D7-04) |
| T-9-18 | DoS (insecure-context crash) | crypto.subtle availability | mitigate | Pitfall 6 guard disables the three actions with a calm status message instead of a TypeError on digest |
| T-9-06 | Tampering (filename) | per-article .md download name | mitigate | sanitizeFilename applied to the article title before download — acceptance-asserted in Task 3 |
</threat_model>

<verification>
- npx vitest run tests/unit/portability exits 0 (now incl. the RTL dialog spec)
- npm run build exits 0
- npx playwright test tests/e2e/portability/ --project=chromium exits 0 (scaffolds still green post-UI)
</verification>

<success_criteria>
- All three PORT capabilities are reader-operable from calm, settings-housed surfaces with the library shelf untouched
- The destructive import write is provably isolated to the Proceed handler; every refusal and result announces via a live region
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-05-SUMMARY.md` when done
</output>
