---
phase: 09-versioned-export-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/portability/markdown.ts
  - src/persistence/highlightsStore.ts
  - src/persistence/notesStore.ts
  - tests/unit/portability/markdown.test.ts
  - tests/unit/portability/bulk-reads.test.ts
autonomous: true
requirements: [PORT-01, PORT-03]
must_haves:
  truths:
    - "Every highlight renders as a Markdown blockquote with a citation line; a note renders as a Note line and is never dropped — including for ambiguous and orphan highlights"
    - "Ambiguous highlights carry an italic approx marker and orphan highlights an orphan marker; a footer line reports honest counts"
    - "Whole-library reads return every valid highlight/note row and silently drop corrupt rows (STATE-04), mirroring loadAllLocations exactly"
  artifacts:
    - path: "src/portability/markdown.ts"
      provides: "Fixed highlights-only Markdown template renderer (D9-06..D9-09) + live tri-state collection"
      exports: ["HighlightEntry", "escapeMarkdownLine", "collectHighlightEntries", "renderArticleHighlights", "renderLibraryHighlights", "orderSectionsByRecency"]
    - path: "src/persistence/highlightsStore.ts"
      provides: "whole-library bulk read"
      exports: ["loadAllHighlights"]
    - path: "src/persistence/notesStore.ts"
      provides: "whole-library bulk read"
      exports: ["loadAllNotes"]
    - path: "tests/unit/portability/markdown.test.ts"
      provides: "template contract truth (markers, citation, footer, escaping)"
  key_links:
    - from: "src/portability/markdown.ts"
      to: "src/content/normalizeText.ts"
      via: "resolveQuoteSelector import (the contract re-export site) — REUSE-DO-NOT-FORK (PATTERNS key correction #3)"
      pattern: "from \"../content/normalizeText\""
    - from: "src/persistence/highlightsStore.ts"
      to: "src/persistence/locationStore.ts"
      via: "loadAllHighlights mirrors loadAllLocations (toArray + per-row safeParse + silent corrupt-row drop, plain-array return)"
      pattern: "toArray\\(\\)"
---

<objective>
The PORT-03 pure side plus the export read surface: the fixed highlights-only Markdown renderer (blockquote + citation + note, honest [approx]/[orphan] inclusion per D9-09) and the two missing whole-library bulk loaders (loadAllHighlights/loadAllNotes) that PORT-01's export service needs.

Purpose: Both halves are pure and independent of Plan 09-01's modules, so they land in Wave 1 in parallel. The renderer's honesty contract (never silently drop a highlight or its note) extends ANNO-07 to the external-tool surface.
Output: src/portability/markdown.ts, two additive store functions, two unit spec files.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixed highlights Markdown renderer with honest tri-state inclusion</name>
  <files>src/portability/markdown.ts, tests/unit/portability/markdown.test.ts</files>
  <read_first>
  - 09-RESEARCH.md Pattern 7 (renderer shape + planner-discretion recommendations being locked here) + §Anti-Patterns (never silently drop ambiguous/orphan)
  - 09-CONTEXT.md D9-06..D9-09 (locked variable contract + honest inclusion)
  - src/content/schema.ts (CanonicalArticle with provenance author/title/sourceUrl, HighlightRecord with quote.exact + position, NoteRecord with text + highlightId)
  - src/content/normalizeText.ts + src/annotations/resolution.ts (resolveQuoteSelector signature: article, selector, positionHint → TextPositionSelector | "ambiguous" | "orphan")
  </read_first>
  <behavior>
  - renderArticleHighlights with one confident entry emits a blockquote whose first line is the quote text prefixed by "> ", whose second line is the citation "> — Author, *Title*", and when the article has provenance.sourceUrl the citation line ends with " ([source](url))"
  - an entry with a note emits a third line "> Note: {note text}"
  - an ambiguous entry's quote line is prefixed by the italic marker "*[approx]* "; an orphan entry's by "*[orphan]* "
  - an orphan entry (article absent from the provided articles) still renders from its stored highlight.quote.exact with its note — never dropped
  - the per-article file ends with a footer line of the shape "_3 highlights · 1 ambiguous · 1 orphan_" (exact counts)
  - escapeMarkdownLine: a quote whose text begins with a hash-run, dash, plus, asterisk, greater-than, or digit-run-then-period gets that leading run backslash-escaped; mid-text punctuation is untouched
  - renderLibraryHighlights emits an h1 "Highlights", one "## {article title}" section per section, each section's blockquotes + per-section footer, and a final totals footer "_Totals: {N} highlights · {M} ambiguous · {K} orphan_"
  - orderSectionsByRecency: sections whose article has a location row sort by savedAt descending first; articles without locations follow, sorted by title ascending
  - collectHighlightEntries: a highlight whose articleId maps to a provided article gets live resolveQuoteSelector status; a highlight with no article resolves to "orphan"; notes attach by highlightId
  </behavior>
  <action>
  1. Create src/portability/markdown.ts — pure module, no DOM, no I/O; header comment citing D9-06/D9-07/D9-08/D9-09. Locked template (planner discretion exercised per RESEARCH Pattern 7):
     - HighlightEntry type: { highlight: HighlightRecord; note?: NoteRecord; status: "confident" | "ambiguous" | "orphan" }.
     - escapeMarkdownLine(text: string): string — backslash-escape ONLY a leading run of the structure-breaking characters (hash, dash, plus, asterisk, greater-than, or digit-run followed by a period) at line start; no mid-text escaping (over-escaping harms the calm-reading ethos).
     - collectHighlightEntries(articles: CanonicalArticle[], highlights: HighlightRecord[], notes: NoteRecord[]): HighlightEntry[] — build an article Map by id; map notes by highlightId (a note with no matching highlight is skipped — count it in no output; the renderer's never-drop rule applies to notes OF highlights); for each highlight, look up the article: absent → status "orphan"; present → call resolveQuoteSelector(article, highlight.quote, highlight.position) and map its result to confident (any TextPositionSelector return) / "ambiguous" / "orphan". Import resolveQuoteSelector from ../content/normalizeText (the contract re-export site — REUSE-DO-NOT-FORK).
     - renderArticleHighlights(article, entries): string — begins with the heading line "Highlights — {article.provenance.title}" as a level-1 heading, then one block per entry: quote line "> {marker}{escaped quote.exact}" where marker is the empty string for confident, the italic approx marker (asterisk-bracketed word approx, wrapped in asterisks, followed by a space) for ambiguous, and the orphan equivalent for orphan; citation line "> — {author}, *{title}*" (omit the author and comma when provenance.author is absent) plus " ([source]({sourceUrl}))" appended when provenance.sourceUrl is present; optional "> Note: {escaped note.text}" line; blank line between blocks; final footer "_{n} highlights · {m} ambiguous · {k} orphan_".
     - renderLibraryHighlights(sections): string — level-1 heading "Highlights", then per section a level-2 heading with the article title followed by the same blocks + per-section footer, then a final totals footer "_Totals: {n} highlights · {m} ambiguous · {k} orphan_".
     - orderSectionsByRecency(sections, locations): ordered copy of sections — sections with a location row (match [articleId, revision] against highlight revisions is NOT needed; match by articleId across provided LocationRecords) sort by savedAt descending; sections without follow sorted by provenance.title ascending (localeCompare).
  2. Create tests/unit/portability/markdown.test.ts per the behavior block using small inline ArticleSchema/HighlightRecordSchema/NoteRecordSchema.parse fixtures (mirror the sample-builder style of tests/unit/ingestion-tags.test.ts). Include the orphan-with-note case asserting BOTH the quote line and the Note line are present.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/portability/markdown.ts exports HighlightEntry, escapeMarkdownLine, collectHighlightEntries, renderArticleHighlights, renderLibraryHighlights, orderSectionsByRecency; imports resolveQuoteSelector from ../content/normalizeText
  - the marker strings, citation shape, Note line, and footer count line in the test assertions match the locked template above byte-for-byte
  - the orphan-with-note test asserts both lines present (D9-09 never-drop)
  - escapeMarkdownLine leaves mid-text punctuation untouched (asserted)
  - npm run build exits 0
  </acceptance_criteria>
  <done>The fixed highlights template renders honest, escaping-safe Markdown for confident/ambiguous/orphan entries with notes and footers, and live tri-state collection reuses the shipped resolver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loadAllHighlights + loadAllNotes bulk store loaders</name>
  <files>src/persistence/highlightsStore.ts, src/persistence/notesStore.ts, tests/unit/portability/bulk-reads.test.ts</files>
  <read_first>
  - src/persistence/locationStore.ts (loadAllLocations at ~L136-147 — the exact pattern to mirror: toArray + per-row safeParse + silent corrupt-row drop + plain-array return)
  - src/persistence/highlightsStore.ts (FULL — header comment conventions, existing per-article loaders, HighlightRecord import)
  - src/persistence/notesStore.ts (FULL — same)
  - src/persistence/db.ts (store names + PK shapes; confirm NO version bump is possible from this task)
  - tests/unit/ingestion-tags.test.ts (fake-indexeddb harness to copy: Dexie.dependencies install at module top-level, wipeDatabase, lazy module imports)
  </read_first>
  <behavior>
  - loadAllHighlights returns every valid HighlightRecord from db.highlights and silently drops rows that fail HighlightRecordSchema.safeParse
  - loadAllNotes returns every valid NoteRecord from db.notes and silently drops rows that fail NoteRecordSchema.safeParse
  - both return plain arrays (NOT the per-article LoadResult unions) — the whole-library read contract mirrors loadAllLocations
  </behavior>
  <action>
  1. Append to src/persistence/highlightsStore.ts an exported async function loadAllHighlights(): Promise<HighlightRecord[]> mirroring loadAllLocations exactly: await db.highlights.toArray(), per-row HighlightRecordSchema.safeParse, push successes, silently skip failures (STATE-04 never-coerce), return the plain array. Add a header-doc comment above it citing Plan 09-02 + RESEARCH Pitfall 5 + the loadAllLocations mirror. Use import type where only types are imported (verbatimModuleSyntax).
  2. Append the symmetric loadAllNotes(): Promise<NoteRecord[]> to src/persistence/notesStore.ts over db.notes.toArray() + NoteRecordSchema.safeParse.
  3. Do NOT touch src/persistence/db.ts — no new stores, no indexes, no version block (Pitfall 9: Phase 9 writes through existing stores at existing shapes).
  4. Create tests/unit/portability/bulk-reads.test.ts on the fake-indexeddb harness (copy the ingestion-tags.test.ts module-top Dexie.dependencies install + wipeDatabase + lazy imports): seed db.highlights with two valid rows and one corrupt row (e.g., missing quote), db.notes with one valid and one corrupt row; assert loadAllHighlights returns exactly 2 and loadAllNotes exactly 1, with the corrupt rows silently absent.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/bulk-reads.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/persistence/highlightsStore.ts exports loadAllHighlights; src/persistence/notesStore.ts exports loadAllNotes; both mirror the toArray + safeParse + drop pattern
  - corrupt-row drop behavior asserted for both stores; plain-array returns
  - git diff --stat src/persistence/db.ts after this plan is empty (db.ts byte-unchanged — Pitfall 9)
  - npm run test:unit -- --run tests/unit/portability exits 0 (markdown + bulk-reads + 09-01's files)
  </acceptance_criteria>
  <done>Whole-library bulk reads exist for highlights and notes with STATE-04 validation discipline and zero Dexie schema changes.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-02)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `HighlightEntry`, `escapeMarkdownLine`, `collectHighlightEntries`, `renderArticleHighlights`, `renderLibraryHighlights`, `orderSectionsByRecency` | types + pure functions | src/portability/markdown.ts (new file) |
| `loadAllHighlights` | store bulk read | src/persistence/highlightsStore.ts (additive export) |
| `loadAllNotes` | store bulk read | src/persistence/notesStore.ts (additive export) |
| tests/unit/portability/{markdown,bulk-reads}.test.ts | test files | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored article/highlight text → exported .md file | reader's own content crosses into an external-tool surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-05 | Tampering (markdown structure injection) | markdown.ts | mitigate | escapeMarkdownLine backslash-escapes leading structure-breaking runs (heading/bquote/list markers) so highlight text cannot forge blockquote structure or inject headings inside quotes; test-locked |
| T-9-06 | Tampering (filename) | per-article .md download filename | mitigate | filename derivation MUST pass article titles through sanitizeFilename (src/portability/zipSlip.ts, landed in 09-01) — enforced at the UI call site in Plan 09-05 Task 3 acceptance |
| T-9-07 | Info Disclosure | corrupt local rows | accept | STATE-04 silent corrupt-row drop is the established local-read posture (bundle records refuse loudly; local reads drop quietly — PATTERNS shared-pattern note) |
</threat_model>

<verification>
- npx vitest run tests/unit/portability exits 0
- npm run build exits 0
- git diff --stat src/persistence/db.ts is empty
</verification>

<success_criteria>
- The fixed template satisfies SC#3's template-variable contract with honest tri-state inclusion and never drops a note
- Export-side whole-library reads exist for highlights and notes without any Dexie schema change
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-02-SUMMARY.md` when done
</output>
