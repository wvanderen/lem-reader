---
phase: 09-versioned-export-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - src/vite-env.d.ts
  - src/portability/bundle.ts
  - src/portability/zipSlip.ts
  - src/portability/manifest.ts
  - src/portability/download.ts
  - tests/unit/portability/bundle-schema.test.ts
  - tests/unit/portability/zip-slip.test.ts
  - tests/unit/portability/manifest.test.ts
  - tests/e2e/portability/download-smoke.spec.ts
  - tests/e2e/portability/round-trip.spec.ts
  - tests/e2e/portability/zip-slip-regression.spec.ts
  - tests/e2e/portability/import-preview.spec.ts
  - tests/e2e/portability/highlights-export.spec.ts
  - tests/e2e/portability/a11y.spec.ts
autonomous: true
requirements: [PORT-01, PORT-02]
must_haves:
  truths:
    - "A bundle envelope with schemaVersion 1 and all five record blocks parses; an envelope with schemaVersion 2 is rejected by the schema"
    - "Every archive entry name that could escape the virtual root is refused by isSafeEntryName, including the mandated traversal corpus"
    - "The SHA-256 manifest of a parsed bundle is byte-identical after a JSON stringify/parse round-trip (determinism)"
    - "Playwright download events are capturable under this repo's config, proven by a real download event in the smoke spec"
    - "fflate 0.8.3 is pinned exact in dependencies and the app builds"
  artifacts:
    - path: "src/portability/bundle.ts"
      provides: "ExportBundleSchema versioned envelope composing the 5 existing record schemas"
      exports: ["ExportBundleSchema", "ExportBundle", "BUNDLE_FILENAME", "resolveAppVersion"]
    - path: "src/portability/zipSlip.ts"
      provides: "Pure Zip Slip guard + download-filename sanitization"
      exports: ["isSafeEntryName", "sanitizeFilename"]
    - path: "src/portability/manifest.ts"
      provides: "Deterministic per-block SHA-256 manifest"
      exports: ["sha256Hex", "Manifest", "computeManifest"]
    - path: "src/portability/download.ts"
      provides: "Cross-browser Blob + anchor download helper"
      exports: ["downloadBlob"]
    - path: "tests/unit/portability/zip-slip.test.ts"
      provides: "The mandated evil-entry regression corpus as unit truth"
    - path: "tests/e2e/portability/download-smoke.spec.ts"
      provides: "acceptDownloads config verification (RESEARCH A1 / Pitfall 9)"
  key_links:
    - from: "src/portability/bundle.ts"
      to: "src/content/schema.ts"
      via: "schema composition — imports ArticleSchema, LocationRecordSchema, HighlightRecordSchema, NoteRecordSchema, ReaderSettingsSchema"
      pattern: "from \"../content/schema\""
    - from: "src/portability/manifest.ts"
      to: "Web Crypto platform"
      via: "globalThis.crypto.subtle.digest(\"SHA-256\", bytes) — the server/safeFetch.ts L94 precedent"
      pattern: "crypto\\.subtle\\.digest"
    - from: "vite.config.ts"
      to: "src/portability/bundle.ts"
      via: "__APP_VERSION__ define consumed by resolveAppVersion()"
      pattern: "__APP_VERSION__"
---

<objective>
Wave-0/1 foundations for the versioned export/import bundle (D9-01..D9-05): install fflate, land the versioned bundle schema (PORT-01/02 versioning hook), the pure Zip Slip guard (SC#2 hard gate), the deterministic SHA-256 manifest (D9-03), and the shared download helper (D9-05) — plus the e2e scaffolding (download-capture smoke + 5 sentinel specs) that makes the Nyquist sampling contract hold from the first task.

Purpose: Every later portability plan composes these pure modules. The Zip Slip guard and manifest are the phase's security/integrity primitives and must exist with regression tests before any import path is written.
Output: 4 pure modules in src/portability/, 3 unit spec files, 6 e2e scaffold files, build wiring for __APP_VERSION__.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fflate, wire appVersion, scaffold Wave-0 e2e specs</name>
  <files>package.json, package-lock.json, vite.config.ts, src/vite-env.d.ts, tests/e2e/portability/download-smoke.spec.ts, tests/e2e/portability/round-trip.spec.ts, tests/e2e/portability/zip-slip-regression.spec.ts, tests/e2e/portability/import-preview.spec.ts, tests/e2e/portability/highlights-export.spec.ts, tests/e2e/portability/a11y.spec.ts</files>
  <read_first>
  - package.json (dependency pin style — mirror the exact-pin form of "@chenglou/pretext": "0.0.8")
  - vite.config.ts (defineConfig structure; the 07-07 note about cloudflare plugin removal)
  - playwright.config.ts (use block, projects, webServer)
  - tests/e2e/library/remove-cascade.spec.ts (BASE URL constant + image-stub route + beforeEach shape)
  - 09-RESEARCH.md §Standard Stack (fflate verification), §Validation Architecture → Wave 0 Gaps, §Assumptions Log A1/A5
  - 09-CONTEXT.md §code_context (integration points)
  </read_first>
  <action>
  1. Install fflate pinned exact: run npm install fflate@0.8.3 --save-exact so package.json dependencies contains "fflate": "0.8.3" (no caret) — fflate is the locked zip library per D9-02. fflate passed the Package Legitimacy Audit (verdict OK) in 09-RESEARCH.md — no checkpoint needed.
  2. vite.config.ts: inside defineConfig add a define block mapping the identifier __APP_VERSION__ to JSON.stringify of the version read from package.json (read it at config top with node:fs readFileSync — config runs in Node; do not hardcode the version string). This is RESEARCH A3; appVersion is diagnostic only (D9-04).
  3. Create src/vite-env.d.ts containing a triple-slash reference to vite/client and a declare const __APP_VERSION__: string; declaration.
  4. Create tests/e2e/portability/download-smoke.spec.ts: one test that navigates to the app base URL, then inside page.evaluate synthesizes a Blob download (URL.createObjectURL over new Blob with text content, a document.createElement("a") with download attribute "smoke.txt", appended, clicked, removed) while page.waitForEvent("download") captures the event; assert the suggested filename is "smoke.txt". This verifies acceptDownloads under the current playwright.config.ts (RESEARCH A1). If and only if this spec fails with a download timeout, add acceptDownloads: true to the use block of playwright.config.ts and re-run until green.
  5. Create 5 sentinel spec files in tests/e2e/portability/: round-trip.spec.ts, zip-slip-regression.spec.ts, import-preview.spec.ts, highlights-export.spec.ts, a11y.spec.ts. Each contains exactly one sentinel test (mirroring the Phase 04-02 Wave-0 precedent: a visible sentinel, not test.todo): navigate to base URL and assert the library heading "Saved articles" is visible. Include the image-stub page.route from remove-cascade.spec.ts's beforeEach. These prove the harness wires up in chromium/firefox/webkit and are REPLACED by real tests in Plan 09-06.
  6. Run the portability e2e dir on chromium to confirm all 6 files are green scaffolds.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/portability/ --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - package.json dependencies contains the exact entry "fflate": "0.8.3" with no semver range prefix
  - vite.config.ts contains the identifier __APP_VERSION__ inside a define block whose value derives from package.json at config load (no hardcoded copy of the version string)
  - src/vite-env.d.ts exists and declares __APP_VERSION__ as string
  - tests/e2e/portability/ contains exactly 6 spec files; npx playwright test tests/e2e/portability/ --project=chromium exits 0 (6+ sentinel cells green)
  - npm run build exits 0 (TypeScript + Vite build unaffected)
  </acceptance_criteria>
  <done>fflate installed exact-pinned; appVersion build wiring in place; download capture proven; 5 sentinel e2e scaffolds green on chromium.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExportBundleSchema + Zip Slip guard with regression corpus</name>
  <files>src/portability/bundle.ts, src/portability/zipSlip.ts, tests/unit/portability/bundle-schema.test.ts, tests/unit/portability/zip-slip.test.ts</files>
  <read_first>
  - src/content/schema.ts (FULL — ArticleSchema, LocationRecordSchema, HighlightRecordSchema, NoteRecordSchema, ReaderSettingsSchema, the z.literal schemaVersion-hook comment convention, z.infer type export convention)
  - 09-RESEARCH.md Pattern 1 (bundle envelope) + Pattern 2 (zipSlip — implement its semantics verbatim)
  - 09-PATTERNS.md §src/portability/bundle.ts + §src/portability/zipSlip.ts (analog conventions: header comment citing decision IDs, named regex constants, pure function doc contracts)
  - server/slugify.ts (module shape analog — named-regex-with-comment style, sanitize-then-fallback)
  </read_first>
  <behavior>
  - bundle-schema.test.ts: a fixture bundle with schemaVersion 1, valid exportedAt ISO datetime, all five record blocks (at least 1 article, 1 highlight, 1 note, 1 location, preferences) and fixtureIds ["some-fixture"] parses successfully via ExportBundleSchema.parse
  - bundle-schema.test.ts: schemaVersion 2 rejects; missing preferences rejects; missing fixtureIds rejects; malformed exportedAt (non-datetime string) rejects; an article row that violates ArticleSchema rejects
  - zip-slip.test.ts: isSafeEntryName returns false for each of: ../../evil.sh, ..%2F..%2Fevil.sh, /etc/passwd, C:\autoexec.bat (backslash form), dir/../../evil.sh, con, .., a name containing a NUL byte, name with only dots/empty after resolution, segment containing a colon (NTFS alternate data stream)
  - zip-slip.test.ts: isSafeEntryName returns true for: bundle.json, manifest.json, a/b/c.json, dir/./file.txt
  - zip-slip.test.ts: sanitizeFilename("What? A / Title \\ With <Junk>", "fallback") strips the reserved punctuation; sanitizeFilename("con", "fallback") returns "fallback"; sanitizeFilename("", "fallback") returns "fallback"; a 200-char title is capped at 120 chars
  </behavior>
  <action>
  1. Create src/portability/bundle.ts: header comment citing D9-01..D9-04. Export ExportBundleSchema = z.object with exactly these fields: schemaVersion z.literal(1) (the PORT-01/02 versioning hook — an importer seeing a higher version refuses), exportedAt z.string().datetime(), appVersion z.string(), articles z.array(ArticleSchema), locations z.array(LocationRecordSchema), highlights z.array(HighlightRecordSchema), notes z.array(NoteRecordSchema), preferences ReaderSettingsSchema (always present — D9-12), fixtureIds z.array(z.string()). Do NOT re-declare any record shape and do NOT add books/articleTags fields (research recommendation: absence is the forward-compatible form; tags already travel inside ArticleSchema.tags). Export type ExportBundle = z.infer. Export const BUNDLE_FILENAME = "lem-reader-bundle-v1.zip". Export function resolveAppVersion(): string that returns the __APP_VERSION__ global when typeof-defined and the literal "dev" otherwise (vitest has no define — the guard keeps unit tests runnable; diagnostic-only field per D9-04).
  2. Create src/portability/zipSlip.ts implementing RESEARCH Pattern 2 semantics exactly, as a pure module with no I/O: module-level OS_RESERVED regex for con/prn/aux/nul/com1-9/lpt1-9 (case-insensitive, anchored to segment end or dot); isSafeEntryName(rawName: string): boolean that refuses control characters (including NUL), any backslash, drive-letter prefixes, leading slash or tilde, then URL-decodes (decodeURIComponent failure = refuse) and re-checks backslash, then walks slash-separated segments virtually: skip empty and dot segments, a dot-dot segment popping an empty stack refuses (traversal escape), any other segment matching OS_RESERVED or containing a colon refuses, and a final empty stack refuses. sanitizeFilename(title: string, fallback: string): string stripping control chars and the characters slash, backslash, less-than, greater-than, colon, quote, pipe, question-mark, asterisk; collapsing whitespace; trimming; capping at 120 chars; returning fallback when the cleaned result is empty or OS_RESERVED. Header doc comment: pure functions, browser-context virtual path.resolve semantics (NO node:path — RESEARCH Pitfall 3), citing SC#2 and Pitfall 11 #5/#6.
  3. Create the two unit test files per the behavior block. In zip-slip.test.ts include the full mandated SC#2 corpus plus the valid controls — this file is the phase-exit regression corpus (extend it e2e-side in Plan 09-06). Pure describe/it/expect style; no fake-indexeddb needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/bundle-schema.test.ts tests/unit/portability/zip-slip.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/portability/bundle.ts exports ExportBundleSchema, ExportBundle, BUNDLE_FILENAME, resolveAppVersion; composes (imports) all five schemas from ../content/schema
  - src/portability/zipSlip.ts exports isSafeEntryName and sanitizeFilename; contains no import of node:path and no I/O
  - both unit spec files exit 0 with the corpus assertions above
  - npm run build exits 0
  </acceptance_criteria>
  <done>Versioned envelope schema and the pure Zip Slip guard exist with green regression tests covering the mandated evil corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic SHA-256 manifest + download helper</name>
  <files>src/portability/manifest.ts, src/portability/download.ts, tests/unit/portability/manifest.test.ts</files>
  <read_first>
  - server/safeFetch.ts (sha256Hex precedent at ~L91-99 — copy the crypto.subtle call + hex encoding exactly)
  - 09-RESEARCH.md Pattern 4 (manifest determinism contract) + Pattern 5 (download helper, incl. A2 deferred revoke)
  - src/portability/bundle.ts (ExportBundle type from Task 2)
  </read_first>
  <behavior>
  - manifest.test.ts: sha256Hex of the UTF-8 bytes of the string "abc" equals the known digest ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad (known-answer test — never trust a hash helper without one)
  - manifest.test.ts: build a sample bundle via ExportBundleSchema.parse (reuse the Task 2 fixture builder or export a shared sampleBundle() helper from bundle-schema.test.ts), compute computeManifest(bundle), then JSON.parse(JSON.stringify(bundle)) round-trip and recompute — the blocks objects are deep-equal (RESEARCH Pitfall 2 / A4 determinism proof)
  - manifest.test.ts: computeManifest returns algorithm "sha256" and exactly the five block keys articles, highlights, notes, locations, preferences
  </behavior>
  <action>
  1. Create src/portability/manifest.ts: export async function sha256Hex(bytes: Uint8Array): Promise<string> using globalThis.crypto.subtle.digest("SHA-256", bytes) and hex-encoding via toString(16).padStart(2, "0") — do NOT prefix with "sha256:" (that prefix is the Provenance/originalHtmlHash convention, not the manifest's). Export type Manifest = { algorithm: "sha256"; blocks: Record<"articles" | "highlights" | "notes" | "locations" | "preferences", string> }. Export async function computeManifest(bundle: ExportBundle): Promise<Manifest> whose contract (documented in a load-bearing comment) is: BOTH export and import hash JSON.stringify of the Zod-parsed block — never raw file bytes, never the pre-parse object — so key order is schema-deterministic on both sides (RESEARCH Pitfall 2). TextEncoder for string→bytes.
  2. Create src/portability/download.ts: export function downloadBlob(parts: BlobPart[], filename: string, mime: string): void — new Blob(parts, { type: mime }), URL.createObjectURL, synthesize an anchor element with href + download attribute, appendChild to document.body, click, remove the anchor, then URL.revokeObjectURL deferred via setTimeout(…, 0) (A2 — immediate revoke can race the click dispatch in WebKit). Shared by the .zip export and the .md highlights export (D9-05).
  3. Create tests/unit/portability/manifest.test.ts per the behavior block. Node 20+ exposes crypto.subtle globally, so no polyfill is needed under vitest.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/manifest.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/portability/manifest.ts exports sha256Hex, Manifest, computeManifest; contains the determinism-contract comment naming JSON.stringify-of-parsed-block on both sides
  - known-answer "abc" digest assertion passes; round-trip determinism assertion passes
  - src/portability/download.ts exports downloadBlob with the deferred revoke
  - npm run test:unit -- --run tests/unit/portability exits 0 (all three unit files)
  </acceptance_criteria>
  <done>Manifest hashing is deterministic and known-answer-verified; the shared cross-browser download helper exists.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-01)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `ExportBundleSchema`, `ExportBundle`, `BUNDLE_FILENAME`, `resolveAppVersion` | Zod schema + types + consts | src/portability/bundle.ts |
| `isSafeEntryName`, `sanitizeFilename` | pure functions | src/portability/zipSlip.ts |
| `sha256Hex`, `Manifest`, `computeManifest` | pure async functions + type | src/portability/manifest.ts |
| `downloadBlob` | browser helper | src/portability/download.ts |
| `__APP_VERSION__` | build-time define | vite.config.ts + src/vite-env.d.ts |
| fflate 0.8.3 | dependency | package.json |
| tests/unit/portability/{bundle-schema,zip-slip,manifest}.test.ts | test files | incl. the mandated evil-entry corpus |
| tests/e2e/portability/*.spec.ts (6 files) | e2e scaffolds | sentinels replaced in 09-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported archive bytes → app memory | untrusted zip file contents cross here (reader-picked file) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering + Elevation of Privilege | archive entry names (zipSlip.ts) | mitigate | isSafeEntryName virtual-resolve guard on EVERY entry; mandated evil corpus locked by tests/unit/portability/zip-slip.test.ts (SC#2 hard gate); wired into the import pipeline in Plan 09-04 |
| T-9-02 | DoS | zip extraction (decompression bomb) | mitigate | fflate unzip filter capping originalSize at 200,000,000 bytes — enforced in validateBundle (Plan 09-04 Task 2 acceptance) |
| T-9-03 | Tampering (V6) | manifest hashing | mitigate | crypto.subtle.digest platform primitive ONLY (never hand-roll); known-answer test proves correctness |
| T-9-SC | Tampering (supply chain) | npm install (fflate) | accept | fflate audited in 09-RESEARCH.md §Package Legitimacy Audit — verdict OK/Approved (67M dl/wk, no postinstall, Vite-internal); no blocking checkpoint required for an OK verdict |
| T-9-04 | Tampering | appVersion define | accept | diagnostic-only field (D9-04); no security decision reads it |
</threat_model>

<verification>
- npx playwright test tests/e2e/portability/ --project=chromium exits 0 (scaffolds + download smoke)
- npx vitest run tests/unit/portability exits 0 (all 3 unit files)
- npm run build exits 0
- package.json carries fflate 0.8.3 exact
</verification>

<success_criteria>
- The versioned envelope, Zip Slip guard, manifest, and download primitives exist as pure, unit-tested modules composing the existing schema surface (no record shape re-declared)
- The mandated traversal corpus is locked in unit tests before any import code exists
- The e2e harness for the whole phase is proven wired (download capture + sentinels) on chromium, with full-engine runs landing in later plans
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-01-SUMMARY.md` when done
</output>
