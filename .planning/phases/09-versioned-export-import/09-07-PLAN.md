---
phase: 09-versioned-export-import
plan: 07
type: execute
wave: 6
depends_on: ["09-06"]
files_modified:
  - tests/e2e/pagination/coverage-invariant.spec.ts
  - tests/e2e/pagination/no-overflow-invariant.spec.ts
  - tests/e2e/pagination/fallback-oversize.spec.ts
  - tests/e2e/pagination/termination.spec.ts
  - tests/e2e/annotations/capture-highlight.spec.ts
  - tests/e2e/ingestion/dexie-migration.spec.ts
  - .planning/phases/09-versioned-export-import/09-07-OUTPUT.md
  - .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md
autonomous: true
requirements: [PAGE-03, PAGE-04, ANNO-01, STATE-04]
must_haves:
  truths:
    - "The 24 pre-existing e2e failures recorded in Phase 08's deferred-items.md are diagnosed and fixed: 18 pagination cells (PAGE-03a/b/c + PAGE-04 specs), 3 capture-highlight cells (ANNO-01), 3 dexie-migration cells (STATE-04 v3→v4)"
    - "No existing assertion is weakened to achieve green (strengthen-only discipline, D6-12 spirit); any spec change re-authors assertions against current UI shapes, never removes coverage"
    - "FULL npm run test in ONE invocation exits 0 with honest pass/fail/skip counts recorded"
  artifacts:
    - path: ".planning/phases/09-versioned-export-import/09-07-OUTPUT.md"
      provides: "the permanent honest-suite record: literal command, per-suite + per-engine counts, literal exit code (mirrors 04-11-OUTPUT.md)"
    - path: "tests/e2e/ingestion/dexie-migration.spec.ts"
      provides: "v3→v4 migration proof re-authored against the LibraryView row shape"
  key_links:
    - from: "this plan"
      to: ".planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md"
      via: "closure note appended pointing at 09-07-OUTPUT.md once green"
      pattern: "09-07-OUTPUT"
---

<objective>
Close the pre-existing test-failure debt that Phase 08 logged forward (24 e2e cells in unrelated specs) so that Phase 9's honest full-suite exit gate — FULL npm run test, exit 0, per PROJECT.md Key Decision #9 — can actually go green, then run that gate and record the honest counts.

Purpose: STATE.md records the Phase 08 close-out with "A gap-closure plan is required to bring the full npm run test exit code to 0." This is that plan, mirroring the Phase 4 04-07..04-11 precedent. It is sequenced LAST (Wave 6) so it cannot destabilize Phase 9's own portability work, whose e2e gates (09-06) run scoped and completed green before this plan starts.
Output: fixed specs (+ any justified production fix at the measurement/pagination seam), 09-07-OUTPUT.md honest-suite record, deferred-items closure note.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
@.planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix the 18 pagination regression cells</name>
  <files>tests/e2e/pagination/coverage-invariant.spec.ts, tests/e2e/pagination/no-overflow-invariant.spec.ts, tests/e2e/pagination/fallback-oversize.spec.ts, tests/e2e/pagination/termination.spec.ts</files>
  <read_first>
  - .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md (the recorded failure table + suspected root cause)
  - the four spec files above (FULL — understand what each asserts before touching anything)
  - src/measurement/useMeasurement.ts + src/measurement/engine.ts (measureAllBlocks path; the suspected Vite 8/Rolldown microtask-timing sensitivity)
  - src/reader/PaginatedSurface.tsx (the always-mounted hidden .article-body-measurement wrapper from Plan 04-08 — partial-DOM defense)
  - STATE.md decisions for Phase 04-06/04-08/04-09 (pre-captured line boxes, data-block-index mapping, Option A)
  </read_first>
  <action>
  1. Reproduce first, in isolation, on all three engines: run the four specs (they are expected RED on 18 cells — technical-post@360x640 and unsupported-case@360x640 returning status fallback in coverage-invariant + no-overflow-invariant, and the two timeout specs fallback-oversize + termination where the pagination result never commits).
  2. Diagnose at the engine/measurement seam. Leading hypothesis from deferred-items.md: measureAllBlocks microtask ordering under the current Vite 8 / Rolldown build — the engine's trusted-commit path is sensitive to when fonts settle / when the hidden measurement clone is queried relative to effect scheduling. Verify with the existing DEV hooks (__lemLastTrustedConstraints, __lemDiagnosticBus, __lemPagination) before changing anything.
  3. Fix the ROOT CAUSE in src/measurement or src/pagination (files_modified may extend to those files when justified by the diagnosis). Do NOT fix by: loosening the specs, removing assertions, adding retries/sleeps to make timeouts pass, or skipping engines. The assertions encode PAGE-03/PAGE-04 invariants — strengthen-only.
  4. If the diagnosis shows the specs encode a contract the engine can no longer satisfy for a legitimate reason (e.g., a corpus fixture changed shape in Phase 8), surface it in the SUMMARY as a Rule 4 finding with evidence rather than silently rewriting the expectation.
  5. Confirm: all four specs green × chromium/firefox/webkit (18 cells), and the pagination-related suites that were already green stay green (run the whole tests/e2e/pagination/ directory).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/pagination/</automated>
  </verify>
  <acceptance_criteria>
  - npx playwright test tests/e2e/pagination/ exits 0 on all three engines (the previously-failing 18 cells now pass)
  - git diff on the four spec files shows no removed assertions (strengthen-only); any production fix is accompanied by a diagnosis note in the SUMMARY citing the observed mechanism
  - the previously-green pagination cells do not regress
  </acceptance_criteria>
  <done>The Phase 4 pagination suite is green again across engines with the root cause fixed (or honestly surfaced), not the bar lowered.</done>
</task>

<task type="auto">
  <name>Task 2: Fix capture-highlight (3 cells) + dexie-migration (3 cells)</name>
  <files>tests/e2e/annotations/capture-highlight.spec.ts, tests/e2e/ingestion/dexie-migration.spec.ts</files>
  <read_first>
  - tests/e2e/annotations/capture-highlight.spec.ts (FULL — the figure-heavy caption case, D5-07) + any error-context.md referenced by deferred-items.md
  - src/annotations/capture.ts (captureSelection — raw-cluster↔norm-cluster alignment) + the figure caption rendering path (src/content/render/BlockRenderer.tsx figure/caption branch)
  - tests/e2e/ingestion/dexie-migration.spec.ts (FULL — the v3→v4 upgrade + *tags index case)
  - src/ingestion/library/LibraryView.tsx (the current row shape: the aria-labelledby title-{id} pattern + the Open article link — what the stale assertion must be re-authored against)
  - .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md §2 + §3 (recommended approaches)
  </read_first>
  <action>
  1. capture-highlight figure-caption case: reproduce on all engines; determine whether the selection regression shares the Task 1 timing root cause (fix there propagates green here) or is independent (then fix the capture path honestly — likely the DOM Range→grapheme mapping over figure caption text). The assertion (a caption selection becomes a highlight) must survive; only the mechanism may change.
  2. dexie-migration v3→v4 case: this is the recorded test-shape staleness — the spec was last green before Plan 08-03 replaced FixtureList with LibraryView and still expects the old row shape. Re-author the assertion against the current LibraryView surface (the article link located via the aria-labelledby title-{id} pattern / the "Open article" accessible name), preserving the SAME semantics: a seeded v3 article row survives the v4 upgrade, appears in the library, and the additive *tags index is declared (Pitfall 9 unchanged — no production change expected; if a production cause is found instead, fix that and document).
  3. Confirm both specs green × 3 engines (6 cells), with the annotations + ingestion directories not regressing.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/ tests/e2e/ingestion/</automated>
  </verify>
  <acceptance_criteria>
  - the figure-caption capture case passes on all three engines with the D5-07 assertion intact
  - the dexie-migration spec asserts the v3-row-survives semantics against the current LibraryView row shape (test re-author; zero production changes unless a real defect is found and documented)
  - npx playwright test tests/e2e/annotations/ tests/e2e/ingestion/ exits 0
  </acceptance_criteria>
  <done>All six remaining pre-existing cells are green with semantics preserved against current UI shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite gate + permanent record</name>
  <files>.planning/phases/09-versioned-export-import/09-07-OUTPUT.md, .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md</files>
  <read_first>
  - .planning/phases/04-*/04-11-OUTPUT.md (the format to mirror — the Phase 4 honest-suite record; locate via glob .planning/phases/04-*/04-11-OUTPUT.md or the milestone archive)
  - package.json (the test script — npm run test = unit then e2e)
  - .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md (to append the closure note)
  </read_first>
  <action>
  1. Run the FULL suite in ONE invocation: npm run test — no subset, no --grep, no engine skip, no watch flags. The executor runs it itself and observes the terminal directly (never trust a prior SUMMARY's numbers — Key Decision #9 anti-pattern guard).
  2. Record to .planning/phases/09-versioned-export-import/09-07-OUTPUT.md: the literal command, the per-suite counts (vitest unit; playwright per engine), combined passed/failed/skipped, and the literal exit code. Expected: exit 0 with 0 failed. If ANY failure appears, do not massage it — record it, fix it (in-scope: it is now this plan's job), and re-run the full invocation until a clean exit-0 run is recorded honestly.
  3. Append a closure note to .planning/phases/08-markdown-pipeline-and-personal-library/deferred-items.md stating the 24 cells were closed by Phase 9 Plan 09-07 with a pointer to 09-07-OUTPUT.md.
  4. Note in the SUMMARY the known intentional skips (Phase 08 recorded 7 unit + 6 e2e skipped as deliberate) so the exit-0 gate is not misread as zero-skips.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
  - npm run test exits 0 in a single invocation; the literal exit code and honest counts appear in 09-07-OUTPUT.md
  - 0 failed across unit + all three e2e engines; skipped counts recorded as-observed
  - deferred-items.md carries the closure note referencing 09-07-OUTPUT.md
  </acceptance_criteria>
  <done>The full suite is honestly green end-to-end and permanently recorded; Phase 9's exit gate satisfies PROJECT.md Key Decision #9.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-07)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `.planning/phases/09-versioned-export-import/09-07-OUTPUT.md` | honest-suite record | literal command + per-engine counts + exit code |
| Fixed pagination/capture/migration specs + any diagnosis-justified src fix | repairs | 24 pre-existing cells closed |
| deferred-items.md closure note | bookkeeping | Phase 08 debt marked closed |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan touches tests, the measurement/pagination seam, and a record file; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-21 | Repudiation (process honesty) | full-suite gate | mitigate | executor runs npm run test itself, records observed counts + literal exit code in 09-07-OUTPUT.md — the 04-11 anti-pattern guard (no trusting prior SUMMARYs, no subset runs) |
| T-9-22 | Tampering (test weakening) | spec repairs | mitigate | strengthen-only acceptance criteria on every spec change; assertion-removal shows in git diff and fails the Task 1 gate |
</threat_model>

<verification>
- npx playwright test tests/e2e/pagination/ exits 0 (Task 1)
- npx playwright test tests/e2e/annotations/ tests/e2e/ingestion/ exits 0 (Task 2)
- npm run test exits 0 — the phase's honest full-suite gate (Task 3)
</verification>

<success_criteria>
- The 24-cell pre-existing deficit recorded at Phase 08 close-out is closed with root-cause fixes (or honestly surfaced Rule 4 findings), not lowered bars
- The FULL suite exits 0 in one invocation with a permanent honest record — Phase 9's exit gate is green end-to-end including all portability work
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-07-SUMMARY.md` when done
</output>
