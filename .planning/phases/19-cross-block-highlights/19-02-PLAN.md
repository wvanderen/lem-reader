---
phase: 19-cross-block-highlights
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/annotations/excerpt.ts
  - src/routes/review/ReviewView.tsx
  - src/routes/review/DeleteHighlightConfirm.tsx
  - src/reader/annotations/AnnotationsDrawer.tsx
  - src/reader/annotations/NotePopover.tsx
  - src/portability/markdown.ts
  - tests/unit/annotations/excerpt.test.ts
  - tests/unit/portability/markdown.test.ts
autonomous: true
requirements: [ANNO-10, ANNO-11]
must_haves:
  truths:
    - "A cross-block highlight's review row shows its FIRST FRAGMENT then a calm ellipsis — never a truncated multi-block blob (D19-10)"
    - "Every surface that renders quote.exact (review row + its aria-labels, drawer, popover, delete-confirm) derives text through ONE shared helper with its per-surface caps unchanged"
    - "A complete single-fragment highlight NEVER gets an ellipsis; the ellipsis appears only on genuine continuation or length truncation (excerpt honesty rule)"
    - "Markdown export renders a cross-block highlight as ONE entry whose block breaks are preserved as blockquote continuation lines, with per-line escaping (D19-12)"
    - "A continuation line beginning with a structure-forging prefix (#, -, 1974.) is escaped and cannot break out of the blockquote (V5 structure-injection guard)"
  artifacts:
    - path: src/annotations/excerpt.ts
      provides: "firstFragmentExcerpt(exact, maxChars) — pure derivation, no React/DOM"
    - path: tests/unit/annotations/excerpt.test.ts
      provides: "Excerpt honesty unit cells"
  key_links:
    - from: src/annotations/excerpt.ts
      to: src/routes/review/ReviewView.tsx
      via: firstFragmentExcerpt replaces raw quote.exact at the excerpt sites
      pattern: "firstFragmentExcerpt"
    - from: src/routes/review/ReviewView.tsx
      to: src/routes/review/DeleteHighlightConfirm.tsx
      via: the confirm's excerpt prop is computed once in ReviewView (single derivation site)
      pattern: "excerpt=\\{"
    - from: src/portability/markdown.ts
      to: src/portability/markdown.ts
      via: blockLines splits exact on "\\n" and applies escapeMarkdownLine PER LINE, marker on first line only
      pattern: "escapeMarkdownLine"
  prohibitions:
    - statement: "No multi-block indicator anywhere — no block-count badge, no span-styling chrome on review rows (D19-11)"
      status: resolved
      verification: "ReviewRow anatomy byte-stable except the excerpt derivation swap; diff adds no className/element to rows"
    - statement: "The ellipsis character is exactly one U+2026 and never appears on a complete single-fragment excerpt"
      status: resolved
      verification: "excerpt.test.ts honesty cells (single-fragment no-ellipsis, continuation ellipsis, truncation ellipsis)"
    - statement: "Export NEVER truncates — the full span exports; ellipsis is a review-surface behavior only (UI-SPEC export copy shape)"
      status: resolved
      verification: "markdown.test.ts multi-line cell asserts the full exact round-trips in the entry"
---

<objective>
Ship the EXCERPT + EXPORT derivation layer (ANNO-11 surfaces, the ANNO-10 export leg of durability):
one pure `firstFragmentExcerpt` helper adopted at every quote.exact surface (D19-10), and the
multi-line Markdown export shape that keeps block breaks inside ONE entry with per-line escaping
(D19-12 — the phase's one security-adjacent change per 19-RESEARCH V5). Pure derivations from
`quote.exact`; independent of capture/rendering changes, unit-provable standalone.

Purpose: review/edit/export/delete operate on the whole highlight atomically (ANNO-11) — these
surfaces must describe a span honestly (first fragment + …) and export it structurally safely.
Output: `src/annotations/excerpt.ts` + helper adoption at 4 surfaces; `markdown.ts` multi-line
`blockLines`; unit cells for both.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@.planning/phases/19-cross-block-highlights/19-RESEARCH.md
@.planning/phases/19-cross-block-highlights/19-PATTERNS.md
@.planning/phases/19-cross-block-highlights/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure firstFragmentExcerpt helper + unit cells</name>
  <files>src/annotations/excerpt.ts, tests/unit/annotations/excerpt.test.ts</files>
  <read_first>
  - src/annotations/overlap.ts (the small-pure-module pattern to copy: header contract comment, one
    pure function, jsdom-safe, no DOM/React)
  - src/content/normalizeText.ts (BLOCK_SEPARATOR definition — the "\n" the helper splits on)
  - src/routes/review/ReviewView.tsx L84-91 (the truncate + caps constants discipline the helper
    parameterizes)
  - 19-RESEARCH.md §Code Examples "First-fragment excerpt helper" + 19-UI-SPEC §Component Inventory
    (NEW helper contract) and §Typography (excerpt honesty rule, locked)
  </read_first>
  <action>
  Create `src/annotations/excerpt.ts` mirroring overlap.ts's module shape (header comment citing
  D19-10 + the honesty rule). Export ONE function:
  `firstFragmentExcerpt(exact: string, maxChars: number): string` —
  take `exact` up to the first `"\n"` (BLOCK_SEPARATOR); if the first fragment is shorter than the
  whole exact (genuine continuation), append the single ellipsis character U+2026 AFTER the length
  cap; if the fragment alone exceeds maxChars, the cap itself appends U+2026; a complete
  single-fragment highlight within the cap gets NO ellipsis. Follow the 19-RESEARCH Code Examples
  sketch (truncate-then-conditional-ellipsis), planner owns the exact composition so the three cases
  above are each distinguishable. No React, no DOM, no side effects.
  Create `tests/unit/annotations/excerpt.test.ts` with cells: single-fragment under cap → identical
  text, no ellipsis; single-fragment over cap → truncated + ellipsis; multi-block exact (contains
  separators) → first fragment + ellipsis; multi-block whose first fragment also exceeds cap →
  truncated first fragment + exactly ONE ellipsis; empty-string exact is impossible per schema
  (`.min(1)`) — assert the function never invents content for it (returns empty/ellipsis-free empty).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/excerpt.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "export function firstFragmentExcerpt" src/annotations/excerpt.ts` finds the export
  - excerpt.test.ts contains a cell asserting NO ellipsis on a complete single fragment (grep the
    `.not.toContain("…")` or equivalent assertion)
  - excerpt.test.ts contains a continuation cell asserting exactly one `…`
  - `npx vitest run tests/unit/annotations/excerpt.test.ts` exits 0
  - `! grep -rn "react\|document\." src/annotations/excerpt.ts` exits 0 (pure module)
  </acceptance_criteria>
  <done>One pure helper with locked honesty semantics, unit-proven for all three ellipsis cases.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt the helper at every quote.exact surface</name>
  <files>src/routes/review/ReviewView.tsx, src/routes/review/DeleteHighlightConfirm.tsx, src/reader/annotations/AnnotationsDrawer.tsx, src/reader/annotations/NotePopover.tsx</files>
  <read_first>
  - src/routes/review/ReviewView.tsx (excerpt L153 + visible L169; EXCERPT_MAX_CHARS=120 /
    ARIA_MAX_CHARS=60 constants L84-91; DeleteHighlightConfirm excerpt prop ~L527)
  - src/reader/annotations/AnnotationsDrawer.tsx (excerpt L181; visible L219-221; aria L202-204)
  - src/reader/annotations/NotePopover.tsx (excerpt L92; visible L225-228; description L262-264)
  - src/routes/review/DeleteHighlightConfirm.tsx (the excerpt prop consumer — structural clone
    discipline; no excerpt computation inside)
  - 19-UI-SPEC.md §Component Inventory "NEW: shared first-fragment excerpt helper" (per-surface caps
    table; per-slice mark aria-labels KEEP slice-local text — do not touch InlineRenderer here)
  </read_first>
  <action>
  Switch every `quote.exact` excerpt derivation to `firstFragmentExcerpt(exact, cap)` — the local
  `truncate` helpers remain ONLY where they still serve non-excerpt text (note previews); do not
  delete them if other call sites remain:
  1. ReviewView.tsx: the visible `.review-quote` (cap 120) AND the excerpt prefixes inside the
     Edit-note / Remove / Go-to-highlight aria-labels (cap 60) — derive the base excerpt ONCE per
     entry and cap twice, or call the helper twice with the two caps (match the file's existing
     structure). The DeleteHighlightConfirm `excerpt` prop switches to the same derived value (cap 200).
  2. AnnotationsDrawer.tsx: `.drawer-entry-excerpt` visible (120) + jump aria-label (60).
  3. NotePopover.tsx: the popover excerpt block (cap 200) + accessible-description (60).
  4. DeleteHighlightConfirm.tsx: no computation change (it consumes the prop) — verify only; touch
     the file only if a cap constant must move.
  Per-surface caps stay EXACTLY as shipped (the helper parameterizes; it does not unify caps).
  ReviewRow anatomy is otherwise byte-stable — no new elements, classes, or badges (D19-11).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations && npx playwright test tests/e2e/annotations/drawer-view.spec.ts tests/e2e/annotations/note-popover-focus.spec.ts tests/e2e/annotations/delete-confirm.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "firstFragmentExcerpt" src/routes/review/ src/reader/annotations/` finds adoption in
    ReviewView.tsx, AnnotationsDrawer.tsx, NotePopover.tsx
  - `! grep -n "quote.exact" src/routes/review/ReviewView.tsx | grep -v firstFragmentExcerpt` — every
    excerpt site routes through the helper (raw exact no longer feeds visible text directly)
  - Constants EXCERPT_MAX_CHARS / ARIA_MAX_CHARS (and drawer/popover equivalents) values unchanged
  - ReviewRow renders no new badge/element (diff adds derivation swap only)
  - The three touched spec files pass on 3 engines (byte-stable chrome regression)
  </acceptance_criteria>
  <done>All excerpt surfaces describe spans as first-fragment + honest ellipsis with shipped caps;
  no span chrome anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-line Markdown export (D19-12)</name>
  <files>src/portability/markdown.ts, tests/unit/portability/markdown.test.ts</files>
  <read_first>
  - src/portability/markdown.ts (FULL — blockLines L158-167 the change site; escapeMarkdownLine
    L71-86; markerFor; HighlightSection; citationLine)
  - tests/unit/portability/markdown.test.ts (existing conventions + where highlight-entry cells live)
  - 19-RESEARCH.md §Pitfall 3 + §Code Examples "Multi-line Markdown export" (the per-line escape rule)
  - 19-UI-SPEC.md §Copywriting Contract "Export copy shape"
  </read_first>
  <action>
  Change `blockLines` so a highlight's quote renders as ONE entry over multiple lines: split
  `e.highlight.quote.exact` on `"\n"`; emit one `> `-prefixed line per fragment with
  `escapeMarkdownLine` applied PER LINE; the status marker (`markerFor(e.status)`) prefixes ONLY the
  first line; empty fragments (consecutive separators) still yield their own escaped (empty)
  continuation line — never collapsed, so block-break count is preserved verbatim. The citation and
  Note lines keep their existing single-line shapes and positions. This is the D19-12 contract AND
  the V5 structure-injection guard: a later block beginning with `#`, `-`, or an ordered-list marker
  digits-period form must not forge Markdown structure (escapeMarkdownLine already handles the
  leading-marker cases — applying it per line extends it to every fragment).
  Add unit cells in tests/unit/portability/markdown.test.ts: (a) a two-block span exact (text +
  separator + text) exports as two `> ` lines in ONE entry with the marker on line 1 only; (b) a
  continuation line starting with a heading-sign prefix and one starting with an ordered-list-style
  numeric prefix are escaped (assert the backslash-escaped or otherwise neutralized form that
  escapeMarkdownLine produces — read its implementation first and assert ITS actual contract);
  (c) a code-block highlight whose verbatim source contains embedded newlines round-trips fully
  (no truncation — export completeness); (d) note + citation lines still follow the quote block.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - markdown.ts blockLines contains a split of exact on the separator and applies
    escapeMarkdownLine per line (grep `escapeMarkdownLine` count ≥ fragments loop usage)
  - `markerFor` is invoked once per entry (first line only)
  - Unit cell (b) asserts a structure-forging continuation line cannot produce an unescaped `#`- or
    list-marker-initial exported line
  - Unit cell (c) asserts full multi-line round-trip (exported text contains every source line)
  - `npx vitest run tests/unit/portability/markdown.test.ts` exits 0
  </acceptance_criteria>
  <done>One entry per span, block breaks preserved, per-line escaping, marker on first line —
  D19-12 + the V5 guard hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored quote.exact → exported Markdown | Persisted (possibly imported) text crosses into a portable document format |
| Stored quote.exact → visible UI text | Persisted text renders in review/drawer/popover/confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | markdown.ts multi-line exact | mitigate | escapeMarkdownLine PER LINE (D19-12) — a continuation line cannot forge blockquote-breaking structure; unit cell (b) locks it |
| T-19-05 | Tampering | Excerpt/note rendering (all 4 surfaces) | accept | React text children only; react/no-danger ESLint active; no new HTML parsing (verified discipline, extended not forked) |
| T-19-06 | Information Disclosure | Excerpts in export | accept | Export is reader-initiated over their own library (existing PORT contract); no new fields |
</threat_model>

<verification>
- `npx vitest run tests/unit/annotations/excerpt.test.ts tests/unit/portability/markdown.test.ts` — green
- `npx playwright test tests/e2e/annotations/drawer-view.spec.ts tests/e2e/annotations/note-popover-focus.spec.ts tests/e2e/annotations/delete-confirm.spec.ts` — byte-stable chrome regression, 3 engines
- Grep: firstFragmentExcerpt adopted at all excerpt sites; no raw quote.exact feeds visible excerpt text
</verification>

<success_criteria>
- Review/drawer/popover/confirm describe a span as its first fragment + calm ellipsis with unchanged caps (D19-10)
- A complete single-fragment highlight never shows an ellipsis (honesty rule)
- Export emits ONE structurally-safe multi-line entry per span (D19-12) — atomically reviewable, editable, deletable (ANNO-11 surface layer)
- No new chrome, no span badges, zero packages, zero schema changes
</success_criteria>

## Artifacts this phase produces

*This plan:*
- `firstFragmentExcerpt(exact, maxChars)` — exported from NEW `src/annotations/excerpt.ts`
- `blockLines` multi-line rendering contract in `src/portability/markdown.ts` (internal, unchanged signature)
- NEW test files: `tests/unit/annotations/excerpt.test.ts`

*From Plan 19-01 (already shipped before this plan merges if waves interleave):* `boundary-ineligible`
/ `empty-span` reason literals, `span-capture.spec.ts`. *Later plans:* `HighlightSlice.isFirst` +
`sliceCodeForHighlights` + `nested-list-paths` fixture (19-03), per-page first-occurrence id pass
(19-04), `eligibility-matrix.spec.ts` (19-05).

<output>
Create `.planning/phases/19-cross-block-highlights/19-02-SUMMARY.md` when done
</output>
