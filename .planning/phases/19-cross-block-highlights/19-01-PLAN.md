---
phase: 19-cross-block-highlights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/annotations/capture.ts
  - src/reader/annotations/HighlightOverlay.tsx
  - src/reader/annotations/SelectionToolbar.tsx
  - tests/unit/annotations/capture-offset-mapping.test.ts
  - tests/e2e/annotations/capture-rejects.spec.ts
  - tests/e2e/annotations/span-capture.spec.ts
autonomous: true
requirements: [ANNO-08, ANNO-12]
must_haves:
  truths:
    - "A native selection spanning two eligible mounted blocks captures ONE highlight — one Dexie record, marks on both blocks (ANNO-08)"
    - "A selection with an endpoint inside an unsupported block refuses the WHOLE selection and shows the new boundary-ineligible hint (D19-05/D19-06)"
    - "A caption endpoint in a figure with non-empty alt stores the correct article-global offset — the Pitfall 1 alt-divergence is fixed"
    - "Backwards drags and page-slice endpoints (data-block-grapheme-start on both endpoints) compose to the same global range as forwards selections"
    - "A new span (single- or cross-block) overlapping any existing highlight's global range is refused with the existing overlap hint (D19-07)"
  artifacts:
    - path: src/annotations/capture.ts
      provides: "Endpoint-composed span capture; CaptureResult reason union without multi-block, with boundary-ineligible + empty-span"
    - path: tests/e2e/annotations/span-capture.spec.ts
      provides: "Span success cells (scrolling + paginated-within-page) incl. the retired D5-06 two-block selection"
    - path: tests/unit/annotations/capture-offset-mapping.test.ts
      provides: "Span composition offset cells, backwards Range, slice endpoints, figure caption alignment, refusal taxonomy"
  key_links:
    - from: src/annotations/capture.ts
      to: src/reader/annotations/HighlightOverlay.tsx
      via: CaptureResult.reason union flows into CreateFromSelectionResult — both unions change in ONE commit
      pattern: "boundary-ineligible"
    - from: src/reader/annotations/HighlightOverlay.tsx
      to: src/reader/annotations/SelectionToolbar.tsx
      via: reason string → hint copy ternary (retire multi-block branch, add boundary-ineligible branch)
      pattern: "selection-toolbar-hint"
  prohibitions:
    - statement: "No largest-eligible-sub-span narrowing machinery exists — refusal is whole-selection only (D19-05)"
      status: resolved
      verification: "capture.ts returns {ok:false, reason} with NO position on refusals; unit refusal cells assert reason-only; diff review shows no range-shrinking computation"
    - statement: "Zero Dexie/schema changes this plan (Pitfall 9 — storage model already cross-block-capable)"
      status: resolved
      verification: "git diff shows no changes under src/content/schema.ts, src/persistence/, src/content/normalizeText.ts"
    - statement: "No walking of intermediate blocks' DOM during capture (19-RESEARCH anti-pattern)"
      status: resolved
      verification: "capture.ts composes the global range from the TWO endpoints' findBlockAncestor + per-endpoint offset math only"
---

<objective>
Ship the span CAPTURE core (ANNO-08 + the ANNO-12 refusal taxonomy): replace the D5-06
single-block gate in `captureSelection` with per-endpoint global-offset composition, fix the
figure-caption alt-divergence (Pitfall 1), retire the `"multi-block"` reason across all three
reason unions, and land the one new reader-facing string (the boundary-ineligible toolbar hint,
D19-06). The D5-07 exhaustive eligibility switch, D5-08 slice math, D5-13/D19-07 global overlap
check, and the storage layer are REUSED unchanged.

Purpose: capture is the first of the three real work surfaces (capture / rendering / excerpts);
without it no span exists to render, review, or export.
Output: span-capturing `capture.ts`, synced unions + toolbar copy, flipped/extended unit cells,
and the new `span-capture.spec.ts` e2e success cells (the retired D5-06 refusal test moves here —
sanctioned churn per 19-RESEARCH Pitfall 7).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@.planning/phases/19-cross-block-highlights/19-RESEARCH.md
@.planning/phases/19-cross-block-highlights/19-PATTERNS.md
@.planning/phases/19-cross-block-highlights/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint-composed span capture in capture.ts</name>
  <files>src/annotations/capture.ts</files>
  <read_first>
  - src/annotations/capture.ts (FULL — the L332-399 captureSelection path, findBlockAncestor,
    buildRawToNormMap, domRangeToIntraBlockGraphemeRange, isEligibleBlock L254-268,
    computeBlockGlobalStart L276-287)
  - src/content/normalizeText.ts (blockText L41-63 — the figure join rule
    `[alt, inlineText(caption)].filter(Boolean).join(BLOCK_SEPARATOR)`; BLOCK_SEPARATOR; graphemeClusters)
  - src/content/schema.ts (TextPositionSelectorSchema.refine(end > start) ~L383; Block union kinds)
  - 19-PATTERNS.md §capture.ts assignment (verbatim excerpts of the gate, slice math, composition)
  </read_first>
  <action>
  Restructure `captureSelection` from single-block to per-endpoint composition (19-RESEARCH Pattern 1 —
  endpoint-only, NO intermediate DOM walk):
  1. Keep the existing `findBlockAncestor` calls for BOTH endpoints and the `"ineligible"` reason when
     either ancestor is missing. DELETE the `startBlock !== endBlock` → `"multi-block"` gate (L338-340).
  2. Extract the existing per-endpoint resolution (data-block-index parse, `isEligibleBlock`, the D5-08
     `data-block-grapheme-start` slice math, `domRangeToIntraBlockGraphemeRange`, `computeBlockGlobalStart`)
     into a helper applied independently to each endpoint, producing
     `{ blockIndex, intraStart, intraEnd }` per side. The single-block path remains the degenerate case
     on the SAME code path (no separate branch).
  3. ELIGIBILITY SPLIT per the new taxonomy: when an endpoint's resolved block fails `isEligibleBlock`
     (i.e. `unsupported`), return the NEW reason `"boundary-ineligible"` (D19-05/D19-08 — reject whole);
     keep `"ineligible"` for missing ancestor / bad index / outside readingRoot. Footnote bodies
     naturally return `"ineligible"` (no data-block-index ancestor — 19-RESEARCH Pitfall 6; document in
     a comment).
  4. FIGURE CAPTION ALIGNMENT (Pitfall 1 — load-bearing bug that exists TODAY): when the endpoint block
     is a `figure`, compute `captionLocalStart = block.alt.length > 0 ? graphemeClusters(block.alt, article.lang).length + BLOCK_SEPARATOR.length : 0`
     (the filter(Boolean) join means empty alt ⇒ 0), slice `fullNormClusters` from `captionLocalStart`
     before passing to `domRangeToIntraBlockGraphemeRange`, then add `captionLocalStart` back to the
     returned intra range — the exact shape of the shipped D5-08 slice-offset math (PATTERNS captures it
     verbatim). Update the L358-363-style divergence comment to state capture is now aligned.
  5. Compose ONE global TextPositionSelector: `start = computeBlockGlobalStart(idxA) + intraA.start`,
     `end = computeBlockGlobalStart(idxB) + intraB.end`. DOM Range already normalizes document order —
     do NOT add a swap step (Pitfall 9 validated). Add the defensive composed-`start === end` check →
     new reason `"empty-span"` (never reachable from a non-collapsed Range; matches the schema refine).
  6. Reason union (L49-54): retire `"multi-block"`; the false variant becomes
     `"empty" | "ineligible" | "measurement-body" | "boundary-ineligible" | "empty-span"`. Keep the
     ok-variant's `blockIndex` field carrying the START endpoint's index (grep-verified consumers read
     only `.ok`/`.position`; document this in the union's doc comment).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/capture-offset-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - capture.ts contains NO occurrence of the retired reason literal: `! grep -rn '"multi-block"' src/annotations/capture.ts` exits 0
  - capture.ts contains `boundary-ineligible` and `empty-span` in the CaptureResult union
  - capture.ts contains a captionLocalStart computation gated on `block.alt` being non-empty (Pitfall 1)
  - The eligibility switch `isEligibleBlock` is byte-unchanged (exhaustive, no default — Pattern F)
  - `git diff --stat` shows zero changes to src/content/schema.ts, src/persistence/, src/content/normalizeText.ts
  - Unit cells (Task 3) prove: two-paragraph span ok:true with exact global offsets; backwards Range
    equals forwards composition; slice-start attributes on both endpoints compose; figure-with-alt
    caption endpoint lands at the caption's true global offset; unsupported endpoint →
    `boundary-ineligible`; composed-empty → `empty-span`
  </acceptance_criteria>
  <done>Span capture composes one global range from endpoints alone; the reason taxonomy is
  {empty, ineligible, measurement-body, boundary-ineligible, empty-span}; the figure alt-offset is
  fixed; schema/persistence untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the three reason unions + toolbar refusal copy</name>
  <files>src/reader/annotations/HighlightOverlay.tsx, src/reader/annotations/SelectionToolbar.tsx</files>
  <read_first>
  - src/reader/annotations/HighlightOverlay.tsx (ToolbarCaptureResult L64-66, CreateFromSelectionResult
    L81-87, captureCurrentSelection L182-201 — the D19-07 overlap seam, ALREADY global-range)
  - src/reader/annotations/SelectionToolbar.tsx (hint ternary L242-252, buttons side, role=toolbar anatomy)
  - 19-UI-SPEC.md §Copywriting Contract (the ONE new string, verbatim; empty-span maps to existing empty copy)
  - src/annotations/capture.ts (Task 1's final union — the source of truth to mirror)
  </read_first>
  <action>
  One coordinated change across the union chain (D19-06/D19-09; UI-SPEC regression targets):
  1. HighlightOverlay.tsx `CreateFromSelectionResult.reason`: retire `"multi-block"`, add
     `"boundary-ineligible"` and `"empty-span"` so the union stays aligned with CaptureResult.
     `ToolbarCaptureResult` keeps `"overlap"` unchanged. The `captureCurrentSelection` overlap check
     needs ZERO math change (D19-07 is caller discipline — it already checks
     `rangesOverlap(capture.position, pos)` against every resolved highlight's global range; single-
     and cross-block spans flow through identically). Update the surrounding doc comments that still
     describe the retired D5-06 vocabulary.
  2. SelectionToolbar.tsx hint ternary: DELETE the retired multi-block branch and its D5-06-era copy
     string (the hint at L244-245 — do not reproduce it anywhere in src). Add a `"boundary-ineligible"`
     branch rendering EXACTLY: `This selection includes content that can't be highlighted.` (the ONE
     new reader-facing string, UI-SPEC verbatim e2e anchor). Map `"empty-span"` to the EXISTING
     empty-selection copy (reader-indistinguishable; zero new strings). Overlap / empty / ineligible
     branches stay byte-stable. The BUTTONS side is untouched (D19-09 — Highlight and Highlight + note
     render and fire identically for spans; no span feedback, no block count).
  </action>
  <verify>
    <automated>! grep -rn '"multi-block"' src/ && ! grep -rq "Select within a single block" src/</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "This selection includes content that can't be highlighted." src/reader/annotations/SelectionToolbar.tsx` finds the new branch
  - `! grep -rn '"multi-block"' src/` exits 0 (all three unions retired in one commit)
  - `! grep -rq "Select within a single block" src/` exits 0 (retired copy fully gone)
  - HighlightOverlay.tsx `captureCurrentSelection` diff contains no change to the rangesOverlap call
    (D19-07 caller-discipline only)
  - SelectionToolbar buttons labels `Highlight` and `Highlight + note` byte-stable (D19-09)
  </acceptance_criteria>
  <done>The reason → copy chain is consistent end-to-end: capture.ts → HighlightOverlay →
  SelectionToolbar; one new string; one retired string; toolbar behavior for spans unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Unit matrix flips + the moved e2e success cell</name>
  <files>tests/unit/annotations/capture-offset-mapping.test.ts, tests/e2e/annotations/capture-rejects.spec.ts, tests/e2e/annotations/span-capture.spec.ts</files>
  <read_first>
  - tests/unit/annotations/capture-offset-mapping.test.ts (FULL — conventions: parseArticle helper,
    baseArticle, makeParagraphBlock, selectFirstTextNode, beforeEach reset; the D5-06 case L217-245)
  - tests/e2e/annotations/capture-rejects.spec.ts (FULL — test 1's page-walk + two-block Range L46-113
    is the setup that MOVES; test 3 measurement-body + overlap tests STAY)
  - tests/e2e/annotations/_fixtures.ts (harness exports: FIXTURES, wipeDatabase, openArticle,
    selectRangeInBlock, findFirstBlockWithText, countHighlightsInDexie, assertHighlightMark)
  - 19-PATTERNS.md §tests assignments (header discipline, mandatory assertions)
  </read_first>
  <action>
  1. UNIT FLIP (sanctioned churn #2): the D5-06 multi-block rejection case in
     capture-offset-mapping.test.ts becomes a span-SUCCESS cell asserting the exact composed global
     offsets (compute expected via the blockText join rule: first block's full norm length + one
     BLOCK_SEPARATOR + intra offsets). Carry a D19 citation comment on the changed assertion.
  2. NEW unit cells in the same file: (a) backwards Range (setStart on the later block, setEnd on the
     earlier — same composed selector as forwards); (b) both endpoints carrying
     `data-block-grapheme-start` slice attributes (paginated-within-page composition); (c) figure
     caption endpoint with non-empty alt asserting the Pitfall 1-corrected global offset (and the
     empty-alt variant landing at caption-local 0); (d) unsupported endpoint → reason
     `boundary-ineligible` with NO position on the refusal; (e) footnote-body-style endpoint (element
     outside any data-block-index) → `ineligible`; (f) defensive `empty-span` via a collapsed composed
     range if constructible, else a direct-type cell documented as defensive-only.
  3. E2E MOVE (sanctioned churn #1): create tests/e2e/annotations/span-capture.spec.ts; MOVE
     capture-rejects.spec.ts test 1's page-walk two-block selection here as a SUCCESS cell — select
     across two paragraphs in scrolling mode on `essay-long-form`, assert the toolbar shows the two
     buttons (not a hint), click Highlight, `countHighlightsInDexie === 1`, and
     `mark.highlight[data-highlight-id]` is visible in BOTH blocks. Add a paginated-mode cell: span two
     blocks WITHIN one mounted page (same composition via slice attributes), same assertions.
     capture-rejects.spec.ts keeps ONLY the overlap + measurement-body tests (test 3 cross-page stays
     byte-stable — ANNO-13 is Future). Copy the header discipline lines (no test.skip/test.fixme;
     3-engine expectation) from capture-rejects.spec.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/capture-offset-mapping.test.ts && npx playwright test tests/e2e/annotations/capture-rejects.spec.ts tests/e2e/annotations/span-capture.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run tests/unit/annotations/capture-offset-mapping.test.ts` exits 0 with the flipped
    span-success case and cells (a)-(f) present
  - `npx playwright test tests/e2e/annotations/span-capture.spec.ts` exits 0 on
    chromium/firefox/webkit (both scrolling and paginated-within-page cells)
  - capture-rejects.spec.ts no longer contains the moved two-block selection body; its
    measurement-body test still asserts refusal (grep the refusal expectation on the cross-page test)
  - Changed assertions carry a D19 citation comment (grep `D19` in both touched spec/test files)
  - `! grep -rn "test.skip\|test.fixme" tests/e2e/annotations/span-capture.spec.ts` exits 0
  </acceptance_criteria>
  <done>The D5-06 retirement is honestly reflected in tests: refusal surfaces shrank to overlap +
  measurement-body + ineligible; span success is proven in real browsers in both modes with one-record
  semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM Selection → stored record | Reader-driven selection offsets cross into persisted data (existing boundary, unchanged shape) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | CaptureResult reason literals | mitigate | Closed-set union literals (not reader input); new reasons are compile-time-checked strings |
| T-19-02 | Tampering | Stored highlight positions | accept | Existing Zod safeParse on every Dexie read + D5-02 tri-state re-anchoring (never silent re-attach); no new fields |
| T-19-03 | Tampering | DOM clobbering via mark ids | accept (this plan) | id discipline unchanged here; Plan 03 lands the first-slice-only fix (T-19-04 there) |
| T-19-SC | Tampering | Package installs | accept | Zero installs this phase (19-RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Unit: `npx vitest run tests/unit/annotations/capture-offset-mapping.test.ts` — flipped + new cells green
- E2e: `npx playwright test tests/e2e/annotations/capture-rejects.spec.ts tests/e2e/annotations/span-capture.spec.ts` — 3 engines
- Grep gates: retired reason literal and retired copy return zero matches in src/ (Task 2 automated)
- Diff audit: zero changes under src/content/schema.ts, src/persistence/, src/content/normalizeText.ts
</verification>

<success_criteria>
- A two-paragraph selection creates exactly ONE highlight visible on both blocks in both reading modes (ANNO-08)
- Unsupported-boundary selections refuse whole with the new calm hint (ANNO-12 / D19-05/D19-06)
- Figure-caption endpoints store offsets that survive reload re-anchoring (Pitfall 1 fixed)
- Global overlap policy rejects overlapping spans with the existing hint (D19-07)
- Zero schema/Dexie changes; zero new packages; honest churn confined to the three sanctioned surfaces
</success_criteria>

## Artifacts this phase produces

*This plan:*
- `CaptureResult` reason literals: `boundary-ineligible`, `empty-span` (new); `multi-block` (removed)
- `captureSelection` span-composition behavior + the per-endpoint resolution helper (capture.ts, planner-named)
- Figure `captionLocalStart` alignment math inside capture.ts (not exported)
- `CreateFromSelectionResult.reason` / `ToolbarCaptureResult` updated unions (HighlightOverlay.tsx)
- New reader-facing string: `This selection includes content that can't be highlighted.` (SelectionToolbar.tsx)
- New spec file: `tests/e2e/annotations/span-capture.spec.ts`

*Later plans in this phase:* `firstFragmentExcerpt` (19-02), `HighlightSlice.isFirst` +
`sliceCodeForHighlights` + `nested-list-paths` fixture (19-03), per-page first-occurrence id pass
(19-04), `eligibility-matrix.spec.ts` (19-05).

<output>
Create `.planning/phases/19-cross-block-highlights/19-01-SUMMARY.md` when done
</output>
