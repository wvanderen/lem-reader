---
phase: 19-cross-block-highlights
plan: 05
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - tests/e2e/annotations/eligibility-matrix.spec.ts
  - tests/e2e/annotations/span-capture.spec.ts
  - tests/e2e/annotations/survive-relayout.spec.ts
  - tests/e2e/annotations/persist-reload.spec.ts
  - tests/e2e/annotations/delete-confirm.spec.ts
  - tests/e2e/annotations/note-create-edit.spec.ts
  - tests/e2e/portability/round-trip.spec.ts
  - tests/unit/annotations/resolve-quote-selector.test.ts
autonomous: true
requirements: [ANNO-08, ANNO-10, ANNO-11, ANNO-12]
must_haves:
  truths:
    - "The eligibility matrix is TESTED in real browsers: every readable kind as span endpoint, crossings, interior gaps, and refusals behave per D19-01..08 (ANNO-12)"
    - "Spans stay attached through typography changes, mode switches, reload, export/import round-trip, and review-to-reader navigation landing at span start (ANNO-10)"
    - "Deleting a span from review removes every fragment atomically — no leftover marks in either mode (ANNO-11)"
    - "Whole-article spans round-trip within the same revision and degrade honestly (orphan) after a mid-span edit — no cap, no silent re-attach (D19-04)"
    - "The honest full-suite gate is green: npm run test exit 0 across chromium/firefox/webkit with zero skips"
  artifacts:
    - path: tests/e2e/annotations/eligibility-matrix.spec.ts
      provides: "The kind × crossing × gap-placement matrix + refusal cells (3 engines)"
    - path: tests/unit/annotations/resolve-quote-selector.test.ts
      provides: "Long-span / whole-article re-anchor drift cells"
  key_links:
    - from: tests/e2e/annotations/eligibility-matrix.spec.ts
      to: tests/e2e/annotations/_fixtures.ts
      via: consumes the harness (selectRangeInBlock, findFirstBlockWithText, turnToPage, assertHighlightMark, wipeDatabase) — imported, never forked
      pattern: "from \"./_fixtures\""
    - from: tests/e2e/annotations/survive-relayout.spec.ts
      to: src/routes/ArticleView.tsx
      via: typography/mode-change triggers re-derive marks from the global range (ANNO-10 proof)
      pattern: "data-theme|readingMode"
  prohibitions:
    - statement: "No test.skip / test.fixme anywhere in the phase's specs — a red suite must stay red"
      status: resolved
      verification: "! grep -rn 'test.skip\\|test.fixme' tests/e2e/annotations/ exits 0"
    - statement: "Strengthen-only outside the three sanctioned churn surfaces already landed in 19-01 (19-RESEARCH Pitfall 7)"
      status: resolved
      verification: "git diff vs phase start shows spec edits are additive cells or D19-cited sanctioned flips only"
    - statement: "No engine subset runs — the matrix runs on chromium, firefox, AND webkit"
      status: resolved
      verification: "eligibility-matrix.spec.ts carries the 3-engine header discipline; playwright default projects run it unfiltered"
---

<objective>
Prove the phase in real browsers (ANNO-12's tested matrix + ANNO-10 durability + ANNO-11 atomicity)
and run the honest full-suite gate. This plan lands the `eligibility-matrix.spec.ts` corpus cells
(kind × crossing × gap placement + refusals per D19-01..08), extends the durability/atomicity specs
with span cells, adds the long-span re-anchor unit cells (D19-04 honesty), and closes the phase with
`npm run test` exit 0.

Purpose: the matrix IS the requirement — capture/render internals from Plans 01-04 are only
trustworthy if every kind, crossing, gap, and refusal is proven in real browsers across engines.
Output: the matrix spec, durability/atomicity span cells, re-anchor drift cells, and the green gate.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@.planning/phases/19-cross-block-highlights/19-RESEARCH.md
@.planning/phases/19-cross-block-highlights/19-PATTERNS.md
@.planning/phases/19-cross-block-highlights/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: eligibility-matrix.spec.ts — the tested matrix (ANNO-12)</name>
  <files>tests/e2e/annotations/eligibility-matrix.spec.ts</files>
  <read_first>
  - tests/e2e/annotations/_fixtures.ts (FULL — FIXTURES, wipeDatabase, openArticle,
    selectRangeInBlock, findFirstBlockWithText, assertHighlightMark, countHighlightsInDexie,
    turnToPage/totalPages; the image-stub route + beforeEach discipline)
  - tests/e2e/annotations/capture-rejects.spec.ts (header discipline lines L14-15 + the refusal
    assertions that must stay green)
  - src/fixtures/index.ts (the 7-fixture corpus incl. nested-list-paths from Plan 03)
  - 19-RESEARCH.md §Validation Architecture Wave 0 gaps (the prescribed cell shape); §Pitfall 6
    (footnote bodies ineligible); 19-CONTEXT.md D19-01..08
  </read_first>
  <action>
  Create `tests/e2e/annotations/eligibility-matrix.spec.ts` importing the `_fixtures.ts` harness
  (never forked), with the 3-engine + no-skip header discipline. Representative cells (not the full
  cartesian product — planner-scoped per 19-RESEARCH; every KIND, every gap class, every refusal
  class appears at least once):
  - ENDPOINT KINDS (scrolling mode, one cell each): paragraph (essay-long-form), heading,
    quotation-child (use the blockquote-bearing fixture — verify which via a quick corpus grep at
    implementation time), list-item (list-reference), nested-list-item (nested-list-paths),
    caption (figure-heavy — the non-empty-alt figure, proving the Pitfall 1 pair end-to-end), code
    (technical-post), footnote-reference marker (footnote-academic). Each: select from inside the
    kind into the adjacent paragraph → one record → marks visible in BOTH blocks.
  - CROSSINGS: cross-two (paragraph → next paragraph); cross-many (paragraph … list … paragraph —
    every intermediate block marked); item → sibling item; item → following paragraph;
    quotation-child → paragraph; caption → code where adjacent, else caption → paragraph.
  - INTERIOR GAPS (D19-02): paragraph → paragraph across a textless figure (marks on both sides,
    zero marks inside the figure element); paragraph → paragraph across an unsupported block
    (unsupported-case fixture); paragraph → paragraph across a code block interior (code IS marked —
    distinct from textless gaps; assert the code mark).
  - REFUSALS (D19-05/D19-06): endpoint INSIDE an unsupported block → whole selection refused, hint
    text exactly the new boundary-ineligible string, NO record created; endpoint inside a footnote
    BODY → refused with the existing ineligible hint (Pitfall 6); overlap refusal on a span
    overlapping an existing highlight (D19-07 — existing string). Cross-page/measurement-body
    refusal already covered green in capture-rejects.spec.ts — do not duplicate.
  - D19-14 assertion: a highlight starting at a list item's first character — the first marked
    character is the item's first text character (locator textContent comparison).
  - Backwards drag cell: compose the same span by selecting end→start (selection.setBaseAndExtent
    or reversed range) — same one record, same marks.
  - D19-11 cell: the review row for a multi-block span shows NO block-count badge (absence-of-
    element assertion) and the first-fragment excerpt with the single ellipsis character.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/eligibility-matrix.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - The spec covers all 8 endpoint kinds, all 3 interior-gap classes (textless figure, unsupported,
    marked code), all 3 refusal classes, the D19-14 marker assertion, backwards drag, and the
    D19-11 no-badge cell (cell inventory greppable by test titles)
  - `npx playwright test tests/e2e/annotations/eligibility-matrix.spec.ts` exits 0 on
    chromium/firefox/webkit
  - `! grep -rn "test.skip\|test.fixme" tests/e2e/annotations/eligibility-matrix.spec.ts` exits 0
  - The refusal cell asserts the exact new hint string (grep the string in the spec)
  - The spec imports from "./_fixtures" (harness reuse, no forked helpers)
  </acceptance_criteria>
  <done>The eligibility matrix is proven in real browsers: spans work across every readable kind,
  gaps cross calmly, ineligible boundaries refuse whole with explicit calm copy.</done>
</task>

<task type="auto">
  <name>Task 2: Durability + atomicity span cells (ANNO-10/11)</name>
  <files>tests/e2e/annotations/survive-relayout.spec.ts, tests/e2e/annotations/persist-reload.spec.ts, tests/e2e/annotations/delete-confirm.spec.ts, tests/e2e/annotations/span-capture.spec.ts, tests/unit/annotations/resolve-quote-selector.test.ts</files>
  <read_first>
  - tests/e2e/annotations/survive-relayout.spec.ts (FULL — the typography/mode-change triggers to
    extend with a span variant)
  - tests/e2e/annotations/persist-reload.spec.ts (FULL — reopen/restore cells to extend)
  - tests/e2e/annotations/delete-confirm.spec.ts (FULL — the atomic delete cells; the
    single-deleteHighlight-call-site discipline)
  - tests/e2e/annotations/span-capture.spec.ts (Plan 01's file — add the review-jump cell here)
  - tests/e2e/annotations/note-create-edit.spec.ts (FULL — the note edit flow the span cell (item
    6) extends; NotePopover open-from-slice conventions)
  - tests/e2e/portability/round-trip.spec.ts (FULL — export→import cycle harness the multi-line
    span cell (item 7) extends; clean-profile import conventions)
  - tests/unit/annotations/resolve-quote-selector.test.ts (FULL — conventions for the long-span
    drift cells)
  - src/annotations/resolution.ts (tri-state algorithm — consumed unchanged; first-cluster guard,
    slack window L135 for context)
  - 19-RESEARCH.md §Pitfall 8 (whole-article spans + wildcard context honesty)
  </read_first>
  <action>
  Strengthen-only extensions (add cells; change no existing assertion):
  1. survive-relayout.spec.ts: a multi-block span cell — capture, change typography (the spec's
     existing trigger), switch mode M, assert marks re-derive with the same extents in both modes
     (shared data-highlight-id; one id per document/mounted page).
  2. persist-reload.spec.ts: a span cell — capture, reload, assert marks restored at the same text
     and the saved location behavior is unchanged for span-containing articles.
  3. delete-confirm.spec.ts: a span cell — capture a multi-block span (with a note), delete from the
     review confirm, assert EVERY mark is gone in both modes (zero data-highlight-id elements for
     the id) and the note row is gone (atomic cascade — ANNO-11).
  4. span-capture.spec.ts: the review-jump cell — from the Highlights review, click Go-to-highlight
     on a span row; assert focus lands on the span's FIRST slice (the hl- id element = span start,
     D10-03 machinery at position.start) and the reader is turned/scrolled to it.
  5. resolve-quote-selector.test.ts unit cells (D19-04 honesty, no cap): a whole-article-length
     exact round-trips confident within the same revision; after a mid-span edit (mutated article
     text) the long span resolves orphan (or ambiguous) — never silent re-attach; a multi-line exact
     (block separators inside) resolves confident on the unchanged article.
  6. note-create-edit.spec.ts (strengthen-only span cell, ANNO-11): capture a multi-block span,
     open the note popover on its first slice, edit the note, assert the edited note reaches the
     ONE record (D5-10 1:1 substrate — note count stays 1) and the mark extents are unchanged in
     both modes after the edit.
  7. tests/e2e/portability/round-trip.spec.ts (strengthen-only span cell, ANNO-10 import leg):
     export a library containing a cross-block span (multi-line quote.exact with block
     separators), import into a clean profile, assert the highlight restores as ONE record whose
     quote.exact preserves the embedded separators and re-anchors confident (marks at the same
     text in both modes).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/resolve-quote-selector.test.ts && npx playwright test tests/e2e/annotations/survive-relayout.spec.ts tests/e2e/annotations/persist-reload.spec.ts tests/e2e/annotations/delete-confirm.spec.ts tests/e2e/annotations/span-capture.spec.ts tests/e2e/annotations/note-create-edit.spec.ts tests/e2e/portability/round-trip.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - Each of the spec files gains at least one span cell; `git diff` on them shows additive
    describe/test blocks only (strengthen-only)
  - The note-edit span cell (item 6) and the multi-line export→import round-trip cell (item 7)
    are present and green — ANNO-11 note-edit-on-span and the ANNO-10 import leg have explicit
    coverage, not just structural inference
  - The delete cell asserts zero remaining marks + note removal in BOTH modes
  - The jump cell asserts the focused element is the hl- id carrier (span start)
  - The re-anchor unit cells cover same-revision confident, mid-span-edit orphan/ambiguous, and
    multi-line exact confident
  - All touched specs green on 3 engines; unit file exits 0
  </acceptance_criteria>
  <done>Spans are proven durable across relayout/mode/reload/jump and atomically manageable; long
  spans degrade honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Honest full-suite phase gate</name>
  <files>tests/e2e/annotations/eligibility-matrix.spec.ts</files>
  <read_first>
  - package.json (the test / test:unit / test:e2e script shapes)
  - 19-RESEARCH.md §Validation Architecture §Sampling Rate (phase-gate definition; webkit-starvation
    lesson — check the dev server's freshness before diagnosing engine failures)
  - .planning/phases/19-cross-block-highlights/19-UI-SPEC.md §Regression Targets (byte-stable
    surfaces audit list)
  </read_first>
  <action>
  Run the honest full-suite gate: `npm run test` in ONE invocation — unit + e2e across
  chromium/firefox/webkit (+ throttled where configured). Record the pass/fail counts verbatim in
  the SUMMARY (the 04-11/05-05 honest-gate precedent: the executor runs the suite itself and records
  fail=0; no subset/grep/engine-skip). If failures appear: (a) failures inside phase-19 surfaces →
  fix in this plan with atomic commits; (b) pre-existing failures in unrelated specs → log to
  .planning/phases/19-cross-block-highlights/deferred-items.md per the 08-05 scope-boundary rule and
  surface in the SUMMARY (do not silently absorb). Then audit the regression-target list: toolbar
  anatomy + carried hint strings byte-stable; mark discipline (tabIndex/aria/tri-state) intact; D5-08
  measurement-body refusal green; review row anatomy (no badge); delete-confirm single call site;
  NotePopover modal discipline; hash grammar unchanged. Fix any drift found (strengthen-only
  outside the sanctioned 19-01 churn).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
  - `npm run test` exits 0 with fail counts recorded verbatim in 19-05-SUMMARY.md
  - `! grep -rn "test.skip\|test.fixme" tests/e2e/annotations/` exits 0
  - Regression-target audit recorded in the SUMMARY (each UI-SPEC §Regression Targets row checked)
  - Any pre-existing unrelated failures are logged to deferred-items.md, not absorbed
  </acceptance_criteria>
  <done>The phase closes green: full suite exit 0, all 3 engines, honest counts, regression targets
  audited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only plan | No production input surface changes; specs assert existing mitigations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Repudiation | Suite honesty | mitigate | One-invocation full gate, verbatim counts in SUMMARY, no engine skips (the 04-11 anti-pattern guard) |
| T-19-14 | Tampering | Long-span re-anchoring | accept | Tri-state contract asserted for degradation (orphan/ambiguous) — never silent re-attach (D5-02/ANNO-07) |
| T-19-15 | DoS | Matrix runtime growth | accept | Representative-cell scoping (not full cartesian); 3-engine discipline preserved within existing worker budget |
</threat_model>

<verification>
- `npx playwright test tests/e2e/annotations/eligibility-matrix.spec.ts` — the matrix, 3 engines
- `npx vitest run tests/unit/annotations/resolve-quote-selector.test.ts` — drift cells
- `npm run test` — the honest phase gate, exit 0
</verification>

<success_criteria>
- The eligibility matrix is tested per D19-01..08: all readable kinds spannable, gaps crossed calmly, ineligible boundaries rejected whole with explicit calm explanation (ANNO-12)
- Spans survive repagination, mode/typography changes, reload, and review navigation to span start (ANNO-10)
- Review/edit/export/delete operate atomically on spans with no leftover fragments (ANNO-11)
- Whole-article spans round-trip same-revision and degrade honestly cross-revision (D19-04)
- Full suite exit 0 — the honest gate, 3 engines, zero skips (phase close)
</success_criteria>

## Artifacts this phase produces

*This plan:*
- NEW spec file: `tests/e2e/annotations/eligibility-matrix.spec.ts` (the tested matrix)
- Span durability/atomicity/jump cells added to survive-relayout / persist-reload / delete-confirm /
  span-capture specs; long-span drift cells in resolve-quote-selector.test.ts
- `deferred-items.md` entries for any pre-existing unrelated failures (honest boundary record)

*From earlier plans:* capture reasons + span-capture.spec (19-01); `firstFragmentExcerpt` +
multi-line export (19-02); `HighlightSlice.isFirst` + `sliceCodeForHighlights` +
`nested-list-paths` + list threading (19-03); per-page first-occurrence id pass + entry-local
threading (19-04).

<output>
Create `.planning/phases/19-cross-block-highlights/19-05-SUMMARY.md` when done
</output>
