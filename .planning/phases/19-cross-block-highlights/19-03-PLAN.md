---
phase: 19-cross-block-highlights
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/annotations/highlightRanges.ts
  - src/content/render/InlineRenderer.tsx
  - src/content/render/BlockRenderer.tsx
  - src/fixtures/articles/nested-list-paths.canonical.json
  - src/fixtures/index.ts
  - tests/e2e/pagination/fixtures-matrix.ts
  - tests/unit/annotations/list-highlight-render.test.tsx
autonomous: true
requirements: [ANNO-09, ANNO-12]
must_haves:
  truths:
    - "A span renders as block-local marks on EVERY readable kind it crosses in scrolling mode — list items (incl. nested), figure captions, code interiors — as ONE identity (ANNO-09)"
    - "Exactly ONE DOM id=\"hl-<highlightId>\" exists per highlight per mounted document, on the first slice in document order; every slice keeps data-highlight-id (Pitfall 2)"
    - "Interior non-text gaps (textless figure, unsupported) render unmarked by construction — the fill continues on both sides (D19-02)"
    - "A highlight starting at a list item's first character marks TEXT only — the first marked character is the item's first text character; markers are never inside a mark (D19-14)"
    - "Nested list items are individually highlightable/spannable children with the same mark anatomy (D19-13/D19-15)"
  artifacts:
    - path: src/annotations/highlightRanges.ts
      provides: "HighlightSlice.isFirst flag + sliceCodeForHighlights (code-source segmentation)"
    - path: src/content/render/BlockRenderer.tsx
      provides: "List per-item threading, caption mark path, code mark path (scrolling)"
    - path: src/fixtures/articles/nested-list-paths.canonical.json
      provides: "D19-15 recursion fixture (schema-valid at load)"
    - path: tests/unit/annotations/list-highlight-render.test.tsx
      provides: "List/caption/code render unit cells incl. multi-run alignment"
  key_links:
    - from: src/annotations/highlightRanges.ts
      to: src/content/render/InlineRenderer.tsx
      via: HighlightSlice.isFirst drives id stamping — id only when slice.isFirst, data-highlight-id always
      pattern: "isFirst"
    - from: src/content/render/BlockRenderer.tsx
      to: src/annotations/highlightRanges.ts
      via: list walk consumes highlightsForBlock + sliceRunsForHighlights UNCHANGED (no slicer fork) over the D-05 item join rule
      pattern: "sliceRunsForHighlights"
    - from: src/fixtures/index.ts
      to: tests/e2e/pagination/fixtures-matrix.ts
      via: new fixture id registered in BOTH the loader array and the e2e FIXTURES array (PERF_FIXTURES untouched)
      pattern: "nested-list-paths"
  prohibitions:
    - statement: "List markers are never rendered as DOM text and never inside a mark (D19-14)"
      status: resolved
      verification: "BlockRenderer list cases keep ::marker/start-attribute chrome; unit cell asserts the first marked char is the item's first text char; diff adds no marker-rendering element"
    - statement: "No per-block fragments or block counts stored on the record (ANNO-09 — fragments are always derived)"
      status: resolved
      verification: "git diff touches nothing under src/content/schema.ts or src/persistence/"
    - statement: "The run slicer is consumed, never reimplemented (REUSE-DO-NOT-FORK)"
      status: resolved
      verification: "sliceRunsForHighlights + splitParagraphRuns signatures unchanged; new call sites only"
    - statement: "Marks alter no geometry — no line-height/margin/width change; pre overflow-x scroll box never widens (UI-SPEC Interaction 7)"
      status: resolved
      verification: "zero new CSS selectors except (at most) one additive code-mark guard under existing tokens; visual cells assert pre geometry unchanged"
---

<objective>
Ship the SCROLLING render coverage (ANNO-09's block-local fragments; the ANNO-12 per-kind render
matrix): list items become highlightable spannable children via the 05-07 blockquote precedent
(D19-13/D19-15 — the items-shape debt paid), figure captions and code interiors gain mark paths
(D19-01 + the Pitfall 1 render-side symmetric offset), and the first-slice-only DOM id discipline
lands (Pitfall 2 — duplicate ids would break the review-jump focus target). `sliceRunsForHighlights`
math is consumed UNCHANGED; a span crossing any mix of covered kinds "just works" through per-block
intersection.

Purpose: rendering is the second work surface — a stored span must be VISIBLE on every readable kind
it crosses, with gaps calm and identity singular.
Output: extended BlockRenderer (list/caption/code paths), isFirst-flagged slices + code segmentation
in highlightRanges.ts, first-slice id in InlineRenderer, the nested-list fixture, and render unit cells.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@.planning/phases/19-cross-block-highlights/19-RESEARCH.md
@.planning/phases/19-cross-block-highlights/19-PATTERNS.md
@.planning/phases/19-cross-block-highlights/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HighlightSlice.isFirst + first-slice-only DOM id</name>
  <files>src/annotations/highlightRanges.ts, src/content/render/InlineRenderer.tsx, tests/unit/annotations/list-highlight-render.test.tsx</files>
  <read_first>
  - src/annotations/highlightRanges.ts (FULL — HighlightSliceEntry/HighlightSlice interfaces,
    sliceRunsForHighlights intersection math L84-109)
  - src/content/render/InlineRenderer.tsx (FULL — the mark discipline L116-132; id at L121)
  - src/routes/ArticleView.tsx L1585-1595 + L1930-1940 (the ONLY hl- consumers: two
    getElementById focus sites — verify they stay working with first-slice-only id)
  - 19-RESEARCH.md §Pitfall 2 + §Open Questions 3; 19-UI-SPEC §Component Inventory InlineRenderer contract
  </read_first>
  <action>
  1. highlightRanges.ts: add optional `isFirst?: boolean` to `HighlightSlice` (documented: true only
     on the highlight's first slice in document order — the DOM id carrier). In
     `sliceRunsForHighlights`, set `isFirst: h.position.start >= blockGlobalStart` on the (single,
     contiguous) intersection slice for each highlight — a highlight's global start lies within this
     block exactly when this block is the first it touches (blocks are contiguous under the D-05
     prefix-sum join, so a start before this block implies an earlier block also intersects). Note in
     the doc comment: PAGINATED entry-local callers must OVERRIDE this flag via a per-page
     first-occurrence pass (Plan 19-04) because entry-local coordinates would otherwise mark every
     fully-covered entry.
  2. InlineRenderer.tsx: stamp `id={`hl-${slice.highlightId}`}` ONLY when `slice.isFirst === true`;
     `data-highlight-id`, `tabIndex={0}`, `aria-haspopup="dialog"`, per-slice aria-label, and the
     tri-state className discipline stay on EVERY slice byte-unchanged. When `isFirst` is absent
     (legacy callers), do not stamp the id — the paginated path always supplies the flag after 19-04;
     for THIS plan's paginated interim, verify cross-fragment-render.spec.ts still passes (its
     single-block-per-page spans get isFirst from the entry pass or temporarily from the slicer —
     if the interim breaks duplicate-id safety in paginated split-block cases, gate 19-04's pass in
     the same commit as this change instead; choose the sequencing that keeps the suite green and
     record it in the SUMMARY).
  3. Create tests/unit/annotations/list-highlight-render.test.tsx with slicer-level cells: a highlight
     spanning two blocks yields isFirst=true on the first block's slice and undefined/false on the
     second; a highlight starting mid-block marks only that block's slice; rendering N slices for one
     highlight produces exactly one element carrying the hl- id (render via the existing
     InlineRenderer test harness pattern — see blockquote-highlight-render.test.tsx for the mount
     conventions).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/list-highlight-render.test.tsx && npx playwright test tests/e2e/annotations/cross-fragment-render.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "isFirst" src/annotations/highlightRanges.ts` finds the field + the slicer assignment
  - InlineRenderer's mark element conditionally stamps the id on `slice.isFirst` (grep the
    conditional; data-highlight-id remains unconditional)
  - Unit cells assert: two-block span → exactly one slice with isFirst truthy; rendered output has
    exactly one `id="hl-…"` element for a multi-slice highlight
  - ArticleView.tsx is untouched (`git diff --name-only` excludes it) — both getElementById consumers
    keep working (the id now lives on the span-start slice, which IS the jump target)
  - cross-fragment-render.spec.ts stays green (3 engines)
  </acceptance_criteria>
  <done>One id per highlight per document, on the document-order first slice; all slices remain
  keyboard-focusable activation targets; the uniqueness contract is spec-assertable.</done>
</task>

<task type="auto">
  <name>Task 2: List per-item threading + nested-list fixture (D19-13/14/15)</name>
  <files> src/content/render/BlockRenderer.tsx, src/fixtures/articles/nested-list-paths.canonical.json, src/fixtures/index.ts, tests/e2e/pagination/fixtures-matrix.ts, tests/unit/annotations/list-highlight-render.test.tsx</files>
  <read_first>
  - src/content/render/BlockRenderer.tsx (FULL — the blockquote childHighlightSlices walk L392-438 to
    MIRROR; list render cases L131-154; buildBlockHighlightIndex L224-237; highlightsForBlock
    L248-267; blockGraphemeLen L274)
  - src/content/normalizeText.ts L48-52 (the load-bearing join rule: items joined by
    BLOCK_SEPARATOR; each item's content blocks joined by BLOCK_SEPARATOR)
  - src/fixtures/index.ts + one existing canonical.json (e.g. list-reference) for the fixture shape
  - tests/e2e/pagination/fixtures-matrix.ts L37-44 (FIXTURES array registration)
  - tests/unit/annotations/blockquote-highlight-render.test.tsx (mount conventions for render cells)
  - 19-RESEARCH.md §Pattern 2 + §Code Examples "List per-item threading"; §Pitfall 4 (coordinate
    systems) and §Pitfall 5 (markers are chrome)
  </read_first>
  <action>
  1. BlockRenderer.tsx: add a recursive per-item slice computation for `bulleted-list` /
     `numbered-list`, mirroring the blockquote walk's structure over the items-shape. A module-level
     helper (planner-named, e.g. `computeListItemSlices(block, blockGlobalStart, effectiveHighlights,
     article)`) walks: list-local accumulator starts at 0; for each item, an item-local accumulator
     walks `item.content` — for each child compute `childLen = blockGraphemeLen(child, article.lang)`
     and `childGlobalStart = blockGlobalStart + itemLocalOffset`; paragraph/heading children get
     `highlightsForBlock` + `sliceRunsForHighlights(child.content, childGlobalStart, entries,
     article.lang)` exactly as the blockquote path does; nested `bulleted-list`/`numbered-list`
     children RECURSE (D19-15) producing the same nested shape; advance BOTH accumulators by
     `childLen + BLOCK_SEPARATOR.length` per child and per item per the join rule. Thread the result
     into the list render cases: each item's content children render via BlockView with
     `highlightSlices={perItem[i]?.[j]}` for leaf children and a new optional
     `itemHighlightSlices`-shaped prop for nested-list children carrying their precomputed nested
     structure (self-contained recursion via props — no global-start plumbing through BlockView).
     Interior gaps need NO code: kinds without slices simply render none (D19-02 by construction).
     Markers stay CSS chrome — add NO marker text element (D19-14).
  2. Fixture: create `src/fixtures/articles/nested-list-paths.canonical.json` — a schema-valid
     article (real-looking prose provenance, title/lang; ~5 blocks) containing: an intro paragraph,
     a bulleted list whose 2nd item contains a nested bulleted list whose item contains a nested
     numbered list (3 levels total, D19-15), one item's content includes a link run mid-item (Pitfall
     4 multi-run alignment), a closing paragraph. Register it: static import + array entry in
     `src/fixtures/index.ts`; append `"nested-list-paths"` to FIXTURES in
     tests/e2e/pagination/fixtures-matrix.ts. Do NOT touch PERF_FIXTURES (perf.harness.spec.ts pins
     its own 3-fixture set — budget unchanged). The pagination corpus + open-every-fixture +
     core-reading-flow specs will iterate the 7th fixture — additive strengthen-only, sanctioned by
     UI-SPEC §Regression Targets.
  3. Unit cells in list-highlight-render.test.tsx: (a) highlight entirely inside one list item marks
     that item's text (and NOT sibling items); (b) span from item 1 into item 3 marks both items,
     ONE id in the rendered output, item 2 unmarked-between? — NO: item 2 IS crossed (contiguous
     global range) so it IS marked; assert item 2's text is marked too (interior blocks are interior
     to the range — D19-08 all-or-nothing); (c) D19-14 cell: a highlight starting at an item's first
     character produces a mark whose first text character is the item's first text character (marker
     absent from mark.textContent); (d) nested-list recursion cell: a span covering outer item text
     through nested sub-item text marks both levels; (e) multi-run item (link mid-item) marks split
     runs with the link preserved across the boundary (Pitfall 4 alignment lock).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/list-highlight-render.test.tsx tests/unit/annotations/blockquote-highlight-render.test.tsx && npx playwright test tests/e2e/pagination/coverage-invariant.spec.ts tests/e2e/acceptance/core-reading-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - BlockRenderer list cases forward per-item/per-child slices (grep `highlightSlices=` usage in the
    list cases; a recursive helper exists — no `splitParagraphRuns` reimplementation anywhere new)
  - `node -e "require('./src/fixtures/index.ts')"`-equivalent load validation: the app's fail-fast
    schema parse passes (the fixture boots — proven by the e2e specs below iterating it)
  - FIXTURES in tests/e2e/pagination/fixtures-matrix.ts contains `nested-list-paths` (7 entries);
    PERF_FIXTURES untouched
  - Unit cells (a)-(e) green; blockquote render tests still green (precedent unregressed)
  - coverage-invariant + core-reading-flow green on 3 engines (the 7th fixture iterates cleanly)
  - `git diff --name-only` excludes src/content/schema.ts, src/persistence/
  </acceptance_criteria>
  <done>List items (and nested items, and their multi-run content) render span marks with one
  identity; markers never highlight; the recursion fixture is a first-class corpus member.</done>
</task>

<task type="auto">
  <name>Task 3: Caption marks + code marks (D19-01 render coverage)</name>
  <files>src/content/render/BlockRenderer.tsx, src/annotations/highlightRanges.ts, tests/unit/annotations/list-highlight-render.test.tsx</files>
  <read_first>
  - src/content/render/BlockRenderer.tsx (figure case L155-172 — figcaption's InlineList; code case —
    pre/code verbatim source; the L357-363 alt-divergence comment to update)
  - src/content/normalizeText.ts (figure join rule L53-54; code-block verbatim rule L55-56)
  - src/annotations/highlightRanges.ts (post-Task-1 state — where sliceCodeForHighlights joins)
  - 19-RESEARCH.md §Pitfall 1 (render-side symmetric offset), §Pitfall 4, §Open Questions 1;
    19-UI-SPEC §Component Inventory (caption + code mark anatomy; geometry constraints; the
    one-additive-selector ceiling)
  </read_first>
  <action>
  1. CAPTION MARKS: in the figure case, compute `captionLocalStart = block.alt.length > 0 ?
     graphemeClusters(block.alt, article.lang).length + BLOCK_SEPARATOR.length : 0` (the SYMMETRIC
     offset to Plan 19-01's capture-side fix — same filter(Boolean) join rule);
     `captionGlobalStart = blockGlobalStart + captionLocalStart`; query
     `highlightsForBlock(effectiveHighlights, captionGlobalStart, captionRunLen)` where
     captionRunLen follows the shipped run-sum discipline (mirror how the blockquote walk feeds
     sliceRunsForHighlights); pass the resulting slices to the figcaption's InlineList via its
     highlightSlices prop. The `<img>` + alt render NO marks ever (alt is an attribute — the
     interior-gap visual is plain surface per D19-02/UI-SPEC). Update the L357-363 comment: the
     divergence is now paid down on both capture and render sides.
  2. CODE MARKS: add `sliceCodeForHighlights(source, blockGlobalStart, highlights, lang)` to
     highlightRanges.ts — a pure segmentation of the verbatim source into an ordered array of
     `{ text, entry: HighlightSliceEntry | null }` segments using grapheme cluster offsets over the
     source (code text is verbatim in the D-05 substrate, so raw == norm — no whitespace-collapse
     alignment needed; note this in the header comment). D-05/norm alignment trivial; intersect each
     highlight's global range with `[blockGlobalStart, blockGlobalStart + clusters(source).length)`.
     In BlockRenderer's code case, render inside `<code>`: plain segments as React text children,
     highlighted segments wrapped in `<mark class="highlight">` carrying the SAME discipline as
     InlineRenderer's marks — `data-highlight-id`, `tabIndex={0}`, `aria-haspopup="dialog"`,
     per-segment aria-label (slice-local text, 80-char cap), hasNote/unresolved modifiers, and
     `id="hl-…"` only on the segment that is the highlight's first (reuse the isFirst rule:
     `h.position.start >= blockGlobalStart`). Verbatim fidelity: segments concatenate to EXACTLY the
     source (whitespace/newlines preserved — assert in unit). Keep `pre` geometry byte-stable: marks
     are inline content; add at most ONE additive CSS guard under existing tokens IF measurement
     demands (document in-plan if added; zero is expected).
  3. Unit cells in list-highlight-render.test.tsx (or a sibling describe): (a) caption endpoint
     highlight renders a mark inside figcaption with correct extent (figure WITH non-empty alt — the
     offset is the Pitfall 1 symmetric pair); (b) a span from a paragraph through a figure's caption
     marks the paragraph AND the caption, not the img (gap by construction); (c) code segment
     round-trip: segments join to the exact source incl. embedded newlines; a mid-source highlight
     wraps only its extent; a whole-code-block highlight wraps everything; (d) one-id-per-highlight
     across a paragraph→caption→code span (exactly one id="hl-…" in the mounted output).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/list-highlight-render.test.tsx && npx vitest run tests/unit/annotations</automated>
  </verify>
  <acceptance_criteria>
  - BlockRenderer figure case computes captionLocalStart gated on non-empty alt and passes slices to
    the figcaption InlineList
  - `grep -n "sliceCodeForHighlights" src/annotations/highlightRanges.ts` finds the exported
    segmentation; BlockRenderer's code case renders mark-wrapped segments
  - Code marks carry data-highlight-id + tabIndex + aria-haspopup (grep in the code case JSX)
  - Unit cells (a)-(d) green; segments round-trip byte-exact
  - Zero or one additive CSS change under src/app.css, using existing custom properties only
    (`git diff src/app.css` reviewed)
  - `git diff --name-only` excludes src/content/schema.ts, src/persistence/
  </acceptance_criteria>
  <done>Every readable kind (paragraph, heading, quotation children, list items incl. nested,
  captions, code, footnote-reference runs) renders mark coverage in scrolling mode; gaps render
  unmarked by construction; one id per highlight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored records → rendered DOM | Persisted ranges drive mark rendering (existing boundary; no new input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | DOM clobbering via duplicate mark ids | mitigate | First-slice-only id (Pitfall 2); unit cell asserts exactly one id per highlight; ids derive from crypto.randomUUID highlight ids — no reader-controlled text |
| T-19-08 | Tampering | Code/caption rendering of persisted ranges | accept | React text children only; segments are string slices of the stored source; no dangerouslySetInnerHTML (react/no-danger active) |
| T-19-09 | DoS | Pathological highlight counts in per-item walks | accept | buildBlockHighlightIndex remains one linear pass gated on highlights-present (shipped perf guard, consumed unchanged) |
</threat_model>

<verification>
- Unit: `npx vitest run tests/unit/annotations` — all annotation render/slicer cells green
- E2e regression: `npx playwright test tests/e2e/annotations/ tests/e2e/pagination/coverage-invariant.spec.ts tests/e2e/acceptance/core-reading-flow.spec.ts` — 3 engines, 7-fixture corpus
- Grep gates: isFirst present; recursive list helper present; no splitParagraphRuns reimplementation; schema/persistence untouched
</verification>

<success_criteria>
- A paragraph → list (incl. nested) → figure-gap → caption → code span renders marks on every readable fragment with ONE id and shared data-highlight-id (ANNO-09 scrolling side)
- Markers never inside marks (D19-14); gaps unmarked (D19-02); nested lists recurse (D19-15)
- The 05-07 blockquote precedent is unregressed; the corpus grows strengthen-only
</success_criteria>

## Artifacts this phase produces

*This plan:*
- `HighlightSlice.isFirst?: boolean` field + slicer assignment (highlightRanges.ts)
- `sliceCodeForHighlights(source, blockGlobalStart, highlights, lang)` — exported from highlightRanges.ts
- `computeListItemSlices` (planner-named recursive list threading helper) + `itemHighlightSlices`-shaped BlockView prop (BlockRenderer.tsx)
- Caption `captionLocalStart` render-side offset (BlockRenderer.tsx, not exported)
- NEW fixture id `nested-list-paths` registered in src/fixtures/index.ts + tests/e2e/pagination/fixtures-matrix.ts FIXTURES
- NEW test file: `tests/unit/annotations/list-highlight-render.test.tsx`

*From earlier plans:* `boundary-ineligible`/`empty-span` reasons + `span-capture.spec.ts` (19-01);
`firstFragmentExcerpt` + multi-line export (19-02). *Later:* per-page first-occurrence id pass
(19-04), `eligibility-matrix.spec.ts` (19-05).

<output>
Create `.planning/phases/19-cross-block-highlights/19-03-SUMMARY.md` when done
</output>
