---
phase: 19-cross-block-highlights
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-03"]
files_modified:
  - src/pagination/fragmentRenderer.tsx
  - tests/unit/annotations/cross-fragment-slicing.test.ts
  - tests/e2e/annotations/cross-fragment-render.spec.ts
autonomous: true
requirements: [ANNO-09, ANNO-10]
must_haves:
  truths:
    - "A span crossing a page boundary renders marks on EACH mounted page sharing one data-highlight-id — no silent gaps at page turns (D5-16 extension)"
    - "Exactly one id=\"hl-<highlightId>\" exists per highlight on the mounted page (the per-page first-occurrence pass owns the flag in paginated mode)"
    - "List items inside page fragments render per-item marks in entry-local coordinates (the paginated twin of Plan 03)"
    - "Caption-endpoint spans render their figcaption mark on the mounted page — the paginated renderer never silently bypasses Plan 03's caption/code paths (no untested routing)"
    - "A native selection still cannot extend past the mounted page — the cross-page refusal spec stays green (ANNO-13 is Future; no new capture machinery)"
    - "Spans survive repagination: marks re-derive from the global range on every page turn (ANNO-10 repagination leg)"
  artifacts:
    - path: src/pagination/fragmentRenderer.tsx
      provides: "Entry-local list threading + per-page first-occurrence id pass"
    - path: tests/unit/annotations/cross-fragment-slicing.test.ts
      provides: "Entry-local span slicing cells incl. items-shape"
    - path: tests/e2e/annotations/cross-fragment-render.spec.ts
      provides: "Multi-page span mark cells (3 engines)"
  key_links:
    - from: src/pagination/fragmentRenderer.tsx
      to: src/annotations/highlightRanges.ts
      via: sliceRunsForHighlights consumed in entry-local coords; isFirst OVERRIDDEN by the per-page pass (slicer flags are not entry-aware)
      pattern: "sliceHighlightsForEntry|sliceRunsForHighlights"
    - from: src/pagination/fragmentRenderer.tsx
      to: src/pagination/splitBlock.ts
      via: sliceList/sliceChildBlocks output resolved.items[i].content[j] is the threading substrate; splittingBlockGraphemeLength for child lengths (Pitfall 4 coordinate discipline)
      pattern: "splittingBlockGraphemeLength"
  prohibitions:
    - statement: "No cross-page capture machinery — a native selection cannot span mounted pages (ANNO-13 is a Future Requirement)"
      status: resolved
      verification: "capture-rejects.spec.ts measurement-body/cross-page refusal test stays green byte-unchanged; no capture.ts change in this plan"
    - statement: "Entry-local coordinates never mix with D-05 global coordinates inside one call (Pitfall 4)"
      status: resolved
      verification: "The list threading mirrors the shipped blockquote entry path exactly — lengths from splittingBlockGraphemeLength, offsets from entry-local accumulators"
    - statement: "No geometry change — page marks inherit the shipped mark anatomy; no new layout selectors"
      status: resolved
      verification: "git diff shows no src/app.css change (or none beyond Plan 03's documented guard)"
---

<objective>
Ship the PAGINATED twin of Plan 03's render coverage (ANNO-09 paginated side + the ANNO-10
repagination leg): entry-local per-item list threading inside page fragments (consuming the shipped
`sliceList`/`sliceChildBlocks` output), and the per-page first-occurrence id pass that owns
`HighlightSlice.isFirst` in paginated mode. D5-16 already covers split-block marks across pages —
this plan extends the same rule over spans and the items-shape.

Purpose: both reading modes must render identical span coverage; the id discipline must hold per
mounted document (one page at a time).
Output: extended fragmentRenderer + entry-local unit cells + multi-page span e2e cells.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@.planning/phases/19-cross-block-highlights/19-RESEARCH.md
@.planning/phases/19-cross-block-highlights/19-PATTERNS.md
@.planning/phases/19-cross-block-highlights/19-UI-SPEC.md
@.planning/phases/19-cross-block-highlights/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry-local list threading in page fragments</name>
  <files>src/pagination/fragmentRenderer.tsx</files>
  <read_first>
  - src/pagination/fragmentRenderer.tsx (FULL — the blockquote entry-local threading L154-205 to
    MIRROR; sliceHighlightsForEntry L252-291; resolveBlockSlice/sliceList/sliceChildBlocks
    L314-496; splittingBlockGraphemeLength L514-547; entry attribute emission L220-221)
  - src/pagination/splitBlock.ts (sliceList/sliceChildBlocks output shapes — the items substrate)
  - src/content/render/BlockRenderer.tsx (Plan 03's landed computeListItemSlices — the scrolling
    twin whose SHAPE this mirrors in entry-local coordinates)
  - 19-PATTERNS.md §fragmentRenderer assignment; 19-RESEARCH.md §Pitfall 4 + §Pitfall 10
  </read_first>
  <action>
  Extend the entry rendering for `bulleted-list` / `numbered-list` entries by mirroring the shipped
  blockquote entry path over the items-shape: for each `resolved.items[i]`, walk `item.content`
  with an entry-local accumulator (child lengths from `splittingBlockGraphemeLength`, advancing by
  `childLen + BLOCK_SEPARATOR.length` per child and per item per the splitting-coordinate join
  rule); filter the entry's already-computed slices per child via the same
  `intersectStart/intersectEnd` clamp the blockquote path uses; leaf children (paragraph/heading)
  consume `sliceRunsForHighlights(child.content, childIntraStart, filtered, lang)` in ENTRY-LOCAL
  coordinates; nested list children recurse (D19-15) through the same shape. Thread results into the
  per-item child rendering exactly as the blockquote case forwards `childHighlightSlices`. Caption
  and code entries: DETERMINE the routing FIRST — grep the figure/code entry cases in
  fragmentRenderer for whether they render through the shared BlockView (in which case Plan 03's
  `captionLocalStart` / `sliceCodeForHighlights` paths already cover them) or render entry-local
  content directly; where the paginated renderer bypasses BlockView, add the SAME entry-local
  slice forwarding for caption text and code segments (the `intersectStart/intersectEnd` clamp +
  `splittingBlockGraphemeLength` lengths — the identical discipline). Either route is pinned by
  tests, not left implicit: Task 2's e2e cell (d) asserts a paginated caption-endpoint span renders
  its figcaption mark, and the chosen routing (delegation vs entry-local forwarding) is recorded in
  19-04-SUMMARY.md. Figure/unsupported entries render no marks by construction (D19-02). Keep the
  `data-block-index` / `data-block-grapheme-start` emission byte-identical (capture round-trips
  depend on it).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/cross-fragment-slicing.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - fragmentRenderer list entries thread per-item/per-child slices (grep the items walk adjacent to
    the blockquote entry path; recursion present for nested list children)
  - Child lengths come from splittingBlockGraphemeLength (Pitfall 4 — no D-05 run-text lengths in
    entry-local calls)
  - Entry attribute emission (data-block-index / data-block-grapheme-start) byte-unchanged
  - Caption/code entry routing is EXPLICIT: either the figure/code entry cases delegate to the
    shared BlockView (grep-cited in 19-04-SUMMARY.md) or entry-local slice forwarding exists in
    those entry cases (grep the forwarding) — no silent bypass is permitted
  - Existing cross-fragment-slicing.test.ts cells stay green (split-block precedent unregressed)
  - `git diff --name-only` excludes src/annotations/capture.ts (no cross-page capture machinery)
  </acceptance_criteria>
  <done>List spans inside page fragments render per-item marks in both modes with identical extent.</done>
</task>

<task type="auto">
  <name>Task 2: Per-page first-occurrence id pass + multi-page span cells</name>
  <files>src/pagination/fragmentRenderer.tsx, tests/unit/annotations/cross-fragment-slicing.test.ts, tests/e2e/annotations/cross-fragment-render.spec.ts</files>
  <read_first>
  - src/pagination/fragmentRenderer.tsx (the component that maps entries for the mounted page —
    where the pass lives; PaginatedSurface.tsx L3/L660 context: ONE PageFragmentView mounted at a
    time)
  - src/annotations/highlightRanges.ts (Plan 03's isFirst doc comment — paginated callers MUST
    override)
  - tests/unit/annotations/cross-fragment-slicing.test.ts (FULL — conventions to extend)
  - tests/e2e/annotations/cross-fragment-render.spec.ts (FULL — the D5-16 spec to extend;
    _fixtures.ts turnToPage/totalPages/assertHighlightMark usage)
  - 19-RESEARCH.md §Pitfall 2 + §Pitfall 10; 19-UI-SPEC §Component Inventory fragmentRenderer contract
  </read_first>
  <action>
  1. Implement the per-page first-occurrence pass at the page-render level: while mapping entries in
     document order, track a Set of highlight ids already sliced on THIS mounted page; the first
     slice encountered per id gets `isFirst = true`, all later ones `isFirst = false` (overriding
     whatever the entry-local slicer produced — entry-local coordinates cannot know document
     firstness). This guarantees exactly one `id="hl-<id>"` per highlight per mounted document, and
     it precedes sibling slices in document order (the D10-03 jump focus lands at the page-local
     span start).
  2. Unit cells in cross-fragment-slicing.test.ts: a highlight whose global range covers multiple
     entries of one page yields isFirst on exactly the FIRST entry's slice; a split-block highlight
     across two pages yields isFirst on each page's first slice when the pass runs per page
     (simulate two page mountings); a page-2-only fragment of a span that started on page 1 still
     gets exactly one id on the mounted page.
  3. E2e cells in cross-fragment-render.spec.ts: (a) a multi-block span captured on page N
     (paginated mode) whose extent crosses onto page N+1 after a typography-triggered repagination —
     marks appear on every mounted page sharing data-highlight-id, `page.locator(`[id="hl-${id}"]`)`
     count === 1 on EACH mounted page (Pitfall 2 mandatory assertion — axe will not catch it);
     (b) a list-containing span in paginated mode marks items on the mounted fragment (mirror of
     Plan 03's scrolling cell); (c) the pre-existing cross-page-selection refusal expectation in
     capture-rejects.spec.ts is referenced green (do not modify it); (d) a caption-endpoint span in
     paginated mode — select from a paragraph into an adjacent figure's caption (figure-heavy
     fixture, non-empty alt) on the mounted page, click Highlight, then assert the mark is visible
     INSIDE the figcaption element, zero marks on the img, and exactly one `id="hl-…"` for the
     highlight on the mounted page. Cell (d) pins Task 1's caption routing decision: it fails if
     the paginated renderer silently bypasses Plan 03's caption path (scrolling twin = 19-03-T3
     unit cells (a)/(b); the 19-05 matrix endpoint cells are scrolling-mode only).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/annotations/cross-fragment-slicing.test.ts && npx playwright test tests/e2e/annotations/cross-fragment-render.spec.ts tests/e2e/annotations/capture-rejects.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - fragmentRenderer contains a per-page first-occurrence pass (grep a seen-ids Set or equivalent in
    the page entry mapping; isFirst overridden per entry)
  - Unit cells assert single isFirst per id per page mounting
  - E2e cell (a) asserts `[id="hl-…"]` toHaveCount(1) on each mounted page for a page-crossing span
  - E2e cell (d) asserts a figcaption mark for a paginated caption-endpoint span (the Task 1
    caption/code routing pin — no silent paginated bypass)
  - Caption/code routing decision (BlockView delegation vs entry-local forwarding) recorded in
    19-04-SUMMARY.md
  - capture-rejects.spec.ts is byte-unchanged by this plan and green (cross-page refusal intact)
  - `! grep -rn "test.skip\|test.fixme" tests/e2e/annotations/cross-fragment-render.spec.ts` exits 0
  </acceptance_criteria>
  <done>Paginated spans honor D5-16 over more blocks: marks per page, one id per mounted document,
  no capture-side cross-page machinery; caption/code entry routing is explicit, tested, and
  recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored global ranges → per-page derived slices | Derived-only rendering (never stored) — existing boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering | Duplicate ids across page fragments | mitigate | Per-page first-occurrence pass owns isFirst in paginated mode; e2e count===1 assertion per mounted page |
| T-19-11 | Tampering | Entry-local coordinate confusion | accept | Mirrors shipped blockquote entry path; Pitfall 4 discipline locked by unit cells; no new input surface |
| T-19-12 | Elevation | Cross-page selection escape | accept | No capture changes; measurement-body + single-page-mount guards unchanged (D5-08/Pitfall 10) |
</threat_model>

<verification>
- Unit: `npx vitest run tests/unit/annotations/cross-fragment-slicing.test.ts` — entry-local cells green
- E2e: `npx playwright test tests/e2e/annotations/cross-fragment-render.spec.ts tests/e2e/annotations/capture-rejects.spec.ts` — 3 engines; refusal intact
- Diff audit: capture.ts, schema.ts, persistence untouched by this plan
</verification>

<success_criteria>
- Page-crossing spans mark every mounted page with shared data-highlight-id and exactly one id per page (ANNO-09 paginated + ANNO-10 repagination)
- List items and caption/code spans render marks inside fragments (paginated parity with Plan 03, routing explicitly tested)
- The cross-page capture boundary is honestly preserved (ANNO-13 deferred, refusal green)
</success_criteria>

## Artifacts this phase produces

*This plan:*
- Per-page first-occurrence id pass (fragmentRenderer.tsx, planner-named helper or inline pass)
- Entry-local list-items threading for `bulleted-list`/`numbered-list` entries (fragmentRenderer.tsx)
- Extended cells in tests/unit/annotations/cross-fragment-slicing.test.ts + tests/e2e/annotations/cross-fragment-render.spec.ts

*From earlier plans:* capture reasons + span-capture.spec (19-01), excerpt/export (19-02),
`HighlightSlice.isFirst` + `sliceCodeForHighlights` + `nested-list-paths` + list threading (19-03).
*Later:* `eligibility-matrix.spec.ts` (19-05).

<output>
Create `.planning/phases/19-cross-block-highlights/19-04-SUMMARY.md` when done
</output>
