---
phase: 15-application-shell-and-destinations
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - src/ingestion/library/librarySession.ts
  - src/ingestion/library/LibraryView.tsx
  - src/App.tsx
  - tests/unit/library/library-session.test.ts
  - tests/e2e/library/library-restore.spec.ts
  - tests/e2e/library/reading-views.spec.ts
autonomous: true
requirements: [NAV-03]
must_haves:
  truths:
    - "Returning from Reader or Highlights to the Library — via BackToLibrary, the shell Library link, or the brand — restores the active query and tag filters"
    - "When the landing view matches the captured view, the scroll position is restored (clamped to the list height) and focus lands on the launched row's 'Open article' link — or on the h1 without disturbing the restored scroll when no article was launched"
    - "When the row is gone, the scroll overshoots the list, or the landing view mismatches the captured view, restore degrades calmly (h1 focus / clamped scroll / fresh reset) with no error and no false restore"
    - "Restore state is session-scoped in-memory only — nothing persists to Dexie, nothing survives reload, and LibraryView remains unmount/remount (no keep-alive)"
  artifacts:
    - path: "src/ingestion/library/librarySession.ts"
      provides: "session snapshot + pure comparators (store-seam discipline)"
      exports: ["LibraryContextSnapshot", "captureLibraryContext", "peekLibraryContext", "viewMatches", "clampScroll"]
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "capture-on-leave / restore-on-return wiring with view-match gating"
      contains: "librarySession"
    - path: "tests/unit/library/library-session.test.ts"
      provides: "pure logic coverage (view-match, clamp, round-trip)"
      contains: "clampScroll"
    - path: "tests/e2e/library/library-restore.spec.ts"
      provides: "NAV-03 return-path × degradation matrix"
      contains: "Open article"
  key_links:
    - from: "src/ingestion/library/LibraryView.tsx unmount cleanup"
      to: "src/ingestion/library/librarySession.ts"
      via: "single capture write merging live window.scrollY + launched article id (StrictMode-idempotent — Pitfall 8)"
      pattern: "captureLibraryContext"
    - from: "src/ingestion/library/LibraryView.tsx post-ready effect"
      to: "window.scrollTo + row-link focus"
      via: "status === 'ready' gate → viewMatches check → clamped scrollTo → row focus (preventScroll iff row intersects viewport — Pitfall 5); null launched row → h1 focus with preventScroll"
      pattern: "viewMatches"
    - from: "src/App.tsx mount effect"
      to: "history.scrollRestoration"
      via: "set to 'manual' once early — the app owns scroll on Back (Pitfall 3)"
      pattern: "scrollRestoration"
  prohibitions:
    - statement: "Restore state must never persist to Dexie or survive reload (D15-12 — session-only, Pitfall 9: no store changes this phase)"
      status: resolved
      verification: "librarySession.ts imports nothing from persistence/Dexie; phase diff contains no db.version( changes"
    - statement: "No keep-alive hidden LibraryView mount (D15-12)"
      status: resolved
      verification: "App.tsx three-view conditional swap unchanged — no hidden mounted instance"
    - statement: "Never restore something that isn't true (D15-14) — no mismatched scroll, no guessed row"
      status: resolved
      verification: "matrix cases (c)/(e): mismatch lands fresh; row-gone falls back to h1"
---

<objective>
Build the return-context restore (D15-11..14): a session-scoped librarySession module (view + query + tag + scroll + row focus) integrated into LibraryView with view-match gating, per-field graceful degradation, and explicit scroll/focus ordering — closing the D14-08 deferral via the D14-05/D14-10 most-specific-target layering.

Purpose: NAV-03 — readers return from an article or highlight review to their prior Library context (filters, scroll, row focus) without losing it, on every return path.
Output: librarySession.ts + unit tests, LibraryView capture/restore wiring, App scroll ownership, library-restore e2e matrix, reading-views assertion supersession.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-application-shell-and-destinations/15-CONTEXT.md
@.planning/phases/15-application-shell-and-destinations/15-RESEARCH.md
@.planning/phases/15-application-shell-and-destinations/15-PATTERNS.md
@.planning/phases/15-application-shell-and-destinations/15-UI-SPEC.md
@.planning/phases/15-application-shell-and-destinations/15-02-SUMMARY.md
@src/ingestion/library/LibraryView.tsx
@src/ingestion/library/readingState.ts
@src/reader/restoreLocation.ts
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — library-session unit cases + library-restore sentinel scaffold</name>
  <files>tests/unit/library/library-session.test.ts, tests/e2e/library/library-restore.spec.ts</files>
  <read_first>
  - tests/unit/library/reading-state.test.ts (the pure-coverage structure to mirror: header comment, boundary-named cases)
  - tests/e2e/library/reading-views.spec.ts (seedCorpus/openView seeding + readiness discipline L539-630; the beforeEach clear-rows pattern L585-630)
  - tests/e2e/library/search-tag-filter.spec.ts (query/tag driving helpers)
  - tests/e2e/annotations/_fixtures.ts (BASE, wipeDatabase)
  </read_first>
  <behavior>
  - viewMatches(landing, captured) is true iff the two LibraryViewName values are equal; false on any mismatch
  - clampScroll(saved, maxScroll) returns saved when saved ≤ maxScroll; returns maxScroll when saved > maxScroll (clamp to bottom)
  - captureLibraryContext then peekLibraryContext round-trips the exact snapshot; a second capture overwrites the first
  - peekLibraryContext() returns null before any capture (cold load)
  </behavior>
  <action>
  **tests/unit/library/library-session.test.ts (NEW — RED):** copy the reading-state.test.ts structure (header comment citing D15-11..14 rows pinned, plain describe/it boundary-named cases, snapshot as plain literals — view name, strings, number, nullable id). Import from ../../../src/ingestion/library/librarySession. Cases fail to resolve while the module is absent — that is the RED state.

  **tests/e2e/library/library-restore.spec.ts (NEW — harness scaffold):** copy the reading-views.spec.ts seeding discipline — beforeEach: image-stub route + goto BASE + h1 wait + raw IndexedDB CLEAR-ROWS over ["articles","settings","location","highlights","notes","books"] (clear-rows, NEVER deleteDatabase — the webkit race); seed rows via shipped Zod schemas in Node (ArticleSchema.parse / LocationRecordSchema.parse); seed BEFORE navigation (the load effect runs once per mount); an openView helper (goto hash + reload + h1 "Saved articles" + /^All \(\d+\)/ readiness wait). One sentinel test proving the harness on chromium. Seed enough articles (≥10) for meaningful scroll offsets.

  Commit: test(15-03): add failing library-session unit cases + library-restore harness scaffold.
  </action>
  <verify>
    <automated>npx vitest --run tests/unit/library/library-session.test.ts; npx playwright test tests/e2e/library/library-restore.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - tests/unit/library/library-session.test.ts exists with viewMatches / clampScroll / round-trip / overwrite / null-peek cases and FAILS (unresolvable import) — RED
  - tests/e2e/library/library-restore.spec.ts exists with wipeDatabase-derived clear-rows beforeEach, ≥10-article seeding, openView readiness helper, and a passing sentinel on chromium
  </acceptance_criteria>
  <done>RED unit cases pin the module contract; the e2e harness is proven before any production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — librarySession pure module + App scroll ownership</name>
  <files>src/ingestion/library/librarySession.ts, src/App.tsx</files>
  <read_first>
  - src/ingestion/library/readingState.ts (the pure-module store-seam discipline header L1-9)
  - src/reader/restoreLocation.ts (findScrollTarget clamp/degrade-calmly precedent L123-146)
  - src/ingestion/library/LibraryView.tsx (LibraryViewName import path)
  - src/App.tsx (AppInner mount effect registering onHash L208-229)
  </read_first>
  <behavior>
  - Module has zero React imports and zero Dexie/persistence imports
  - captureLibraryContext stores one snapshot; peekLibraryContext returns it without clearing (idempotent re-reads — StrictMode twin mounts)
  - viewMatches and clampScroll are pure functions matching Task 1's cases
  </behavior>
  <action>
  **src/ingestion/library/librarySession.ts (NEW — D15-12):** module-level singleton + pure comparators, the readingState.ts store-seam discipline verbatim (header comment: components own the IO — reads on mount, writes on change/unmount; this module owns only the algebra). Export:
  - `interface LibraryContextSnapshot { view: LibraryViewName; query: string; activeTag: string | null; scrollTop: number; lastArticleId: string | null }` (import LibraryViewName type from ../../App — type-only import, no runtime cycle)
  - `captureLibraryContext(next: LibraryContextSnapshot): void`
  - `peekLibraryContext(): LibraryContextSnapshot | null` (peek, not take — restore is idempotent)
  - `viewMatches(landing: LibraryViewName, captured: LibraryViewName): boolean` (pure)
  - `clampScroll(saved: number, maxScroll: number): number` (pure — clamps to bottom, the findScrollTarget "corpus changed since save" calm-clamp twin)

  **src/App.tsx (Pitfall 3):** in the AppInner mount effect that registers onHash, set `history.scrollRestoration = "manual"` once, early — the app's own restore becomes the only scroll actor on Back (grep-verified currently unset). Add the conscious comment: reload loses browser scroll restore — accepted per D15-12/D14-17 (view restores from URL; session scroll does not survive reload by decision).

  Commit: feat(15-03): librarySession pure module + manual scroll ownership.
  </action>
  <verify>
    <automated>npx vitest --run tests/unit/library/library-session.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - src/ingestion/library/librarySession.ts exports all five symbols; rg "from \"react\"|dexie|persistence" over the file returns 0 hits
  - All Task-1 unit cases now PASS (GREEN)
  - src/App.tsx contains history.scrollRestoration = "manual" inside the mount effect; rg "scrollRestoration" src returns exactly this one hit
  </acceptance_criteria>
  <done>Pure restore algebra green in jsdom; the app owns scroll on Back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LibraryView capture/restore integration + reading-views supersession + restore matrix</name>
  <files>src/ingestion/library/LibraryView.tsx, tests/e2e/library/library-restore.spec.ts, tests/e2e/library/reading-views.spec.ts</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (h1Ref/lastViewRef L138-145; query/activeTag state L148-149; mount focus effect L180-184; view-switch effect L198-204; async load + status L222-272; library-list ul L481; view-switcher nav L431-464)
  - src/ingestion/library/librarySession.ts (Task 2 output)
  - tests/e2e/library/reading-views.spec.ts (L824-851 — the "Back refocuses the library h1" assertions this task supersedes)
  - tests/e2e/library/library-restore.spec.ts (Task 1 harness)
  </read_first>
  <behavior>
  - Filters restore on ALL return paths regardless of view match (D15-13); cold load (null peek) keeps today's defaults
  - View-matched return: clamped scrollTo, then row-link focus (preventScroll true iff the row intersects the restored viewport, else default focus); null launched row (departure was not an article open — e.g. a Highlights round-trip) → clamped scrollTo + h1 focus with preventScroll true (the restored scroll stays authoritative); row gone → h1 focus with default scroll resetting to top (D14-05 default — the truthful D15-14 degrade)
  - View-mismatch return: filters apply, scroll/focus reset fresh (D15-14)
  - StrictMode twin mounts are harmless: capture writes byte-identical snapshots from live values; restore is idempotent (Pitfall 8)
  </behavior>
  <action>
  **LibraryView.tsx (D15-11..14; UI-SPEC §Interaction 6-9):**
  - *Capture (one write point):* a ref updated every render holding `{ view, query, activeTag }`; a delegated onClick on the `.library-list` ul recording the launched article id — `event.target.closest('a[href^="#/article/"]')` with the id parsed from the constant-template href (ids arrive from validated records — T-10-02c; the plain anchor still navigates natively, the handler only records) — into that ref; ONE unmount-only cleanup effect (`[]` deps) merging live `window.scrollY` (DOM still mounted at cleanup — the value is valid) + the launched id into `captureLibraryContext`. Refs hold live values so a StrictMode twin-pass rewrite is byte-identical (Pitfall 8).
  - *Filters restore (always):* lazy `useState` initializers for query and activeTag reading `peekLibraryContext()` (null on cold load → today's defaults). NO Dexie, NO keep-alive (D15-12).
  - *Mount h1 focus deferral:* when a snapshot exists (an in-session return), skip the early `warmMount` h1 focus and defer the h1-vs-row decision to the ready gate (one decision point — UI-SPEC §Interaction 7); when no snapshot exists, today's warmMount h1 behavior is byte-unchanged (cold loads keep natural focus — D14-03).
  - *Ready-gated restore:* a `[status]`-keyed effect running only at `status === "ready"` (rows painted — Pitfall 4): if `peekLibraryContext()` returns a snapshot AND `viewMatches(view, snapshot.view)`: compute `maxScroll = document.documentElement.scrollHeight - window.innerHeight`; `window.scrollTo(0, clampScroll(snapshot.scrollTop, maxScroll))`; then branch on `snapshot.lastArticleId` — null (no article was launched this visit — e.g. a Highlights round-trip): `h1Ref.current?.focus({ preventScroll: true })` so the focus does not reset the just-restored scroll (UI-SPEC §Interaction 8); non-null: row lookup via the constant-template selector `a[href="#/article/${snapshot.lastArticleId}"]` (querySelector on the list container) — if found: focus with `preventScroll: true` IFF the row's `getBoundingClientRect()` intersects the viewport, else default focus (the row becomes visible — Pitfall 5 ordering: scroll FIRST, then focus); if not found (row removed or filtered out — D15-14): `h1Ref.current?.focus()` with default scroll, i.e. reset-to-top — the D14-05 fallback and the truthful degrade (the launched row is gone; holding the old offset over unfamiliar rows would restore something untrue). If view mismatch: nothing further (filters already applied via initializers; scroll/focus reset fresh). Idempotent — a StrictMode double-run re-restores identical values. NO live-region announcement (D14-09 — focus landing IS the communication).
  - Preserve the `lastViewRef` previous-value comparison in the view-switch effect byte-for-byte (Pitfall 8 — booleans are not StrictMode-safe).

  **reading-views.spec.ts (Pitfall 7 — deliberate supersession):** update the "Back refocuses the library h1" block (L824+): Back with a captured + view-matched row → the row's `Open article` link is `document.activeElement`; Back with NO capture (the cold→article→Back edge) or a view mismatch → h1 (unchanged). Every changed assertion carries a D15-11..14 citation comment (strengthen-only discipline permits decision-locked behavior changes — the D14-25 precedent).

  **library-restore.spec.ts matrix (extend the scaffold; reuse the seeding helpers):**
  - (a) BackToLibrary path, view-match: openView `#/unread`, type a query into LibrarySearch, scroll down, click a row's `Open article` → Reader → activate Back to library → query restored in the search input, scrollY within tolerance of the capture, the launched row's link focused.
  - (b) Shell Library link path, view-match: capture from `#/` (the All view — precondition pinned because the Library link's constant target is `#/` per D15-05/D15-09, so a capture from any other view would land mismatched on this path), set filters, scroll, open an article, return via the header Primary-nav "Library" link → query restored, scrollY within tolerance of the capture, the launched row's link focused.
  - (c) Brand path, view-mismatch: capture in `#/unread` with filters → Reader → click the "Lem Reader" brand link → URL `#/` (All view): filters restored, scroll at top, h1 focused (D15-14 — never restore mismatched scroll).
  - (d) Highlights return (null launched row): Library with filters set and scrolled, NO article opened this visit (captured lastArticleId is null) → shell "Highlights" link → Back to library → filters + scroll restored (clamped) AND h1 focused via the null-row branch — h1 focus with preventScroll true, so the scroll restore survives the focus (assert scrollY within tolerance after focus lands).
  - (e) Row-gone degradation: capture with a launched row → while in Reader, remove that article's rows via raw IndexedDB clear (or seed so the row filters out on return) → Back → h1 focused with default scroll (scroll reset to top — the default-scroll h1 fallback is reserved for this degrade, where reset is the truthful D15-14 behavior), no crash, no false row focus.
  - (f) Clamp sanity: capture a deep scroll with a launched row, then while in Reader delete OTHER articles' rows via raw IndexedDB, KEEPING the launched article so the row-found path runs (otherwise the row-gone h1 reset zeroes scrollY and the clamp asserts nothing) → Back → short list, scrollY within tolerance of maxScroll AND > 0 (clamped to bottom, not reset), the launched row's link focused, no error.

  Commit: feat(15-03): view-matched library return-context restore (filters, scroll, row focus).
  </action>
  <verify>
    <automated>npx vitest --run tests/unit/library/library-session.test.ts tests/component/App.test.tsx && npx playwright test tests/e2e/library/library-restore.spec.ts tests/e2e/library/reading-views.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - LibraryView.tsx imports captureLibraryContext/peekLibraryContext/viewMatches/clampScroll; contains a delegated click capture on the library-list ul and one unmount-cleanup capture write
  - rg "db\.version\(" over the phase diff returns nothing (Pitfall 9 — no store changes)
  - Matrix cases (a)-(f) pass on chromium; reading-views superseded assertions pass with D15-11..14 citation comments present
  - App.test.tsx still green (router grammar untouched)
  </acceptance_criteria>
  <done>NAV-03 proven: all three return paths restore filters; view-matched returns restore scroll + row focus; every degradation lands calmly and truthfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session module ← LibraryView | App-generated snapshot values (view names, strings, numbers, validated article ids) |
| Departure click → snapshot | The launched-row id is derived from a constant-template href over validated records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | session-restore state pollution | mitigate | Snapshot holds only app-generated values; row lookup uses a constant-template selector; results are used only as lookup keys, never DOM-anchored or interpolated into URLs |
| T-15-08 | Tampering | row-focus selector | mitigate | `a[href="#/article/${id}"]` template with ids from validated records only (T-10-02c discipline); no user-controlled string reaches the selector beyond the validated id |
| T-15-09 | Denial of service | restore on unready DOM | mitigate | status === "ready" gate prevents scrollTo clamping to zero height and row lookups on an unpainted list (Pitfall 4) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None — zero package installs this phase |
</threat_model>

<verification>
- Unit: library-session.test.ts fully green (pure algebra).
- Component: App.test.tsx unchanged-green (router untouched by restore logic beyond scrollRestoration).
- e2e (chromium quick gate): library-restore matrix (a)-(f) + updated reading-views assertions.
- 3-engine matrix + honest full-suite gate: Plan 15-04.
</verification>

<success_criteria>
- Full restore set (view from URL per D14-17; query + tag + scroll + row focus from session) behaves per D15-11.
- All return paths restore filters; scroll + row focus only on view match (D15-13).
- Degradations truthful per D15-14: row gone → h1; scroll overshoot → clamp; mismatch → fresh.
- No persistence, no keep-alive, no Dexie changes (D15-12); StrictMode-idempotent (Pitfall 8).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| src/ingestion/library/librarySession.ts | Module (NEW) | exports LibraryContextSnapshot, captureLibraryContext, peekLibraryContext, viewMatches, clampScroll |
| LibraryView capture/restore wiring | Integration | delegated row capture, unmount snapshot write, ready-gated view-matched restore |
| `history.scrollRestoration = "manual"` | App behavior | set once early in AppInner mount (Pitfall 3) |
| tests/unit/library/library-session.test.ts | Test file (NEW) | pure comparators + snapshot round-trip |
| tests/e2e/library/library-restore.spec.ts | Test File (NEW) | 3-path × match/mismatch × degradation matrix |
| reading-views.spec supersession | Test update | row-focus beats h1 when captured+matched (D15-11 citations) |

(No new packages; no Dexie schema changes; no new components.)

<output>
Create `.planning/phases/15-application-shell-and-destinations/15-03-SUMMARY.md` when done
</output>
