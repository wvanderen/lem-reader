---
phase: 15-application-shell-and-destinations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/routes/review/ReviewView.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/app.css
  - tests/component/App.test.tsx
  - tests/e2e/a11y.spec.ts
  - tests/e2e/chrome/back-nav.spec.ts
  - tests/e2e/forced-colors.spec.ts
  - tests/e2e/library/reading-views.spec.ts
  - tests/e2e/panel-keyboard.spec.ts
  - tests/e2e/reduced-motion.spec.ts
  - tests/e2e/review-panel/curate.spec.ts
  - tests/e2e/review-panel/empty-states.spec.ts
  - tests/e2e/review-panel/jump-bidirectional.spec.ts
  - tests/e2e/review-panel/listing.spec.ts
  - tests/e2e/review-panel/route-entry.spec.ts
  - tests/e2e/review-panel/tri-state.spec.ts
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "Visiting #/highlights shows a page whose h1 is 'Highlights' and whose document.title is 'Highlights — Lem Reader'"
    - "Visiting a legacy #/review URL lands the same Highlights page with the URL normalized to #/highlights and no extra history entry"
    - "Unknown #/ segments (including #/highlights/x) still fall back to the All list view (D14-16 intact)"
    - "After the rename commit, no repo file pins the retired two-word review vocabulary (rg gate returns zero files)"
  artifacts:
    - path: "src/App.tsx"
      provides: "#/highlights grammar arm + #/review legacy alias with replaceState normalization"
      contains: "#/highlights"
    - path: "src/routes/review/ReviewView.tsx"
      provides: "Renamed destination surface"
      contains: "Highlights"
    - path: "tests/component/App.test.tsx"
      provides: "parseHash grammar + alias unit coverage"
      contains: "#/highlights"
  key_links:
    - from: "src/App.tsx onHash handler"
      to: "history.replaceState + setView(parseHash())"
      via: "alias normalization mirrors switchLibraryView — replaceState fires NO hashchange so the direct setView is load-bearing"
      pattern: "replaceState\\(null, \"\", \"#/highlights\"\\)"
    - from: "src/routes/review/ReviewView.tsx"
      to: "src/ingestion/library/pageMeta.ts"
      via: "setDocumentTitle(\"Highlights\") — suffix/separator live ONLY in the helper (D14-02)"
      pattern: "setDocumentTitle\\(\"Highlights\"\\)"
  prohibitions:
    - statement: "Never use pushState for the alias redirect (would corrupt Back-count semantics D14-14)"
      status: resolved
      verification: "code read — only replaceState touches the alias; e2e alias case asserts single-entry Back behavior stays intact"
    - statement: "Never implement the alias via location.hash assignment (fires a second hashchange — double processing)"
      status: resolved
      verification: "unit + alias-compat e2e land exactly once on Highlights with normalized URL"
---

<objective>
Rename the review destination to **Highlights** (D15-06) — route `#/highlights`, h1, document.title — and add the legacy `#/review` alias-redirect (D15-07), commit-atomic with every pinned-assertion update across the suite (Pitfall 6: one commit renames grammar + copy + ALL specs together).

Purpose: NAV-01's destination grammar foundation — the shell nav (Plan 15-02) links to `#/highlights`, so the canonical route and rename must land first with zero red suites between commits.
Output: Extended parseHash grammar, renamed ReviewView copy, ~13 spec files updated atomically, alias-compat e2e coverage.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-application-shell-and-destinations/15-CONTEXT.md
@.planning/phases/15-application-shell-and-destinations/15-RESEARCH.md
@.planning/phases/15-application-shell-and-destinations/15-PATTERNS.md
@.planning/phases/15-application-shell-and-destinations/15-UI-SPEC.md
@src/App.tsx
@src/routes/review/ReviewView.tsx
@src/ingestion/library/pageMeta.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — parseHash unit cases for the Highlights grammar + legacy alias</name>
  <files>tests/component/App.test.tsx</files>
  <read_first>
  - tests/component/App.test.tsx (the parseHash describe block L51-128, mock discipline L14-49)
  - src/App.tsx (parseHash L50-83, View union L45-48, switchLibraryView L264-268)
  </read_first>
  <behavior>
  - "#/highlights maps to the review view" — parseHash() returns { name: "review" }
  - "#/review maps to the review view with the legacy alias marker" — parseHash() returns { name: "review", legacyAlias: true }
  - "#/highlights/x (unknown sub-route) maps to the list view" — parseHash() returns { name: "list", view: "all" }
  - Existing "#/review/x → list/all" case stays byte-stable and green (D14-16 fallback discipline)
  </behavior>
  <action>
  Extend the existing parseHash describe in tests/component/App.test.tsx with the four cases above. The View review alternative will gain `legacyAlias?: true` (Task 2) — assert the exact object shapes listed in behavior. Follow the existing mock discipline (vi.mock of the content repository, beforeEach hash reset). Commit as the RED commit: test(15-01): add failing parseHash cases for Highlights grammar + legacy alias.
  </action>
  <verify>
    <automated>npx vitest --run tests/component/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - tests/component/App.test.tsx contains a case mapping the literal hash "#/highlights" to the review view
  - tests/component/App.test.tsx contains a case asserting the legacyAlias marker on the "#/review" parse
  - New cases FAIL (RED) while all pre-existing cases in the file PASS
  - `npx vitest --run tests/component/App.test.tsx` exits non-zero solely due to the new cases
  </acceptance_criteria>
  <done>RED commit landed; failing cases pin the exact grammar contract for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the atomic rename commit (grammar + alias + copy + all pinned specs)</name>
  <files>src/App.tsx, src/routes/review/ReviewView.tsx, src/ingestion/library/LibraryView.tsx, src/app.css, tests/component/App.test.tsx, tests/e2e/a11y.spec.ts, tests/e2e/chrome/back-nav.spec.ts, tests/e2e/forced-colors.spec.ts, tests/e2e/library/reading-views.spec.ts, tests/e2e/panel-keyboard.spec.ts, tests/e2e/reduced-motion.spec.ts, tests/e2e/review-panel/curate.spec.ts, tests/e2e/review-panel/empty-states.spec.ts, tests/e2e/review-panel/jump-bidirectional.spec.ts, tests/e2e/review-panel/listing.spec.ts, tests/e2e/review-panel/route-entry.spec.ts, tests/e2e/review-panel/tri-state.spec.ts</files>
  <read_first>
  - src/App.tsx (parseHash L50-83, onHash + fragment guard L218-229, switchLibraryView L264-268, mount effect L208-229)
  - src/routes/review/ReviewView.tsx (title mount effect L294-298, review-header + h1 L346-357, file-header comments)
  - src/ingestion/library/LibraryView.tsx (D10-02 button L388-396)
  - src/ingestion/library/pageMeta.ts (setDocumentTitle — suffix/separator ownership, D14-02)
  - src/app.css (comment blocks at ~L2706, ~L2717, ~L3165 referencing the old destination vocabulary)
  - tests/e2e/review-panel/route-entry.spec.ts (the (a)-(e) structure and selector discipline)
  - tests/e2e/chrome/back-nav.spec.ts (L124-142 drives the in-page button + the #/review deep-link case)
  </read_first>
  <action>
  ONE commit carries everything in this task (Pitfall 6 — a grammar/copy rename split from its pinned assertions turns the suite red between commits).

  **App.tsx (D15-06/D15-07):**
  - View union: the review alternative becomes `{ name: "review"; legacyAlias?: true }` (OQ3 resolution: the INTERNAL name stays "review" with a comment citing D15-06 — user-facing vocabulary renames, internal grammar name stays stable to avoid touching every view.name site).
  - parseHash grammar order stays: article regex → NEW `#/highlights` literal arm (returns plain review) → the existing `#/review` arm now returns `{ name: "review", legacyAlias: true }` → view segments → All fallback (D14-16 discipline; `#/review/x` and `#/highlights/x` both fall through to All).
  - Alias normalization mirrors switchLibraryView (App.tsx L264-268) EXACTLY: in onHash, when parseHash returns the review view carrying legacyAlias, call history.replaceState(null, "", "#/highlights") — constant href, same-origin by construction (T-14-04) — then setView(parseHash()) as the load-bearing direct update (replaceState fires NO hashchange — Pitfall 2). NEVER location.hash assignment for the normalization (double-processing) and NEVER pushState (Back-count corruption — RESEARCH anti-pattern).
  - Cold-load normalization: the mount effect that registers onHash also runs the same normalization once at mount when window.location.hash is the legacy literal (view state from the useState initializer is already correct; only the URL needs normalizing).

  **ReviewView.tsx (D15-06):** h1 text becomes `Highlights` (tabIndex={-1} + ref stay byte-stable); `setDocumentTitle("Highlights")` so the title renders "Highlights — Lem Reader" (suffix/separator/truncation live ONLY in pageMeta — D14-02; never re-implement at the call site). Update file-header comments naming the route/h1 to the new vocabulary, citing D15-06.

  **LibraryView.tsx (interim, renamed not removed):** the D10-02 button label becomes `Highlights` and its onClick assigns the canonical `#/highlights` (retired vocabulary must not survive — UI-SPEC auto-resolution #5 fallback). Removal happens in Plan 15-02 when the shell link replaces it.

  **app.css:** update ONLY the prose comments at ~L2706, ~L2717, ~L3165 that name the old destination vocabulary (the class `.article-export-highlights` stays — it is a shared token set, not vocabulary). No rule changes in this plan.

  **Spec update pass (enumerated, not guessed):** run `rg -l "Review highlights|#/review" src tests` and update EVERY hit in the same commit:
  - Heading assertions `name: "Review highlights"` → `name: "Highlights"` (level 1 unchanged).
  - URL assertions /#\/review$/ → /#\/highlights$/.
  - page.goto targets whose intent is the CANONICAL route switch to `#/highlights`; only deliberate alias-exercise gotos keep `#/review` AND gain a normalized-URL assertion.
  - back-nav.spec.ts L124: the driven button's accessible name becomes "Highlights".
  - Update prose comments in specs that name the old vocabulary.
  - ADD an alias-compat e2e case to tests/e2e/review-panel/route-entry.spec.ts: page.goto `${BASE}/#/review` → h1 "Highlights" visible AND page URL is `#/highlights` (proves D15-07 end-to-end).

  Commit: feat(15-01): rename review destination to Highlights with legacy alias.

  <!-- planner-discipline-allow: Review highlights -->
  </action>
  <verify>
    <automated>npx vitest --run tests/component/App.test.tsx && npx playwright test tests/e2e/review-panel/ tests/e2e/library/reading-views.spec.ts tests/e2e/a11y.spec.ts tests/e2e/reduced-motion.spec.ts tests/e2e/forced-colors.spec.ts tests/e2e/panel-keyboard.spec.ts tests/e2e/chrome/back-nav.spec.ts --project=chromium && rg -l "Review highlights" src tests | wc -l | grep -qx 0 && rg -n "#/review" src tests</automated>
  </verify>
  <acceptance_criteria>
  - src/App.tsx contains a literal `#/highlights` comparison arm ordered before the `#/review` alias arm in parseHash
  - src/App.tsx alias normalization pairs history.replaceState(null, "", "#/highlights") with a direct setView(parseHash()) in onHash; grep for pushState in the diff returns nothing
  - The rg gate returns 0 files pinning the retired vocabulary; residual `#/review` hits are ONLY the parseHash alias arm, the alias unit cases, and the alias-compat e2e (review the final rg -n output in the SUMMARY)
  - src/routes/review/ReviewView.tsx contains `Highlights` as the h1 text and setDocumentTitle("Highlights")
  - All commands in verify exit 0; the vitest file is fully green (RED cases now pass)
  </acceptance_criteria>
  <done>Canonical #/highlights route live; legacy #/review URLs normalize calmly; entire suite green on chromium with zero retired-vocabulary pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location.hash → parseHash | Foreign-influenced input crosses into the router grammar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | parseHash (App.tsx) | mitigate | Closed literal allowlist — `#/highlights` compared === against a constant; unknown segments → All fallback (D14-16); no route value interpolated into DOM/URL (T-10-02c) |
| T-15-02 | Tampering | ReviewView copy / pageMeta title | mitigate | All copy is static React text children; setDocumentTitle is text-only with the existing 64-char cap (T-14-01) |
| T-15-03 | Tampering | alias redirect | mitigate | Constant rewrite target `#/highlights` via replaceState — same-origin by construction (T-14-04); no attacker-influenced URL reaches navigation |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None — zero package installs this phase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Unit: full App.test.tsx parseHash surface green (new grammar + alias + fallback cases).
- e2e (chromium quick gate): renamed spec set listed in Task 2 verify green.
- Gates: retired-vocabulary rg returns 0 files; residual #/review hits reviewed as intentional alias coverage.
- 3-engine matrix + full-suite gate run in Plan 15-04 (phase gate).
</verification>

<success_criteria>
- parseHash maps #/highlights → review; #/review → review + legacyAlias; unknown #/ segments → All.
- h1 "Highlights" + title "Highlights — Lem Reader" render on the destination.
- Legacy URL normalizes via replaceState (no extra history entry, no hashchange double-processing).
- One atomic commit covers grammar + copy + all pinned assertions (suite green at commit boundary).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `#/highlights` grammar arm | Route literal | canonical Highlights destination (D15-06) |
| `#/review` → `#/highlights` alias | Route redirect | replaceState normalization (D15-07) |
| `View` review variant `legacyAlias?: true` field | Type extension | src/App.tsx (internal name stays "review" — OQ3 resolution) |
| Renamed ReviewView copy | Surface copy | h1/title "Highlights" |
| New App.test.tsx parseHash cases | Tests | grammar + alias + fallback |

(No new packages; no Dexie changes; no new CSS rules in this plan.)

<output>
Create `.planning/phases/15-application-shell-and-destinations/15-01-SUMMARY.md` when done
</output>
