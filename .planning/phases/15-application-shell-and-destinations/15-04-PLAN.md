---
phase: 15-application-shell-and-destinations
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - src/app.css
  - tests/e2e/chrome/shell-nav.spec.ts
autonomous: true
requirements: [POLISH-07]
must_haves:
  truths:
    - "Library, Highlights, Add (as it exists today), and Reader surfaces align to the shared tokens — content inset from main#main, the 1100px centered measure family, the --space scale — with any deviation either fixed or documented as an intentional difference with a citation"
    - "Every interactive control on every audited surface keeps a ≥44px touch target and the global :focus-visible ring is nowhere suppressed"
    - "At 320×640 in Reader the 48px header row holds with no wrap and no overflow on chromium, firefox, and webkit"
    - "The full suite passes in one invocation — npm run test exit 0 — with fail counts recorded honestly (no subset, no engine skip)"
  artifacts:
    - path: "src/app.css"
      provides: "token drift fixes from the POLISH-07 audit (D15-03)"
      contains: "shell-nav"
    - path: "tests/e2e/chrome/shell-nav.spec.ts"
      provides: "strengthened 320×640 geometry assertions across 3 engines"
      contains: "320"
  key_links:
    - from: "audited surfaces (Library/Highlights/Add/Reader)"
      to: "src/app.css token invariants"
      via: "every top-level block aligns to main#main inset + the 1100px measure family; spacing from the --space scale only"
      pattern: "max-width: 1100px"
  prohibitions:
    - statement: "No new layout system and no shared shell layout component (D15-03 — token fixes only)"
      status: resolved
      verification: "phase diff on app.css contains token alignments only; no new layout abstraction file exists"
    - statement: "The 48px header height never changes (load-bearing: banner top offsets, calc(100dvh − 48px), restoreLocation headerPx = 48)"
      status: resolved
      verification: "grep — min-height: 48px on .app-header byte-stable"
    - statement: "Intentional differences must not be 'fixed' (paginated-main --space-2xl block inset; Reader 64ch measure; .review-select 16px form-control text)"
      status: resolved
      verification: "those rules byte-unchanged in the diff, with citation comments added where absent"
    - statement: "No outline suppression anywhere (global focus ring is contractual)"
      status: resolved
      verification: "phase diff adds no outline: 0/none declarations (app.css L131-134 rule stays authoritative)"
---

<objective>
Close POLISH-07 (D15-03): run the token audit across Library, Highlights, Add (as-is), and Reader against the existing invariants, fix drift where found, harden the 320×640 header geometry across all three engines (with the sanctioned relief ladder if overflow is measured), and run the phase's honest full-suite gate.

Purpose: Success criterion 5 — coherent gutters, headers, spacing, responsive behavior, and visible focus across all four surfaces — plus the phase-exit proof (3-engine matrix + npm run test exit 0).
Output: app.css drift fixes with documented intentional differences, strengthened geometry assertions, green full suite with recorded counts.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-application-shell-and-destinations/15-CONTEXT.md
@.planning/phases/15-application-shell-and-destinations/15-RESEARCH.md
@.planning/phases/15-application-shell-and-destinations/15-PATTERNS.md
@.planning/phases/15-application-shell-and-destinations/15-UI-SPEC.md
@.planning/phases/15-application-shell-and-destinations/15-01-SUMMARY.md
@.planning/phases/15-application-shell-and-destinations/15-02-SUMMARY.md
@.planning/phases/15-application-shell-and-destinations/15-03-SUMMARY.md
@src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-07 token audit + drift fixes (D15-03)</name>
  <files>src/app.css</files>
  <read_first>
  - src/app.css (tokens L22-41; :focus-visible L131-134; main#main inset L199-234; .app-header L392-412; .library-header ~L2154+; .review-header ~L2717+; .ingest-control ~L750+; the 1100px measure family — .view-switcher L2059, .library-search L2082; the sub-640 media-query family)
  - 15-UI-SPEC.md §Layout Contract "Token audit checklist" table (the six invariants + the intentional-differences list)
  </read_first>
  <action>
  Audit the four surfaces (Library, Highlights, Add as-is, Reader) against the six EXISTING invariants and fix drift in app.css only — no new layout system, no shared shell component (D15-03):
  1. Content inset: every scrolling surface inherits `main#main { padding-inline: var(--space-md); padding-block: var(--space-3xl) }` — no per-view wrapper insets (the 01-04 unification).
  2. Centered measure family: top-level blocks (.library-header, .library-search, .tag-filter, .view-switcher, .library-list, .review-header, .review-filter-row, .review-legend, .review-section) align to `max-width: 1100px; margin-inline: auto`.
  3. Header height: 48px single row everywhere — byte-stable (load-bearing constants).
  4. Touch targets: every interactive control ≥ var(--touch) (44px) — including the new shell-nav links and collapsed-brand link at 320px.
  5. Focus: the global :focus-visible 2px ring is inherited everywhere; no surface overrides or suppresses it.
  6. Spacing: --space scale only — no orphan literal paddings/margins/gaps off-token.

  INTENTIONAL differences (document, do NOT fix): `main.paginated-main` block padding `--space-2xl` (viewport geometry — the paginated page-height budget); Reader article measure `--measure: 64ch` (reading surface, not the 1100px chrome measure); `.review-select` 16px control text (form-control register). Where these lack a citation comment, add one line citing the reason.

  Method: work surface-by-surface (audit ordering is discretion); for each candidate deviation decide drift-vs-intentional BEFORE editing; every fix is a token alignment (replace the orphan literal with the var() reference). Record the audit table (surface × invariant → keep/fix/intentional) in the SUMMARY.

  Commit: fix(15-04): POLISH-07 token audit drift fixes.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/reflow.spec.ts tests/e2e/touch-targets.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - The audit table (surface × invariant → disposition) appears in 15-04-SUMMARY.md
  - Every app.css hunk in the commit is either a token alignment or a citation comment; the three intentional-difference rules are byte-unchanged
  - rg "min-height: 48px" src/app.css still matches the .app-header rule; the phase diff adds no outline suppression
  - reflow + touch-targets chromium runs exit 0
  </acceptance_criteria>
  <done>Four surfaces audited against the six invariants; drift fixed on-token; intentional differences documented with citations.</done>
</task>

<task type="auto">
  <name>Task 2: 320×640 geometry hardening + sanctioned relief ladder</name>
  <files>src/app.css, tests/e2e/chrome/shell-nav.spec.ts</files>
  <read_first>
  - tests/e2e/chrome/shell-nav.spec.ts (the 320px geometry + collapse-safety tests from 15-02)
  - src/app.css (the ≤639px block from 15-02 — header padding/gap tuning + wordmark collapse)
  - 15-UI-SPEC.md §Interaction 11 (the 68px on-token recipe + the --space-xs relief ladder)
  </read_first>
  <action>
  Run the shell-nav 320px tests across all three engines (default matrix). IF any engine measures wrap or horizontal overflow on .app-header at 320×640 in Reader: apply the SANCTIONED relief ladder ONLY — in the ≤639px block, step `.app-header { padding-inline }` down from var(--space-sm) to var(--space-xs) (−8px more). Nothing else moves: 44px touch targets untouched, the 48px height untouched, destination links stay text links (D15-17). If the ladder step is applied, re-run and record the measured before/after numbers in the SUMMARY. If no overflow is measured, do not apply the ladder — record the passing measurements instead.

  Strengthen (never weaken) the shell-nav 320 assertions if the audit surfaced adjacent geometry worth pinning (e.g., assert .header-controls fits within the row's content box) — strengthen-only discipline.

  Commit: fix(15-04): 320px header geometry hardening (relief ladder applied only if measured).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/shell-nav.spec.ts -g "320"</automated>
  </verify>
  <acceptance_criteria>
  - shell-nav -g 320 tests pass on chromium, firefox, AND webkit (plain test() blocks inherit the full matrix)
  - The brand link remains keyboard reachable at 320×640 on all three engines (collapse-safety test green)
  - If the relief ladder was applied: the ONLY CSS delta is the ≤639px .app-header padding-inline step; measurements recorded in SUMMARY
  </acceptance_criteria>
  <done>The tightest constraint of the phase (Pitfall 1) proven in real browsers at 320×640 with the narrowest sanctioned budget.</done>
</task>

<task type="auto">
  <name>Task 3: 3-engine phase matrix + honest full-suite gate</name>
  <files>tests/e2e/chrome/shell-nav.spec.ts</files>
  <read_first>
  - 15-VALIDATION.md (sampling contract — phase gate definition)
  - tests/e2e/library/library-restore.spec.ts, tests/e2e/library/reading-views.spec.ts, tests/e2e/review-panel/route-entry.spec.ts (the matrix surface — read for context; fix engine-specific drift if any surfaces)
  </read_first>
  <action>
  1. Run the phase's new + renamed spec surface across all three engines: shell-nav.spec.ts, library-restore.spec.ts, reading-views.spec.ts, and the review-panel suite (route-entry + its siblings). Fix any engine-specific drift surfaced (focus-settle and scroll-timing quirks are the known firefox/webkit risks — the 05-05 precedent: fix in the spec's settle/wait discipline or the product code honestly, never by skipping an engine).
  2. Honest full-suite gate: run `npm run test` in ONE invocation; record the passed/failed/skipped counts verbatim in the SUMMARY (the 04-11/05-05 precedent: the executor runs the suite itself and records fail=0 honestly; a red run is fixed and re-run, never omitted). No subset runs, no --project filters, no grep cherry-picks at this gate.
  3. Update REQUIREMENTS.md traceability: NAV-01, NAV-02, NAV-03, NAV-05, POLISH-07 → Complete (Phase 15 rows), mirroring the Phase 14 completion pattern.

  Commit: test(15-04): 3-engine phase matrix + honest full-suite gate green.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
  - `npm run test` exits 0 in one invocation; unit + e2e pass/fail/skip counts recorded verbatim in 15-04-SUMMARY.md
  - The documented intentional-skip set (13 skips per STATE history) is unchanged or explained
  - REQUIREMENTS.md shows NAV-01/02/03/05 + POLISH-07 checked with Phase 15 rows Complete
  - .planning/STATE.md progress fields updated (5 phase plans total incl. this one)
  </acceptance_criteria>
  <done>Phase 15 exit criteria proven: every requirement green in real browsers across the matrix; the honest gate record is the permanent proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only audit + test runs; no new input surface, no network, no storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | CSS drift fixes | mitigate | Token alignments only — var() references replace orphan literals; no new url() imports, no inline style surface added |
| T-15-11 | Repudiation | honest gate | mitigate | fail counts recorded verbatim in the SUMMARY; suite run in one invocation (the 04-11 anti-misreport precedent) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None — zero package installs this phase |
</threat_model>

<verification>
- Task 1: reflow + touch-targets chromium nets green; audit table in SUMMARY.
- Task 2: 320px shell-nav tests green × 3 engines; relief ladder applied only on measurement.
- Task 3: npm run test exit 0, counts recorded; REQUIREMENTS/STATE updated.
- Manual-Only (per 15-VALIDATION): visual token-coherence comparison of the four surfaces at 320/768/1280 + focus-ring visibility — performed during Task 1, findings recorded in SUMMARY.
</verification>

<success_criteria>
- POLISH-07 closed: audit complete, drift fixed on-token, intentional differences documented.
- 320×640 Reader header holds on all three engines; brand reachable; 44px targets intact.
- Full suite green in one invocation with honest counts; all five Phase 15 requirements marked Complete.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| POLISH-07 audit table | Documentation | surface × invariant → keep/fix/intentional (15-04-SUMMARY.md) |
| app.css token-alignment fixes + citation comments | CSS | drift fixes only (D15-03) |
| Strengthened shell-nav geometry assertions | Test update | 320×640 × 3 engines |
| REQUIREMENTS.md / STATE.md completion updates | Planning state | Phase 15 rows closed |

(No new packages; no Dexie changes; no new source modules.)

<output>
Create `.planning/phases/15-application-shell-and-destinations/15-04-SUMMARY.md` when done
</output>
