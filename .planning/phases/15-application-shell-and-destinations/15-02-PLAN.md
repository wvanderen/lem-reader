---
phase: 15-application-shell-and-destinations
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/reader/Header.tsx
  - src/App.tsx
  - src/app.css
  - src/ingestion/library/LibraryView.tsx
  - tests/e2e/chrome/shell-nav.spec.ts
  - tests/e2e/review-panel/route-entry.spec.ts
  - tests/e2e/chrome/back-nav.spec.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-05]
must_haves:
  truths:
    - "A nav landmark labeled 'Primary' with the text links Library and Highlights renders inside the 48px app-header on the Library, Highlights, and Reader destinations"
    - "Exactly one shell-nav link carries aria-current='page' matching the active destination (Library for any list view, Highlights for review, neither in Reader); the brand link never carries it"
    - "Activating the brand link named 'Lem Reader' navigates to #/ and shows the All library view with h1 'Saved articles'"
    - "The reading-mode toggle renders only when an article is mounted; the settings gear renders on every destination"
    - "At 320×640 in Reader the 48px header row neither wraps nor overflows and the brand link stays keyboard reachable while visually collapsed"
    - "The library header no longer contains an in-page Highlights button — the shell link is the sole library→highlights entry"
  artifacts:
    - path: "src/reader/Header.tsx"
      provides: "brand link + shell destination nav + ModeToggle gating"
      contains: "shell-nav"
    - path: "src/app.css"
      provides: ".shell-nav / .header-start styles + ≤639px wordmark collapse + narrow tuning"
      contains: "shell-nav"
    - path: "tests/e2e/chrome/shell-nav.spec.ts"
      provides: "NAV-01/NAV-02/NAV-05 + 320px geometry e2e"
      contains: "Primary"
  key_links:
    - from: "src/App.tsx AppInner"
      to: "src/reader/Header.tsx"
      via: "destination prop derived from view (list→library, review→highlights, article→reader) drives aria-current"
      pattern: "destination="
    - from: "src/reader/Header.tsx shell-nav"
      to: "hash router"
      via: "plain <a href> links — activation assigns the hash, pushing a history entry (desired Back semantics); NO onClick interception"
      pattern: "href=\"#/highlights\""
    - from: "tests/e2e/chrome/shell-nav.spec.ts"
      to: "Header gating"
      via: "count-zero + contrast-leg pattern (mode toggle absent on Library/Highlights, present in Reader)"
      pattern: "Reading mode:"
  prohibitions:
    - statement: "No icon-only destination links — destination links are text links at every width (D15-17)"
      status: resolved
      verification: "shell-nav.spec asserts accessible names Library and Highlights by role link at 320×640"
    - statement: "No second nav row and no two-row header at any width (D15-01 — the 48px single-row geometry is load-bearing)"
      status: resolved
      verification: "320×640 geometry assertion: .app-header scrollHeight ≤ clientHeight + 1"
    - statement: "The collapsed wordmark must never be dropped from the accessibility tree — use the shipped .visually-hidden clip helper, never a rule that removes it from layout (Pitfall 9)"
      status: resolved
      verification: "320×640 keyboard-reachability assertion reaches the 'Lem Reader' link"
    - statement: "aria-current never appears on the brand link (D15-09)"
      status: resolved
      verification: "shell-nav.spec asserts the brand link's aria-current attribute is absent in every destination"
    - statement: "No Add destination link in the shell — exactly two destinations (D15-08)"
      status: resolved
      verification: "shell-nav.spec asserts the Primary nav contains exactly 2 links"
    - statement: "No quick-controls added to the header; the gear stays the sole global-prefs mechanism (D15-16)"
      status: resolved
      verification: "header-controls children on Library = gear only (gating assertion)"
---

<objective>
Build the persistent application shell (D15-01/02): destination nav in the existing 48px app-header, brand link to `#/` (D15-05), ModeToggle joins the articleMounted gate (D15-15), ≤639px wordmark collapse + narrow-width tuning (D15-17), and removal of the in-page Highlights button (OQ1 resolution — the shell link replaces it).

Purpose: NAV-01 (direct Library ↔ Highlights navigation through a consistent shell), NAV-02 (brand as predictable home), NAV-05 (reading-only controls gated, global prefs everywhere).
Output: Shell chrome in Header/App/app.css, shell-nav e2e spec, D10-02 button removed.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-application-shell-and-destinations/15-CONTEXT.md
@.planning/phases/15-application-shell-and-destinations/15-RESEARCH.md
@.planning/phases/15-application-shell-and-destinations/15-PATTERNS.md
@.planning/phases/15-application-shell-and-destinations/15-UI-SPEC.md
@.planning/phases/15-application-shell-and-destinations/15-01-SUMMARY.md
@src/reader/Header.tsx
@src/App.tsx
@src/app.css
@src/ingestion/library/LibraryView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — shell-nav.spec.ts sentinel scaffold</name>
  <files>tests/e2e/chrome/shell-nav.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/route-entry.spec.ts (harness + selector discipline L26-48)
  - tests/e2e/annotations/_fixtures.ts (BASE at L33, wipeDatabase at L44)
  - tests/e2e/chrome/header-geometry.spec.ts (evaluate-geometry assertion style)
  </read_first>
  <action>
  Create tests/e2e/chrome/shell-nav.spec.ts with the harness only: import BASE and wipeDatabase from ../annotations/_fixtures; test.beforeEach wipeDatabase; one sentinel test proving the harness wires up — page.goto `${BASE}/` then expect the level-1 heading "Saved articles" to be visible (the 04-02 Wave-0 precedent: a real passing sentinel, NOT test.todo). Plain test() blocks inherit the 3-engine matrix from playwright.config.ts.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/shell-nav.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/chrome/shell-nav.spec.ts exists and imports BASE + wipeDatabase from ../annotations/_fixtures
  - beforeEach calls wipeDatabase
  - Sentinel test passes on chromium (harness proven)
  </acceptance_criteria>
  <done>Sentinel green — the spec file is wired and ready for Task 3's real assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shell chrome — brand link, destination nav, gating, collapse + narrow tuning</name>
  <files>src/reader/Header.tsx, src/App.tsx, src/app.css</files>
  <read_first>
  - src/reader/Header.tsx (wordmark span at L94, articleMounted gates at L113/L132, ungated ModeToggle at L153, gear at L154-167, file-header comment L1-29)
  - src/App.tsx (Header wiring site L273-283, view state, articleMounted derivation at L277)
  - src/app.css (.app-header/.app-wordmark L392-412, .view-switcher L2058-2078, .visually-hidden L154-161, .header-controls ~L1299-1303, the sub-640px media-query family ~L3263)
  - src/ingestion/library/LibraryView.tsx (view-switcher aria-current precedent L431-464)
  </read_first>
  <behavior>
  - Header renders a `.header-start` group containing the brand link and nav.shell-nav[aria-label="Primary"] with exactly links "Library" (#/) and "Highlights" (#/highlights)
  - aria-current="page" appears on the Library link iff destination === "library", on the Highlights link iff destination === "highlights", never on the brand link
  - ModeToggle renders only when articleMounted is true; the gear renders unconditionally
  - At viewport ≤639px the wordmark is visually hidden via the clip helper but remains focusable; at 320×640 in Reader the .app-header row has no wrap and no horizontal overflow
  </behavior>
  <action>
  **Header.tsx (D15-01/02/05/09/10/15/16/18):**
  - New prop `destination: "library" | "highlights" | "reader"` on HeaderProps.
  - Wordmark: replace the span with `<a className="app-wordmark" href="#/">Lem Reader</a>` — accessible name is the text, NO aria-label, NO "home" suffix (D15-10); href is the fixed literal `#/`, never view-tracking (D15-05). Update the stale file-header comment ("the wordmark is a span, NOT a link") with the D15-05 citation.
  - Wrap brand + nav in a NEW `.header-start` div (the single inline-start child; `.app-header`'s space-between keeps controls inline-end).
  - `<nav className="shell-nav" aria-label="Primary">` containing exactly TWO plain `<a>` links: `Library` → `#/` and `Highlights` → `#/highlights` (D15-08: exactly two destinations; NO Add link). NO onClick interception — destination activation must push a history entry (desired Back semantics); modified clicks fall through natively. `aria-current={destination === "library" ? "page" : undefined}` on Library; the same shape for Highlights; NEVER on the brand (D15-09). Landmark label "Primary" is distinct from "Library views" (view-switcher) and "Book chapters" so all three nav landmarks stay distinguishable.
  - Wrap `<ModeToggle … />` in the same `{articleMounted && …}` guard as the tags/annotations triggers (D15-15) → Reader header reads [tags][annotations][mode][gear]; Library/Highlights read shell nav + gear only. The gear button stays ungated (D15-16). Article-scoped triggers stay in the shell header, NOT content headers (D15-18).

  **App.tsx:** derive `destination` from view inside AppInner (view.name "list" → "library", "review" → "highlights", "article" → "reader") and pass it to Header (Header stays presentational).

  **app.css (UI-SPEC §Component Inventory + §Interaction 11):**
  - `.shell-nav { display: flex; align-items: center; gap: var(--space-sm); white-space: nowrap; }` and `.shell-nav a { display: inline-flex; align-items: center; min-height: var(--touch); padding-inline: var(--space-xs); font-family: var(--font-ui); font-size: 14px; line-height: 1.45; }` — mirrors .view-switcher a verbatim (44px touch, inherited global focus ring, no color override — links ride the global a rule).
  - `.shell-nav a[aria-current="page"] { font-weight: 600; text-decoration-thickness: 2px; }` — the ONLY current-state styling; never color alone (forced-colors safe).
  - `.header-start { display: inline-flex; align-items: center; gap: var(--space-sm); min-width: 0; }`.
  - `.app-wordmark`: REMOVE the `color: var(--ink-soft)` declaration so the global link rule applies; KEEP font-family/size/weight/letter-spacing. REWRITE the stale comment at ~L411 citing D15-05.
  - ≤639px media query (the existing sub-640 family): the wordmark gets the shipped `.visually-hidden` clip treatment — NEVER a rule that removes it from the accessibility tree (Pitfall 9) — applied on ALL destinations (one shell, one rule — D15-02/D15-17). Narrow tuning recipe (padding/gap trims ONLY — touch targets stay 44px, the 48px height never changes): `.app-header { padding-inline: var(--space-sm); gap: var(--space-sm); }`, `.header-controls { gap: var(--space-xs); }`, `.header-start { gap: var(--space-xs); }`, `.shell-nav a { padding-inline: 0; }`.
  - ZERO new transition/animation/scroll-behavior properties (A11Y-06 — reduced-motion gate trivially satisfied).

  Commit: feat(15-02): shell destination nav, brand link, control gating, narrow-width collapse.
  </action>
  <verify>
    <automated>npm run lint && npx playwright test tests/e2e/chrome/mobile-first-page-chrome.spec.ts tests/e2e/reflow.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
  - src/reader/Header.tsx contains nav className "shell-nav" with aria-label "Primary" and exactly two <a> links (href "#/" and "#/highlights")
  - src/reader/Header.tsx brand is an <a> with href "#/" and text "Lem Reader"; grep for aria-label on the brand link returns nothing
  - ModeToggle is wrapped in an articleMounted conditional (grep order in .header-controls: tags-trigger, annotations-trigger, ModeToggle, gear-button)
  - src/App.tsx passes a destination prop derived from view to Header
  - src/app.css contains .shell-nav, .header-start, and a ≤639px block applying the visually-hidden treatment to .app-wordmark; the 48px min-height on .app-header is byte-unchanged
  - verify commands exit 0 (existing 320px nets stay green — the collapse + tuning landed with the nav, not after)
  </acceptance_criteria>
  <done>Shell renders identically in all three destinations; brand is a link; mode is reader-only; narrow-width collapse + tuning keep the 320px nets green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D10-02 button removal + shell-nav e2e (nav, aria-current, brand, gating, 320px)</name>
  <files>src/ingestion/library/LibraryView.tsx, tests/e2e/review-panel/route-entry.spec.ts, tests/e2e/chrome/back-nav.spec.ts, tests/e2e/chrome/shell-nav.spec.ts</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (library-header + D10-02 button L375-397)
  - tests/e2e/review-panel/route-entry.spec.ts (the (a) case this task rewrites; the (e) count-zero + contrast-leg gating pattern)
  - tests/e2e/chrome/back-nav.spec.ts (L124 drives the in-page button — this task rewrites it to the shell link)
  - tests/e2e/chrome/shell-nav.spec.ts (Task 1 sentinel)
  - tests/e2e/chrome/header-geometry.spec.ts (evaluate-geometry assertion style)
  </read_first>
  <behavior>
  - The library header contains ONLY the h1 — no in-page Highlights button
  - route-entry (a): clicking the shell "Highlights" link shows h1 "Highlights" and URL #/highlights
  - aria-current moves with the destination; brand click from Reader lands #/ All view
  - Mode toggle count 0 on Library + Highlights; present in Reader; gear visible on all three
  - 320×640 Reader: header row no wrap/no horizontal overflow; brand link keyboard reachable while collapsed
  </behavior>
  <action>
  **LibraryView.tsx (OQ1 resolution — UI-SPEC auto-resolution #5):** REMOVE the in-page Highlights button (L388-396) — the shell Highlights link replaces it; the library header returns to the calm h1 row POLISH-06 established. Delete its comment block too. (BackToLibrary on the Highlights side stays — D15-04, byte-unchanged.)

  **route-entry.spec.ts (a):** rewrite to drive navigation via the shell link — getByRole("link", { name: "Highlights" }) click → h1 "Highlights" + toHaveURL /#\/highlights$/.

  **back-nav.spec.ts:** the flow that drove the in-page button now navigates via the shell Highlights link (same Back-return expectations — the push semantics are identical).

  **shell-nav.spec.ts real tests (extend the sentinel; role/name queries only):**
  1. Nav presence: nav[aria-label="Primary"] with links "Library" + "Highlights" visible on `#/`, `#/highlights`, AND `#/article/<FIXTURES[0] from _fixtures>` (persistent shell — D15-02). Assert exactly 2 links in the nav (D15-08).
  2. aria-current discipline: on `#/` and `#/unread` the Library link carries aria-current="page" and Highlights does not (ANY list view = Library destination); on `#/highlights` the reverse; on `#/article/<id>` NEITHER link is current; the brand link has no aria-current attribute in all three destinations (D15-09).
  3. Brand home (NAV-02): from the article view, click the link named "Lem Reader" → URL is `#/` and h1 "Saved articles" visible (D15-05); assert the accessible name is exactly "Lem Reader" (D15-10).
  4. Gating (NAV-05): button name /Reading mode:/ count 0 on `#/` and `#/highlights`; visible on `#/article/<id>` (the count-zero + contrast-leg pattern from route-entry (e)); gear button "Reading settings" visible on all three destinations (D15-16).
  5. 320×640 geometry (POLISH-07 assist): setViewportSize 320×640 on the article view; evaluate .app-header — scrollHeight ≤ clientHeight + 1 AND scrollWidth ≤ clientWidth + 1 (no wrap, no horizontal overflow — Pitfall 1).
  6. Collapse safety (Pitfall 9): at 320×640, a bounded Tab walk from the document start reaches the link named "Lem Reader" as document.activeElement (keyboard/SR reachability while visually collapsed).

  Commit: feat(15-02): shell-nav e2e + remove in-page Highlights entry (shell link replaces it).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/shell-nav.spec.ts tests/e2e/review-panel/route-entry.spec.ts tests/e2e/chrome/back-nav.spec.ts --project=chromium && ! rg -q "article-export-highlights" src/ingestion/library/LibraryView.tsx</automated>
  </verify>
  <acceptance_criteria>
  - src/ingestion/library/LibraryView.tsx no longer contains the article-export-highlights button (region-scoped grep on LibraryView.tsx returns 0 — the class legitimately remains in ReviewView's export button, hence the file scope)
  - shell-nav.spec.ts contains all six test groups; all pass on chromium; plain test() blocks inherit the 3-engine matrix
  - route-entry (a) drives the shell link; back-nav's library→highlights flow drives the shell link
  - verify command chain exits 0
  </acceptance_criteria>
  <done>NAV-01/NAV-02/NAV-05 proven in a real browser: persistent two-destination shell, brand home, gated reader controls, 320px fit, sole Highlights entry via the shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header chrome → router | Nav hrefs are the only new navigation-initiating surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | brand/nav hrefs | mitigate | Constant hrefs (`#/`, `#/highlights`) — same-origin by construction (T-14-04); no attacker-influenced URL reaches navigation |
| T-15-05 | Information disclosure | collapsed wordmark | mitigate | Visual collapse uses the shipped .visually-hidden clip (stays in a11y tree + tab order); e2e asserts keyboard reachability |
| T-15-06 | Tampering | header gating | accept | Conditional rendering only — no data crosses a trust boundary; count-zero + contrast-leg e2e pins the gating |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None — zero package installs this phase |
</threat_model>

<verification>
- Task-level: chromium quick gates (above) per VALIDATION sampling rate.
- Wave-level: 3-engine run of shell-nav + route-entry + back-nav happens at Plan 15-04's matrix task.
- Regression targets byte-stable: h1 "Saved articles", .library-list rows, gear aria-label "Reading settings", 48px header geometry (banner offsets, paginated calc, headerPx = 48 all depend on it).
</verification>

<success_criteria>
- Shell identical in Library, Highlights, Reader (D15-02); exactly two destinations.
- Brand → #/ All view, accessible name "Lem Reader", never aria-current.
- Reader header: [tags][annotations][mode][gear]; elsewhere: gear only.
- 320×640 Reader header fits one 48px row; brand keyboard reachable.
- In-page Highlights button gone; shell link is the entry (route-entry/back-nav rewritten in the same commit).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `HeaderProps.destination` | Prop | "library" \| "highlights" \| "reader", derived in App, consumed for aria-current |
| `nav.shell-nav[aria-label="Primary"]` | Component surface | two text links; D14-22 pattern promoted to shell level |
| `.shell-nav`, `.header-start` | CSS classes | app.css — mirror of .view-switcher anatomy |
| `.app-wordmark` link conversion + ≤639px collapse rules | CSS/JSX change | brand link rides the global a rule |
| tests/e2e/chrome/shell-nav.spec.ts | Test file | NAV-01/02/05 + 320px geometry |

(No new packages; no Dexie changes.)

<output>
Create `.planning/phases/15-application-shell-and-destinations/15-02-SUMMARY.md` when done
</output>
