---
phase: 11-pdf-intake
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - tests/e2e/pdf-intake.spec.ts
autonomous: true
requirements: ["ING-04"]
must_haves:
  truths:
    - "A reader can upload a text-heavy PDF and it opens as a normalized article that paginates, annotates, and restores location identically to other articles (SC#1)"
    - "Scanned and multi-column PDFs refuse with calm copy in the status live region and nothing enters the library (SC#2/SC#3)"
    - "Re-uploading identical PDF bytes dedupe-refuses with the existing calm copy (D7-07)"
  artifacts:
    - path: "tests/e2e/pdf-intake.spec.ts"
      provides: "the ING-04 browser-level proof across chromium/firefox/webkit"
      min_lines: 200
  key_links:
    - from: "tests/e2e/pdf-intake.spec.ts"
      to: "src/ingestion/IngestControl.tsx"
      via: "setInputFiles on input#ingest-file + Add file button drives the real upload flow"
      pattern: "setInputFiles"
    - from: "tests/e2e/pdf-intake.spec.ts"
      to: "dev-server middleware + server/ingest.ts"
      via: "the POST travels the real /api/ingest pipeline against the dev server"
      pattern: "#/article/pdf-"
---

<objective>
Replace the Wave-0 e2e sentinel with the full pdf-intake spec: upload→read happy path with annotate + location restore, the three refusal flows, and dedupe — all against the real dev-server pipeline on all three engines.

Purpose: SC#1–SC#3 are browser-observable truths; this plan is where ING-04's end-to-end behavior is proven in real browsers (the 04-02/09-01/10-01 split precedent — the requirement's proving plan).
Output: tests/e2e/pdf-intake.spec.ts complete.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md
@.planning/phases/11-pdf-intake/11-RESEARCH.md (§Validation Architecture REQ→Test map)
@.planning/phases/11-pdf-intake/11-PATTERNS.md (§tests/e2e/pdf-intake.spec.ts analog excerpts)
@.planning/phases/11-pdf-intake/11-03-SUMMARY.md
@.planning/phases/11-pdf-intake/11-04-SUMMARY.md
@tests/e2e/library/markdown-upload.spec.ts
@tests/e2e/annotations/_fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload→read happy path + refusal + dedupe flows</name>
  <files>tests/e2e/pdf-intake.spec.ts</files>
  <read_first>tests/e2e/library/markdown-upload.spec.ts (beforeEach harness, setInputFiles + Add file click, waitForURL + badge + library-row assertion shapes), tests/e2e/pdf-intake.spec.ts (sentinel from 11-01), src/ingestion/IngestControl.tsx (status live region + exact calm copy strings)</read_first>
  <action>Keep the markdown-upload beforeEach (image route stub, IndexedDB wipe via indexedDB.deleteDatabase, goto BASE). Load fixture bytes with node:fs readFileSync resolved from tests/fixtures/pdf/. Write the tests:
  1. Happy path (SC#1): setInputFiles with name "calm-report.pdf", mimeType "application/pdf", buffer = synthetic-single-column bytes → click the Add file button → waitForURL(/#\/article\/pdf-/, 15s) → the provenance h1 shows "calm-report" (filename chain) → .source-badge filters to text "PDF" → body paragraph text from the fixture is visible → [data-block-index] elements exist (the article paginates through the standard reading surface like any other article).
  2. Scanned refusal (SC#2): upload synthetic-scanned.pdf → .ingest-control .status shows the exact scanned copy substring "looks like scanned images" → URL stays on /#/ → .library-list > li count is 0.
  3. Multi-column refusal (SC#3): upload synthetic-two-column.pdf → status shows "multiple text columns" → no navigation, 0 library rows — the reader never sees reordered text.
  4. Corrupt refusal: upload synthetic-corrupt.pdf → status shows "couldn't be opened".
  5. Dedupe (D7-07): upload calm-report.pdf → article opens; navigate back to /#/; re-upload the identical buffer → status shows "Already in your library." and the library row count stays 1.
  Use direct-child selectors for library row counts (.library-list > li — nested chips otherwise over-count) and assert BOTH the status copy and the no-side-effect state on every refusal (the markdown-upload dedupe assertion shape).</action>
  <verify>
    <automated>npx playwright test pdf-intake</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/pdf-intake.spec.ts contains waitForURL pattern for #/article/pdf-, the three refusal assertions with their exact calm-copy substrings, and the dedupe flow
    - npx playwright test pdf-intake exits 0 (all tests × chromium/firefox/webkit)
    - No test bypasses the UI (no direct API POSTs — the picker flow is exercised)
  </acceptance_criteria>
  <done>SC#1 open/read, SC#2/SC#3 calm refusals with zero library side effects, and dedupe all proven through the real upload flow on three engines.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate + location-restore identity</name>
  <files>tests/e2e/pdf-intake.spec.ts</files>
  <read_first>tests/e2e/annotations/_fixtures.ts (selection→highlight capture helpers), the annotations capture spec for the paginated-surface selection pattern, tests/e2e/persistence.spec.ts (location-restore assertion pattern)</read_first>
  <action>Add the "identically to other articles" proof (SC#1 second half):
  1. From the opened calm-report article, create one highlight using the same selection→toolbar→highlight flow the annotations specs drive (reuse their helpers; the PDF article mounts the identical ArticleView/PaginatedSurface machinery).
  2. Assert the highlight mark renders; reload the page; assert the mark is still rendered (persistence through the same Dexie path).
  3. Change the reading position (turn to a later page in paginated mode via the existing turn control, or scroll substantially in scrolling mode), reload, and assert the restored position equals the pre-reload position (page indicator / visible block offset — mirror persistence.spec.ts's restore assertions).
  Keep to ONE representative typography/viewport (the core-reading-flow precedent — OQ2 economy); the cross-engine matrix is the engine axis, not typography.</action>
  <verify>
    <automated>npx playwright test pdf-intake</automated>
  </verify>
  <acceptance_criteria>
    - The spec creates a highlight on a PDF article via the real selection flow, proves it survives reload, and proves position restoration after reload
    - npx playwright test pdf-intake exits 0 across chromium/firefox/webkit with the expanded suite
  </acceptance_criteria>
  <done>A PDF article annotates and restores location through the identical machinery as every other article — the SC#1 identity claim is browser-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e browser → dev middleware | full untrusted-input path exercised (real pipeline, real storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | silently reordered multi-column text reaching a reader | mitigate | e2e asserts the multi-column fixture REFUSES (status copy + zero library rows + no navigation) — the SC#3 hard line |
| T-11-04 | Info disclosure | refusal copy in the live region | mitigate | assertions match the exact calm strings; no stack/enum leakage observable |
| T-11-13 | Repudiation | refusal with silent side effects | mitigate | every refusal asserts BOTH copy and no-library-row/no-navigation state |
</threat_model>

<verification>
npx playwright test pdf-intake green on all engines; each refusal asserts copy + absence of side effects; happy path proves open/read/annotate/restore identity.
</verification>

<success_criteria>
ING-04's browser-observable truths proven: upload→normalized article that behaves identically (paginates, annotates, restores), honest calm refusals for scanned/multi-column/corrupt with zero side effects, dedupe on identical bytes.
</success_criteria>

## Artifacts this phase produces

- tests/e2e/pdf-intake.spec.ts — full ING-04 e2e suite (happy + refusal + dedupe + annotate/restore), replacing the Wave-0 sentinel

<output>
Create `.planning/phases/11-pdf-intake/11-05-SUMMARY.md` when done
</output>
