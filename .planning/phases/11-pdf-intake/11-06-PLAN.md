---
phase: 11-pdf-intake
plan: 06
type: execute
wave: 5
depends_on: ["11-03", "11-05"]
files_modified:
  - tests/unit/server/pdf-calibration/harness.ts
  - tests/unit/server/pdf-calibration/harness.test.ts
  - tests/unit/server/pdf-calibration/manifest.json
  - tests/unit/server/pdf-calibration/ground-truth/README.md
  - tests/unit/server/pdf-calibration/derive.spec.ts
  - tests/unit/server/pdf-calibration/replay.spec.ts
  - corpus/pdf/README.md
  - .gitignore
  - package.json
  - docs/pdf-calibration.md
autonomous: false
requirements: ["ING-04"]
user_setup:
  - service: local PDF calibration corpus
    why: "D11-04/05 — real PDFs stay local + gitignored (licensing/size); only derived evidence commits"
must_haves:
  truths:
    - "A committed manifest + committed pdf-evidence.json record the corpus, its SHA-256 integrity, and the thresholds that produced every verdict (D11-04/05)"
    - "CI replays the committed evidence against the D11-06 bar and fails honestly when the record is missing — never a silent skip"
    - "Every corpus PDF is classified correctly AND every admitted PDF reaches at least 0.90 block-level agreement before promotion (D11-06)"
    - "The full honest test suite passes in one invocation with counts + exit code recorded (04-11/09-07 discipline)"
  artifacts:
    - path: "tests/unit/server/pdf-calibration/harness.ts"
      provides: "importable calibration core: manifest/evidence loaders, SHA-256 verification, validateEvidence, deriveEvidence, refuse-empty guards"
      exports: ["validateEvidence", "deriveEvidence", "loadManifest", "writeEvidence"]
      min_lines: 150
    - path: "tests/unit/server/pdf-calibration/manifest.json"
      provides: "committed corpus manifest (file, sha256, expectedClass, producer)"
      contains: "expectedClass"
    - path: "tests/unit/server/pdf-calibration/pdf-evidence.json"
      provides: "committed derived evidence record CI replays"
      contains: "agreement"
    - path: "docs/pdf-calibration.md"
      provides: "the local derive workflow + CI replay contract"
      min_lines: 30
  key_links:
    - from: "tests/unit/server/pdf-calibration/harness.ts"
      to: "server/pdfToBlocks.ts"
      via: "derive runs the real adapter and snapshots PDF_THRESHOLDS into the evidence record"
      pattern: "PDF_THRESHOLDS"
    - from: "tests/unit/server/pdf-calibration/replay.spec.ts"
      to: "tests/unit/server/pdf-calibration/pdf-evidence.json"
      via: "vitest replay validates the committed record against the D11-06 bar inside npm run test"
      pattern: "validateEvidence"
    - from: "package.json"
      to: "tests/unit/server/pdf-calibration/derive.spec.ts"
      via: "calibrate:pdf script runs the derive path with the gating env var"
      pattern: "calibrate:pdf"
---

<objective>
Ship the SC#4b calibration instrument and run it: an importable harness core (Phase 3 fingerprint.compare.ts discipline), the human-supplied local real-PDF corpus behind a checkpoint, threshold tuning against the D11-06 promotion bar, the committed evidence record CI replays, and the honest full-suite phase gate.

Purpose: The font-size→heading and vertical-gap→paragraph thresholds (and every detection number) are calibrated against real PDFs BEFORE promotion — the explicit, auditable gate the roadmap demands.
Output: Harness + manifest + ground truth + committed pdf-evidence.json + docs + phase OUTPUT record.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md (D11-02/03/04/05/06 — locked calibration contract)
@.planning/phases/11-pdf-intake/11-RESEARCH.md (§Pattern 8 + §Open Questions 4–5)
@.planning/phases/11-pdf-intake/11-PATTERNS.md (§pdf-calibration analog — fingerprint.compare.ts excerpts)
@.planning/phases/11-pdf-intake/11-02-SUMMARY.md
@tests/e2e/calibration/fingerprint.compare.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibration harness core + wiring + docs</name>
  <files>tests/unit/server/pdf-calibration/harness.ts, tests/unit/server/pdf-calibration/harness.test.ts, corpus/pdf/README.md, .gitignore, package.json, docs/pdf-calibration.md, tests/unit/server/pdf-calibration/ground-truth/README.md</files>
  <read_first>tests/e2e/calibration/fingerprint.compare.ts (refuse-empty exit-2 guard L199-211, committed-artifact write L216-230, exit-code header contract L16-18), server/pdfToBlocks.ts (pdfToBlocks + PDF_THRESHOLDS), server/ingest.ts (ingest for the anchor-round-trip evidence field), package.json (scripts block), .gitignore</read_first>
  <behavior>
    - loadManifest rejects a manifest with duplicate files, unknown expectedClass values, or a missing schemaVersion
    - A sha256 mismatch between manifest and corpus file is detected (verifyCorpus returns the offending file)
    - validateEvidence fails when: a manifest entry lacks a result; a sha256 disagrees; an admitted verdict contradicts an expected class of scanned/multi-column; a refused verdict contradicts single-column/borderline; an admitted result's agreement is below 0.90; thresholds are absent; results are empty (refuse-empty — the fingerprint.compare.ts L205-211 precedent)
    - validateEvidence passes a hand-built valid manifest+evidence pair (pure in-memory fixtures — no corpus needed)
    - corpus/pdf/ is gitignored (git check-ignore corpus/pdf/anything.pdf succeeds)
  </behavior>
  <action>1. tests/unit/server/pdf-calibration/harness.ts — importable core (plain functions; no process.exit inside — exit semantics map to spec pass/fail): loadManifest (zod-validated { schemaVersion, entries: [{ file, sha256, expectedClass: single-column|scanned|multi-column|borderline, producer? }] }); evidence schema { schemaVersion, generatedAt, thresholds: PDF_THRESHOLDS snapshot, results: [{ file, sha256, expectedClass, verdict: "admitted" | "refused:" + reason, agreement?, anchorRoundTrip? }] }; verifyCorpus (presence + node:crypto SHA-256 vs manifest — exit-2-class refusal on missing/mismatch); validateEvidence(manifest, evidence) enforcing the D11-06 bar: classification correctness for every entry (borderline + single-column → admitted; scanned → refused pdf-scanned; multi-column → refused pdf-multi-column) AND agreement ≥ 0.90 for every admitted entry AND non-empty results; deriveEvidence(corpusDir): per PDF — read bytes, base64, call ingest({ pdf }) recording the typed verdict (or admitted + article), compute agreement for admitted entries against ground-truth labels, record anchorRoundTrip from the successful ingest (SC#4a on real PDFs), snapshot PDF_THRESHOLDS, return the evidence object; writeEvidence with the refuse-empty guard; agreement metric: align extracted blocks to labels [{ kind, level?, textPrefix }] by normalized textPrefix fuzzy match with ±1 boundary drift; agreement = matched-kind / max(labels, blocks).
  2. tests/unit/server/pdf-calibration/harness.test.ts — the pure behavior table above (in-memory fixtures only).
  3. Wiring — .gitignore gains corpus/pdf/; corpus/pdf/README.md explains the local-only convention (D11-04: never commit PDFs, never synthesize fixtures for CI); ground-truth/README.md documents the label format; package.json script calibrate:pdf = PDF_CALIBRATION_DERIVE=1 vitest run tests/unit/server/pdf-calibration/derive.spec.ts (vitest owns TS module resolution — plain-node execution cannot resolve the repo's extensionless imports; the env var gates the local-only derive).
  4. docs/pdf-calibration.md — the workflow: place 6–10 real PDFs (classes + producers per D11-05), author manifest + ground truth, run npm run calibrate:pdf, review, commit only derived evidence; CI replay contract + the accepted limitation.
  Borderline-class semantics: expected ADMITTED (pull quotes/sidebars are the D11-02 admit-side tuning pressure) — encode in validateEvidence.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdf-calibration/harness.test.ts && git check-ignore corpus/pdf/x.pdf</automated>
  </verify>
  <acceptance_criteria>
    - harness.ts exports loadManifest, verifyCorpus, validateEvidence, deriveEvidence, writeEvidence
    - harness.test.ts proves the full behavior table; npx vitest run tests/unit/server/pdf-calibration/harness.test.ts exits 0
    - git check-ignore corpus/pdf/x.pdf exits 0; package.json contains calibrate:pdf; docs/pdf-calibration.md exists with the derive + replay workflow
  </acceptance_criteria>
  <done>The calibration instrument exists, is unit-proven with in-memory fixtures, the corpus dir is gitignored, and the workflow is documented — ready for the real corpus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Supply the local real-PDF corpus (D11-04/05)</name>
  <what-built>The harness, corpus directory, and docs from Task 1. The corpus itself is human-supplied: real PDFs cannot be synthesized (D11-04 explicitly forbids synthetic calibration fixtures) and stay out of git.</what-built>
  <how-to-verify>
    1. Place 6–10 real PDFs into corpus/pdf/ spanning all four classes: at least 2 single-column (include one Word export and one LaTeX build), at least 1 scanned/image-only, at least 1 multi-column, at least 1 borderline (pull quotes, sidebars, or indented blockquotes). Vary producers (InDesign/print if available).
    2. Reply with a list of each filename + its class (+ producer if known). The agent then computes SHA-256 values, authors manifest.json, drafts ground-truth labels from a first extraction, and asks you to correct the labels (~30–60 min, 11-RESEARCH OQ5).
  </how-to-verify>
  <resume-signal>Type "corpus ready" plus the filename → class list (then review the drafted ground-truth labels when prompted)</resume-signal>
  <files>corpus/pdf/</files>
  <action>Blocking gate — the human places 6–10 real PDFs (4 classes, varied producers) into the gitignored corpus/pdf/ directory and reports filename → class (+ producer). Real PDFs cannot be synthesized (D11-04). After resume, the agent computes SHA-256 values, authors manifest.json, drafts ground-truth labels from a first extraction, and asks the human to correct the labels before Task 3 derives evidence.</action>
  <verify>Human reply contains "corpus ready" + the class list; ls corpus/pdf/ shows the files (blocking-human gate)</verify>
  <done>Corpus files present locally with a reported classification per file; manifest + ground-truth authoring can proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Derive evidence, tune to the D11-06 bar, honest phase gate</name>
  <files>tests/unit/server/pdf-calibration/manifest.json, tests/unit/server/pdf-calibration/ground-truth, tests/unit/server/pdf-calibration/pdf-evidence.json, tests/unit/server/pdf-calibration/derive.spec.ts, tests/unit/server/pdf-calibration/replay.spec.ts</files>
  <read_first>tests/unit/server/pdf-calibration/harness.ts, server/pdfToBlocks.ts (PDF_THRESHOLDS), 11-CONTEXT.md D11-02 (balanced tuning mandate), tests/e2e/calibration/fingerprint.compare.ts (committed-write shape), .planning/phases/10-*/10-06-OUTPUT.md or 09-07-OUTPUT.md (honest-gate record precedent)</read_first>
  <action>1. Author manifest.json from the Task 2 classification (agent-computed SHA-256; expectedClass per the user; producer recorded).
  2. derive.spec.ts — gated by describe.skipIf(process.env.PDF_CALIBRATION_DERIVE !== "1") (visible skip in normal runs — one of the documented intentional local-only skips, per the user-accepted D11-04 CI limitation): verifyCorpus, deriveEvidence, refuse-empty guard, writeEvidence to pdf-evidence.json.
  3. replay.spec.ts — always-on: loadManifest + committed pdf-evidence.json → validateEvidence; a missing record fails with the honest message "calibration requires the local corpus — see docs/pdf-calibration.md" (never a silent skip; SC#4b inside npm run test).
  4. Run npm run calibrate:pdf. Tune-to-bar loop (D11-02 balanced BOTH directions — refuse multi-column, admit single-column; the borderline class is the tuning pressure): if classification or agreement misses the bar, adjust PDF_THRESHOLDS values in server/pdfToBlocks.ts, re-run npx vitest run tests/unit/server/pdf-to-blocks.spec.ts (synthetic extreme cases MUST stay green — no threshold retune may flip them), re-derive. Iterate until every corpus PDF classifies correctly AND every admitted PDF reaches agreement ≥ 0.90 — or surface a Rule 4 blocker to the user with the per-file evidence if the bar is unreachable.
  5. Commit the derived pdf-evidence.json + manifest + ground truth (derived evidence only — never the PDFs).
  6. Honest phase gate (04-11/09-07 discipline): run the FULL npm run test in ONE invocation (no subset, no grep, no engine skip), record per-suite + per-engine pass/fail counts and the literal exit code in .planning/phases/11-pdf-intake/11-06-OUTPUT.md, including the anti-pattern attestation (executor ran the suite itself; failures recorded honestly).</action>
  <verify>
    <automated>npm run calibrate:pdf && npx vitest run tests/unit/server/pdf-calibration && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json lists every supplied corpus file with sha256 + expectedClass; pdf-evidence.json exists with thresholds snapshot + per-file results (verdict + agreement for admitted + anchorRoundTrip true)
    - Every entry satisfies the D11-06 bar (validateEvidence passes — replay.spec.ts green); derive.spec skips cleanly without the env var
    - The synthetic adapter suite is still green after any threshold tuning
    - npm run test exits 0 in one invocation; 11-06-OUTPUT.md records the counts + exit code + attestation
  </acceptance_criteria>
  <done>Calibration evidence committed and replayable; thresholds corpus-proven at the explicit promotion bar; the phase's honest full-suite gate recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local corpus files → harness | integrity of calibration inputs (tampered/substituted PDFs) |
| committed evidence → CI replay | the durable truth CI validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | corpus supply chain | mitigate | manifest SHA-256 verification before derive; evidence carries the hashes CI replays |
| T-11-14 | Repudiation | uncalibrated thresholds promoted | mitigate | D11-06 gate: classification correctness + ≥0.90 agreement enforced by validateEvidence inside npm run test |
| T-11-15 | Repudiation | silent CI skip of calibration | mitigate | replay.spec fails loudly on missing evidence with the see-docs message; derive skip is visible and documented (user-accepted limitation) |
</threat_model>

<verification>
Harness unit tests green; derive + replay specs wired; evidence committed at the D11-06 bar; synthetic suite unaffected by tuning; full honest suite recorded in 11-06-OUTPUT.md.
</verification>

<success_criteria>
SC#4 closed: real-PDF calibration validates the detection + coercion thresholds before promotion, evidence is committed and CI-replayable, and the phase exits through the honest full-suite gate.
</success_criteria>

## Artifacts this phase produces

- `loadManifest`, `verifyCorpus`, `validateEvidence`, `deriveEvidence`, `writeEvidence` — tests/unit/server/pdf-calibration/harness.ts
- Committed data: manifest.json, ground-truth/*.json labels, pdf-evidence.json (thresholds + verdicts + agreement)
- derive.spec.ts (env-gated local derive) + replay.spec.ts (always-on CI replay) + harness.test.ts
- `calibrate:pdf` npm script; corpus/pdf/ gitignored convention + README; docs/pdf-calibration.md
- .planning/phases/11-pdf-intake/11-06-OUTPUT.md — the honest phase-gate record

<output>
Create `.planning/phases/11-pdf-intake/11-06-SUMMARY.md` when done
</output>
