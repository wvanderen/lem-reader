---
phase: 11-pdf-intake
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/ingestion/IngestionClient.ts
  - src/ingestion/IngestControl.tsx
  - tests/unit/ingestion-client.test.ts
  - tests/unit/pdf-copy.test.ts
autonomous: true
requirements: ["ING-04"]
must_haves:
  truths:
    - "The file picker accepts .pdf files and the client refuses oversized PDFs before any network cost"
    - "A picked PDF is base64-encoded in the browser and posted through ingestPdf with its filename"
    - "All five PDF refusal reasons surface as calm DOC-06 copy in the .status live region — zero new chrome"
    - "The client bundle contains no unpdf/pdfjs code (server-only boundary holds)"
  artifacts:
    - path: "src/ingestion/IngestionClient.ts"
      provides: "ingestPdf client wrapper"
      contains: "export async function ingestPdf"
    - path: "src/ingestion/IngestControl.tsx"
      provides: "the .pdf upload arm, extension-aware size cap, calm copy entries"
      contains: 'accept=".md,.html,.pdf"'
    - path: "tests/unit/pdf-copy.test.ts"
      provides: "exact-string calm-copy assertions for the five PDF reasons"
      min_lines: 30
  key_links:
    - from: "src/ingestion/IngestControl.tsx"
      to: "src/ingestion/IngestionClient.ts"
      via: "the .pdf dispatch arm calls ingestPdf(base64, file.name)"
      pattern: "ingestPdf"
    - from: "src/ingestion/IngestControl.tsx"
      to: "src/ingestion/types.ts"
      via: "extension-aware cap reads PDF_MAX_BYTES from the shared constant"
      pattern: "PDF_MAX_BYTES"
---

<objective>
Extend the client upload path for PDF: ingestPdf wrapper, .pdf picker arm with binary→base64 encoding, extension-aware client size cap, and the five calm DOC-06 refusal copy entries.

Purpose: ING-04's reader-facing verb — "add a document by uploading a PDF" — plus the calm-voice surfacing of every typed refusal (D7-04 carry-forward). Parallel-safe with 11-02 (zero file overlap); e2e proof lands in 11-05.
Output: Client upload path complete and unit-tested; bundle boundary verified.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md
@.planning/phases/11-pdf-intake/11-RESEARCH.md (§Pattern 7 copy table + §Pitfalls 7, 12)
@.planning/phases/11-pdf-intake/11-PATTERNS.md (§IngestionClient.ts, §IngestControl.tsx)
@src/ingestion/IngestionClient.ts
@src/ingestion/IngestControl.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ingestPdf client wrapper</name>
  <files>src/ingestion/IngestionClient.ts, tests/unit/ingestion-client.test.ts</files>
  <read_first>src/ingestion/IngestionClient.ts (ingestMarkdown + private ingest + typed-refusal throw + ArticleSchema re-validation), tests/unit/ingestion-client.test.ts (fetch-mock patterns)</read_first>
  <behavior>
    - ingestPdf(base64, filename) POSTs JSON body exactly { pdf: base64, filename } to /api/ingest and returns the article on 200 + ok:true
    - A 400 + ok:false response throws IngestionError carrying the typed reason (e.g. pdf-scanned) verbatim
    - The success path still re-validates the article through ArticleSchema.parse (STATE-04 defense-in-depth — existing behavior, unchanged)
  </behavior>
  <action>Add export async function ingestPdf(pdfBase64: string, filename?: string): Promise&lt;IngestionSuccess&gt; delegating to the private ingest({ pdf: pdfBase64, filename }); widen the private body union type with the pdf variant. Everything below the ingest() call site (JSON parse, typed refusal throw, res.ok guard, client-side ArticleSchema.parse re-validation) is reused unchanged — do not fork it. Extend tests/unit/ingestion-client.test.ts with the two behavior cases above using the existing fetch-mock harness.</action>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/ingestion/IngestionClient.ts contains export async function ingestPdf
    - The private ingest body union includes the pdf + filename variant
    - tests/unit/ingestion-client.test.ts covers the pdf POST body and the typed-refusal throw; suite exits 0
  </acceptance_criteria>
  <done>ingestPdf posts the widened envelope through the single shared ingest path with client-side re-validation intact.</done>
</task>

<task type="auto">
  <name>Task 2: Picker arm + size cap + base64 encode + calm copy</name>
  <files>src/ingestion/IngestControl.tsx, tests/unit/pdf-copy.test.ts</files>
  <read_first>src/ingestion/IngestControl.tsx (handleFileSubmit + mapReasonToCopy + accept attribute + meta copy), src/ingestion/types.ts (PDF_MAX_BYTES), 11-RESEARCH.md §Pattern 7 (exact copy strings)</read_first>
  <action>Extend IngestControl:
  1. File input accept becomes .md,.html,.pdf; the meta paragraph copy becomes "Accepts .md, .html, and PDF".
  2. Extension-aware client cap in handleFileSubmit BEFORE any read: detect /\.pdf$/i on file.name; refuse with setStatus("error") + mapReasonToCopy("pdf-too-large") when a PDF exceeds PDF_MAX_BYTES (imported from ./types), keeping the existing 5MB + "response-too-large" branch for .md/.html unchanged (T-8-14 precedent; Pitfall 7 — refuse before any network cost).
  3. PDF dispatch arm: read via file.arrayBuffer() into a Uint8Array and base64-encode with a local bytesToBase64 helper that converts in 0x8000-element chunks (String.fromCharCode spread + btoa) to avoid call-stack limits on multi-MB files; then await ingestPdf(b64, file.name). Dispatch order: .md → ingestMarkdown, .pdf → ingestPdf, else → ingestHtml. The dedupe-refuse has() check and save/navigation flow are reused verbatim — identical bytes produce a pdf-<hash> id so re-upload collides (D7-07 + D11 id invariant).
  4. mapReasonToCopy — add the five cases with EXACT copy (11-RESEARCH Pattern 7): pdf-unreadable → "This PDF couldn't be opened — it may be corrupt or not a PDF."; pdf-encrypted → "This PDF is password-protected, so its text can't be read."; pdf-scanned → "This PDF looks like scanned images rather than text. An OCR tool could convert it first."; pdf-multi-column → "This PDF has multiple text columns, and its reading order can't be reconstructed reliably yet."; pdf-too-large → "This PDF is too long or too large to read here." Keep "server-error"/default last. Export mapReasonToCopy for unit testing.
  5. New tests/unit/pdf-copy.test.ts — assert each of the five reasons maps to its exact string above and that no entry contains jargon markers (no "Exception", no "Error:", no enum hyphenation like pdf-unreadable leaking into copy text).
  Server-only boundary check: after npm run build, no file under dist/ contains the pdfjs class-name marker string (see acceptance criteria) — unpdf must never enter the client bundle (Pitfall 12).</action>
  <verify>
    <automated>npx vitest run tests/unit/pdf-copy.test.ts tests/unit/ingestion-client.test.ts && npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - src/ingestion/IngestControl.tsx contains accept=".md,.html,.pdf", the PDF size-cap branch referencing PDF_MAX_BYTES, a bytesToBase64 helper, and the ingestPdf call
    - mapReasonToCopy is exported and returns the five exact Pattern-7 strings; tests/unit/pdf-copy.test.ts exits 0
    - npm run build exits 0 and grep -r "InvalidPDFException" dist/ has no matches (client bundle free of pdfjs code)
    - Full unit suite exits 0
  </acceptance_criteria>
  <done>Reader can pick a .pdf, it is capped/encoded/posted with filename, and every PDF refusal surfaces calm copy in the existing .status live region with zero new chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file system → browser | picked .pdf file (size, type untrusted) |
| client bundle ← /server | unpdf must not cross (bundle contamination) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | DoS | unbounded request body (base64 inflation) | mitigate | client-side PDF_MAX_BYTES refusal pre-POST + server content-length guard (11-03) + decoded re-check |
| T-11-04 | Info disclosure | jargon/payload leakage in refusal copy | mitigate | exact calm-copy strings + pdf-copy.test.ts no-jargon assertions (T-7-26 pattern extended) |
| T-11-09 | Tampering | client bundle contamination | mitigate | build gate greps dist/ for the pdfjs marker; unpdf imported only under /server |
</threat_model>

<verification>
Unit suites green (ingestion-client + pdf-copy + full unit run); npm run build green with no pdfjs marker in dist/; accept attribute and meta copy updated.
</verification>

<success_criteria>
Picker accepts .pdf; oversized PDFs refused client-side with pdf-too-large copy; binary read → chunked base64 → ingestPdf(filename); five calm copy entries shipped and exact-string tested; bundle boundary proven.
</success_criteria>

## Artifacts this phase produces

- `ingestPdf(pdfBase64, filename?)` — src/ingestion/IngestionClient.ts
- `bytesToBase64(bytes)` local helper + widened private ingest body union
- `mapReasonToCopy` exported with five new PDF cases (exact Pattern-7 strings)
- Updated accept attribute (".md,.html,.pdf") + meta copy + extension-aware cap branch — src/ingestion/IngestControl.tsx
- tests/unit/pdf-copy.test.ts

<output>
Create `.planning/phases/11-pdf-intake/11-04-SUMMARY.md` when done
</output>
