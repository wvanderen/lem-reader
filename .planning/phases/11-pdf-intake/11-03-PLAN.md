---
phase: 11-pdf-intake
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - server/ingest.ts
  - dev-server/ingest-middleware.ts
  - tests/unit/server/ingest-pdf.spec.ts
autonomous: true
requirements: ["ING-04"]
must_haves:
  truths:
    - "ingest({ pdf, filename }) returns ok:true with a pdf-<hash> id article whose blocks pass the round-trip anchor gate (SC#1/SC#4a)"
    - "Identical PDF bytes always produce the same id (D7-07 save-once dedupe substrate)"
    - "The D11-07 title chain (sane Info-title → filename minus .pdf → PDF document) and D11-09 doubled-title consume run on the fourth branch"
    - "Oversized request bodies are refused by content-length before readBody accumulates (Pitfall 7)"
  artifacts:
    - path: "server/ingest.ts"
      provides: "the fourth Stage-1 branch + PDF title chain + title consume"
      contains: 'pdf-${shortHash'
    - path: "dev-server/ingest-middleware.ts"
      provides: "pre-read body cap on POST /api/ingest"
      contains: "MAX_INGEST_BODY_BYTES"
    - path: "tests/unit/server/ingest-pdf.spec.ts"
      provides: "fourth-branch integration suite incl. round-trip gate + typed refusals"
      min_lines: 150
  key_links:
    - from: "server/ingest.ts"
      to: "server/pdfToBlocks.ts"
      via: "pdf branch calls pdfToBlocks(bytes) and destructures the five-field result with the same code as the other three branches"
      pattern: "pdfToBlocks"
    - from: "dev-server/ingest-middleware.ts"
      to: "server/limits.ts"
      via: "content-length guard imports MAX_INGEST_BODY_BYTES"
      pattern: "MAX_INGEST_BODY_BYTES"
    - from: "server/ingest.ts"
      to: "src/content/schema.ts"
      via: "stages 2+ run unchanged — ArticleSchema.parse → assertRoundTripAnchor → deriveConfidence → stamp (locked invariant)"
      pattern: "assertRoundTripAnchor"
---

<objective>
Wire PDF into the orchestrator as the fourth Stage-1 branch (pdf-<shortHash> id, D11-07 title chain, D11-09 doubled-title consume), add the pre-read content-length guard to the Vite ingest middleware, and prove the fourth branch end-to-end at the unit-integration level including the round-trip anchor gate.

Purpose: The locked invariant — every admitted PDF runs the identical stages 2+ and is a fixture to the reading engine — is proven here (SC#1/SC#4a) before the browser e2e (11-05).
Output: Four-variant orchestrator, capped middleware, integration suite.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md
@.planning/phases/11-pdf-intake/11-RESEARCH.md (§Code Examples 1–2)
@.planning/phases/11-pdf-intake/11-PATTERNS.md (§server/ingest.ts, §dev-server/ingest-middleware.ts, §ingest-pdf.spec.ts)
@.planning/phases/11-pdf-intake/11-02-SUMMARY.md
@server/ingest.ts
@dev-server/ingest-middleware.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fourth Stage-1 branch + title chain + doubled-title consume</name>
  <files>server/ingest.ts</files>
  <read_first>server/ingest.ts (Stage 0 guard L125-134, markdown branch L200-224, title chain L244-258, typed catch L323-333), server/pdfToBlocks.ts (PdfToBlocksResult + isSanePdfTitle), server/markdownToBlocks.ts (stripMarkdownExtension precedent), server/limits.ts (PDF_MAX_BYTES)</read_first>
  <behavior>
    - ingest with a base64 synthetic-single-column.pdf + filename "calm-report.pdf" → ok:true; article.id starts with "pdf-"; ingestionMeta.source is "pdf"; origin is "upload"; provenance.title is "calm-report" (filename chain — synthetic has no Info title); extractionConfidence is high or low
    - Two ingest calls with identical bytes → identical ids (content-hash, D7-07)
    - consumeDuplicatedTitle: a leading heading block whose normalized text fuzzy-matches the final title (case/whitespace-insensitive containment, either direction) is dropped; a non-matching leading heading and any non-heading first block are kept
    - Refusals serialize as typed envelopes: corrupt bytes → { ok:false, reason:"pdf-unreadable" }; scanned fixture → "pdf-scanned"; two-column fixture → "pdf-multi-column"; a decoded length over PDF_MAX_BYTES → "pdf-too-large"
  </behavior>
  <action>Extend server/ingest.ts (mirror the markdown branch exactly):
  1. Stage 0 — add hasPdf to the exactly-one-variant count (four variants; final unreachable else throws IngestionError("server-error")).
  2. pdf branch — decode Buffer.from(b64, "base64"); if bytes.byteLength > PDF_MAX_BYTES throw IngestionError("pdf-too-large") (decoded re-check — third layer after client + content-length); finalUrl = undefined; call pdfToBlocks over a Uint8Array view of the bytes; destructure the identical five fields; id = pdf-${shortHash(b64)} (content-hash on the base64 channel — mirrors md-<hash> D8-18); source = "pdf" (widen the closure type annotation to include it); origin = "upload"; fetchedAt = undefined; sourceBytes = b64; stash pdfFilenameHint beside markdownFilenameHint.
  3. D11-07 title chain — for the pdf path: provenancePartial.title (the adapter only sets it when isSanePdfTitle passed) ?? (pdfFilenameHint ? stripPdfExtension(pdfFilenameHint) : "PDF document"). Add local pure helper stripPdfExtension(filename) stripping a case-insensitive trailing .pdf (planner placement per 11-PATTERNS L129: the chain is orchestrator-owned, so the helper lives here, mirroring stripMarkdownExtension's shape). Do not change the url/paste/markdown chains.
  4. D11-09 consume — export pure helper consumeDuplicatedTitle(blocks, title): if the FIRST block is kind "heading" and its text normalized (lowercase, whitespace-collapsed) is contained in — or contains — the normalized title, drop it; otherwise return blocks unchanged. Apply only on the pdf path, after the title resolves, before assembling the article object (one-h1-per-page discipline — the provenance header renders the title; the body never repeats it).
  5. Stages 2+ untouched — ArticleSchema.parse → assertRoundTripAnchor → deriveConfidence → stamp run identically on all four branches (locked invariant; do not fork). IngestionError catch already serializes pdf-* reasons verbatim.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/ingest-pdf.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/ingest.ts contains hasPdf, pdfToBlocks, pdf-${shortHash, stripPdfExtension, consumeDuplicatedTitle, and pdfFilenameHint
    - consumeDuplicatedTitle is exported; the pdf-too-large decoded re-check references PDF_MAX_BYTES
    - Behavior lines above pass (assertions live in Task 2's suite; Task 1 may run them incrementally)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Fourth branch produces pdf-<hash>-id articles through the unchanged stages 2+, with the D11-07 chain and D11-09 consume in the orchestrator where the final title is known.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Middleware body cap + integration suite</name>
  <files>dev-server/ingest-middleware.ts, tests/unit/server/ingest-pdf.spec.ts</files>
  <read_first>dev-server/ingest-middleware.ts (readBody + route match), server/limits.ts (MAX_INGEST_BODY_BYTES), tests/unit/server/markdown-to-blocks.spec.ts (round-trip describe L421-463 pattern), tests/unit/server/ingest-pdf.spec.ts (sentinel left by 11-01)</read_first>
  <behavior>
    - A POST /api/ingest request whose content-length header exceeds MAX_INGEST_BODY_BYTES gets statusCode 413 + JSON body { ok:false, reason:"pdf-too-large" } WITHOUT the body being read
    - A chunked request (no content-length) whose accumulated raw body exceeds the cap gets the same 413 envelope after readBody
    - Full-pipeline happy path: ingest(pdf single-column fixture, filename) → ok:true; re-running assertRoundTripAnchor(article) on the returned article does not throw (SC#4a proof at integration level)
    - Typed refusal envelopes: pdf-unreadable / pdf-scanned / pdf-multi-column from the synthetic fixtures; id stability across two identical calls
  </behavior>
  <action>1. dev-server/ingest-middleware.ts — before readBody, read the content-length header and refuse with 413 + the typed pdf-too-large JSON envelope when it exceeds MAX_INGEST_BODY_BYTES (import from ../server/limits; Pitfall 7 — readBody accumulates unbounded otherwise). After readBody, re-check raw.length against the same cap for chunked requests. Nothing else in the route-match/response pattern changes — base64-in-JSON keeps readBody + handleIngestBody byte-identical (locked transport decision).
  2. tests/unit/server/ingest-pdf.spec.ts — replace the 11-01 sentinel with the full integration suite: the middleware cap tests (drive viteIngestMiddleware against a fake ViteDevServer whose middlewares.use captures the handler; craft req as a node:stream Readable with headers, and a res stub capturing statusCode/setHeader/end — assert both cap paths); the ingest() happy path + round-trip re-proof + typed refusals + id stability from Task 1's behavior list; a consumeDuplicatedTitle unit describe (match dropped / containment dropped / non-match kept / non-heading kept) and a stripPdfExtension describe ("Report.pdf" → "Report", "report.PDF" → "report", no-extension passthrough).</action>
  <verify>
    <automated>npx vitest run tests/unit/server/ingest-pdf.spec.ts && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - dev-server/ingest-middleware.ts references MAX_INGEST_BODY_BYTES and returns 413 with the pdf-too-large envelope on both cap paths
    - tests/unit/server/ingest-pdf.spec.ts covers: happy path incl. assertRoundTripAnchor non-throw, the three typed refusals, id stability, both middleware cap paths, consumeDuplicatedTitle + stripPdfExtension units
    - npx vitest run tests/unit/server/ingest-pdf.spec.ts exits 0; the full unit suite exits 0
  </acceptance_criteria>
  <done>Transport capped pre-read; fourth branch proven at integration level with the anchor gate, typed refusals, and id stability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP POST /api/ingest → middleware | untrusted body size (base64 bombs) |
| orchestrator → library | only round-trip-validated articles pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | DoS | unbounded body accumulation in readBody | mitigate | content-length pre-check + post-read raw-length re-check, both refusing 413 pdf-too-large before pipeline work |
| T-11-10 | Tampering | id drift across re-extraction | mitigate | id = pdf-shortHash(base64) — deterministic on identical bytes; unit-asserted stability |
| T-11-11 | Repudiation | generic Error escapes the pdf path | mitigate | existing typed catch serializes IngestionError reasons verbatim; timeout folds to server-error with descriptive message |
| T-11-12 | Tampering | doubled-title / heading-structure corruption | mitigate | consumeDuplicatedTitle fuzzy-match unit table (D11-09) |
</threat_model>

<verification>
npx vitest run tests/unit/server/ingest-pdf.spec.ts green; full unit suite green; npx tsc --noEmit green; middleware returns 413 typed envelopes on both cap paths.
</verification>

<success_criteria>
Four-variant orchestrator with pdf-<hash> ids, D11-07 chain, D11-09 consume; capped middleware; integration proof that an admitted PDF round-trips the anchor gate and every refusal class serializes typed.
</success_criteria>

## Artifacts this phase produces

- `hasPdf` fourth Stage-1 branch + `pdfFilenameHint` closure — server/ingest.ts
- `stripPdfExtension(filename)` and `consumeDuplicatedTitle(blocks, title)` exported pure helpers — server/ingest.ts
- pdf-<shortHash> id derivation + decoded PDF_MAX_BYTES re-check on the pdf path
- Content-length + raw-length body caps (413 pdf-too-large) — dev-server/ingest-middleware.ts
- tests/unit/server/ingest-pdf.spec.ts full integration suite (replaces the 11-01 sentinel)

<output>
Create `.planning/phases/11-pdf-intake/11-03-SUMMARY.md` when done
</output>
