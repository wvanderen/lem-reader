---
phase: 11-pdf-intake
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - server/pdfToBlocks.ts
  - tests/unit/server/pdf-to-blocks.spec.ts
autonomous: true
requirements: ["ING-04"]
must_haves:
  truths:
    - "A text-heavy single-column PDF extracts into the five-field adapter result with paragraphs, headings, and no rejection"
    - "A scanned PDF refuses with typed reason pdf-scanned and a multi-column PDF refuses with pdf-multi-column — never silently reordered (D11-01/SC#2/SC#3)"
    - "Encrypted or corrupt PDFs refuse via pdf-encrypted / pdf-unreadable; oversized page counts refuse via pdf-too-large"
    - "Extraction runs under one proxy with pixel/page caps checked before extraction and a timeout race (resource invariants)"
  artifacts:
    - path: "server/pdfToBlocks.ts"
      provides: "the unpdf Stage-1 adapter (detection + refusal + block assembly)"
      exports: ["pdfToBlocks", "PdfToBlocksResult", "PDF_THRESHOLDS", "classifyDocument", "mapPdfjsError", "isSanePdfTitle", "assertPageCap"]
    - path: "tests/unit/server/pdf-to-blocks.spec.ts"
      provides: "adapter unit suite over synthetic fixtures"
      min_lines: 200
  key_links:
    - from: "server/pdfToBlocks.ts"
      to: "src/content/schema.ts"
      via: "produces Block[] (9 kinds) — types-only import, same as markdownToBlocks"
      pattern: 'import type { Block'
    - from: "server/ingest.ts (11-03)"
      to: "server/pdfToBlocks.ts"
      via: "fourth Stage-1 branch destructures the identical five-field result"
      pattern: "pdfToBlocks"
    - from: "tests/unit/server/pdf-calibration/harness (11-06)"
      to: "server/pdfToBlocks.ts"
      via: "imports PDF_THRESHOLDS so evidence records the numbers that produced it"
      pattern: "PDF_THRESHOLDS"
---

<objective>
Build server/pdfToBlocks.ts — the fourth intake adapter (markdownToBlocks sibling): one getDocumentProxy reused across extractTextItems + getMeta + getOutline, scanned and multi-column detection with page-weighted majority verdicts BEFORE assembly, outline-first heading coercion with font-size fallback, paragraph assembly with hyphenation joins, and typed IngestionError refusals for every honest-failure class.

Purpose: ING-04's extraction + normalization core and every SC#2/SC#3 refusal decision live here; the calibration harness (11-06) tunes this module's PDF_THRESHOLDS.
Output: The complete adapter plus its unit suite over the synthetic fixtures from 11-01.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md
@.planning/phases/11-pdf-intake/11-RESEARCH.md
@.planning/phases/11-pdf-intake/11-PATTERNS.md
@.planning/phases/11-pdf-intake/11-01-SUMMARY.md
@server/markdownToBlocks.ts
@server/errors.ts
@server/limits.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Proxy lifecycle + detection + typed refusals</name>
  <files>server/pdfToBlocks.ts, tests/unit/server/pdf-to-blocks.spec.ts</files>
  <read_first>server/markdownToBlocks.ts (banner + result-contract + server-only import discipline), server/errors.ts (IngestionError), server/limits.ts (caps), node_modules/unpdf/dist/types/src/display/api.d.ts (PDFDocumentProxy: getOutline, getDestination, getPageIndex, maxImageSize, loadingTask), node_modules/unpdf README or dist types for extractTextItems/getMeta signatures, 11-RESEARCH.md Patterns 2–4 + Pitfalls 1, 4, 6, 9</read_first>
  <behavior>
    - synthetic-corrupt.pdf → pdfToBlocks rejects with IngestionError reason "pdf-unreadable"
    - mapPdfjsError with a fake { name: "PasswordException" } error returns reason "pdf-encrypted"; { name: "InvalidPDFException" } → "pdf-unreadable"; unknown names → null
    - assertPageCap(501) throws IngestionError "pdf-too-large"; assertPageCap(500) does not throw
    - synthetic-scanned.pdf → rejects "pdf-scanned"; synthetic-two-column.pdf → rejects "pdf-multi-column"; synthetic-single-column.pdf + synthetic-outline.pdf → do NOT reject (resolve)
    - classifyDocument over hand-built item arrays: 2-of-3 columnar text-bearing pages → multiColumn true (page-weighted majority, D11-03); 1-of-3 → false; 2-of-3 near-empty pages → scanned true; 1-of-3 → false; a page with zero text items is not text-bearing and cannot be columnar
  </behavior>
  <action>Create server/pdfToBlocks.ts mirroring the markdownToBlocks banner comment (unpdf is the server-only dependency; PDFs carry no HTML so the DOMPurify path does not apply — D8-16 precedent; only Block/InlineRun types cross from src, erased by tsc). Implement:
  1. withPdfDocument(bytes, op) — getDocumentProxy with maxImageSize: MAX_IMAGE_PIXELS; check pdf.numPages against PDF_MAX_PAGES via exported assertPageCap BEFORE any extract call (Pitfall 9 — extractTextItems processes all pages in one call); run op inside Promise.race against a timer that rejects with IngestionError("server-error") carrying a descriptive timeout message (planner fold of pdf-timeout per 11-RESEARCH Pattern 7); finally destroy the proxy via pdf.loadingTask.destroy().
  2. Error mapping — export mapPdfjsError(err): match err.name "PasswordException" → IngestionError "pdf-encrypted"; "InvalidPDFException" → "pdf-unreadable"; otherwise null (name-string matching is the stable pattern; the classes are not re-exported from the unpdf index).
  3. Detection — export classifyDocument(pages: per-page text-item arrays): per page compute text-bearing (≥ PDF_THRESHOLDS.scannedItemFloor items with non-whitespace str AND ≥ scannedCharFloor non-whitespace chars) and near-empty (< nearEmptyItemFloor items or < nearEmptyCharFloor chars); per text-bearing page run band-coverage gutter analysis per 11-RESEARCH Pattern 3: bin items into y-bands with tolerance bandYToleranceRatio × modal line height; exclude bands spanning > wideBandRatio of the page's item-bbox text extent (spanning headers/pull quotes); exclude top/bottom marginBandRatio bands (page furniture, Pitfall 4); each narrow band casts one binary vote per x-bin covered; a gutter is a contiguous x-run where votes < gutterVoteRatio × narrow-band count with width ≥ minGutterEm × modal fontSize; a page is columnar when ≥ 2 separated regions each hold ≥ colTextShare of page text. Document verdicts page-weighted (D11-03): multiColumn when columnar pages > columnarMajorityRatio of text-bearing pages; scanned when near-empty pages > scannedMajorityRatio of ALL pages. All numbers live in the exported PDF_THRESHOLDS const object (wideBandRatio 0.55, gutterVoteRatio 0.2, minGutterEm 1, colTextShare 0.15, columnarMajorityRatio 0.5, scannedItemFloor 8, scannedCharFloor 40, nearEmptyItemFloor 3, nearEmptyCharFloor 15, scannedMajorityRatio 0.5 — corpus-calibrated starting values per D11-02).
  4. pdfToBlocks orchestration — one proxy runs getMeta + extractTextItems + getOutline; map errors via mapPdfjsError before anything else; then scanned verdict → throw IngestionError("pdf-scanned"); then multi-column verdict → throw IngestionError("pdf-multi-column"); PDF user-space y increases UP — sort lines y-DESCENDING for reading order (Pitfall 1).
  This task may leave assembly as a minimal placeholder returning the typed stub only if tests demand it; Task 2 completes it.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdf-to-blocks.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/pdfToBlocks.ts exports pdfToBlocks, PdfToBlocksResult, PDF_THRESHOLDS, classifyDocument, mapPdfjsError, assertPageCap, isSanePdfTitle
    - server/pdfToBlocks.ts contains getDocumentProxy, extractTextItems, getOutline, loadingTask
    - Unit suite covers every behavior line above; npx vitest run tests/unit/server/pdf-to-blocks.spec.ts exits 0
    - No import of dompurify in server/pdfToBlocks.ts (grep for the dompurify import specifier returns nothing in this file)
  </acceptance_criteria>
  <done>One-proxy lifecycle with caps + timeout race; scanned/multi-column page-weighted detection; corrupt/encrypted/oversize typed refusals — all unit-proven over synthetic fixtures and hand-built item arrays.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block assembly + headings + title sanity + adapter contract</name>
  <files>server/pdfToBlocks.ts, tests/unit/server/pdf-to-blocks.spec.ts</files>
  <read_first>server/pdfToBlocks.ts (as left by Task 1), server/markdownToBlocks.ts (MarkdownToBlocksResult + UnsupportedBlock emission + isReaderable heuristic), 11-RESEARCH.md Patterns 1, 5, 6 + Pitfalls 10, 11, 3, 11-PATTERNS.md §pdfToBlocks excerpt blocks</read_first>
  <behavior>
    - pdfToBlocks(synthetic-single-column.pdf) resolves with all five result fields; footnotes is empty; lang is "en"; isReaderable is true; blocks.length ≥ 3; every block parses against the Block schema
    - 18pt bold groups become kind "heading" blocks (font-size fallback: dominant fontSize ≥ body × headingFontRatio AND text under headingMaxWords); 12pt groups become kind "paragraph"
    - A paragraph split across lines joins with a space; a line ending in a hyphen followed by a lowercase-starting line dehyphenates (no hyphen, no space); an x-gap between items over itemGapRatio × fontSize inserts a space
    - synthetic-outline.pdf: outline destinations coerce their target blocks to heading levels clamp(depth + 2, 2, 6) — top-level bookmark → level 2; url-bearing and null-dest entries are skipped; both string-named and explicit-array dest shapes resolve (Pitfall 10)
    - The page-3 large vertical gap yields one kind "unsupported" block with a plainDescription mentioning no extractable text
    - isSanePdfTitle: empty/whitespace → false; "untitled"/"Microsoft Word - x.docx"/".docx-suffixed"/hex-or-UUID strings/over-200-char → false; "Annual Report 2025" → true; provenancePartial.title is undefined for the synthetic fixtures (no Info titles) and equals the Info title when sane
    - isReaderable false when fewer than 3 blocks or zero text-bearing pages
  </behavior>
  <action>Complete the adapter:
  1. Result contract — export interface PdfToBlocksResult exactly matching MarkdownToBlocksResult's five fields (blocks, footnotes, lang, provenancePartial, isReaderable) so server/ingest.ts destructures all four branches with the same code (locked invariant). footnotes always empty (PDF footnotes are body text in Phase 11); lang "en"; isReaderable = at least 3 blocks AND at least 1 text-bearing page.
  2. Line/paragraph assembly (Pattern 6) — within a page sort lines y-descending; new paragraph when baseline delta exceeds modal line delta × paragraphGapRatio (1.35) OR on a fontSize/fontFamily regime change; join lines per the hyphen/space behavior above. These joins are adapter-internal assembly like htmlToBlocks' DOM walk — the shared normalizer (Pitfall 2) is NOT forked or touched; only if a rule must later apply to already-normalized text does normalizeText change with a DOC-04 bump.
  3. Heading detection (D11-08) — outline-first: flatten getOutline() items with depth; skip url-bearing/null dest; resolve string dests via pdf.getDestination and arrays as-is; pdf.getPageIndex(dest[0]) gives the 0-based page; coerce the extracted block on that page whose top y is within outlineYToleranceLines (1.5) line-heights of the dest XYZ y to level clamp(depth + 2, 2, 6). Fallback + gap-filler: font-size rule (dominant fontSize ≥ char-count-weighted modal body fontSize × headingFontRatio 1.15 AND under headingMaxWords 10 words).
  4. Non-text regions — a near-empty page inside an admitted document emits ONE kind "unsupported" block (originalKind "non-text-region", plainDescription like "A page or region with no extractable text — likely a figure, chart, or table."); an intra-page vertical gap over figureGapLines × modal line height emits one between paragraphs. Figure-heavy PDFs may then honestly derive low confidence downstream (unsupportedRatio rule — ING-06 working, not a bug).
  5. Title sanity (D11-07 helper half) — export PDF_TITLE_GARBAGE pattern list + isSanePdfTitle(t): trimmed length 2–200 and no garbage pattern matches; set provenancePartial.title from getMeta info.Title (PascalCase "Title" key) only when sane. The adapter stays filename-agnostic (D8-17 mirror); the filename → neutral fallbacks live in the orchestrator (11-03).
  Do NOT sort items by content-stream array order and call it reading order (Pitfall 2 of this phase); do not treat hasEOL as authoritative paragraph structure (hint only).</action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdf-to-blocks.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/pdfToBlocks.ts contains the PdfToBlocksResult interface with all five fields and exports isSanePdfTitle
    - Unit suite proves every behavior line above (headings via font-size AND via outline destinations, hyphen join, gap space, unsupported emission, title sanity table, result-shape contract)
    - npx vitest run tests/unit/server/pdf-to-blocks.spec.ts exits 0 with the full suite (Task 1 + Task 2 describes)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>pdfToBlocks produces a schema-valid Block tree with outline-first headings, dehyphenated paragraphs, honest unsupported blocks, and a sane Info-title channel — the five-field contract the orchestrator destructures identically to markdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded PDF bytes → pdfjs parser | untrusted binary input (bombs, encrypted payloads, hostile streams) |
| extracted text → Block JSON | doc model is the security boundary — output is inert data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | DoS | decompression/image bombs in pdfjs | mitigate | maxImageSize pixels cap + numPages cap before extraction + timeout race + proxy destroy (withPdfDocument) |
| T-11-03 | Tampering | silently reordered multi-column text | mitigate | band-coverage detection + page-weighted refusal BEFORE assembly (D11-01); e2e refusal proof lands in 11-05 |
| T-11-06 | Tampering | encrypted/DRM payloads | mitigate | PasswordException name-match → typed pdf-encrypted refusal; never decrypt |
| T-11-08 | Tampering | y-axis / reading-order inversion | mitigate | y-descending sort + unit tests over synthetic two-column refusal (Pitfall 1) |
</threat_model>

<verification>
npx vitest run tests/unit/server/pdf-to-blocks.spec.ts green (detection + assembly + contract describes); npx tsc --noEmit green; no dompurify import in the adapter; unpdf imported only inside /server.
</verification>

<success_criteria>
Adapter refuses scanned/multi-column/encrypted/corrupt/oversized PDFs with typed reasons; admits single-column synthetic PDFs into a schema-valid Block tree with outline-first headings, dehyphenated paragraphs, and honest unsupported blocks; PDF_THRESHOLDS exported for calibration.
</success_criteria>

## Artifacts this phase produces

- `pdfToBlocks(pdfBytes: Uint8Array): Promise<PdfToBlocksResult>` — server/pdfToBlocks.ts
- `PdfToBlocksResult` interface (blocks, footnotes, lang, provenancePartial, isReaderable)
- `PDF_THRESHOLDS` const (all detection/assembly starting values — the 11-06 calibration surface)
- `classifyDocument`, `mapPdfjsError`, `assertPageCap`, `isSanePdfTitle`, `PDF_TITLE_GARBAGE` exports
- Internal helpers: withPdfDocument proxy lifecycle, band-coverage gutter analysis, outline destination resolution

<output>
Create `.planning/phases/11-pdf-intake/11-02-SUMMARY.md` when done
</output>
