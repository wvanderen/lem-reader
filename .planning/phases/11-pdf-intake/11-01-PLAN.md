---
phase: 11-pdf-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/ingestion/types.ts
  - src/content/schema.ts
  - src/ingestion/library/SourceBadge.tsx
  - server/limits.ts
  - tests/unit/ingestion-schema.test.ts
  - tests/fixtures/pdf/generate-synthetic-pdfs.ts
  - tests/fixtures/pdf/README.md
  - tests/unit/server/pdf-to-blocks.spec.ts
  - tests/unit/server/ingest-pdf.spec.ts
  - tests/e2e/pdf-intake.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "unpdf is installed at the human-approved exact pin and no module under src/ imports it"
    - "IngestionRequestSchema accepts a pdf base64 variant with optional filename and rejects non-base64 payloads"
    - "IngestionFailureReasonEnum catalogs 16 reasons including the five PDF members and ArticleSourceSchema includes pdf"
    - "Five synthetic PDF fixtures load as valid PDF-prefixed bytes in both vitest projects and the e2e sentinel passes on three engines"
  artifacts:
    - path: "src/ingestion/types.ts"
      provides: "pdf request variant, five PDF failure reasons, shared PDF_MAX_BYTES constant"
      contains: "pdf: z.string().base64()"
    - path: "server/limits.ts"
      provides: "server-side PDF resource caps (pixels, pages, timeout, body)"
      contains: "MAX_IMAGE_PIXELS"
    - path: "tests/fixtures/pdf/generate-synthetic-pdfs.ts"
      provides: "deterministic synthetic PDF fixture generator"
      min_lines: 80
  key_links:
    - from: "src/ingestion/types.ts"
      to: "server/limits.ts"
      via: "limits re-exports PDF_MAX_BYTES so client and server share one cap constant"
      pattern: "PDF_MAX_BYTES"
    - from: "src/content/schema.ts"
      to: "src/ingestion/library/SourceBadge.tsx"
      via: "ArticleSourceSchema pdf member must be handled by the exhaustive badgeLabel switch"
      pattern: 'case "pdf"'
---

<objective>
Foundation for PDF intake: human-gated unpdf install (SUS legitimacy audit), additive schema/enum widening, PDF resource-limit constants, and committable synthetic PDF fixtures with Wave-0 test sentinels.

Purpose: Every later plan (adapter, orchestrator, client, e2e, calibration) compiles against these widened contracts; the synthetic fixtures make the adapter testable in CI without touching the D11-04 local-only calibration corpus.
Output: Installed pinned unpdf dependency, widened Zod schemas, PDF caps in server/limits.ts, five synthetic fixtures + generator, three Wave-0 sentinel specs.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-CONTEXT.md
@.planning/phases/11-pdf-intake/11-RESEARCH.md
@.planning/phases/11-pdf-intake/11-PATTERNS.md
@server/limits.ts
@src/ingestion/types.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve unpdf package legitimacy + exact pin (T-11-SC)</name>
  <what-built>Nothing yet — this is the package-legitimacy gate required before any install. The 11-RESEARCH.md audit rates unpdf SUS ("too-new": latest publish 2026-08-13 was three days before research). Mitigating signals already verified: 1.85M weekly downloads, unjs org, MIT, no postinstall script, zero runtime dependencies, types shipped. This checkpoint also resolves Open Question 1: pin 1.8.0 (STACK.md L63 lock) vs 1.8.1 (current; verified API-neutral diff — a Math.sumPrecise polyfill refactor).</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/unpdf — confirm publisher unjs, MIT license, download volume, and that the version you approve shows no install scripts.
    2. Optionally skim https://github.com/unjs/unpdf (active repo, recent releases).
    3. Choose the pin: 1.8.0 (honor the STACK.md lock literally) or 1.8.1 (current patch; diff verified trivial).
  </how-to-verify>
  <resume-signal>Type "approved 1.8.0" or "approved 1.8.1" (or describe concerns)</resume-signal>
  <files>package.json</files>
  <action>Blocking gate before the Task 2 install — no file changes in this task. The executor presents the legitimacy evidence and pin choice; on approval, records the chosen pin for Task 2's npm install --save-exact call.</action>
  <verify>Human reply contains the approved pin (blocking-human gate)</verify>
  <done>Pin approved and recorded; install may proceed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Install pinned unpdf + widen schemas, limits, badge</name>
  <files>package.json, src/ingestion/types.ts, src/content/schema.ts, src/ingestion/library/SourceBadge.tsx, server/limits.ts, tests/unit/ingestion-schema.test.ts</files>
  <read_first>server/limits.ts, src/ingestion/types.ts, src/content/schema.ts (ArticleSourceSchema block near L204-217), src/ingestion/library/SourceBadge.tsx, tests/unit/ingestion-schema.test.ts, 11-PATTERNS.md sections "server/limits.ts", "src/ingestion/types.ts", "src/content/schema.ts", "SourceBadge.tsx"</read_first>
  <action>Install with an exact pin (no caret): npm install --save-exact unpdf@&lt;approved-pin&gt;. Then widen additively, never editing shipped members (D-carrying-forward + Pitfall 9 discipline):
  1. src/ingestion/types.ts — add a fourth union member to IngestionRequestSchema: an object with pdf as a base64-validated min(1) string and filename as an optional string, commented "Phase 11 ING-04 + D11 — PDF upload, base64-in-JSON (keeps middleware body path byte-identical)". Add five members to IngestionFailureReasonEnum after "round-trip-anchor-failed": pdf-unreadable, pdf-encrypted, pdf-scanned, pdf-multi-column, pdf-too-large (Pattern 7 of 11-RESEARCH.md; "already-in-library" and "server-error" stay last). Add exported constant PDF_MAX_BYTES = 10 * 1024 * 1024 with a comment noting it is the decoded-byte cap shared by client picker, middleware content-length guard, and orchestrator re-check, cross-referencing server/limits.ts (the /src→/server import direction is forbidden, so the shared constant lives here and limits re-exports it).
  2. server/limits.ts — import PDF_MAX_BYTES from ../src/ingestion/types and re-export it; add PDF_MAX_PAGES = 500; PDF_EXTRACTION_TIMEOUT_MS = 30_000 (mirrors REQUEST_TIMEOUT_MS; planner resolution of 11-RESEARCH Open Question 2); MAX_IMAGE_PIXELS = 16_777_216 with a provenance comment stating it is TOTAL PIXELS (width×height ≈ 16 megapixels), NOT bytes — correcting the ARCHITECTURE.md L781 "16 MB" label per 11-RESEARCH State of the Art; MAX_INGEST_BODY_BYTES = Math.ceil((PDF_MAX_BYTES * 4) / 3) + 2048 (base64 ~33% inflation + JSON overhead, Pitfall 7).
  3. src/content/schema.ts — append "pdf" to ArticleSourceSchema with comment "Phase 11 — ING-04 (.pdf upload via pdfToBlocks)".
  4. src/ingestion/library/SourceBadge.tsx — add case "pdf" returning "PDF" (the no-default switch forces it once the enum widens).
  5. tests/unit/ingestion-schema.test.ts — extend the two exact-array assertions: ArticleSourceSchema.options equals the six members ending with pdf; IngestionFailureReasonEnum.options has length 16 in the exact new order. Add cases: parses a valid base64 pdf payload with filename; rejects a pdf value containing non-base64 characters.
  Server-only boundary: no module under src/ may import the unpdf package (client bundle stays clean, Pitfall 12).</action>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains an unpdf dependency pinned to the approved version with no caret (matches "unpdf": "X.Y.Z")
    - src/ingestion/types.ts contains pdf: z.string().base64() and PDF_MAX_BYTES = 10 * 1024 * 1024
    - server/limits.ts contains MAX_IMAGE_PIXELS = 16_777_216, PDF_MAX_PAGES = 500, PDF_EXTRACTION_TIMEOUT_MS = 30_000, MAX_INGEST_BODY_BYTES
    - src/content/schema.ts ArticleSourceSchema includes "pdf"; SourceBadge.tsx contains case "pdf": return "PDF"
    - tests/unit/ingestion-schema.test.ts references "pdf-scanned" and asserts the reason enum has 16 members
    - npx vitest run tests/unit/ingestion-schema.test.ts exits 0; npx tsc --noEmit exits 0
    - grep -rn 'from "unpdf"' src/ returns no matches
  </acceptance_criteria>
  <done>unpdf installed at the approved exact pin; all widenings shipped additively; schema unit suite and type-check green; client tree free of unpdf imports.</done>
</task>

<task type="auto">
  <name>Task 3: Synthetic PDF fixtures + Wave-0 sentinels</name>
  <files>tests/fixtures/pdf/generate-synthetic-pdfs.ts, tests/fixtures/pdf/README.md, tests/fixtures/pdf/synthetic-single-column.pdf, tests/fixtures/pdf/synthetic-two-column.pdf, tests/fixtures/pdf/synthetic-scanned.pdf, tests/fixtures/pdf/synthetic-outline.pdf, tests/fixtures/pdf/synthetic-corrupt.pdf, tests/unit/server/pdf-to-blocks.spec.ts, tests/unit/server/ingest-pdf.spec.ts, tests/e2e/pdf-intake.spec.ts</files>
  <read_first>tests/e2e/library/markdown-upload.spec.ts (beforeEach harness pattern), tests/unit/server/markdown-to-blocks.spec.ts (suite skeleton), 11-RESEARCH.md §Validation Architecture "Synthetic unit fixtures", 11-PATTERNS.md §No Analog Found row "Synthetic tiny-PDF fixture generation"</read_first>
  <action>Create tests/fixtures/pdf/generate-synthetic-pdfs.ts — a Node script (only node: builtins, zero repo imports, type-stripping-safe TS) that programmatically emits minimal valid PDFs (header %PDF-1.4, object table with computed xref offsets, /MediaBox, Helvetica F1 + Helvetica-Bold F2 font resources, BT/Tf/Td/Tj/ET text operators). It writes five fixtures, each under ~10KB, deterministically (idempotent re-runs):
  1. synthetic-single-column.pdf — 3 pages; page 1 has one 18pt bold title-size line plus 12pt body paragraphs; page 2 an 18pt bold heading + paragraphs; page 3 two paragraph groups separated by a ~200pt vertical gap (figure stand-in); include at least one hyphen-at-line-end split word so Plan 11-02 can test dehyphenation.
  2. synthetic-two-column.pdf — 2 pages; every page lays text lines in two x-ranges (approximately 60–280 and 312–532 points) with each column carrying well over 15% of page text (D11-03 refuse-side fixture).
  3. synthetic-scanned.pdf — 2 pages with empty content streams (no text operators at all) — zero text items.
  4. synthetic-outline.pdf — 2 single-column pages plus a /Outlines tree with two bookmark items using explicit array destinations pointing at page tops (outline-first heading fixture for D11-08).
  5. synthetic-corrupt.pdf — literal ASCII bytes "this is not a pdf".
  Commit generator + outputs; add tests/fixtures/pdf/README.md stating these are GENERATED SYNTHETIC fixtures, committable, and explicitly NOT the D11-04 real-PDF calibration corpus (which stays local + gitignored).
  Wave-0 sentinels (h1-visible precedent from Plan 04-02 — prove wiring, not test.todo):
  - tests/unit/server/pdf-to-blocks.spec.ts — reads each fixture via readFileSync into Uint8Array; asserts the four valid fixtures begin with %PDF- and have length greater than 500 bytes; asserts the corrupt fixture contains the ASCII marker text.
  - tests/unit/server/ingest-pdf.spec.ts — asserts IngestionRequestSchema parses a real base64 payload with filename and rejects a non-base64 pdf string.
  - tests/e2e/pdf-intake.spec.ts — clones the markdown-upload.spec.ts beforeEach (image route stub + IndexedDB wipe + goto BASE) with one test asserting input#ingest-file is visible.</action>
  <verify>
    <automated>node tests/fixtures/pdf/generate-synthetic-pdfs.ts && npx vitest run tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts && npx playwright test pdf-intake</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/pdf/ contains the five .pdf files, the generator script, and README.md
    - head -c 5 on synthetic-single-column.pdf prints %PDF-
    - Generator re-run is idempotent (byte-identical outputs)
    - Both unit sentinel specs exit 0 under the vitest server project
    - npx playwright test pdf-intake exits 0 (1 test × chromium/firefox/webkit)
  </acceptance_criteria>
  <done>Synthetic corpus committed and labeled; all three Wave-0 sentinel suites green across the full engine matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | unpdf install (SUS-rated by legitimacy audit) |
| client bundle ← /server | unpdf must never cross into src/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm install unpdf | mitigate | blocking checkpoint:human-verify before install (Task 1); exact pin; registry signals re-confirmed by the human |
| T-11-01 | DoS | PDF parsing (future plans) | mitigate | caps land now: MAX_IMAGE_PIXELS (pixels not bytes), PDF_MAX_PAGES, PDF_EXTRACTION_TIMEOUT_MS, MAX_INGEST_BODY_BYTES |
| T-11-05 | Tampering | extracted PDF text (future plans) | mitigate | schema widening keeps the doc model the security boundary — Block JSON renders as inert React children |
</threat_model>

<verification>
npx vitest run tests/unit/ingestion-schema.test.ts tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts green; npx tsc --noEmit green; npx playwright test pdf-intake green on 3 engines; no unpdf import anywhere under src/.
</verification>

<success_criteria>
Approved exact unpdf pin installed; PDF_MAX_BYTES/MAX_IMAGE_PIXELS/PDF_MAX_PAGES/PDF_EXTRACTION_TIMEOUT_MS/MAX_INGEST_BODY_BYTES defined; pdf request variant + five failure reasons + pdf source member + PDF badge shipped additively; five synthetic fixtures committed with generator + README; all sentinels green.
</success_criteria>

## Artifacts this phase produces

- `PDF_MAX_BYTES` constant — src/ingestion/types.ts (re-exported by server/limits.ts)
- `MAX_IMAGE_PIXELS`, `PDF_MAX_PAGES`, `PDF_EXTRACTION_TIMEOUT_MS`, `MAX_INGEST_BODY_BYTES` constants — server/limits.ts
- `pdf` request variant (base64 + optional filename) on IngestionRequestSchema; five new IngestionFailureReasonEnum members: pdf-unreadable, pdf-encrypted, pdf-scanned, pdf-multi-column, pdf-too-large
- `pdf` member on ArticleSourceSchema; `case "pdf"` on SourceBadge badgeLabel
- tests/fixtures/pdf/generate-synthetic-pdfs.ts + five committed synthetic fixtures + README
- Wave-0 sentinel specs: tests/unit/server/pdf-to-blocks.spec.ts, tests/unit/server/ingest-pdf.spec.ts, tests/e2e/pdf-intake.spec.ts

<output>
Create `.planning/phases/11-pdf-intake/11-01-SUMMARY.md` when done
</output>
