---
phase: 08-markdown-pipeline-and-personal-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/markdownToBlocks.ts
  - server/ingest.ts
  - src/content/schema.ts
  - src/ingestion/types.ts
  - src/ingestion/IngestionClient.ts
  - tests/unit/server/markdown-to-blocks.spec.ts
  - package.json
autonomous: true
requirements:
  - ING-03
user_setup: []

must_haves:
  truths:
    - "A Markdown (.md) document posted to /api/ingest as {markdown} returns a valid CanonicalArticle whose blocks carry the same 9-kind shape as HTML-extracted articles"
    - "YAML front-matter (title/author/date) in a .md document is recognized as Provenance metadata"
    - "Raw HTML inside a .md document (e.g. a script tag) is escaped to plain text by the parser; zero HTML payload survives into the Block tree"
    - "Two uploads of byte-identical .md content produce the same article id (md-<shortHash>) and the second is refuseable as already-in-library by the existing dedupe check"
    - "A representative markdown corpus fixture (heading + paragraph + list + blockquote + code) passes assertRoundTripAnchor (5-offset gate resolves to confident)"
  artifacts:
    - path: "server/markdownToBlocks.ts"
      provides: "Strict-CommonMark mdast → 9-kind Block tree adapter (sibling of htmlToBlocks)"
      exports: ["markdownToBlocks", "MarkdownToBlocksResult"]
      min_lines: 120
    - path: "tests/unit/server/markdown-to-blocks.spec.ts"
      provides: "Unit tests: mdast mapping, raw-HTML escape, front-matter, round-trip gate"
      min_lines: 120
  key_links:
    - from: "server/ingest.ts"
      to: "server/markdownToBlocks.ts"
      via: "markdown dispatch branch when input carries {markdown}"
      pattern: "markdownToBlocks\\("
    - from: "src/ingestion/IngestionClient.ts"
      to: "src/ingestion/types.ts"
      via: "ingestMarkdown widens the body union to include {markdown}"
      pattern: "ingestMarkdown"
---

<objective>
Ship the server-side Markdown pipeline (ING-03): a `markdownToBlocks` adapter that is a strict sibling of `htmlToBlocks`, plus the schema + orchestrator + client wrappers needed for `.md` content to flow through the SAME 7-stage pipeline as URL and paste-HTML inputs.

Purpose: Establishes the lowest-risk second intake format. The doc model IS the security boundary — strict CommonMark escapes raw HTML by default (no sanitizer needed), and the existing `assertRoundTripAnchor` gate proves byte-equivalence with fixtures (Pitfall 8-1). This plan also lands the additive `tags` field on `ArticleSchema` so Plan 02 can build the tag store on top.

Output: `server/markdownToBlocks.ts` (new), widened `ArticleSourceSchema` + `IngestionMetaSchema.origin` + `ArticleSchema.tags`, widened `IngestionRequestSchema`, `ingestMarkdown` client wrapper, `md-<shortHash>` id derivation branch, and a comprehensive unit suite covering the mdast → Block mapping, raw-HTML escape (Pitfall 8-2), front-matter extraction, and round-trip anchor gate.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-CONTEXT.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-RESEARCH.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-PATTERNS.md
@server/htmlToBlocks.ts
@server/ingest.ts
@src/content/schema.ts
@src/ingestion/types.ts
@src/ingestion/IngestionClient.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: markdownToBlocks adapter + unit tests</name>
  <files>server/markdownToBlocks.ts, tests/unit/server/markdown-to-blocks.spec.ts, package.json</files>
  <read_first>
    - `server/htmlToBlocks.ts` — the locked sibling adapter; copy the output shape (HtmlToBlocksResult L252-258 + ExtractAndNormalizeResult L462-464), the extractInline + tidyRuns inline-run logic (L121-181), the exhaustive visit() structure (L272-366), and the ProvenancePartial type (L37-42)
    - `src/content/schema.ts` — BlockSchema 9 kinds + Mark (4: link/code/strong/em) + FootnoteBody `/^fn-\d+$/` regex (L115) + FootnoteReferenceBlock shape
    - `tests/unit/server/extraction.spec.ts` — the test shape to mirror (SCHEMA_KINDS constant asserting every output block has a kind in the 9-kind tuple)
    - `tests/unit/server/mxss.spec.ts` — the mXSS gate pattern to mirror for the raw-HTML escape test (Pitfall 8-2)
    - `08-RESEARCH.md` §Pattern 1 (L290-373) — the locked mdast → Block mapping table; §Pitfall 8-1 (round-trip drift) + §Pitfall 8-2 (raw HTML escape)
    - `08-PATTERNS.md` §server/markdownToBlocks.ts (L59-134) — import patterns, output shape, inline-run extraction contract
  </read_first>
  <action>
    Install the four server-only packages (per D8-16 + RESEARCH §Standard Stack + §Package Legitimacy Audit — all four are `[OK / Approved]`, no blocking checkpoint): `npm install unified@11.0.5 remark-parse@11.0.0 remark-frontmatter@5.0.0 yaml@2.9.0`. These are ESM-only and match the project's `"type": "module"` (Pitfall 8-6 — server-only, never client-bundled).

    Create `server/markdownToBlocks.ts` as a sibling of `server/htmlToBlocks.ts`. Export `MarkdownToBlocksResult` (identical to `ExtractAndNormalizeResult` from htmlToBlocks.ts L462-464: `{ blocks, footnotes, lang, provenancePartial, isReaderable }`) and `markdownToBlocks(md: string): Promise<MarkdownToBlocksResult>`.

    Build the unified processor as `const processor = unified().use(remarkParse).use(remarkFrontmatter)` — strict CommonMark (remark-parse default; raw HTML is escaped, no `allowDangerousHTML` — D8-16). Parse via `processor.parse(md)` to get the mdast tree.

    Walk `tree.children` with an exhaustive if/switch chain (Pattern F — no `default:` clause; the unsupported branch IS the catch-all). Per the RESEARCH.md §Pattern 1 mapping table (L298-317):
    - `yaml` node → do NOT emit a Block; parse `node.value` via `parseYaml(node.value)` from the `yaml` package (strict YAML 1.2); merge `title`/`author`/`date` string fields into `provenancePartial` per D8-17 (date → `new Date(date).toISOString()`; non-string fields dropped). `continue` the loop.
    - `heading` (depth 1-6) → `HeadingBlock` with `level = depth`; content via the shared inline extractor (below).
    - `paragraph` → `ParagraphBlock`; content via the shared inline extractor.
    - `blockquote` → `BlockquoteBlock` with `.children` = flatMap of walking each child node (mirror htmlToBlocks L310-316).
    - `list` with `ordered: false` → `BulletedListBlock`; `ordered: true` → `NumberedListBlock` (`start` from `node.start ?? 1`). Each `listItem` → `{ content: Block[] }` where content is the flatMap of walking the item's children (paragraph children become paragraph blocks, mirroring htmlToBlocks L318-329).
    - `code` (fenced or indented) → `CodeBlock` with `source = node.value`; `language` from `node.lang` if present (lowercased).
    - `image` (block-level standalone) → `FigureBlock` (alt = `node.alt`, src = `node.url`); if src is not http(s), demote to `UnsupportedBlock` (mirror htmlToBlocks link/image scheme discipline).
    - `html` node (raw HTML block) → emit a `ParagraphBlock` whose inline run text is the literal `node.value` string (escaped by the parser; the string is inert text — Pitfall 8-2). NEVER carry raw HTML as structured payload. Add a header comment in the file documenting D8-16 so the next maintainer can't silently re-open the surface.
    - `thematicBreak` → skip (decorative; no Block kind).
    - `table`/`math`/`toml`/anything else → `UnsupportedBlock` with `originalKind: node.type` and a calm `plainDescription` (DOC-06).

    Inline extraction: implement `extractInlineMdast(node, marks)` that recurses `.children` and emits D-04 marks exactly (link with http(s)/mailto href else demoted to text per htmlToBlocks L141-144; inlineCode → CodeMark; strong → StrongMark; emphasis → EmMark; text → whitespace-collapsed run per htmlToBlocks L125 `replace(/\s+/g, " ")`; break → space). Apply the same `tidyRuns` post-processing (drop leading/trailing whitespace-only runs, merge adjacent same-mark runs) as htmlToBlocks L159-181. Pitfall 8-1 (round-trip drift) is the single highest-risk concern: the inline-run shape MUST match what `normalizeText` expects. If shapes converge, extract a shared helper; otherwise mirror the logic byte-faithfully.

    `lang` defaults to `"en"` (markdown carries no lang attribute; mirrors htmlToBlocks detectLang default). `isReaderable = blocks.length >= 3` (mirrors deriveConfidence heuristic; planner-tunable). `footnotes: []` (strict CommonMark has no footnote syntax).

    Create `tests/unit/server/markdown-to-blocks.spec.ts` mirroring `tests/unit/server/extraction.spec.ts` header structure: define `SCHEMA_KINDS` as the 9-kind tuple and assert every output block has a kind in it. Cover:
    - mdast → Block mapping for each kind (heading, paragraph, bulleted list, numbered list, blockquote, code, figure). Assert exact `block.kind` values and content shapes.
    - YAML front-matter: a `.md` with `---\ntitle: My Essay\nauthor: Jane Doe\ndate: 2024-01-15\n---` produces `provenancePartial.title === "My Essay"`, `provenancePartial.author === "Jane Doe"`, `provenancePartial.publishedAt` is an ISO string.
    - Missing front-matter: a `.md` without `---` block leaves `provenancePartial` empty (title fallback happens in ingest.ts, not here).
    - Raw-HTML escape (Pitfall 8-2): a `.md` containing a literal script tag block produces blocks whose rendered text contains the tag as inert text — assert zero `<` in any `InlineRun.text` payload starts a tag-bearing sequence that survives as structured HTML; assert the markdownToBlocks output blocks contain no `kind` outside the 9-kind tuple.
    - Round-trip gate (Pitfall 8-1): build a representative fixture (front-matter + h1 + paragraph + bulleted list + blockquote + code block), run the full pipeline through `markdownToBlocks` → assemble a minimal article → call `assertRoundTripAnchor` (import from server/ingest.ts or the module that exports it) → assert it does not throw.

    Run `npm run test:unit -- --run tests/unit/server/markdown-to-blocks.spec.ts` and ensure all tests pass before committing.
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run test:unit -- --run tests/unit/server/markdown-to-blocks.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/markdownToBlocks.ts` exists and exports `markdownToBlocks` and `MarkdownToBlocksResult`
    - `grep -c 'remarkParse' server/markdownToBlocks.ts` returns ≥ 1 (the parser is wired)
    - `grep -c 'allowDangerousHTML\|allowDangerousHtml' server/markdownToBlocks.ts` returns 0 (strict CommonMark — D8-16)
    - `grep -c 'remarkFrontmatter' server/markdownToBlocks.ts` returns ≥ 1 (YAML extraction)
    - `grep -c 'parseYaml\|parse as parseYaml' server/markdownToBlocks.ts` returns ≥ 1 (YAML parsing)
    - The mdast mapping handles all node types in the RESEARCH.md §Pattern 1 table (heading/paragraph/blockquote/list/code/image/html/thematicBreak + yaml + unsupported fallback)
    - The raw-HTML escape test passes: a `.md` with a script tag produces zero structured HTML in the Block tree
    - The round-trip test passes: `assertRoundTripAnchor` does not throw on a representative fixture
    - `package.json` includes `unified`, `remark-parse`, `remark-frontmatter`, `yaml` in dependencies with the pinned versions
    - `npm run test:unit -- --run tests/unit/server/markdown-to-blocks.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    markdownToBlocks ships as a strict sibling of htmlToBlocks. Strict CommonMark escapes raw HTML by default (zero XSS surface from the Markdown path per D8-16). The mdast → Block mapping is mechanical and exhaustive (Pattern F). Front-matter is parsed via the strict `yaml` package. The round-trip gate passes on a representative fixture (Pitfall 8-1 defense). All unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Schema widening + pipeline dispatch + client wrapper</name>
  <files>src/content/schema.ts, src/ingestion/types.ts, server/ingest.ts, src/ingestion/IngestionClient.ts</files>
  <read_first>
    - `src/content/schema.ts` L195-236 — `ArticleSourceSchema` (L207), `IngestionMetaSchema` (L215-223), `ArticleSchema` (L226-236); the comment at L205-207 explicitly anticipates the `"markdown"` widening
    - `src/ingestion/types.ts` L19-22 — `IngestionRequestSchema` (the union to widen)
    - `server/ingest.ts` L91-93 (`shortHash`), L123-152 (the input-validation guard + url/html dispatch), L177 (the paste id-derivation line `md-` mirrors), L250-260 (the typed-reason catch)
    - `src/ingestion/IngestionClient.ts` L65-77 (the `ingestUrl`/`ingestHtml` pattern to clone for `ingestMarkdown`), L90 (the private `ingest` signature to widen)
    - `08-RESEARCH.md` §Code Examples Example 1 (L645-660 — IngestionRequestSchema widening), Example 2 (L664-679 — ArticleSourceSchema + origin widening), Example 3 (L683-731 — ingest.ts dispatch + id branch)
  </read_first>
  <action>
    Widen `ArticleSourceSchema` in `src/content/schema.ts` (per D8-16 + ARCHITECTURE.md L390 — the closed enum anticipated this additive widening): add `"markdown"` and `"html-upload"` to the existing `z.enum(["fixture", "url", "paste"])` so the result is `z.enum(["fixture", "url", "paste", "markdown", "html-upload"])`. Reference decision D8-15 (ship both .md and .html) and D8-16 in the comment.

    Widen `IngestionMetaSchema.origin` discriminator in `src/content/schema.ts` (L217): from `z.enum(["url", "paste"]).optional()` to `z.enum(["url", "paste", "upload"]).optional()`. The `"upload"` value covers both markdown + html-upload (both come from the file picker); the `source` field carries the format distinction. Per RESEARCH.md §Code Examples Example 2 L675-678.

    Add the `tags` field to `ArticleSchema` in `src/content/schema.ts` (per D8-05..D8-08 + RESEARCH §Pattern 2): `tags: z.array(z.string().min(1)).default([]).optional()` placed after `ingestionMeta` (additive; v1.0 fixtures + Phase 7 rows omit the field and hydrate to `[]` via the `.default()` mechanism — Pitfall 9 backward-compat). Reference decision D8-05 (document-tag namespace) in the comment.

    Widen `IngestionRequestSchema` in `src/ingestion/types.ts` (L19-22): add `z.object({ markdown: z.string().min(1) })` as a third union member. Per RESEARCH §Code Examples Example 1. (The `.html` upload reuses the existing `{html}` variant; the server-side `source`/`origin` tags distinguish them.)

    Edit `server/ingest.ts` (per RESEARCH §Code Examples Example 3):
    - Add `import { markdownToBlocks } from "./markdownToBlocks";` near the existing htmlToBlocks import.
    - Widen the Stage 0 input-validation guard (L127-131): count exactly-one-of `{url, html, markdown}` — `const hasMarkdown = "markdown" in input && input.markdown !== undefined;` and the guard becomes `if ((hasUrl ? 1 : 0) + (hasHtml ? 1 : 0) + (hasMarkdown ? 1 : 0) !== 1) throw new IngestionError("server-error");`.
    - Add a markdown dispatch branch alongside the url/html branches (around L140-152): when `hasMarkdown`, call `await markdownToBlocks(md)` and destructure `{ blocks, footnotes, lang, provenancePartial, isReaderable }`. Set `origin = "upload"`, `source = "markdown"`, `fetchedAt = undefined`, `finalUrl = undefined`.
    - Widen the id-derivation line (L177): the current `const id = finalUrl ? slugifyUrl(finalUrl) : \`paste-\${shortHash(html)}\`` becomes a three-way branch — url keeps `slugifyUrl(finalUrl)`; paste keeps `paste-${shortHash(html)}`; markdown uses `md-${shortHash(md)}` per D8-18 (content-hash slug, NOT filename — dedupe-refuse on re-upload mirrors D7-07).
    - The `originalHtmlHash` for markdown = `"sha256:" + createHash("sha256").update(md).digest("hex")` (traceability preserved per D8-17).
    - The title fallback (L185) for markdown: `provenancePartial.title ?? "Markdown document"` (markdownToBlocks may set title from front-matter; if absent, a neutral fallback — the filename fallback is NOT wired here because the server envelope carries no filename; the client-side upload form may pass the filename as a hint if the planner wires that channel, but the server owns the canonical fallback per D8-17 single-source-of-truth).
    - The assembled `ingestionMeta.source` for markdown = `"markdown"`; `ingestionMeta.origin = "upload"`. The existing `ArticleSchema.parse` + `assertRoundTripAnchor` + `deriveConfidence` stages run identically on all three paths — the load-bearing invariant.

    Add `ingestMarkdown` to `src/ingestion/IngestionClient.ts` (per RESEARCH §Pattern — clone `ingestHtml` L75-77): `export async function ingestMarkdown(markdown: string): Promise<IngestionSuccess> { return ingest({ markdown }); }`. Widen the private `ingest` body type from `{url} | {html}` to `{url} | {html} | {markdown}`. STATE-04 defense-in-depth (ArticleSchema.parse on the response at L122) applies unchanged.

    Run `npm run test:unit -- --run` (full Vitest suite) and ensure zero regressions — the existing Phase 7 unit tests (`tests/unit/server/ingest-adapter.spec.ts`, `extraction.spec.ts`, `mxss.spec.ts`) must still pass with the widened schemas + dispatch.

    Run `npm run build` (tsc + vite build) and ensure zero TypeScript errors — the schema widening must be type-safe across all consumers (repository.ts, LibrarySource.ts, ArticleView.tsx).
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run test:unit -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"markdown"' src/content/schema.ts` returns ≥ 1 (ArticleSourceSchema widened per D8-16)
    - `grep -c '"html-upload"' src/content/schema.ts` returns ≥ 1 (D8-15)
    - `grep -c '"upload"' src/content/schema.ts` returns ≥ 1 (IngestionMetaSchema.origin widened)
    - `grep -c 'tags:' src/content/schema.ts` returns ≥ 1 (the additive tags field on ArticleSchema)
    - `grep -c 'markdown' src/ingestion/types.ts` returns ≥ 1 (IngestionRequestSchema widened)
    - `grep -c 'markdownToBlocks' server/ingest.ts` returns ≥ 1 (dispatch branch wired)
    - `grep -c 'md-' server/ingest.ts` returns ≥ 1 (the id-derivation branch per D8-18)
    - `grep -c 'ingestMarkdown' src/ingestion/IngestionClient.ts` returns ≥ 1 (client wrapper)
    - `npm run test:unit -- --run` exits 0 (zero regressions on the existing 408-test Phase 7 suite)
    - `npm run build` exits 0 (TypeScript type-check passes across all consumers)
    - The existing `lint:no-danger` gate still passes (the repo-wide grep for the forbidden React XSS primitive)
  </acceptance_criteria>
  <done>
    The schema is widened additively (ArticleSource + origin + tags field). The server pipeline dispatches `{markdown}` through markdownToBlocks → ArticleSchema.parse → assertRoundTripAnchor identically to the url/html paths. The id derivation produces `md-<shortHash(canonical)>` per D8-18 (content-hash, dedupe-refuse on re-upload). The client wrapper `ingestMarkdown` mirrors `ingestHtml`. The full Vitest suite + tsc build pass with zero regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/ingest | The `{markdown}` body crosses here; same-origin POST + `connect-src 'self'` CSP (unchanged from Phase 7) |
| markdownToBlocks → ArticleSchema.parse | Untrusted markdown content → validated Block tree; the doc model IS the security boundary (ING-07) |
| server → client (response) | ArticleSchema-validated response re-parsed on client read (STATE-04 defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering / XSS | Raw HTML in `.md` files | mitigate | Strict CommonMark escapes raw HTML to inert text by default (remark-parse without `allowDangerousHTML` per D8-16); the mdast walker maps `html` nodes to ParagraphBlock text — NEVER structured payload. Unit test in Task 1 (mirror mxss.spec.ts pattern). |
| T-8-02 | Tampering / XSS | `javascript:`/`data:` URIs in markdown links/images | mitigate | mdast `link.url` validated against http(s)/mailto allow-list (mirror htmlToBlocks L141-144); non-conforming URLs demoted to plain text. The schema's `linkableUrl`/`httpUrl` refinements re-validate at ArticleSchema.parse time. |
| T-8-03 | Tampering | Malicious YAML (billion-laughs / type coercion) | mitigate | `yaml` package is strict YAML 1.2 (safe-schema; no implicit type coercion); front-matter parsed once, never re-serialized. Validate the parsed shape before merging into provenancePartial (only string title/author/date carried). |
| T-8-04 | DoS | Markdown content bomb (multi-MB `.md`) | accept (server-side) | Server re-applies Phase 7's content-length cap; client-side 5MB cap lands in Plan 04 (IngestControl upload form). Not a Plan 01 concern. |
| T-8-05 | Info Disclosure | markdownToBlocks normalization drift vs htmlToBlocks | mitigate | Reuse the SAME `extractInline` + `tidyRuns` logic shape (Pitfall 8-1); the round-trip anchor gate refuses any drift. Unit test in Task 1. |
| T-8-SC | Tampering | npm install of unified/remark-parse/remark-frontmatter/yaml | mitigate | All four packages verified `[OK / Approved]` via the Package Legitimacy Audit in RESEARCH.md (no postinstall scripts, no slopsquatting). No blocking checkpoint needed for Approved packages. |
</threat_model>

<verification>
- `npm run test:unit -- --run tests/unit/server/markdown-to-blocks.spec.ts` exits 0 (mdast mapping + raw-HTML escape + front-matter + round-trip gate)
- `npm run test:unit -- --run` exits 0 (zero Phase 7 regressions)
- `npm run build` exits 0 (TypeScript type-check + Vite client build; Pitfall 8-6 — verify client bundle does not materially grow)
- The existing `lint:no-danger` gate continues to pass (no new XSS surface introduced)
</verification>

<success_criteria>
- A `.md` document with front-matter + heading + paragraph + list + blockquote + code flows through the full pipeline and returns a valid CanonicalArticle
- The article id is `md-<shortHash(canonical)>` (D8-18) — two uploads of identical content produce the same id
- Raw HTML in a `.md` is escaped to inert text (Pitfall 8-2)
- The round-trip anchor gate passes on a representative fixture (Pitfall 8-1)
- Schema widening is additive (v1.0 fixtures + Phase 7 rows hydrate unchanged via Pitfall 9 `.optional()`/`.default()`)
- Zero Phase 7 unit-test regressions
</success_criteria>

<output>
Create `.planning/phases/08-markdown-pipeline-and-personal-library/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Plan 01 symbols:**
- `server/markdownToBlocks.ts`: `markdownToBlocks(md: string): Promise<MarkdownToBlocksResult>`, `MarkdownToBlocksResult` interface, internal `extractInlineMdast`, internal mdast-walker
- `src/content/schema.ts`: widened `ArticleSourceSchema` (adds `"markdown"`, `"html-upload"`), widened `IngestionMetaSchema.origin` (adds `"upload"`), new `ArticleSchema.tags` field (`z.array(z.string().min(1)).default([]).optional()`)
- `src/ingestion/types.ts`: widened `IngestionRequestSchema` (adds `{markdown: z.string().min(1)}` union member)
- `server/ingest.ts`: widened `ingest(input)` dispatch (3-way: url/html/markdown), new markdown branch with `md-${shortHash(md)}` id derivation
- `src/ingestion/IngestionClient.ts`: new `ingestMarkdown(markdown: string)` export, widened private `ingest` body type
- `tests/unit/server/markdown-to-blocks.spec.ts`: SCHEMA_KINDS constant, mdast mapping tests, raw-HTML escape test, front-matter tests, round-trip gate test
- `package.json`: `unified@11.0.5`, `remark-parse@11.0.0`, `remark-frontmatter@5.0.0`, `yaml@2.9.0` in dependencies
