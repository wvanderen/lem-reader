---
phase: 08-markdown-pipeline-and-personal-library
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/App.tsx
  - src/ingestion/library/libraryFilter.ts
  - src/ingestion/library/LibrarySearch.tsx
  - src/ingestion/library/TagFilter.tsx
  - src/ingestion/library/SourceBadge.tsx
  - src/ingestion/library/LibraryRow.tsx
  - src/ingestion/library/ContinueReadingStrip.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/routes/FixtureList.tsx
  - src/app.css
  - tests/unit/library-search.test.ts
autonomous: true
requirements:
  - LIB-01
  - LIB-03
  - LIB-05
  - LIB-06
user_setup: []

must_haves:
  truths:
    - "The personal library is the default route at #/ (replacing FixtureList) and shows v1.0 fixtures badged source:\"fixture\" alongside ingested articles (SC#1)"
    - "No v1.0 e2e test regresses — the <h1>Saved articles</h1> heading, <ul><li><a href=\"#/article/<id>\">Open article</a> structure, and per-row <h2 id=\"title-{id}\"> aria-labelledby pattern are byte-stable (Pitfall 8-5)"
    - "Reader can search the library by title, author, source-domain, or tag name via a single search input (D8-06)"
    - "Reader can filter the library by a single tag via chip toggles; chips auto-prune when no article carries the tag (D8-07, D8-08)"
    - "Each library row shows title + author (when present) + an inline source indicator (D8-02) + a positional progress hairline when partially read (D8-11)"
    - "A Continue-reading strip above the list shows the 1-3 most-recently-opened unfinished articles (D8-09, D8-10)"
    - "Finished articles (progress >= 0.98) leave the strip and show a Finished mark in the row (D8-12)"
  artifacts:
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "Default route component replacing FixtureList; composes ContinueReadingStrip + LibrarySearch + TagFilter + library list"
      min_lines: 80
    - path: "src/ingestion/library/LibraryRow.tsx"
      provides: "One article row: title + author + SourceBadge + ProgressHairline + FinishedMark + tag chips + remove trigger"
    - path: "src/ingestion/library/libraryFilter.ts"
      provides: "Pure filter+sort helper (filterLibrary + domainOf)"
      exports: ["filterLibrary", "domainOf", "LibraryFilter"]
    - path: "src/ingestion/library/ContinueReadingStrip.tsx"
      provides: "1-3 most-recently-opened unfinished articles above the list"
    - path: "src/ingestion/library/SourceBadge.tsx"
      provides: "Per-row source indicator (fixture/url/paste/markdown/html-upload) with optional source link"
    - path: "src/ingestion/library/LibrarySearch.tsx"
      provides: "Controlled search input (title/author/domain/tag)"
    - path: "src/ingestion/library/TagFilter.tsx"
      provides: "Auto-pruned single-select tag chip strip"
  key_links:
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "src/content/repository.ts"
      via: "listArticles() to load the composite library (fixtures + ingested)"
      pattern: "listArticles"
    - from: "src/ingestion/library/LibraryRow.tsx"
      to: "src/reader/ProgressHairline.tsx"
      via: "renders <ProgressHairline progress={ratio}> when 0 < ratio < 0.98"
      pattern: "ProgressHairline"
    - from: "src/ingestion/library/ContinueReadingStrip.tsx"
      to: "src/persistence/locationStore.ts"
      via: "loadAllLocations() to derive the most-recently-opened unfinished set"
      pattern: "loadAllLocations"
    - from: "src/ingestion/library/TagFilter.tsx"
      to: "src/ingestion/library/tagsStore.ts"
      via: "loadAllTags() to derive the auto-pruned chip list"
      pattern: "loadAllTags"
---

<objective>
Replace FixtureList with the personal LibraryView at the `#/` default route. Composes the shipped `compositeLibraryRepository`, `locationStore.loadAllLocations`, `tagsStore.loadAllTags`, and `ProgressHairline` into the calm reading-room shelf: browse + search + tag-filter + source badges + per-row progress hairline + continue-reading strip.

Purpose: Delivers the reader-facing surface of LIB-01 (library replaces fixture list), LIB-03 (search by title/metadata/tag), LIB-05 (ingestion metadata + source link), and LIB-06 (recently-read shortcuts + positional progress). SC#1 (no v1.0 e2e regression) is the load-bearing constraint — the `<h1>Saved articles</h1>` heading, `<ul><li><a>` structure, and per-row `<h2 id="title-{id}">` aria-labelledby pattern stay byte-stable per Pitfall 8-5 + the UI-SPEC §Regression Targets table.

Output: 7 new components (`LibraryView`, `LibraryRow`, `SourceBadge`, `ContinueReadingStrip`, `LibrarySearch`, `TagFilter`) + 1 pure helper (`libraryFilter`), the App.tsx one-line swap, the library CSS in `app.css`, FixtureList removed/renamed, and a unit suite for the filter helper.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-CONTEXT.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-RESEARCH.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-PATTERNS.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-UI-SPEC.md
@src/routes/FixtureList.tsx
@src/App.tsx
@src/reader/ProgressHairline.tsx
@src/content/repository.ts
@src/app.css
@.planning/phases/08-markdown-pipeline-and-personal-library/08-01-SUMMARY.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: libraryFilter helper + LibrarySearch + TagFilter + unit tests</name>
  <files>src/ingestion/library/libraryFilter.ts, src/ingestion/library/LibrarySearch.tsx, src/ingestion/library/TagFilter.tsx, tests/unit/library-search.test.ts</files>
  <read_first>
    - `08-RESEARCH.md` §Code Examples Example 5 (L798-836 — the `filterLibrary` + `domainOf` skeleton to copy verbatim; this IS the spec)
    - `src/ingestion/IngestControl.tsx` L143-159 — the controlled-input form pattern (LibrarySearch mirrors the onSubmit/onChange shape)
    - `src/ingestion/library/tagsStore.ts` (from Plan 02) — `loadAllTags()` is the chip-list source
    - `src/content/types.ts` or `src/content/schema.ts` — `CanonicalArticle` type (the filter input shape)
    - `08-UI-SPEC.md` §LibrarySearch (L157-163) + §TagFilter (L165-177) + §TagChip (L173-177) + §Copywriting (L251-254)
    - `08-PATTERNS.md` §src/ingestion/library/libraryFilter.ts (L297-301) + §LibrarySearch (L248-269) + §TagFilter (L240-245)
  </read_first>
  <action>
    Create `src/ingestion/library/libraryFilter.ts` per RESEARCH §Code Examples Example 5 L798-836 (copy the skeleton verbatim — it IS the spec). Export `LibraryFilter` interface (`{ query: string; activeTag: string | null }`), `filterLibrary(articles, filter): CanonicalArticle[]`, and `domainOf(url: string | undefined): string`. The filter: trim+lowercase the query; if `activeTag !== null`, exclude articles whose `tags ?? []` does not include it (D8-07 single-tag AND-style); if query is non-empty, build a haystack of `[title, author ?? "", domainOf(sourceUrl), ...tags ?? []]` joined lowercase and require `.includes(q)` (D8-06 — title + author + source-domain + tag-names). `domainOf` is defensive: `try { return new URL(url).hostname; } catch { return ""; }` on `undefined` returns `""`.

    Create `src/ingestion/library/LibrarySearch.tsx` per UI-SPEC §LibrarySearch. A `<form role="search" className="library-search">` containing a visually-hidden `<label>` ("Search your library") + `<input type="search" id="library-search" name="q" placeholder="Search by title, author, or tag" autoComplete="off">`. Controlled input; `onChange` lifts the query to the parent (LibraryView) on every keystroke (D8-06 — filter-on-keystroke, no submit button). Escape clears the query (native `<input type="search">` UA behavior). The 44px min-height touch target + 1px `--hairline` border + 4px radius + `:focus-visible` global ring per UI-SPEC §Spacing + §Interaction 11.

    Create `src/ingestion/library/TagFilter.tsx` per UI-SPEC §TagFilter + §TagChip. A `<fieldset className="tag-filter">` with a visually-hidden `<legend>` ("Filter by tag"). Render one `<button type="button" className="tag-chip" aria-pressed={active}>` per tag from the `tags` prop (the auto-pruned list derived by LibraryView via `loadAllTags`). Single-select toggle behavior (D8-07): clicking a chip sets it active (calls `onSelect(tag)`); clicking the active chip clears (calls `onSelect(null)`). The `aria-pressed` attribute conveys state beyond color (forced-colors safety per UI-SPEC §Interaction 10). If the `tags` prop is empty, render nothing (no "no tags yet" copy — spare chrome per UI-SPEC). Each chip is a 44px-min touch target per UI-SPEC §Spacing.

    Create `tests/unit/library-search.test.ts` (a pure-function test — no Dexie needed). Import `filterLibrary` + `domainOf`. Build a small `sampleArticles` array covering: an article with title "Plato's Republic" + author "Plato" + sourceUrl "https://example.com/essay" + tags `["essay"]`; an article with title "Meditations" + author "Marcus" + no sourceUrl + tags `["essay", "stoic"]`; a fixture with no tags. Cover:
    - Empty query + null activeTag → returns all articles unchanged.
    - Query "plato" → returns only the first (title match).
    - Query "example.com" → returns only the first (domain match via domainOf).
    - Query "essay" → returns the first two (tag-name match — D8-06 tags are first-class searchable metadata).
    - Query "stoic" → returns only the second.
    - activeTag "essay" + empty query → returns the first two.
    - activeTag "stoic" + query "marcus" → returns only the second (tag AND query compose).
    - activeTag "nonexistent" → returns empty (no article carries it).
    - `domainOf(undefined)` returns `""`; `domainOf("not-a-url")` returns `""`; `domainOf("https://foo.example.com/path")` returns `"foo.example.com"`.
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run test:unit -- --run tests/unit/library-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ingestion/library/libraryFilter.ts` exports `filterLibrary`, `domainOf`, and `LibraryFilter`
    - `grep -c 'activeTag' src/ingestion/library/libraryFilter.ts` returns ≥ 2 (the tag filter branch + the interface)
    - `grep -c 'hostname' src/ingestion/library/libraryFilter.ts` returns ≥ 1 (domainOf extracts hostname)
    - `src/ingestion/library/LibrarySearch.tsx` renders `<input type="search">` with `id="library-search"` and `placeholder="Search by title, author, or tag"`
    - `src/ingestion/library/TagFilter.tsx` renders `<fieldset className="tag-filter">` with a `<legend>` and `<button aria-pressed>` chips
    - `grep -c 'aria-pressed' src/ingestion/library/TagFilter.tsx` returns ≥ 1 (state beyond color — forced-colors safety)
    - The unit test covers: empty filter, title match, domain match, tag-name match, tag + query composition, nonexistent tag, domainOf edge cases
    - `npm run test:unit -- --run tests/unit/library-search.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    The pure filter helper ships with full coverage (title/author/domain/tag search + single-tag filter + composition). LibrarySearch is a controlled input with filter-on-keystroke. TagFilter is an auto-pruned single-select chip strip with `aria-pressed` state. All unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: SourceBadge + LibraryRow + ContinueReadingStrip</name>
  <files>src/ingestion/library/SourceBadge.tsx, src/ingestion/library/LibraryRow.tsx, src/ingestion/library/ContinueReadingStrip.tsx</files>
  <read_first>
    - `src/routes/FixtureList.tsx` L63-76 — the existing `<li>` row markup (title + author + Open-article link) that LibraryRow extends byte-stably per Pitfall 8-5
    - `src/reader/ProgressHairline.tsx` — the component contract (`progress?: number` in [0,1]; renders via scaleX; consumed UNCHANGED — do not fork)
    - `src/persistence/locationStore.ts` — `loadAllLocations()` (from Plan 02) returns `LocationRecord[]` with `articleId`, `graphemeOffset`, `savedAt`
    - `src/content/schema.ts` — `CanonicalArticle` shape (`provenance.title`, `provenance.author`, `provenance.sourceUrl`, `ingestionMeta?.source`, `tags`, `lang`, `blocks`)
    - `src/content/normalizeText.ts` — `normalizeText(article)` + `graphemeClusters` (the D-05 substrate for progress computation)
    - `08-RESEARCH.md` §Pattern 4 (L443-498 — ContinueReadingStrip + LibraryRow skeletons; FINISHED_THRESHOLD = 0.98 recommendation at L498)
    - `08-UI-SPEC.md` §LibraryRow (L113-126), §SourceBadge (L127-141), §ContinueReadingStrip (L143-155), §Copywriting source-badge table (L264-268)
    - `08-PATTERNS.md` §LibraryRow (L212-228), §ContinueReadingStrip (L232-237)
  </read_first>
  <action>
    Create `src/ingestion/library/SourceBadge.tsx` per UI-SPEC §SourceBadge (D8-02). Renders ONE of five text variants based on `article.ingestionMeta?.source ?? "fixture"`: `"fixture"` → "Sample"; `"url"` → "Web"; `"paste"` → "Pasted"; `"markdown"` → "Markdown"; `"html-upload"` → "HTML file". A `<p className="meta source-badge">` (14px/400/1.45 `var(--font-ui)` `var(--ink-soft)` per UI-SPEC §Typography + §Color — no border, no fill, no icon). When `article.provenance.sourceUrl` is present (url + paste variants), render the text inside an `<a href={sourceUrl} rel="noreferrer noopener" target="_blank">` so LIB-05's "reach the original source" is satisfied at the row level (mirrors the existing ArticleView "Originally published at {domain}" link). Markdown + html-upload + fixture variants render plain text (no source URL — D8-17 markdown has no canonical URL). The exhaustive switch has no default — the five variants ARE exhaustive (the enum is closed).

    Create `src/ingestion/library/LibraryRow.tsx` per UI-SPEC §LibraryRow + RESEARCH §Pattern 4 L473-494. A `<li className="library-row">` extending the FixtureList `<li>` markup byte-stably (Pitfall 8-5): the `<h2 id={`title-${a.id}`}>{a.provenance.title}</h2>`, `<p className="meta">{author}</p>`, and `<a href={`#/article/${a.id}`} aria-labelledby={`title-${a.id}`}>Open article</a>` structure is preserved verbatim. Add as SIBLINGS inside the `<li>` (not structural changes): `<SourceBadge article={a} />`, a `<ProgressHairline progress={ratio} />` when `0 < ratio < 0.98` (D8-11), a `<p className="meta finished-mark">● Finished</p>` when `ratio >= 0.98` (D8-12 — filled-circle glyph + text for forced-colors safety per UI-SPEC §Interaction 10), and a `<ul className="library-row-tags">` of display-only `<span className="tag-chip tag-chip-readonly">` chips ONLY when `article.tags?.length > 0` (D8-05 — display-only on the row, no edit affordance). Compute `ratio` via `Math.min(1, location.graphemeOffset / total)` where `total = graphemeClusters(normalizeText(article), article.lang).length` (D-05 substrate; reuse the existing `normalizeText` + `graphemeClusters` exports — do NOT fork them per Pitfall 2). The remove trigger button (trash glyph + visually-hidden "Remove {title} from library" aria-label) is wired in Plan 04 when RemoveConfirm lands; for now, accept an optional `onRemove?: () => void` prop and render the button only when the prop is provided (so Plan 04 can wire it without re-editing this file). Default to no remove button in Plan 03.

    Create `src/ingestion/library/ContinueReadingStrip.tsx` per UI-SPEC §ContinueReadingStrip + RESEARCH §Pattern 4 L451-466. Export `FINISHED_THRESHOLD = 0.98` as a named constant (RESEARCH L498 recommendation). A `<section className="continue-reading-strip" aria-labelledby="cr-heading">` rendered ONLY when there is ≥1 unfinished article. Compute the set: `const all = await listArticles(); const locations = await loadAllLocations();` annotate each article with `{ lastOpened: loc?.savedAt ?? null, progress: loc ? loc.graphemeOffset / total : 0 }`; filter by `lastOpened !== null && progress < FINISHED_THRESHOLD`; sort by `savedAt` descending (most-recently-opened first — D8-10); slice to cap 3 (D8-09 calm lower end). Render `<h2 id="cr-heading">Continue reading</h2>` + `<ul className="continue-reading">` of up to 3 `<li>` cards each containing: title link (`<a href={`#/article/${a.id}`}>`), author `.meta` (when present), and `<ProgressHairline progress={ratio}>`. NO source badge, NO remove affordance, NO tag chips on the strip (the strip is spare and focused on the resume gesture per UI-SPEC §ContinueReadingStrip). Wrap the loader in a small `useEffect` + `useState` pattern (loading/ready); render `null` while loading or when the unfinished set is empty.

    Run `npm run test:unit -- --run` and `npm run build` to verify zero regressions.
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/ingestion/library/SourceBadge.tsx` renders all five variants (Sample/Web/Pasted/Markdown/HTML file) and the sourceUrl link when present (LIB-05)
    - `grep -c 'source-badge' src/ingestion/library/SourceBadge.tsx` returns ≥ 1 (the className hook)
    - `src/ingestion/library/LibraryRow.tsx` preserves the byte-stable `<h2 id={`title-${id}`}>` + `<a href={`#/article/${id}`} aria-labelledby={`title-${id}`}>Open article</a>` structure (Pitfall 8-5 — verify by reading)
    - `grep -c 'ProgressHairline' src/ingestion/library/LibraryRow.tsx` returns ≥ 1 (the per-row hairline — D8-11)
    - `grep -c 'finished-mark' src/ingestion/library/LibraryRow.tsx` returns ≥ 1 (the Finished mark — D8-12)
    - `src/ingestion/library/ContinueReadingStrip.tsx` exports `FINISHED_THRESHOLD` and renders `<h2 id="cr-heading">Continue reading</h2>`
    - `grep -c 'loadAllLocations' src/ingestion/library/ContinueReadingStrip.tsx` returns ≥ 1 (the location substrate)
    - `grep -c 'FINISHED_THRESHOLD' src/ingestion/library/ContinueReadingStrip.tsx` returns ≥ 2 (the constant + its use in the filter)
    - The ContinueReadingStrip renders null when there are zero unfinished articles (spare chrome)
    - `npm run build` exits 0 (TypeScript type-check passes)
    - `npm run test:unit -- --run` exits 0 (zero regressions)
  </acceptance_criteria>
  <done>
    SourceBadge carries the per-row source indicator (D8-02) with the LIB-05 source link. LibraryRow preserves the v1.0 row structure byte-stably (Pitfall 8-5) and adds source badge + progress hairline + finished mark + display-only tag chips. ContinueReadingStrip derives the 1-3 most-recently-opened unfinished articles from loadAllLocations (D8-09, D8-10) with the 0.98 finished threshold (D8-12). All builds + unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: LibraryView + App.tsx swap + app.css + FixtureList removal</name>
  <files>src/ingestion/library/LibraryView.tsx, src/App.tsx, src/routes/FixtureList.tsx, src/app.css</files>
  <read_first>
    - `src/routes/FixtureList.tsx` — the FULL file (79 lines). LibraryView is a superset; copy the `<main id="main">` + `<h1>Saved articles</h1>` + `<IngestControl />` + `.status` region + `<ul>` structure verbatim per Pitfall 8-5
    - `src/App.tsx` L22 (the FixtureList import) + L191-192 (the list-view JSX) — the one-line swap targets per RESEARCH §Pattern 5
    - `src/app.css` — the existing `:root` tokens + `.fixture-list` responsive grid rule (LibraryView renames to `.library-list` with identical CSS per UI-SPEC §LibraryView L111)
    - `08-UI-SPEC.md` §LibraryView (L99-112 — DOM order structure), §Layout Contract (L317-376 — the full layout diagram), §Copywriting (L237-260 — all copy verbatim), §Regression Targets (L393-408 — the byte-stable elements table)
    - `08-RESEARCH.md` §Pattern 5 (L500-521 — the App.tsx swap + SC#1 regression guard)
    - `08-PATTERNS.md` §LibraryView (L137-208) + §App.tsx (L504-517)
    - `tests/e2e/ingestion/happy-path.spec.ts` L93 — the `<h1>Saved articles</h1>` assertion (must still pass)
    - `tests/e2e/open-every-fixture.spec.ts` L65-69 — the per-fixture row-count assertion (must still pass)
  </read_first>
  <action>
    Create `src/ingestion/library/LibraryView.tsx` as a superset of FixtureList per UI-SPEC §LibraryView + Pitfall 8-5. Copy the FixtureList structure verbatim and extend. DOM order (UI-SPEC §LibraryView L103-111):
    1. `<main id="main">` wrapper (byte-stable id — skip-link target)
    2. `<h1>Saved articles</h1>` (byte-stable text — SC#1 regression target per UI-SPEC §Regression Targets + happy-path.spec.ts L93)
    3. `<IngestControl />` (existing — extended in Plan 04)
    4. `<div className="status" role="status" aria-live="polite" aria-atomic="true">` for list loading/error (byte-stable from FixtureList L45-53; copy the "Opening article…" + "Couldn't open this article." copy verbatim)
    5. `<ContinueReadingStrip />` (mounted only when ≥1 unfinished article — the component returns null otherwise)
    6. `<LibrarySearch query={query} onQueryChange={setQuery} />` + `<TagFilter tags={allTags} activeTag={activeTag} onSelect={setActiveTag} />`
    7. `<ul className="library-list">` of `<LibraryRow>` items (one per article from `filterLibrary(listArticles result, { query, activeTag })`)
    8. Empty-state block when `items.length === 0 && status === "ready"`: `<h2>Your library is empty</h2>` + `<p>Paste a URL or upload a file to begin.</p>` (D8-04 — calm voice pointing at Add)

    State: `items` (CanonicalArticle[]), `status` ("loading" | "ready" | "error"), `query` (string), `activeTag` (string | null), `allTags` (string[]), `locations` (Map<articleId, LocationRecord> for per-row hairline + finished mark). Use `useEffect` to load `listArticles()` + `loadAllLocations()` + `loadAllTags()` on mount (parallel `Promise.all`). Default sort = recently-added descending (D8-03 — `items.sort((a, b) => (a.addedAt ?? "").localeCompare(b.addedAt ?? ""))` reversed; fixtures with no `addedAt` sort to the bottom). Re-derive `allTags` whenever items change (tags may have been edited in ArticleView).

    Edit `src/App.tsx` (per RESEARCH §Pattern 5 L506-517): swap the import `import { FixtureList } from "./routes/FixtureList"` → `import { LibraryView } from "./ingestion/library/LibraryView"` (L22 region) and swap the JSX `<FixtureList />` → `<LibraryView />` (L191-192 region). Everything else (parseHash, hashchange listener, Gap 3 fragment guard, View type, ArticleView branch) stays byte-unchanged.

    Remove or rename `src/routes/FixtureList.tsx`: rename to `src/routes/LegacyFixtureList.tsx` (preserved for reference) OR delete it. The import in App.tsx no longer references it. The existing `tests/e2e/open-every-fixture.spec.ts` test name stays valid because it asserts DOM structure (not the component file name) per UI-SPEC §Regression Targets L408.

    Add the library CSS to `src/app.css` (additive — new selectors only; never re-declare `:root` tokens). Per UI-SPEC §LibraryView + §Component Inventory + §Layout Contract:
    - `.library-list` — byte-identical to the existing `.fixture-list` responsive grid (1 column <640px, 2 columns 640-1024px, 3 columns ≥1024px). Reference the existing `.fixture-list` rule and copy it with the new class name.
    - `.library-row` — extends `.fixture-list li` geometry: `--surface-raised` background + 1px `--hairline` border + 4px radius + `--space-lg` padding + 44px min hit area on the title link.
    - `.source-badge` — `.meta` typography (14px/400/1.45 `var(--font-ui)` `var(--ink-soft)`); no border, no fill. The link variant inherits the global `a { color: var(--accent) }` rule.
    - `.continue-reading-strip` — `<section>` with `<h2>` (22px/600); `.continue-reading` is single-column (`grid-template-columns: 1fr` — the strip never widens per UI-SPEC L154). Hairline rule below: `1px solid var(--hairline)` + `--space-lg` margin-block.
    - `.library-search` — `<form>` with `<input type="search">` at `--space-md` width 100%, 1px `--hairline` border, 4px radius, `--space-sm` padding, 44px min-height. `:focus-visible` inherits the global 2px ring.
    - `.tag-filter` — `<fieldset>` with chips wrapping (flex-wrap, `gap: var(--space-sm)`). `.tag-chip` — `--surface-raised` background, 1px `--hairline` border, 4px radius, `--space-xs` block + `--space-sm` inline padding, 14px/400/1.45 `var(--font-ui)`, 44px min-height, `--ink-soft` text at rest; `.tag-chip[aria-pressed="true"]` → `--accent` border + `--accent` text. `.tag-chip-readonly` (display variant) — same geometry minus the `aria-pressed` interactivity.
    - `.finished-mark` — `.meta` typography + a filled-circle glyph (●) + " Finished" text; `--ink-soft` color.
    - `.library-row-tags` — `<ul>` inline, `gap: var(--space-xs)`, display flex.

    Run `npm run test:unit -- --run` (zero regressions) and `npm run build` (type-check). The existing `tests/e2e/ingestion/happy-path.spec.ts` + `tests/e2e/open-every-fixture.spec.ts` e2e tests are verified in Plan 05's full-suite run (they should pass unchanged because the heading + row structure + Open-article link are byte-stable per Pitfall 8-5).
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/ingestion/library/LibraryView.tsx` exists and is the default route component
    - `grep -c 'Saved articles' src/ingestion/library/LibraryView.tsx` returns ≥ 1 (the byte-stable h1 — SC#1)
    - `grep -c 'library-list' src/ingestion/library/LibraryView.tsx` returns ≥ 1 (the renamed ul class)
    - `grep -c 'ContinueReadingStrip' src/ingestion/library/LibraryView.tsx` returns ≥ 1
    - `grep -c 'LibrarySearch' src/ingestion/library/LibraryView.tsx` returns ≥ 1
    - `grep -c 'TagFilter' src/ingestion/library/LibraryView.tsx` returns ≥ 1
    - `grep -c 'Your library is empty' src/ingestion/library/LibraryView.tsx` returns ≥ 1 (D8-04 empty state)
    - `grep -c 'LibraryView' src/App.tsx` returns ≥ 2 (import + JSX)
    - `grep -c 'FixtureList' src/App.tsx` returns 0 (the old import is fully removed)
    - `src/routes/FixtureList.tsx` is either deleted or renamed to `LegacyFixtureList.tsx` (no live import references it)
    - `grep -c 'library-list' src/app.css` returns ≥ 1 (the new responsive-grid rule)
    - `grep -c 'tag-chip' src/app.css` returns ≥ 2 (the chip base + the active/readonly variants)
    - `grep -c 'continue-reading' src/app.css` returns ≥ 1 (the strip rule)
    - `npm run build` exits 0
    - `npm run test:unit -- --run` exits 0 (zero regressions)
  </acceptance_criteria>
  <done>
    LibraryView ships as the default route, composing ContinueReadingStrip + LibrarySearch + TagFilter + the library list of LibraryRow items. App.tsx is a one-line swap. FixtureList is removed/renamed with no live imports. The library CSS is additive (new selectors only; `:root` tokens untouched). The byte-stable regression targets (h1 heading, ul/li/a structure, h2 id + aria-labelledby) are preserved per Pitfall 8-5. Zero Phase 7 unit-test regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compositeLibraryRepository → LibraryView | Article rows cross here; already Zod-validated on read (STATE-04 via DexieLibrarySource.list) |
| locationStore → ContinueReadingStrip + LibraryRow | Location rows cross here; loadAllLocations Zod-validates per row |
| tagsStore → TagFilter | Tag names cross here; derived from validated article rows (no separate trust surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-10 | Tampering / XSS | Tag chip rendering (tag name → DOM) | mitigate | Tag names render as React text children (`<span>{name}</span>`); React escapes text by default. No HTML parsing, no dangerouslySetInnerHTML (repo-wide lint gate). |
| T-8-11 | Info Disclosure | Stale location row leaks cross-article progress | mitigate | `loadAllLocations` Zod-validates every row (STATE-04); the articleId-keyed Map only matches locations to articles that exist in the composite library list. |
| T-8-12 | Tampering | SourceBadge link href injection | mitigate | The sourceUrl comes from `article.provenance.sourceUrl` which is `httpUrl`-refined at ArticleSchema parse time (only http(s) URLs survive). The `<a rel="noreferrer noopener" target="_blank">` prevents reverse-tabnabbing. |
| T-8-13 | Spoofing | v1.0 fixture appears as ingested (or vice versa) | accept | The source badge derives from `article.ingestionMeta?.source ?? "fixture"` — the field is set at ingest time and Zod-validated. A fixture has no ingestionMeta (hydrates to "fixture"); an ingested article always carries it. No spoofing surface in Plan 03. |
</threat_model>

<verification>
- `npm run test:unit -- --run tests/unit/library-search.test.ts` exits 0 (filter helper coverage)
- `npm run test:unit -- --run` exits 0 (zero Phase 7 regressions)
- `npm run build` exits 0 (TypeScript + Vite build passes)
- The SC#1 regression targets (h1 heading, ul/li/a structure) are byte-stable — verified by Plan 05's v1-regression.spec.ts + open-every-fixture.spec.ts e2e run
</verification>

<success_criteria>
- Library is the default route at #/ replacing FixtureList (LIB-01)
- Fixtures badged source:"fixture" alongside ingested articles with per-row source indicators (LIB-05, D8-02)
- Search by title/author/domain/tag works client-side (LIB-03, D8-06)
- Single-tag chip filter with auto-prune works (LIB-04 UI side, D8-07/D8-08)
- Per-row progress hairline + Continue-reading strip derive from loadAllLocations (LIB-06, D8-09/D8-10/D8-11)
- Finished articles leave the strip, marked in the list (D8-12)
- Zero v1.0 e2e regressions (SC#1 — verified in Plan 05)
</success_criteria>

<output>
Create `.planning/phases/08-markdown-pipeline-and-personal-library/08-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Plan 03 symbols:**
- `src/ingestion/library/libraryFilter.ts`: `filterLibrary(articles, filter)`, `domainOf(url)`, `LibraryFilter` interface
- `src/ingestion/library/LibrarySearch.tsx`: `LibrarySearch` component (props: `query`, `onQueryChange`)
- `src/ingestion/library/TagFilter.tsx`: `TagFilter` component (props: `tags`, `activeTag`, `onSelect`)
- `src/ingestion/library/SourceBadge.tsx`: `SourceBadge` component (props: `article`)
- `src/ingestion/library/LibraryRow.tsx`: `LibraryRow` component (props: `article`, `location?`, `onRemove?`)
- `src/ingestion/library/ContinueReadingStrip.tsx`: `ContinueReadingStrip` component, `FINISHED_THRESHOLD = 0.98` export
- `src/ingestion/library/LibraryView.tsx`: `LibraryView` component (default route, replaces FixtureList)
- `src/App.tsx`: swapped import (`FixtureList` → `LibraryView`) + JSX
- `src/routes/FixtureList.tsx`: removed or renamed to `LegacyFixtureList.tsx`
- `src/app.css`: new selectors `.library-list`, `.library-row`, `.source-badge`, `.continue-reading-strip`, `.continue-reading`, `.library-search`, `.tag-filter`, `.tag-chip`, `.tag-chip-readonly`, `.finished-mark`, `.library-row-tags`
- `tests/unit/library-search.test.ts`: filterLibrary + domainOf coverage
