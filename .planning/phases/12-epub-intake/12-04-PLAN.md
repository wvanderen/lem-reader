---
phase: 12-epub-intake
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - server/ingest.ts
  - dev-server/ingest-middleware.ts
  - tests/unit/server/epub-fixtures.ts
  - tests/unit/server/ingest-epub.spec.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "The orchestrator's fifth Stage-1 branch runs epubToBooks, derives book id epub-<shortHash(b64)> and chapter ids epub-<shortHash(b64)-cNN, and runs stages 2+ (ArticleSchema.parse → assertRoundTripAnchor → deriveConfidence → stamp) PER CHAPTER unchanged (SC#4 anchor gate per chapter)"
    - "A chapter failing extraction or the round-trip anchor gate is skipped and counted — the rest of the book enters; only ZERO admitted chapters refuses with epub-empty (D12-11)"
    - "The book envelope carries chapterArticleIds (admitted, ordered), skippedChapterCount, originalFileHash, source epub-upload, addedAt — and parses through BookSchema"
    - "Identical EPUB bytes produce identical book + chapter ids on re-ingest (content-hash determinism, the dedupe-refuse foundation)"
    - "The middleware transport cap derives from max(PDF_MAX_BYTES, EPUB_MAX_BYTES); the pre-read 413 reason follows the format query hint and the post-read reason branches on the parsed body key (Pitfall 2 closed — an over-cap EPUB never shows PDF copy)"
    - "Every admitted chapter's provenance carries the book's authors/publishedDate and its own TOC-derived title with the doubled-title consume applied"
  artifacts:
    - path: "server/ingest.ts"
      provides: "the fifth Stage-1 branch + per-chapter stages 2+ loop + skip/disclose accounting + book assembly"
      contains: "epubToBooks"
    - path: "dev-server/ingest-middleware.ts"
      provides: "cap-coupling fix — max()-derived body cap + format-aware 413 reasons"
      contains: "epub-too-large"
    - path: "tests/unit/server/ingest-epub.spec.ts"
      provides: "the server-side integration round-trip gate — happy path, TOC shapes, refusals, skip disclosure, determinism, caps"
      min_lines: 150
  key_links:
    - from: "server/ingest.ts"
      to: "server/epubToBooks.ts"
      via: "the fifth Stage-1 branch calls the adapter and composes its ChapterDrafts through the unchanged stages 2+"
      pattern: "epubToBlocks|epubToBooks"
    - from: "server/ingest.ts"
      to: "src/content/schema.ts"
      via: "the book record parses through BookSchema before it crosses the response boundary"
      pattern: "BookSchema.parse"
    - from: "dev-server/ingest-middleware.ts"
      to: "server/limits.ts"
      via: "MAX_INGEST_BODY_BYTES now derives from the max of both format caps; refusal reasons branch pdf/epub"
      pattern: "MAX_INGEST_BODY_BYTES"
---

<objective>
Wire EPUB into the 7-stage orchestrator: the fifth Stage-1 branch (decode → cap re-check → epubToBooks → book/chapter ids → per-chapter stages 2+ with skip-and-disclose accounting → book envelope), plus the Pitfall 2 middleware cap-coupling fix, plus the server-side integration round-trip gate that proves the whole chain over the synthetic corpus before any UI lands.

Purpose: The orchestrator is where publisher intent (TOC titles), identity (content-hash ids), and honesty (anchor gate per chapter, skip disclosure, zero-admit refusal) become one typed response. The middleware fix closes a real wrong-copy bug class before e2e can pin it.
Output: ingest() accepts {epub, filename?} end-to-end at unit level; middleware format-aware; integration spec green.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@server/ingest.ts
@dev-server/ingest-middleware.ts
@server/epubToBooks.ts
@tests/unit/server/ingest-pdf.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fifth Stage-1 branch + per-chapter stages 2+ + book assembly + middleware fix</name>
  <files>server/ingest.ts, dev-server/ingest-middleware.ts</files>
  <read_first>server/ingest.ts (the pdf Stage-1 branch L264-296, the per-article stages ArticleSchema.parse L396 + assertRoundTripAnchor import L62-87 + deriveConfidence L409 + stamp L420-423, the title chain + consumeDuplicatedTitle L148-162/L349-367, the catch envelope L432-442, shortHash L94-96), dev-server/ingest-middleware.ts (refuseTooLarge L48-54 + both guards L97-117), server/epubToBooks.ts (the ChapterDraft contract from 12-02), src/content/schema.ts (BookSchema), src/ingestion/types.ts (EPUB_MAX_BYTES + the book ok-variant), server/limits.ts</read_first>
  <action>1. server/ingest.ts — add the fifth Stage-1 branch (the pdf branch is the template), keeping the 7-stage ordering byte-stable for existing formats:
   a. Branch on the {epub, filename?} variant: bytes = Buffer.from(b64, "base64"); decoded re-check bytes.byteLength > EPUB_MAX_BYTES → IngestionError("epub-too-large") (the third enforcement layer).
   b. const result = await epubToBooks(new Uint8Array(bytes)) (the adapter owns its timeout race); destructure {bookMeta, chapters, skippedCount, originalFileHash}.
   c. Identity: bookBase = `epub-${shortHash(b64)}`; admitted chapter i gets id `${bookBase}-c${String(i).padStart(2, "0")}` (i = position within the ADMITTED list — Pitfall 10: skipped chapters never renumber).
   d. Per-chapter loop running the UNCHANGED stages 2+ on each ChapterDraft: ArticleSchema.parse → assertRoundTripAnchor (the 5-offset gate, imported unchanged — SC#4) → deriveConfidence → stamp. Build each article with: lang = draft.lang; provenance {title: draft.title, author: bookMeta.authors.join(", ") when non-empty else undefined, publishedAt: bookMeta.publishedDate ?? undefined}; ingestionMeta {source: "epub-chapter", origin: "upload", originalHtmlHash: draft.sourceHtmlHash, extractionConfidence, extractionWarnings, bookId: bookBase, chapterIndex: i}. Apply consumeDuplicatedTitle per chapter when its first heading block matches the chapter title (the D11-09 EPUB analog — bodies start at h2, the provenance header renders the title). Wrap each chapter in try/catch: a chapter failing parse/anchor/confidence increments skipped (D12-11) and is OMITTED — never a whole-book failure.
   e. Zero admitted chapters → IngestionError("epub-empty").
   f. Assemble the book record: {id: bookBase, title: bookMeta.title || stripEpubExtension(filename) || "Book" (the short EPUB title chain — OPF dc:title is spec-REQUIRED so the chain is short), authors: bookMeta.authors, language: bookMeta.language, chapterArticleIds: admitted ids in order, publisher/publishedDate/identifier when present, skippedChapterCount: total skipped (adapter + stages), source: "epub-upload", originalFileHash, tags: [] (books start untagged — D12-04 tagging is a reader act), addedAt: new Date().toISOString()}; BookSchema.parse it before it crosses the boundary.
   g. Return the book ok-variant envelope: {ok: true, book, articles: admittedArticles, skippedCount: book.skippedChapterCount}.
   2. dev-server/ingest-middleware.ts — the Pitfall 2 fix:
   a. MAX_INGEST_BODY_BYTES is now imported (already derived in 12-01 from Math.max(PDF_MAX_BYTES, EPUB_MAX_BYTES)) — remove any local re-derivation.
   b. Pre-read content-length guard (fires BEFORE readBody attaches a listener — the pulled-flag proof stays): refuse 413 with reason selected from the URL query hint — req.url containing format=epub → "epub-too-large", otherwise "pdf-too-large" (the hint selects COPY only; enforcement remains content-length-based and body-agnostic).
   c. Post-read byte re-check (chunked/lying content-length path): parse the body shape and branch — the epub key present → "epub-too-large"; the pdf key → "pdf-too-large"; the existing behavior for text variants unchanged.
   Keep the two-guard structure byte-identical otherwise.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/server/ingest-pdf.spec.ts tests/unit/server/ingest-adapter.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/ingest.ts contains an epub branch invoking epubToBooks, the -c padded chapter id template, a per-chapter try/catch incrementing a skipped counter, the zero-admit epub-empty throw, and BookSchema.parse on the assembled record
    - assertRoundTripAnchor is called per chapter with NO local fork (import unchanged from its existing module)
    - The middleware contains both reason branches: a format=epub query check on the pre-read path and a parsed-body-key check on the post-read path; the literal pdf-too-large no longer fires unconditionally for over-cap requests
    - Existing orchestrator specs (ingest-pdf, ingest-adapter) stay green — the 7-stage ordering for url/paste/markdown/pdf is untouched
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The server accepts EPUB end-to-end with per-chapter gating, honest skip accounting, deterministic ids, and format-correct 413 copy; existing formats regress nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Integration round-trip gate (ingest-epub.spec.ts) + mixedAdmission fixture</name>
  <files>tests/unit/server/epub-fixtures.ts, tests/unit/server/ingest-epub.spec.ts</files>
  <read_first>tests/unit/server/ingest-pdf.spec.ts (the integration gate structure — how ingest() is invoked server-side, envelope assertions, refusal patterns), tests/unit/server/epub-fixtures.ts (current builder set — extend, do not fork), server/ingest.ts (Task 1 state), 12-RESEARCH.md §Validation Architecture rows ING-05 (parser/DRM/security/honesty)</read_first>
  <action>1. tests/unit/server/epub-fixtures.ts — add ONE builder: mixedAdmissionBook() — 2 readerable chapters + 1 pure-image plate chapter between them → adapter/stage admission yields 2 admitted + skippedCount 1 (if 12-01 already shipped mixedAdmissionBook, instead add anchorGateFailBook(): a readerable-looking chapter whose normalized text cannot round-trip a TextQuoteSelector — e.g. whitespace-collapse-hostile repeated separator runs — so the per-chapter anchor gate skip path is exercised end-to-end; keep whichever builder is absent).
  2. tests/unit/server/ingest-epub.spec.ts (the 11-03 ingest-pdf.spec pattern — call the exported ingest() directly with base64 from the fixture builders):
   - Happy path (validBookEpub3): ok envelope; articles.length 4; every article parses ArticleSchema; every ingestionMeta carries bookId + chapterIndex + source epub-chapter; book.chapterArticleIds matches the -c00..-c03 id sequence in order; book.title/authors/language from OPF; provenance.author joined.
   - Determinism: ingest the same b64 twice → identical book id and chapter ids (content-hash stability — the dedupe-refuse foundation).
   - TOC shapes: publisherSplitBook → 3 articles with chapter 1 containing both split documents' text; frontMatterBook → 3 units, leading unit first; degenerateTocBook → 4; ncxOnlyBook → 3; oebpsNestedBook → fallbackUsed false surfaced via chapter count matching the TOC (not the spine).
   - Skip disclosure (mixedAdmissionBook or anchorGateFailBook): ok envelope with skippedCount 1 and the remaining chapters admitted with UNRENUMBERED stable ids (admitted chapters keep their admitted-order -cNN sequence).
   - Refusals: drmAdeptBook/drmLcpBook/drmUnknownAlgBook → {ok: false, reason: "epub-protected"}; corruptNotEpub/missingContainerBook/entityBombOpf/protoPollutionOpf/zipSlipBook/bombEntryBook → "epub-unreadable"; emptyBook → "epub-empty".
   - Caps: a fixture padded past EPUB_MAX_BYTES (append a benign extra zip entry or truncate/pad the b64 payload to exceed the decoded cap WITHOUT invalidating the zip — simplest: wrap validBookEpub3 bytes inside a larger invalid container by concatenating trailing garbage AFTER a valid zip is still parseable; if that route is fragile, pad the base64 with a leading whitespace-free oversized chapter entry and assert the decoded re-check fires) → "epub-too-large". The orchestrator is stateless — refusal responses are the only observable (zero-side-effect discipline).</action>
  <verify>
    <automated>npx vitest run tests/unit/server/ingest-epub.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/server/ingest-epub.spec.ts exists and every case drives the real ingest() (no adapter-only shortcuts for the happy/refusal paths)
    - The happy-path assertion set includes: articles.length === 4, chapterArticleIds ending -c00 through -c03 in order, per-article ingestionMeta.bookId + chapterIndex present, source "epub-chapter"
    - The determinism case asserts deep-equal ids across two ingests of identical bytes
    - All seven hostile fixtures assert their typed reasons; the skip-disclosure case asserts skippedCount 1
    - An over-cap epub payload asserts reason "epub-too-large" from the orchestrator re-check
    - npx vitest run tests/unit/server/ingest-epub.spec.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The full server chain — decode, cap, adapter, TOC-merge, per-chapter anchor gate, assembly — is proven green over the synthetic corpus at the orchestrator boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request body → orchestrator | base64-decoded EPUB bytes re-checked against EPUB_MAX_BYTES after decode (third enforcement layer) |
| orchestrator → response | book + articles validated through BookSchema/ArticleSchema before crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | DoS | oversized upload bypassing the client | mitigate | three-point enforcement completed here: orchestrator decoded re-check throws epub-too-large (client picker + middleware preceded it) |
| T-12-13 | Tampering | book envelope forgery | mitigate | BookSchema.parse at the orchestrator boundary + client re-parse (12-03) — Zod twice |
| T-12-14 | DoS | per-chapter work amplification | mitigate | adapter timeout race + chapter cap carried through; skip accounting bounds wasted stage work |
| T-12-07 | Repudiation/Legal | DRM handling posture | mitigate | refusal passes the typed reason through the existing catch envelope verbatim; no orchestrator DRM-specific code, no marker bytes logged |
</threat_model>

<verification>
Integration spec green; existing pdf/adapter orchestrator specs green (no 7-stage regression); tsc clean; middleware reasons branch correctly (asserted again at e2e in 12-06).
</verification>

<success_criteria>
ingest({epub, filename?}) returns the book envelope with anchor-gated, deterministically-id'd, TOC-titled chapters; per-chapter honesty (skip + disclose, zero-admit refusal) proven; middleware 413 copy is format-correct; every prior intake format byte-stable.
</success_criteria>

## Artifacts this phase produces

- The fifth Stage-1 branch in server/ingest.ts (epub decode → cap → epubToBooks → ids → per-chapter stages 2+ → book envelope)
- Book id convention `epub-<shortHash(b64)>` + chapter id convention `epub-<shortHash(b64)>-c<NN>` (admitted-order, zero-padded)
- Format-aware middleware refusal reasons (pre-read query hint + post-read body-key branch)
- tests/unit/server/ingest-epub.spec.ts (integration round-trip gate); the mixedAdmissionBook/anchorGateFailBook fixture builder extension

<output>
Create `.planning/phases/12-epub-intake/12-04-SUMMARY.md` when done
</output>
