---
phase: 12-epub-intake
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - server/epubToBooks.ts
  - tests/unit/server/epub-to-books.spec.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "epubToBooks is a pure adapter: bytes in, {bookMeta, chapters, skippedCount, originalFileHash} or typed IngestionError out — no I/O beyond the bytes, no Dexie, no React (SC#4 adapter isolation, D12-13/D12-14)"
    - "DRM refusal is allowlist-based and detection-only: license.lcpl or rights.xml presence refuses; any encryption.xml algorithm other than the OCF font-obfuscation URI refuses; font-obfuscation-only books pass (D12 carries DRM honesty)"
    - "Every archive entry passes isSafeEntryName before any byte is used; over-cap entries are filtered before inflation (Zip Slip + bomb discipline, Phase 9 gates reused)"
    - "TOC-driven chapters: spine items mapping to the same top-level TOC entry merge into one article; fallback one-per-spine-item fires only when fewer than 2 TOC entries resolve (D12-09)"
    - "Non-content spine items (cover, nav document, pure-image plates) are skipped — not failures; front matter with real text stays (D12-10)"
    - "Chapter XHTML normalizes through sanitizeExtractedHtml + htmlToBlocks with NO Readability pass; every figure block downgrades to unsupported (D12-16 text-first; remote-src IP-leak closed)"
    - "A book whose spine yields zero readerable documents refuses with epub-empty; href normalization is one shared function used by both TOC and manifest sides"
  artifacts:
    - path: "server/epubToBooks.ts"
      provides: "the swappable EPUB parser adapter — unzip, DRM gate, container/OPF/nav/NCX parse, TOC-merge, per-chapter normalization, EPUB_THRESHOLDS"
      exports: ["epubToBooks", "EPUB_THRESHOLDS"]
      min_lines: 300
    - path: "tests/unit/server/epub-to-books.spec.ts"
      provides: "the adapter unit matrix over the synthetic corpus — parse/TOC-merge/DRM/slip/bomb/caps/entity/proto/figure-downgrade/admission"
      min_lines: 250
  key_links:
    - from: "server/epubToBooks.ts"
      to: "server/htmlToBlocks.ts"
      via: "per-chapter sanitizeExtractedHtml + htmlToBlocks call — the shared ING-07 boundary, Readability skipped"
      pattern: "sanitizeExtractedHtml"
    - from: "server/epubToBooks.ts"
      to: "src/portability/zipSlip.ts"
      via: "isSafeEntryName on EVERY entry key before use — the Phase 9 gate reused, never re-derived"
      pattern: "isSafeEntryName"
    - from: "server/epubToBooks.ts"
      to: "server/limits.ts"
      via: "EPUB_MAX_ENTRY_BYTES filter + EPUB_MAX_CHAPTERS cap + EPUB_EXTRACTION_TIMEOUT_MS race"
      pattern: "EPUB_MAX_ENTRY_BYTES"
    - from: "tests/unit/server/epub-to-books.spec.ts"
      to: "tests/unit/server/epub-fixtures.ts"
      via: "every spec case consumes a named fixture builder"
      pattern: "epub-fixtures"
---

<objective>
Build server/epubToBooks.ts — the pure, swappable EPUB parser adapter (SC#4): fflate unzip with bomb filter + Zip Slip gate (D12-13), the DRM allowlist refusal, the namespaced container.xml → OPF → nav|NCX parse chain via fast-xml-parser (D12-14), the TOC→spine-range chapter merge with degenerate/front-matter/fallback handling (D12-09), non-content skipping via the EPUB-adapted readerability algebra (D12-10), and per-chapter normalization through the EXISTING sanitize + htmlToBlocks path with figure downgrade (D12-16). The two genuinely novel logics in this phase (TOC-merge, DRM allowlist) have no in-tree analog — they are built from the spec-verified shapes in 12-RESEARCH §Code Examples.

Purpose: This is the only new server-side parsing surface in the phase; everything downstream (orchestrator branch, client save, library grouping) composes its output contract.
Output: The adapter + its full unit matrix green over the 12-01 synthetic corpus.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@server/pdfToBlocks.ts
@server/htmlToBlocks.ts
@server/limits.ts
@server/errors.ts
@src/portability/zipSlip.ts
@tests/unit/server/epub-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive layer + DRM gate + container/OPF/nav/NCX parsing</name>
  <files>server/epubToBooks.ts, tests/unit/server/epub-to-books.spec.ts</files>
  <read_first>server/pdfToBlocks.ts (adapter anatomy: header comment, thresholds object, typed error mapping, timeout race, pure exported helpers), server/htmlToBlocks.ts (sanitizeExtractedHtml + the walk contract), server/errors.ts (IngestionError), server/limits.ts, src/portability/zipSlip.ts, src/portability/ExportImportService.ts L145-194 (unzipSync + filter discipline), tests/unit/server/epub-fixtures.ts, 12-RESEARCH.md §Code Examples L434-558 + §Architecture Patterns 2-3</read_first>
  <action>Create server/epubToBooks.ts with the pdfToBlocks file anatomy (header comment naming plan/decision lineage + the server-only dependency boundary: fflate and the XML parser stay behind /server imports — only Block/InlineRun types cross; do NOT name the rejected vendor renderer package in source comments, cite "Pitfall 6 renderer rejection" instead). Export EPUB_THRESHOLDS = { minChapterBlocks: 3, tocMergeMinEntries: 2, maxNestedXmlTags: 40 } as const (the calibration harness pins against this object — the PDF_THRESHOLDS discipline). Implement, in order:
  1. Timeout race — withEpubTimeout(op) wrapping the whole parse in Promise.race([op, EPUB_EXTRACTION_TIMEOUT_MS timer]) with clearTimeout in finally (the withPdfDocument pattern); timeout → IngestionError("epub-unreadable") with a message naming the timeout.
  2. Unzip — fflate unzipSync(bytes, { filter: (f) => f.originalSize <= EPUB_MAX_ENTRY_BYTES }) — over-cap entries are skipped BEFORE inflation (Phase 9 bomb discipline); a subsequently-missing required entry surfaces naturally as epub-unreadable.
  3. Zip Slip gate — for EVERY entry key: isSafeEntryName(name) (imported from ../src/portability/zipSlip — never re-derived); any violation → IngestionError("epub-unreadable"). The mimetype entry is tolerated (presence + value checked only as a soft signal — do NOT refuse on a missing/nonconforming mimetype; tolerant like URL extraction; corrupt zips fail at unzipSync).
  4. DRM gate (12-RESEARCH Pattern 3, order matters — cheapest checks first, BEFORE any chapter work): entries containing META-INF/license.lcpl → epub-protected; META-INF/rights.xml → epub-protected; if META-INF/encryption.xml exists, parse it and refuse epub-protected unless EVERY EncryptedData EncryptionMethod Algorithm equals http://www.idpf.org/2008/embedding (the font-obfuscation URI — the ONLY legitimate non-DRM use, OCF §4.4.5; font entries are then ignored — we extract no fonts). Detection only: no decryption code, and never include marker byte content in error messages or logs.
  5. XML parser configuration — ONE module-level XMLParser factory carrying: ignoreAttributes: false, attributeNamePrefix: "@_", removeNSPrefix: true, processEntities: false (entity-expansion DoS guard — the library docs' own recommendation for untrusted XML), maxNestedTags: 40, isArray: (name) => ["item", "itemref", "creator", "navPoint", "rootfile", "EncryptedData", "nav", "ol", "li", "navLabel"]. Pin the key convention in fixtures: with removeNSPrefix:true, dc:title arrives as key "title" and the nav epub:type attribute as "@_type" (assumption A7 — assert both in unit tests).
  6. container.xml — parse META-INF/container.xml; read container.rootfiles.rootfile[0]["@_full-path"] → the OPF path relative to the zip root. Missing/unparseable/no rootfile → epub-unreadable.
  7. OPF — parse the OPF document; extract metadata (title, creator[] — all kept, role="aut" preferred ordering when present, language, publisher, date, identifier; title/language are spec-REQUIRED — a missing dc:title falls back to "Untitled book" rather than refusing), the manifest map id → {href, mediaType, properties}, and the ordered spine itemref[] {idref, linear}. bindings/tours/guide elements are ignored (deprecated, EPUB 3.3 Appendix A).
  8. href normalization — ONE exported helper normalizeEpubHref(rawHref, baseDir): decode %XX (try/catch — undecodable stays literal), resolve ../ and ./ against baseDir, strip the #fragment, posix-join to a zip-root-relative path; case-sensitive compare (spec paths ARE case-sensitive). Used by BOTH the TOC side (nav/NCX-relative hrefs) and the manifest side (OPF-relative hrefs) — Pitfall 1's whole mitigation.
  9. Chapter cap — assertChapterCap(count) throwing epub-too-large when over EPUB_MAX_CHAPTERS (mirrors assertPageCap).
  Then write tests/unit/server/epub-to-books.spec.ts describe "archive + DRM + manifests": corruptNotEpub/missingContainerBook/entityBombOpf/protoPollutionOpf → IngestionError reason epub-unreadable; drmAdeptBook/drmLcpBook/drmUnknownAlgBook → epub-protected with a message that does NOT contain marker bytes; fontObfuscatedBook → passes the DRM gate (reaches OPF parsing); zipSlipBook → epub-unreadable; bombEntryBook → epub-unreadable (filtered entry → missing required document); oebpsNestedBook → OPF located + manifest resolved through normalization; metadata extraction asserts title/authors/language/spine order on validBookEpub3; the removeNSPrefix key convention assertions (metadata key "title"; nav attribute "@_type"). Call the adapter through the exported pure entry (a parse-stage function is fine to expose for testability — pdfToBlocks precedent of pure exported helpers).</action>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-to-books.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/epubToBooks.ts exports epubToBooks and EPUB_THRESHOLDS and contains no import of dexie, react, or anything under src/ except type-only Block/InlineRun imports and the zipSlip helper
    - The DRM allowlist constant string http://www.idpf.org/2008/embedding appears exactly once as the pass condition; license.lcpl and rights.xml presence checks precede encryption.xml parsing
    - processEntities: false and maxNestedTags appear in the XMLParser configuration
    - Every DRM refusal test asserts the thrown reason and that the message matches /protected|DRM/i without containing XML marker content
    - entityBombOpf, protoPollutionOpf, corruptNotEpub, missingContainerBook, zipSlipBook, bombEntryBook all refuse with reason epub-unreadable
    - fontObfuscatedBook reaches OPF metadata parsing (test asserts extracted title)
    - npx vitest run tests/unit/server/epub-to-books.spec.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Archive + DRM + manifest layers green over the hostile fixture matrix; href normalization shared and pinned by the OEBPS case.</done>
</task>

<task type="auto">
  <name>Task 2: TOC-merge (D12-09) + chapter assembly + admission + output contract</name>
  <files>server/epubToBooks.ts, tests/unit/server/epub-to-books.spec.ts</files>
  <read_first>server/htmlToBlocks.ts (sanitizeExtractedHtml, htmlToBlocks signature, the L223-231 figure http(s) gate), 12-RESEARCH.md §Architecture Patterns 4-5 + §Common Pitfalls 4/5/10, tests/unit/server/epub-fixtures.ts, the Task 1 state of server/epubToBooks.ts</read_first>
  <action>Complete the adapter (12-RESEARCH Pattern 4 + Pattern 5):
  1. Navigation resolution — EPUB 3: find the manifest item whose properties contains "nav"; parse that document with the shared XMLParser; locate the single nav element whose "@_type" is "toc"; flatten its ol>li>a hierarchy to ordered {label, href, depth} (top-level li = depth 1; nested ol increments depth). A nav document that fails XML parsing falls through to the NCX path (tolerated — malformed XHTML nav is common). EPUB 2 / fallback: spine[@toc] idref → the application/x-dtbncx+xml manifest item → parse the NCX → flatten navMap>navPoint recursively ({label = navLabel text, href = content@src, depth by nesting}). EPUB 3 nav is preferred over NCX whenever both exist (EPUB 3.3 §5.9.5); NCX-only books use NCX; neither → spine fallback.
  2. Chapter partition (D12-09) — keep top-level (depth-1) TOC entries as chapter units; deeper entries are in-chapter sections (Pitfall 4). Degenerate case: exactly one depth-1 entry → descend one level and use its depth-2 children instead. Resolve each unit's normalized href → manifest id → FIRST spine position it appears at (a TOC-only href absent from the spine is ignored; a fragment-only href resolves to its document's position). Chapter k's spine range = [pos(k), pos(k+1)) over spine positions. Spine items BEFORE the first TOC entry form their own leading unit (front matter — titled from its first document, fallback label "Front matter"; planner resolution of assumption A5). If fewer than EPUB_THRESHOLDS.tocMergeMinEntries distinct entries resolve → FALLBACK: one chapter unit per readerable spine item. Expose whether fallback fired on the result (a fallbackUsed flag on EpubToBooksResult — the calibration evidence needs it, Pitfall 1 warning sign).
  3. Per-spine-item walk — decode the document text; sanitizeExtractedHtml(xhtml); new JSDOM(sanitized); htmlToBlocks(dom.window.document, undefined) — NO Readability/isProbablyReaderable call anywhere in this file (chapters are already content; the anti-pattern). Blocks concatenate across the items of a merged range, in spine order. lang = bookMeta.language.
  4. Figure downgrade (D12-16 + T-12-05) — post-process every walked chapter: any block of kind "figure" is replaced by an unsupported block carrying the figure's alt/plainDescription text where available (relative-src figures already failed the http(s) gate and arrived as unsupported; remote-src figures are downgraded HERE — the reader must never fetch EPUB-embedded resources).
  5. Admission (D12-10) — per spine document, BEFORE chapter assembly: skip the nav document itself (known manifest id), and skip any document whose walked output fails the EPUB-adapted readerability algebra: blocks.length >= EPUB_THRESHOLDS.minChapterBlocks AND at least one block of kind paragraph or heading with non-empty text (the 11-07 relaxed algebra translated to the document unit — cover plates and pure-image pages fail it; front matter with real paragraphs passes). linear="no" is a skip HINT only — the readerability gate decides (spec: "purely supplemental").
  6. Chapter titles — chain: TOC label → document title element (the htmlToBlocks provenance extraction) → first heading block text → "Chapter N". Numbering for display runs over ADMITTED chapters only (Pitfall 10 — the denominator is chapterArticleIds.length; skipped chapters are disclosed, never renumbered).
  7. Output contract — EpubToBooksResult { bookMeta {title, authors, language, publisher?, publishedDate?, identifier?}, chapters: ChapterDraft[] ordered, skippedCount, originalFileHash: sha256 of the EPUB bytes, fallbackUsed }. ChapterDraft carries the SAME five per-article fields stages 2+ consume: {blocks, footnotes, lang, title, spineIndex} PLUS sourceHtmlHash (sha256 of the chapter's concatenated spine-item XHTML text — the IngestionMeta.originalHtmlHash input; planner addition so the orchestrator never re-reads bytes). Zero readerable spine documents → IngestionError("epub-empty") thrown here (orchestrator separately refuses when all chapters fail stages 2+). assertChapterCap runs on the final chapter count.
  8. Extend the spec with describe "TOC-merge + chapters": validBookEpub3 → 4 chapters, nested depth-2 NOT split; publisherSplitBook → 3 chapters with chapter 1 blocks containing BOTH split documents' text in spine order; ncxOnlyBook → 3 chapters via NCX; oebpsNestedBook → chapters resolve with fallbackUsed false (Pitfall 1 regression); deepNavBook → exactly 3 chapters; degenerateTocBook → 4 chapters (descent); frontMatterBook → 3 units with the leading unit first; imageChapterBook → zero figure-kind blocks survive, ≥1 unsupported block with description text, remote URL absent from all block payloads; emptyBook → epub-empty; mixedAdmissionBook → 2 chapters + skippedCount 1; chapter titles chain (TOC label primary; a chapter with no TOC label falls to document title); admitted-order numbering (chapter i id-suffix order matches spine order); every chapter's blocks parse against the Block shape and footnotes default to [] when absent.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-to-books.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/epubToBooks.ts contains no reference to Readability or isProbablyReaderable (chapters ride sanitize + walk only)
    - The word figure appears in the downgrade pass and no admitted chapter in any test retains a figure-kind block (assert via a blocks.every scan in the imageChapterBook test)
    - validBookEpub3 yields chapters.length 4; publisherSplitBook 3; ncxOnlyBook 3; deepNavBook 3; degenerateTocBook 4; frontMatterBook 3; mixedAdmissionBook 2 with skippedCount 1; emptyBook throws epub-empty
    - oebpsNestedBook test asserts fallbackUsed === false (href normalization regression pinned)
    - EpubToBooksResult exposes bookMeta, chapters, skippedCount, originalFileHash, fallbackUsed; ChapterDraft exposes blocks, footnotes, lang, title, spineIndex, sourceHtmlHash
    - npx vitest run tests/unit/server/epub-to-books.spec.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The adapter is complete, pure, and green across the full synthetic matrix; the orchestrator (12-04) can compose it unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded EPUB bytes → adapter | untrusted archive + XML + XHTML crosses here (the entire hostile-input surface) |
| adapter → Block JSON | sanitized, schema-valid inert content crosses out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | DoS | zip bomb via decompression amplification | mitigate | unzipSync filter on originalSize <= EPUB_MAX_ENTRY_BYTES — over-cap entries skipped BEFORE inflation (Phase 9 pattern); bombEntryBook fixture proves it |
| T-12-02 | Tampering | Zip Slip path-traversal entries | mitigate | isSafeEntryName (the tested Phase 9 gate) on EVERY entry key before any byte use; zipSlipBook fixture proves it |
| T-12-03 | Tampering/Elevation | mXSS via chapter XHTML | mitigate | sanitizeExtractedHtml (DOMPurify SANITIZE_CONFIG — the 07-04 mXSS-suite-covered surface) before the walk; blocks are inert JSON |
| T-12-04 | DoS | XML entity expansion (billion laughs) | mitigate | processEntities: false + whole parse inside try → epub-unreadable envelope; entityBombOpf fixture proves it |
| T-12-06 | Tampering | prototype pollution via XML property names | mitigate | fast-xml-parser default onDangerousProperty throw on __proto__/constructor/prototype names; protoPollutionOpf fixture proves it |
| T-12-05 | Information Disclosure | remote-src image fetch on chapter open (tracking beacon) | mitigate | every figure block downgrades to unsupported — reader never fetches EPUB-embedded resources; imageChapterBook test asserts zero survivors |
| T-12-07 | Repudiation/Legal | DRM circumvention posture | mitigate | detection-only allowlist refusal; no decryption code; no marker bytes in messages/logs — typed epub-protected with calm copy surfaced at 12-03 |
| T-12-08 | DoS | hostile nested/huge manifests | mitigate | maxNestedTags 40 + EPUB_MAX_CHAPTERS cap + EPUB_EXTRACTION_TIMEOUT_MS race + EPUB_MAX_BYTES (enforced 12-03/12-04) |
</threat_model>

<verification>
Full adapter unit matrix green (both describes); tsc clean; the adapter imports nothing from src/ except types + the zipSlip helper; grep confirms no Readability usage in server/epubToBooks.ts.
</verification>

<success_criteria>
server/epubToBooks.ts ships the complete parse chain (unzip → slip → DRM → container → OPF → nav|NCX → TOC-merge → per-chapter sanitize+walk → downgrade → admission) as a pure adapter with EPUB_THRESHOLDS exported; every hostile fixture refuses calmly with a typed reason; every structural fixture produces the pinned chapter counts; the output contract feeds stages 2+ unchanged.
</success_criteria>

## Artifacts this phase produces

- `epubToBooks(bytes, hints?)` — server/epubToBooks.ts (the SC#4 swappable adapter entry)
- `EPUB_THRESHOLDS` — { minChapterBlocks, tocMergeMinEntries, maxNestedXmlTags } (calibration-pinned)
- `EpubToBooksResult` / `ChapterDraft` types — {bookMeta, chapters, skippedCount, originalFileHash, fallbackUsed} / {blocks, footnotes, lang, title, spineIndex, sourceHtmlHash}
- `normalizeEpubHref(rawHref, baseDir)` — the one shared href normalizer (Pitfall 1 mitigation)
- `withEpubTimeout`, `assertChapterCap`, the DRM allowlist gate, the XMLParser factory — internal exported-for-test helpers
- tests/unit/server/epub-to-books.spec.ts — the adapter unit matrix (two describes: archive+DRM+manifests; TOC-merge+chapters)

<output>
Create `.planning/phases/12-epub-intake/12-02-SUMMARY.md` when done
</output>
