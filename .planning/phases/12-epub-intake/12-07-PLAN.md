---
phase: 12-epub-intake
plan: 07
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified:
  - src/portability/bundle.ts
  - src/portability/ExportImportService.ts
  - src/portability/conflicts.ts
  - tests/unit/portability/bundle-schema.test.ts
  - tests/unit/portability/conflicts.test.ts
  - tests/unit/portability/export-service.test.ts
  - tests/unit/portability/atomic-import.test.ts
  - tests/e2e/portability/round-trip.spec.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "Export emits schemaVersion 2 with a books array; every chapter rides articles as an ordinary article (ingestionMeta.bookId survives serialization)"
    - "A v1 bundle (no books) imports exactly as before; a v3+ bundle forward-refuses via the newer-schema-version gate bumped to > 2 (D9-04 preserved)"
    - "Import writes books + chapters + annotations in the SAME single Dexie transaction (atomicity preserved); book-id conflicts resolve skip-by-default (D9-14 table extended)"
    - "Chapters whose book is missing from the bundle import as standalone epub-chapter articles — orphan-tolerant, shown ungrouped"
    - "A book exported on one machine re-imports on another with every chapter highlight re-resolving to confident or surfacing honestly (round-trip integrity holds at book granularity)"
  artifacts:
    - path: "src/portability/bundle.ts"
      provides: "schemaVersion 1|2 union read + optional books array"
      contains: "z.union([z.literal(1), z.literal(2)])"
    - path: "src/portability/conflicts.ts"
      provides: "the book-id conflict kind (skip-by-default, D9-14 extension)"
      contains: "book"
  key_links:
    - from: "src/portability/ExportImportService.ts"
      to: "src/persistence/booksStore.ts"
      via: "loadAllBooks joins the export Promise.all; db.books joins the import transaction table set"
      pattern: "listBooks"
    - from: "src/portability/bundle.ts"
      to: "src/content/schema.ts"
      via: "books array composes BookSchema — no record shape re-declared (REUSE-DO-NOT-FORK)"
      pattern: "BookSchema"
---

<objective>
Books ride the Phase 9 export/import loop (Pattern 9): the bundle schema bumps to a 1|2 union with an optional books array (writers emit 2), export assembly includes book records, import applies them inside the existing atomic transaction with book-id conflicts resolved skip-by-default, orphan chapters import as standalone articles, and the two-context round-trip e2e proves a book travels between machines with its highlights intact.

Purpose: PORT-01/02 extension is the cross-device story for books — without it, a reader's library breaks silently on migration (the never-silent ethos). Runs parallel with 12-05 (disjoint files).
Output: Bundle v2 shipped compatibly; portability suites + round-trip e2e green.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@src/portability/bundle.ts
@src/portability/ExportImportService.ts
@src/portability/conflicts.ts
@src/persistence/booksStore.ts
@tests/e2e/portability/round-trip.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle schemaVersion 2 (union read) + books in export</name>
  <files>src/portability/bundle.ts, src/portability/ExportImportService.ts, tests/unit/portability/bundle-schema.test.ts, tests/unit/portability/export-service.test.ts</files>
  <read_first>src/portability/bundle.ts (the L13-16 books-absence comment to update + ExportBundleSchema), src/portability/ExportImportService.ts (buildBundleBytes L72-118 Promise.all assembly + the L221-227 newer-version peek), src/content/schema.ts (ReaderSettingsSchema v1|v2 union precedent L273-302 + BookSchema), src/persistence/booksStore.ts (listBooks), tests/unit/portability/bundle-schema.test.ts, tests/unit/portability/export-service.test.ts, 12-RESEARCH.md §Pattern 6 + §Code Examples "Bundle widening"</read_first>
  <action>1. src/portability/bundle.ts — widen additively: schemaVersion: z.union([z.literal(1), z.literal(2)]) (the ReaderSettingsSchema precedent — v1 rows hydrate, v3+ forward-reject per D9-04) and books: z.array(BookSchema).optional() (absent on v1 imports, always present on v2 writes). Import BookSchema from ../content/schema — composed, never re-declared. Update the header comment: the books-absence note becomes the filled-contract note (Phase 12 fills what Pattern 7 anticipated). BUNDLE_FILENAME stays "lem-reader-bundle-v1.zip" — planner resolution: the zip FILENAME is not the version contract (the inner bundle.json schemaVersion is; D9-01's locked filename is unchanged to avoid breaking reader expectations and tests).
   2. src/portability/ExportImportService.ts — buildBundleBytes: add listBooks() to the existing Promise.all load; include books in the bundle payload; write schemaVersion: 2; keep the deterministic SHA-256 manifest mechanism byte-identical (books live inside bundle.json — no new archive entries, manifest integrity unchanged). Bump the newer-version peek from peeked > 1 to peeked > 2 (the refusal kind newer-schema-version with bundleVersion surfaces for v3+ — older-reader honesty preserved).
   3. tests/unit/portability/bundle-schema.test.ts — extend: a v1 bundle without books parses (regression); a v2 bundle with books parses; a v3 schemaVersion rejects; books absent-from-v1 AND present-on-v2 shapes both hydrate; BookSchema composition rejects a malformed book row inside a bundle.
   4. tests/unit/portability/export-service.test.ts — extend: buildBundleBytes on a library containing a book + chapters emits schemaVersion 2, the books array with the full record, and chapters among articles carrying ingestionMeta.bookId; an empty-books library still emits v2 with books: [] (writers always emit the field); determinism — two exports of the same library hash identically.</action>
  <verify>
    <automated>npx vitest run tests/unit/portability/bundle-schema.test.ts tests/unit/portability/export-service.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - bundle.ts contains z.union([z.literal(1), z.literal(2)]) for schemaVersion and books: z.array(BookSchema).optional()
    - A v1 bundle fixture (the Phase 9 shape, no books key) parses; a literal(3) payload rejects; the peek guard now refuses > 2
    - buildBundleBytes output contains schemaVersion 2 and the books array; chapters appear in articles with bookId intact
    - BUNDLE_FILENAME is unchanged in the diff
    - npx vitest run tests/unit/portability/bundle-schema.test.ts tests/unit/portability/export-service.test.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>v2 bundles carry books; v1 bundles import unchanged; newer-bundle honesty preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Import apply + book conflicts + orphan tolerance + round-trip e2e</name>
  <files>src/portability/conflicts.ts, src/portability/ExportImportService.ts, tests/unit/portability/conflicts.test.ts, tests/unit/portability/atomic-import.test.ts, tests/e2e/portability/round-trip.spec.ts</files>
  <read_first>src/portability/conflicts.ts (the id-kind conflict table + resolveImportPlan), src/portability/ExportImportService.ts (applyImport L292-342 — the explicit-arity transaction calls + puts-only closure rule L276-289), tests/unit/portability/conflicts.test.ts, tests/unit/portability/atomic-import.test.ts (the injected-failure rollback harness), tests/e2e/portability/round-trip.spec.ts (the two-browser-context A/B surrogate + _portability.ts helpers), src/persistence/booksStore.ts</read_first>
  <action>1. src/portability/conflicts.ts — extend the conflict table with the book-id kind: a bundle book whose id exists locally with a DIFFERENT originalFileHash is a conflict resolving skip-by-default with per-kind override support (the D9-14 semantics extended; an identical-hash duplicate book is a calm no-op skip — the 09-03 identical-duplicate precedent applied at book level). Chapter articles conflict as article-id rows exactly as today.
   2. src/portability/ExportImportService.ts — applyImport: widen the transaction table set with db.books (the explicit-arity call shape — keep the two-call tuple pattern tsc requires; the closure stays puts-only: no Zod, no crypto, no network inside). Books under plan.applyBooks (mirroring the per-kind gating) are put; chapters ride the existing article puts (their bookId travels inside the article rows). Orphan tolerance: a chapter whose bookId references a book absent from the bundle (skipped conflict or partial export) STILL imports as a standalone epub-chapter article — the library renders it ungrouped (the BookRow live-index rendering from 12-05 already tolerates this).
   3. tests/unit/portability/conflicts.test.ts — extend: same-id different-hash book → conflict row (kind book, skip default); identical-hash book → no conflict, calm skip; override=keep-both/replace semantics mirror the article id-kind table for books; preview shape includes book counts.
   4. tests/unit/portability/atomic-import.test.ts — extend: a bundle with books + chapters + a chapter highlight applies atomically (all stores populated); an injected mid-transaction failure rolls back books AND chapters (no partial book); a v1 bundle regression case applies with zero book writes.
   5. tests/e2e/portability/round-trip.spec.ts — extend the two-context A/B flow: on context A upload a book (fixture builder), highlight a passage in chapter 2, export the whole library → on context B import the bundle → the book groups with all chapters, the chapter-2 highlight re-resolves confident and renders at the same offset (the byte-equality assertion pattern), book progress/hairline reflects any traveled locations, and re-export from B is deterministic. Add a v1-compat case: import the committed Phase 9 v1 bundle fixture (or synthesize the v1 shape) → imports exactly as before with zero books (the never-break-older-bundles gate).</action>
  <verify>
    <automated>npx vitest run tests/unit/portability/conflicts.test.ts tests/unit/portability/atomic-import.test.ts && npx playwright test tests/e2e/portability/round-trip.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - conflicts.ts carries the book-id kind with skip-by-default; the identical-hash book is a no-op skip (test pinned)
    - applyImport's transaction includes db.books; the closure remains puts-only (no safeParse/crypto/fetch inside — grep-level discipline maintained)
    - The atomic rollback test proves no partial book persists on mid-transaction failure
    - The round-trip e2e proves confident highlight re-resolution at the same offset on the second machine and deterministic re-export
    - The v1-bundle regression case passes unchanged
    - Both automated commands exit 0
  </acceptance_criteria>
  <done>Books travel the portability loop atomically, conflict-calmy, and backward-compatibly — proven across two browser contexts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle file → import pipeline | hostile/corrupt bundles (the 09-04 six-refusal surface) now also carry book rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Tampering | forged book rows in a hand-crafted bundle | mitigate | books parse through BookSchema inside ExportBundleSchema (Zod-at-boundary); unknown keys stripped; puts-only transaction |
| T-12-18 | Tampering | silent book loss on version mismatch | mitigate | union read keeps v1 importable; > 2 peek refuses loudly (newer-schema-version) — never a silent partial import (D9-04) |
| T-12-19 | DoS | book-flooded bundles stressing import | accept | the existing bomb-cap/entry limits bound the archive; book count is bounded by library scale; measured in the perf harness at Phase 13 |
</threat_model>

<verification>
Portability unit suites + round-trip e2e green; v1 regression intact; tsc clean.
</verification>

<success_criteria>
Bundle v2 carries books with union-read compatibility; import is atomic and conflict-honest; orphan chapters tolerate; the two-machine book round-trip with highlight integrity is browser-proven.
</success_criteria>

## Artifacts this phase produces

- ExportBundleSchema schemaVersion 1|2 union + optional books array; writers emit 2; newer-version peek threshold 2
- The book-id conflict kind in conflicts.ts (skip-by-default + identical-hash no-op)
- applyImport db.books table-set widening + orphan-chapter tolerance
- Portability unit extensions (bundle-schema, export-service, conflicts, atomic-import) + the round-trip e2e book flow

<output>
Create `.planning/phases/12-epub-intake/12-07-SUMMARY.md` when done
</output>
