---
phase: 12-epub-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/phases/12-epub-intake/12-01-fxml-approval.md
  - src/ingestion/types.ts
  - src/content/schema.ts
  - server/limits.ts
  - src/ingestion/library/SourceBadge.tsx
  - tests/unit/ingestion-schema.test.ts
  - tests/unit/server/epub-fixtures.ts
autonomous: false
requirements: ["ING-05"]
must_haves:
  truths:
    - "fast-xml-parser is installed at the human-approved exact pin and no module under src/ imports it (D12-15)"
    - "IngestionRequestSchema accepts an {epub base64, filename?} variant and rejects non-base64 payloads"
    - "IngestionFailureReasonEnum catalogs 20 reasons including the four EPUB members (epub-protected, epub-unreadable, epub-empty, epub-too-large)"
    - "ArticleSourceSchema includes epub-chapter; BookSchema parses {id, title, authors, language, chapterArticleIds, tags, addedAt, skippedChapterCount, source epub-upload, originalFileHash}"
    - "IngestionResponseSchema parses BOTH ok-variants — the existing single-article variant byte-stable, plus {book, articles min 1, skippedCount}"
    - "The synthetic EPUB generator self-verifies at module load: deterministic byte-identical rebuild, DRM/corrupt discriminators present, no binary fixtures committed"
  artifacts:
    - path: "src/ingestion/types.ts"
      provides: "epub request variant, four EPUB failure reasons, shared EPUB_MAX_BYTES constant, multi-article book ok-variant"
      contains: "epub: z.string().base64()"
    - path: "src/content/schema.ts"
      provides: "epub-chapter source member, BookSchema, IngestionMeta bookId/chapterIndex additive fields"
      contains: "export const BookSchema"
    - path: "server/limits.ts"
      provides: "EPUB resource caps (chapters, timeout, entry bomb cap) + max()-derived transport cap"
      contains: "EPUB_MAX_CHAPTERS"
    - path: "tests/unit/server/epub-fixtures.ts"
      provides: "self-verifying synthetic EPUB fixture builders (fflate zipSync) covering the full discriminator matrix"
      min_lines: 200
  key_links:
    - from: "src/ingestion/types.ts"
      to: "server/limits.ts"
      via: "limits re-exports EPUB_MAX_BYTES so client picker, middleware, and orchestrator share one cap constant (three-point enforcement)"
      pattern: "EPUB_MAX_BYTES"
    - from: "src/content/schema.ts"
      to: "src/ingestion/library/SourceBadge.tsx"
      via: "the epub-chapter enum member must be handled by the exhaustive badgeLabel switch"
      pattern: 'case "epub-chapter"'
    - from: "tests/unit/server/epub-fixtures.ts"
      to: "tests/unit/server/epub-to-books.spec.ts"
      via: "every adapter spec case consumes a named builder; module-load self-check proves discriminating power"
      pattern: "zipSync"
---

<objective>
Foundation for EPUB intake (ING-05): the D12-15 blocking-human gate for fast-xml-parser (SUS "too-new" audit → exact pin + legitimacy record), the additive schema/enum/cap widening every later plan compiles against, and the self-verifying synthetic EPUB fixture generator (the 11-01 unpdf/serializePdf precedent, EPUB edition). The spec files (epub-to-books.spec.ts, ingest-epub.spec.ts, epub-intake.spec.ts) are NOT pre-created — each lands with the plan that owns its full content (12-02, 12-04, 12-05), mirroring the 11-01 scope note.

Purpose: Every Wave-2+ plan (adapter, client, orchestrator, UI, portability, calibration) imports these widened contracts; the synthetic fixtures make the adapter testable in CI without touching the D12-12 local-only calibration corpus.
Output: Installed pinned fast-xml-parser + approval doc; widened Zod schemas + caps + Book badge; committed fixture generator with module-load self-check.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@.planning/phases/11-pdf-intake/11-01-unpdf-approval.md
@src/ingestion/types.ts
@src/content/schema.ts
@server/limits.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve fast-xml-parser legitimacy + exact pin (D12-15, T-12-SC)</name>
  <what-built>Nothing yet — this is the package-legitimacy gate required before any install (D12-15, the 11-01 unpdf precedent). The 12-RESEARCH.md §Package Legitimacy Audit rates fast-xml-parser SUS solely on the "too-new" signal (5.11.0 published 2026-08-16, one day before research). Every other signal is top-tier: ~69.3M weekly downloads, maintainer NaturalIntelligence/amitgupta (~10 yrs), MIT, no postinstall/preinstall scripts, bundled types, zero native bindings. This checkpoint also resolves 12-RESEARCH Open Question 1: pin 5.10.1 (STACK.md-verified, in-registry since 2026-07-16 — the default) vs 5.11.0 (current; diff vs 5.10.1 must be verified API-neutral for our option surface if chosen).</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/fast-xml-parser — confirm maintainer NaturalIntelligence, MIT license, ~69M weekly downloads, and that the version you approve shows no install scripts on the Files tab.
    2. Optionally skim https://github.com/NaturalIntelligence/fast-xml-parser (active repo; docs/v4,v5/2.XMLparseOptions.md is the API surface we use: XMLParser, ignoreAttributes, removeNSPrefix, isArray, processEntities, maxNestedTags, onDangerousProperty).
    3. If preferring 5.11.0: check the 5.10.1→5.11.0 diff (release notes / changelog) and confirm it is API-neutral for XMLParser options.
    4. Choose the pin: 5.10.1 (default — older signal, STACK.md lineage) or 5.11.0 (latest, requires the diff check).
  </how-to-verify>
  <resume-signal>Type "approved 5.10.1" or "approved 5.11.0" (or describe concerns)</resume-signal>
  <files>package.json</files>
  <action>Blocking gate before the Task 2 install — no file changes in this task. The executor presents the legitimacy evidence and pin choice; on approval, records the chosen pin for Task 2's npm install --save-exact call and for the approval doc.</action>
  <verify>Human reply contains the approved pin (blocking-human gate; auto_advance ignored per legitimacy-checkpoint policy)</verify>
  <done>Pin approved and recorded; install may proceed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Install pinned fast-xml-parser + widen schemas, limits, badge</name>
  <files>package.json, .planning/phases/12-epub-intake/12-01-fxml-approval.md, src/ingestion/types.ts, src/content/schema.ts, server/limits.ts, src/ingestion/library/SourceBadge.tsx, tests/unit/ingestion-schema.test.ts</files>
  <read_first>server/limits.ts, src/ingestion/types.ts, src/content/schema.ts (ArticleSourceSchema + IngestionMetaSchema blocks), src/ingestion/library/SourceBadge.tsx, tests/unit/ingestion-schema.test.ts, .planning/phases/11-pdf-intake/11-01-unpdf-approval.md, 12-PATTERNS.md sections "src/ingestion/types.ts + src/content/schema.ts" and "dev-server/ingest-middleware.ts + server/limits.ts"</read_first>
  <action>Install with an exact pin (no caret): npm install --save-exact fast-xml-parser@&lt;approved-pin&gt;. Write .planning/phases/12-epub-intake/12-01-fxml-approval.md mirroring 11-01-unpdf-approval.md: chosen pin, legitimacy evidence table (maintainer, downloads, license, zero install scripts, bundled types), the 5.10.1-vs-5.11.0 diff note (assumption A6), the user approval date, and the server-only boundary rule (fast-xml-parser joins unpdf/jsdom behind /server imports; only Block/InlineRun types cross). Then widen additively — never edit shipped members (Pitfall 9 discipline):
  1. src/ingestion/types.ts — add a fifth union member to IngestionRequestSchema: z.object({ epub: z.string().base64().min(1), filename: z.string().optional() }) with a Phase 12 ING-05 comment (base64-in-JSON keeps the middleware body path byte-identical; filename is a title-fallback hint only, never part of ids). Add EPUB_MAX_BYTES = 10 * 1024 * 1024 exported constant beside PDF_MAX_BYTES with the same three-point-enforcement comment (client picker → middleware → orchestrator re-check; mirrors 12-RESEARCH assumption A2). Add four members to IngestionFailureReasonEnum after "pdf-too-large", before "already-in-library": epub-protected (DRM — detection-only refusal), epub-unreadable (corrupt/unparseable container/OPF/zip, Zip Slip, entity/proto hostility), epub-empty (zero chapters admit — whole-document refusal per D12-11), epub-too-large (over EPUB_MAX_BYTES / EPUB_MAX_CHAPTERS). Enum grows 16 → 20. Widen IngestionResponseSchema with a SECOND ok-variant: z.object({ ok: z.literal(true), book: BookSchema, articles: z.array(ArticleSchema).min(1), skippedCount: z.number().int().min(0) }) — import BookSchema from ../content/schema. Planner resolution of 12-RESEARCH Pitfall 3 sketch: NO top-level confidence field — each article carries its own ingestionMeta.extractionConfidence (byte-stable per-article contract; a duplicated envelope signal would fork the truth). The existing single-article ok-variant stays byte-stable for url/paste/markdown/pdf.
  2. src/content/schema.ts — append "epub-chapter" to ArticleSourceSchema with comment "Phase 12 — ING-05 (.epub upload via epubToBooks; one article per chapter, Option A)". Add bookId: z.string().regex(/^[a-z0-9-]+$/).optional() and chapterIndex: z.number().int().min(0).optional() to IngestionMetaSchema (ARCHITECTURE L401-402; existing rows parse unchanged — absent fields). Add BookSchema per 12-RESEARCH L565-577 lifted verbatim PLUS two planner additions the sketch omitted: tags: z.array(z.string().min(1)).default([]).optional() (D12-04 — tags live on the Book record; mirrors ArticleSchema.tags Phase 8 mechanism) and addedAt: z.string().datetime() (library default-sort + continue-strip ordering). Full member set: id (regex /^[a-z0-9-]+$/), title min(1), authors array default([]), language min(2), chapterArticleIds array of id-regex strings (ordered — the book's own TOC per D12-06), publisher/publishedDate/identifier optional strings, skippedChapterCount int min(0) default(0) (D12-11), source: z.literal("epub-upload"), originalFileHash string, tags, addedAt. Export type Book = z.infer&lt;typeof BookSchema&gt;.
  3. server/limits.ts — import EPUB_MAX_BYTES from ../src/ingestion/types and re-export it (the /src→/server import-direction rule). Add the Phase 12 caps block: EPUB_MAX_CHAPTERS = 1000 (assumption A2 sanity ceiling; a real book never approaches it), EPUB_EXTRACTION_TIMEOUT_MS = 30_000 (mirrors REQUEST_TIMEOUT_MS and PDF_EXTRACTION_TIMEOUT_MS), EPUB_MAX_ENTRY_BYTES = 64 * 1024 * 1024 (per-entry decompressed bomb cap — a single XHTML over 64MB inside a ≤10MB zip is pathological; over-cap entries are filtered BEFORE inflation, the Phase 9 MAX_ENTRY_ORIGINAL_SIZE discipline at an EPUB-appropriate constant). Change MAX_INGEST_BODY_BYTES to derive from Math.max(PDF_MAX_BYTES, EPUB_MAX_BYTES) with the same Math.ceil((BYTES * 4) / 3) + 2048 formula and a comment noting it is numerically identical today (equal caps) but structurally correct for future divergence (12-RESEARCH Pitfall 2).
  4. src/ingestion/library/SourceBadge.tsx — add case "epub-chapter" returning "Book" (the no-default exhaustive switch forces it once the enum widens; D12-01 chapter sub-rows reuse it).
  5. tests/unit/ingestion-schema.test.ts — extend the exact-array assertions: ArticleSourceSchema.options equals the seven members ending with epub-chapter; IngestionFailureReasonEnum.options has length 20 in the exact new order (epub members between pdf-too-large and already-in-library). Add cases: parses a valid base64 epub payload with filename; rejects an epub value containing non-base64 characters; the book ok-variant parses {ok: true, book, articles: [article], skippedCount: 0} against fixture-shaped objects; the legacy article ok-variant still parses; a book variant with articles: [] is rejected (min(1) — an admitted book always has at least one chapter, epub-empty refuses upstream).
  Server-only boundary: no module under src/ may import fast-xml-parser (client bundle stays clean; the dist/ grep proof lands at 12-08).</action>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "fast-xml-parser": "X.Y.Z" (the approved pin, no caret) and .planning/phases/12-epub-intake/12-01-fxml-approval.md records the evidence + approval
    - src/ingestion/types.ts contains epub: z.string().base64(), EPUB_MAX_BYTES = 10 * 1024 * 1024, all four epub-* enum members, and a second ok-variant containing book: BookSchema and articles:
    - src/content/schema.ts contains export const BookSchema with chapterArticleIds, skippedChapterCount, source: z.literal("epub-upload"), tags, addedAt; IngestionMetaSchema contains bookId and chapterIndex; ArticleSourceSchema includes "epub-chapter"
    - server/limits.ts contains EPUB_MAX_CHAPTERS = 1000, EPUB_EXTRACTION_TIMEOUT_MS = 30_000, EPUB_MAX_ENTRY_BYTES, and MAX_INGEST_BODY_BYTES derived from Math.max(PDF_MAX_BYTES, EPUB_MAX_BYTES)
    - SourceBadge.tsx contains case "epub-chapter": return "Book"
    - tests/unit/ingestion-schema.test.ts asserts the enum has 20 members and references "epub-protected" and "epub-empty"
    - npx vitest run tests/unit/ingestion-schema.test.ts exits 0; npx tsc --noEmit exits 0
    - grep -rn 'from "fast-xml-parser"' src/ returns no matches
  </acceptance_criteria>
  <done>fast-xml-parser installed at the approved exact pin with the approval doc committed; all widenings shipped additively; schema unit suite and type-check green; client tree free of fast-xml-parser imports.</done>
</task>

<task type="auto">
  <name>Task 3: Self-verifying synthetic EPUB fixture generator</name>
  <files>tests/unit/server/epub-fixtures.ts</files>
  <read_first>tests/fixtures/pdf/generate-synthetic-pdfs.ts (self-check discipline), src/portability/ExportImportService.ts L145-194 (fflate unzipSync + filter import discipline), 12-RESEARCH.md §Code Examples L434-558 (verified container.xml/OPF/nav/NCX/DRM XML shapes — lift these verbatim into the templates), 12-RESEARCH.md §Wave 0 Gaps L708 (fixture matrix)</read_first>
  <action>Create tests/unit/server/epub-fixtures.ts — an importable fixture-builder module (NOT a committed-binary corpus): named exported functions each returning a deterministic Uint8Array EPUB built in-process with fflate zipSync (import only zipSync/strToU8 from "fflate" — the ExportImportService import discipline). Include the mimetype entry ("application/epub+zip", stored at level 0) FIRST in every valid book, then META-INF/container.xml, then the content documents. XML templates lift the verified shapes from 12-RESEARCH §Code Examples (container namespace urn:oasis:names:tc:opendocument:xmlns:container; OPF namespace http://www.idpf.org/2007/opf with dc: metadata; EPUB 3 nav with epub:type="toc" ol>li>a hierarchy; EPUB 2 NCX navMap/navPoint/navLabel/content@src). Builder matrix:
  1. validBookEpub3() — OPF (dc:title "The Synthetic Book", two dc:creator, dc:language "en", nav item properties="nav"), nav.xhtml with FOUR top-level entries (one carrying a nested depth-2 ol) + 4 chapter XHTML documents (headings h2 + 3+ paragraphs each so every chapter clears admission), spine [nav linear="no", c1..c4].
  2. publisherSplitBook() — nav has 3 top-level entries; spine carries ch1a + ch1b both resolving into TOC entry 1's spine range → D12-09 merge proof.
  3. ncxOnlyBook() — EPUB 2 shape: no nav item; spine toc="ncx" + application/x-dtbncx+xml NCX with 3 navPoints (one nested for depth).
  4. oebpsNestedBook() — OPF at OEBPS/content.opf, chapters at OEBPS/text/chN.xhtml, nav hrefs relative to OEBPS/ — Pitfall 1 href-normalization proof.
  5. deepNavBook() — 3 depth-1 entries each with 2-3 nested depth-2/3 children — Pitfall 4 proof (only 3 chapters).
  6. degenerateTocBook() — single depth-1 entry ("Contents") wrapping 4 depth-2 children — the degenerate descent case.
  7. frontMatterBook() — spine starts with a title page + copyright page (real paragraphs, before the first TOC entry) then 2 TOC chapters — the leading-unit case.
  8. drmAdeptBook() — valid shell + META-INF/rights.xml + META-INF/encryption.xml carrying an ADEPT-namespace encryptedKey entry.
  9. drmLcpBook() — valid shell + META-INF/license.lcpl entry.
  10. drmUnknownAlgBook() — encryption.xml whose EncryptionMethod Algorithm is a vendor URI that is not the font-obfuscation URI.
  11. fontObfuscatedBook() — encryption.xml whose ONLY EncryptedData uses Algorithm http://www.idpf.org/2008/embedding + one readerable chapter — passes the DRM gate (OCF §4.4.5).
  12. corruptNotEpub() — the ASCII bytes "this is not a zip".
  13. missingContainerBook() — a valid zip with chapters but no META-INF/container.xml.
  14. entityBombOpf() — OPF embedding ENTITY declarations shaped like nested entity expansions (the classic amplification pattern) in metadata.
  15. protoPollutionOpf() — OPF containing a literal __proto__ element name in metadata.
  16. zipSlipBook() — valid book plus an extra entry named ../../evil.xhtml.
  17. bombEntryBook() — valid book plus one entry whose DECLARED originalSize is over EPUB_MAX_ENTRY_BYTES, crafted by patching the zip size fields post-zipSync (the 09-04 declared-size patch technique — never materialize the inflated bytes).
  18. imageChapterBook() — one chapter embedding a remote-src image (absolute https URL) and a relative-src image, plus readerable text.
  19. emptyBook() — valid container/OPF/spine but every spine document is a pure-image plate (no readerable text).
  20. mixedAdmissionBook() — 2 readerable chapters + 1 pure-image plate chapter → skippedCount 1 (the D12-11 disclosure fixture).
  Module-load self-check (the 10-04 seed-time precedent): on import, rebuild every fixture twice and assert byte-identity (determinism), assert the DRM markers/corrupt ASCII/entity/proto/slip discriminators are present in the built bytes, assert every valid book's mimetype entry is the EPUB media string, and assert at least one builder pair genuinely diverges (validBookEpub3 vs publisherSplitBook produce different chapter counts when parsed by a tiny in-module TOC counter — catches rot that would silently flatten the matrix). Throw on any violation. No .epub binaries are committed; the generator IS the fixture source.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-fixtures.ts --typecheck.only 2>/dev/null; node -e "import('./tests/unit/server/epub-fixtures.ts').then(m => { const u = m.validBookEpub3(); if (!(u instanceof Uint8Array) || u.length < 200) process.exit(1); console.log('fixtures OK', u.length); }).catch(e => { console.error(e); process.exit(1) })" || npx vitest run --config vitest.config.ts tests/unit/server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/server/epub-fixtures.ts exists, exports all 20 named builders, and imports only zipSync/strToU8 (plus node builtins) from fflate
    - The module-load self-check runs on import and throws on any violation (determinism, discriminator presence, mimetype-first, diverging chapter counts)
    - A quick import of the module succeeds with a non-trivial Uint8Array (validBookEpub3().length > 200) — verify command exits 0
    - No .epub files are added to git (git status shows only the generator)
    - The XML templates use the verified namespaces verbatim (urn:oasis:names:tc:opendocument:xmlns:container, http://www.idpf.org/2007/opf, http://www.daisy.org/z3986/2005/ncx/)
  </acceptance_criteria>
  <done>Fixture generator committed and self-verifying; consuming plans (12-02/12-04/12-05/12-06) import named builders; no binary fixtures in git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | fast-xml-parser install (SUS-rated "too-new" by the legitimacy audit) |
| client bundle ← /server | fast-xml-parser must never cross into src/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | npm install fast-xml-parser | mitigate | blocking checkpoint:human-verify before install (Task 1, D12-15); exact pin; registry signals re-confirmed by the human; approval doc committed |
| T-12-04 | DoS | XML entity expansion via hostile OPF/NCX (future plans) | mitigate | contract lands now: processEntities:false + maxNestedTags:40 are pinned in the approval doc + fixtures; enforcement ships in 12-02 |
| T-12-06 | Tampering | prototype pollution via XML property names (future plans) | mitigate | fast-xml-parser default onDangerousProperty throw + Zod strip documented; the protoPollutionOpf fixture forces the test in 12-02 |
| T-12-01 | DoS | decompression amplification (future plans) | mitigate | EPUB_MAX_ENTRY_BYTES + filter-before-inflate discipline pinned in limits.ts; bombEntryBook fixture forces the test in 12-02 |
</threat_model>

<verification>
npx vitest run tests/unit/ingestion-schema.test.ts green; npx tsc --noEmit green; fixture generator imports cleanly with the module-load self-check passing; no fast-xml-parser import anywhere under src/; approval doc records pin + evidence.
</verification>

<success_criteria>
Approved exact fast-xml-parser pin installed with the D12-15 approval record; EPUB_MAX_BYTES/EPUB_MAX_CHAPTERS/EPUB_EXTRACTION_TIMEOUT_MS/EPUB_MAX_ENTRY_BYTES/max()-derived MAX_INGEST_BODY_BYTES defined; epub request variant + four failure reasons + epub-chapter source + Book badge + BookSchema + bookId/chapterIndex shipped additively; book ok-variant parses alongside the byte-stable article variant; 20-builder self-verifying fixture generator committed with zero binary fixtures.
</success_criteria>

## Artifacts this phase produces

- `EPUB_MAX_BYTES` constant — src/ingestion/types.ts (re-exported by server/limits.ts)
- `EPUB_MAX_CHAPTERS`, `EPUB_EXTRACTION_TIMEOUT_MS`, `EPUB_MAX_ENTRY_BYTES` constants — server/limits.ts; `MAX_INGEST_BODY_BYTES` re-derived from Math.max(PDF_MAX_BYTES, EPUB_MAX_BYTES)
- `epub` request variant (base64 + optional filename) on IngestionRequestSchema; four new IngestionFailureReasonEnum members: epub-protected, epub-unreadable, epub-empty, epub-too-large (catalog: 20)
- Second IngestionResponseSchema ok-variant: {ok: true, book, articles (min 1), skippedCount}
- `BookSchema` + `Book` type — src/content/schema.ts (id/title/authors/language/chapterArticleIds/publisher/publishedDate/identifier/skippedChapterCount/source "epub-upload"/originalFileHash/tags/addedAt)
- `IngestionMetaSchema.bookId` + `.chapterIndex` optional fields; `epub-chapter` member on ArticleSourceSchema; `case "epub-chapter"` → "Book" on SourceBadge
- tests/unit/server/epub-fixtures.ts — 20 named EPUB builders + module-load self-check (validBookEpub3, publisherSplitBook, ncxOnlyBook, oebpsNestedBook, deepNavBook, degenerateTocBook, frontMatterBook, drmAdeptBook, drmLcpBook, drmUnknownAlgBook, fontObfuscatedBook, corruptNotEpub, missingContainerBook, entityBombOpf, protoPollutionOpf, zipSlipBook, bombEntryBook, imageChapterBook, emptyBook, mixedAdmissionBook)
- .planning/phases/12-epub-intake/12-01-fxml-approval.md — the D12-15 legitimacy/approval record
- Spec files epub-to-books.spec.ts / ingest-epub.spec.ts / epub-intake.spec.ts are created by their owning plans (12-02 / 12-04 / 12-05), not pre-created here

<output>
Create `.planning/phases/12-epub-intake/12-01-SUMMARY.md` when done
</output>
