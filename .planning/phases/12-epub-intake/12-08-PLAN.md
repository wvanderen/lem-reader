---
phase: 12-epub-intake
plan: 08
type: execute
wave: 6
depends_on: ["12-06", "12-07"]
files_modified:
  - tests/unit/server/epub-calibration/harness.ts
  - tests/unit/server/epub-calibration/harness.test.ts
  - tests/unit/server/epub-calibration/manifest.json
  - tests/unit/server/epub-calibration/epub-evidence.json
  - tests/unit/server/epub-calibration/derive.spec.ts
  - tests/unit/server/epub-calibration/replay.spec.ts
  - corpus/epub/README.md
  - docs/epub-calibration.md
  - .gitignore
  - package.json
  - .planning/phases/12-epub-intake/12-08-OUTPUT.md
  - .planning/phases/12-epub-intake/12-VALIDATION.md
autonomous: false
requirements: ["ING-05"]
user_setup:
  - service: local EPUB calibration corpus
    why: "D12-12 — real EPUBs stay local + gitignored (licensing/size); only derived evidence commits"
must_haves:
  truths:
    - "A committed manifest + committed epub-evidence.json record the corpus, its SHA-256 integrity, and the EPUB_THRESHOLDS that produced every verdict (D12-12, the D11-04/05 mirror)"
    - "CI replays the committed evidence and fails honestly when the record is missing — never a silent skip"
    - "Every corpus book parses DRM-free, admits its expected chapter count, uses TOC-merge (not fallback) when its nav/NCX resolves, and passes the anchor gate on every admitted chapter before promotion"
    - "The vendor renderer dependency is absent: the package name token appears nowhere in src/, server/, or package.json dependencies, and the XML parser is absent from the built client bundle (dist/ grep)"
    - "The full honest test suite passes in one invocation with counts + exit code recorded in 12-08-OUTPUT.md (the 04-11/09-07/11-06 discipline)"
  artifacts:
    - path: "tests/unit/server/epub-calibration/harness.ts"
      provides: "importable calibration core — manifest/evidence loaders, SHA-256 verification, validateEvidence, deriveEvidence, refuse-empty guards"
      exports: ["loadManifest", "verifyCorpus", "validateEvidence", "deriveEvidence", "writeEvidence"]
      min_lines: 120
    - path: "tests/unit/server/epub-calibration/replay.spec.ts"
      provides: "always-on CI replay of the committed evidence against the D12-12 bar + the EPUB_THRESHOLDS pin"
      contains: "validateEvidence"
    - path: "docs/epub-calibration.md"
      provides: "the local derive workflow + CI replay contract"
      min_lines: 30
  key_links:
    - from: "tests/unit/server/epub-calibration/harness.ts"
      to: "server/epubToBooks.ts"
      via: "derive runs the real adapter + orchestrator path and snapshots EPUB_THRESHOLDS into the evidence record"
      pattern: "EPUB_THRESHOLDS"
    - from: "package.json"
      to: "tests/unit/server/epub-calibration/derive.spec.ts"
      via: "the calibrate:epub script runs the derive path behind the gating env var"
      pattern: "calibrate:epub"
---

<objective>
Ship the D12-12 calibration instrument and run it: the harness core (the 11-06 pdf-calibration mirror), the human-supplied local real-EPUB corpus behind a blocking checkpoint, threshold tuning against the promotion bar, the committed evidence record CI replays, the SC#4 structural gates (no renderer dependency anywhere; XML parser absent from the client bundle), and the honest full-suite phase gate.

Purpose: The two novel logics (TOC-merge, DRM allowlist) were built from spec-verified shapes — the corpus proves them against real publisher weirdness before the phase closes, and the replay keeps the proof alive in CI.
Output: Calibration evidence committed + replay-pinned; SC#4 grep gates green; full-suite exit-0 record.

Scope note (checker-reviewed): 12 files modified — a file-for-file mirror of the proven pdf-calibration layout plus its docs/gitignore/package wiring and the OUTPUT record; the cohesion is the mirror itself. Accepted as-is.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@tests/unit/server/pdf-calibration/
@server/epubToBooks.ts
@server/ingest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibration harness core + wiring + docs</name>
  <files>tests/unit/server/epub-calibration/harness.ts, tests/unit/server/epub-calibration/harness.test.ts, corpus/epub/README.md, docs/epub-calibration.md, .gitignore, package.json</files>
  <read_first>tests/unit/server/pdf-calibration/harness.ts + harness.test.ts + derive.spec.ts + replay.spec.ts (the file-for-file mirror source), server/epubToBooks.ts (EPUB_THRESHOLDS + the result contract incl. fallbackUsed), server/ingest.ts (the ingest entry the derive path drives), .gitignore, package.json (the calibrate:pdf script precedent)</read_first>
  <action>Mirror the pdf-calibration layout file-for-file:
   1. tests/unit/server/epub-calibration/harness.ts — importable core, plain functions, no process.exit: loadManifest (zod-validated { schemaVersion: 1, entries: [{ file, sha256, expected: { drmFree: true, navType: "nav" | "ncx" | "none", expectedChapters: number, tocResolvable: boolean, producer? } }] }); the evidence schema { schemaVersion: 1, generatedAt, thresholds: EPUB_THRESHOLDS snapshot, results: [{ file, sha256, verdict: "admitted" | "refused:" + reason, chapterCount?, fallbackUsed?, anchorRoundTrip? }] }; verifyCorpus (presence + node:crypto SHA-256 vs manifest); validateEvidence(manifest, evidence) enforcing the D12-12 promotion bar: every entry present, every drm-free corpus book admitted, admitted chapterCount === expectedChapters, fallbackUsed === false whenever expected.tocResolvable, anchorRoundTrip === true on every admitted entry, non-empty results; deriveEvidence(corpusDir): per EPUB — read bytes, base64, call the real ingest({ epub }) recording the typed verdict + chapterCount + fallbackUsed + anchorRoundTrip (admitted implies the per-chapter anchor gate passed — record it explicitly from the envelope shape); snapshot EPUB_THRESHOLDS; writeEvidence with the refuse-empty guard (never overwrite a committed record with a placeholder — the fingerprint.compare exit-2 discipline).
   2. Wiring — .gitignore gains corpus/epub/; corpus/epub/README.md explains the local-only convention (D12-12: never commit real books, never synthesize calibration fixtures — the synthetic corpus is 12-01's job); package.json script calibrate:epub = EPUB_CALIBRATION_DERIVE=1 vitest run tests/unit/server/epub-calibration/derive.spec.ts (vitest owns TS resolution; the env var gates the local-only derive).
   3. tests/unit/server/epub-calibration/harness.test.ts — pure in-memory fixtures (no corpus needed): validateEvidence passes a hand-built manifest+evidence pair; fails each bar clause independently (wrong chapterCount, fallback on a resolvable book, missing anchor flag, empty results); verifyCorpus flags a sha256 mismatch; writeEvidence refuses empty results.
   4. tests/unit/server/epub-calibration/derive.spec.ts — describe.skipIf(process.env.EPUB_CALIBRATION_DERIVE !== "1") wrapper around the derive run (the pdf derive precedent — inert in CI).
   5. docs/epub-calibration.md — the derive + replay workflow, the corpus shape requirements (mapped to the Task 2 checkpoint), and the CI-absence contract (replay fails loudly when evidence is missing).
   6. tests/unit/server/epub-calibration/replay.spec.ts + manifest.json + epub-evidence.json — created in Task 3 with the real corpus output; the replay spec itself: always-on, loads the committed manifest + evidence, runs validateEvidence, pins EPUB_THRESHOLDS deep-equal against the evidence snapshot, and fails with the loud message "EPUB calibration requires the local corpus — see docs/epub-calibration.md" when records are absent.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-calibration/harness.test.ts && git check-ignore corpus/epub/x.epub</automated>
  </verify>
  <acceptance_criteria>
    - All six harness exports exist and the in-memory bar tests pass (each clause independently provable)
    - git check-ignore corpus/epub/x.epub exits 0; package.json contains calibrate:epub; docs/epub-calibration.md exists with the derive + replay workflow
    - The replay spec's missing-record path emits the loud calibration-requires message (assert via a temp-rename test inside harness.test.ts or the replay spec's absence branch)
    - npx vitest run tests/unit/server/epub-calibration/harness.test.ts exits 0
  </acceptance_criteria>
  <done>The calibration instrument exists, is unit-proven with in-memory fixtures, the corpus dir is gitignored, and the workflow is documented — ready for the real corpus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Supply the local real-EPUB corpus (D12-12)</name>
  <what-built>The harness, corpus directory, and docs from Task 1. The corpus itself is human-supplied: real books cannot be synthesized for calibration (D12-12 explicitly reserves synthetic fixtures for code paths) and stay out of git for licensing/size reasons.</what-built>
  <how-to-verify>
    1. Place 6-10 DRM-free EPUBs into corpus/epub/ spanning the shapes the bar discriminates: at least 2 EPUB 3 nav books (one with a deep-nested TOC, one with publisher chapter-splitting), at least 1 EPUB 2 NCX-only book, at least 1 with the OPF nested under OEBPS/, at least 1 with real front matter before the first TOC entry, and where findable one degenerate single-entry TOC.
    2. Note each file's shape class — Task 3 records it in the manifest (file, sha256, expected shape).
    3. Confirm none of the files are DRM-protected (they must admit; a DRM book belongs in a refusal test, not this corpus).
  </how-to-verify>
  <resume-signal>Type "corpus ready: N books" (or describe gaps — the bar can proceed with 4+ if a shape is unfindable, recorded honestly in the manifest)</resume-signal>
  <files>corpus/epub/</files>
  <action>Blocking gate — no code changes. The executor waits for the corpus, then records the per-file expected shapes for Task 3's manifest.</action>
  <verify>ls corpus/epub/*.epub lists the supplied books (local only — gitignored)</verify>
  <done>Corpus present locally with recorded shape expectations; derive may proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Derive + commit evidence + SC#4 gates + honest full-suite phase gate</name>
  <files>tests/unit/server/epub-calibration/manifest.json, tests/unit/server/epub-calibration/epub-evidence.json, tests/unit/server/epub-calibration/replay.spec.ts, .planning/phases/12-epub-intake/12-08-OUTPUT.md, .planning/phases/12-epub-intake/12-VALIDATION.md, server/epubToBooks.ts, package.json</files>
  <read_first>tests/unit/server/pdf-calibration/replay.spec.ts (the thresholds pin + loud CI-absence message), server/epubToBooks.ts (EPUB_THRESHOLDS current values — tuning lands here if the corpus demands it), the Task 1-2 state of the epub-calibration directory, 12-RESEARCH.md §Validation Architecture phase-gate row</read_first>
  <action>1. Author tests/unit/server/epub-calibration/manifest.json from the corpus (file, sha256 via node:crypto, expected shape per the Task 2 record). Run npm run calibrate:epub → epub-evidence.json (thresholds snapshot + per-book verdicts). If any book misses the bar: tune EPUB_THRESHOLDS in server/epubToBooks.ts (strengthen-only — the full synthetic suite from 12-02/12-04 must stay green after any change; re-derive; document each tuning decision in the evidence-adjacent summary).
   2. Commit manifest.json + epub-evidence.json + replay.spec.ts; the replay must pass inside npm run test with NO env vars (always-on).
   3. SC#4 structural gates: (a) the renderer-dependence grep — the npm package-name token for the vendor EPUB renderer must return zero matches across src/, server/, and package.json (import-pattern scope; the dependency list is the strongest guarantee); (b) the client-bundle grep — run npm run build, then grep the dist/ output for the XML parser package name and the fflate server usage — the XML parser must be absent from every dist/assets file (the 11-01 unpdf discipline applied to fast-xml-parser).
   4. Honest full-suite phase gate: run npm run test in ONE invocation (unit + e2e across chromium/firefox/webkit + throttled perf); record the exact counts, skips, and exit code in .planning/phases/12-epub-intake/12-08-OUTPUT.md (the 09-07/11-06 permanent-record format). If any pre-existing unrelated failure appears, record it honestly and scope it — never omit, never subset (the 04-11 anti-pattern guard).
   5. Update the phase VALIDATION.md per-task map rows to green per the executed evidence (the draft template's fill-in step — this step is part of THIS task's completion, not deferred: the fill-in lands in the same commit as the evidence).</action>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-calibration/replay.spec.ts && npm run build && ! grep -rl "fast-xml-parser" dist/assets/ && ! grep -rn "epubjs" src/ server/ package.json</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json + epub-evidence.json are committed; replay.spec.ts passes inside the plain suite (no env gate); the evidence pins EPUB_THRESHOLDS deep-equal
    - Every corpus book's verdict row shows admitted, the expected chapterCount, fallbackUsed false wherever tocResolvable, and anchorRoundTrip true — the D12-12 bar holds on real books
    - The dist/assets grep finds zero fast-xml-parser references; the renderer package-name token has zero matches in src/, server/, and package.json
    - 12-08-OUTPUT.md records the single-invocation full-suite counts and exit code; npm run test exits 0
    - 12-VALIDATION.md per-task map rows are updated to green per the executed evidence in the same commit (step 5 executed — the draft template left no pending fill-in)
    - Any EPUB_THRESHOLDS change kept the 12-02/12-04 synthetic suites green (re-run recorded)
  </acceptance_criteria>
  <done>D12-12 calibration closed with committed replay-pinned evidence; SC#4 structural gates green; the phase's honest full-suite record exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local corpus files → derive harness | real publisher bytes cross into the adapter (the same untrusted-input surface as production uploads) |
| committed evidence → CI replay | the record is integrity-checked against the manifest SHA-256s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20 | Tampering | silently loosened admission via threshold drift | mitigate | replay pins EPUB_THRESHOLDS deep-equal against the committed evidence; any change re-derives + keeps synthetic suites green (the 11-06 CI-replay-pin discipline) |
| T-12-21 | Repudiation | unverifiable calibration claims | mitigate | committed manifest SHA-256s + evidence + the honest full-suite OUTPUT record — every claim replayable |
| T-12-SC | Tampering | supply-chain regression post-approval | mitigate | the dist/ grep proves the gated dependency never reached the client bundle; the dependency list grep proves no renderer package entered |
</threat_model>

<verification>
Replay green in- suite; corpus bar met on real books; build + both grep gates clean; full npm run test exit 0 recorded.
</verification>

<success_criteria>
D12-12 closed (instrument + corpus + committed evidence + honest CI absence); SC#4's structural proofs green (adapter isolation, no renderer dependency, clean client bundle); the phase exits with the honest full-suite record the verify-work step consumes.
</success_criteria>

## Artifacts this phase produces

- tests/unit/server/epub-calibration/ — harness.ts (loadManifest/verifyCorpus/validateEvidence/deriveEvidence/writeEvidence), harness.test.ts, derive.spec.ts (env-gated), replay.spec.ts (always-on), manifest.json, epub-evidence.json
- corpus/epub/README.md + .gitignore entry; docs/epub-calibration.md; package.json calibrate:epub script
- .planning/phases/12-epub-intake/12-08-OUTPUT.md — the honest full-suite phase record
- Possibly tuned EPUB_THRESHOLDS values in server/epubToBooks.ts (strengthen-only, synthetic suites re-proven)

<output>
Create `.planning/phases/12-epub-intake/12-08-SUMMARY.md` when done
</output>
