---
phase: 17-reader-owned-metadata
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/ingestion/library/LibraryRow.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/ingestion/library/EditMetadataDialog.tsx
  - src/ingestion/library/libraryFilter.ts
  - src/ingestion/library/ContinueReadingStrip.tsx
  - src/app.css
  - tests/unit/library-search.test.ts
  - tests/e2e/library/metadata-edit.spec.ts
autonomous: true
requirements: [META-01, META-02, META-03]
must_haves:
  truths:
    - "A reader opens an edit dialog from a Library row, saves a title and author override, and the row shows the effective values while the stored row's provenance, id, and revision are untouched (D17-01, D17-02, META-01)"
    - "A blank title cannot be saved — Save is disabled with an inline explanation — and per-field Reset restores the canonical value, including returning an absent canonical author to no-author-shown with the override key deleted from the row (D17-03, D17-04, META-03)"
    - "Search matches the override only — a renamed article does not surface for its old canonical name (D17-07)"
    - "The Continue Reading strip and the remove-dialog copy show effective values; book rows, chapter sub-rows, and Sample fixture rows have NO edit affordance (D17-05, D17-06, fixture gate)"
  artifacts:
    - path: "src/ingestion/library/EditMetadataDialog.tsx"
      provides: "Native-dialog edit surface (structural clone of RemoveConfirm/AddDialog grammar) with the single override write"
      contains: "readerTitle"
      min_lines: 120
    - path: "src/ingestion/library/LibraryRow.tsx"
      provides: "onEdit optional prop + EditIcon affordance + effective-value display swap"
      contains: "library-row-edit"
    - path: "src/ingestion/library/LibraryView.tsx"
      provides: "editTarget state + dialog mount + fixture/chapter gating + refresh on save"
      contains: "EditMetadataDialog"
    - path: "src/ingestion/library/libraryFilter.ts"
      provides: "Article haystack swapped to effective values (override-only matching)"
      contains: "effectiveTitle"
    - path: "tests/e2e/library/metadata-edit.spec.ts"
      provides: "Dialog flow, reset, blank-refusal, fixture-gate, search-consistency e2e"
  key_links:
    - from: "src/ingestion/library/EditMetadataDialog.tsx Save onClick"
      to: "db.articles (Dexie)"
      via: "the ONLY db.articles.put call site — conditional-spread row build omitting cleared keys (Pitfall 8 single write; Pitfall 2 never persist a blank override)"
      pattern: "db\\.articles\\.put"
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "src/ingestion/library/EditMetadataDialog.tsx"
      via: "onEdit passed only on top-level article rows with ingestionMeta defined; onSaved closes + bumps refreshKey (removeTarget precedent)"
      pattern: "ingestionMeta !== undefined"
    - from: "src/ingestion/library/libraryFilter.ts"
      to: "src/ingestion/library/effectiveMetadata.ts"
      via: "haystack first two slots use effectiveTitle/effectiveAuthor — what you see is what matches (D17-07)"
      pattern: "effectiveTitle\\(a\\)"
---

<objective>
Ship META-01/02/03's library-side behavior: the Library-row edit affordance (D17-01), the native edit dialog with calm validation and per-field Reset (D17-02, D17-03, D17-04), the override write with atomic whole-row semantics, and the effective-value swap across every library-surface consumer (row, remove copy, strip, search haystack — D17-07, D17-09) while books/chapters/fixtures stay untouched (D17-05, D17-06, OQ1 fixture gate).

Purpose: The reader-facing half of reader-owned metadata — editing, clearing, and seeing one name in the Library. Reader/review/export surfaces swap in parallel plan 17-03; portability lands in 17-04.
Output: EditMetadataDialog (new), LibraryRow onEdit/EditIcon + display swap, LibraryView wiring + gating, libraryFilter haystack swap, ContinueReadingStrip swap, app.css affordance + dialog styles, unit + e2e coverage.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reader-owned-metadata/17-CONTEXT.md
@.planning/phases/17-reader-owned-metadata/17-RESEARCH.md
@.planning/phases/17-reader-owned-metadata/17-PATTERNS.md
@.planning/phases/17-reader-owned-metadata/17-01-SUMMARY.md
@src/ingestion/library/RemoveConfirm.tsx
@src/ingestion/AddDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Effective-value swap on library surfaces + override-only search haystack</name>
  <files>src/ingestion/library/LibraryRow.tsx, src/ingestion/library/LibraryView.tsx, src/ingestion/library/libraryFilter.ts, src/ingestion/library/ContinueReadingStrip.tsx, tests/unit/library-search.test.ts</files>
  <read_first>
  - src/ingestion/library/LibraryRow.tsx (full — heading L90, .meta author truthy guard L92-93, remove aria-label template L128, onRemove prop L50)
  - src/ingestion/library/LibraryView.tsx (removeTarget title site ~L702; book-row mapping L710-725 stays untouched)
  - src/ingestion/library/libraryFilter.ts (article haystack ~L88 and book haystack ~L132 — only the article one changes)
  - src/ingestion/library/ContinueReadingStrip.tsx (article entry L228-235; book entries L238-248 stay canonical)
  - src/ingestion/library/effectiveMetadata.ts (17-01 output — the only derivation source)
  - tests/unit/library-search.test.ts (full — existing haystack cells to extend strengthen-only)
  </read_first>
  <behavior>
  - library-search: an article with readerTitle set is found by the override text and NOT found by its old canonical title (D17-07)
  - library-search: an article with readerAuthor set matches the override author; the canonical author text no longer matches once overridden
  - library-search: an article without overrides matches canonical title/author exactly as before (regression)
  </behavior>
  <action>
  Import effectiveTitle/effectiveAuthor from ./effectiveMetadata and swap the VALUE SOURCE only — markup shape, heading ids, aria-label templates, and truthy guards stay byte-stable (strengthen-only anchor discipline):

  1. LibraryRow.tsx: row heading text (L90) and the remove button's aria-label interpolation (L128) use effectiveTitle(article); the .meta author expression (L92-93) uses effectiveAuthor(article) inside the existing truthy guard. The template strings and title-{id} heading id are unchanged — only the interpolated value source changes.

  2. LibraryView.tsx: the removeTarget capture (~L702) reads effectiveTitle(a) instead of the canonical title so RemoveConfirm copy shows the one name (D17-09).

  3. ContinueReadingStrip.tsx: the article entry link text and author line (L228-235) use effectiveTitle/effectiveAuthor keeping the truthy guard. Book entries (entry.book) stay canonical (D17-05).

  4. libraryFilter.ts: in the ARTICLE haystack only (~L88), swap the first two slots to effectiveTitle(a) and effectiveAuthor(a) with an empty-string fallback; domain and tags slots unchanged. The book haystack (~L132) and filterBooks stay canonical (D17-05/D17-06). Add a one-line comment citing D17-07 (override-only matching; a renamed article must not surface for its old name).

  5. tests/unit/library-search.test.ts: add the three behavior cases above, building overridden articles via ArticleSchema.parse (the 17-01 builder discipline).
  </action>
  <acceptance_criteria>
  - rg -c 'effectiveTitle' src/ingestion/library/LibraryRow.tsx finds the heading + aria-label reads; rg -c 'title-' still finds the byte-stable heading id pattern
  - rg -n 'effectiveAuthor' src/ingestion/library/ContinueReadingStrip.tsx finds the swapped read inside a truthy guard; git diff shows book-entry lines untouched
  - src/ingestion/library/libraryFilter.ts article haystack references effectiveTitle/effectiveAuthor; the book haystack lines are byte-unchanged
  - tests/unit/library-search.test.ts contains a case asserting the old canonical title yields NO match after override, and one asserting the override text matches
  - npx vitest run tests/unit/library-search.test.ts exits 0 with no prior case removed
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/library-search.test.ts && npx playwright test tests/e2e/library/search-tag-filter.spec.ts --project=chromium</automated>
  </verify>
  <done>All library surfaces derive their display values from effectiveMetadata; search is override-only both ways (found by new, not by old); fixture-pinned e2e anchors stay green on chromium (fixtures never carry overrides, so their rendered text is byte-stable).</done>
</task>

<task type="auto">
  <name>Task 2: EditMetadataDialog + LibraryRow onEdit/EditIcon + LibraryView wiring + styles</name>
  <files>src/ingestion/library/EditMetadataDialog.tsx, src/ingestion/library/LibraryRow.tsx, src/ingestion/library/LibraryView.tsx, src/app.css</files>
  <read_first>
  - src/ingestion/library/RemoveConfirm.tsx (full — the dialog shell grammar to clone: open/showModal sync, triggerRef focus capture + close-listener restore, explicit [data-initial-focus] focus, dialog markup + inner/actions structure)
  - src/ingestion/AddDialog.tsx (full — prevented-submit labeled form controls L446-462, cancel-event preventDefault + in-flight submittingRef mirror L110-196; NEVER a method-dialog form)
  - src/reader/ImportPreviewDialog.tsx (L122-194 — the openRef/Esc-routing hardening)
  - src/ingestion/library/LibraryView.tsx (removeTarget state L241-246, row callback L699-704, RemoveConfirm mount + onConfirm refreshKey L782-800, AddDialog mount L834)
  - src/ingestion/library/LibraryRow.tsx (onRemove prop L50 + conditional button L124-133 + TrashIcon glyph anatomy L146-171)
  - src/app.css (.library-row-remove L2093-2109 token pattern; .library-remove-confirm* dialog geometry; .library-clear-filters quiet-button tokens)
  - .planning/phases/17-reader-owned-metadata/17-PATTERNS.md (Pattern 3 — the full dialog contract incl. the save seam; Pattern 7 — affordance + fixture gate)
  </read_first>
  <action>
  Build the edit surface per D17-01, D17-02, D17-03, D17-04, D17-08 (structural clone — Pitfall 8: NO shared dialog abstraction, NO shared metadata-write service):

  1. EditMetadataDialog.tsx (NEW). Props: open, article (CanonicalArticle | null), onSaved, onCancel. Clone the RemoveConfirm five-part dialog grammar verbatim: useEffect open/showModal sync with idempotent guards; triggerRef captures document.activeElement on open and the close listener restores it; explicit focus on the [data-initial-focus] control after showModal (WebKit); a cancel-event handler that preventDefaults and routes through onCancel unless the save is in flight (the 09-05 wedge fix); a submittingRef mirror rewritten every render blocking dismissal/double-submit while the put is outstanding (16-02). Markup: native dialog with aria-labelledby pointing at the heading Edit title and author (plain dialog role, NOT alertdialog — non-destructive, AddDialog precedent), inner/actions structure cloning .library-remove-confirm geometry. Form: ONE prevented-submit form (onSubmit preventDefault then Save handler — never a method-dialog form) with two labeled inputs: label Title with htmlFor edit-metadata-title, label Author with htmlFor edit-metadata-author; controlled inputs. Placeholders carry the canonical values — the title placeholder is the article's provenance.title and the author placeholder is provenance.author, or the literal text No author when canonical is absent (D17-03/D17-08: canonical visible ONLY here, display-only, never persisted). State: titleValue initialized to readerTitle or the empty string; authorValue initialized to readerAuthor or the empty string; titleReset boolean initialized false. Per-field quiet Reset buttons (library-clear-filters token treatment) with visible labels Reset title and Reset author: title Reset sets titleValue to empty AND titleReset true; any title input change sets titleReset false; author Reset sets authorValue to empty (an empty author field IS the no-override state per D17-04). Save button disabled while the title is blank after trim AND titleReset is false — with the calm inline explanation line, exact copy: Type a title, or choose Reset to keep the original. (OQ4 pinned rule — empty-title-plus-Save is invalid; Reset is the one explicit clear affordance.) Save onClick is the ONLY db.articles.put call site in the codebase for this feature: build the row as a spread of the article plus conditionally-spread readerTitle/readerAuthor keys present ONLY when the trimmed input is non-empty — cleared fields are OMITTED so the whole-row put deletes the key (META-03; never assign an empty-string value to an override key — Pitfall 2 row-poison). Provenance, id, revision, blocks are carried untouched by the spread (META-01). data-initial-focus on Cancel. After the put resolves, call onSaved.

  2. LibraryRow.tsx: add the optional prop onEdit beside onRemove (same optional-prop pattern); render a conditional edit button immediately before the remove button in the same actions cluster — type button, className library-row-edit, aria-label template: Edit title and author for — interpolated with effectiveTitle(article). EditIcon: a local inline-SVG pencil glyph cloning the TrashIcon anatomy exactly (20 by 20, viewBox 0 0 24 24, fill none, stroke currentColor, strokeWidth 1.75, round linecap/linejoin, aria-hidden, focusable false).

  3. LibraryView.tsx: add editTarget state holding the captured article row (nullable). In the TOP-LEVEL article-row mapping ONLY, pass onEdit={() => setEditTarget(a)} gated on the row being Dexie-persisted — the predicate a.ingestionMeta !== undefined (the SourceBadge fixture inference; bundled Sample rows have nowhere to persist an override — OQ1 resolved via gate, consistent with remove-on-fixture being a silent no-op). Book rows, chapter sub-rows, and fixture rows get NO onEdit (D17-05/D17-06). Mount EditMetadataDialog beside RemoveConfirm: open is editTarget non-null, article is the captured row, onSaved closes editTarget and bumps refreshKey (the removeTarget onConfirm precedent), onCancel closes editTarget.

  4. app.css: .library-row-edit + .library-row-edit:hover cloning the .library-row-remove token pattern but with the hover accent color/border on the existing accent custom property (edit is not destructive); .edit-metadata, .edit-metadata-inner, .edit-metadata-actions, and .edit-metadata-field styles cloning the .library-remove-confirm*/add-dialog geometry with the existing spacing custom properties. 44px touch box via the touch token, transparent rest, zero motion properties — POLISH-07 discipline, no new tokens.
  </action>
  <acceptance_criteria>
  - src/ingestion/library/EditMetadataDialog.tsx exists; rg -c 'db.articles.put' src/ingestion/library/EditMetadataDialog.tsx returns exactly 1 (the single write site) and rg -l 'db.articles.put' across src/ returns only this new file plus pre-existing unrelated sites (no second writer added anywhere)
  - The dialog file contains: data-initial-focus on Cancel, the literal copy Type a title, or choose Reset to keep the original., labels Reset title and Reset author, placeholders reading provenance.title/provenance.author with the No author fallback, and the cancel-event preventDefault routing
  - The Save button's disabled condition references titleReset and the trimmed title value (blank-and-not-reset rule)
  - The row build omits cleared keys via conditional spreads — rg -c 'readerTitle' in the dialog shows schema-key reads/writes only through the spread idiom (no unconditional assignment of an input value to an override key)
  - src/ingestion/library/LibraryRow.tsx contains onEdit as an optional prop, the library-row-edit class, and the aria-label template Edit title and author for
  - src/ingestion/library/LibraryView.tsx gates onEdit with ingestionMeta !== undefined in the top-level article mapping only; BookRow/chapter mapping lines are byte-unchanged; EditMetadataDialog is mounted and onSaved bumps the refresh key
  - src/app.css contains .library-row-edit and .edit-metadata rules consuming var( tokens; no motion properties; no hardcoded palette values
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/remove-cascade.spec.ts tests/e2e/library/browse-open.spec.ts --project=chromium</automated>
  </verify>
  <done>The edit dialog ships as a structural clone with the pinned title-validity rule and single write; the affordance renders beside remove on editable rows only; wiring mirrors removeTarget; existing library dialog/row specs stay green on chromium.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: metadata-edit e2e — dialog flow, reset, blank refusal, fixture gate, search consistency</name>
  <files>tests/e2e/library/metadata-edit.spec.ts</files>
  <read_first>
  - tests/e2e/library/remove-cascade.spec.ts (full — the PASTE_HTML seeding-through-real-middleware pattern + readRow/countRows Dexie-row assertion helpers to clone verbatim)
  - tests/e2e/library/add-dialog.ts (shared dialog helpers if reusable)
  - src/ingestion/library/EditMetadataDialog.tsx (Task 2 result — labels, copy, disabled rule)
  - src/ingestion/library/LibraryView.tsx (Task 2 result — search box + gating)
  </read_first>
  <behavior>
  - Save flow: open the edit dialog from an ingested row, type a new title + author, Save → dialog closes, row heading + meta show the effective values, and readRow asserts the stored row carries readerTitle/readerAuthor with provenance.title and id unchanged (META-01)
  - Reset title: reopen, Reset title, Save → row shows the canonical title; readRow asserts the stored row no longer has a readerTitle key (META-03 — key deleted, not blank)
  - Absent author: on an article whose canonical author is absent, set an author override → author line shows; Reset author, Save → no author line rendered and the readerAuthor key is gone from the row (META-03 absent-author restore)
  - Blank title refusal: clear the title input without Reset → Save disabled + the inline explanation line visible; typing re-enables (D17-04)
  - Esc path: press Esc → dialog closes, editTarget resets (the edit button reopens cleanly), focus returns to the row's edit button (Pitfall 6)
  - Fixture gate: on a fresh install the bundled Sample rows render NO edit button while an ingested row does (OQ1 gate)
  - Persistence: after page.reload the row still shows the override (Dexie round-trip)
  - Search consistency (library box): the renamed article is found by the new name and NOT by the old canonical name (D17-07)
  - Strip consistency: with a saved reading location on the edited article, the Continue Reading strip shows the effective title
  </behavior>
  <action>
  Create tests/e2e/library/metadata-edit.spec.ts cloning the remove-cascade harness: seed articles by ingesting paste HTML through the real /api/ingest middleware (never seed overrides on bundled fixtures — Pitfall 8 spec discipline), reuse/adapt readRow for direct Dexie row assertions, and use the openLibrary reload pattern. Implement the behavior cells above, one describe per decision (cite D17-01..D17-04, D17-07, META-01/03 in case names). Assert row-state truth at the IndexedDB level for override presence/absence, not just UI text. Fixture-gate cell runs on the default composite list (fixtures union Dexie rows).
  </action>
  <acceptance_criteria>
  - tests/e2e/library/metadata-edit.spec.ts exists with cases covering all nine behaviors; rg -c 'test\(' finds at least 9
  - The spec asserts stored-row truth via readRow-style helper (readerTitle present after save; key absent after reset — absence asserted on the raw row, not on UI text)
  - The spec seeds ONLY via the real middleware paste path; no fixture id is edited
  - npx playwright test tests/e2e/library/metadata-edit.spec.ts exits 0 on chromium, firefox, and webkit
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/metadata-edit.spec.ts</automated>
  </verify>
  <done>The full edit lifecycle — save, per-field reset incl. absent author, blank-title refusal, Esc/focus hygiene, fixture gating, reload persistence, override-only search, strip consistency — is proven in all three engines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog inputs → db.articles.put | reader-typed strings cross into persistence here; the only override write path |
| persisted rows → ArticleSchema.parse | reads re-validate; a malformed override would drop the row (containment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering (XSS) | override strings rendered on library surfaces | mitigate | React text-child rendering (escaped by construction); aria-label interpolation is attribute-safe; schema min(1)+string at the boundary (T-8-12 analysis holds) |
| T-17-04 | Denial of Service | blank/oversized override persisted | mitigate | Save disabled on blank (D17-04) + conditional-spread omits cleared keys (Pitfall 2); no length cap — parity with canonical provenance.title (also uncapped, RESEARCH V5) |
| T-17-05 | Tampering | fixture shadow-row materialization | mitigate | ingestionMeta gate prevents Sample rows from ever reaching the put (OQ1/Pitfall 5 — no shadow rows riding exports) |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- npx vitest run tests/unit/library-search.test.ts green
- npx playwright test tests/e2e/library/metadata-edit.spec.ts green on chromium, firefox, webkit
- Existing library specs (remove-cascade, browse-open, search-tag-filter) green — strengthen-only held
- Exactly one db.articles.put call site for overrides repo-wide (grep-verifiable, Pitfall 8)
</verification>

<success_criteria>
- META-01: edit + save leaves identity/provenance/revision/position/annotations untouched (row-level e2e assertions)
- META-03: Reset restores canonical incl. absent author, with the override key deleted from the stored row
- META-02 (library half): row, remove copy, strip, and search all show/match the one effective name; D17-05/D17-06 scope line holds (no affordance off top-level article rows)
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- Component `src/ingestion/library/EditMetadataDialog.tsx` (native dialog; props open/article/onSaved/onCancel; the single override write site)
- LibraryRow prop `onEdit` + `EditIcon` glyph + `library-row-edit` hook class
- LibraryView state `editTarget` + EditMetadataDialog mount + the ingestionMeta edit gate
- CSS hook classes `.library-row-edit`, `.edit-metadata`, `.edit-metadata-inner`, `.edit-metadata-actions`, `.edit-metadata-field`
- New spec `tests/e2e/library/metadata-edit.spec.ts` + extended cases in `tests/unit/library-search.test.ts`
- Pinned UI copy: dialog heading `Edit title and author`, explanation `Type a title, or choose Reset to keep the original.`, field reset labels `Reset title` / `Reset author`, author placeholder fallback `No author`, aria-label template `Edit title and author for …`
</artifacts_produced>

<output>
Create `.planning/phases/17-reader-owned-metadata/17-02-SUMMARY.md` when done
</output>
