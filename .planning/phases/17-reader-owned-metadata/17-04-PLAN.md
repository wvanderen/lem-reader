---
phase: 17-reader-owned-metadata
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/portability/bundle.ts
  - src/portability/ExportImportService.ts
  - src/portability/conflicts.ts
  - src/reader/ImportPreviewDialog.tsx
  - src/reader/SettingsPanel.tsx
  - tests/unit/portability/bundle-schema.test.ts
  - tests/unit/portability/validate-bundle.test.ts
  - tests/unit/portability/export-service.test.ts
  - tests/unit/portability/conflicts.test.ts
  - tests/unit/portability/import-preview-dialog.test.tsx
autonomous: true
requirements: [META-04]
must_haves:
  truths:
    - "Bundles export with schemaVersion 3 carrying overrides inside each article record; v1/v2 bundles import unchanged; a schemaVersion-4 bundle calm-refuses as newer-schema-version (D17-12)"
    - "A same-id article whose readerTitle/readerAuthor differ from local — including one-side-only — surfaces an article-metadata-override conflict with a keep-LOCAL default; an identical duplicate INCLUDING override state stays a calm no-op (D17-11)"
    - "When an incoming article wins on revision or content divergence, the written row keeps the LOCAL overrides (a refresh never renames the library back — D17-10); a per-item take-incoming choice writes the incoming overrides instead"
    - "The ImportPreviewDialog shows a per-article choice for metadata conflicts — default keep mine, optional use imported — and the choice flows through Proceed into resolveImportPlan"
  artifacts:
    - path: "src/portability/bundle.ts"
      provides: "schemaVersion 1|2|3 union (writers emit 3 in ExportImportService)"
      contains: "z.literal(3)"
    - path: "src/portability/conflicts.ts"
      provides: "article-metadata-override conflict kind + metadataDiffers classification + merge-on-win in resolveImportPlan"
      contains: "article-metadata-override"
    - path: "src/reader/ImportPreviewDialog.tsx"
      provides: "Per-item keep-mine/use-imported choice for metadata conflicts (D17-11)"
      contains: "metadataTakeIncoming"
  key_links:
    - from: "src/portability/conflicts.ts resolveImportPlan"
      to: "src/portability/ExportImportService.ts applyImport"
      via: "merge shaping happens in articlesToWrite BEFORE the puts-only transaction — applyImport untouched (D17-13-adjacent: the article put IS the override put; cascade rides row deletion)"
      pattern: "articlesToWrite\\.push"
    - from: "src/reader/ImportPreviewDialog.tsx onProceed"
      to: "src/reader/SettingsPanel.tsx resolveImportPlan call"
      via: "onProceed signature extended with the per-item take-incoming id set; SettingsPanel threads it into resolveImportPlan as itemChoices"
      pattern: "metadataTakeIncoming"
---

<objective>
Ship META-04's portability half: bundle v3 on the 12-07 union-read discipline (D17-12), the article-metadata-override conflict kind with keep-local default and per-item take-incoming choice (D17-11), and merge-on-win so a content refresh never renames the reader's library (D17-10). The removal cascade (D17-13) is structural — overrides live on the article row the existing 4-store remove transaction already deletes — and is proven in 17-05.

Purpose: Reader-owned means owned across devices: overrides round-trip, conflicts are explicit, and local names survive imports.
Output: bundle.ts v3 union; ExportImportService writer emits 3 + peek refuses 4+; conflicts.ts kind + classification + merge; ImportPreviewDialog per-item UI + SettingsPanel threading; five unit test files extended (incl. the 12-07-discipline assertion updates in export-service/bundle-schema/validate-bundle).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reader-owned-metadata/17-CONTEXT.md
@.planning/phases/17-reader-owned-metadata/17-RESEARCH.md
@.planning/phases/17-reader-owned-metadata/17-PATTERNS.md
@.planning/phases/17-reader-owned-metadata/17-01-SUMMARY.md
@src/portability/conflicts.ts
@src/reader/ImportPreviewDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bundle v3 — union read, writer emits 3, peek refuses 4+</name>
  <files>src/portability/bundle.ts, src/portability/ExportImportService.ts, tests/unit/portability/bundle-schema.test.ts, tests/unit/portability/validate-bundle.test.ts, tests/unit/portability/export-service.test.ts</files>
  <read_first>
  - src/portability/bundle.ts (full — schemaVersion union L40-56 with the 12-07 comments; BUNDLE_FILENAME; the articles array composing ArticleSchema — overrides ride automatically once schema-declared, D17-12)
  - src/portability/ExportImportService.ts (writer buildBundleBytes ~L112-125; peek threshold ~L238-244; applyImport puts ~L336-348 which stay untouched)
  - tests/unit/portability/bundle-schema.test.ts (full — the 12-07 v1/v2 matrix to extend; the forward-compat rejection case L163-169 and the L4-5 header union comment both pin v3 as refused and must move to v4)
  - tests/unit/portability/validate-bundle.test.ts (full — peek refusal cases to extend; the peek-priority fixture L247-271 and the buildBundleBytes round-trip assertion L432 both pin the old writer version)
  - tests/unit/portability/export-service.test.ts (the writer-output sites: assertion L316, case name + assertion L381/L408, case name + assertion L423/L428 — these break when the writer emits 3)
  - .planning/phases/17-reader-owned-metadata/17-RESEARCH.md (Pattern 5 — exact edit sites + compatibility matrix; Pitfall 7 manifest determinism)
  </read_first>
  <behavior>
  - bundle-schema: a v3 bundle object with an article carrying readerTitle/readerAuthor parses successfully and the parsed article retains both fields (D17-12 — inside ArticleSchema, no separate block)
  - bundle-schema: v1 and v2 bundle fixtures parse exactly as before (union read — regression)
  - bundle-schema: a v3 bundle whose article has an empty-string readerTitle fails to parse (schema min(1) holds at the bundle boundary)
  - bundle-schema: the forward-compat rejection case now refuses schemaVersion 4 — 3 must parse (the flip of the existing L163-169 case)
  - validate-bundle: a bundle with schemaVersion 4 refuses with the newer-schema-version kind; schemaVersion 3 passes the peek
  - validate-bundle: the peek-priority damaged fixture flips to schemaVersion 4 refusing with bundleVersion 4; the buildBundleBytes round-trip assertion expects 3 (writer output)
  - export-service (via existing suite): the built bundle carries schemaVersion 3 — the existing v2-emit assertions flip 2→3 (12-07 version-bump precedent)
  </behavior>
  <action>
  Four scoped edits replaying the 12-07 v2 bump (D17-12):

  1. src/portability/bundle.ts — extend the schemaVersion union at ~L43 with a third literal for 3, extending the adjacent version-history comment to name Phase 17 reader-owned metadata fields. BUNDLE_FILENAME is NOT changed (the filename is not the version contract — 12-07). manifest.ts is NOT touched (Pitfall 7 — it hashes the Zod-parsed block; new fields hash identically on both sides).

  2. src/portability/ExportImportService.ts — in buildBundleBytes (~L115) change the emitted schemaVersion literal from 2 to 3 with a comment citing Phase 17; in the peek guard (~L239) change the comparison from greater-than 2 to greater-than 3 so v4+ still calm-refuses as newer-schema-version. applyImport and its puts-only closure stay byte-unchanged.

  3. bundle-schema.test.ts and validate-bundle.test.ts — extend with the new behavior cases AND apply the legitimate version-bump assertion flips (the 12-07 v2-bump precedent: when the union grows, the prior version's refusal cases and writer-emit assertions are UPDATED, explicitly not strengthen-only): (a) bundle-schema.test.ts forward-compat case L163-169 flips from rejecting schemaVersion 3 to rejecting schemaVersion 4 (case name + fixture literal; also refresh the L4-5 header comment to name the 1|2|3 union and v4+ forward-reject); (b) validate-bundle.test.ts peek-priority test L247-271 flips its damaged fixture from schemaVersion 3 to 4 and the expected refusal to bundleVersion: 4, refreshing the L250 comment and the L12 header note (v3 now readable — Phase 17); (c) validate-bundle.test.ts L432 flips toBe(2) → toBe(3) with its trailing writers-emit comment re-worded. Read-path assertions over v1/v2 INPUT fixtures (e.g. the v2-peek-accept test L273-303 asserting the parsed v2 stays 2) are NOT writer output and stay unchanged — union read means v2 bundles still parse as v2.

  4. tests/unit/portability/export-service.test.ts — the writer bump breaks its existing v2-emit assertions; update them in the same 12-07 assertion-update discipline: L316, L408, L428 expect(bundle.schemaVersion) flips toBe(2) → toBe(3); the case names at L381 and L423 re-word emits v2 / still emits v2 to v3. Input-side fixtures are NOT writer output and stay unchanged (the v1 bundle fixtures at L128/143/156; the L88 schemaVersion belongs to ReaderSettings, unrelated to the bundle writer).
  </action>
  <acceptance_criteria>
  - rg -n 'z.literal\(3\)' src/portability/bundle.ts finds the union member; the union still contains the literals for 1 and 2
  - rg -n '3 as const' src/portability/ExportImportService.ts finds the writer emission with a Phase 17 comment; rg -n '> 3' finds the peek threshold
  - rg -c 'lem-reader-bundle-v1' src/portability/ExportImportService.ts is unchanged (BUNDLE_FILENAME untouched); git diff on src/portability/manifest.ts is empty
  - rg -c 'toBe\(3\)' tests/unit/portability/export-service.test.ts finds at least 3 (the flipped writer-emit assertions); rg -c 'rejects schemaVersion 4' tests/unit/portability/bundle-schema.test.ts finds the flipped forward-compat case; validate-bundle.test.ts expects bundleVersion: 4
  - npx vitest run tests/unit/portability/bundle-schema.test.ts tests/unit/portability/validate-bundle.test.ts tests/unit/portability/export-service.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/bundle-schema.test.ts tests/unit/portability/validate-bundle.test.ts tests/unit/portability/export-service.test.ts</automated>
  </verify>
  <done>Writers emit v3 with overrides riding the articles array (existing writer-emit assertions updated 2→3 per the 12-07 precedent); v1/v2 import unchanged; v4+ calm-refuses (refusal cases flipped 3→4); manifest + applyImport byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: conflicts.ts — article-metadata-override kind, classification, merge-on-win</name>
  <files>src/portability/conflicts.ts, tests/unit/portability/conflicts.test.ts</files>
  <read_first>
  - src/portability/conflicts.ts (full — ConflictKind union + kind-list comment L11/L51-57; detectImportPreview classification loop ~L318-331 incl. the identical-duplicate calm no-op ~L553-555 area; resolveImportPlan signature L474-479 + articles decision tree ~L529-556; the zero-writes module contract L20-24)
  - tests/unit/portability/conflicts.test.ts (full — the fake-indexeddb + Dexie harness and describe-per-behavior shape to extend)
  - .planning/phases/17-reader-owned-metadata/17-RESEARCH.md (Pattern 6 + Code Examples — classification sketch + merge-on-win; Pitfall 3 + Pitfall 4)
  </read_first>
  <behavior>
  - Classification: same id + same revision + same hash + readerTitle differs (both sides set) → exactly one article-metadata-override conflict, no writes
  - Classification: one-side-only differences (incoming has override, local does not; and the mirror) → article-metadata-override conflict (D17-11 verbatim — Pitfall 4 fix: the identical-duplicate calm no-op now requires override state to match)
  - Classification: identical id+revision+hash+override state → still a calm no-op (zero conflicts — regression)
  - Classification: revision differs AND metadata differs → classified article-revision (exclusive else-if order: revision, then divergence, then metadata) — metadata protection in that branch comes from merge-on-win, not from a second conflict row
  - Resolution: metadata-only conflict + default skip → nothing written, local override intact, counted skipped (keep-LOCAL default, D17-11)
  - Resolution: metadata-only conflict + the id in the take-incoming set → the incoming row is written whole (its overrides win; an incoming row WITHOUT an override key removes the local override — explicit reader choice)
  - Resolution: incoming revision+1 wins under overwrite + keep-local → the written row carries the incoming revision/content with the LOCAL readerTitle/readerAuthor merged on (D17-10 — a refresh never renames the library back; Pitfall 3 fix)
  - Resolution: incoming revision+1 wins + the id take-incoming → the incoming row wins whole (no merge)
  - Resolution: divergence overwrite + keep-local → same merge rule as revision-win
  </behavior>
  <action>
  Additive extensions only (the module keeps zero writes — merge is data shaping inside articlesToWrite):

  1. ConflictKind union (+ its doc comment): add the seventh kind article-metadata-override — same id where readerTitle/readerAuthor differ, including one-side-only.

  2. detectImportPreview classification: after the existing divergence branch, add an else-if calling a new exported-or-local predicate metadataDiffers(a, local) — strict inequality on readerTitle OR readerAuthor — pushing into a metadataConflicts id list summarized like the existing kinds. The final else (identical duplicate) now implicitly requires override equality — add a comment noting the Pitfall 4 fix (the calm no-op requires override state to match). ALSO: extend ImportPreviewData additively with a metadataConflicts detail array (one entry per conflicted id carrying localReaderTitle/incomingReaderTitle/localReaderAuthor/incomingReaderAuthor, optional strings) so the dialog can render every conflicted article — the existing summarize sampleIds cap of 5 is display-only and must not limit the per-item choice.

  3. resolveImportPlan: add a fifth optional parameter itemChoices with a single field metadataTakeIncoming (a ReadonlySet of article ids, defaulting to empty when omitted — existing callers unchanged). In the articles decision tree: (a) metadata-only conflict branch — default skip keeps local (counted skipped); when the id is in the take-incoming set, push the incoming row whole; (b) revision-win and divergence-win branches where the incoming row is pushed — when the id is NOT in the take-incoming set, push the incoming row with the local readerTitle/readerAuthor spread onto it (merge-on-win, D17-10); when it is, push the incoming row unmerged. The keep-higher-revision path where LOCAL wins stays untouched (no write — local row and its overrides already safe).
  </action>
  <acceptance_criteria>
  - rg -c 'article-metadata-override' src/portability/conflicts.ts finds the kind union + classification + (via the Overrides record) resolution references; the kind-list header comment names it
  - The classification branch order is revision → divergence → metadata → no-op (verifiable by reading the else-if chain); metadataDiffers uses strict inequality on both fields
  - resolveImportPlan accepts the optional itemChoices/metadataTakeIncoming parameter; calls without it behave identically to HEAD on a no-override library (default-empty set — existing tests green unchanged)
  - tests/unit/portability/conflicts.test.ts gains the nine behavior cases (classification 4 + resolution 5) with decision-named describes citing D17-10/D17-11; no prior case removed
  - npx vitest run tests/unit/portability/conflicts.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/conflicts.test.ts tests/unit/portability/atomic-import.test.ts</automated>
  </verify>
  <done>The seventh kind classifies honestly (one-side-only included, identical-override no-op preserved), keep-local is the default, take-incoming is per-item, and merge-on-win protects local names across content refreshes — all unit-proven; the module still performs zero writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ImportPreviewDialog per-item choice + SettingsPanel threading</name>
  <files>src/reader/ImportPreviewDialog.tsx, src/reader/SettingsPanel.tsx, tests/unit/portability/import-preview-dialog.test.tsx</files>
  <read_first>
  - src/reader/ImportPreviewDialog.tsx (full — DEFAULT_OVERRIDES L54, KIND_LABELS L65-78, KEEP_BOTH_KINDS L83, the conflict li + select shape L210-237, onProceed contract L24-47 + the single invocation site L188-193)
  - src/reader/SettingsPanel.tsx (the Proceed wiring around L306-312 + the dialog mount L593)
  - tests/unit/portability/import-preview-dialog.test.tsx (full — render + interaction patterns to extend)
  - .planning/phases/17-reader-owned-metadata/17-PATTERNS.md (No Analog Found — per-item choice shape; OQ2 recommendation)
  </read_first>
  <behavior>
  - The preview renders the metadata conflict summary row with KIND_LABELS copy: one article with a different title or author / other articles with a different title or author
  - Expanding the metadata conflict row lists EVERY conflicted article (not just the cap-5 samples) with the local name and the incoming name visible
  - Each per-article row defaults to keep mine; toggling to use imported adds the id to the choice payload; toggling back removes it
  - The per-kind select for the new kind offers skip/overwrite only — keep-both is NOT offered (KEEP_BOTH_KINDS narrowing precedent)
  - Proceed fires onProceed with the overrides AND the take-incoming id set; Cancel/Esc resets the per-item state alongside the existing reset paths
  </behavior>
  <action>
  Extend the shipped dialog (D17-11 — per-item choice is REQUIRED, not optional):

  1. DEFAULT_OVERRIDES gains article-metadata-override mapping to skip (keep-LOCAL default). KIND_LABELS gains the pinned copy above. KEEP_BOTH_KINDS is NOT extended (the kind is meaningless for keep-both).

  2. Per-item choice UI under the metadata conflict li: a disclosure control (a button toggling an expanded region) that, when open, renders one row per entry of the preview's metadataConflicts detail array from Task 2 — showing the local effective name and the incoming name, with a two-option choice control per article labeled Keep mine and Use imported (radio-pair or select — follow the existing select markup grammar; aria-label template naming the article, e.g. Import choice for followed by the local name). Component state: metadataTakeIncoming as a Set of ids, defaulting empty, reset whenever the dialog state machine resets (mirror every existing reset path — the 09-06 close-path lesson).

  3. onProceed signature: extend the callback type to pass the take-incoming id set as a third argument (overrides and applyPreferences unchanged). SettingsPanel's Proceed handler threads it into resolveImportPlan as the itemChoices parameter. The single-invocation-site discipline (Pitfall 8) is preserved — still exactly one onProceed call.

  4. Extend import-preview-dialog.test.tsx strengthen-only with the behavior cases (render the summary row + expanded list from a crafted preview payload; default payload empty; toggle adds/removes ids; keep-both absent for the kind).
  </action>
  <acceptance_criteria>
  - rg -c 'article-metadata-override' src/reader/ImportPreviewDialog.tsx finds the DEFAULT_OVERRIDES entry and KIND_LABELS entry; rg -c 'KEEP_BOTH_KINDS' context shows the array unchanged (highlight-id/note-id only)
  - The dialog contains the per-item state (metadataTakeIncoming) and the Keep mine / Use imported labels; every reset path clears it
  - onProceed's type carries the id set; src/reader/SettingsPanel.tsx resolveImportPlan call passes it through as itemChoices
  - tests/unit/portability/import-preview-dialog.test.tsx gains cases for summary row, full per-item list beyond 5 entries, default-empty payload, toggle add/remove, and keep-both absence; npx vitest run on the file exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/import-preview-dialog.test.tsx</automated>
  </verify>
  <done>The conflict row reports honestly and the reader can take an incoming name per article; the choice reaches resolveImportPlan; existing dialog behaviors (focus, reset, single Proceed) hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle zip → ExportBundleSchema (peek + parse) | hostile override strings arrive here from another device's (or a crafted) bundle |
| resolveImportPlan → applyImport transaction | the only same-id article write path (the AddDialog has(id) dedupe-refuse blocks ingestion clobber) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | hostile bundle injects oversized/structural override strings | mitigate | ArticleSchema string+min(1) at bundle parse; React text-child rendering; zip bomb cap (200MB) upstream; no length cap — parity with uncapped canonical provenance.title (RESEARCH V5) |
| T-17-09 | Tampering | import overwrite silently renames the library (T-9-11/T-7-28 extension) | mitigate | Merge-on-win keeps local overrides on every incoming-wins branch (D17-10); puts-only atomic transaction unchanged; skip-by-default (D17-11) |
| T-17-10 | Spoofing | forged override fields impersonate/squat | accept | Metadata is display-only; identity remains id/revision/hashes; every differing override surfaces explicitly as a conflict — never silently adopted (honesty constraint) |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- npx vitest run tests/unit/portability/ green (bundle-schema, validate-bundle, export-service, conflicts, atomic-import, import-preview-dialog)
- v1/v2 bundle fixtures import unchanged; writer emits 3; peek refuses 4+
- resolveImportPlan byte-compatible for existing callers (optional param default empty)
</verification>

<success_criteria>
- META-04 (portability half): overrides ride the bundle inside ArticleSchema; conflicts are explicit with keep-local default + per-item take-incoming; merge-on-win protects reader names across content refreshes (D17-10/D17-11/D17-12 all implemented)
- D17-13 substrate: overrides are article-row fields — the existing atomic remove transaction cascades them by construction (e2e proof in 17-05)
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- ConflictKind member `article-metadata-override`; conflicts.ts predicate `metadataDiffers`; resolveImportPlan optional parameter `itemChoices` (`metadataTakeIncoming: ReadonlySet<string>`)
- ImportPreviewData additive field: the per-article `metadataConflicts` detail array (local/incoming override values per id)
- ImportPreviewDialog: DEFAULT_OVERRIDES/KIND_LABELS entries, per-item disclosure UI, state `metadataTakeIncoming`, extended `onProceed(overrides, applyPreferences, takeIncoming)` signature
- SettingsPanel threading of itemChoices into resolveImportPlan
- Bundle v3: `z.literal(3)` union member, writer emission `3 as const`, peek threshold `> 3`
- KIND_LABELS copy: `article with a different title or author` / `articles with a different title or author`; per-item labels `Keep mine` / `Use imported`
</artifacts_produced>

<output>
Create `.planning/phases/17-reader-owned-metadata/17-04-SUMMARY.md` when done
</output>
