---
phase: 17-reader-owned-metadata
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/routes/ArticleView.tsx
  - src/routes/review/ReviewView.tsx
  - src/routes/review/reviewFilter.ts
  - src/portability/markdown.ts
  - tests/unit/portability/markdown.test.ts
autonomous: true
requirements: [META-02]
must_haves:
  truths:
    - "Opening an overridden article shows the effective title in document.title, the article h1, and the effective author in the byline — while chapter-nav neighbor titles and the book half of the chapter document.title stay canonical (D17-09, D17-06)"
    - "The Highlights review shows the effective title in the article select options, the section headings, and the article sort order (one name, one order — D17-09)"
    - "Per-article markdown export uses the effective title in the download filename and in citation/heading content (D17-09; OQ5 pinned effective)"
  artifacts:
    - path: "src/routes/ArticleView.tsx"
      provides: "Reader-side effective-value consumption at the four title sites + byline + export filename"
      contains: "effectiveTitle"
    - path: "src/routes/review/ReviewView.tsx"
      provides: "Review select labels, options sort, section h2 on effective title"
      contains: "effectiveTitle"
    - path: "src/routes/review/reviewFilter.ts"
      provides: "Article section sort key on effective title"
      contains: "effectiveTitle"
    - path: "src/portability/markdown.ts"
      provides: "Export citations, headings, section sort on effective values"
      contains: "effectiveTitle"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/ingestion/library/effectiveMetadata.ts"
      via: "document.title standalone + chapter combo (article half only), byline, h1, export filename all call effectiveTitle/effectiveAuthor — chapter neighbor + book halves stay canonical"
      pattern: "effectiveTitle\\(article\\)"
    - from: "src/portability/markdown.ts"
      to: "src/ingestion/library/effectiveMetadata.ts"
      via: "citation lines and headings derive from the effective values — export presentation is a META-02 surface"
      pattern: "effectiveTitle"
---

<objective>
Swap every reader-, review-, and export-presentation consumer of provenance.title/author to the 17-01 derivation module (D17-09 — the effective value is used everywhere; one name follows the reader). Chapter and book surfaces stay canonical by decision (D17-05/D17-06); SectionAnnouncer, ResumeBanner, and Header are verified NON-consumers and are deliberately untouched (17-RESEARCH Pattern 2 — avoids a spurious task).

Purpose: META-02's downstream half. Library surfaces swap in 17-02 (parallel); cross-surface behavioral e2e lands in 17-05 after both waves merge.
Output: Display-source swaps in ArticleView (5 sites), ReviewView (3 sites), reviewFilter (1), markdown.ts (4), plus a unit extension proving export content uses effective values.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reader-owned-metadata/17-CONTEXT.md
@.planning/phases/17-reader-owned-metadata/17-RESEARCH.md
@.planning/phases/17-reader-owned-metadata/17-PATTERNS.md
@.planning/phases/17-reader-owned-metadata/17-01-SUMMARY.md
@src/ingestion/library/effectiveMetadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArticleView — document.title, byline, h1, export filename on effective values</name>
  <files>src/routes/ArticleView.tsx</files>
  <read_first>
  - src/routes/ArticleView.tsx (full read of the five target regions: standalone document.title ~L1289, chapter-combo document.title ~L1283-1285, per-article export filename ~L1827, byline ~L1877-1883, h1 ~L2050-2052; plus the chapter-nav neighbor titles ~L1233-1243 which must stay canonical)
  - src/ingestion/library/effectiveMetadata.ts (17-01 output)
  - .planning/phases/17-reader-owned-metadata/17-PATTERNS.md (ArticleView site table with verbatim current code)
  </read_first>
  <action>
  Import effectiveTitle/effectiveAuthor from ../ingestion/library/effectiveMetadata and swap the VALUE SOURCE at exactly five sites (D17-09; OQ5 pinned — the export filename uses the effective title):

  1. Standalone document.title (~L1289): setDocumentTitle call argument becomes effectiveTitle(article).
  2. Chapter-combo document.title (~L1283-1285): the ARTICLE half of the template becomes effectiveTitle(article); the book half stays chapterContext.book.title byte-identical (D14-07 chapter format contract; books not overridable — D17-05).
  3. Per-article export filename (~L1827): the sanitizeFilename argument becomes effectiveTitle(article) (with the id unchanged).
  4. Byline (~L1877-1883): the author expression becomes effectiveAuthor(article) inside the existing truthy guard; the publishedAt branch logic is untouched.
  5. Article h1 (~L2050-2052): content becomes effectiveTitle(article); the ref and tabIndex attributes stay byte-stable.

  Deliberately unchanged: chapter-nav neighbor titles (~L1233-1243 — neighbors may be chapters, never overridable this phase, D17-06) and every SectionAnnouncer/ResumeBanner/Header-related line (verified non-consumers). No markup/structure changes — display-source swaps only.
  </action>
  <acceptance_criteria>
  - rg -c 'effectiveTitle' src/routes/ArticleView.tsx finds at least 4 occurrences (document.title x2, filename, h1) and rg -c 'effectiveAuthor' finds the byline read
  - The chapter-combo line still references chapterContext.book.title for the book half (rg finds it adjacent to the effectiveTitle call)
  - The chapter-nav neighbor region still reads neighbor provenance.title (canonical) — rg -n 'neighbor' context confirms no effective call in that region
  - git diff on SectionAnnouncer/ResumeBanner/Header files is empty (they are not modified at all this plan)
  - npx playwright test tests/e2e/library/browse-open.spec.ts tests/e2e/library/v1-regression.spec.ts --project=chromium exits 0 (fixtures carry no overrides → rendered text byte-stable)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/library/browse-open.spec.ts tests/e2e/library/v1-regression.spec.ts --project=chromium</automated>
  </verify>
  <done>Reader surfaces derive title/author from the one module; chapter/book surfaces stay canonical; fixture-pinned regression specs green on chromium.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewView + reviewFilter + markdown export on effective values</name>
  <files>src/routes/review/ReviewView.tsx, src/routes/review/reviewFilter.ts, src/portability/markdown.ts, tests/unit/portability/markdown.test.ts</files>
  <read_first>
  - src/routes/review/ReviewView.tsx (the three sites: options sort ~L341-343, select option labels ~L415-419, section h2 ~L462-463)
  - src/routes/review/reviewFilter.ts (article section sort key ~L227-229)
  - src/portability/markdown.ts (citation construction ~L139-146, heading ~L178, ~L204, section sort ~L251-253)
  - tests/unit/portability/markdown.test.ts (full — builder patterns to extend strengthen-only)
  - src/ingestion/library/effectiveMetadata.ts (17-01 output)
  </read_first>
  <behavior>
  - markdown export: an article with readerTitle/readerAuthor set renders the override values in its citation line and heading; the canonical title text does NOT appear in the exported markdown (D17-09 — one name)
  - markdown export: an article without overrides renders canonical values exactly as before (regression)
  - markdown export: sections order by the effective title (OQ5/A5 pinned — sort keys use effective values)
  </behavior>
  <action>
  Import effectiveTitle/effectiveAuthor (path per file: ../ingestion/library/effectiveMetadata from routes files; ../ingestion/library/effectiveMetadata from portability) and swap the value source at: ReviewView options sort comparator (both operands), select option label text, section h2 text; reviewFilter article sort key (both operands of the localeCompare); markdown.ts article citation, per-article heading, and section sort comparator. Keep all markup shapes, ids, and guards byte-stable. Extend tests/unit/portability/markdown.test.ts strengthen-only with the three behavior cases (build articles via ArticleSchema.parse with override fields — the 17-01 builder discipline).
  </action>
  <acceptance_criteria>
  - rg -c 'effectiveTitle' src/routes/review/ReviewView.tsx finds 3 sites; src/routes/review/reviewFilter.ts and src/portability/markdown.ts each reference effectiveTitle (markdown also effectiveAuthor for citations)
  - tests/unit/portability/markdown.test.ts contains an override case asserting the canonical title string is absent from the exported markdown and the override string present
  - npx vitest run tests/unit/portability/markdown.test.ts tests/unit/review-filter.test.ts exits 0 with no prior case removed
  - git diff on src/routes/review/reviewFilter.ts shows only the sort-key operand change
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/markdown.test.ts tests/unit/review-filter.test.ts</automated>
  </verify>
  <done>Highlights review and markdown export show/sort/cite the one effective name; unit-proven; regression suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| override strings → document.title / markdown export | display-only text sinks fed by already-validated fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering (XSS) | override text in document.title/markdown | mitigate | document.title is text-only assignment (T-14-01 precedent); markdown export escapes title text through the existing citation/heading escape helpers — no new HTML sinks introduced |
| T-17-07 | Spoofing | effective title in export citations | accept | Metadata is display-only; identity remains id/revision/hashes (unchanged); conflict reporting in 17-04 surfaces every differing override |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- Unit: markdown + review-filter suites green with new override cases
- e2e regression: browse-open + v1-regression green on chromium (fixture text byte-stable)
- Cross-surface behavioral proof (reader h1/document.title/byline, review surfaces, export) lands in 17-05 Task 3 against this plan + 17-02 merged
</verification>

<success_criteria>
- META-02 downstream: Reader, Highlights, and export presentation surfaces all consume the single derivation — no forked title logic anywhere (rg for provenance.title display reads outside schema/persistence returns only the deliberate canonical surfaces: chapter neighbors, book halves, book rows)
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- Effective-value consumption at 8 new call sites across ArticleView/ReviewView/reviewFilter/markdown.ts (imports of effectiveTitle/effectiveAuthor)
- Extended override cases in tests/unit/portability/markdown.test.ts
- OQ5 pin recorded in code: export filename + all sort keys use effective values
</artifacts_produced>

<output>
Create `.planning/phases/17-reader-owned-metadata/17-03-SUMMARY.md` when done
</output>
