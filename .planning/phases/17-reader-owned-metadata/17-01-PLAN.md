---
phase: 17-reader-owned-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/schema.ts
  - src/persistence/db.ts
  - src/ingestion/library/effectiveMetadata.ts
  - tests/unit/library/effective-metadata.test.ts
  - tests/unit/ingestion-schema.test.ts
autonomous: true
requirements: [META-01, META-03]
must_haves:
  truths:
    - "A row carrying readerTitle round-trips ArticleSchema.parse with the field intact and its provenance bytes untouched (META-01 substrate; strip-mode trap guard)"
    - "An article row whose override field is an empty string FAILS ArticleSchema.safeParse — no schema state can represent a blank override (D17-04)"
    - "Existing rows without override keys parse unchanged with readerTitle and readerAuthor hydrating to undefined (Pitfall 9 additive hydration)"
    - "effectiveTitle returns the override when present and the canonical provenance.title otherwise; effectiveAuthor returns undefined when both the override and the canonical author are absent (META-03 absent-author restore)"
  artifacts:
    - path: "src/content/schema.ts"
      provides: "readerTitle/readerAuthor optional min(1) override fields on ArticleSchema"
      contains: "readerTitle"
    - path: "src/ingestion/library/effectiveMetadata.ts"
      provides: "The ONE effective-value derivation (effectiveTitle/effectiveAuthor)"
      exports: ["effectiveTitle", "effectiveAuthor"]
    - path: "tests/unit/library/effective-metadata.test.ts"
      provides: "Derivation truth table + strip-mode + empty-string guards"
      min_lines: 40
    - path: "src/persistence/db.ts"
      provides: "Additive articles table type widening + no-bump rationale comment (overrides are non-indexed row fields)"
      contains: "readerTitle"
  key_links:
    - from: "src/ingestion/library/effectiveMetadata.ts"
      to: "src/content/schema.ts (ArticleSchema readerTitle/readerAuthor)"
      via: "nullish coalescing over article.readerTitle ?? article.provenance.title (and the author twin)"
      pattern: "readerTitle \\?\\? article\\.provenance\\.title"
    - from: "src/persistence/db.ts (articles Table type)"
      to: "src/content/schema.ts"
      via: "optional string row columns readerTitle/readerAuthor stored without a version bump (non-indexed Dexie fields need no declaration; ingestionMeta precedent)"
      pattern: "readerTitle\\?: string"
---

<objective>
Ship the Phase 17 foundation: reader-owned override fields on ArticleSchema (D17-12 — overrides travel INSIDE the article record, never a separate table or bundle block) and the single pure effective-value derivation module every surface will consume (META-02's structural guarantee, D14-20 one-derivation-point precedent). Also pin the storage-model discretion: schema-declared optional fields, NOT the bookId strip-mode precedent (which would make overrides invisible on every read — Pitfall 1 in 17-RESEARCH) and NOT a separate overrides table (rejected in RESEARCH Alternatives).

Purpose: META-01 (additive fields leave provenance/id/revision untouched) + META-03 (absent override ⇔ canonical value, including absent author) + META-04 substrate (additive hydration is the migration mechanism; Option A of RESEARCH OQ3 — no Dexie version bump, mirroring the bumpless ingestionMeta landing).
Output: Two schema fields, one pure module, db.ts type widening + rationale comment, unit truth-table coverage.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reader-owned-metadata/17-CONTEXT.md
@.planning/phases/17-reader-owned-metadata/17-RESEARCH.md
@.planning/phases/17-reader-owned-metadata/17-PATTERNS.md
@src/content/schema.ts
@src/persistence/db.ts
@src/ingestion/library/readingState.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ArticleSchema override fields + db.ts type widening + effectiveMetadata module</name>
  <files>src/content/schema.ts, src/persistence/db.ts, src/ingestion/library/effectiveMetadata.ts</files>
  <read_first>
  - src/content/schema.ts (full — ArticleSchema L250-267 with the tags optional-field precedent L260-266 and its phase-tagged comment discipline; Provenance L177-191 which must stay byte-unchanged; the ingestionMeta comment L257-259)
  - src/persistence/db.ts (full — v1..v5 version blocks L102-196 which must stay byte-unchanged; the articles Table property type + definite-assignment commentary L69-97 including the bookId strip-mode note)
  - src/ingestion/library/readingState.ts (the D14-20 pure-policy-module shape: module banner citing the decision, type-only imports, function-per-derivation)
  - .planning/phases/17-reader-owned-metadata/17-RESEARCH.md (Pattern 1 — why schema declaration is mandatory; Pattern 2 — the derivation module; Pattern 4 — Option A no-bump rationale)
  </read_first>
  <behavior>
  - Test 1 (ships in Task 2 file but pins this task's contract): ArticleSchema.parse of a row object carrying readerTitle returns parsed data with readerTitle intact and provenance deep-equal to the input provenance (META-01)
  - Test 2: safeParse of a row with an empty-string readerTitle fails (D17-04 — blank override unrepresentable)
  - Test 3: effectiveTitle(articleWithOverride) === override; effectiveTitle(articleWithout) === provenance.title
  - Test 4: effectiveAuthor returns undefined when readerAuthor and provenance.author are both absent (META-03)
  </behavior>
  <action>
  Three scoped edits (per D17-12, D17-04, META-01, META-03):

  1. src/content/schema.ts — insert two fields after the tags field (L266), before ArticleSchema's closing brace: readerTitle and readerAuthor, each z.string() with a min of 1 chained to optional(). Precede them with a phase-tagged comment in the shipped tags/ingestionMeta discipline stating: Phase 17 reader-owned display overrides; additive-optional so existing rows parse unchanged (Pitfall 9 hydration, the ingestionMeta/tags mechanism); min(1) makes a blank override unrepresentable per D17-04 so no override can ever produce an untitled article and an empty author field means no-author-override, not an empty-string override. Do NOT touch Provenance (L177-191) or any existing field — canonical bytes are the identity/anchor contract (META-01). Because CanonicalArticle is inferred from ArticleSchema, both fields join the type automatically; no separate type edit.

  2. src/persistence/db.ts — widen the articles Table property type additively near the existing column-typing precedent (L69-90): add optional string properties readerTitle and readerAuthor to the stored-row type. Add one short comment at the widening stating why there is NO version bump: overrides are non-indexed row fields, and only indexed properties require a version-block declaration (Dexie design; the ingestionMeta precedent landed bumpless); v1..v5 blocks stay byte-unchanged and there is no upgrade callback (Pitfall 9; RESEARCH OQ3 Option A).

  3. src/ingestion/library/effectiveMetadata.ts (NEW) — clone the readingState.ts module discipline: a module banner citing META-02 + the D14-20 one-derivation-point decision, listing which consumers import it (library row/strip/search, reader header + document.title + byline + export filename, review surfaces, markdown export). Import ONLY the CanonicalArticle type from ../../content/types — zero I/O, zero React. Export effectiveTitle(article): string returning the nullish-coalesced article.readerTitle over article.provenance.title, and effectiveAuthor(article): string | undefined returning article.readerAuthor over article.provenance.author. JSDoc on effectiveAuthor states the META-03 absent-author semantics: with no override key, the canonical author returns — including the absent case, where every consumer's existing truthy guard renders nothing.
  </action>
  <acceptance_criteria>
  - src/content/schema.ts contains both field declarations matching readerTitle and readerAuthor as optional strings with min(1), placed after the tags field
  - rg -c 'title: z.string().min\(1\)' src/content/schema.ts is unchanged from HEAD (Provenance canonical title untouched — META-01); git diff on the Provenance block is empty
  - src/persistence/db.ts contains readerTitle in the articles table row type and a comment naming the ingestionMeta/no-bump rationale; git diff shows zero changes inside the version(1)..version(5) blocks and no upgrade( call added
  - src/ingestion/library/effectiveMetadata.ts exports effectiveTitle and effectiveAuthor; rg -c 'provenance' on the file shows only the two fallback reads inside the derivations
  - npx tsc passes (npm run typecheck or the repo's equivalent script) — the inferred CanonicalArticle carries both optional fields
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-schema.test.ts && npm run lint</automated>
  </verify>
  <done>Schema carries the two optional override fields with phase-tagged comments; db.ts row type widened with the no-bump rationale; the pure derivation module exists with the D14-20 banner; existing schema tests and lint stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivation truth-table + strip-mode + hydration unit coverage</name>
  <files>tests/unit/library/effective-metadata.test.ts, tests/unit/ingestion-schema.test.ts</files>
  <read_first>
  - tests/unit/library/reading-state.test.ts (full — the schema-parse builder pattern and decision-named describe/it shape to clone; NEVER hand-write rows)
  - tests/unit/ingestion-schema.test.ts (full — existing ArticleSchema cases to extend strengthen-only)
  - src/content/schema.ts (Task 1 result — the new fields)
  - src/ingestion/library/effectiveMetadata.ts (Task 1 result)
  </read_first>
  <behavior>
  - effective-metadata: override present → effectiveTitle/effectiveAuthor return the override values
  - effective-metadata: overrides absent → canonical provenance.title/author returned
  - effective-metadata: absent override + absent canonical author → effectiveAuthor is undefined (META-03)
  - effective-metadata: a row object WITH readerTitle round-trips ArticleSchema.parse with the field intact (strip-mode trap guard — Pitfall 1)
  - effective-metadata: safeParse REJECTS a row whose readerTitle is an empty string (D17-04 / Pitfall 2 guard)
  - effective-metadata: parsing a row with an override leaves provenance deep-equal to the input provenance (META-01 identity untouched)
  - ingestion-schema (extend): a v5-shaped row WITHOUT override keys parses with readerTitle/readerAuthor undefined (Pitfall 9 hydration); empty-string readerAuthor also rejected
  </behavior>
  <action>
  Create tests/unit/library/effective-metadata.test.ts cloning the reading-state.test.ts shape: import describe/expect/it from vitest, ArticleSchema from ../../../src/content/schema, and the two derivations from ../../../src/ingestion/library/effectiveMetadata; build every article via ArticleSchema.parse of a literal row object (reuse the minimal valid row shape from existing schema tests — never hand-write typed rows). Cover the seven behavior cases above in decision-named describes citing META-01/02/03, D17-04, and the Pitfall 1/Pitfall 2 guards. Extend tests/unit/ingestion-schema.test.ts strengthen-only with the hydration and empty-string-rejection cases (both fields). No production changes in this task.
  </action>
  <acceptance_criteria>
  - tests/unit/library/effective-metadata.test.ts exists and rg -c 'it\(' finds at least 7 cases; the file builds articles only via ArticleSchema.parse (no hand-written CanonicalArticle literals)
  - tests/unit/ingestion-schema.test.ts gains cases asserting undefined hydration for rows without the keys and rejection of blank override strings for BOTH fields; no existing case removed (strengthen-only)
  - npx vitest run tests/unit/library/effective-metadata.test.ts tests/unit/ingestion-schema.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/library/effective-metadata.test.ts tests/unit/ingestion-schema.test.ts</automated>
  </verify>
  <done>Truth table, strip-mode round-trip guard, empty-string rejection, provenance-untouched assertion, and v5-row hydration all green — META-01/META-03 substrate proven at the unit boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted rows → ArticleSchema.parse | every Dexie read validates through Zod strip mode; override strings cross here |
| (future, landed in 17-04) bundle JSON → ExportBundleSchema | hostile override strings arrive here; validated by the same ArticleSchema composition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | override strings on article rows | mitigate | z.string().min(1).optional() at the schema boundary (V5, ASVS L1); React text-child rendering escapes display; min(1) blocks the empty-string row-poison class (Pitfall 2) |
| T-17-02 | Tampering | prototype-pollution-style keys | accept | Zod 4 strip mode removes unknown keys (documented in ExportImportService.ts L183-187); overrides are plain declared string fields |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH §Package Legitimacy Audit — no installs) |
</threat_model>

<verification>
- npx vitest run tests/unit/library/effective-metadata.test.ts tests/unit/ingestion-schema.test.ts green
- Existing schema suite green (strengthen-only); lint green
- Provenance block and db.ts v1..v5 blocks byte-unchanged (git diff)
</verification>

<success_criteria>
- META-01 substrate: override fields are additive; provenance/id/revision structurally untouched and test-proven
- META-03 substrate: absent-override → canonical derivation including the absent-author case, test-proven
- The one derivation module exists for 17-02/03/04/05 to consume — no surface forks it later (Anti-Pattern: forking title derivation)
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- ArticleSchema fields `readerTitle` / `readerAuthor` (z.string().min(1).optional()) — and the same two keys on the inferred CanonicalArticle type
- Dexie articles stored-row type properties `readerTitle?: string` / `readerAuthor?: string` (db.ts, no version bump)
- Module `src/ingestion/library/effectiveMetadata.ts` exporting `effectiveTitle(article)` and `effectiveAuthor(article)`
- Test file `tests/unit/library/effective-metadata.test.ts` (new) + extended cases in `tests/unit/ingestion-schema.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/17-reader-owned-metadata/17-01-SUMMARY.md` when done
</output>
