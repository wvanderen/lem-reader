---
phase: 17-reader-owned-metadata
plan: 05
type: execute
wave: 3
depends_on: ["17-02", "17-03", "17-04"]
files_modified:
  - tests/e2e/ingestion/dexie-migration.spec.ts
  - tests/e2e/portability/round-trip.spec.ts
  - tests/e2e/portability/import-preview.spec.ts
  - tests/e2e/library/metadata-edit.spec.ts
autonomous: true
requirements: [META-02, META-04]
must_haves:
  truths:
    - "A pre-Phase-17 library (v5 rows without override keys) opens with every row intact and its on-disk rows byte-unchanged — overrides hydrate to absent, nothing is written back (META-04 migration)"
    - "An override edited on device A exports in a schemaVersion-3 bundle and imports on device B with the exact override bytes at the row level (META-04 round-trip)"
    - "Importing a same-id article with differing overrides surfaces the article-metadata-override conflict row; the keep-local default preserves the local name, and a per-item use-imported choice applies the incoming one (D17-11)"
    - "Importing a higher-revision refresh of an overridden article keeps the local title/author override on the merged row (D17-10 proven end-to-end)"
    - "Removing an overridden article removes its override with it — no residue, no orphan window (D17-13: the atomic row deletion IS the cascade)"
    - "After editing in the Library, the Reader document.title/h1/byline, the Highlights select + section headings, and the per-article markdown export filename + content all show the one effective name (META-02 cross-surface proof)"
  artifacts:
    - path: "tests/e2e/ingestion/dexie-migration.spec.ts"
      provides: "v5-row override-hydration migration describe (08-02 tags template transplanted)"
      contains: "readerTitle"
    - path: "tests/e2e/portability/round-trip.spec.ts"
      provides: "Override round-trip cells across two browser contexts with row-level byte equality"
      contains: "readerTitle"
    - path: "tests/e2e/portability/import-preview.spec.ts"
      provides: "Metadata conflict preview + keep-local default + per-item take-incoming + merge-on-win cells"
      contains: "article-metadata-override"
    - path: "tests/e2e/library/metadata-edit.spec.ts"
      provides: "Cross-surface consistency extension (reader/review/export) + removal-cascade assertion"
  key_links:
    - from: "tests/e2e/portability/round-trip.spec.ts"
      to: "src/portability/ExportImportService.ts"
      via: "two-context A/B surrogate (isolated IndexedDB per profile — the 09-06 pattern); asserts bundle.json carries schemaVersion 3 and row-level readerTitle equality"
      pattern: "schemaVersion"
    - from: "tests/e2e/library/metadata-edit.spec.ts (cross-surface cells)"
      to: "src/routes/ArticleView.tsx + src/routes/review/ReviewView.tsx + src/portability/markdown.ts"
      via: "drive the real UI after a dialog edit and assert the effective name on each surface (D17-09)"
      pattern: "effective|readerTitle"
---

<objective>
Prove META-02/META-04 end-to-end in real browsers and close the phase with the honest full-suite gate. This plan writes tests only — every production surface shipped in 17-01..04 — plus the honest `npm run test` exit-0 record (the 09-07/13-06/15-04 gate precedent).

Purpose: The phase's four ROADMAP success criteria are only TRUE when proven: migration safety, round-trip with explicit conflicts, refresh-never-renames, cascade-on-removal, and one-name-everywhere consistency.
Output: Four extended/new e2e spec sections + the phase gate record (17-05-SUMMARY carries honest counts).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reader-owned-metadata/17-CONTEXT.md
@.planning/phases/17-reader-owned-metadata/17-RESEARCH.md
@.planning/phases/17-reader-owned-metadata/17-PATTERNS.md
@.planning/phases/17-reader-owned-metadata/17-01-SUMMARY.md
@.planning/phases/17-reader-owned-metadata/17-02-SUMMARY.md
@.planning/phases/17-reader-owned-metadata/17-03-SUMMARY.md
@.planning/phases/17-reader-owned-metadata/17-04-SUMMARY.md
@tests/e2e/portability/_portability.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration proof — v5 rows hydrate overrides without a write-back</name>
  <files>tests/e2e/ingestion/dexie-migration.spec.ts</files>
  <read_first>
  - tests/e2e/ingestion/dexie-migration.spec.ts (full — especially the v3→v4 tags-hydration describe L424-571: the exact template to transplant; the dual-path cross-engine seeding + clear-rows-not-deleteDatabase beforeEach discipline)
  - src/persistence/db.ts (the no-bump rationale comment from 17-01)
  - src/content/schema.ts (17-01 result — the optional fields)
  </read_first>
  <behavior>
  - Seed a v5-shaped article row WITHOUT readerTitle/readerAuthor keys via the spec's established seeding path → open the app → the row renders in the library; the raw stored row still has NO readerTitle key (byte-unchanged, no write-back) while the parsed surface hydrates both to undefined
  - A row WITH override keys seeded directly opens and renders the effective values (forward shape)
  </behavior>
  <action>
  Add a new describe to dexie-migration.spec.ts cloning the tags-hydration template: seed a v5-shaped row (the existing builder) lacking both override keys, reload, assert render + raw-row key absence + parsed-value absence; add the forward-shape cell with both keys present. Cite META-04 + the 17-01 Option A decision (no version bump — non-indexed fields hydrate) in the describe name/comment. Strengthen-only: no existing migration cell changes.
  </action>
  <acceptance_criteria>
  - tests/e2e/ingestion/dexie-migration.spec.ts contains a Phase 17 hydration describe with the no-write-back assertion on the raw row and the undefined-hydration assertion; rg -c 'readerTitle' on the file finds at least 2 references
  - npx playwright test tests/e2e/ingestion/dexie-migration.spec.ts exits 0 on chromium, firefox, and webkit
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ingestion/dexie-migration.spec.ts</automated>
  </verify>
  <done>Pre-Phase-17 libraries migrate safely with zero write-back — META-04's migration clause proven across engines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip + import-conflict e2e (v3 bundle, keep-local default, take-incoming, merge-on-win, cascade)</name>
  <files>tests/e2e/portability/round-trip.spec.ts, tests/e2e/portability/import-preview.spec.ts</files>
  <read_first>
  - tests/e2e/portability/round-trip.spec.ts (full — the two-context A/B surrogate harness, export→transfer→import flow, row-level raw IndexedDB assertion pattern)
  - tests/e2e/portability/import-preview.spec.ts (full — the preview-dialog driving patterns to extend)
  - tests/e2e/portability/_portability.ts (shared helpers)
  - src/portability/conflicts.ts (17-04 result — the kind, metadataConflicts detail, itemChoices)
  - src/reader/ImportPreviewDialog.tsx (17-04 result — the per-item UI labels)
  </read_first>
  <behavior>
  - Round-trip: context A edits an override via the dialog → export → the bundle's bundle.json carries schemaVersion 3 and the article's readerTitle/readerAuthor → transfer → context B imports → B's raw row equals A's override values byte-for-byte and B's library row shows the effective name
  - Round-trip: an article WITHOUT overrides round-trips unchanged (regression cell)
  - Import preview: B has a DIFFERENT override on the same-id article → importing A's bundle shows the article-metadata-override conflict row (label: article with a different title or author) with the per-article list showing both names
  - Import preview: one-side-only (B has none, A has one) → conflict row still appears (D17-11 verbatim)
  - Keep-local default: Proceed without touching choices → B's name unchanged; the imported bundle's name did NOT silently replace it
  - Take-incoming: toggle that article to Use imported → Proceed → B now shows A's name (and when A had none, B's override is removed)
  - Merge-on-win (D17-10): import a bundle whose same-id article carries revision+1 (seed via direct Dexie put of a bumped row in A before export, or the spec's established revision-bump path) over B's overridden local → Proceed with the revision conflict set to overwrite + keep-mine metadata → B's row shows the NEW revision with the LOCAL title override intact
  - Cascade (D17-13): remove an overridden article via the library Remove flow → the raw articles row is gone (override deleted with it — no residue)
  </behavior>
  <action>
  Extend both specs strengthen-only with the cells above, reusing the A/B two-context harness and raw-row assertion helpers. Drive conflicts through the real ImportPreviewDialog UI (expand the metadata row, read Keep mine / Use imported labels). For merge-on-win, follow the spec's existing revision-conflict overwrite driving pattern. Cite D17-10/D17-11/D17-12/D17-13 in cell names.

  Version-bump assertion update (explicitly NOT strengthen-only): round-trip.spec.ts L169 — the existing expect(bundleJson.schemaVersion).toBe(2) flips to toBe(3) because 17-04's writer now emits v3, and the L167-168 comment re-words writers emit v2 since Phase 12 (12-07) to cite Phase 17 v3 (the 12-07 version-bump assertion-update precedent). This flip is the precondition for the new schemaVersion 3 cell above; all other existing round-trip assertions are unchanged.
  </action>
  <acceptance_criteria>
  - tests/e2e/portability/round-trip.spec.ts contains schemaVersion 3 + row-level override-equality assertions (rg finds readerTitle and schemaVersion references); the former v2-emit assertion now expects 3
  - tests/e2e/portability/import-preview.spec.ts contains the conflict-row label, per-item toggle labels, keep-local default, take-incoming, merge-on-win, and cascade cells (rg finds article-metadata-override, Keep mine, Use imported)
  - npx playwright test tests/e2e/portability/round-trip.spec.ts tests/e2e/portability/import-preview.spec.ts exits 0 on chromium, firefox, and webkit
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/portability/round-trip.spec.ts tests/e2e/portability/import-preview.spec.ts</automated>
  </verify>
  <done>META-04 is proven end-to-end: migrate, round-trip, explicit conflicts with both resolutions, refresh-never-renames, cascade-on-removal — across all three engines.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-surface consistency cells + honest full-suite phase gate</name>
  <files>tests/e2e/library/metadata-edit.spec.ts</files>
  <read_first>
  - tests/e2e/library/metadata-edit.spec.ts (17-02 result — the harness + seeding to extend)
  - src/routes/ArticleView.tsx / src/routes/review/ReviewView.tsx / src/portability/markdown.ts (17-03 result — the surfaces being asserted)
  - tests/e2e/portability/highlights-export.spec.ts (the per-article markdown export driving pattern)
  - .planning/phases/16-organized-library-and-focused-add-flow/16-04-SUMMARY.md (the honest-gate record shape)
  </read_first>
  <action>
  Two parts:

  1. CROSS-SURFACE CELLS — extend metadata-edit.spec.ts with one describe that edits an ingested article's title + author via the dialog, then asserts the effective name on every remaining META-02 surface through the real UI: navigate to the reader (document.title contains the effective title; the article h1 text is the effective title; the byline shows the effective author); create/locate a highlight on that article and open the Highlights view (the article select option + the section h2 show the effective title); run the per-article highlights export (the downloaded filename and the markdown citation/heading contain the effective title — canonical title absent); with a saved reading location, the Continue Reading strip shows the effective title. Strengthen-only — the 17-02 cells stay byte-unchanged.

  2. HONEST PHASE GATE — run the FULL suite in one invocation: npm run test. The gate is exit 0 with honest counts recorded in 17-05-SUMMARY (passed/failed/skipped exactly as printed; the 15-04 lesson applies — if webkit cells starve on a reused dev server, restart a FRESH server before touching any spec; identical-cell failure across engines = regression, engine-only + isolation-green = harness). Any failure is fixed forward with atomic commits and the suite re-run to green before the SUMMARY is written. No subset runs, no engine skips, no grep-filtered counts.
  </action>
  <acceptance_criteria>
  - tests/e2e/library/metadata-edit.spec.ts contains the cross-surface describe asserting document.title, article h1, byline, review select option + section h2, export filename + markdown content, and the strip (rg finds references to the highlights route and the export flow)
  - The canonical title string is asserted ABSENT from the exported markdown content cell (one name — D17-08/D17-09)
  - npm run test exits 0 in one invocation; 17-05-SUMMARY records the exact passed/failed/skipped counts and the invocation evidence
  </acceptance_criteria>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>META-02's consistency inventory is browser-proven on every enumerated surface, and the phase closes behind an honest full-suite green gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — test-only plan) | all flows exercise surfaces shipped in 17-01..04; no new input paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Repudiation | gate honesty (hidden failures misreported) | mitigate | One-invocation full-suite gate with exact printed counts recorded in the SUMMARY (the 04-11/09-07 anti-misreport precedent); no subsets/greps/engine skips |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- All four touched specs green on chromium, firefox, webkit
- npm run test exit 0 in one invocation — the permanent phase gate record
</verification>

<success_criteria>
- ROADMAP SC1–SC4 each backed by at least one green browser proof: edit-without-identity-change (17-02 + row assertions), one-name-everywhere (Task 3), clear-to-canonical incl. absent author (17-02), migrate/round-trip/conflicts/cascade (Tasks 1–2)
- Honest gate: phase closes only behind a real exit-0 full-suite run
</success_criteria>

<artifacts_produced>
## Artifacts this phase plan produces

- Phase 17 hydration describe in tests/e2e/ingestion/dexie-migration.spec.ts
- Override round-trip + conflict/merge/cascade cells in tests/e2e/portability/round-trip.spec.ts + import-preview.spec.ts
- Cross-surface consistency describe in tests/e2e/library/metadata-edit.spec.ts
- The honest full-suite gate record in 17-05-SUMMARY.md (exact counts + evidence)
</artifacts_produced>

<output>
Create `.planning/phases/17-reader-owned-metadata/17-05-SUMMARY.md` when done
</output>
