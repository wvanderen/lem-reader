---
phase: 01-canonical-article-foundation
plan: 04
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - src/app.css
  - src/routes/FixtureList.tsx
  - src/routes/ArticleView.tsx
  - tests/component/FixtureList.test.tsx
  - tests/component/ArticleView.test.tsx
autonomous: true
gap_closure: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Fixture-list heading and list content have a comfortable horizontal inset (md) from the viewport edge on small screens (UI-SPEC §Layout: padding-inline md at <640px)"
    - "Invalid-article error state renders the full two-line UI-SPEC Copywriting contract — the 'Couldn't open this article.' heading plus the guidance body sentence — in both FixtureList and ArticleView"
    - "The status region is visually styled as a calm chrome card (surface-raised background, hairline border, spacing-scale padding) in both views"
    - "The existing article-ready view layout is visually unchanged (no double inset; calm 64ch measure preserved)"
  artifacts:
    - path: "src/app.css"
      provides: "main#main shared view inset + .status card styling"
      contains: "main#main"
    - path: "src/routes/ArticleView.tsx"
      provides: "Two-line error contract + .status className on the status region"
      contains: "status"
    - path: "src/routes/FixtureList.tsx"
      provides: "Two-line error contract + .status className on the status region"
      contains: "status"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/app.css"
      via: "status div className=status targets the .status rule"
      pattern: 'className="status"'
    - from: "src/routes/FixtureList.tsx"
      to: "src/app.css"
      via: "status div className=status targets the .status rule"
      pattern: 'className="status"'
---

<objective>
Close UAT gaps 1 (test 2, cosmetic) and 2 (test 9, minor) for Phase 01. Gap 1: the fixture-list view has no horizontal inset, so the "Saved articles" heading renders flush against the viewport edge. Gap 2: the invalid-article error state renders only the bare heading string — the UI-SPEC Copywriting Contract's guidance body sentence is missing, the status region has no CSS class, and app.css has no `.status` rule.

Purpose: Both gaps share one root asymmetry — `ArticleView` insets its content via `.article-body` (`padding-inline: var(--space-md)`), but `FixtureList` and the error/loading views inset nothing. The fix unifies the reading-surface inset at the `main#main` level for every view, adds the missing `.status` card styling, and renders the full two-line error contract verbatim from UI-SPEC §Copywriting.

Output: One CSS change (unified inset + `.status` card) and two component markup changes (two-line error copy + `className="status"`), plus updated component tests asserting the guidance body sentence.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-article-foundation/01-UAT.md
@.planning/phases/01-canonical-article-foundation/01-UI-SPEC.md
@.planning/debug/fixture-list-header-spacing.md
@.planning/debug/error-state-guidance-copy.md
@src/app.css
@src/routes/FixtureList.tsx
@src/routes/ArticleView.tsx
@tests/component/FixtureList.test.tsx
@tests/component/ArticleView.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify view inset on main#main + add .status card styling</name>
  <files>src/app.css</files>
  <action>
Close the fixture-list inset asymmetry (Gap 1, UAT test 2) and add the missing status-region styling (Gap 2 styling half). All changes in src/app.css.

1. Add a NEW rule for the shared reading-surface inset. Selector `main#main`. Declarations: `padding-inline: var(--space-md)` and `padding-block: var(--space-3xl)`. This gives EVERY view (fixture list, article error/loading, article body) the same calm inset the UI-SPEC §Layout specifies for narrow viewports (padding-inline: md). Place it near the existing layout section (before the `.article-body` rule is fine).

2. Remove the now-redundant `padding-inline: var(--space-md)` AND `padding-block: var(--space-3xl)` declarations from the existing `.article-body` rule. KEEP its `max-width: 64ch` and `margin-inline: auto` declarations (the calm measure is invariant). Update the section comment to note the inset moved up to `main#main`.

   Box-model equivalence (so the executor does not second-guess this): previously `.article-body` supplied 16px inline + 64px block INSIDE the article wrapper while `main#main` had no padding. After the change, `main#main` supplies the same 16px inline + 64px block OUTSIDE the article wrapper, and `.article-body` is centered at max-width 64ch inside the padded main. At narrow widths the article fills the padded main box, so body content remains 16px from the viewport edge — visually identical to before. At wide widths the 64ch column centers unchanged. No double inset occurs because `.article-body` no longer carries inline/block padding. The `.article-body p`, `.article-body h2`, `.article-body blockquote`, figure, figcaption, pre, code DESCENDANT rules are untouched (they set element margins, not container padding).

3. Add a NEW `.status` rule for the loading/error status card, honoring UI-SPEC §Layout "Visual hierarchy" (chrome recedes on the 30% secondary surface token). Declarations: `margin-block: var(--space-lg)`, `padding: var(--space-lg)`, `background: var(--surface-raised)`, `border: 1px solid var(--hairline)`, `border-radius: 4px`. Then add two descendant rules: `.status p` with `margin-block: var(--space-sm)` and `color: var(--ink-soft)` (guidance body recedes behind the heading); and a combined `.status h1, .status h2` rule with `margin-block: 0 var(--space-sm)` (tighten the heading-to-body gap inside the card so the two lines read as one unit). Do NOT use `--destructive` for text or background — UI-SPEC reserves `--destructive` for icon/border only and Phase 1 has no destructive actions; the card stays calm (surface-raised + hairline).

Reference the gap root causes in 01-UAT.md (Gaps for test 2 and test 9) and the debug sessions fixture-list-header-spacing.md + error-state-guidance-copy.md.
  </action>
  <verify>
    <automated>npm run build && npm run lint && rg -q 'main#main\s*\{' src/app.css && rg -q '^\.status\s*\{' src/app.css && rg -q '^\.status\s+(p|h1)' src/app.css && echo OK</automated>
  </verify>
  <done>
`main#main` supplies inline (md) + block (3xl) inset for every view; `.status` card rule exists with surface-raised bg, hairline border, and spacing-scale padding; `.article-body` retains only max-width + margin-inline (no double inset); build and lint are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render two-line error contract + .status className in both views</name>
  <files>src/routes/ArticleView.tsx, src/routes/FixtureList.tsx, tests/component/ArticleView.test.tsx, tests/component/FixtureList.test.tsx</files>
  <action>
Close Gap 2 (UAT test 9) markup half: render the full two-line UI-SPEC §Copywriting "Error state — open failed" contract and attach `className="status"` so Task 1's styling applies. UI-SPEC §Copywriting is the verbatim source of truth for the copy.

In src/routes/ArticleView.tsx — the early-return error/loading branch (the `if (status !== "ready" || !article)` block):
- Add `className="status"` to the status `<div>` (it already carries role="status", aria-live="polite", aria-atomic="true" — keep those attributes).
- Replace the single bare-text conditional with structured output. When status is "loading", render a `<p>` containing the loading copy (the existing "Opening article…" sentence). When status is "error", render a heading element `<h1>` containing the heading copy (the existing "Couldn't open this article." sentence) FOLLOWED BY a `<p>` containing the guidance body sentence VERBATIM from UI-SPEC §Copywriting "Error state — open failed" body: "The article could not be loaded. Select it again from the list, or try a different article."
- The error view is a standalone page with no other heading, so the error copy is the page's `<h1>` — this preserves the one-h1-per-page rule (consistent with the ready view's `<h1>` title) and also fixes a latent missing-primary-heading a11y gap on the error page.

In src/routes/FixtureList.tsx — the status region `<div>` (currently a direct child of `<main>`):
- Add `className="status"` to it.
- Replace the bare-string conditionals with structured output. When status is "loading", render a `<p>` with the loading copy. When status is "error", render an `<h2>` with the heading copy followed by a `<p>` with the SAME guidance body sentence as ArticleView. Use `<h2>` here, NOT `<h1>`: the fixture-list page already has its `<h1>Saved articles</h1>`, and a second `<h1>` would violate the one-h1-per-page rule, so the error copy takes the next heading level (clean h1 → h2 hierarchy).

The copy is identical between the two components (UI-SPEC §Copywriting — single source of truth). Loading copy wording is unchanged (only wrapped in a `<p>`). Never introduce internal jargon in user-facing copy (UI-SPEC microcopy rules — no "fixture", "Zod", "schema", "IndexedDB").

Then update the component tests:
- tests/component/ArticleView.test.tsx: in BOTH error tests ("rejects" and "resolves null"), the existing `getByText("Couldn't open this article.")` assertion still passes (the text now lives in an `<h1>`). ADD an assertion that the guidance body sentence is present — `expect(screen.getByText(/The article could not be loaded/)).not.toBeNull()`. ADD an assertion that the status region is found by role: `expect(screen.getByRole("status")).not.toBeNull()`.
- tests/component/FixtureList.test.tsx: in the error test ("shows the error copy when listArticles rejects"), the existing heading-copy assertion still passes (now in an `<h2>`). ADD the same guidance-body assertion (`/The article could not be loaded/`) and the `getByRole("status")` assertion.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:unit -- --run tests/component/ArticleView.test.tsx tests/component/FixtureList.test.tsx</automated>
  </verify>
  <done>
Both components render the full two-line error contract (heading + guidance body); the status `<div>` carries `className="status"` and role="status"; component tests assert the guidance body sentence and the status role in both files; the targeted unit tests are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-only static SPA, in-memory repository, no network/auth/DB-write surface introduced by this plan. Existing boundaries unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-T1 | Tampering | status region (FixtureList/ArticleView) | accept | Error copy flows into the DOM as static string literals via JSX; React escapes text children. No user input reaches the status region. No injection surface. |
| T-01-04-I1 | Information Disclosure | error copy strings | accept | Copy is generic reader-facing guidance ("could not be loaded"); honors UI-SPEC microcopy rule — never leaks internal ids, jargon ("fixture/Zod/schema"), or source offsets. |
| T-01-04-C1 | CORS / supply chain | (none) | accept | No packages installed; no network calls added. CSS tokens and copy are local literals. |
</threat_model>

<verification>
- `npm run build` — TypeScript + Vite build green.
- `npm run lint` — ESLint green (no new violations; react/no-danger still enforced).
- `npm run test:unit -- --run` — all 99 prior unit/component tests + the updated FixtureList/ArticleView assertions pass.
- `npm run test:e2e` — the 42 prior e2e tests (open-every-fixture + a11y) remain green across Chromium/Firefox/WebKit, proving the `main#main` inset move did not regress the article view layout or a11y.
- Manual spot-check (optional): run `npm run dev`, open the fixture list — confirm the "Saved articles" heading and cards have a comfortable inset at a narrow (<640px) width; navigate to `#/article/does-not-exist` — confirm the two-line error copy in a styled card.
</verification>

<success_criteria>
- The fixture-list heading and content are inset from the viewport edge (Gap 1 closed).
- The invalid-article error state shows BOTH the heading and the guidance body sentence, styled as a calm card (Gap 2 closed).
- The article-ready view is visually unchanged (calm measure, no double inset).
- No regressions: the 99 prior unit tests and 42 prior e2e tests remain green; updated component tests assert the new behavior.
</success_criteria>

<output>
Create `.planning/phases/01-canonical-article-foundation/01-04-SUMMARY.md` when done.
</output>
