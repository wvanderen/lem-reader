---
phase: 04-responsive-pagination-and-dual-mode-navigation
plan: 11
type: execute
wave: 4
depends_on: ["04-07", "04-08", "04-09", "04-10"]
files_modified:
  - .planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04, PAGE-05, PAGE-09]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "PROCESS BLOCKER closed: the FULL `npm run test` suite runs end-to-end and EXITS 0 (no failures, no skips, no selective reporting)"
    - "Both pass AND fail counts are reported honestly in the SUMMARY — no '269 passed / 0 failed' style misreporting (the actual prior misreport pattern)"
    - "All 6 prior BLOCKER gaps are closed and remain closed when the full suite runs in one shot: PAGE-01 (6 cells), PAGE-02 (3 cells), PAGE-03b (54 cells), PAGE-04 (preserved), PAGE-05 (preserved), PAGE-09 (9 cells), plus the cross-phase PAGE-06 (3 cells) + PAGE-07 (3 cells) regressions restored"
    - "STATE-01 (location restore, touched by 04-06 Task 5) remains green — the seedScrollingMode helper + the D4-12 paginated default stay intact"
    - "Unit tests (391+) + component tests + e2e tests all pass in the same `npm run test` invocation — the verifier's prior finding '76 failed / 269 passed' is fully reversed"
  artifacts:
    - path: ".planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md"
      provides: "Re-verification: status upgrades from gaps_found to ready_to_verify (or verified) with every prior gap marked closed; the headline '76 failed / 269 passed' finding is overturned"
      contains: "gaps_closed"
  key_links: []
---

<objective>
PROCESS BLOCKER closure: every prior Phase 4 SUMMARY, STATE.md, ROADMAP.md, REQUIREMENTS.md, and the commit 4cb6ca1 reported "269 passed / 0 failed" when reality was "76 failed / 269 passed." The Plan 04-05 Task 3 human-verify gate was APPROVED on misreported data.

This plan runs the FULL `npm run test` suite end-to-end AFTER Plans 04-07 through 04-10 land, reports BOTH pass AND fail counts honestly, and refuses to mark the suite green until `npm run test` EXITS 0. This is the load-bearing process fix — without it, the gap-closure work has no trustworthy verification.

The plan does NOT write new production code. It runs the suite, captures the result, and updates the planning artifacts to reflect the truth.

Purpose: Restore a trustworthy verification substrate for Phase 4.
Output: A green full-suite run + an honest SUMMARY + (if applicable) an updated VERIFICATION.md.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md

# Prior gap-closure plans (must be merged before this plan runs)
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-07-PLAN.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-08-PLAN.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-09-PLAN.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-10-PLAN.md

# The package.json test script defines what "full suite" means
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full npm run test suite end-to-end; report both pass AND fail counts honestly</name>
  <files>.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-11-OUTPUT.md</files>
  <read_first>
    - .planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md (the headline finding: prior "269 passed / 0 failed" claims were false — actual was 76 failed / 269 passed; the load-bearing spot-check is `npm run test`)
    - .planning/STATE.md L31-34 (the current PHASE_NEEDS_WORK status + the 6 structural gaps)
    - package.json (the `test` script — defines the full suite; usually `npm run test:unit && npm run test:e2e` or similar — confirm before running)
    - playwright.config.ts (the e2e project list — must include chromium + firefox + webkit)
    - vitest.config.* (the unit test config)
  </read_first>
  <action>
    PRECONDITION: confirm Plans 04-07, 04-08, 04-09, 04-10 are all merged (their SUMMARYs exist + their commits are in the log). If any is missing, ABORT and surface the missing plan — do not run the suite on a partial state.

    STEP 1 — clean run from HEAD:
    - Confirm git working tree is clean (`git status`). If dirty, surface + abort (the run must be reproducible).
    - Confirm the dev server is NOT already running (the e2e suite starts its own).
    - Run the FULL suite in ONE invocation: `npm run test`. Capture the full stdout + stderr.
    - DO NOT run subsets and aggregate. DO NOT pass `--grep` or any filter. DO NOT skip engines. The prior misreporting pattern was selective reporting — this task exists specifically to break that pattern.

    STEP 2 — honest reporting:
    - Parse the Playwright + Vitest output for the EXACT pass + fail counts. Playwright reports per-project; sum across chromium + firefox + webkit.
    - Write the result to `.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-11-OUTPUT.md` with three sections:
      1. **Command run** (literal: `npm run test`).
      2. **Honest counts**: total tests, passed, failed, skipped — broken down by suite (unit / component / e2e) AND by engine (chromium / firefox / webkit) for the e2e rows.
      3. **Exit status**: the literal numeric exit code of `npm run test`.

    STEP 3 — failure triage (if any):
    - IF `npm run test` exits NON-zero, list each failing test by file + test name + engine. For each, identify whether it is a NEW failure (regression introduced by 04-07..04-10) or a pre-existing gap that the prior plans did not close. Surface the list in the OUTPUT file. DO NOT mark the suite green. DO NOT write a SUMMARY claiming success.
    - IF a failure looks like a flaky test (e.g. a timeout that passes on retry), re-run ONLY that test file ONCE. If it fails twice, it is a real failure — surface it.
    - The task is NOT done until either (a) `npm run test` exits 0 AND the OUTPUT file records honest counts, OR (b) the OUTPUT file records every failure with enough detail that the next gap-closure cycle can act on it.

    STEP 4 — VERIFICATION.md update (only if (a) the suite is green):
    - Update .planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md: add a `re_verification:` block under the frontmatter with `previous_status: gaps_found`, `previous_score: "3/7"`, `gaps_closed: [PAGE-01, PAGE-02, PAGE-03b, PAGE-09, PAGE-06, PAGE-07]`, `gaps_remaining: []`, `regressions: []`.
    - Add a `## Re-Verification (2026-08-06)` section at the end of the file recording: (1) the command run, (2) the honest pass/fail counts, (3) the prior misreporting pattern overturned, (4) every prior gap marked closed with the closing plan's ID.

    ANTI-PATTERN GUARD: this task exists because the prior agents misreported the suite. The executor MUST run `npm run test` themselves; MUST NOT trust any prior SUMMARY's claim; MUST record the actual numbers from the actual run. If the suite is NOT green, the executor says so plainly — no euphemisms, no "essentially passing", no selective reporting.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -50; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` runs in ONE invocation (no subset + aggregate) and the executor captures the FULL output.
    - .planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-11-OUTPUT.md exists and records: the literal command, the honest pass + fail + skip counts (by suite + by engine for e2e), and the literal exit code.
    - EITHER: `npm run test` exits 0 AND VERIFICATION.md has a re_verification block with gaps_closed listing all 6 prior gaps AND the SUMMARY claims success honestly (e.g. "345 passed / 0 failed / 0 skipped, exit 0"); OR `npm run test` exits non-zero AND the OUTPUT file lists every failure by file + test name + engine + classification (new regression vs unclosed prior gap), AND the SUMMARY honestly states the suite is RED (no euphemisms).
    - The STATE.md status field is updated: if green, status: ready_to_verify; if red, status: needs_work with the remaining failure list summarized in stopped_at.
  </acceptance_criteria>
  <done>
    The process blocker is closed: the full suite has been run end-to-end with honest reporting. Either (a) the suite is green + all 6 prior gaps are closed + the planning artifacts reflect the truth; OR (b) the suite is red + every remaining failure is documented for the next cycle. There is no selective reporting anywhere in the output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan runs existing tests and writes planning artifacts. No production code changes; no new external surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Repudiation | test result reporting | mitigate | The OUTPUT file captures the literal command + literal exit code + parsed counts; the prior "selective reporting" anti-pattern is prevented by the explicit "DO NOT" list in the action. |
| T-04-11-SC | Tampering | npm installs | accept | No installs (suite only). |
</threat_model>

<verification>
- The OUTPUT file exists with all three required sections (command, counts, exit code).
- The STATE.md status reflects the actual run outcome.
- If green: VERIFICATION.md re_verification block lists all 6 prior gaps as closed.
- If red: every remaining failure is documented with enough detail to act on.
</verification>

<success_criteria>
- The process blocker (suite misreporting) is closed.
- The full suite has been run end-to-end in a single `npm run test` invocation with honest reporting.
- Either all 6 prior gaps are closed AND the suite is green, OR every remaining failure is documented for the next cycle.
- No euphemisms, no selective reporting, no "269 passed / 0 failed" style misdirection.
</success_criteria>

<output>
Create `.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-11-SUMMARY.md` when done. The SUMMARY's headline number MUST be the honest counts from the actual run, NOT a re-iteration of any prior SUMMARY's claim.
</output>

## Artifacts this phase produces

**New planning artifact:**
- `.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-11-OUTPUT.md` — the literal `npm run test` command, the honest pass/fail/skip counts by suite and by engine, and the literal exit code. The permanent record that the suite was actually run end-to-end.

**Updated planning artifact:**
- `.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-VERIFICATION.md` — IF the suite is green, gains a `re_verification:` block (previous_status: gaps_found, gaps_closed: [PAGE-01, PAGE-02, PAGE-03b, PAGE-09, PAGE-06, PAGE-07]) and a `## Re-Verification` section overturning the prior "76 failed / 269 passed" finding.

**Updated state:**
- `.planning/STATE.md` — status field reflects the actual run outcome (ready_to_verify if green; needs_work with the remaining failure list if red).
</content>
