---
phase: 04-responsive-pagination-and-dual-mode-navigation
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/pagination/fragmentRenderer.tsx
  - src/reader/PaginatedSurface.tsx
  - src/reader/PageIndicator.tsx
  - src/reader/ProgressHairline.tsx
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/component/PaginatedSurface.test.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-05]
user_setup: []

must_haves:
  truths:
    - "A reader opening an article with readingMode 'paginated' sees a single page fragment rendered as semantic blocks (the same BlockView scrolling mode renders) — not the full scrolling article (PAGE-03 rendering surface)"
    - "A reader can move forward and backward through pages using quiet page-side chevron buttons; the rendered content swaps to the next/previous page fragment instantly (PAGE-02 pointer path)"
    - "The page content-box geometry is viewport-bounding (height calc(100vh - 48px - 2px - 2*var(--space-2xl))) and the page never scrolls (overflow hidden); content is paginated, not scrolled (PAGE-03 no-clipping at the layout level)"
    - "The progress hairline fill derives from N/M in paginated mode and stays scroll-ratio in scrolling mode; the 'N of M' indicator appears only in paginated mode (D4-08)"
    - "ArticleView reads useMeasurement's trustedView return value (previously ignored) and derives pages via paginateDocument; the staleness contract (PAGE-06/PAGE-07) is inherited, not re-implemented (PAGE-05 substrate)"
    - "Only the current page's DOM is in the document (one fragment mounted at a time — Pattern 5 single content tree, A11Y-03)"
  artifacts:
    - path: "src/pagination/fragmentRenderer.tsx"
      provides: "PageFragmentView component rendering a fragment's blocks via BlockView (reuses, does not fork BlockRenderer)"
      contains: "PageFragmentView"
    - path: "src/reader/PaginatedSurface.tsx"
      provides: "Container that derives pages from trustedView, holds currentPageIdx state, mounts the current PageFragmentView, renders chevrons"
      contains: "PaginatedSurface"
    - path: "src/reader/PageIndicator.tsx"
      provides: "Decorative aria-hidden 'N of M' span driven by current/total props"
      contains: "PageIndicator"
    - path: "src/reader/ProgressHairline.tsx"
      provides: "Extended to accept a page prop; fill derives from N/M when page present, scroll-ratio otherwise"
      contains: "page"
    - path: "src/routes/ArticleView.tsx"
      provides: "Mode-aware render branch reading settings.readingMode + useMeasurement return"
      contains: "PaginatedSurface"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/measurement/useMeasurement.ts"
      via: "reads the trustedView return value (previously ignored) and passes it to PaginatedSurface"
      pattern: "useMeasurement"
    - from: "src/reader/PaginatedSurface.tsx"
      to: "src/pagination/fragment.ts"
      via: "calls paginateDocument(article, trustedView, articleEl, ...) to derive pages"
      pattern: "paginateDocument"
    - from: "src/pagination/fragmentRenderer.tsx"
      to: "src/content/render/BlockRenderer.tsx"
      via: "imports BlockView and renders fragment block slices via it (no forked renderer)"
      pattern: "BlockView"
---

## Phase Goal

**As a** reader, **I want to** move predictably through complete responsive pages and switch or fall back to scrolling without losing my logical passage, **so that** I can read long-form content in a calm booklike way while retaining stable orientation.

This plan delivers the **first vertical slice a reader can experience**: open an article in paginated mode, see a real page fragment of the same semantic blocks, and click chevrons to turn pages end-to-end. It consumes Plan 01's engine + Plan 02's readingMode field. Mode-toggle, keyboard bundle, swipe, anchors, and fallback land in Plans 04–05 — this plan proves the pagination pipeline renders and turns.

<objective>
Wire ArticleView to read Phase 3's `useMeasurement()` trustedView return (currently ignored), derive page fragments via `paginateDocument()`, and render ONE fragment at a time through a new `PageFragmentView` that reuses `BlockView` (no forked renderer — DOC-02 reading order + D-05 integrity + react/no-danger security depend on reuse). Add quiet page-side chevron buttons (D4-06) for pointer page-turn, the "N of M" indicator (D4-08), and extend ProgressHairline so its fill derives from N/M in paginated mode. Ship the page content-box geometry in authored CSS (no Tailwind).

Purpose: Prove the pagination pipeline works end-to-end (measurement → fragmentation → semantic rendering → chevron turn) before layering full keyboard/swipe/mode-toggle/fallback in later plans. This is the thinnest slice that lets a reader actually turn pages.

Output: One new fragment renderer + one new paginated surface container + one new indicator + one extended hairline + the ArticleView mode-aware branch + page geometry CSS + a component test.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-CONTEXT.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-RESEARCH.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-PATTERNS.md
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-UI-SPEC.md

# Plan 01 SUMMARY (the engine this plan consumes) — read after Plan 01 completes
@.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-01-SUMMARY.md

# Source contracts to extend/reuse
@src/routes/ArticleView.tsx
@src/reader/ProgressHairline.tsx
@src/content/render/BlockRenderer.tsx
@src/content/render/InlineRenderer.tsx
@src/measurement/useMeasurement.ts
@src/settings/SettingsContext.tsx
@src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: PageFragmentView + PaginatedSurface (derive pages, render one fragment, basic turn)</name>
  <files>src/pagination/fragmentRenderer.tsx, src/reader/PaginatedSurface.tsx, tests/component/PaginatedSurface.test.tsx</files>
  <read_first>
    - src/content/render/BlockRenderer.tsx (BlockView L21 + ArticleBody L115 — the renderer to REUSE; the security comment L9-17 to copy into fragmentRenderer)
    - src/pagination/fragment.ts (paginateDocument signature + FragmentationResult shape from Plan 01 — the engine this container calls)
    - src/pagination/types.ts (PageFragment shape from Plan 01)
    - src/routes/ArticleView.tsx (the callback-ref + state seam L74-86; the cancelled-flag effect pattern L107-129; the rAF deferral L154-172 — PaginatedSurface mirrors these)
    - src/measurement/useMeasurement.ts (the trustedView return type — what PaginatedSurface receives)
    - src/settings/SettingsContext.tsx (useSettings().settings.readingMode — the branch signal)
    - 04-RESEARCH.md §Architecture Patterns §Pattern 5 (single content tree — render one page at a time) + §System Architecture Diagram
    - 04-PATTERNS.md §"src/pagination/fragmentRenderer.tsx" + §"src/reader/PaginatedSurface.tsx"
    - 04-UI-SPEC.md §Component Inventory (the .page-fragment section aria-label, the .paginated-surface modifier)
  </read_first>
  <behavior>
    - PageFragmentView renders a <section class="page-fragment" aria-label="Page {N}"> containing BlockView for each block slice in the fragment (reuses BlockView, never forks a parallel renderer)
    - PaginatedSurface receives {article, trustedView, articleEl, diagnostics} and derives pages via paginateDocument in a cancelled-flag effect; holds currentPageIdx state; mounts ONLY the current PageFragmentView (single content tree)
    - Chevrons: Previous/Next <button> with aria-label "Previous page"/"Next page", aria-disabled at first/last page, 44x44 hit area, inline-SVG chevron glyph
    - On turn: setCurrentPageIdx(idx ± 1); the article element stays mounted; only the fragment children swap
    - Component test: a PaginatedSurface rendered with a 3-page fixture + a trustedView stub renders exactly one page-fragment section, and clicking Next advances to page 2
  </behavior>
  <action>
    Create `src/pagination/fragmentRenderer.tsx`. Import `BlockView` from `../content/render/BlockRenderer` and the `PageFragment` type from `./types`. Copy the security header comment from BlockRenderer.tsx L9-17 verbatim (the react/no-danger note applies identically — the fragment renderer slices blocks and feeds them to BlockView; it emits ONLY JSX/text children). Export `PageFragmentView({fragment, pageIndex, article}: {fragment: PageFragment; pageIndex: number; article: CanonicalArticle})` rendering `<section className="page-fragment" aria-label={`Page ${pageIndex + 1}`}>`. Inside, map the fragment.blocks entries: for each entry, resolve the source block from article.blocks[blockIndex] and render `<BlockView block={sourceBlock} />` (for whole-block fragments startGrapheme===0 and endGrapheme===blockLength; intra-block splits are handled by slicing — for this first slice, render whole blocks only and leave a TODO comment that Plan 04/05 wires the run-split path for mid-paragraph breaks via splitParagraphRuns). Import CanonicalArticle via `import type`. Do NOT introduce a parallel block-kind switch — BlockView owns it.

    Create `src/reader/PaginatedSurface.tsx`. Import useEffect/useState/useRef/useCallback from react; useSettings from `../settings/SettingsContext`; paginateDocument from `../pagination/fragment`; PageFragmentView from `../pagination/fragmentRenderer`; PageIndicator from `./PageIndicator`; types from `../measurement/types` + `../content/types`. Define props `{article: CanonicalArticle; trustedView: MeasurementResult | null; articleEl: HTMLElement | null; diagnostics: DiagnosticBus; pageContentBoxHeightPx: number}`. Hold `const [pages, setPages] = useState<PageFragment[] | null>(null)` and `const [currentPageIdx, setCurrentPageIdx] = useState(0)`. In a cancelled-flag useEffect (mirror ArticleView L107-129) keyed on [article, trustedView, articleEl]: if trustedView && articleEl, call paginateDocument({article, measurement: trustedView, articleEl, pageContentBoxHeightPx, diagnostics, signal: controller.signal}) inside an AbortController; on resolve if not cancelled setPages(result.pages) (and if result.status === "fallback" leave pages null — Plan 05 wires the banner; for this plan a null pages falls back to rendering nothing and the parent keeps scrolling mode visible). Mount ONLY `<PageFragmentView fragment={pages[currentPageIdx]} pageIndex={currentPageIdx} article={article} />` plus the chevrons + PageIndicator. Chevron handlers: `const goNext = () => setCurrentPageIdx(i => Math.min(i+1, pages.length-1))` and goPrev symmetric; Previous has aria-disabled={currentPageIdx===0}, Next has aria-disabled={currentPageIdx===pages.length-1}. Render `<button type="button" className="page-turn page-turn-previous" aria-label="Previous page" aria-disabled={...}><ChevronLeftIcon aria-hidden="true" /></button>` and the Next mirror (inline SVG, viewBox 0 0 24 24, stroke currentColor, focusable="false" — mirror Header.tsx GearIcon L42-59 discipline). The article element itself stays in ArticleView (shared between modes per UI-SPEC §Component Inventory L734); PaginatedSurface renders the fragment children + chevrons + indicator inside it.

    Create `tests/component/PaginatedSurface.test.tsx` using React Testing Library (mirror tests/component/ArticleView.test.tsx structure). Render PaginatedSurface with a 3-block article fixture, a trustedView stub whose blocks array has 3 entries with heightPx that yields 3 pages at the given pageContentBoxHeightPx, and a mock articleEl whose querySelectorAll returns 3 stub elements. Assert: exactly one `.page-fragment` section is in the document; clicking the Next button advances the aria-label from "Page 1" to "Page 2"; the Previous button has aria-disabled="true" on page 1; the Next button has aria-disabled="true" on the last page.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/component/PaginatedSurface.test.tsx</automated>
  </verify>
  <done>
    - PageFragmentView renders a section.page-fragment via BlockView (imports BlockView, does not re-declare a block-kind switch)
    - PaginatedSurface derives pages from trustedView via paginateDocument in a cancelled-flag effect; mounts one fragment at a time
    - Chevrons advance/retreat the page; aria-disabled reflects first/last page
    - Component test exits 0 (single content tree, turn advances, disabled states correct)
  </done>
  <acceptance_criteria>
    - src/pagination/fragmentRenderer.tsx contains `import { BlockView }` from `../content/render/BlockRenderer` and exports `PageFragmentView`
    - src/pagination/fragmentRenderer.tsx does NOT contain a `switch (block.kind)` (it reuses BlockView, no parallel switch)
    - src/reader/PaginatedSurface.tsx contains `import { paginateDocument }` and `useState` and renders exactly one PageFragmentView
    - src/reader/PaginatedSurface.tsx chevron buttons carry `aria-label="Previous page"` and `aria-label="Next page"`
    - `npm run test:unit -- --run tests/component/PaginatedSurface.test.tsx` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ArticleView mode-aware branch + PageIndicator + ProgressHairline N/M + page geometry CSS</name>
  <files>src/routes/ArticleView.tsx, src/reader/PageIndicator.tsx, src/reader/ProgressHairline.tsx, src/app.css</files>
  <read_first>
    - src/routes/ArticleView.tsx (the full file — the render block L260-295 to branch; the useMeasurement call L103 to read; useScrollSave L92; queryBlocks L51)
    - src/reader/ProgressHairline.tsx (the presentational pattern L34-49; the no-transition comment L14-18 to preserve)
    - src/settings/SettingsContext.tsx (useSettings().settings.readingMode — now present after Plan 02)
    - src/measurement/diagnostics.ts (DiagnosticBus — ArticleView must thread the bus instance to PaginatedSurface)
    - src/app.css (the existing .progress-hairline rules; the --space-2xl token; the .status card pattern; the global reduced-motion + forced-colors gates)
    - 04-PATTERNS.md §"EDIT src/routes/ArticleView.tsx" + §"EDIT src/reader/ProgressHairline.tsx" + §"src/reader/PageIndicator.tsx"
    - 04-UI-SPEC.md §Layout §Page geometry (the calc(100vh - 48px - 2px - 2*var(--space-2xl))) + §Component Inventory L729 (the page-indicator span)
  </read_first>
  <behavior>
    - ArticleView reads useMeasurement's return into `trustedView`, reads settings.readingMode, and branches: readingMode==="paginated" && trustedView && articleEl renders <PaginatedSurface .../> inside the shared <article class="article-body paginated-surface">; otherwise renders <ArticleBody> (scrolling, unchanged)
    - ProgressHairline accepts an optional page prop {current, total}; when present the fill scaleX uses current/total and a PageIndicator sibling renders; when absent the fill uses the scroll-ratio progress (unchanged)
    - PageIndicator renders <span class="page-indicator" aria-hidden="true">{N} of {M}</span> via Intl.NumberFormat
    - app.css gains .paginated-surface (width var(--measure), height calc(100vh - 48px - 2px - 2*var(--space-2xl)), margin-inline auto, overflow hidden, touch-action pan-y pinch-zoom), .page-fragment, .page-indicator, .page-turn/.page-turn-previous/.page-turn-next (44x44 hit area, transparent bg, 24x24 glyph, position fixed at viewport edges, --ink-soft default/--accent on hover/focus-visible, aria-disabled opacity ~40%) — all authored CSS, zero motion properties on new selectors
  </behavior>
  <action>
    Edit `src/routes/ArticleView.tsx`: change `useMeasurement(article, articleRef);` (L103, return ignored) to `const trustedView = useMeasurement(article, articleRef);`. Add `const { settings } = useSettings();` and `const isPaginated = settings.readingMode === "paginated";` (import useSettings from `../settings/SettingsContext`). The measurement hook internally owns the DiagnosticBus (useMeasurement.ts L77-80); expose it for PaginatedSurface by extending useMeasurement to also return `{trustedView, diagnostics}` OR by reading the diagnostics ref — prefer the minimal change: have useMeasurement return only trustedView and construct a SEPARATE DiagnosticBus in ArticleView passed to both useMeasurement (via an opts arg) and PaginatedSurface. If useMeasurement's signature change is invasive, instead have PaginatedSurface subscribe to a bus it receives as a prop and have ArticleView lift the bus via a useRef + a small useMeasurement opts extension — pick the path that keeps useMeasurement's existing call sites stable and is the smaller diff; document the choice in a comment citing Plan 03. Compute `pageContentBoxHeightPx` from articleEl.getBoundingClientRect() after mount (rAF-deferred, mirror L154-172). In the render block (L260-295), change `<article ref={articleCallbackRef} className="article-body">` to `<article ref={articleCallbackRef} className={isPaginated ? "article-body paginated-surface" : "article-body"}>` and branch the children: `isPaginated && trustedView && articleEl ? <PaginatedSurface article={article} trustedView={trustedView} articleEl={articleEl} diagnostics={diagnostics} pageContentBoxHeightPx={...} /> : <ArticleBody article={article} />`. Pass `page={{current: currentPageIdx+1, total: pages?.length}}` to ProgressHairline when paginated (lift currentPageIdx/pages from PaginatedSurface via callback props OR render ProgressHairline inside PaginatedSurface — the latter keeps state local; pick the cleaner seam and document it). Import PaginatedSurface. Leave useScrollSave, ResumeBanner, SectionAnnouncer mounted in BOTH modes (they serve scrolling; paginated derives progress from N/M).

    Create `src/reader/PageIndicator.tsx` mirroring ProgressHairline's presentational discipline: header comment citing D4-08 + READ-05, `aria-hidden="true"` (decorative — SR users get the polite announce from Plan 04's PageTurnControls), props `{current: number; total: number}`, render `<span className="page-indicator" aria-hidden="true">{fmt.format(current)} of {fmt.format(total)}</span>` where fmt = new Intl.NumberFormat(navigator.language). Defensive: if total <= 0 render null.

    Edit `src/reader/ProgressHairline.tsx`: extend the props interface to `{progress?: number; page?: {current: number; total: number}}`. When page is present, ratio = total>0 ? current/total : 0 and render `<span className="page-indicator" aria-hidden="true">...</span>` OR delegate to PageIndicator; when absent, ratio = clamp(progress) (unchanged). Preserve the no-motion-property comment block L14-18 verbatim — the N/M swap is instant (calm aesthetic; reduced-motion gate trivially satisfied).

    Edit `src/app.css`: add authored rules (no Tailwind, no component suite). `.paginated-surface { width: var(--measure); max-width: 100%; height: calc(100vh - 48px - 2px - 2 * var(--space-2xl)); margin-inline: auto; overflow: hidden; touch-action: pan-y pinch-zoom; }`. `.page-fragment { height: 100%; }`. `.page-indicator { font: 400 14px/1.45 system-ui, -apple-system, sans-serif; color: var(--ink-soft); }` (Meta/Label role per UI-SPEC). `.page-turn { position: fixed; top: calc(50% + 25px); min-width: var(--touch); min-height: var(--touch); background: transparent; border: 0; color: var(--ink-soft); z-index: 5; touch-action: manipulation; }` with `.page-turn-previous { left: var(--space-sm); }` and `.page-turn-next { right: var(--space-sm); }`. Add `:hover, :focus-visible { color: var(--accent); }` and `[aria-disabled="true"] { opacity: 0.4; }` and the standard `:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 2px; }`. Do NOT add any motion-related property to these new selectors (the reduced-motion gate must be trivially satisfied). Confirm --space-2xl and --touch tokens already exist in :root (they do — Phase 1/2).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/component/</automated>
  </verify>
  <done>
    - ArticleView reads trustedView from useMeasurement and branches render on settings.readingMode
    - The shared <article> element gains .paginated-surface in paginated mode (no duplicate content tree)
    - ProgressHairline derives fill from N/M when page prop present; PageIndicator renders the N of M text
    - app.css has .paginated-surface with the exact calc height + overflow hidden + touch-action pan-y pinch-zoom
    - Component tests exit 0; existing scrolling-mode tests still pass (the branch is additive)
  </done>
  <acceptance_criteria>
    - src/routes/ArticleView.tsx contains `const trustedView = useMeasurement(` (the return is now read, not ignored)
    - src/routes/ArticleView.tsx contains `settings.readingMode` and `paginated-surface` and `PaginatedSurface`
    - src/reader/ProgressHairline.tsx accepts a `page` prop and computes ratio from current/total when present
    - src/reader/PageIndicator.tsx contains `aria-hidden="true"` and `Intl.NumberFormat`
    - src/app.css contains `calc(100vh - 48px - 2px - 2 * var(--space-2xl))` and `touch-action: pan-y pinch-zoom` inside a `.paginated-surface` rule
    - `npm run test:unit -- --run tests/component/` exits 0
    - `npm run test:unit -- --run` (full unit) exits 0 — no scrolling-mode regression
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement trustedView → PaginatedSurface | Phase 3's staleness-safe result crosses into the pagination derivation; a stale view is already filtered by PAGE-06/PAGE-07 inside useMeasurement |
| Block model → fragment renderer | The fragment renderer slices the same Block model scrolling mode renders; no new injection surface (BlockView emits JSX/text only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | PageFragmentView rendering | mitigate | Reuse BlockView — React escapes text/attribute children; react/no-danger ESLint rule forbids raw-HTML injection; the fragment renderer slices the same Block model (no new injection surface) |
| T-04-08 | Repudiation | page-turn (no audit log) | accept | Local-only reader UI; page turns are ephemeral view state, never persisted (page number is not identity per READ-05) |
| T-04-09 | Denial of Service | repagination effect | mitigate | Cancelled-flag effect + AbortController; stale pagination pass cannot overwrite a newer one (PAGE-05 substrate); engine termination guards (Plan 01) bound the work |
| T-04-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `npm run test:unit -- --run tests/component/PaginatedSurface.test.tsx` exits 0 (single content tree + turn + disabled states)
- `npm run test:unit -- --run` exits 0 (no scrolling-mode regression — the branch is additive)
- `npx tsc --noEmit` exits 0 (useMeasurement return read; readingMode field resolved; BlockView import resolves)
- `npm run lint` exits 0 (no react/no-danger violation in fragmentRenderer)
- Manual (deferred to Plan 05 phase gate): open an article in paginated mode in a real browser, confirm a page renders and chevrons turn — Playwright corpus matrix proofs land in Plan 05
</verification>

<success_criteria>
- A reader with readingMode "paginated" sees a single semantic page fragment (not the full scrolling article)
- Chevrons turn pages forward/backward with correct aria-disabled at the boundaries
- ProgressHairline + PageIndicator reflect N/M in paginated mode
- The page content-box is viewport-bounding and never scrolls (overflow hidden)
- Scrolling mode is byte-unchanged in behavior (additive branch only)
</success_criteria>

<output>
Create `.planning/phases/04-responsive-pagination-and-dual-mode-navigation/04-03-SUMMARY.md` when done
</output>

## Artifacts this plan produces

**New symbols:**
- `PageFragmentView({fragment, pageIndex, article})` — `src/pagination/fragmentRenderer.tsx`
- `PaginatedSurface({article, trustedView, articleEl, diagnostics, pageContentBoxHeightPx})` — `src/reader/PaginatedSurface.tsx`
- `PageIndicator({current, total})` — `src/reader/PageIndicator.tsx`
- `ChevronLeftIcon`, `ChevronRightIcon` (inline SVG) — inside PaginatedSurface.tsx

**Edited symbols:**
- `ProgressHairline` (now accepts `page` prop) — `src/reader/ProgressHairline.tsx`
- `ArticleView` (mode-aware render branch, reads useMeasurement return) — `src/routes/ArticleView.tsx`
- `.paginated-surface`, `.page-fragment`, `.page-indicator`, `.page-turn` CSS — `src/app.css`

**New test files:**
- `tests/component/PaginatedSurface.test.tsx`
