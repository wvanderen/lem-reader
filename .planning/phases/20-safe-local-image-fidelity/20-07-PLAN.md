---
phase: 20-safe-local-image-fidelity
plan: 07
type: execute
wave: 5
depends_on: ["20-04", "20-05", "20-06"]
files_modified:
  - tests/e2e/
  - .planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md
  - .planning/phases/20-safe-local-image-fidelity/deferred-items.md
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, IMG-05, IMG-06]
must_haves:
  truths:
    - "npm run test exits 0 in ONE invocation with a fresh dev server — honest counts recorded in 20-OUTPUT.md (the 04-11/09-07/19-05 permanent-record discipline)"
    - "Every requirement IMG-01..06 is closed by a plan proving end-to-end behavior, with this plan recording the closure ledger"
    - "The animated-AVIF residual (is-animated covers GIF/APNG/WebP only) is recorded honestly as a documented residual, not silently ignored (Pitfall 8)"
    - "Cap numbers (transport budget, per-asset/count/total caps, geometry constants) are re-checked against the corpus evidence the phase produced — generous bomb-stoppers, not reading-police (per D20-11)"
  artifacts:
    - path: ".planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md"
      provides: "Honest full-suite gate record + closure ledger + residual notes"
    - path: ".planning/phases/20-safe-local-image-fidelity/deferred-items.md"
      provides: "Recorded residuals (animated AVIF; any corpus-tuning follow-ups)"
  key_links:
    - from: "20-OUTPUT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "closure ledger maps each IMG-xx to the plan(s) that proved it"
      pattern: "IMG-"
  prohibitions:
    - statement: "No requirement flips to complete without green end-to-end evidence in this gate run"
      status: enforced
      verification: "20-OUTPUT.md records per-requirement evidence pointers (plan + spec + counts)"
---

<objective>
Phase gate: honest full-suite verification, the IMG closure ledger, residual
recording, and cap sanity re-check.

Purpose: The 05-05/09-07/19-05 gate discipline — the phase is only done when
the FULL suite passes in one invocation from a fresh server, every requirement
closure traces to evidence, and every honest residual is written down.

Output: 20-OUTPUT.md (gate record + closure ledger), deferred-items entries,
green full suite.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-UI-SPEC.md
@.planning/phases/20-safe-local-image-fidelity/20-01-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-02-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-03-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-04-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-05-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-06-SUMMARY.md
</context>

## Artifacts this phase-plan produces

- `.planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md` — gate counts, per-requirement closure ledger, residual list
- `deferred-items.md` entries: animated-AVIF residual (Pitfall 8); cap-number tuning notes if corpus evidence argues (D20-11)
- No production code (test-sweep/realignment fixes only if the gate surfaces them — each fix committed atomically with a justification)

<tasks>

<task type="auto">
  <name>Task 1: Honest full-suite gate</name>
  <files>tests/e2e/, .planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md</files>
  <read_first>.planning/phases/19-cross-block-highlights/19-05-SUMMARY.md (honest-gate precedent + stale-pin realignment discipline), .planning/STATE.md 15-04/18-04 lessons (fresh dev server; worker contention control), package.json test scripts, the six Phase 20 SUMMARYs</read_first>
  <action>Run the FULL npm run test in one invocation from a FRESH dev server (kill any reuseExistingServer leftovers first — the 15-04 aged-server lesson; if machine load starves webkit, the documented contention control is a bounded --workers setting per the 18-04 precedent, recorded honestly). If failures surface: classify each as (a) stale pins the sanctioned changes broke → realign strengthen-only with D20 citations, (b) real regression → fix atomically with a commit per fix, or (c) pre-existing/unrelated → record honestly, never silently dropped. Record exact counts (unit passed/failed/skipped + e2e per engine) and the invocation command in 20-OUTPUT.md as the permanent record.
  <acceptance_criteria>
    - npm run test exits 0 in one invocation (or the recorded honest-red path with classification + follow-ups — never a silent subset/grep/engine-skip)
    - 20-OUTPUT.md contains exact pass/fail/skip counts for unit + e2e and the command line used
    - Every realignment/fix commit carries a justification referencing the decision or lesson it cites
  </acceptance_criteria>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Full suite green from a fresh server; counts + command recorded permanently</done>
</task>

<task type="auto">
  <name>Task 2: Closure ledger + residuals + cap sanity</name>
  <files>.planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md, .planning/phases/20-safe-local-image-fidelity/deferred-items.md</files>
  <read_first>20-OUTPUT.md (Task 1 record), the six Phase 20 SUMMARYs (requirements-completed frontmatter), .planning/REQUIREMENTS.md IMG rows, 20-RESEARCH.md Assumptions Log (A4 animated AVIF; A3 cap ranges) + Pitfall 8, 20-CONTEXT.md D20-11</read_first>
  <action>Write the closure ledger into 20-OUTPUT.md mapping each IMG-01..06 requirement to the plan(s) + spec(s) that prove it end-to-end (IMG-01 → 20-02/20-06 ingest + epub specs; IMG-02 → 20-01 sniff matrix + ssrf extension evidence; IMG-03 → 20-04 offline-reopen + 20-03 cascades; IMG-04 → 20-05 bundle round-trip; IMG-05/06 → 20-04 geometry + page-identity specs). Record residuals honestly in deferred-items.md: the animated-AVIF gap (accepted per A4 with the corpus rationale); any cap number whose corpus evidence argues for tuning (constants stay named + commented — tuning is a follow-up, not a silent edit). Flag in the SUMMARY frontmatter requirements-completed per the honest split discipline (foundation plans that shipped substrate only keep their requirement open until the proving plan — mirror the 04-02/19-01 precedent if any plan's evidence is partial).
  <acceptance_criteria>
    - 20-OUTPUT.md contains a table with all six IMG rows, each pointing at plan + spec evidence
    - deferred-items.md contains the animated-AVIF residual entry with rationale
    - No requirement is claimed without a pointing evidence cell in the ledger
  </acceptance_criteria>
  <verify>
    <automated>rg -c "IMG-0[1-6]" .planning/phases/20-safe-local-image-fidelity/20-OUTPUT.md</automated>
  </verify>
  <done>Every requirement traced to evidence; residuals written down; closure honest</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate evidence → requirement closure | claims must trace to green automated runs, not summaries of runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-28 | Repudiation | overstated completion (subset/grep/engine-skip gates) | mitigate | one-invocation full-suite rule; exact counts recorded; the 04-11 misreport lesson encoded as the permanent-record format |
| T-20-29 | Repudiation | silent residual loss (animated AVIF etc.) | mitigate | deferred-items entries with rationale; ledger requires evidence pointers per requirement |
</threat_model>

<verification>
- Full suite green (or honestly recorded red with classified follow-ups)
- Ledger + residuals complete
</verification>

<success_criteria>
- Phase 20 closes with every IMG requirement traced to real-browser/unit evidence
- Honest gate record permanent; residuals documented
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-07-SUMMARY.md` when done
</output>
