---
phase: 20-safe-local-image-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - server/assetStage.ts
  - server/ingest.ts
  - server/htmlToBlocks.ts
  - server/markdownToBlocks.ts
  - src/ingestion/types.ts
  - src/ingestion/IngestionClient.ts
  - tests/unit/server/assetStage.spec.ts
  - tests/unit/server/markdown-to-blocks.spec.ts
  - tests/unit/server/extraction.spec.ts
  - tests/unit/ingestion-client.test.ts
autonomous: true
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "Ingesting URL/paste-HTML/Markdown rewrites every accepted figure to an asset:img-<12hex> ref with originalSrc provenance and stored width/height, inline in the same request (per D20-01, D20-04, D20-12, D20-13)"
    - "One bad image never blocks the article: each refused/failed/over-budget figure stays a FigureBlock with alt + caption intact and src omitted, while the article saves normally (per D20-05, D20-06)"
    - "Figure refusals are disclosed in ingestionMeta.extractionWarnings with a count, never silently (honesty constraint)"
    - "The alt+caption D-05 substrate is byte-identical across the rewrite — saved locations, highlights, and caption anchors are untouched by construction (D19-01)"
    - "Markdown figures with non-http srcs become refused FigureBlocks (alt preserved, no src), not UnsupportedBlocks (OQ2 resolution, one placeholder surface)"
    - "The ingest response envelope carries validated asset entries; the client re-validates, decodes, re-checks byteLength, and re-hashes assetId before anything reaches persistence (Pitfall 10, STATE-04)"
    - "PDF ingestion is untouched and stays text-only (per D20-01)"
  artifacts:
    - path: "server/assetStage.ts"
      provides: "Per-article collect/budget/deadline/fetch/rewrite stage"
      exports: ["runAssetStage", "rewriteFiguresWithAssets"]
    - path: "server/ingest.ts"
      provides: "Asset stage wired post-extract pre-stamp; envelope += assets"
      contains: "runAssetStage"
    - path: "src/ingestion/types.ts"
      provides: "AssetEnvelopeSchema + IngestionResponseSchema assets field"
      contains: "AssetEnvelopeSchema"
    - path: "src/ingestion/IngestionClient.ts"
      provides: "ValidatedAsset exposure on IngestionSuccess (transport re-validation)"
      contains: "ValidatedAsset"
  key_links:
    - from: "server/ingest.ts"
      to: "server/assetStage.ts"
      via: "stage call between extractAndNormalize and ArticleSchema.parse/stamp"
      pattern: "runAssetStage"
    - from: "server/assetStage.ts"
      to: "server/fetchImageAsset.ts"
      via: "bounded-concurrency fetchImageAsset per unique src"
      pattern: "fetchImageAsset"
    - from: "src/ingestion/IngestionClient.ts"
      to: "src/ingestion/types.ts"
      via: "IngestionResponseSchema.parse then per-asset byteLength + assetId re-hash before exposure"
      pattern: "AssetEnvelopeSchema"
  prohibitions:
    - statement: "The rewrite never touches alt or caption fields (D-05 substrate byte-identity)"
      status: enforced
      verification: "assetStage.spec.ts asserts the figure-case splittingBlockText output is byte-identical before/after rewrite"
    - statement: "No post-save/background asset fetching exists"
      status: enforced
      verification: "runAssetStage is invoked only inside the ingest request; no scheduler/queue/worker code added"
    - statement: "data: URI figure srcs are never admitted (no escape hatch)"
      status: enforced
      verification: "collect step only gathers http(s) srcs; schema union has no data arm (D20-02)"
---

<objective>
Inline asset stage + URL/paste/Markdown integration: the per-article
collect/budget/fetch/rewrite stage joins the locked ingest orchestration; the
htmlToBlocks figureSrcResolver hook and markdownToBlocks refused-FigureBlock
promotion land; the response envelope carries base64 assets; the client
re-validates them at the network boundary.

Purpose: Implements IMG-01/IMG-02 end-to-end for the three network-path sources
— a saved article is always complete (D20-04), per-figure refusal keeps the
essay alive (D20-05), and the canonical model becomes self-contained
(D20-12). PDF stays text-only per D20-01 (no pdfToBlocks change).

Output: server/assetStage.ts + wired server/ingest.ts + evolved
htmlToBlocks/markdownToBlocks + widened envelope + re-validating
IngestionClient, all unit-proven.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-01-SUMMARY.md
@server/ingest.ts
@server/htmlToBlocks.ts
@server/markdownToBlocks.ts
@src/pagination/splitBlock.ts
</context>

## Artifacts this phase-plan produces

- `runAssetStage(article draft blocks, options)` and `rewriteFiguresWithAssets(blocks, results)` — server/assetStage.ts (the recursion shape mirrors the retiring downgradeFigures skeleton per PATTERNS)
- `AssetResolution` stage-level type — `ImageAsset | ImageAssetRefusal | "count" | "budget" | "deadline"` (server/assetStage.ts). DECISION: 20-01's `ImageAssetRefusal` union stays CLOSED at fetch-level refusals (fetch|type|bytes|pixels|animated — the sniff module's concern); the three stage-level arms (count cap, response-budget exhaustion, deadline) are article-budget concepts represented ONLY here, on the supertype both the stage and 20-06 consume. No widening of the 20-01 union.
- `figureSrcResolver` optional parameter on `htmlToBlocks` (EPUB marker hook; url/paste path passes nothing)
- Markdown `figureFromImage` promotion: non-http srcs → refused FigureBlocks
- `AssetEnvelopeSchema` ({ assetId, contentType, byteLength, dataBase64 }) + `assets: z.array(AssetEnvelopeSchema).default([])` on the ok-variants of IngestionResponseSchema — src/ingestion/types.ts
- `ValidatedAsset` ({ assetId, contentType, byteLength, bytes: Uint8Array }) exposed on `IngestionSuccess` — src/ingestion/IngestionClient.ts
- New spec: tests/unit/server/assetStage.spec.ts

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/assetStage.ts — collect, budget, fetch, rewrite</name>
  <files>server/assetStage.ts, tests/unit/server/assetStage.spec.ts</files>
  <read_first>server/ingest.ts (staged orchestrator L438-502; per-chapter try/catch skip accounting L306-379; shortHash L140-142), server/fetchImageAsset.ts (Task 20-01 output), server/epubToBooks.ts L692-713 (downgradeFigures recursion skeleton — the rewrite template), src/pagination/splitBlock.ts L113-116 (figure case byte-identity contract), src/content/schema.ts (FigureBlock post-20-01), 20-PATTERNS.md §assetStage, 20-RESEARCH Pattern 1 + OQ4</read_first>
  <action>Create server/assetStage.ts exporting the stage-level resolution type AssetResolution = ImageAsset | ImageAssetRefusal | "count" | "budget" | "deadline" (cross-plan contract decision: 20-01's ImageAssetRefusal stays CLOSED at fetch-level refusals — fetch|type|bytes|pixels|animated; the three stage arms are article-budget concepts represented ONLY on this supertype, consumed by this stage and by 20-06) and rewriteFiguresWithAssets(blocks, resolution: Map&lt;originalSrc, AssetResolution&gt;) — recursive map over blocks (figure direct; blockquote children; bulleted/numbered-list items content) that for each figure with an http(s) src looks up the resolution and returns: accepted → { ...block, src: "asset:" + assetId, originalSrc: &lt;the url&gt;, width, height } (ONLY these four fields differ); refused → { ...block, src: undefined } with originalSrc kept when the fetch produced provenance, alt and caption untouched; figure with no src already → unchanged. Also export runAssetStage(blocks, { signal/deadline }) which: collects unique http(s) figure srcs in document order (data: and other schemes never collected — they are already refused upstream by the schema/httpUrl gate per D20-02); enforces MAX_FIGURES_PER_ARTICLE (srcs beyond the cap refuse "count" — a stage-level AssetResolution arm, never a fetchImageAsset refusal; no fetch performed); enforces MAX_ASSET_RESPONSE_BYTES as a running decoded-byte budget over accepted assets in document order (first-come deterministic; beyond-budget → per-figure refusal "budget" per D20-05); enforces ASSET_STAGE_DEADLINE_MS overall (remaining srcs refuse "deadline" when it passes — bounded inline latency per D20-04); fetches with bounded concurrency ASSET_FETCH_CONCURRENCY via fetchImageAsset; returns { blocks: rewritten, assets: ImageAsset[] , refusedCount }. runAssetStage never throws for per-figure problems; only infrastructure errors (e.g. invalid input shape) escape. The caller (Task 2) stamps the disclosure. Spec tests/unit/server/assetStage.spec.ts with vi.mock fetchImageAsset: per-figure refusal (one refusing src among three — article blocks all survive, refused one has src omitted + alt/caption intact); count-cap refusal; response-budget refusal mid-list (deterministic document order); deadline refusal; substrate byte-identity — for every kind of figure (accepted/refused/nested-in-list/nested-in-blockquote) assert the splitBlock.ts figure-case output ([alt, captionText].filter(Boolean).join(BLOCK_SEPARATOR)) is byte-identical before vs after the rewrite; accepted rewrite asserts exactly src/originalSrc/width/height changed (deep-equal on the rest).
  <acceptance_criteria>
    - server/assetStage.ts exports runAssetStage, rewriteFiguresWithAssets, and the AssetResolution stage-level type (fetch-level refusals stay closed in 20-01's ImageAssetRefusal)
    - assetStage.spec.ts exits 0 with cells: per-figure refusal, count cap, response budget, deadline, nested containers, substrate byte-identity, four-field-only rewrite diff
    - The spec imports splittingBlockText (or its exact figure-case output) from src/pagination/splitBlock.ts — no forked reimplementation
    - No throw assertion for any per-figure refusal cell (typed-value checks only)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/server/assetStage.spec.ts</automated>
  </verify>
  <done>Stage + rewrite shipped; D-05 byte-identity and D20-05 per-figure composure proven green</done>
</task>

<task type="auto">
  <name>Task 2: htmlToBlocks resolver hook, Markdown promotion, ingest orchestration wiring</name>
  <files>server/htmlToBlocks.ts, server/markdownToBlocks.ts, server/ingest.ts, tests/unit/server/markdown-to-blocks.spec.ts, tests/unit/server/extraction.spec.ts</files>
  <read_first>server/htmlToBlocks.ts (figureBlock L199-232), server/markdownToBlocks.ts (figureFromImage L288-302, standalone promotion L213-222), server/ingest.ts (locked stage order: safeFetch → extractAndNormalize → slugify → ArticleSchema.parse → anchor gate → confidence → stamp), src/content/schema.ts extractionWarnings (L237), 20-PATTERNS.md §htmlToBlocks + §markdownToBlocks + §assetStage, 20-RESEARCH OQ2/OQ4 + Pitfall 1</read_first>
  <action>htmlToBlocks: add an optional figureSrcResolver parameter threaded to figureBlock; when a non-http src is claimed by the resolver (the EPUB marker path, used by 20-06) return a FigureBlock carrying that raw src string for later container resolution; when no resolver is passed (url/paste default) behavior is byte-stable — non-http srcs keep producing the existing unsupported fallback. markdownToBlocks figureFromImage: non-http srcs (relative paths, data: URIs, bare words) now return a refused FigureBlock { kind: "figure", alt: node.alt ?? "", caption: [] } with src omitted (OQ2 resolution confirmed; D20-06 one-surface rule) instead of the unsupported block — alt is preserved as content. server/ingest.ts: after extractAndNormalize and before slugify/parse/stamp, call runAssetStage on the draft blocks (url/paste/html-upload and markdown paths); stamp disclosure — when refusedCount > 0 push an extractionWarnings entry shaped "N image(s) could not be included" (exact plural form consistent with the existing array's tone); attach the accepted assets to the response envelope as assets: AssetEnvelope[] (base64 via the existing chunked bytesToBase64 — the 11-04 String.fromCharCode stack-limit lesson; Buffer base64 acceptable server-side) with the MAX_ASSET_RESPONSE_BYTES budget already enforced inside the stage; the PDF path calls nothing new (D20-01 — no pdfToBlocks edit). Extend tests/unit/server/extraction.spec.ts: url/paste figure blocks byte-stable when no resolver passed (existing cells untouched + one new resolver-marker cell); extend markdown-to-blocks.spec.ts: relative-src image → refused FigureBlock with alt, https image → unchanged FigureBlock.
  <acceptance_criteria>
    - rg "figureSrcResolver" server/htmlToBlocks.ts >= 1 and default-absent path keeps every pre-existing extraction.spec.ts assertion green
    - markdown-to-blocks.spec.ts contains a cell asserting a relative-src standalone image yields kind "figure" with alt preserved and src absent (no unsupported block)
    - rg "runAssetStage" server/ingest.ts >= 1 (the stage call site, post-extract pre-stamp)
    - rg "extractionWarnings" server/ingest.ts shows the refusal-count disclosure push
    - server/pdfToBlocks.ts is byte-unchanged in this plan's commits
    - npx vitest run tests/unit/server/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/server/markdown-to-blocks.spec.ts tests/unit/server/extraction.spec.ts tests/unit/server/assetStage.spec.ts tests/unit/server/ingest-pdf.spec.ts</automated>
  </verify>
  <done>Stage wired into the orchestrator; Markdown refusals promoted; htmlToBlocks hook lands byte-stable; PDF untouched</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Envelope widening + client transport re-validation</name>
  <files>src/ingestion/types.ts, src/ingestion/IngestionClient.ts, tests/unit/ingestion-client.test.ts, tests/unit/ingestion-schema.test.ts</files>
  <read_first>src/ingestion/types.ts (IngestionResponseSchema discriminated-union L129-155), src/ingestion/IngestionClient.ts (full — the shared pipeline note L107-111, ingestEpub two-layer T-12-10 defense L177-195), tests/unit/ingestion-client.test.ts, 20-PATTERNS.md §ingestion/types + §assetsStore, 20-RESEARCH Pitfall 10</read_first>
  <action>src/ingestion/types.ts: add AssetEnvelopeSchema = z.object({ assetId: assetRef-shaped string matching img-[a-z0-9]{12} (import or mirror the regex from content/schema — prefer importing assetRef and deriving via .regex source or a dedicated envelope field zod chain consistent with the schema module's exports), contentType: z.enum(["image/jpeg","image/png","image/webp","image/gif","image/avif"]), byteLength: z.number().int().min(1), dataBase64: z.string().min(1) }); add assets: z.array(AssetEnvelopeSchema).default([]) to every ok-variant of IngestionResponseSchema (single-article + book envelope variants). IngestionClient: in the shared post-parse pipeline, validate each envelope asset before exposure — decode dataBase64 (chunked at 0x8000 to respect the 11-04 stack-limit lesson), re-check decoded bytes.byteLength === byteLength, re-hash sha256(bytes).slice(0,12) and require it to equal assetId (the server is not trusted — Pitfall 10); any mismatch fails the whole ingest calmly as a typed refusal (network boundary violation — reuse the existing IngestionError surface with an appropriate existing reason or a narrowly widened reason value consistent with the enum discipline). Expose validated results as IngestionSuccess.assets: ValidatedAsset[] ({ assetId, contentType, byteLength, bytes }) — consumption/persistence wiring is 20-03/20-04 scope; this plan only exposes the validated array (compiles standalone). tests/unit/ingestion-client.test.ts: envelope with one valid asset → success carries ValidatedAsset with equal byteLength; tampered byteLength → typed refusal; mismatched hash (valid bytes, wrong assetId) → typed refusal; v-shape: assets absent (default []) → success with empty array (back-compat with any pre-update server responses).
  <acceptance_criteria>
    - rg "AssetEnvelopeSchema" src/ingestion/types.ts >= 1 and rg "assets" on the ok-variant(s) of IngestionResponseSchema
    - IngestionClient.ts contains the re-hash + byteLength re-check before exposure (rg "sha256|digest" src/ingestion/IngestionClient.ts)
    - ingestion-client.test.ts cells green: valid asset exposure, byteLength tamper refusal, hash-mismatch refusal, empty default
    - npx vitest run tests/unit/ingestion-client.test.ts tests/unit/ingestion-schema.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-client.test.ts tests/unit/ingestion-schema.test.ts</automated>
  </verify>
  <done>Envelope carries assets; client re-validation proven (server never trusted); ValidatedAsset exposed for the persistence plans</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → client save path | base64 assets cross the network boundary; the server is not trusted (STATE-04) |
| article JSON → response transport | generous asset sets can exceed the prod response ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering | forged/tampered asset envelope | mitigate | client re-validates: zod parse, byteLength equality, assetId re-hash (Pitfall 10) |
| T-20-07 | DoS | ingest-response bloat / base64 amplification | mitigate | MAX_ASSET_RESPONSE_BYTES running budget enforced in-stage (per-figure refusal beyond it, deterministic document order) |
| T-20-08 | DoS | image-spam pages (count bomb) | mitigate | MAX_FIGURES_PER_ARTICLE cap refuses beyond-cap figures without fetching |
| T-20-09 | DoS | unbounded inline latency | mitigate | ASSET_STAGE_DEADLINE_MS overall deadline + bounded concurrency 4 (D20-04) |
| T-20-10 | Repudiation | silent image loss | mitigate | refusedCount disclosed via ingestionMeta.extractionWarnings — never silent (honesty constraint) |
| T-20-11 | Information Disclosure | third-party beacon via accepted figure | mitigate | src rewritten to asset: ref at ingest; original URL moves to originalSrc (D20-12) — renderer consumption lands in 20-04 |
</threat_model>

<verification>
- All unit specs green: assetStage, markdown-to-blocks, extraction, ingestion-client, ingestion-schema
- ssrf-matrix + safe-fetch specs still green (no safeFetch changes in this plan)
- server/pdfToBlocks.ts byte-unchanged
</verification>

<success_criteria>
- URL/paste/Markdown ingest produces self-contained canonical figures (asset refs + dims + provenance) or calm refused FigureBlocks
- Client transport path validates every asset byte-for-byte before persistence
- D-05 substrate byte-identity proven (anchors/highlights untouched)
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-02-SUMMARY.md` when done
</output>
