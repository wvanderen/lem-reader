---
phase: 20-safe-local-image-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/content/schema.ts
  - src/ingestion/types.ts
  - server/limits.ts
  - server/safeFetch.ts
  - server/fetchImageAsset.ts
  - tests/unit/schema.test.ts
  - tests/unit/ingestion-schema.test.ts
  - tests/unit/server/safe-fetch.spec.ts
  - tests/unit/server/fetchImageAsset.spec.ts
autonomous: true
requirements: [IMG-02]
must_haves:
  truths:
    - "Every secondary image fetch passes the same 9-measure SSRF pipeline as document fetches (scheme, metadata-hostname, DNS, IP deny-list, per-hop redirects, pinning, timeout, byte cap, content-type gate) — the pipeline is parameterized, never forked (per IMG-02, D20-12 substrate)"
    - "A sniffed type outside jpeg/png/webp/gif/avif — including SVG bytes under a lying image header — refuses with typed reason 'type' and never throws (per D20-08, D20-10)"
    - "Animated GIF/WebP/APNG buffers refuse with typed reason 'animated'; static forms of the same types pass (per D20-09)"
    - "A JPEG with EXIF orientation >= 5 yields orientation-corrected width/height so the reserved box matches rendered aspect (per D20-13, Pitfall 2)"
    - "The document fetch profile is observably byte-identical to today's single-profile safeFetch (Pitfall 4)"
    - "FigureBlock parses legacy remote-src rows, refused no-src figures, and asset-ref rows; a data: URI src fails parse (per D20-02, D20-06, Pitfall 5)"
  artifacts:
    - path: "server/fetchImageAsset.ts"
      provides: "Image-profile fetch + authoritative byte sniff + typed refusals"
      exports: ["fetchImageAsset", "sniffImageAsset", "ImageAsset", "ImageAssetRefusal"]
    - path: "src/content/schema.ts"
      provides: "FigureBlock additive evolution (assetRef src union, originalSrc, width, height)"
      contains: "asset:img-"
    - path: "src/ingestion/types.ts"
      provides: "Shared image cap constants (three-enforcement-point pattern)"
      contains: "MAX_ASSET_BYTES"
    - path: "server/safeFetch.ts"
      provides: "Parameterized safeFetchCore with byte-stable document profile"
      exports: ["safeFetchCore"]
  key_links:
    - from: "server/fetchImageAsset.ts"
      to: "server/safeFetch.ts"
      via: "safeFetchCore(url, image profile) — the ONLY fetch entry for assets"
      pattern: "safeFetchCore"
    - from: "server/fetchImageAsset.ts"
      to: "src/content/schema.ts"
      via: "assetId format img-<12hex> matches the assetRef regex"
      pattern: "img-"
    - from: "server/limits.ts"
      to: "src/ingestion/types.ts"
      via: "import + re-export of the image cap constants (PDF_MAX_BYTES precedent)"
      pattern: "MAX_ASSET_BYTES"
  prohibitions:
    - statement: "No data: URI can ever become a FigureBlock.src"
      status: enforced
      verification: "tests/unit/schema.test.ts asserts zod parse failure for a data: URI src (D20-02, Pitfall 5)"
    - statement: "No second/parallel SSRF fetch pipeline exists for images"
      status: enforced
      verification: "server/fetchImageAsset.ts calls safeFetchCore; no other module performs image egress"
    - statement: "Content-Type headers are never authoritative for image admission"
      status: enforced
      verification: "fetchImageAsset.spec.ts cell: SVG bytes served with an image/png header refuse 'type'"
---

<objective>
Guarded asset-fetch substrate: install the two sniff libraries, evolve FigureBlock
additively, land the shared cap constants, parameterize safeFetch into a
two-profile core, and ship the fetchImageAsset sniff module with its typed
refusal surface.

Purpose: This is the foundation every other Phase 20 plan composes — the asset
stage (20-02), EPUB extraction (20-06), and renderer (20-04) all consume
ImageAsset/assetRef/caps from here. It implements the IMG-02 fetch/limit
machinery and the D20-02/D20-08/D20-09/D20-10 media boundary at the earliest,
most testable seam.

Output: server/fetchImageAsset.ts (fetchImageAsset + sniffImageAsset), evolved
src/content/schema.ts FigureBlock, image caps in src/ingestion/types.ts
re-exported by server/limits.ts, parameterized server/safeFetch.ts, green unit
specs.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@server/safeFetch.ts
@server/limits.ts
@src/content/schema.ts
</context>

## Artifacts this phase-plan produces

- `fetchImageAsset(url)` / `sniffImageAsset(bytes)` — server/fetchImageAsset.ts
- Types `ImageAsset`, `ImageAssetRefusal` (typed union: "fetch" | "type" | "bytes" | "pixels" | "animated")
- `safeFetchCore(url, profile)` — server/safeFetch.ts (exported parameterized core; `safeFetch` remains the byte-stable document wrapper)
- FigureBlock fields: `src: z.union([httpUrl, assetRef]).optional()`, `originalSrc: httpUrl.optional()`, `width`/`height: z.number().int().min(1).optional()`; exported `assetRef` regex `/^asset:img-[a-z0-9]{12}$/`
- Constants in src/ingestion/types.ts (re-exported from server/limits.ts): `MAX_ASSET_BYTES`, `MAX_ASSET_PIXELS`, `ASSET_FETCH_TIMEOUT_MS`, `ASSET_STAGE_DEADLINE_MS`, `ASSET_FETCH_CONCURRENCY`, `MAX_FIGURES_PER_ARTICLE`, `MAX_ARTICLE_ASSET_BYTES`, `MAX_ASSET_RESPONSE_BYTES`
- New spec files: tests/unit/server/fetchImageAsset.spec.ts
- npm deps (exact-pinned, server-only): image-size@2.0.2, is-animated@2.0.2

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sniff packages, shared cap constants, FigureBlock additive evolution</name>
  <files>package.json, package-lock.json, src/ingestion/types.ts, server/limits.ts, src/content/schema.ts, tests/unit/schema.test.ts, tests/unit/ingestion-schema.test.ts</files>
  <read_first>src/content/schema.ts (FigureBlock L100-105, httpUrl L28, extractionWarnings L237), src/ingestion/types.ts (PDF_MAX_BYTES three-point pattern L52-76), server/limits.ts (L92-100 MAX_IMAGE_PIXELS precedent), tests/unit/schema.test.ts, 20-PATTERNS.md §schema, 20-RESEARCH §Standard Stack + Pattern 2</read_first>
  <action>Install exactly: npm install image-size@2.0.2 is-animated@2.0.2 (both legitimacy-audit OK in 20-RESEARCH §Package Legitimacy Audit — no blocking gate). Add the cap constants to src/ingestion/types.ts following the PDF_MAX_BYTES shared-home pattern, each with a one-line corpus-tunable comment (per D20-11, numbers are generous bomb-stoppers; 20-07 re-checks against corpus evidence): MAX_ASSET_BYTES = 16 * 1024 * 1024 (per-asset decoded bytes); MAX_ASSET_PIXELS = 16_777_216 (same value as server MAX_IMAGE_PIXELS — one auditable bomb-cap family); ASSET_FETCH_TIMEOUT_MS = 15_000; ASSET_STAGE_DEADLINE_MS = 60_000 (overall per-article inline budget per D20-04); ASSET_FETCH_CONCURRENCY = 4; MAX_FIGURES_PER_ARTICLE = 120; MAX_ARTICLE_ASSET_BYTES = 150 * 1024 * 1024 (model-level per-article total); MAX_ASSET_RESPONSE_BYTES = 3 * 1024 * 1024 (decoded asset total in the single-article JSON transport path — base64 inflation ~4MB keeps the Vercel 4.5MB response ceiling honest; assets beyond it refuse per-figure per D20-05; dev middleware is uncapped but the constant keeps dev/prod behavior uniform). server/limits.ts imports + re-exports all eight (mirroring how it already re-exports PDF_MAX_BYTES). Evolve FigureBlock in src/content/schema.ts per RESEARCH Pattern 2: add exported assetRef = z.string().regex(/^asset:img-[a-z0-9]{12}$/); change src to z.union([httpUrl, assetRef]).optional(); add originalSrc: httpUrl.optional(); width and height as z.number().int().min(1).optional(). Do NOT touch alt or caption (D-05 substrate byte-identity). Add schema tests: legacy row with remote httpUrl src parses unchanged; refused figure with src omitted parses; asset:img-0123456789ab ref parses; a src of the banned URI scheme data:text/html fails parse (D20-02 — the union simply has no arm for it); width=0 fails min(1). Both libs are server-only imports — nothing in src/ imports them.
  <acceptance_criteria>
    - package.json contains "image-size": "2.0.2" and "is-animated": "2.0.2" with no caret/tilde range
    - rg -c "MAX_ASSET_BYTES" src/ingestion/types.ts >= 1 and rg -c "MAX_ASSET_BYTES" server/limits.ts >= 1 (re-export)
    - src/content/schema.ts contains the regex asset:img-[a-z0-9]{12} and originalSrc
    - tests/unit/schema.test.ts asserts: legacy remote-src parse OK, no-src parse OK, asset-ref parse OK, data:-scheme src parse FAILS, width below 1 fails
    - npx vitest run tests/unit/schema.test.ts tests/unit/ingestion-schema.test.ts exits 0
    - rg -l "image-size" src/ returns nothing (server-only discipline)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/schema.test.ts tests/unit/ingestion-schema.test.ts && rg -c "MAX_ASSET_BYTES" server/limits.ts</automated>
  </verify>
  <done>Constants + evolved FigureBlock shipped with green schema specs; packages exact-pinned; no src/ sniff imports</done>
</task>

<task type="auto">
  <name>Task 2: safeFetch two-profile refactor (isolated commit, document profile byte-stable)</name>
  <files>server/safeFetch.ts, tests/unit/server/safe-fetch.spec.ts</files>
  <read_first>server/safeFetch.ts (full — L110-207 pipeline, L171-196 profile seam), tests/unit/server/safe-fetch.spec.ts, tests/e2e/ingestion/ssrf-matrix.spec.ts, 20-PATTERNS.md §fetchImageAsset (L61-106), 20-RESEARCH Pattern 1 + Pitfall 4</read_first>
  <action>Extract the 9-measure pre-body pipeline (scheme → metadata-hostname → DNS resolve → IP deny-list → pinning/timeout → manual redirect per hop → content-length cap → content-type gate) into exported safeFetchCore(rawUrl, profile, hopDepth) where profile = { allowedContentTypes: string[], timeoutMs: number, maxBytes: number }. Keep the existing exported safeFetch as the document profile wrapper passing today's exact constants (ALLOWED_CONTENT_TYPES, REQUEST_TIMEOUT_MS, MAX_RESPONSE_BYTES) and returning text — its observable behavior must be byte-identical (same IngestionError reasons, same redirect recursion, same headers). Add an image-profile mode to the core that reads arrayBuffer() instead of text() and performs the post-read bytes.byteLength re-check against maxBytes (the 12-04 header-lie discipline — content-length can lie/absent/chunked); for the image profile the content-type gate matches the "image/" prefix (advisory only — sniff decides later) so early non-image refusal stays calm. This refactor lands as its OWN commit before any feature work (Pitfall 4); only strengthen existing safe-fetch spec cells if they were under-specified, never relax. Do not modify tests/e2e/ingestion/ssrf-matrix.spec.ts — it is the pin.
  <acceptance_criteria>
    - server/safeFetch.ts exports safeFetchCore (rg "export.*safeFetchCore" server/safeFetch.ts)
    - Document profile constants unchanged: rg "ALLOWED_CONTENT_TYPES|REQUEST_TIMEOUT_MS|MAX_RESPONSE_BYTES" usage preserved on the document wrapper
    - tests/unit/server/safe-fetch.spec.ts green with zero deletions of existing assertions
    - npx playwright test tests/e2e/ingestion/ssrf-matrix.spec.ts green on chromium (run from the refactor commit before starting Task 3)
    - The image-profile branch contains the post-read byteLength re-check (rg "byteLength" server/safeFetch.ts >= 1)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/server/safe-fetch.spec.ts && npx playwright test tests/e2e/ingestion/ssrf-matrix.spec.ts --project=chromium</automated>
  </verify>
  <done>Parameterized core shipped; document path byte-stable proven by the untouched SSRF matrix; refactor isolated in one commit</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fetchImageAsset sniff module + typed-refusal matrix</name>
  <files>server/fetchImageAsset.ts, tests/unit/server/fetchImageAsset.spec.ts</files>
  <read_first>server/safeFetch.ts (post-Task-2 safeFetchCore), server/ingest.ts L140-142 (shortHash precedent), src/ingestion/types.ts (Task-1 constants), node_modules/image-size/README (bundled), node_modules/is-animated/README (bundled), 20-RESEARCH Pattern 1 sketch + Pitfalls 2/5, tests/unit/server/safe-fetch.spec.ts (vi.mock dns + vi.stubGlobal fetch pattern)</read_first>
  <action>Create server/fetchImageAsset.ts with the Pattern-1 sketch shape: export interface ImageAsset { assetId, contentType ("image/jpeg"|"image/png"|"image/webp"|"image/gif"|"image/avif"), width, height (orientation-corrected ints), bytes: Uint8Array }; export type ImageAssetRefusal = "fetch" | "type" | "bytes" | "pixels" | "animated"; export async function fetchImageAsset(url): Promise&lt;ImageAsset | ImageAssetRefusal&gt; calling safeFetchCore with the image profile { allowedContentTypes: ["image/"], timeoutMs: ASSET_FETCH_TIMEOUT_MS, maxBytes: MAX_ASSET_BYTES } then sniffing the returned bytes; export function sniffImageAsset(bytes): sniff result WITHOUT network (the same sniff sequence the EPUB container path reuses in 20-06 — sniff is the shared seam, fetch is the network-only wrapper). Sniff sequence, in order: byteLength re-check vs MAX_ASSET_BYTES → "bytes"; imageSize(bytes) sniffed type not in jpeg/png/webp/gif/avif (svg lands here per D20-10) → "type"; sniffed width*height > MAX_ASSET_PIXELS → "pixels"; isAnimated(bytes) → "animated" (D20-09); EXIF orientation >= 5 swaps stored width/height (Pitfall 2); assetId = "img-" + createHash("sha256").update(bytes).digest("hex").slice(0, 12) (D7-07 locality). Every refusal is a returned typed value, never a throw (D20-05 composure); a fetch-layer IngestionError maps to "fetch". The sniffed type overrules any content-type header — headers are advisory (media-type smuggling closes only via sniff). Spec: table-driven matrix in tests/unit/server/fetchImageAsset.spec.ts using vi.mock("node:dns") + vi.stubGlobal("fetch", fetchMock) (safe-fetch.spec pattern): valid minimal real bytes for each of the 5 formats (embed tiny authentic headers — 1x1-class images; an animated GIF needs the NETSCAPE2.0 loop extension, an animated WebP needs VP8X+ANIM chunks; is-animated README documents the minimal shapes); refusal cells: svg bytes under a lying image/png header → "type"; pixel-bomb (sniffed dims product above the cap — craft via a PNG IHDR with huge declared dims) → "pixels"; over-cap bytes → "bytes"; EXIF-rotated JPEG (orientation 6) → returned width/height swapped vs raw header; fetch refusal (private-IP fetch error) → "fetch"; and a body-never-read guard asserting the refusal path does not consume the response body. Verify A1-adjacent behavior: none needed here (no Dexie).
  <acceptance_criteria>
    - server/fetchImageAsset.ts exports fetchImageAsset, sniffImageAsset, ImageAsset, ImageAssetRefusal
    - tests/unit/server/fetchImageAsset.spec.ts exits 0 with cells for: 5 valid formats, animated GIF/WebP/APNG refusals, svg-under-lying-header refusal, pixel-bomb refusal, byte-cap refusal, EXIF orientation>=5 swap, fetch-failure mapping, body-never-read guard
    - Every refusal assertion checks the typed value (no try/catch expected-throw assertions)
    - rg "img-" server/fetchImageAsset.ts shows the assetId slice(0, 12) construction
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/server/fetchImageAsset.spec.ts</automated>
  </verify>
  <done>Sniff module shipped with the full typed-refusal matrix green; sniffImageAsset is the network-free seam 20-06 reuses</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker URL → server asset fetch | untrusted image URL crosses into the Node ingest function |
| hostile bytes → sniff/store | fetched image bytes are attacker-controlled until sniffed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing/Tampering | fetchImageAsset URL input | mitigate | reuse safeFetchCore 9-measure pipeline — scheme, metadata-hostname, DNS, IP deny-list, per-hop redirect re-validation, DNS pinning (no second egress path) |
| T-20-02 | Tampering | media-type smuggling (SVG as image/png) | mitigate | magic-byte sniff is authoritative; non-raster sniffed type refuses "type" (D20-10) |
| T-20-03 | DoS | decode/pixel bomb | mitigate | sniffed width*height checked against MAX_ASSET_PIXELS before any store (16,777,216 cap) |
| T-20-04 | Tampering | content-length header lies | mitigate | post-read bytes.byteLength re-check (12-04 discipline) |
| T-20-05 | Tampering | EXIF orientation geometry corruption | mitigate | orientation >= 5 swaps stored dims at ingest (Pitfall 2) |
| T-20-SC | Tampering | npm installs (image-size, is-animated) | mitigate | Package Legitimacy Audit in 20-RESEARCH: both verdict OK/Approved, zero-dep, no install scripts; exact-pinned 2.0.2; server-only imports; single-seam module (fetchImageAsset.ts) keeps either swappable |
</threat_model>

<verification>
- All three task specs green; ssrf-matrix.spec.ts byte-unchanged and green (document profile pin)
- No src/ file imports image-size or is-animated
- package.json pins exact versions
</verification>

<success_criteria>
- IMG-02 machinery exists: SSRF-safe image fetch + type/bytes/pixels/animation limits with typed calm refusals
- FigureBlock models all three figure states (accepted asset-ref / refused no-src / legacy remote) at parse time
- safeFetch refactor proven byte-stable by the existing 19-vector matrix
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-01-SUMMARY.md` when done
</output>
