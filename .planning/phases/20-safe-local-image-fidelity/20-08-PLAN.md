---
phase: 20-safe-local-image-fidelity
plan: 08
type: execute
wave: 5
depends_on: ["20-04", "20-06"]
files_modified:
  - tests/e2e/imagery/offline-reopen.spec.ts
  - tests/e2e/imagery/geometry.spec.ts
  - tests/e2e/imagery/decode-matrix.spec.ts
  - tests/e2e/imagery/refusal-matrix.spec.ts
  - tests/e2e/open-every-fixture.spec.ts
  - tests/e2e/library/
  - tests/e2e/pagination/
autonomous: true
requirements: [IMG-03, IMG-05, IMG-06]
must_haves:
  truths:
    - "Reopening a saved figure article renders accepted images from local blobs and fires ZERO third-party requests (per IMG-03, D20-12 — proven by a non-vacuous route-abort e2e guard registered before navigation)"
    - "The figure box is fully reserved by the model before decode: decode/load/error events cause zero layout shift and page-count identity across image load (per D20-13, IMG-06 — geometry.spec)"
    - "Figures decode in all three engines for jpeg/png/webp/gif/avif from local blobs (per D20-08 — decode-matrix.spec)"
    - "Refused figures (no src / legacy remote src) render the placeholder with alt visible and captions still highlightable in both modes (per D20-14, D19-01 — refusal-matrix.spec)"
    - "Every realigned corpus pin in open-every-fixture/library/pagination carries a justification comment citing the D20 decision that sanctioned it — strengthen-only, zero silent deletions"
  artifacts:
    - path: "tests/e2e/imagery/offline-reopen.spec.ts"
      provides: "Route-abort external-request guard + legacy remote-src placeholder cell"
      contains: "page.route"
    - path: "tests/e2e/imagery/geometry.spec.ts"
      provides: "Reserved-vs-rendered aspect, page-count identity across load, both-mode placeholder, tall-figure letterbox cells"
    - path: "tests/e2e/imagery/decode-matrix.spec.ts"
      provides: "5 formats × 3 engines decode cells from local blobs"
    - path: "tests/e2e/imagery/refusal-matrix.spec.ts"
      provides: "Refused/legacy figure placeholder + caption-mark cells"
  key_links:
    - from: "tests/e2e/imagery/offline-reopen.spec.ts"
      to: "src/fixtures/figure-assets.ts"
      via: "seeds asset rows from the 20-04 fixture registry (real per-format bytes)"
      pattern: "fixtureAssetRegistry"
    - from: "tests/e2e/imagery/"
      to: "src/content/assets/AssetProvider.tsx"
      via: "assertions exercise the provider-resolved object URLs (20-04 output)"
      pattern: "useAssetUrl|AssetProvider"
  prohibitions:
    - statement: "The offline guard is never vacuous — routes are registered BEFORE navigation and the external-request array is asserted empty"
      status: enforced
      verification: "offline-reopen.spec.ts registers page.route abort for every non-localhost URL pre-navigation (the font-failure L16-26 pattern) and asserts the collected external array length 0"
    - statement: "No expectation in open-every-fixture/library/pagination is deleted or weakened without a justification comment citing a D20-xx decision"
      status: enforced
      verification: "git diff on realigned cells shows only cited strengthen-only changes; acceptance criterion requires the citation per changed expectation"
---

<objective>
The 3-engine imagery e2e matrix + honest corpus pin realignment: the four
imagery specs (offline-reopen, geometry, decode-matrix, refusal-matrix) prove
the 20-04 renderer guarantees in real browsers across chromium/firefox/webkit,
and the corpus suites (open-every-fixture, library, pagination) realign
strengthen-only against the sanctioned figure-heavy/placeholder/EPUB changes.

Purpose: Implements the PROOF half of IMG-03/05/06 — the structural guarantees
landed in 20-04 (and the EPUB model changes in 20-06) are only reader-true
when a real browser confirms zero third-party requests, stable page counts
across decode, per-format decode, and calm refusal surfaces. Sequenced after
20-06 so the realignment sweep runs once, against ALL sanctioned model changes
(figure-heavy regen, placeholder semantics, EPUB downgrade retirement), not
twice against partial state.

Output: tests/e2e/imagery/ with four green 3-engine specs + honestly realigned
open-every-fixture/library/pagination pins with per-decision citations.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-UI-SPEC.md
@.planning/phases/20-safe-local-image-fidelity/20-04-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-06-SUMMARY.md
@tests/e2e/annotations/_fixtures.ts
@tests/e2e/font-failure.spec.ts
</context>

## Artifacts this phase-plan produces

- `tests/e2e/imagery/offline-reopen.spec.ts` — non-vacuous route-abort external-request guard; saved-figure reopen + legacy remote-src placeholder cells
- `tests/e2e/imagery/geometry.spec.ts` — reserved-vs-rendered aspect (incl. EXIF-rotated fixture), page-count identity across load events, both-mode placeholder, tall-figure letterbox
- `tests/e2e/imagery/decode-matrix.spec.ts` — jpeg/png/webp/gif/avif × chromium/firefox/webkit decode cells
- `tests/e2e/imagery/refusal-matrix.spec.ts` — refused/legacy placeholder + caption-mark cells (D19-01 inside refused figures)
- Realigned strengthen-only pins in open-every-fixture/library/pagination — every changed expectation cited to its D20 decision
- No production code changes (a failing matrix cell is a 20-04/20-06 regression to route back, not a spec to weaken)

<tasks>

<task type="auto">
  <name>Task 1: The four imagery specs — offline, geometry, decode, refusal</name>
  <files>tests/e2e/imagery/offline-reopen.spec.ts, tests/e2e/imagery/geometry.spec.ts, tests/e2e/imagery/decode-matrix.spec.ts, tests/e2e/imagery/refusal-matrix.spec.ts</files>
  <read_first>tests/e2e/font-failure.spec.ts (route-before-navigate non-vacuous pattern L16-26), tests/e2e/annotations/_fixtures.ts (wipeDatabase/openArticle helpers), tests/e2e/ingestion/happy-path.spec.ts (API-mock envelope pattern L141-162), src/fixtures/figure-assets.ts (20-04 registry), src/content/assets/AssetProvider.tsx (20-04 output), 20-RESEARCH Code Examples (offline proof sketch) + Pitfalls 3/5, 20-UI-SPEC §Regression Targets</read_first>
  <action>Create tests/e2e/imagery/ with shared helpers (seed asset rows via Dexie in page.evaluate — the dexie-migration seeding precedent; reuse wipeDatabase/openArticle from annotations/_fixtures.ts). offline-reopen.spec.ts: page.route abort guard for every non-localhost URL registered BEFORE navigation + page.on("request") external array; seed a saved figure article with asset rows; assert figure img visible, naturalWidth > 0, external requests array empty (IMG-03 literally — the 06-04 non-vacuous guard); also a legacy remote-src cell asserting the placeholder renders and external stays empty. geometry.spec.ts: reserved-vs-rendered aspect (img clientWidth/clientHeight ratio equals stored w/h ratio incl. the EXIF-rotated fixture); page-count identity across image load events (open figure-heavy in paginated mode, wait for all img load/error events, assert page count + no fallback banner — decode is paint not layout, IMG-06); placeholder renders in BOTH modes (scrolling + paginated) with caption visible; tall-figure letterbox cell (a stored tall aspect stays under the media cap — no oversize fallback banner). decode-matrix.spec.ts: 5 formats x 3 engines via the API-mock envelope (real registry bytes base64'd) OR fixture-registry seeding — each format opens, renders naturalWidth > 0. refusal-matrix.spec.ts: seeded refused FigureBlocks (no src; legacy httpUrl src) render the placeholder with alt visible + caption highlightable (a D19-01 caption mark cell inside a refused figure) + article opens normally both modes. Run all imagery specs across chromium/firefox/webkit. If a cell fails because 20-04/20-06 regressed a guarantee, fix the SOURCE per its plan's discipline — never weaken the spec.
  <acceptance_criteria>
    - tests/e2e/imagery/ contains the four spec files; all green on chromium + firefox + webkit (npx playwright test tests/e2e/imagery --project=chromium --project=firefox --project=webkit)
    - offline-reopen asserts the external-request array is empty AND the guard is non-vacuous (route registered pre-navigation)
    - geometry spec asserts page-count identity across image load + no PaginationFallbackBanner
    - decode-matrix covers all 5 formats on all 3 engines (15 green cells)
    - refusal-matrix includes a caption-mark cell inside a refused figure (D19-01) and both-mode article-open cells
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/imagery --project=chromium --project=firefox --project=webkit</automated>
  </verify>
  <done>3-engine imagery matrix green; offline-by-construction, reserved geometry, per-format decode, and calm refusals proven in real browsers</done>
</task>

<task type="auto">
  <name>Task 2: Corpus pin realignment — open-every-fixture, library, pagination</name>
  <files>tests/e2e/open-every-fixture.spec.ts, tests/e2e/library/, tests/e2e/pagination/</files>
  <read_first>tests/e2e/open-every-fixture.spec.ts, tests/e2e/library/ + tests/e2e/pagination/ corpus cells (current figure-heavy + EPUB-downgrade pins), .planning/phases/19-cross-block-highlights/19-05-SUMMARY.md (stale-pin realignment discipline), 20-04-SUMMARY.md + 20-06-SUMMARY.md (the sanctioned changes), 20-RESEARCH Pitfall 7</read_first>
  <action>Realignment sweep (Pitfall 7 — planned work, strengthen-only, every changed expectation gets a justification comment citing its D20 decision): run the open-every-fixture + library + pagination suites; realign ONLY cells broken by the sanctioned changes — figure-heavy regen (asset: srcs + stored dims from 20-04-T1), placeholder semantics for legacy remote-src fixtures (20-04-T2), and EPUB downgrade retirement cells (20-06). Zero expectation deletions without a citation; a cell broken by anything else is a regression to route back to the owning plan, never a pin to nudge. Where the new model state admits a STRONGER assertion (e.g. local img naturalWidth > 0 where the old pin asserted a downgraded placeholder), strengthen with the citation.
  <acceptance_criteria>
    - every realigned expectation in open-every-fixture/library/pagination carries a justification comment referencing a D20-xx decision; zero expectation deletions without citation
    - realigned suites green: npx playwright test tests/e2e/open-every-fixture.spec.ts --project=chromium
    - library + pagination suites green on chromium: npx playwright test tests/e2e/library tests/e2e/pagination --project=chromium
    - no production file modified by this task (test files only — git diff proves it)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/open-every-fixture.spec.ts --project=chromium && npx playwright test tests/e2e/library tests/e2e/pagination --project=chromium</automated>
  </verify>
  <done>Corpus pins honestly realigned with citations; all three suites green against the final Phase 20 model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted model + blobs → DOM | the proof target: the renderer's ONLY inputs are the model and local blobs (structural offline guarantee from 20-04) |
| sanctioned model changes → corpus pins | realignment is a trust exercise: pins move only with a cited decision, never to make a failing suite green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | tracking beacon on reopen (proof half) | mitigate | offline-reopen.spec route-abort guard registered pre-navigation + asserted-empty external-request array — any accidental fetch is a test failure |
| T-20-15 | DoS | image decode destabilizing pagination (proof half) | mitigate | geometry.spec page-count identity across all img load/error events + no-fallback-banner assertion + tall-figure letterbox cell |
| T-20-29 | Repudiation | realignment weakening corpus honesty | mitigate | strengthen-only rule with mandatory per-expectation D20 citations; deletions-without-citation fail acceptance; regressions route back to the owning plan instead of nudging pins |
</threat_model>

<verification>
- Four imagery specs green on chromium/firefox/webkit
- open-every-fixture + library + pagination green with cited realignments only
- No production code touched by this plan
</verification>

<success_criteria>
- IMG-03 proven end-to-end in real browsers (zero third-party requests, local decode)
- IMG-05/06 proven (reserved geometry, page-count identity, calm refusals both modes)
- Corpus pins honestly realigned with per-decision citations
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-08-SUMMARY.md` when done
</output>
