---
phase: 20-safe-local-image-fidelity
plan: 04
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - src/fixtures/figure-assets.ts
  - src/fixtures/articles/figure-heavy.canonical.json
  - src/fixtures/index.ts
  - src/ingestion/AddDialog.tsx
  - src/content/assets/AssetProvider.tsx
  - src/content/render/BlockRenderer.tsx
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/unit/content/asset-provider.test.tsx
  - tests/e2e/ingestion/happy-path.spec.ts
autonomous: true
requirements: [IMG-03, IMG-05, IMG-06]
must_haves:
  truths:
    - "The figure box is fully reserved by the model before decode: stored dims → inline aspect-ratio; decode/load/error events cannot change geometry by construction (per D20-13, IMG-06 — browser proof lands in 20-08)"
    - "Refused-at-ingest, legacy remote-src, and broken-at-read figures render the IDENTICAL quiet placeholder (framed box + glyph + visible alt or the 'Image unavailable.' note) with caption marks byte-stable (per D20-14, D19-01)"
    - "A remote URL never reaches an img element — the render branch emits an img only for resolvable local asset refs (per IMG-03; the real-browser offline proof is 20-08's offline-reopen spec)"
    - "Object URLs are provider-owned: created once per article open, all revoked on article switch/unmount; page turns never create or revoke (per Pitfall 9)"
    - "Validated assets flow into saves: AddDialog threads success.assets into save(article, assets) and saveBook(..., assets) so persisted articles are complete (per D20-04, D20-15)"
  artifacts:
    - path: "src/content/assets/AssetProvider.tsx"
      provides: "Per-article object-URL map (fixture registry first, Dexie second) + lifecycle"
      exports: ["AssetProvider", "useAssetUrl"]
    - path: "src/content/render/BlockRenderer.tsx"
      provides: "Figure case: reserved-box img | one placeholder surface"
      contains: "figure-placeholder"
    - path: "src/fixtures/figure-assets.ts"
      provides: "Bundled per-format fixture bytes registry (incl. animated + EXIF-rotated samples)"
      exports: ["fixtureAssetRegistry"]
    - path: "src/app.css"
      provides: ".figure-placeholder surface + figure media geometry + two custom properties"
      contains: "figure-placeholder"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/content/assets/AssetProvider.tsx"
      via: "provider wraps both bodies (visible + hidden measurement clone)"
      pattern: "AssetProvider"
    - from: "src/content/render/BlockRenderer.tsx"
      to: "src/content/assets/AssetProvider.tsx"
      via: "useAssetUrl(block.src) — undefined for every non-asset state"
      pattern: "useAssetUrl"
    - from: "src/ingestion/AddDialog.tsx"
      to: "src/ingestion/LibrarySource.ts"
      via: "save(article, success.assets) / saveBook(..., assets) wiring"
      pattern: "\\.assets"
  prohibitions:
    - statement: "The renderer never fetches: only local blob object URLs resolve; a src that is not a resolvable local asset renders the placeholder by construction"
      status: enforced
      verification: "BlockRenderer emits img only on the resolved-object-URL branch (source: the img render is gated on the resolved object URL); the real-browser route-abort proof lands in 20-08-T1 offline-reopen.spec.ts"
    - statement: "No new transition/animation/scroll-behavior properties on any Phase 20 selector (D20-09 trivially satisfies reduced motion)"
      status: enforced
      verification: "rg 'transition|animation' on the new .figure-placeholder / figure media rules returns 0"
    - statement: "Placeholder alt text is not highlightable (media-box text; D19-02 gap rule)"
      status: enforced
      verification: "the placeholder span sits outside the caption InlineList path; refusal-matrix spec asserts caption marks still work while placeholder text produces no mark surface"
---

<objective>
Reserved-geometry rendering + the placeholder surface + the fixture corpus:
AssetProvider resolves local blobs, BlockRenderer renders reserved-box images
or ONE calm placeholder, app.css gains the two geometry tokens, figure-heavy
regenerates with local refs, and the save-call wiring persists ValidatedAssets
from ingest (20-02/20-03 outputs).

Purpose: Implements the reader-visible half of IMG-03/05/06 — decode is paint,
not layout (D20-13); one surface for every non-asset state (D20-14); zero
post-save network by construction. The 3-engine e2e matrix and corpus pin
realignment that PROVE these behaviors ship in 20-08 (wave 5, after 20-06).

Output: AssetProvider + evolved BlockRenderer + CSS + fixture registry +
regenerated corpus + save wiring + asset-provider unit spec + the happy-path
ingestion cell.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-UI-SPEC.md
@.planning/phases/20-safe-local-image-fidelity/20-02-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-03-SUMMARY.md
@.planning/phases/19-cross-block-highlights/19-CONTEXT.md
@src/content/render/BlockRenderer.tsx
@src/app.css
</context>

## Artifacts this phase-plan produces

- `AssetProvider` / `useAssetUrl(src)` — src/content/assets/AssetProvider.tsx (optional-context hook — null outside provider, legacy callers byte-unchanged)
- `figureAssetIds(article)` helper (walks blocks for asset: srcs)
- `fixtureAssetRegistry: Map<articleId, AssetRecordRow[]>` — src/fixtures/figure-assets.ts (tiny authentic per-format bytes: jpeg, png, webp, gif, avif, animated gif, EXIF-rotated jpeg; self-verifying at module load — the 11-01 generator precedent)
- BlockRenderer figure-case surfaces: reserved-box `<img>` (width/height attrs + inline aspect-ratio + loading lazy + decoding async + onError swap) and `.figure-placeholder` span (glyph + visible alt or the verbatim note)
- CSS: `.figure-placeholder`, `.article-body figure > img` geometry rule, `--figure-media-max-h: calc((100dvh - 48px - 2 * 48px) * 0.5)`, `--figure-placeholder-ratio: 3 / 2`
- AddDialog wiring: article + book save calls pass ValidatedAssets
- New specs: tests/unit/content/asset-provider.test.tsx + the happy-path ingestion cell (the four imagery e2e specs + corpus realignment are 20-08 scope)
- Regenerated: src/fixtures/articles/figure-heavy.canonical.json (asset: refs + dims)

<tasks>

<task type="auto">
  <name>Task 1: Fixture asset corpus + save-call wiring</name>
  <files>src/fixtures/figure-assets.ts, src/fixtures/articles/figure-heavy.canonical.json, src/fixtures/index.ts, src/ingestion/AddDialog.tsx, tests/e2e/ingestion/happy-path.spec.ts</files>
  <read_first>src/fixtures/index.ts (build-time Zod-validated loading L8-33), src/fixtures/articles/figure-heavy.canonical.json (current remote wikimedia srcs), src/ingestion/AddDialog.tsx (ingest handlers + save/saveBook call sites), src/ingestion/IngestionClient.ts (IngestionSuccess.assets + EpubIngestionSuccess), tests/e2e/ingestion/happy-path.spec.ts (API-mock envelope pattern L141-162, inert image route L76-77), 20-RESEARCH Validation Architecture Wave-0 + Pitfall 7</read_first>
  <action>Create src/fixtures/figure-assets.ts exporting fixtureAssetRegistry built from embedded authentic minimal bytes per format (reuse the byte-crafting approach from the 20-01 sniff spec; bytes may be shared by importing a common bytes module placed under src/fixtures so server tests can also import it — server→src import direction is allowed). Include: one valid tiny image per format (jpeg/png/webp/gif/avif), one animated GIF, one EXIF-orientation-6 JPEG. Registry maps the figure-heavy article id to AssetRecordRow-shaped entries (Blob via new Blob at module init or lazy — keep it deterministic); assetIds = img- + sha256 slice(0,12) computed at build/runtime init, and the module self-verifies (byte-magic prefix check; throws at module load on drift — 11-01 precedent). Regenerate figure-heavy.canonical.json: rewrite the two remote wikimedia srcs to asset:img-<12hex> refs matching registry ids, add width/height + originalSrc (keep alt/caption byte-identical — D-05). Wire AddDialog: the article path passes success.assets into dexieLibrarySource.save(article, assets); the book path passes chapter assets into saveBook (shape agreed in 20-03). Extend happy-path.spec.ts with one API-mock cell whose envelope includes a valid base64 asset — assert the saved article renders the local img (naturalWidth > 0) after navigation.
  <acceptance_criteria>
    - src/fixtures/figure-assets.ts exports fixtureAssetRegistry and self-verifies (module-load guard present)
    - figure-heavy.canonical.json contains zero wikimedia URLs (rg "upload.wikimedia" returns 0) and contains asset:img- refs with width/height
    - AddDialog save calls thread assets (rg "assets" src/ingestion/AddDialog.tsx on both save and saveBook paths)
    - happy-path new cell green on chromium
    - alt/caption strings in figure-heavy.canonical.json byte-identical to the previous revision (git diff shows only src/originalSrc/width/height changes)
  </acceptance_criteria>
  <verify>
    <automated>! rg -q "upload.wikimedia" src/fixtures/articles/figure-heavy.canonical.json && npx playwright test tests/e2e/ingestion/happy-path.spec.ts --project=chromium</automated>
  </verify>
  <done>Corpus is local-asset-backed; validated assets flow into saves end-to-end</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssetProvider + BlockRenderer figure case + geometry CSS</name>
  <files>src/content/assets/AssetProvider.tsx, src/content/render/BlockRenderer.tsx, src/routes/ArticleView.tsx, src/app.css, tests/unit/content/asset-provider.test.tsx</files>
  <read_first>src/content/render/BlockRenderer.tsx (figure case L229-248, BlockViewProps L95-144, captionHighlightSlices computation L631-664), src/reader/annotations/HighlightOverlay.tsx (context + optional-hook pattern L155-165, L277-281), src/routes/ArticleView.tsx (mount structure incl. hidden measurement body), src/app.css (token blocks + .disclosure/.status anatomy), 20-UI-SPEC.md §Component Inventory + §Layout + Auto-Resolved #2-#8/#13, 20-RESEARCH Patterns 4 + Pitfalls 3/5/9</read_first>
  <action>AssetProvider (RESEARCH Pattern 4 sketch, HighlightOverlay context anatomy): per-article Map keyed by assetId → objectURL; useEffect resolve loop — fixtureAssetRegistry consult FIRST for fixture article ids (fixtures never touch Dexie), else db.assets bulkGet via 20-03's bulkGetAssets; missing rows simply stay unresolved (broken state); revoked-flag + revoke-all cleanup on articleId change/unmount (StrictMode twin-mount safe). Export useAssetUrl as an OPTIONAL-context hook (null outside provider — BlockRenderer compiles for any legacy caller). Mount the provider in ArticleView where BOTH the visible body and the hidden measurement body render inside it (per-article map = deterministic geometry, no per-turn churn). BlockRenderer figure case: resolve via useAssetUrl(block.src); when an object URL exists render the img with width={block.width} height={block.height} (only when dims present), loading="lazy" decoding="async", inline style aspectRatio from stored dims, and onError → local broken state rendering the placeholder INSIDE the same reserved box; otherwise render the placeholder span (class figure-placeholder): inline style aspectRatio = stored dims when present else the default 3/2 token, contents = the 20px inline-SVG image glyph (aria-hidden, mirrors header-icon stroke anatomy) + visible alt text, or the verbatim note string when alt is empty (never both — the note never replaces non-empty alt). The figcaption branch + captionHighlightSlices threading stays byte-identical in every state (D19-01); elementProps/data-block-index forwarding unchanged; a remote-httpUrl src (legacy row) takes the placeholder branch — the img element is emitted ONLY on the resolved-object-URL branch. app.css: add the two custom properties to :root (verbatim from UI-SPEC §Layout: --figure-media-max-h and --figure-placeholder-ratio: 3 / 2); .figure-placeholder per UI-SPEC (surface-raised bg, 1px hairline border, 4px radius, --space-md padding, column flex centered, --space-sm gap, --font-ui 14px --ink-soft text, forced-colors CanvasText mapping — RestorationMarker precedent); .article-body figure > img rule: display block, max-width 100%, margin-inline auto, max-height var(--figure-media-max-h), object-fit contain, letterbox areas show plain surface (no img background rule). ZERO transition/animation properties on any new rule (UI-SPEC Interaction 4). Unit spec tests/unit/content/asset-provider.test.tsx: create/revoke symmetry (spy on URL.createObjectURL/revokeObjectURL — every created URL revoked exactly once on unmount; articleId switch revokes the old set); fixture-registry article resolves without Dexie (fake-indexeddb untouched); missing row → undefined URL.
  <acceptance_criteria>
    - src/content/assets/AssetProvider.tsx exports AssetProvider + useAssetUrl; revoke symmetry test green
    - BlockRenderer emits the img element only inside the resolved-object-URL branch; the placeholder span class figure-placeholder handles refused (no src), legacy (httpUrl src), and broken (onError/missing row) states
    - The exact note string for empty-alt placeholders matches the UI-SPEC verbatim copy (Image unavailable. — with the period)
    - rg "transition|animation" within the new .figure-placeholder / figure img CSS rules returns 0 matches
    - app.css contains --figure-media-max-h and --figure-placeholder-ratio declarations
    - caption marks path byte-unchanged (git diff on the figcaption branch shows no change)
    - npx vitest run tests/unit/content/asset-provider.test.tsx exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/content/asset-provider.test.tsx && rg -c "figure-placeholder" src/app.css</automated>
  </verify>
  <done>One render surface maps all four figure states; reserved geometry CSS shipped; provider lifecycle proven leak-free</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted model + blobs → DOM | the renderer's ONLY inputs are the model and local blobs (structural offline guarantee) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | tracking beacon on reopen | mitigate | structural: img element only on the resolved object-URL branch (this plan); the offline route-abort e2e that makes any accidental fetch a test failure lands in 20-08-T1 |
| T-20-15 | DoS | image decode destabilizing pagination | mitigate | reserved aspect boxes (model-determined geometry) + media-height cap (this plan); page-count identity asserted across load events in 20-08-T1 geometry.spec |
| T-20-16 | DoS | blob-URL leaks across article switches | mitigate | provider-owned create/revoke symmetry, revoke-all on switch/unmount (unit-proven) |
| T-20-17 | Tampering | broken/missing asset row at read | mitigate | unresolved ref renders the placeholder inside the same reserved box — geometry from the model never changes (Pitfall 5) |
</threat_model>

<verification>
- asset-provider unit spec green; happy-path ingestion cell green
- No new transition/animation CSS; caption-mark path byte-identical
- figure-heavy corpus fully local; save wiring threads assets
</verification>

<success_criteria>
- IMG-03 render-half shipped structurally (img only for resolved local refs; e2e proof in 20-08)
- IMG-05/06 render-half shipped (reserved geometry attrs/CSS, calm one-surface failures; e2e proof in 20-08)
- Corpus + fixture registry + save wiring ready for the 20-08 matrix
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-04-SUMMARY.md` when done
</output>
