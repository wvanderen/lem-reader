---
phase: 20-safe-local-image-fidelity
plan: 06
type: execute
wave: 4
depends_on: ["20-02", "20-04"]
files_modified:
  - server/epubToBooks.ts
  - src/ingestion/IngestionClient.ts
  - src/ingestion/AddDialog.tsx
  - tests/unit/server/epub-to-books.spec.ts
  - tests/e2e/epub-intake.spec.ts
  - tests/e2e/library/
autonomous: true
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "EPUB chapter figures extract from the already-open container with ZERO network — every admitted figure is a local asset:img ref with stored dims, and the D12-16 downgrade retirement admits them as figures (per D20-01, D20-03 scope)"
    - "EPUB-internal figures inherit the same sniff caps minus fetch: type (raster-only, SVG refuses), bytes, pixels, animation — over-cap chapter figures refuse per-figure with alt+caption preserved (per D20-08/09/10/11)"
    - "Chapter-relative srcs resolve through OPF-dir-relative path math off the archive entries map; unresolvable srcs refuse calmly per-figure"
    - "Cover images are never extracted (chapter-content figures only — per D20-03)"
    - "Refused chapter figures are disclosed (extractionWarnings count), never silently dropped (D12-11 honesty mirror)"
    - "Per-book asset totals stay bounded by budget arithmetic — over-budget chapter figures refuse per-figure (Pitfall 6)"
  artifacts:
    - path: "server/epubToBooks.ts"
      provides: "Container extraction replacing downgradeFigures; assets emitted alongside chapter drafts"
      contains: "sniffImageAsset"
  key_links:
    - from: "server/epubToBooks.ts"
      to: "server/fetchImageAsset.ts"
      via: "sniffImageAsset(bytes) — the network-free sniff seam (no fetchImageAsset call anywhere in the EPUB path)"
      pattern: "sniffImageAsset"
    - from: "server/epubToBooks.ts"
      to: "server/htmlToBlocks.ts"
      via: "figureSrcResolver hook passed by walkChapterDocument (marker for container-relative srcs)"
      pattern: "figureSrcResolver"
    - from: "server/epubToBooks.ts"
      to: "server/assetStage.ts"
      via: "rewriteFiguresWithAssets reuse for the recursive block rewrite"
      pattern: "rewriteFiguresWithAssets"
  prohibitions:
    - statement: "The EPUB asset path performs zero network egress"
      status: enforced
      verification: "no fetch/safeFetch call in the container-extraction code path (rg on the new code); unit test asserts extraction succeeds with fetch stubbed to throw"
    - statement: "Cover-image/manifest-cover extraction does not exist (chapter-content figures only)"
      status: enforced
      verification: "no code path reads the OPF cover-image meta or spawns book-level asset rows; only chapter figures produce assets"
    - statement: "alt/caption substrate byte-identity holds on the EPUB rewrite too"
      status: enforced
      verification: "reuse of rewriteFiguresWithAssets (the same function assetStage.spec.ts pins) — no forked EPUB rewrite"
---

<objective>
EPUB container extraction: retire D12-16 downgradeFigures, resolve chapter
figure srcs against the archive entries map, run the shared sniff caps minus
fetch, rewrite blocks via the shared stage helper, and carry chapter assets
through the book envelope to the client save path.

Purpose: Implements the EPUB half of IMG-01/IMG-02 — figures come from the
already-local container (zero network), the anti-beacon guarantee moves from
"downgrade everything" to "local assets only", and honest disclosure covers
refused figures.

Output: evolved server/epubToBooks.ts (+ IngestionClient book-asset exposure),
honestly realigned EPUB unit + e2e specs.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-02-SUMMARY.md
@.planning/phases/20-safe-local-image-fidelity/20-04-SUMMARY.md
@server/epubToBooks.ts
@server/htmlToBlocks.ts
</context>

## Artifacts this phase-plan produces

- Deleted: `downgradeFigures` (D12-16 retirement — the function and its walkChapterDocument call site)
- New in server/epubToBooks.ts: chapter-figure container extraction — figureSrcResolver wiring (marker srcs), chapter-relative → OPF-dir-relative resolution (dirOf precedent), entry-bytes read + sniffImageAsset caps, per-chapter + per-book budget arithmetic, extractionWarnings disclosure, chapter assets emitted on the book envelope
- IngestionClient: EpubIngestionSuccess carries validated chapter assets for the saveBook call (AddDialog wiring already threads from 20-04 Task 1)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Container extraction — resolve, sniff-cap, rewrite, disclose</name>
  <files>server/epubToBooks.ts, src/ingestion/IngestionClient.ts, tests/unit/server/epub-to-books.spec.ts</files>
  <read_first>server/epubToBooks.ts (unzipEpub L313-347 + isSafeEntryName discipline, dirOf + OPF-dir precedents L219/L472/L614/L664, walkChapterDocument L734-749 + downgradeFigures call site L745, per-chapter try/catch skip accounting L306-379, ingestEpubBook envelope L281-414), server/htmlToBlocks.ts (figureSrcResolver hook from 20-02), server/fetchImageAsset.ts (sniffImageAsset), server/assetStage.ts (rewriteFiguresWithAssets), src/ingestion/IngestionClient.ts (ingestEpub two-layer validation L177-195), 20-PATTERNS.md §epubToBooks, 20-RESEARCH Pattern 5 + Pitfall 6</read_first>
  <action>Delete downgradeFigures and its call. In walkChapterDocument: pass a figureSrcResolver to htmlToBlocks that returns the RAW relative src as a marker (option (a) per RESEARCH Pattern 5 — url/paste path stays byte-stable since it passes nothing). After walking, for each figure carrying a marker src: resolve chapter-relative → OPF-dir-relative using dirOf on the chapter item href; read bytes from the already-open entries map (isSafeEntryName on the composed key before any byte use — the existing discipline); run sniffImageAsset caps (type/SVG refusal, bytes, pixels, animated — same constants, NO fetch); accepted → rewrite via rewriteFiguresWithAssets with the same { src: "asset:"+assetId, originalSrc: chapter-relative path or a container-absolute provenance string consistent with httpUrl-vs-path semantics — originalSrc is httpUrl-typed in the schema, so for EPUB keep provenance in extractionWarnings/asset meta instead of forcing a non-URL into originalSrc; omit originalSrc on the EPUB path unless the src is a valid absolute URL }, width, height } shape; refused/unresolvable/over-budget → per-figure refusal (src omitted, alt+caption intact). Budgets: per-chapter figure caps identical to the network path constants + a per-book total asset budget (bounded naturally by the container; enforce MAX_ARTICLE_ASSET_BYTES as the guard — over-budget figures refuse per-figure per Pitfall 6). Disclosure: push extractionWarnings entries on refused figures (count form matching 20-02's tone). Chapter assets flow out alongside chapter drafts through ingestEpubBook's envelope (the book envelope's assets field from 20-02). IngestionClient.ingestEpub: validate book envelope assets with the SAME re-validation used for single articles (byteLength + re-hash) and expose them on EpubIngestionSuccess (AddDialog already threads assets into saveBook from 20-04 Task 1 — verify the shapes line up; adjust the AddDialog book call only if the article-vs-book asset shape differs). Spec (epub-to-books.spec.ts, extend): a container fixture with one valid PNG chapter figure → admitted as figure with asset: ref + dims + alt/caption intact (NOT an unsupported block — the retirement assertion); animated GIF entry → refused figure with alt preserved; SVG entry → refused (type); over-pixel entry → refused; missing-entry src (dangling path) → refused calmly; zero-network cell: vi.stubGlobal("fetch", () => throw) and extraction still succeeds; cover-image manifest present but NOT extracted (assert zero book-level asset rows); substrate byte-identity cell reusing the splitBlock figure-case output on a rewritten EPUB figure.
  <acceptance_criteria>
    - rg "downgradeFigures" server/epubToBooks.ts returns 0 (function + call site deleted)
    - rg "sniffImageAsset|rewriteFiguresWithAssets|figureSrcResolver" server/epubToBooks.ts each >= 1 (shared seams reused, no fork)
    - epub-to-books.spec.ts cells green: admitted figure w/ dims, animated/SVG/pixel refusals, dangling path, zero-network, no-cover-extraction, substrate identity
    - EpubIngestionSuccess exposes validated book assets (rg on src/ingestion/IngestionClient.ts)
    - npx vitest run tests/unit/server/epub-to-books.spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/server/epub-to-books.spec.ts tests/unit/server/ingest-epub.spec.ts</automated>
  </verify>
  <done>D12-16 retired; chapter figures are local assets or calm refusals; zero-network proven; covers untouched</done>
</task>

<task type="auto">
  <name>Task 2: EPUB e2e realignment + figure rendering cells</name>
  <files>tests/e2e/epub-intake.spec.ts, tests/e2e/library/</files>
  <read_first>tests/e2e/epub-intake.spec.ts (current downgrade-based expectations), tests/e2e/imagery/_helpers or the 20-04 seeding helpers, tests/e2e/library/ EPUB corpus cells, 20-UI-SPEC.md §Regression Targets (deliberate change #2), 20-RESEARCH Pitfall 7</read_first>
  <action>Realign epub-intake.spec.ts honestly (strengthen-only, every changed expectation gets a justification comment citing D20-01/D12-16-retirement): chapter figures now render as semantic figure elements with local imgs (naturalWidth > 0) or placeholders — the DOC-06 disclosure expectation RETIRES from the EPUB figure path only (it stays for genuinely unsupported content); add one cell ingesting an EPUB whose chapter contains a valid figure via the API-mock book envelope (or fixture-backed book) asserting the figure renders in the reader; add one refused-figure cell (animated or SVG entry) asserting the placeholder + caption survive. Sweep tests/e2e/library/ EPUB corpus cells for pins broken by the un-downgrade (book row counts unchanged — figure admission affects block content, not book/article counts; realign only rendering-derived pins). Run the EPUB suite across all three engines.
  <acceptance_criteria>
    - epub-intake.spec.ts contains a chapter-figure rendering cell (figure element + visible local img or placeholder) with a D20 citation comment
    - every modified expectation in epub-intake/library carries a justification comment; zero uncited deletions
    - npx playwright test tests/e2e/epub-intake.spec.ts --project=chromium --project=firefox --project=webkit exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/epub-intake.spec.ts --project=chromium</automated>
  </verify>
  <done>EPUB specs honestly realigned; figure rendering + refusal proven in real browsers on 3 engines</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EPUB archive entries → sniff/store | container bytes are attacker-controlled until sniffed |
| chapter src strings → archive keys | relative-path resolution must not escape the entry map discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-23 | Tampering | path traversal via chapter-relative src | mitigate | composed keys pass isSafeEntryName before any byte use; entries map is the only read source |
| T-20-24 | Tampering | media-type smuggling inside the container (SVG as figure) | mitigate | sniffImageAsset authoritative type gate (raster-only) |
| T-20-25 | DoS | decode/pixel bombs inside EPUB | mitigate | same MAX_ASSET_PIXELS/MAX_ASSET_BYTES caps as network path |
| T-20-26 | DoS | container-response inflation (10MB book + base64 assets) | mitigate | per-book asset budget guard — over-budget figures refuse per-figure (Pitfall 6) |
| T-20-27 | Information Disclosure | third-party beacon via EPUB figure | mitigate | zero-network extraction + asset-only rendering (the 20-04 structural guarantee); zero-network unit cell |
</threat_model>

<verification>
- epub-to-books + ingest-epub unit specs green
- epub-intake e2e green on 3 engines with cited realignments
- downgradeFigures gone; cover extraction absent
</verification>

<success_criteria>
- EPUB figures admitted from the container with the same caps discipline as network assets
- D12-16 retirement complete with honest spec realignment
- D20-03 scope held (no covers anywhere)
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-06-SUMMARY.md` when done
</output>
