---
phase: 20-safe-local-image-fidelity
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/persistence/db.ts
  - src/persistence/assetsStore.ts
  - src/ingestion/LibrarySource.ts
  - src/persistence/booksStore.ts
  - tests/unit/persistence/assets-cascade.spec.ts
  - tests/unit/library/library-source.test.ts
autonomous: true
requirements: [IMG-03, IMG-04]
must_haves:
  truths:
    - "Article + assets save in ONE Dexie read-write transaction — an asset-put failure rolls back the article put (a saved article is always complete, per D20-04/D20-15)"
    - "Deleting an article removes its asset rows in the same single transaction as the existing cascade (per D20-15, D17-13 shape)"
    - "Deleting a book removes every chapter's asset rows in the same single transaction (per D20-15)"
    - "Re-ingest upsert (same article id) range-deletes the old asset rows and puts the new ones inside one transaction — a figure that now refuses leaves no orphan blob (per D20-07, D9-14)"
    - "The v6 Dexie block is additive-only: v1..v5 blocks byte-unchanged, no upgrade callback (Pitfall 9)"
    - "Asset reads drop corrupt rows calmly after Zod-at-boundary parse — never coerce (STATE-04)"
  artifacts:
    - path: "src/persistence/db.ts"
      provides: "v6 assets store (compound PK [articleId+assetId] + articleId index)"
      contains: "assets"
    - path: "src/persistence/assetsStore.ts"
      provides: "Zod-validated asset reads/writes + export bulk read"
      exports: ["AssetRecordSchema", "putAssets", "bulkGetAssets", "loadAllAssets", "deleteAssetsForArticle"]
    - path: "src/ingestion/LibrarySource.ts"
      provides: "save(article, assets?) atomic upsert + remove cascade extension"
      contains: "assets"
  key_links:
    - from: "src/ingestion/LibrarySource.ts"
      to: "src/persistence/db.ts"
      via: "db.assets.where(\"articleId\").equals(id).delete() inside the existing remove transaction"
      pattern: "db\\.assets"
    - from: "src/persistence/booksStore.ts"
      to: "src/persistence/db.ts"
      via: "saveBook/removeBook transactions gain the assets table (array overload — SEVEN tables)"
      pattern: "db\\.assets"
    - from: "src/persistence/assetsStore.ts"
      to: "src/content/schema.ts"
      via: "assetId format shared with the assetRef regex (img-<12hex>)"
      pattern: "img-"
  prohibitions:
    - statement: "No Dexie version-block edits to v1..v5 and no .upgrade() callback anywhere"
      status: enforced
      verification: "assets-cascade.spec.ts asserts v1..v5 store declarations unchanged vs git baseline; rg '\.upgrade\(' src/persistence/db.ts returns 0"
    - statement: "No refcounting/GC machinery for assets (article-owned rows only)"
      status: enforced
      verification: "the only asset deletes are the article/book cascades + upsert replacement; no shared-asset table exists"
---

<objective>
Dexie v6 assets store + atomic lifecycle: the additive version block, the
Zod-at-boundary store seam, one-transaction save/upsert for articles and books,
and the deletion cascades.

Purpose: Implements the persistence half of IMG-03/IMG-04 — assets are
article-owned blobs (D20-15) whose lifecycle is exactly the article's: saved
together, replaced together on re-ingest, deleted together. Ships the store
interface 20-04 (renderer reads) and 20-05 (portability reads/writes) consume.

Output: src/persistence/db.ts v6 block, src/persistence/assetsStore.ts,
extended LibrarySource.save/remove + booksStore.saveBook/removeBook, green
assets-cascade spec.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-01-SUMMARY.md
@src/persistence/db.ts
@src/persistence/booksStore.ts
@src/ingestion/LibrarySource.ts
</context>

## Artifacts this phase-plan produces

- Dexie `assets` store: `"\\[articleId+assetId\\], articleId"` in a `this.version(6).stores({...})` block (all v5 stores re-declared verbatim inside it, v1..v5 blocks untouched) + typed `assets!: Dexie.Table<AssetRecordRow, [string, string]>` property
- `AssetRecordRow` / `AssetRecordSchema`: { articleId, assetId, contentType, byteLength, data: Blob, createdAt (ISO) }
- `putAssets(articleId, assets: ValidatedAsset[])`, `bulkGetAssets(articleId, assetIds)`, `loadAllAssets()` (export whole-library read — the loadAllHighlights precedent), `deleteAssetsForArticle(articleId)` — src/persistence/assetsStore.ts
- `DexieLibrarySource.save(article, assets?)` and `saveBook(book, articles, assets?)` signatures (assets optional, default [] — existing call sites compile unchanged; call-site wiring of real assets is 20-04 scope)
- New spec: tests/unit/persistence/assets-cascade.spec.ts

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dexie v6 assets store + assetsStore seam</name>
  <files>src/persistence/db.ts, src/persistence/assetsStore.ts, tests/unit/persistence/assets-cascade.spec.ts</files>
  <read_first>src/persistence/db.ts (v5 append block L199-206, compound-PK location precedent L118, books! definite-assignment L103-108, readerTitle bumpless note L83-93), src/persistence/highlightsStore.ts (Zod-read + corrupt-row-drop L66-84), src/persistence/booksStore.ts (never-throw + classifyStorageError L75-90, loadAllHighlights L124-135), 20-PATTERNS.md §db + §assetsStore, 20-RESEARCH Pattern 3</read_first>
  <action>db.ts: append this.version(6).stores({ ...all v5 store declarations verbatim..., assets: "[articleId+assetId], articleId" }) — no .upgrade() (Pitfall 9), v1..v5 blocks byte-unchanged; add the typed table property with the definite-assignment marker mirroring the books! precedent. Create src/persistence/assetsStore.ts: AssetRecordSchema (zod) with AssetRecordRow shape { articleId: string, assetId: img-[a-z0-9]{12}-constrained string, contentType enum of the five image types, byteLength: int min 1, data: z.instanceof(Blob), createdAt: ISO string }; putAssets builds rows from ValidatedAsset inputs (new Blob([bytes], { type: contentType })) with createdAt stamped OUTSIDE any transaction (stamp-before-transaction, the saveBook discipline); bulkGetAssets queries compound keys; loadAllAssets returns the plain-array whole-store read with per-row safeParse and calm corrupt-row drop; deleteAssetsForArticle wraps the articleId index range delete. All public functions never-throw: discriminate storage failures via the classifyStorageError precedent. Tests follow the fake-indexeddb pattern used by existing persistence specs (see tests/unit/persistence/books-store.test.ts harness): put → bulkGet round-trip preserves byteLength + contentType; corrupt row (bad assetId shape) is dropped from reads without throwing; loadAllAssets returns [] on empty store.
  <acceptance_criteria>
    - rg "version\\(6\\)" src/persistence/db.ts >= 1 and the v6 block contains the assets store string "[articleId+assetId], articleId"
    - rg "\\.upgrade\\(" src/persistence/db.ts returns 0
    - git diff shows v1..v5 version blocks byte-unchanged (assert in spec or verify step via git diff -- src/persistence/db.ts inspection)
    - assetsStore exports AssetRecordSchema, putAssets, bulkGetAssets, loadAllAssets, deleteAssetsForArticle
    - round-trip + corrupt-row-drop cells green: npx vitest run tests/unit/persistence/assets-cascade.spec.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/persistence/assets-cascade.spec.ts && rg -c "version\\(6\\)" src/persistence/db.ts</automated>
  </verify>
  <done>v6 store + seam shipped additively; reads Zod-validated with calm drops</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic save/upsert + article/book deletion cascades</name>
  <files>src/ingestion/LibrarySource.ts, src/persistence/booksStore.ts, tests/unit/persistence/assets-cascade.spec.ts, tests/unit/library/library-source.test.ts</files>
  <read_first>src/ingestion/LibrarySource.ts (save L82, remove cascade L108-151 — collect-before-delete shape), src/persistence/booksStore.ts (saveBook puts-only one-transaction L139-158, removeBook chapter loop L179-239, six-table readonly-array overload note), 20-PATTERNS.md §LibrarySource + Shared Patterns §One-transaction atomicity, 20-RESEARCH Pattern 3 + STATE 12-07 array-overload lesson</read_first>
  <action>LibrarySource.save(article, assets: ValidatedAsset[] = []) — inside ONE db.transaction("rw", ...) covering articles + assets: range-delete db.assets.where("articleId").equals(article.id).delete() (the upsert-replacement — D20-07 leaves no orphan blob) then the existing article put and the asset puts; stamps (createdAt) computed before the transaction; the closure stays puts-only (no zod, no crypto, no network — the 09-04 rule). remove(id): add the same one-line assets range delete INSIDE the existing four-table transaction (the articleId index exists exactly for this). booksStore.saveBook(book, articles, assetsByArticle or a flat list keyed by articleId — choose the flat ValidatedAsset-with-articleId shape consistent with the envelope) — one transaction over books + articles + assets, puts-only, per-chapter asset range-delete + put mirroring the article upsert; BOTH branches (with/without settings) that exceed five tables use the readonly-ARRAY overload form (Dexie tuple overloads stop at five — adding assets makes seven tables in the import path later, so standardize the array form here). removeBook: extend the chapter loop with the per-chapter assets range delete — still ONE transaction (D17-13). Extend assets-cascade.spec.ts: (a) save with assets → article + rows present; (b) injected asset-put failure (Dexie creating-hook throw) → article put rolled back (zero rows — atomicity); (c) re-save same id with different assets → old rows replaced, none orphaned; (d) remove(article) → assets gone in the same transaction (zero asset rows after); (e) removeBook → every chapter's assets gone. Extend tests/unit/library/library-source.test.ts for the save signature default ([] — no asset writes when omitted).
  <acceptance_criteria>
    - rg "db\\.assets" src/ingestion/LibrarySource.ts >= 1 (upsert range-delete + remove cascade) and rg "db\\.assets" src/persistence/booksStore.ts >= 1 (saveBook + removeBook)
    - assets-cascade.spec.ts cells green: save-atomic rollback, upsert replacement, article cascade, book cascade
    - No zod parse / hash / fetch call inside any transaction closure (source inspection: closures contain only puts/deletes)
    - npx vitest run tests/unit/persistence/ tests/unit/library/library-source.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/persistence/assets-cascade.spec.ts tests/unit/library/library-source.test.ts tests/unit/persistence/books-store.test.ts</automated>
  </verify>
  <done>One-transaction save/upsert/cascade shipped and proven; puts-only discipline held; array-overload form adopted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB rows → reader | persisted rows are re-validated on read (tampered/drifted rows refuse calmly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Tampering | tampered asset rows at read | mitigate | Zod safeParse per row; corrupt rows dropped calmly (never coerced) — the broken state renders the placeholder (20-04) |
| T-20-13 | Repudiation | orphan blobs after re-ingest/delete | mitigate | upsert range-delete + article/book cascades all inside the same transaction as the article writes |
| T-20-14 | DoS | partial save (article without its assets) | mitigate | single rw transaction — asset-put failure rolls back the article put (test-proven) |
</threat_model>

<verification>
- assets-cascade + library-source + books-store specs green
- v1..v5 Dexie blocks byte-unchanged; no upgrade callback
- Existing library/persistence specs unaffected (save default-param back-compat)
</verification>

<success_criteria>
- Assets persist as article-owned blobs with atomic lifecycle identical to the article's
- The store seam 20-04/20-05 consume exists with Zod-at-boundary reads
- Pitfall 9 held with zero exceptions
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-03-SUMMARY.md` when done
</output>
