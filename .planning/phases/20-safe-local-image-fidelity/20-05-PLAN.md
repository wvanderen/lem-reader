---
phase: 20-safe-local-image-fidelity
plan: 05
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - src/portability/bundle.ts
  - src/portability/manifest.ts
  - src/portability/ExportImportService.ts
  - src/portability/conflicts.ts
  - src/reader/ImportPreviewDialog.tsx
  - tests/unit/portability/bundle-v4.spec.ts
  - tests/e2e/portability/round-trip.spec.ts
  - tests/e2e/portability/import-preview.spec.ts
autonomous: true
requirements: [IMG-04]
must_haves:
  truths:
    - "Export bundles carry every referenced asset as raw zip entries at assets/<articleId>/<assetId> with per-asset sha256 in a manifest-verified metadata block — writers emit schemaVersion 4 (per IMG-04)"
    - "v1/v2/v3 bundles import unchanged (union read); v5+ forward-refuses via the peek threshold moved to > 4"
    - "An imported asset's bytes equal the exported bytes exactly (sha256 + byteLength verified at import; mismatch routes to corrupted — never-throw typing)"
    - "A bundle whose article references an asset entry that is missing/damaged skips that article with an explicit preview warning — never a silent placeholder-rewrite (honesty constraint, D12-11 mirror)"
    - "Asset conflicts ride their article's D9-14 resolution — identical article = calm no-op; incoming-wins = article + assets upsert in-transaction; skip = nothing; NO new ConflictKind"
    - "Import-side limits enforce the same cap family: fflate filter-first entry caps + per-asset byte caps + total-assets budget (IMG-04 bundle limits)"
    - "Orphan asset entries (no referencing article) drop inertly"
  artifacts:
    - path: "src/portability/bundle.ts"
      provides: "v4 union + AssetExportMeta"
      contains: "z.literal(4)"
    - path: "src/portability/manifest.ts"
      provides: "assets block hashing JSON.stringify(bundle.assets) on both sides"
      contains: "assets"
    - path: "src/portability/ExportImportService.ts"
      provides: "Asset zip entries, import validation gates, no-broken-refs, applyImport asset puts"
      contains: "assets/"
    - path: "tests/unit/portability/bundle-v4.spec.ts"
      provides: "v4 round-trip / bomb / sha256-mismatch / dangling-ref / conflict-ride proof"
  key_links:
    - from: "src/portability/ExportImportService.ts"
      to: "src/persistence/assetsStore.ts"
      via: "loadAllAssets on export; db.assets puts inside the applyImport transaction"
      pattern: "loadAllAssets|db\\.assets"
    - from: "src/portability/conflicts.ts"
      to: "src/portability/ExportImportService.ts"
      via: "ResolvedImportPlan assetsToWrite consumed by the puts-only closure"
      pattern: "assetsToWrite"
  prohibitions:
    - statement: "No new ConflictKind and no new preview row/choice — assets ride article records"
      status: enforced
      verification: "conflicts.ts ConflictKind union unchanged (git diff); ImportPreviewDialog gains only the warning list entries"
    - statement: "No zod/crypto/network inside the applyImport transaction closure (09-04 puts-only rule)"
      status: enforced
      verification: "closure contains only puts/deletes; validation happens before applyImport"
    - statement: "Zip entry names for assets are service-generated only — never derived from bundle-supplied free text without the isSafeEntryName guard"
      status: enforced
      verification: "entry construction uses assets/<articleId>/<assetId> from validated id fields; reads keep the existing isSafeEntryName guard"
---

<objective>
Bundle v4 asset round-trip: schema widening (fourth application of the union
discipline), manifest assets block, export zip entries, import validation
gates (bomb caps, sha256, no-broken-refs), D9-14 ride conflict semantics, and
the two verbatim import-preview warning strings.

Purpose: Implements IMG-04 — assets leave and re-enter the device as first-class
bundle citizens with validation, limits, honest conflict handling, and zero
broken references after import.

Output: evolved bundle.ts/manifest.ts/ExportImportService.ts/conflicts.ts,
extended ImportPreviewDialog warnings, green bundle-v4 unit + portability e2e.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-safe-local-image-fidelity/20-CONTEXT.md
@.planning/phases/20-safe-local-image-fidelity/20-RESEARCH.md
@.planning/phases/20-safe-local-image-fidelity/20-PATTERNS.md
@.planning/phases/20-safe-local-image-fidelity/20-UI-SPEC.md
@.planning/phases/20-safe-local-image-fidelity/20-03-SUMMARY.md
@src/portability/bundle.ts
@src/portability/ExportImportService.ts
</context>

## Artifacts this phase-plan produces

- `schemaVersion: z.union([z.literal(1), z.literal(2), z.literal(3), z.literal(4)])` + `assets: z.array(AssetExportMeta).optional()` — bundle.ts
- `AssetExportMeta` = { articleId, assetId, contentType, byteLength, sha256, entry } (entry = "assets/<articleId>/<assetId>")
- Manifest `assets` block (deterministic JSON hash both sides)
- `assetsToWrite` on ResolvedImportPlan — conflicts.ts
- ImportPreviewDialog: dangling-asset warning entries with the two verbatim strings from UI-SPEC §Copywriting
- New spec: tests/unit/portability/bundle-v4.spec.ts

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v4 schema + manifest assets block + writer asset entries</name>
  <files>src/portability/bundle.ts, src/portability/manifest.ts, src/portability/ExportImportService.ts, tests/unit/portability/bundle-v4.spec.ts</files>
  <read_first>src/portability/bundle.ts (schemaVersion union L48-65, books optional widening precedent), src/portability/manifest.ts (computeManifest L37-57 determinism contract), src/portability/ExportImportService.ts (buildBundleBytes + zipSync L130-133, L197-204, validateBundle peek L241-247), src/persistence/assetsStore.ts (loadAllAssets — 20-03 output), 20-PATTERNS.md §portability, 20-RESEARCH Pattern 6 + Assumption A5</read_first>
  <action>bundle.ts: widen schemaVersion to the 1|2|3|4 union; add AssetExportMeta zod object (fields above, sha256 hex-64, entry matching ^assets/[^/]+/img-[a-z0-9]{12}$) and assets: z.array(AssetExportMeta).optional(). manifest.ts: add the assets block hashing JSON.stringify(bundle.assets ?? []) on BOTH compute sides (writer + verifier — keep determinism). ExportImportService writer: loadAllAssets() joins the export set; bundle.assets built from rows (Blob → bytes → sha256 via the existing sha256Hex helper OUTSIDE any transaction); zip entries assets/<articleId>/<assetId> carry the RAW Uint8Array bytes riding the SAME zipSync call as bundle.json/manifest.json (fflate accepts mixed Uint8Array + strToU8 values — verify in the spec's first cell: the Assumption A5 Wave-0 proof). validateBundle: peek threshold > 3 becomes > 4 (forward-refuse v5+). Filename stays lem-reader-bundle-v1.zip. Spec cells (new file): A5 proof (zipSync with one str + one Uint8Array entry round-trips byte-equal); v4 write carries assets meta + entries; manifest assets hash matches both sides; v1/v2/v3 fixtures (existing spec fixtures) still parse (union read); v5 peek refusal.
  <acceptance_criteria>
    - rg "z\\.literal\\(4\\)" src/portability/bundle.ts >= 1 and rg "AssetExportMeta" src/portability/bundle.ts >= 1
    - manifest.ts contains the assets block entry consistent with the other blocks' JSON-determinism shape
    - zip entries use the assets/ path prefix (rg "assets/" src/portability/ExportImportService.ts)
    - validateBundle peek compares against > 4
    - bundle-v4.spec.ts exits 0 including the mixed-value zipSync cell, union-read cells, and v5 refusal
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/bundle-v4.spec.ts</automated>
  </verify>
  <done>Writers emit v4 with asset entries; union read + forward-refusal preserved; A5 verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import gates — caps, sha256, no-broken-refs, conflict ride, apply</name>
  <files>src/portability/ExportImportService.ts, src/portability/conflicts.ts, tests/unit/portability/bundle-v4.spec.ts</files>
  <read_first>src/portability/ExportImportService.ts (validateBundle never-throw routing, unzip filter L197-204, applyImport puts-only closure L326-395, six-table readonly-array overload L374-383), src/portability/conflicts.ts (ConflictKind taxonomy L55-73, ResolvedImportPlan L147+), src/ingestion/types.ts (cap constants), 20-PATTERNS.md §conflicts + Shared Patterns, 20-RESEARCH Pattern 6 + Security Domain (zip-slip row)</read_first>
  <action>validateBundle/import path: per-asset validation AFTER unzip + union parse — entry present in the zip, isSafeEntryName on every key (existing guard), byteLength equals entry bytes length, sha256Hex(entry bytes) equals meta.sha256; per-asset byte cap (MAX_ASSET_BYTES) and total-assets budget (MAX_ARTICLE_ASSET_BYTES) with the existing MAX_ENTRY_ORIGINAL_SIZE filter-first bomb discipline unchanged. No-broken-refs gate: for every incoming article, every block src matching the asset-ref shape must resolve to an included asset entry; dangling → mark that article skipped with the explicit reason dangling-assets (surfaced in preview as the warning string; never a silent placeholder rewrite); orphan entries (no referencing article) drop inertly. conflicts.ts: assets ride article records — extend ResolvedImportPlan with assetsToWrite (validated rows for incoming-wins articles); NO ConflictKind additions; identical article (same id+revision+hash) keeps the calm no-op (its assets untouched); skip writes nothing. applyImport: assets join the puts-only closure via db.assets.put AFTER the per-article range-delete of superseded rows (upsert-replacement discipline from 20-03); the closure stays free of zod/crypto/network; the transaction now spans SEVEN tables — use the readonly-ARRAY overload on both branches (the STATE 12-07 lesson). Spec cells: bomb entry (patched central-directory originalSize — the 09-04 technique) → filtered/corrupted path, never-throw; sha256 mismatch → corrupted; dangling ref → article skipped with reason + others import; incoming-wins article → old asset rows replaced in-transaction (rollback unit: injected creating-hook failure rolls back everything); identical article → no-op leaves assets untouched; orphan entry dropped.
  <acceptance_criteria>
    - rg "assetsToWrite" src/portability/conflicts.ts >= 1 and applyImport consumes it (rg in ExportImportService.ts)
    - rg "\\.upgrade\\(" stays absent; applyImport closure contains only puts/deletes (source inspection)
    - bundle-v4.spec.ts cells green: bomb, sha256 mismatch, dangling skip + partial import, incoming-wins replacement rollback, identical no-op, orphan drop
    - ConflictKind union byte-unchanged (git diff on the union)
    - npx vitest run tests/unit/portability/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/portability/bundle-v4.spec.ts tests/unit/portability/validate-bundle.test.ts</automated>
  </verify>
  <done>Import gates enforce caps/integrity/no-broken-refs with honest skips; conflicts ride; apply stays atomic puts-only</done>
</task>

<task type="auto">
  <name>Task 3: Preview warnings + round-trip e2e cells</name>
  <files>src/reader/ImportPreviewDialog.tsx, tests/e2e/portability/round-trip.spec.ts, tests/e2e/portability/import-preview.spec.ts</files>
  <read_first>src/reader/ImportPreviewDialog.tsx (import-preview-warnings list + KIND_LABELS + single-onProceed Pitfall-8 anatomy + [data-initial-focus]), tests/e2e/portability/round-trip.spec.ts (two-context A/B surrogate + offset byte-equality pattern), tests/e2e/portability/import-preview.spec.ts, 20-UI-SPEC.md §Copywriting (verbatim strings) + §Component Inventory (ImportPreviewDialog EXTENDED)</read_first>
  <action>ImportPreviewDialog: the existing import-preview-warnings list gains the dangling-asset entries using the UI-SPEC verbatim copy — singular: "1 article will be skipped because its images aren't included in the bundle." and plural with honest count substitution: "{n} articles will be skipped because their images aren't included in the bundle." — count + plain-word noun + honest predicate matching the shipped register; anatomy/KIND_LABELS/overrides/onProceed/data-initial-focus byte-stable; NO new row/choice/ConflictKind. e2e round-trip extension: export from context A an article WITH assets → transfer zip → import into context B → assert the asset row exists in B's Dexie with equal byteLength + the figure renders the local img (naturalWidth > 0) — the 09-06 raw-row byte-equality precedent extended to assets; plus a dangling-ref bundle cell (crafted zip missing one asset entry) asserting the preview warning string appears verbatim and the OTHER articles import. Run across chromium + firefox + webkit.
  <acceptance_criteria>
    - ImportPreviewDialog contains both warning strings byte-exact (singular + plural template)
    - round-trip e2e asserts asset byte-equality across contexts + rendered local img
    - dangling-ref cell asserts the verbatim singular warning + partial import succeeds
    - npx playwright test tests/e2e/portability/ --project=chromium exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/portability/round-trip.spec.ts tests/e2e/portability/import-preview.spec.ts --project=chromium</automated>
  </verify>
  <done>Reader-facing import honesty shipped with verbatim copy; asset round-trip proven end-to-end in real browsers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle zip file → import pipeline | fully attacker-controlled archive crosses into local storage |
| exported bundle → filesystem | library bytes (incl. images) leave the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-18 | Tampering | bundle asset tampering in transit/at rest | mitigate | per-asset sha256 in the manifest-verified assets block; mismatch → corrupted refusal (never-throw) |
| T-20-19 | DoS | zip bomb via asset entries | mitigate | fflate filter-first originalSize caps + per-asset byte cap + total-assets budget |
| T-20-20 | Tampering | zip slip via crafted entry names | mitigate | entry names service-generated from validated ids; existing isSafeEntryName guard on every read |
| T-20-21 | Repudiation | silent image loss on import | mitigate | no-broken-refs gate skips with explicit preview warning — never silent placeholder-rewrite |
| T-20-22 | Tampering | partial apply (article without assets) | mitigate | one puts-only transaction; injected-failure rollback test |
</threat_model>

<verification>
- bundle-v4 + validate-bundle + existing portability unit specs green
- Portability e2e green on 3 engines; existing v1/v2/v3 bundle fixtures still import
- ImportPreviewDialog anatomy byte-stable beyond the two new warning entries
</verification>

<success_criteria>
- IMG-04 proven: validation, limits, conflicts, no broken references, deletion lifecycle documented by 20-03's cascades
- Fourth union-widening application lands with the 12-07/17-04 discipline intact
</success_criteria>

<output>
Create `.planning/phases/20-safe-local-image-fidelity/20-05-SUMMARY.md` when done
</output>
