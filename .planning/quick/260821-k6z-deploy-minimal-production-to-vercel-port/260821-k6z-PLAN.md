---
phase: quick-260821-k6z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/ingest.ts
  - tests/unit/server/vercel-ingest-endpoint.spec.ts
  - vercel.json
  - .gitignore
  - package.json
autonomous: true
requirements:
  - QUICK-260821-K6Z
must_haves:
  truths:
    - "A POST of a JSON body to /api/ingest on the Vercel deployment runs the full ingestion pipeline in a Node (non-edge) function and returns the typed IngestionResponse envelope with the identical 200/400 status mapping of the dev middleware and the Cloudflare shape"
    - "The Vercel function delegates to the UNCHANGED shared helper server/ingestAdapter.ts (D7-05) — zero logic fork"
    - "Cloudflare spike artifacts (functions/**, wrangler.toml) and server/**, src/**, index.html remain byte-untouched"
    - "npm run build and npm run lint pass with the new api/ file in place"
    - "A logged-in user can ship production with npm run deploy:vercel — the agent prepares everything but never deploys and never attempts interactive login"
  artifacts:
    - path: "api/ingest.ts"
      provides: "The Vercel Node function port of the D7-05 ingest adapter boundary"
      contains: "D7-05"
      min_lines: 20
    - path: "tests/unit/server/vercel-ingest-endpoint.spec.ts"
      provides: "Contract tests for the wrapper's request.json → handleIngestBody → Response.json glue"
      min_lines: 40
    - path: "vercel.json"
      provides: "Explicit buildCommand (runs the tsc gate) + outputDirectory dist; nothing auto-detection can't infer beyond these two"
    - path: "package.json"
      provides: "deploy:vercel script (user-run only)"
  key_links:
    - from: "api/ingest.ts"
      to: "server/ingestAdapter.ts"
      via: "import { handleIngestBody } — the same shared helper as functions/api/ingest.ts and dev-server/ingest-middleware.ts"
      pattern: "handleIngestBody"
    - from: "tests/unit/server/vercel-ingest-endpoint.spec.ts"
      to: "api/ingest.ts"
      via: "default-import the handler object and invoke .fetch(new Request(...)) — Node 22 global Request/Response"
      pattern: "\\.fetch\\(new Request"
    - from: "package.json"
      to: "Vercel platform"
      via: "deploy:vercel script → npx --yes vercel@latest deploy --prod --yes (user-run, post-login)"
      pattern: "deploy:vercel"
---

<objective>
Port the /api/ingest adapter to a Vercel Node function and prepare (but do not execute) a minimal production deploy of Lem Reader.

Purpose: The production host is Vercel per locked decision 1 — workerd cannot run the extraction pipeline (jsdom hard-crashes `ReferenceError: MessagePort is not defined`, per the 07-01 spike verdict, HYBRID CONTINGENCY, human-approved 2026-08-11). Vercel Node functions run jsdom + isomorphic-dompurify + Readability natively. The D7-05 adapter boundary was designed for exactly this port: "Porting to Vercel/Netlify changes ONLY this file." Ingestion MUST work in production (locked decision 2 — the deploy is not complete without /api/ingest). The Cloudflare artifacts (functions/**, wrangler.toml) stay untouched in-repo as documented spike/adapter evidence (locked decision 3). The executor prepares everything and hands the deploy itself back to the user — no interactive login, no deploy execution (locked decision 4).

Output: `api/ingest.ts` (the Vercel Node function), a unit contract spec for its I/O glue, `vercel.json` (two keys), `.gitignore` entry for `.vercel/`, a `deploy:vercel` npm script, and a verification record proving the build/lint/test gates and the untouched-files boundary.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@functions/api/ingest.ts
@server/ingestAdapter.ts
@dev-server/ingest-middleware.ts
@tests/unit/server/ingest-adapter.spec.ts
@vite.config.ts

# Platform facts (verified against current Vercel docs, 2026-08-21)
# - A file at repo-root `api/ingest.ts` auto-serves at /api/ingest. Zero routing config.
# - The DEFAULT runtime for api/ files is Node.js (jsdom requirement — do NOT opt into edge).
# - Current docs' recommended handler form is the module object with a web-standard
#   fetch signature: `export default { fetch(request: Request) { return new Response(...) } }`.
#   Zero npm dependencies needed — no @vercel/node devDep (that package is only for
#   VercelRequest/VercelResponse typings, which the web-standard form avoids).
# - Vercel builds api/ functions with its own esbuild-based bundler at deploy time;
#   jsdom + isomorphic-dompurify + Readability bundle fine (250MB uncompressed limit;
#   this stack is far under). Full Node.js API coverage (safeFetch's node:dns + ip-address work natively).
# - Platform request-body cap: 4.5MB → 413 FUNCTION_PAYLOAD_TOO_LARGE BEFORE our code runs.
#   This binds tighter than our own MAX_INGEST_BODY_BYTES (~13.3MB) — documented residual below.
# - Hobby plan duration 300s default+max (Fluid compute default for new projects); our pipeline
#   self-caps at ~30s fetch + ~30s extraction timeout. No maxDuration config needed.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Vercel Node function api/ingest.ts with its contract spec</name>
  <files>api/ingest.ts, tests/unit/server/vercel-ingest-endpoint.spec.ts</files>
  <behavior>
    - Test 1: handler.fetch called with a Request whose body is malformed JSON (e.g. "{not json") resolves to status 400 with body { ok: false, reason: "server-error" } and the mocked ingest orchestrator is NOT called
    - Test 2: with the mocked ingest resolving an ok:true envelope, a Request carrying { url: "https://example.com/article" } resolves to status 200, the response body is exactly the mocked envelope, and ingest was called once with the parsed body object
    - Test 3: with the mocked ingest resolving { ok: false, reason: "ssrf-blocked-metadata" }, the response is status 400 with that exact typed body
    - Test 4: the response content-type header is application/json (Response.json sets it)
  </behavior>
  <action>
    Create `api/ingest.ts` (new repo-root `api/` directory — Vercel's convention auto-serves it at /api/ingest on the DEFAULT Node.js runtime, which jsdom requires; never edge). This file is the D7-05 port locked decision 1 was built on: it mirrors `functions/api/ingest.ts` exactly, changing ONLY the I/O shape.

    Structure (write tests first per tdd, then implement):
    1. Header comment block (repo convention of dense decision-trace comments — see `functions/api/ingest.ts`): document that this is the D7-05 adapter-boundary port to Vercel per the 07-01 spike verdict (HYBRID CONTINGENCY, human-approved 2026-08-11 — jsdom hard-crashes on workerd with `MessageError: MessagePort is not defined`, so the host moved to Vercel Node functions where jsdom + isomorphic-dompurify + Readability run natively); that `functions/api/ingest.ts` + `wrangler.toml` remain in-repo as the documented spike/adapter artifacts (locked decision 3); that all three wrappers (Cloudflare Pages Function, Vite dev middleware, this file) share `server/ingestAdapter.ts` so behavior is byte-identical; and the known platform residual: Vercel enforces a 4.5MB request-body cap (413 FUNCTION_PAYLOAD_TOO_LARGE) that binds before our MAX_INGEST_BODY_BYTES — binary ingests over ~3.4MB decoded surface the platform refusal, which IngestionClient's catch-all maps to calm server-error copy; the blob-upload bypass (Vercel KB "bypass body size limit") is out of scope for this minimal deploy.
    2. Exports: the module-object web-standard form from current Vercel docs — a default export object whose `fetch` property is an async function taking `request: Request` and returning `Promise<Response>`. Zero npm dependencies; no @vercel/node import.
    3. Body handling, byte-identical to `functions/api/ingest.ts`: parse with `await request.json()` inside try/catch; on throw return `Response.json({ ok: false, reason: "server-error" } satisfies IngestionResponse, { status: 400 })`.
    4. Delegate: `const { status, body: responseBody } = await handleIngestBody(body);` then `return Response.json(responseBody, { status });`.
    5. Imports: `handleIngestBody` from `../server/ingestAdapter`, and type-only `IngestionResponse` from `../src/ingestion/types` (verbatimModuleSyntax — use `import type`).

    Create `tests/unit/server/vercel-ingest-endpoint.spec.ts` mirroring the established adapter-spec convention (`tests/unit/server/ingest-adapter.spec.ts`): `vi.mock("../../../server/ingest", ...)` so the wrapper test runs in isolation from the real pipeline (extraction behavior is already covered by extraction.spec.ts / mxss.spec.ts). Default-import the handler object from `../../../api/ingest` and drive it with `new Request("https://lem-reader.example.com/api/ingest", { method: "POST", headers: { "content-type": "application/json" }, body: ... })` — Node 22 provides global Request/Response. Assert against `await handler.fetch(req)` using `res.status`, `await res.json()`, and `res.headers.get("content-type")`. Reuse the `okArticle()` envelope shape from the adapter spec for the ok:true case.

    Placement rationale (do NOT change tsconfig): `api/` deliberately sits OUTSIDE tsconfig.json's include ["src","tests"], mirroring the `functions/` precedent — platform adapters are thin I/O glue. The spec's import transitively type-checks api/ingest.ts under the root tsc program (DOM lib provides Request/Response types), so `npm run build` covers the new file with zero config drift.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/server/vercel-ingest-endpoint.spec.ts</automated>
  </verify>
  <done>
    Spec passes (4 contract cases green); api/ingest.ts default-exports the web-standard fetch object, imports handleIngestBody from ../server/ingestAdapter, contains no Vercel npm dependency, and its header documents D7-05 + the 07-01 spike rationale + the 4.5MB platform residual.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add minimal Vercel config, gitignore the CLI state, add the user-run deploy script</name>
  <files>vercel.json, .gitignore, package.json</files>
  <action>
    1. Create `vercel.json` with exactly two keys (least config — auto-detection infers everything else): `"buildCommand": "npm run build"` (runs the repo's tsc gate in production builds — same verification as local, immune to Vite-preset drift) and `"outputDirectory": "dist"` (matches wrangler.toml's pages_build_output_dir and Vite's static output). NO functions config (Node runtime is default), NO rewrites (hash-based routing needs no SPA fallback), NO headers. vercel.json cannot carry comments — the plan and SUMMARY are the documentation home for the two-key rationale.
    2. Append `.vercel/` to `.gitignore` (with the existing comment style) — the Vercel CLI writes project-link state there on first deploy; it must never be committed.
    3. In package.json scripts, add `"deploy:vercel": "npx --yes vercel@latest deploy --prod --yes"` — zero new devDependencies; downloads the official Vercel CLI on demand; `--yes` makes the deploy non-interactive once the user is logged in and the project is linked. The executor MUST NOT run this script or any `vercel` / `npx vercel` command (locked decision 4 — login is an interactive browser flow only the user performs).
  </action>
  <verify>
    <automated>node -e "const v=require('./vercel.json'); if(Object.keys(v).length!==2||v.buildCommand!=='npm run build'||v.outputDirectory!=='dist')process.exit(1)" && node -e "const p=require('./package.json'); if(!p.scripts['deploy:vercel'])process.exit(1)" && grep -q '^\.vercel/$' .gitignore && echo CONFIG-OK</automated>
  </verify>
  <done>
    vercel.json parses with exactly buildCommand npm run build + outputDirectory dist; package.json carries deploy:vercel; .gitignore ignores .vercel/; no other package.json changes (deps untouched — zero new packages).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full gate sweep, untouched-files proof, and deploy handoff record</name>
  <files>.planning/quick/260821-k6z-deploy-minimal-production-to-vercel-port/260821-k6z-SUMMARY.md</files>
  <action>
    Run the repo's honest full gates in order and record real output (no subsets, no skips):
    1. `npm run build` — tsc && vite build must pass; confirm dist/index.html exists.
    2. `npm run lint` and `npm run lint:no-danger` (AGENTS.md gates).
    3. `npm run test:unit -- --run` — the full unit suite including the new wrapper spec.
    4. Untouched-files proof (locked decision 3 + constraints): `git status --porcelain` must list ONLY the five intended files (api/ingest.ts, tests/unit/server/vercel-ingest-endpoint.spec.ts, vercel.json, .gitignore, package.json) plus this task's SUMMARY — functions/**, wrangler.toml, server/**, src/**, index.html all byte-untouched.
    5. Write the SUMMARY with the deploy handoff (the user runs, in order): `npx vercel login` (interactive browser flow), then `npm run deploy:vercel` (non-interactive after login; first run links the project with defaults), then verify production at the returned URL: paste an article via the UI ingest, and `curl -X POST <deployed-url>/api/ingest -H 'content-type: application/json' -d '{bad json'` returns 400 {"ok":false,"reason":"server-error"}. Include the known-limits note: bodies over 4.5MB (binary ingests over ~3.4MB decoded) hit Vercel's platform 413 before our code runs and surface as calm server-error copy in the client; blob-upload bypass is the documented future path.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run lint:no-danger && npm run test:unit -- --run && [ -f dist/index.html ] && echo GATES-GREEN</automated>
  </verify>
  <done>
    All gates exit 0 with honest recorded output; git status shows exactly the six intended files; SUMMARY contains the user-run deploy steps, the production curl check, and the 4.5MB known-limits note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /api/ingest function | Untrusted JSON bodies cross here (same boundary as dev middleware + Cloudflare shape — the port adds NO new input surface) |
| Function → reader-supplied URL | The {url} path fetches untrusted hosts (inherited safeFetch boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1-01 | Information Disclosure (SSRF via {url}) | api/ingest.ts → server/safeFetch.ts | mitigate (inherited) | Unchanged safeFetch pipeline: node:dns.promises pinning + ip-address private-range deny-list + metadata-hostname blocklist all run natively on Vercel Node functions (same code path as the Vite dev middleware) |
| T-Q1-02 | DoS (oversized bodies) | api/ingest.ts entry | accept (documented residual) | Vercel's 4.5MB platform cap (413 FUNCTION_PAYLOAD_TOO_LARGE) binds BEFORE our MAX_INGEST_BODY_BYTES; IngestionClient's catch-all surfaces calm server-error copy; blob-upload bypass (Vercel KB) explicitly out of scope for this minimal deploy |
| T-Q1-03 | Tampering (malformed IngestionResponse) | src/ingestion/IngestionClient.ts | mitigate (inherited) | Existing STATE-04 defense-in-depth: ArticleSchema.parse re-validation on the client read path (unchanged) |
| T-Q1-04 | Repudiation (unlogged refusals) | server/ingest.ts | accept (inherited) | Every throw already wraps to a typed IngestionResponse (T-7-23 owner, unchanged); no new logging surface introduced by the port |
| T-Q1-SC | Supply chain (npm) | package.json / deploy script | mitigate | Executor installs NOTHING — zero new dependencies (web-standard handler form avoids @vercel/node). The only new tool is the official Vercel CLI via `npx --yes vercel@latest`, invoked solely by the logged-in user inside deploy:vercel; the agent never executes it |
</threat_model>

<verification>
- `npx vitest run tests/unit/server/vercel-ingest-endpoint.spec.ts` — wrapper contract (parse-fail 400, delegate 200/400, content-type)
- `npm run build && npm run lint && npm run lint:no-danger` — repo gates with the new api/ file in the tsc program (transitive via the spec import)
- `npm run test:unit -- --run` — full honest unit suite
- `git status --porcelain` — exactly: api/ingest.ts, tests/unit/server/vercel-ingest-endpoint.spec.ts, vercel.json, .gitignore, package.json, SUMMARY
- Post-deploy (USER-run, not agent): deployed URL loads the SPA; UI paste-ingest produces an article; malformed-JSON curl returns 400 {"ok":false,"reason":"server-error"}
</verification>

<success_criteria>
- /api/ingest contract on Vercel is byte-identical to dev + Cloudflare shapes via the UNCHANGED server/ingestAdapter.ts helper (D7-05; locked decisions 1+2)
- functions/**, wrangler.toml, server/**, src/**, index.html untouched (locked decision 3; proven by git status)
- All repo gates green with zero new npm dependencies in the executor's changes
- Deploy prepared and documented for the user (locked decision 4): login + `npm run deploy:vercel` + production verification steps in the SUMMARY; agent never attempted either
</success_criteria>

<output>
Create `.planning/quick/260821-k6z-deploy-minimal-production-to-vercel-port/260821-k6z-SUMMARY.md` when done
</output>
