# Existing browser suite blockers

The reduced-motion suite has four library/review destination cases per browser that expect “The looting of science fiction” in the library after a wipe. All fail before motion assertions because that fixture is absent from the library. Reader routes and all changed-surface checks pass. Investigate fixture setup separately; no unrelated library changes included.
