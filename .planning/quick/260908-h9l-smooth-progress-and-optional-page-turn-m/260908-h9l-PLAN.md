---
status: planned
---
# Smooth progress and optional page-turn motion

1. Ease the progress transform over 200ms under no-preference motion only.
2. Add a locally persisted, default-off Animate page turns checkbox. Fade the single semantic page fragment over 180ms only after explicit navigation. Cancel on further navigation, settings changes, reduced motion, repagination, and unmount; never delay focus or change layout geometry.
3. Verify settings compatibility, browser navigation, reduced motion, persistence, and build/lint. Record results and commit.

User request supersedes historical no-animation design decisions for these two surfaces. Keep pagination and measurement unchanged. Execute inline using the GSD quick skill's runtime fallback.
