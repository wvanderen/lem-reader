---
phase: quick-260821-ov7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/ingest.ts
  - tests/unit/server/ingest-plaintext-paste.spec.ts
  - src/ingestion/IngestControl.tsx
autonomous: true
requirements:
  - QUICK-260821-OV7
must_haves:
  truths:
    - "Pasting rendered article text (no HTML tags) into the paste box and submitting adds the article to the library and opens it in the reader — no more extraction-unsupported refusal"
    - "Pasting actual HTML (anything containing a tag opener) still goes through the byte-stable Readability path — paste- ids, same refusals, existing suites green"
    - "Thin plain-text pastes (whitespace-only, or under the markdown path's 3-block readerability bar) still get the honest Couldn't reliably read this page. refusal"
    - "The same plain text pasted as {html} and uploaded as {markdown} produces the SAME md-<content-hash> id (one article, dedupe-refuse on repeat)"
    - "The .md upload flow, the Vercel endpoint contract, and the full unit suite stay green; the executor never deploys (the orchestrator deploys and live-verifies after)"
  artifacts:
    - path: "server/ingest.ts"
      provides: "Exported looksLikePlainText predicate + Stage 0.5 plain-text paste reroute ({html: tagless} → {markdown: tagless} before Stage-1 dispatch)"
      contains: "looksLikePlainText"
    - path: "tests/unit/server/ingest-plaintext-paste.spec.ts"
      provides: "Predicate unit table + real-orchestrator integration cases for the reroute (happy path, id equivalence, honest refusals, conservative boundary)"
      min_lines: 80
    - path: "src/ingestion/IngestControl.tsx"
      provides: "Honest paste-box label ('Paste HTML or text') matching the widened server admission"
      contains: "Paste HTML or text"
  key_links:
    - from: "server/ingest.ts (html branch dispatch)"
      to: "server/markdownToBlocks.ts (markdown branch)"
      via: "Stage 0.5 rewrites the html variant onto the markdown variant when looksLikePlainText is true — the reroute lands in the EXISTING markdown branch (id, source, origin, D8-17 title chain all reused, zero new machinery)"
      pattern: "looksLikePlainText"
    - from: "tests/unit/server/ingest-plaintext-paste.spec.ts"
      to: "server/ingest.ts"
      via: "drives the REAL ingest() orchestrator with no mocks (the ingest-pdf.spec.ts convention)"
      pattern: "await ingest\\(\\{ html"
    - from: "src/ingestion/IngestControl.tsx (paste textarea label)"
      to: "the widened server admission"
      via: "label copy change only — the accessible name keeps matching the unanchored /paste html/i queries in tests/component/IngestControl.test.tsx and tests/e2e/ingestion/happy-path.spec.ts"
      pattern: "Paste HTML or text"
---

<objective>
Make plain-text pastes (rendered article text, no HTML tags) succeed instead of being refused with `extraction-unsupported` — by routing tag-less `{html}` requests through the EXISTING `markdownToBlocks` intake, server-side, in one place (locked decision 1 + 2).

Purpose: Prod-confirmed root cause (`.planning/todos/pending/2026-08-21-fix-prod-ui-paste-ingest-flow.md`): the paste textarea receives rendered plain text (a `<textarea>` yields text, and copying an article from a browser page yields rendered text, not HTML source). The html branch's `isProbablyReaderable` pre-check refuses tag-less content → `server/ingest.ts` returns `{ok:false, reason:"extraction-unsupported"}` → the UI shows the calm "Couldn't reliably read this page." refusal. Plain text IS valid strict CommonMark, and the markdown branch already returns the byte-identical downstream shape (`{blocks, footnotes, lang, provenancePartial, isReaderable}`), so a server-side reroute inside `ingest()` fixes ALL clients at once (dev middleware, Vercel prod `api/ingest`, future Cloudflare shape) with the D7-05 adapters + UI flow untouched.

Output: `server/ingest.ts` gains an exported `looksLikePlainText` predicate + a Stage 0.5 reroute; a new integration spec `tests/unit/server/ingest-plaintext-paste.spec.ts` proves the reroute, the unchanged HTML path, and the honest thin-content refusals; `src/ingestion/IngestControl.tsx` gets the one-string honest label tweak.

Locked decisions honored: D1 accept plain text as first-class via the existing markdown intake; D2 server-side reroute (single place, fixes all clients — label tweak allowed in src/); D3 title follows the EXISTING markdown-path convention (no filename → existing "Markdown document" fallback — NO new title machinery); D4 scope guards (functions/**, wrangler.toml, server/markdownToBlocks.ts internals, pdf/epub paths untouched; D8-16 reasoning cited in new comments); D5 verification = new unit spec cases + existing contract spec + full suite green, NO deploy in the executor.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-21-fix-prod-ui-paste-ingest-flow.md
@server/ingest.ts
@server/markdownToBlocks.ts
@src/ingestion/IngestControl.tsx
@src/ingestion/IngestionClient.ts
@src/ingestion/types.ts
@tests/unit/server/normalization.spec.ts
@tests/unit/server/vercel-ingest-endpoint.spec.ts
@tests/component/IngestControl.test.tsx

# Verified facts (planner, 2026-08-21)
# - The two extraction-unsupported refusals in server/ingest.ts are the shared
#   `if (!isReaderable || blocks.length === 0)` gate (just before Stage 6a) and
#   the `deriveConfidence` "unsupported" return (Stage 7). Both stay byte-unchanged —
#   the reroute only changes WHICH Stage-1 branch produces the inputs.
# - markdownToBlocks returns isReaderable = blocks.length >= 3 (its own heuristic);
#   blank-line-separated plain paragraphs → paragraph blocks, lang "en", footnotes [].
# - The markdown branch identity: id = md-<shortHash(content)> (D8-18 content-hash,
#   NOT filename), source "markdown", origin "upload", title chain D8-17:
#   front-matter → stripMarkdownExtension(filename) → "Markdown document".
#   ArticleSourceSchema already contains "markdown"; origin enum contains "upload"
#   (src/content/schema.ts) — the rerouted article is schema-valid with zero schema changes.
# - Stage 0's exactly-one-of check is preserved by the reroute BY CONSTRUCTION:
#   {html:x} → {markdown:x} maps one variant onto another 1:1.
# - tests/component/IngestControl.test.tsx (L96, L155) and
#   tests/e2e/ingestion/happy-path.spec.ts (L101) query the paste textarea with the
#   UNANCHORED regex /paste html/i — verified it still matches "Paste HTML or text",
#   so the label change needs ZERO test edits.
# - Existing html-path regression proofs already in the suite (cite, don't duplicate):
#   normalization.spec.ts — full v1.0 fixture HTML via ingest({html}) → ok:true;
#   ingest({html:"<p>short</p>"}) → refused (thin content). Both inputs contain tag
#   openers → not rerouted → staying green IS the "HTML paste unchanged" proof.
# - vitest workspace: `unit` + `server` projects; `npx vitest run <file>` works with
#   explicit paths regardless of project.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — plain-text paste reroute spec</name>
  <files>tests/unit/server/ingest-plaintext-paste.spec.ts</files>
  <behavior>
    - Predicate table (imported looksLikePlainText from server/ingest.ts):
      1. "First paragraph.\n\nSecond paragraph.\n\nThird paragraph." → true
      2. "   \n\t " (whitespace-only) → true (reroutes; refused downstream for zero blocks — pinned by integration case 3)
      3. "count: 5 < 6 and 6 > 5" (angle brackets, no tag opener) → true
      4. "<b>hi</b>" → false
      5. "<p>para one</p><p>para two</p>" → false
      6. "<!DOCTYPE html><html><body></body></html>" → false
      7. "see the <!-- note --> here" → false
      8. "you can use <br> in HTML" → false (conservative boundary: prose MENTIONING a tag stays on the html path)
    - Integration through the REAL ingest() (no mocks — the ingest-pdf.spec.ts convention):
      1. tag-less multi-paragraph paste (3–4 realistic multi-sentence paragraphs, inline string literal) → ok:true, article variant (narrow via `"article" in response` — two ok-variants since Phase 12); blocks.length >= 3; EVERY block.kind === "paragraph"; id matches /^md-[0-9a-f]{12}$/; ingestionMeta.source === "markdown"; ingestionMeta.origin === "upload"; provenance.title === "Markdown document" (the EXISTING D8-17 no-filename fallback — locked decision 3); provenance.sourceUrl undefined (D7-08 — "open original" hidden); confidence.state within ["confident","low"]
      2. id equivalence / D8-18 content-hash identity: (await ingest({html: TEXT})).article.id === (await ingest({markdown: TEXT})).article.id for the same TEXT — proves the reroute lands in the markdown branch
      3. ingest({html: "   \n  "}) → {ok:false, reason:"extraction-unsupported"} (zero blocks → the shared honest-refusal gate)
      4. ingest({html: "<b>hi</b>"}) → {ok:false, reason:"extraction-unsupported"} (single tag, no article structure — stays html path, honest refusal per locked decision 5)
      5. plain text containing a non-tag angle bracket ("count: 5 < 6\n\nSecond paragraph.\n\nThird paragraph.") → ok:true with paragraph blocks (CommonMark treats `< 6` as literal text; D8-16 escapes any residual html-ish node to inert text)
  </behavior>
  <action>
    Create tests/unit/server/ingest-plaintext-paste.spec.ts. Header comment cites quick task 260821-ov7, the prod-confirmed root cause (rendered-text paste → isProbablyReaderable refuses → extraction-unsupported), and states the suite's scope: the reroute predicate + the real-orchestrator reroute behavior; html-path-unchanged proofs live in normalization.spec.ts (cited, not duplicated). Import { ingest, looksLikePlainText } from "../../../server/ingest" and drive the real pipeline with zero vi.mock (mirrors tests/unit/server/ingest-pdf.spec.ts). Use describe blocks: "looksLikePlainText (260821-ov7 reroute predicate)" and "ingest — plain-text paste reroute (260821-ov7)". Narrow the Phase 12 two-variant envelope with the `"article" in response` guard before reading .article (the ingest-pdf.spec.ts L62-65 pattern). Assert refusal outcomes with toEqual({ ok: false, reason: "extraction-unsupported" }).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/server/ingest-plaintext-paste.spec.ts; echo "exit=$?"</automated>
  </verify>
  <done>Spec file exists with all 13 cases (8 predicate + 5 integration). The run exits NON-ZERO (RED proof — the looksLikePlainText import is missing and/or integration cases fail against the current refusing behavior). Record the RED output honestly in the task summary.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Stage 0.5 plain-text reroute in the orchestrator</name>
  <files>server/ingest.ts</files>
  <action>
    Three edits, all inside server/ingest.ts — nothing else changes:

    1. Exported predicate near the shortHash/safeHostname helpers: a module-level regex constant matching an HTML tag opener (a `<` immediately followed by an ASCII letter for opening tags, a slash for closing tags, or `!` for comments/DOCTYPE — any HTML markup necessarily contains at least one of these) and `export function looksLikePlainText(content: string): boolean` returning true when the TRIMMED content contains no such opener. Doc comment cites 260821-ov7 + the prod evidence, states the conservative direction (when in doubt — e.g. prose mentioning a tag like the <br> case — the content KEEPS the byte-stable html path), and cites the D8-16 / Pitfall 8-2 security-boundary reasoning from server/markdownToBlocks.ts's header for WHY untrusted text through the markdown intake is safe: strict CommonMark escapes raw HTML to inert paragraph text, the Block tree is pure inert JSON, React escapes text children on render, and the repo-wide react/no-danger rule is the belt-and-suspenders structural defense. Do NOT modify server/markdownToBlocks.ts itself (locked decision 4) — cite its reasoning, don't restate it as new code.

    2. Stage 0.5 reroute in ingest(), inserted AFTER the exactly-one-of validation block and BEFORE the five has* flag computations: build `const request: IngestionRequest = "html" in input && looksLikePlainText(input.html) ? { markdown: input.html } : input;` (the `"html" in input` narrowing makes input.html type-safe with no cast). Comment cites 260821-ov7, explains that the textarea yields rendered text which Readability cannot extract, that plain text is valid strict CommonMark, that this single place fixes every client (dev middleware, Vercel api function, future Cloudflare shape — they all funnel through ingest() per D7-05), that the reroute preserves the exactly-one-of invariant by construction ({html:x} → {markdown:x}), and that downstream (ArticleSchema.parse, assertRoundTripAnchor, deriveConfidence, persistence, dedupe) is shared and unchanged.

    3. Mechanical rename so dispatch reads the rerouted request: change the five has* flag computations from `input` to `request` ("url" in request, etc.), and every Stage-1 branch-body read of `input` — input.url in the url branch, the four (input as {...}) casts in the html/pdf/markdown branches, and the ingestEpubBook(input as {epub...}) call — to `request`. The hasHtml branch can now only fire for TAGGED content (the reroute rewrote tag-less html onto the markdown variant). Branch bodies, ingestEpubBook, the shared refusal gates, the title chain, and stages 6–7 stay otherwise byte-stable (locked decision 4: pdf/epub/url/markdown branch LOGIC untouched — identifier rename only). Add one line to the file-header contract list noting the 260821-ov7 Stage-0 plain-text reroute.

    Comments follow the repo decision-trace convention (cite 260821-ov7 and the decision IDs D8-16/D8-18/D8-17 where relevant).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/server/ingest-plaintext-paste.spec.ts tests/unit/server/normalization.spec.ts tests/unit/server/markdown-to-blocks.spec.ts tests/unit/server/vercel-ingest-endpoint.spec.ts</automated>
  </verify>
  <done>The new spec is fully GREEN (Task 1's RED cases now pass). normalization.spec.ts stays green — its two html-path proofs (full v1.0 fixture HTML → ok:true with the html pipeline; "<p>short</p>" → thin-content refusal) are the "HTML paste unchanged" regression evidence. markdown-to-blocks.spec.ts (untouched adapter) and vercel-ingest-endpoint.spec.ts (D7-05 contract spec per locked decision 5) stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Honest paste-box label + full executor gates</name>
  <files>src/ingestion/IngestControl.tsx</files>
  <action>
    One-string change in src/ingestion/IngestControl.tsx: the paste textarea's label text "Paste HTML" → "Paste HTML or text" (the htmlFor-linked accessible name). Add a brief comment citing 260821-ov7: the server now admits tag-less pastes through the markdown intake, so the label stops under-promising — this is honesty copy only, the fix itself is server-side. Do NOT change the placeholder, the button copy ("Add pasted article" stays accurate), or any test: the component tests (tests/component/IngestControl.test.tsx L96/L155) and the e2e happy-path spec query the textarea with the UNANCHORED /paste html/i regex which still matches the new label (verified by the planner — leave the specs byte-untouched).

    Then run the full executor gate set (locked decision 5 — NO deploy, NO e2e, NO vercel commands; the orchestrator deploys and live-verifies afterward): npm run build (build:api + tsc + vite build), eslint on the three touched files, the IngestControl component spec, and the full unit suite via npm run test:unit -- --run (both vitest projects; e2e excluded — the orchestrator's live verification covers the prod flow).
  </action>
  <verify>
    <automated>npx vitest run tests/component/IngestControl.test.tsx && npm run build && npx eslint server/ingest.ts src/ingestion/IngestControl.tsx tests/unit/server/ingest-plaintext-paste.spec.ts && npm run test:unit -- --run</automated>
  </verify>
  <done>Component spec green (label regex still matches — no test edits were needed), npm run build green (tsc type-checks the request rewrite), eslint clean on all three touched files, full unit suite exits 0. Scope boundary holds: functions/**, wrangler.toml, server/markdownToBlocks.ts, server/pdfToBlocks.ts, server/epubToBooks.ts, and all D7-05 adapters byte-untouched (verify via git status showing only the three files_modified paths plus the new spec).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/ingest | Untrusted pasted text crosses here (body capped by the existing middleware content-length guard + IngestionRequestSchema at the adapter boundary — unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-OV7-01 | Tampering / Elevation | Stage 0.5 reroute → markdownToBlocks | mitigate | The D8-16 boundary already holds: strict CommonMark escapes raw HTML to inert paragraph text, the Block tree is inert JSON, React escapes text children — cite this in the new comment. The predicate only reroutes content with NO tag opener, so real HTML never enters the markdown path via this route; and even if it did, D8-16 holds. |
| T-OV7-02 | DoS | tag-less paste reaching remark-parse | mitigate | No new amplification: markdown uploads already run text of the same size through the same parser, and the existing three-layer body caps (client, middleware content-length, schema) bound the input identically. |
| T-OV7-03 | Spoofing (mislabel) | rerouted article carries source "markdown" / origin "upload" | accept | Both are existing valid ArticleSourceSchema/origin enum members (Phase 8); the D8-18 content-hash id means identical text pasted or uploaded as .md dedupes to ONE article — the intended save-once semantics, not a mislabel that affects reader behavior. |
</threat_model>

<verification>
- RED→GREEN discipline: Task 1's spec run recorded failing BEFORE the Task 2 implementation; Task 2 turns it fully green (honest gate — no skipped cases).
- Locked decision 5 coverage: tag-less multi-paragraph paste → ok:true with paragraph blocks (integration case 1); HTML paste unchanged (predicate negatives + the cited normalization.spec.ts proofs + full suite); edge cases whitespace-only + single-tag "<b>hi</b>" → honest extraction-unsupported refusals (integration cases 3–4); tests/unit/server/vercel-ingest-endpoint.spec.ts green; full unit suite green; NO deploy executed.
- Locked decision 4 scope guards: git status shows ONLY server/ingest.ts, tests/unit/server/ingest-plaintext-paste.spec.ts, src/ingestion/IngestControl.tsx changed.
- Live prod verification (paste rendered text → article opens) is the ORCHESTRATOR's post-deploy step, not the executor's.
</verification>

<success_criteria>
- The planner-level multi-source audit is clean: GOAL (fix the paste flow for plain text) covered by Tasks 2+3; evidence artifact (the pending todo's confirmed root cause) resolved and citable for closure; locked decisions 1–5 each implemented (D1/D2 Task 2, D3 Task 2 via the existing markdown title chain, D4 scope guards in all tasks, D5 verification set across Tasks 1–3); no deferred items from the todo's "Solution TBD" list (the UX-affordance direction is superseded by the accept-as-first-class decision; the size-cap suspect stays documented as the existing Vercel residual — out of scope here).
- A user pasting rendered article text into the prod paste box gets a working article; a user pasting HTML source gets today's behavior exactly.
</success_criteria>

<output>
Create `.planning/quick/260821-ov7-fix-paste-flow-accept-plain-text-pastes-/260821-ov7-SUMMARY.md` when done
</output>
