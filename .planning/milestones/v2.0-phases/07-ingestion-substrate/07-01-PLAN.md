---
phase: 07-ingestion-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - wrangler.toml
  - vite.config.ts
  - vitest.config.ts
  - playwright.config.ts
  - tests/unit/server/spike-jsdom-workers.spec.ts
  - tests/unit/server/mxss.spec.ts
  - tests/unit/server/extraction.spec.ts
  - tests/unit/server/normalization.spec.ts
  - tests/unit/server/confidence.spec.ts
  - tests/unit/server/slugify.spec.ts
  - tests/e2e/ingestion/ssrf-matrix.spec.ts
  - tests/e2e/ingestion/happy-path.spec.ts
  - tests/e2e/ingestion/dexie-migration.spec.ts
autonomous: false
requirements: [ING-07, ING-08]
user_setup: []
decisions_implemented: [D7-05, D7-06, D7-09, D7-10]

must_haves:
  truths:
    - "The repo installs five new server-only deps and two new devDeps with exact pins, and `npm install` exits 0"
    - "A wrangler.toml exists at the repo root declaring `compatibility_flags = [\"nodejs_compat\"]` and a current compat_date"
    - "Vitest can run a `server` project that imports `/server` code under the jsdom env"
    - "Playwright config boots `wrangler pages dev` alongside `vite dev` for ingestion e2e tests"
    - "All eight Wave-0 test files exist and run with `test.todo` placeholders so later plans replace them with real bodies"
    - "The jsdom-on-Workers spike records a PASS/FAIL outcome for jsdom + isomorphic-dompurify + @mozilla/readability end-to-end against a real workerd runtime (workerd-from-wrangler or @cloudflare/vite-plugin), and the outcome is recorded in the SUMMARY"
    - "The two `[ASSUMED]` items from RESEARCH.md (cf.resolveOverride DNS pinning; @cloudflare/vite-plugin preserving the v1.0 Playwright webServer on :5173) are explicitly verified or refuted by the spike and the result is recorded"
  artifacts:
    - path: "wrangler.toml"
      provides: "Cloudflare Pages Functions config with nodejs_compat flag + current compat_date"
      contains: "nodejs_compat"
    - path: "vite.config.ts"
      provides: "Either @cloudflare/vite-plugin in plugins OR server.proxy to wrangler — spike-decided"
      contains: "cloudflare"
    - path: "vitest.config.ts"
      provides: "Vitest workspace with a server project for /server imports"
      contains: "projects"
    - path: "playwright.config.ts"
      provides: "webServer array booting vite dev + wrangler pages dev"
      contains: "wrangler"
    - path: "tests/unit/server/spike-jsdom-workers.spec.ts"
      provides: "Spike test that constructs JSDOM, runs Readability.parse, runs DOMPurify.sanitize on Workers runtime"
      min_lines: 40
    - path: "tests/unit/server/mxss.spec.ts"
      provides: "Wave-0 stub with test.todo placeholders for the mXSS regression corpus"
      contains: "test.todo"
    - path: "tests/e2e/ingestion/ssrf-matrix.spec.ts"
      provides: "Wave-0 stub for SSRF matrix (replaced by 07-07)"
      contains: "test.todo"
    - path: "tests/e2e/ingestion/dexie-migration.spec.ts"
      provides: "Wave-0 stub for v1→v3 Dexie migration (replaced by 07-07)"
      contains: "test.todo"
  key_links:
    - from: "tests/unit/server/spike-jsdom-workers.spec.ts"
      to: "wrangler.toml"
      via: "Spike runs under wrangler pages dev which reads wrangler.toml for nodejs_compat"
      pattern: "nodejs_compat"
    - from: "vite.config.ts"
      to: "playwright.config.ts"
      via: "Playwright webServer boots the vite dev process; spike confirms both run together"
      pattern: "5173"
---

<objective>
Install the Phase 7 server-only framework, scaffold all eight Wave-0 test files (so every later task can claim automated verify against a real file), extend the test configs, and front-load the jsdom-on-Workers spike — the single highest-novelty empirical risk in the phase. The spike's outcome gates the entire extraction architecture (jsdom-primary vs linkedom-fallback vs hybrid-contingency per RESEARCH.md §The jsdom-on-Workers Spike).

Purpose: Resolve the empirical risks (A1 cf.resolveOverride, A2 jsdom-on-Workers, A3 @cloudflare/vite-plugin webServer preservation) BEFORE Wave 2 implements the extraction pipeline, so 07-04 builds against the chosen DOM substrate without replan risk. Mirrors v1.0's "real-browser for layout truth" discipline applied to the backend.

Output: wrangler.toml, extended vite/vitest/playwright configs, eight Wave-0 test stubs, a recorded spike outcome (PASS/FAIL/HYBRID), and the two ASSUMED items explicitly verified.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ingestion-substrate/07-CONTEXT.md
@.planning/phases/07-ingestion-substrate/07-RESEARCH.md
@.planning/phases/07-ingestion-substrate/07-PATTERNS.md
@.planning/phases/07-ingestion-substrate/07-VALIDATION.md
@vite.config.ts
@vitest.config.ts
@playwright.config.ts
@package.json
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Phase 7 framework + extend all three test configs + write eight Wave-0 test stubs</name>
  <files>package.json, wrangler.toml, vite.config.ts, vitest.config.ts, playwright.config.ts, tests/unit/server/mxss.spec.ts, tests/unit/server/extraction.spec.ts, tests/unit/server/normalization.spec.ts, tests/unit/server/confidence.spec.ts, tests/unit/server/slugify.spec.ts, tests/e2e/ingestion/ssrf-matrix.spec.ts, tests/e2e/ingestion/happy-path.spec.ts, tests/e2e/ingestion/dexie-migration.spec.ts</files>

  <read_first>
    - package.json (current deps — exact-pin convention, no `^` on existing entries)
    - vite.config.ts (currently 7 lines — minimal SPA, no proxy)
    - vitest.config.ts (single-project; needs `server` project extension)
    - playwright.config.ts (single webServer on :5173; needs wrangler alongside)
    - 07-RESEARCH.md §Standard Stack L102-158 (exact versions + legitimacy verdicts)
    - 07-RESEARCH.md §Local-Dev Mechanism L622-668 (plugin-first, proxy-fallback — Task 2 picks one)
    - 07-RESEARCH.md §Validation Architecture L928-1000 (Wave-0 test layout)
    - 07-PATTERNS.md §vitest.config.ts L427-444 and §playwright.config.ts L448-462 (extension patterns)
    - eslint.config.js (confirm `react/no-danger: "error"` already covers /server and /functions per 07-PATTERNS.md L713)
  </read_first>

  <action>
    Install server-only deps with exact pins (no `^` — repo convention): `npm install @mozilla/readability@0.6.0 isomorphic-dompurify@3.22.0 jsdom@30.0.1 ip-address@10.5.0` (per D7-09 `@mozilla/readability` 0.6.0 is the extractor — Defuddle NOT evaluated; per D7-10 jsdom is the primary DOM substrate via nodejs_compat). Install devDeps: `npm install -D wrangler@4.120.1 @cloudflare/vite-plugin@1.51.2 @cloudflare/workers-types@latest`. jsdom@^25.0.0 already a devDep — leave the existing entry but the new server install will pin transitive jsdom@28+ via isomorphic-dompurify; verify `npm ls jsdom` shows the resolution. Confirm `npm install` exits 0.

    Create wrangler.toml at repo root with: `name = "lem-reader"`, `compatibility_date = "2024-09-23"` (the date `nodejs_compat_v2` merged into `nodejs_compat` per RESEARCH.md §State of the Art L866), `compatibility_flags = ["nodejs_compat"]`, and `pages_build_output_dir = "dist"`. Do NOT set `nodejs_compat_v2` separately (deprecated).

    Extend vitest.config.ts into a `defineConfig({ test: { projects: [...] } })` workspace (Vitest 4 syntax). Project 1 ("unit"): existing config (environment jsdom, globals, setupFiles ./tests/setup.ts, include `tests/unit/**/*.test.{ts,tsx}` excluding `tests/unit/server/**`, exclude tests/e2e + node_modules). Project 2 ("server"): same jsdom env + globals + setupFiles, include `tests/unit/server/**/*.spec.ts`. Both inherit plugins:[react()].

    Extend playwright.config.ts: convert `webServer` to an ARRAY. Keep the existing entry `command: "npm run dev", url: "http://localhost:5173", reuseExistingServer: !process.env.CI, timeout: 30_000`. Add a second entry: `command: "npx wrangler pages dev --port 8788", url: "http://localhost:8788", reuseExistingServer: !process.env.CI, timeout: 60_000, cwd: "."`. Task 2 below will finalize vite.config.ts (plugin vs proxy decision). For now leave vite.config.ts unchanged; Task 2's spike will modify it.

    Create eight Wave-0 test stub files. Each file MUST have a top-of-file comment naming the requirement it covers + the plan that will replace the stub with a real body, plus ONE `test.todo("replaced by 07-0X")` per discrete case so the file compiles and the harness runs without failures:
    - tests/unit/server/mxss.spec.ts: covers ING-07 / SC#4; replaced by 07-04. test.todo entries: "script tag stripped", "inline on* handler stripped", "javascript: URI rejected by linkableUrl", "SVG/MathML stripped by USE_PROFILES html", "dangerouslySetInnerHTML grep gate zero matches".
    - tests/unit/server/extraction.spec.ts: covers ING-01/ING-02; replaced by 07-04. test.todo entries: "Readability output maps to 9 block kinds", "paste-HTML path produces same Block shape as URL path".
    - tests/unit/server/normalization.spec.ts: covers ING-01 / SC#1; replaced by 07-05. test.todo entries: "representative fixture round-trips to confident", "extracted sample round-trips to confident", "article that cannot round-trip is refused".
    - tests/unit/server/confidence.spec.ts: covers ING-06; replaced by 07-03. test.todo entries per threshold row in RESEARCH.md §Confidence Thresholds L538-542.
    - tests/unit/server/slugify.spec.ts: covers D7-07; replaced by 07-03. test.todo entries for IDN/punycode, default-port strip, tracking-param strip, hash-fallback, collision.
    - tests/e2e/ingestion/ssrf-matrix.spec.ts: covers ING-08 / SC#3; replaced by 07-07. test.todo per Pitfall 3 measure.
    - tests/e2e/ingestion/happy-path.spec.ts: covers ING-01; replaced by 07-07. test.todo for "real URL → article opens in reader".
    - tests/e2e/ingestion/dexie-migration.spec.ts: covers SC#5; replaced by 07-07. test.todo for "v1 fixture snapshot intact after v3 upgrade".

    Run `npm run test:unit -- --run` and confirm 0 failures (the stubs use test.todo, not failing assertions). Run `npx tsc --noEmit` and confirm 0 type errors.
  </action>

  <verify>
    <automated>npm install && npx tsc --noEmit && npm run test:unit -- --run</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` dependencies block contains exactly-pinned entries for `@mozilla/readability: "0.6.0"`, `isomorphic-dompurify: "3.22.0"`, `jsdom: "30.0.1"`, `ip-address: "10.5.0"` (no `^` prefix on any).
    - `package.json` devDependencies block contains `wrangler: "4.120.1"`, `@cloudflare/vite-plugin: "1.51.2"`, `@cloudflare/workers-types`.
    - `wrangler.toml` exists at repo root and `grep -c "nodejs_compat" wrangler.toml` returns ≥1.
    - `wrangler.toml` does NOT contain the deprecated token `nodejs_compat_v2`.
    - `vitest.config.ts` contains `projects:` (workspace form) with both a `unit` and a `server` entry.
    - `playwright.config.ts` contains `webServer:` as an array including a wrangler entry on `:8788`.
    - All eight stub files exist; `grep -l "test.todo" tests/unit/server/ tests/e2e/ingestion/ -r` returns 8 paths.
    - `npx tsc --noEmit` exits 0.
    - `npm run test:unit -- --run` exits 0 (stubs use `test.todo`, not failing assertions).
    - `npm ls jsdom` shows the 30.0.1 pin (or 28.x transitive via isomorphic-dompurify).
  </acceptance_criteria>

  <done>
    Five server-only deps + two devDeps + workers-types installed with exact pins; wrangler.toml declares nodejs_compat with current compat_date; vitest has a `server` project; playwright boots wrangler alongside vite; all eight Wave-0 test files exist with test.todo bodies; tsc + test:unit both green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The jsdom-on-Workers spike — resolve A1/A2/A3 and record the architecture decision</name>
  <files>tests/unit/server/spike-jsdom-workers.spec.ts, vite.config.ts</files>
  <what-built>
    Task 1 installed the framework and the spike target runtime (wrangler pages dev). This task RUNS the spike: a Vitest spec that exercises jsdom + isomorphic-dompurify + @mozilla/readability end-to-end against the real workerd runtime, plus verifies the two `[ASSUMED]` items (cf.resolveOverride DNS pinning; @cloudflare/vite-plugin preserving v1.0 Playwright webServer). The spike is TIMEBOXED — if neither jsdom nor linkedom passes within the budget, surface the hybrid contingency.
  </what-built>

  <read_first>
    - 07-RESEARCH.md §The jsdom-on-Workers Spike L467-486 (acceptance criteria verbatim)
    - 07-RESEARCH.md §SSRF Guard Implementation L384-394 (cf.resolveOverride Option 1)
    - 07-RESEARCH.md §Local-Dev Mechanism L622-668 (plugin-first, proxy-fallback)
    - 07-RESEARCH.md §Assumptions Log L878-887 (A1, A2, A3, A5 explicitly tagged)
    - vite.config.ts (current state post-Task-1)
    - wrangler.toml (created in Task 1)
  </read_first>

  <action>
    Write `tests/unit/server/spike-jsdom-workers.spec.ts` that exercises FIVE concrete capabilities against the real workerd runtime (run via `npx vitest run tests/unit/server/spike-jsdom-workers.spec.ts` while `wrangler pages dev` is up, OR via a direct wrangler invocation — your choice as spike executor, but the runtime MUST be workerd, not pure Node):

    1. `import { JSDOM } from "jsdom"` succeeds (no ESM/CJS resolution error).
    2. `new JSDOM(html, { url })` constructs and `window.document.querySelector("p")` returns a node — verify with a representative publisher snippet.
    3. `import DOMPurify from "isomorphic-dompurify"; DOMPurify.sanitize(dirty)` returns sanitized output where `dirty` contains `<script>alert(1)</script><img src=x onerror=alert(1)>` — assert the returned string contains NEITHER `script` NOR `onerror`. Call `clearWindow()` after.
    4. `import { Readability, isProbablyReaderable } from "@mozilla/readability"; const doc = new JSDOM(html).window.document; const reader = new Readability(doc); const article = reader.parse()` returns a non-null object with `.content` containing `<p>` and `.textContent.length > 0`.
    5. `import { Address4 } from "ip-address"; new Address4("10.0.0.1").isInSubnet(new Address4("10.0.0.0/8"))` returns true (proves ip-address works on Workers).

    Also verify the two `[ASSUMED]` items explicitly — record PASS/FAIL for each:
    - A1: cf.resolveOverride DNS pinning. Attempt `fetch(url, { cf: { resolveOverride: "1.2.3.4" } })` against a known endpoint; record whether Workers accepts the option (does not throw) and whether the resolved IP appears pinned in the request. Document the outcome honestly.
    - A3: @cloudflare/vite-plugin vs server.proxy. Pick ONE: try `@cloudflare/vite-plugin` in vite.config.ts `plugins` array first (Option A). Run the existing v1.0 Playwright suite (`npm run test:e2e -- tests/e2e/open-every-fixture.spec.ts` is the v1.0 smoke). If the v1.0 webServer on :5173 still boots and the smoke passes, choose Option A and record PASS for A3. If the plugin breaks the SPA dev flow, fall back to Option B (`vite.config.ts` `server.proxy: { "/api": "http://localhost:8788" }`) and record Option B as chosen with A3 = PASS-under-Option-B.

    Record the spike outcome in the task SUMMARY: which path (jsdom-primary / linkedom-fallback / hybrid-contingency) is the architecture for 07-04 to implement against. If the spike REJECTS jsdom, also run a quick linkedom verification (`DOMPurify(linkedomWindow)` on one mXSS payload) and note whether linkedom looks viable for the mXSS gate that 07-04 will run.
  </action>

  <how-to-verify>
    1. `cd` to repo root, ensure `npm install` has run.
    2. In one terminal: `npx wrangler pages dev --port 8788` (or rely on the playwright config webServer to start it).
    3. Run: `npx vitest run tests/unit/server/spike-jsdom-workers.spec.ts`. All five capability tests must pass.
    4. For A3: `npm run test:e2e -- tests/e2e/open-every-fixture.spec.ts` must still pass (v1.0 smoke unaffected).
    5. Record the outcome in the task SUMMARY under a clear heading: "## Spike Outcome" with the path chosen (jsdom-primary | linkedom-fallback | hybrid-contingency), the A1/A3 verdicts, and the chosen vite.config.ts option (A or B). The next plan (07-04) reads this SUMMARY before implementing htmlToBlocks.
  </how-to-verify>

  <resume-signal>
    Type "approved" with the recorded spike outcome (e.g. "approved — jsdom-primary, A1 PASS, A3 PASS under Option A"). Describe issues if any capability failed; the planner will need to know before 07-04 is built.
  </resume-signal>

  <verify>
    <automated>npx vitest run tests/unit/server/spike-jsdom-workers.spec.ts && npx playwright test tests/e2e/open-every-fixture.spec.ts</automated>
  </verify>

  <done>
    The spike spec passes all five capability assertions (jsdom import + construct, DOMPurify sanitize, Readability parse, ip-address isInSubnet) against the real workerd runtime. The v1.0 smoke (open-every-fixture) still passes (A3 preserved — vite-plugin did not break the SPA dev flow). The SUMMARY records: (1) chosen architecture path (jsdom-primary | linkedom-fallback | hybrid-contingency); (2) A1 cf.resolveOverride verdict; (3) A3 vite-plugin webServer verdict; (4) chosen vite.config.ts option (A or B).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Worker | Untrusted URL or HTML crosses into the edge function |
| Worker → Publisher | Worker fetches a reader-supplied URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-SC | Tampering | npm installs (5 new deps + 2 devDeps) | mitigate | All seven packages passed RESEARCH.md §Package Legitimacy Audit (L143-156); SUS flags are documented false-positives on high-download official-source packages. Executor re-runs `npm view <pkg> version` to confirm versions before install. |
| T-7-01 | Information Disclosure | wrangler.toml compat flags | accept | Default compat flags are documented; no secrets in config. |
| T-7-02 | Tampering | vite.config.ts plugin vs proxy | mitigate | Spike verifies the v1.0 Playwright webServer flow is preserved (A3); fallback to server.proxy if the plugin breaks it. |
| T-7-03 | Tampering | jsdom-on-Workers compatibility (A2) | mitigate | This spike IS the mitigation — front-loaded Wave 1 with linkedom/hybrid fallback per RESEARCH.md L481-483. |
| T-7-04 | Tampering | cf.resolveOverride DNS pinning (A1) | mitigate | Spike verifies; if A1 fails, 07-03 ships validate-then-fetch with documented residual TOCTOU per RESEARCH.md L757-759. |
</threat_model>

<verification>
- `npm install` exits 0 with no postinstall scripts (RESEARCH.md L139 confirms none on the seven packages — re-verify with `npm ls --long`).
- `npx tsc --noEmit` exits 0.
- `npm run test:unit -- --run` exits 0 (Wave-0 stubs use test.todo).
- The spike spec at tests/unit/server/spike-jsdom-workers.spec.ts passes all five capability assertions against workerd.
- The v1.0 smoke `npx playwright test tests/e2e/open-every-fixture.spec.ts` still passes (A3 preserved).
- The spike outcome is recorded in the SUMMARY with the chosen architecture path for 07-04.
</verification>

<success_criteria>
- Framework installed + configs extended + eight Wave-0 stubs land.
- The spike's outcome is known and recorded: jsdom-primary OR linkedom-fallback OR hybrid-contingency.
- A1 (cf.resolveOverride) and A3 (vite-plugin webServer preservation) have explicit PASS/FAIL verdicts.
- The next plan (07-04) can implement htmlToBlocks against the chosen DOM substrate without replan risk.
</success_criteria>

<output>
Create `.planning/phases/07-ingestion-substrate/07-01-SUMMARY.md` when done. The SUMMARY MUST include a "## Spike Outcome" section recording: (1) the chosen architecture path; (2) A1/A3 verdicts; (3) the chosen vite.config.ts option; (4) any caveats for 07-04.
</output>

<artifacts_this_phase_produces>
## Artifacts this plan (07-01) produces

**Configuration:**
- `wrangler.toml` — `name`, `compatibility_date = "2024-09-23"`, `compatibility_flags = ["nodejs_compat"]`, `pages_build_output_dir`
- `vite.config.ts` extension — either `cloudflare()` plugin (Option A) or `server.proxy: { "/api": "http://localhost:8788" }` (Option B); spike-decided
- `vitest.config.ts` extension — workspace with `unit` + `server` projects
- `playwright.config.ts` extension — `webServer` array with vite (:5173) + wrangler (:8788)

**Dependencies added to `package.json`:**
- runtime: `@mozilla/readability@0.6.0`, `isomorphic-dompurify@3.22.0`, `jsdom@30.0.1`, `ip-address@10.5.0`
- dev: `wrangler@4.120.1`, `@cloudflare/vite-plugin@1.51.2`, `@cloudflare/workers-types`

**Test scaffolding (Wave-0 stubs replaced by later plans):**
- `tests/unit/server/spike-jsdom-workers.spec.ts` (real spike — kept)
- `tests/unit/server/mxss.spec.ts` → replaced by 07-04
- `tests/unit/server/extraction.spec.ts` → replaced by 07-04
- `tests/unit/server/normalization.spec.ts` → replaced by 07-05
- `tests/unit/server/confidence.spec.ts` → replaced by 07-03
- `tests/unit/server/slugify.spec.ts` → replaced by 07-03
- `tests/e2e/ingestion/ssrf-matrix.spec.ts` → replaced by 07-07
- `tests/e2e/ingestion/happy-path.spec.ts` → replaced by 07-07
- `tests/e2e/ingestion/dexie-migration.spec.ts` → replaced by 07-07
</artifacts_this_phase_produces>
