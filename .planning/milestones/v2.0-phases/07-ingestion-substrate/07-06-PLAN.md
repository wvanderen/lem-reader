---
phase: 07-ingestion-substrate
plan: 06
type: execute
wave: 4
depends_on: [07-05]
files_modified:
  - functions/api/ingest.ts
  - src/ingestion/IngestionClient.ts
  - src/ingestion/LibrarySource.ts
  - src/content/repository.ts
  - src/ingestion/IngestControl.tsx
  - src/routes/FixtureList.tsx
autonomous: true
requirements: [ING-01, ING-02, ING-06]
user_setup: []
decisions_implemented: [D7-01, D7-02, D7-03, D7-04, D7-05, D7-07, D7-08]

must_haves:
  truths:
    - "POST /api/ingest accepts same-origin JSON {url} or {html}, delegates to server/ingest, and returns 200 + {article, confidence} on success or 400 + {reason} on typed failure"
    - "IngestionClient.ingestUrl(url) and IngestionClient.ingestHtml(html) both POST to /api/ingest, re-validate the response through ArticleSchema.parse (STATE-04 defense-in-depth), and throw IngestionError on failure"
    - "DexieLibrarySource implements ArticleRepository.list()/open(id) PLUS save(article), has(id), remove(id) — the dedupe-refuse check (D7-07) uses has(id) before save"
    - "compositeLibraryRepository (the new default export in src/content/repository.ts) UNIONs inMemoryRepository.list() (fixtures) with DexieLibrarySource.list() (ingested) — callers (FixtureList, ArticleView) are unchanged"
    - "FixtureList renders the new IngestControl (URL input + paste textarea) above the existing article list; the control uses the existing `.status` live region for refusal messages (D7-04 — zero new chrome)"
    - "IngestControl has four states: idle | submitting | success | error; refusal reasons map to calm DOC-06 voice copy in the .status region (no red toasts, no modal wizard)"
    - "Successful ingest saves the article to Dexie v3, navigates to #/article/<id> via window.location.hash, and the existing ArticleView opens it unchanged (load-bearing invariant: reader cannot tell ingested from fixture)"
  artifacts:
    - path: "functions/api/ingest.ts"
      provides: "Cloudflare Pages Function onRequest(context) — thin adapter delegating to server/ingest"
      contains: "onRequest"
    - path: "src/ingestion/IngestionClient.ts"
      provides: "ingestUrl(url), ingestHtml(html), IngestionClient class with ArticleSchema re-validation"
      contains: "ingestUrl"
    - path: "src/ingestion/LibrarySource.ts"
      provides: "DexieLibrarySource implementing ArticleRepository + save/has/remove; compositeLibraryRepository"
      contains: "DexieLibrarySource"
    - path: "src/content/repository.ts"
      provides: "Swap from inMemoryRepository to compositeLibraryRepository as the default"
      contains: "compositeLibraryRepository"
    - path: "src/ingestion/IngestControl.tsx"
      provides: "Minimal URL + paste-HTML control with four-state .status live region"
      contains: "IngestControl"
    - path: "src/routes/FixtureList.tsx"
      provides: "FixtureList with IngestControl mounted above the article list"
      contains: "IngestControl"
  key_links:
    - from: "functions/api/ingest.ts"
      to: "server/ingest.ts"
      via: "Adapter imports ingest() and serializes IngestionResponse to JSON"
      pattern: "from ['\\\"]../../server/ingest['\\\"]"
    - from: "src/ingestion/IngestionClient.ts"
      to: "functions/api/ingest.ts"
      via: "fetch('/api/ingest', { method: 'POST', body: JSON.stringify({url}|{html}) })"
      pattern: "/api/ingest"
    - from: "src/ingestion/IngestionClient.ts"
      to: "src/content/schema.ts"
      via: "Re-validates server response through ArticleSchema.parse (STATE-04)"
      pattern: "ArticleSchema\\.parse"
    - from: "src/ingestion/LibrarySource.ts"
      to: "src/persistence/db.ts"
      via: "db.articles.put / db.articles.get / db.articles.where for Dexie v3 reads/writes"
      pattern: "db\\.articles"
    - from: "src/content/repository.ts"
      to: "src/ingestion/LibrarySource.ts"
      via: "listArticles/openArticle delegate to compositeLibraryRepository after the D7-02 swap"
      pattern: "compositeLibraryRepository"
    - from: "src/routes/FixtureList.tsx"
      to: "src/ingestion/IngestControl.tsx"
      via: "<IngestControl /> mounted above the article <ul>"
      pattern: "<IngestControl"
---

<objective>
Ship the reader-facing half of the ingestion pipeline: the Cloudflare Pages Function adapter (`functions/api/ingest.ts`), the client glue (`IngestionClient`), the Dexie-backed repository swap (`DexieLibrarySource` + `compositeLibraryRepository`), and the minimal proof-form ingest control (D7-01 — URL input + paste textarea, zero library chrome). The existing FixtureList mounts the control above its list; on successful ingest the article lands in Dexie v3 and opens in the UNCHANGED ArticleView. This is the load-bearing-invariant proof: an ingested article reads identically to a fixture.

Purpose: Deliver ING-01 (URL), ING-02 (paste-HTML), ING-06 (honest three-state surfacing via the existing `.status` live region per D7-04), and the D7-02 ArticleRepository swap (the D-08 forward-compat hook lands here, not Phase 8). The control is intentionally minimal — Phase 8 wraps the library surface around the already-working ingest+write.

Output: 6 files (1 new edge function, 3 new client modules, 2 modified files).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ingestion-substrate/07-CONTEXT.md
@.planning/phases/07-ingestion-substrate/07-RESEARCH.md
@.planning/phases/07-ingestion-substrate/07-PATTERNS.md
@.planning/phases/07-ingestion-substrate/07-01-SUMMARY.md
@.planning/phases/07-ingestion-substrate/07-05-SUMMARY.md
@src/content/repository.ts
@src/content/schema.ts
@src/persistence/db.ts
@src/persistence/highlightsStore.ts
@src/routes/FixtureList.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge function adapter + IngestionClient + DexieLibrarySource + repository swap</name>
  <files>functions/api/ingest.ts, src/ingestion/IngestionClient.ts, src/ingestion/LibrarySource.ts, src/content/repository.ts</files>
  <behavior>
    - POST /api/ingest with JSON body { url: "https://example.com/article" } returns 200 + { ok: true, article: {...CanonicalArticle}, confidence: {...} } on a successful ingest.
    - POST /api/ingest with JSON body { html: "<article>...</article>" } returns the same shape on a successful paste ingest.
    - POST /api/ingest with JSON body { url: "http://169.254.169.254/" } returns 400 + { ok: false, reason: "ssrf-blocked-metadata" } — and NO upstream body in the response.
    - POST /api/ingest with empty JSON returns 400 + { ok: false, reason: "server-error" }.
    - IngestionClient.ingestUrl(url) returns a CanonicalArticle that PASSES ArticleSchema.parse (re-validation on the client side, STATE-04 defense-in-depth).
    - IngestionClient.ingestUrl(url) throws an Error with .reason === "ssrf-blocked-metadata" when the server returns 400 with that reason.
    - DexieLibrarySource.save(article) writes a row to db.articles that persists across DB close/reopen.
    - DexieLibrarySource.has(id) returns true after save and false before save.
    - DexieLibrarySource.list() returns only Dexie rows (NOT fixtures — the composite merges fixtures separately).
    - DexieLibrarySource.remove(id) deletes the article row AND cascades to its highlights/notes/locations (D5-12 cascade pattern).
    - compositeLibraryRepository.list() returns the UNION of inMemoryRepository.list() (fixtures) and DexieLibrarySource.list() (ingested) — no duplicates by id.
    - compositeLibraryRepository.open(id) finds the article in either source.
  </behavior>

  <read_first>
    - 07-RESEARCH.md §Code Examples Example 1 L786-823 (IngestionClient reference)
    - 07-RESEARCH.md §Pattern 1 L235-281 + §Recommended Project Structure L203-233 (functions/api/ingest.ts shape)
    - 07-PATTERNS.md §functions/api/ingest.ts L42-65 (the onRequest adapter)
    - 07-PATTERNS.md §src/ingestion/IngestionClient.ts L213-240 (discriminated-result + fetch shape)
    - 07-PATTERNS.md §src/ingestion/LibrarySource.ts L243-296 (the ArticleRepository interface + Dexie read/write analog)
    - 07-PATTERNS.md §src/content/repository.ts L366-381 (the swap point — module-level wrappers preserved)
    - src/content/repository.ts (FULL FILE — the existing inMemoryRepository + listArticles/openArticle wrappers)
    - src/persistence/highlightsStore.ts L62-110 (the Zod-safeParse-on-read + cascade-delete patterns to mirror)
    - src/persistence/db.ts (the articles store + the v3 source/addedAt indexes from 07-02)
    - src/ingestion/types.ts (IngestionRequest, IngestionResponse, IngestionFailureReason from 07-02)
  </read_first>

  <action>
    Create `functions/api/ingest.ts` as the thin Cloudflare Pages Function adapter per PATTERNS.md L42-65:
    - Imports: `import { ingest } from "../../server/ingest";`, `import type { IngestionRequest, IngestionResponse } from "../../src/ingestion/types";`. Optionally `import type { PagesFunction } from "@cloudflare/workers-types";` for typing.
    - Export `export const onRequest: PagesFunction = async (context) => { let body: IngestionRequest; try { body = await context.request.json(); } catch { return Response.json({ ok: false, reason: "server-error" } satisfies IngestionResponse, { status: 400 }); } const result: IngestionResponse = await ingest(body); return result.ok ? Response.json(result) : Response.json(result, { status: 400 }); };`
    - The adapter is the ONLY file that knows about Cloudflare — porting to Vercel/Netlify changes ONLY this file (CONTEXT.md D7-05 adapter boundary).

    Create `src/ingestion/IngestionClient.ts` per PATTERNS.md L213-240 + RESEARCH.md Example 1:
    - Imports: `import { ArticleSchema, type CanonicalArticle } from "../content/schema";`, `import type { IngestionFailureReason, IngestionResponse } from "./types";`.
    - Export class `IngestionError extends Error { constructor(public reason: IngestionFailureReason) { super(reason); this.name = "IngestionError"; } }` (the client-side mirror of server/errors.ts; lives in src/ so it's in the client bundle).
    - Export `export async function ingestUrl(url: string): Promise<{ article: CanonicalArticle; confidence: { state: "confident" | "low" }; }> { return ingest({ url }); }`.
    - Export `export async function ingestHtml(html: string): Promise<{ article: CanonicalArticle; confidence: { state: "confident" | "low" }; }> { return ingest({ html }); }`.
    - Private `async function ingest(body: { url?: string; html?: string }): Promise<{ article: CanonicalArticle; confidence: { state: "confident" | "low" }; }> { const res = await fetch("/api/ingest", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const json: IngestionResponse = await res.json(); if (!json.ok) throw new IngestionError(json.reason); if (!res.ok) throw new IngestionError("server-error"); const article = ArticleSchema.parse(json.article); return { article, confidence: json.confidence }; }` (STATE-04 — re-validate the network response through Zod).

    Create `src/ingestion/LibrarySource.ts` per PATTERNS.md §src/ingestion/LibrarySource.ts L243-296:
    - Imports: `import { db } from "../persistence/db";`, `import { ArticleSchema, type CanonicalArticle } from "../content/schema";`, `import { fixtures } from "../fixtures";`, `import type { ArticleRepository } from "../content/repository";`.
    - Export class `DexieLibrarySource implements ArticleRepository { async list(): Promise<CanonicalArticle[]> { const rows = await db.articles.toArray(); const valid: CanonicalArticle[] = []; for (const row of rows) { const parsed = ArticleSchema.safeParse(row); if (parsed.success) valid.push(parsed.data); } return valid; } async open(id: string): Promise<CanonicalArticle | null> { const row = await db.articles.get(id); if (!row) return null; const parsed = ArticleSchema.safeParse(row); return parsed.success ? parsed.data : null; } async save(article: CanonicalArticle): Promise<void> { await db.articles.put(article); } async has(id: string): Promise<boolean> { return (await db.articles.get(id)) !== undefined; } async remove(id: string): Promise<void> { await db.transaction("rw", db.articles, db.highlights, db.notes, db.location, async () => { await db.articles.delete(id); await db.highlights.where("[articleId+revision]").between([id, 0], [id, Number.MAX_SAFE_INTEGER]).delete(); await db.notes.where("highlightId").anyOf(await db.highlights.where("[articleId+revision]").between([id, 0], [id, Number.MAX_SAFE_INTEGER]).primaryKeys().then(keys => keys.map(k => Array.isArray(k) ? k[0] : k))).delete(); await db.location.where("[articleId+revision]").between([id, 0], [id, Number.MAX_SAFE_INTEGER]).delete(); }); } }` (D5-12 cascade pattern from highlightsStore.ts L105-109).
    - Export `const dexieLibrarySource = new DexieLibrarySource();`
    - Export `export const compositeLibraryRepository: ArticleRepository = { async list() { const [fixtureList, ingestedList] = await Promise.all([Promise.resolve([...fixtures]), dexieLibrarySource.list()]); const seen = new Set<string>(); const merged: CanonicalArticle[] = []; for (const a of [...ingedList, ...fixtureList]) { if (!seen.has(a.id)) { seen.add(a.id); merged.push(a); } } return merged; }, async open(id) { const ingested = await dexieLibrarySource.open(id); if (ingested) return ingested; return fixtures.find(a => a.id === id) ?? null; } };` (ingested takes precedence on id collision — the reader's local library wins over bundled fixtures).

    Modify `src/content/repository.ts` per PATTERNS.md §src/content/repository.ts L366-381:
    - Keep the ArticleRepository interface (L9-12) and inMemoryRepository (L14-21) UNCHANGED.
    - Re-export `compositeLibraryRepository` from `../ingestion/LibrarySource`.
    - Swap the module-level wrappers (L24-25): `export const listArticles = compositeLibraryRepository.list; export const openArticle = compositeLibraryRepository.open;` — callers (FixtureList L9, ArticleView L23) are UNCHANGED. This is the D-08 forward-compat hook landing (D7-02).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "onRequest" functions/api/ingest.ts` returns 1.
    - `grep -c "from ['\\\"]../../server/ingest['\\\"]" functions/api/ingest.ts` returns 1 (adapter delegates to /server, not reimplements).
    - `grep -c "ingestUrl" src/ingestion/IngestionClient.ts` returns 1.
    - `grep -c "ingestHtml" src/ingestion/IngestionClient.ts` returns 1.
    - `grep -c "ArticleSchema.parse" src/ingestion/IngestionClient.ts` returns ≥1 (STATE-04 re-validation).
    - `grep -c "class DexieLibrarySource" src/ingestion/LibrarySource.ts` returns 1.
    - `grep -c "async save" src/ingestion/LibrarySource.ts` returns 1.
    - `grep -c "async has" src/ingestion/LibrarySource.ts` returns 1 (D7-07 dedupe check).
    - `grep -c "async remove" src/ingestion/LibrarySource.ts` returns 1.
    - `grep -c "db.transaction" src/ingestion/LibrarySource.ts` returns ≥1 (cascade-delete in a transaction).
    - `grep -c "compositeLibraryRepository" src/ingestion/LibrarySource.ts` returns ≥1.
    - `grep -c "compositeLibraryRepository" src/content/repository.ts` returns ≥1 (the D7-02 swap).
    - `grep -c "inMemoryRepository" src/content/repository.ts` returns ≥1 (the v1.0 impl is preserved, not deleted — fixtures still flow through it via composite).
    - The module-level wrappers `listArticles`/`openArticle` in src/content/repository.ts delegate to compositeLibraryRepository.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/unit/` exits 0 (all v1.0 unit + component tests unaffected by the repository swap).
  </acceptance_criteria>

  <done>
    The Cloudflare adapter delegates cleanly to /server; IngestionClient re-validates server responses; DexieLibrarySource implements ArticleRepository + save/has/remove with cascade; compositeLibraryRepository merges fixtures + ingested; the repository swap preserves all existing callers. v1.0 unit tests unaffected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal ingest control UI (D7-01) mounted in FixtureList (D7-02 merge surface)</name>
  <files>src/ingestion/IngestControl.tsx, src/routes/FixtureList.tsx</files>
  <behavior>
    - IngestControl renders a URL input (labeled "Add by URL") and a paste textarea (labeled "Paste HTML") above the article list when mounted in FixtureList.
    - The control has four states: idle | submitting | success | error.
    - On submit, IngestControl calls IngestionClient.ingestUrl or IngestionClient.ingestHtml based on which input has content.
    - On a successful ingest, the control calls DexieLibrarySource.has(id) first; if has returns true, surface "already in your library" in the .status region (D7-07 dedupe-refuse) and offer to open it. Else DexieLibrarySource.save(article) then navigate to #/article/<id> via window.location.hash.
    - On a typed failure (IngestionError), map the reason to calm DOC-06 voice copy in the .status live region (D7-04 — zero new chrome): ssrf-blocked-* → "This address points somewhere the reader can't reach.", fetch-failed → "Couldn't reach this page.", response-too-large → "This page is too large.", unsupported-content-type → "This page isn't an article.", extraction-unsupported → "Couldn't reliably read this page.", extraction-too-low-confidence → "Couldn't reliably read this page.", round-trip-anchor-failed → "Couldn't reliably read this page.", already-in-library → "Already in your library.", server-error → "Something went wrong. Try again.".
    - The .status live region carries role="status", aria-live="polite", aria-atomic="true" (existing pattern from FixtureList L36).
    - The control announces the submitting state ("Fetching article…") and success state (navigation happens — no separate success message needed).
    - FixtureList renders <IngestControl /> above the <ul className="fixture-list">.
  </behavior>

  <read_first>
    - 07-RESEARCH.md §D7-01 (minimal proof form) and §D7-04 (reuse .status + PAGE-09) — from CONTEXT.md `<decisions>` L51-55
    - 07-PATTERNS.md §src/ingestion/IngestControl.tsx L313-339 (the FixtureList analog + four-state pattern)
    - 07-PATTERNS.md §The `.status` live region shared pattern L642-650
    - src/routes/FixtureList.tsx (FULL FILE — the loading/ready/error status pattern + .status live region to mirror)
    - src/persistence/highlightsStore.ts L48-50 (the discriminated-result union pattern)
    - The DOC-06 disclosure vocabulary (existing calm voice — "Couldn't open this article." etc.)
    - The PAGE-09 fallback banner pattern (from 04 phase — low-confidence surfaces here)
  </read_first>

  <action>
    Create `src/ingestion/IngestControl.tsx` per PATTERNS.md L313-339 + the FixtureList L13-44 pattern:
    - Imports: `import { useState } from "react";`, `import { ingestUrl, ingestHtml, IngestionError } from "./IngestionClient";`, `import { dexieLibrarySource } from "./LibrarySource";`.
    - Export `export function IngestControl() { const [urlValue, setUrlValue] = useState(""); const [htmlValue, setHtmlValue] = useState(""); const [status, setStatus] = useState<"idle" | "submitting" | "success" | "error">("idle"); const [message, setMessage] = useState<string | null>(null);`
    - `async function handleSubmit(which: "url" | "paste") { setStatus("submitting"); setMessage("Fetching article…"); try { const result = which === "url" ? await ingestUrl(urlValue) : await ingestHtml(htmlValue); const alreadyInLibrary = await dexieLibrarySource.has(result.article.id); if (alreadyInLibrary) { setStatus("error"); setMessage("Already in your library."); return; } await dexieLibrarySource.save(result.article); setStatus("success"); window.location.hash = `#/article/${result.article.id}`; } catch (e) { setStatus("error"); if (e instanceof IngestionError) setMessage(mapReasonToCopy(e.reason)); else setMessage("Something went wrong. Try again."); } }`
    - `function mapReasonToCopy(reason: IngestionFailureReason): string { switch (reason) { case "ssrf-blocked-scheme": case "ssrf-blocked-private-ip": case "ssrf-blocked-metadata": return "This address points somewhere the reader can't reach."; case "fetch-failed": return "Couldn't reach this page."; case "response-too-large": return "This page is too large."; case "unsupported-content-type": return "This page isn't an article."; case "extraction-unsupported": case "extraction-too-low-confidence": case "round-trip-anchor-failed": return "Couldn't reliably read this page."; case "already-in-library": return "Already in your library."; default: return "Something went wrong. Try again."; } }` — import IngestionFailureReason type from `./types`.
    - JSX: a `<section aria-labelledby="ingest-heading">` containing `<h2 id="ingest-heading">Add an article</h2>`, a URL `<form onSubmit={...}>` with a labeled `<input type="url">` and a submit `<button>`, a paste `<form onSubmit={...}>` with a labeled `<textarea>`, and the existing `.status` live region pattern: `<div className="status" role="status" aria-live="polite" aria-atomic="true">{status === "submitting" && <p>{message}</p>}{status === "error" && <p>{message}</p>}</div>`. The success state doesn't render a message because navigation takes the user to ArticleView.
    - Disable both submit buttons when status === "submitting".

    Modify `src/routes/FixtureList.tsx` per PATTERNS.md §src/routes/FixtureList.tsx L411-413:
    - Import `import { IngestControl } from "../ingestion/IngestControl";` at the top.
    - Insert `<IngestControl />` between the existing `<h1>Saved articles</h1>` (L35) and the `<div className="status">` (L36) — OR after the status block but before the items list. Either position is acceptable; pick the one that keeps the existing status region's responsibility (loading/ready/error for the list itself) visually distinct from the IngestControl's status region (refusal messages for ingest). Prefer mounting IngestControl BEFORE the list `<ul>`.
    - The existing list rendering, useEffect, and listArticles call are UNCHANGED — the compositeLibraryRepository swap in Task 1 means listArticles now returns fixtures + ingested rows automatically.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/ tests/component/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function IngestControl" src/ingestion/IngestControl.tsx` returns 1.
    - `grep -c 'role="status"' src/ingestion/IngestControl.tsx` returns ≥1 (reuses .status live region per D7-04).
    - `grep -c "ingestUrl" src/ingestion/IngestControl.tsx` returns ≥1.
    - `grep -c "ingestHtml" src/ingestion/IngestControl.tsx` returns ≥1.
    - `grep -c "dexieLibrarySource.has" src/ingestion/IngestControl.tsx` returns ≥1 (D7-07 dedupe-refuse).
    - `grep -c "dexieLibrarySource.save" src/ingestion/IngestControl.tsx` returns ≥1.
    - `grep -c "window.location.hash" src/ingestion/IngestControl.tsx` returns ≥1 (navigation to ArticleView).
    - `grep -c "mapReasonToCopy" src/ingestion/IngestControl.tsx` returns ≥1 (D7-04 honest-failure copy mapping).
    - `grep -c "<IngestControl" src/routes/FixtureList.tsx` returns ≥1 (the control is mounted).
    - The existing FixtureList listArticles call + useEffect + list rendering are byte-unchanged (verify via git diff).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/unit/ tests/component/` exits 0 (all v1.0 component tests pass — FixtureList.test.tsx may need a small update to account for the new IngestControl mount; if so, update it minimally without changing existing assertions).
  </acceptance_criteria>

  <done>
    IngestControl renders URL input + paste textarea with four-state .status live region; refuses re-ingest via DexieLibrarySource.has (D7-07); maps every IngestionFailureReason to calm DOC-06 copy (D7-04); navigates to ArticleView on success; FixtureList mounts the control above its list with zero changes to existing rendering logic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/ingest (same-origin) | Ingestion request crosses into the edge function |
| Worker → Server (server/ingest) | Adapter delegates to the platform-agnostic orchestrator |
| Server → Browser (IngestionResponse) | Typed response crosses back through ArticleSchema.parse re-validation |
| Browser → Dexie v3 (articles store) | The compositeLibraryRepository write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-25 | Tampering | Malformed IngestionResponse from server | mitigate | IngestionClient re-validates via ArticleSchema.parse — STATE-04 defense-in-depth on the network read path. |
| T-7-26 | Tampering | Refusal copy leaks internal jargon (fixture/Zod/schema/revision) | mitigate | mapReasonToCopy maps every IngestionFailureReason to calm DOC-06 voice; UI-SPEC §Copywriting enforced. |
| T-7-27 | Repudiation | Reader submits URL, claims they didn't | accept | Stateless backend owns no identity; no auth in Phase 7 (RESEARCH.md §Security Domain V2). |
| T-7-28 | Tampering | Re-ingest of the same URL silently overwrites the article (breaks D7-07 immutability + orphans highlights) | mitigate | DexieLibrarySource.has(id) check BEFORE save; if has returns true, refuse with "already in your library" (D7-07). |
| T-7-29 | Information Disclosure | Cascade-delete misses highlights/notes/locations (orphans Dexie rows) | mitigate | DexieLibrarySource.remove uses db.transaction("rw", db.articles, db.highlights, db.notes, db.location, ...) — atomic cascade. |
| T-7-30 | Denial of Service | Reader submits infinite URLs from the form | accept | Stateless backend; Cloudflare edge rate-limits. Phase 7 has no per-identity quota (acceptable for a local-first prototype). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/unit/ tests/component/` exits 0 — all v1.0 tests unaffected by the repository swap and the FixtureList mount.
- The full request flow `POST /api/ingest {url}` works against `wrangler pages dev` (defer the comprehensive matrix to 07-07).
- The repository swap is invisible to callers — FixtureList + ArticleView read through compositeLibraryRepository without code changes.
</verification>

<success_criteria>
- The Cloudflare adapter delegates to /server (the adapter boundary is one file).
- IngestionClient re-validates server responses through ArticleSchema.parse (STATE-04).
- DexieLibrarySource implements ArticleRepository + save/has/remove (D7-07 dedupe + D5-12 cascade).
- compositeLibraryRepository merges fixtures + ingested behind the unchanged listArticles/openArticle wrappers.
- IngestControl delivers D7-01 (URL + paste), D7-04 (.status live region reuse), D7-07 (dedupe-refuse), D7-08 (paste omits sourceUrl).
- FixtureList mounts the control; the existing list still works.
</success_criteria>

<output>
Create `.planning/phases/07-ingestion-substrate/07-06-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this plan (07-06) produces

**Edge function:**
- `functions/api/ingest.ts` — `onRequest(context)` adapter delegating to `server/ingest`; serializes `IngestionResponse` to JSON with status 200/400

**Client glue (`src/ingestion/`):**
- `src/ingestion/IngestionClient.ts` — `ingestUrl(url)`, `ingestHtml(html)`, `IngestionError` (client-side class), private `ingest(body)` with ArticleSchema re-validation
- `src/ingestion/LibrarySource.ts` — `DexieLibrarySource` class (list/open/save/has/remove), `dexieLibrarySource` singleton, `compositeLibraryRepository` (UNION of fixtures + ingested)
- `src/ingestion/IngestControl.tsx` — minimal URL + paste textarea control with four-state .status live region + mapReasonToCopy

**Modified files:**
- `src/content/repository.ts` — listArticles/openArticle wrappers now delegate to compositeLibraryRepository (D7-02 swap)
- `src/routes/FixtureList.tsx` — mounts <IngestControl /> above the article list
</artifacts_this_phase_produces>
