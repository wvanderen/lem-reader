---
phase: 07-ingestion-substrate
plan: 07
type: execute
wave: 5
depends_on: [07-06]
files_modified:
  - tests/e2e/ingestion/ssrf-matrix.spec.ts
  - tests/e2e/ingestion/happy-path.spec.ts
  - tests/e2e/ingestion/dexie-migration.spec.ts
  - package.json
autonomous: true
requirements: [ING-07, ING-08]
user_setup: []
decisions_implemented: [D7-06]

must_haves:
  truths:
    - "The SSRF regression matrix (tests/e2e/ingestion/ssrf-matrix.spec.ts) runs against real wrangler pages dev and covers all 9 Pitfall 3 measures: scheme allowlist, private/loopback/link-local IPs, cloud-metadata, CGNAT, encoding bypasses, redirect-into-internal, DNS-rebinding, and NO upstream body on refusal"
    - "Every SSRF matrix case refuses the request with a typed IngestionResponse and NO upstream body bytes (Measure 7)"
    - "The happy-path e2e (tests/e2e/ingestion/happy-path.spec.ts) submits a real publisher URL via IngestControl, waits for the article to land in Dexie, opens it via #/article/:id, and asserts it renders blocks + paginates + accepts a highlight identically to a fixture (SC#1 load-bearing invariant)"
    - "The Dexie migration snapshot (tests/e2e/ingestion/dexie-migration.spec.ts) seeds a v1/v2 Dexie database with the v1.0 fixture snapshot, triggers the v3 upgrade by opening the app, and asserts EVERY v1.0 row is intact and addressable (SC#5)"
    - "The repo-wide grep gate `! grep -rn dangerouslySetInnerHTML src/ server/ functions/` is wired as a package.json script (e.g. lint:no-danger) and exits 0 today (the structural XSS defense holds)"
    - "All four phase-exit gates pass when run as a single command sequence (npm run test:unit -- --run && npm run test:e2e)"
  artifacts:
    - path: "tests/e2e/ingestion/ssrf-matrix.spec.ts"
      provides: "Real SSRF regression matrix against real wrangler pages dev (SC#3)"
      contains: "describe"
    - path: "tests/e2e/ingestion/happy-path.spec.ts"
      provides: "Real happy-path e2e — URL → reader (SC#1 integration truth)"
      contains: "describe"
    - path: "tests/e2e/ingestion/dexie-migration.spec.ts"
      provides: "Real v1→v3 Dexie migration snapshot (SC#5)"
      contains: "describe"
  key_links:
    - from: "tests/e2e/ingestion/ssrf-matrix.spec.ts"
      to: "functions/api/ingest.ts via wrangler pages dev :8788"
      via: "page.request.post('http://localhost:8788/api/ingest', { data: { url } }) — exercises the real edge function + the real fetch+DNS+redirect stack"
      pattern: "8788/api/ingest"
    - from: "tests/e2e/ingestion/happy-path.spec.ts"
      to: "src/routes/FixtureList.tsx + src/ingestion/IngestControl.tsx"
      via: "page.goto('http://localhost:5173/#/'); fills the URL input; clicks submit; waits for #/article/:id"
      pattern: "IngestControl"
    - from: "tests/e2e/ingestion/dexie-migration.spec.ts"
      to: "src/persistence/db.ts"
      via: "page.evaluate(() => indexedDB.open('lem-reader')) triggers the v3 declaration; reads back settings/location/highlights/notes rows"
      pattern: "indexedDB"
---

<objective>
Wire the four phase-exit gates as real Playwright e2e tests against the actual deployed-shape stack (real wrangler pages dev for the SSRF matrix + real IndexedDB for the Dexie migration snapshot), and add the repo-wide `dangerouslySetInnerHTML` grep gate as a CI-runnable npm script. These are the SC#1/3/4/5 proofs that the phase is honestly done. Replaces the three remaining Wave-0 e2e stubs.

Purpose: Phase 7 ships 4 phase-exit gates (SSRF matrix SC#3, mXSS suite SC#4 — already done in 07-04, round-trip anchor SC#1 — already done in 07-05, and Dexie migration SC#5). This plan delivers the three e2e gates that need the full stack running (real workerd for fetch+DNS, real IndexedDB for migration). Mirrors v1.0's "honest full-suite execution discipline" (Key Decision #9) applied to the backend.

Output: Three real e2e specs replacing Wave-0 stubs + a package.json `lint:no-danger` script.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ingestion-substrate/07-CONTEXT.md
@.planning/phases/07-ingestion-substrate/07-RESEARCH.md
@.planning/phases/07-ingestion-substrate/07-VALIDATION.md
@.planning/phases/07-ingestion-substrate/07-PATTERNS.md
@.planning/phases/07-ingestion-substrate/07-01-SUMMARY.md
@.planning/phases/07-ingestion-substrate/07-06-SUMMARY.md
@playwright.config.ts
@tests/e2e/persistence.spec.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSRF regression matrix (SC#3) + happy-path e2e (SC#1)</name>
  <files>tests/e2e/ingestion/ssrf-matrix.spec.ts, tests/e2e/ingestion/happy-path.spec.ts</files>
  <behavior>
    SSRF matrix (SC#3 — all 9 Pitfall 3 measures):
    - POST :8788/api/ingest { url: "file:///etc/passwd" } → 400 + { reason: "ssrf-blocked-scheme" }; response body has NO file content.
    - POST :8788/api/ingest { url: "gopher://x" } → 400 + { reason: "ssrf-blocked-scheme" }.
    - POST :8788/api/ingest { url: "http://169.254.169.254/latest/meta-data/" } → 400 + { reason: "ssrf-blocked-metadata" }; response body does NOT contain "ami-id" or any metadata.
    - POST :8788/api/ingest { url: "http://metadata.google.internal/" } → 400 + { reason: "ssrf-blocked-metadata" }.
    - POST :8788/api/ingest { url: "http://10.0.0.1/" } → 400 + { reason: "ssrf-blocked-private-ip" } (DNS resolves to private range).
    - POST :8788/api/ingest { url: "http://127.0.0.1/" } → 400 + { reason: "ssrf-blocked-private-ip" }.
    - POST :8788/api/ingest { url: "http://100.64.0.1/" } → 400 + { reason: "ssrf-blocked-private-ip" } (CGNAT).
    - POST :8788/api/ingest { url: "http://0x7f000001/" } → 400 (encoding bypass — ip-address normalizes).
    - POST :8788/api/ingest { url: "http://[::ffff:127.0.0.1]/" } → 400 + private-ip reason (IPv4-mapped IPv6).
    - Redirect-into-internal: POST a URL that returns 302 to 169.254.169.254 → 400 + ssrf-blocked reason (per-hop re-validation).
    - For each refusal: assert response.text() does NOT contain any bytes that would have come from the upstream target (Measure 7 — no body leak).

    Happy-path (SC#1):
    - page.goto http://localhost:5173/#/ → FixtureList renders.
    - Fill the URL input with a known-good publisher URL (use a stable test fixture URL OR a real public URL).
    - Click submit → wait for #/article/:id navigation.
    - ArticleView renders the ingested article; assert at least one heading + one paragraph are visible.
    - Create a highlight via the existing annotation UI → assert the highlight persists across reload (the round-trip anchor holds for ingested content).
    - Reload the page → ArticleView restores to the saved location (STATE-01 invariant holds for ingested articles).
  </behavior>

  <read_first>
    - 07-RESEARCH.md §Gate 1 L953-962 (SSRF matrix structure verbatim)
    - 07-VALIDATION.md §The Four Phase-Exit Gates L87-103 (the gate definitions)
    - 07-PATTERNS.md §tests/e2e/ingestion/ssrf-matrix.spec.ts L563-582 (the persistence.spec.ts harness layout to copy)
    - 07-PATTERNS.md §tests/e2e/ingestion/happy-path.spec.ts L585-589 (page.goto + getByRole + reload pattern)
    - tests/e2e/persistence.spec.ts (FULL FILE — the e2e harness pattern: imports, beforeEach wipe, helper functions)
    - playwright.config.ts (the webServer array with vite :5173 + wrangler :8788 from 07-01)
    - 07-06-SUMMARY (the IngestControl UI to drive in the happy-path)
  </read_first>

  <action>
    Replace `tests/e2e/ingestion/ssrf-matrix.spec.ts` Wave-0 stub with a REAL test body. Delete all test.todo placeholders. Build a table-driven `describe.each` over the SSRF attack vectors:

    - `const SSRF_VECTORS = [{ name, url, expectedReason, forbiddenContent }, ...]` with at least 10 entries: file scheme, gopher scheme, data scheme, dict scheme, ftp scheme, 169.254.169.254 metadata, metadata.google.internal, 10.0.0.1 private, 127.0.0.1 loopback, 100.64.0.1 CGNAT, 0x7f000001 hex encoding, ::ffff:127.0.0.1 IPv4-mapped IPv6.
    - For each: `test(`refuses ${name}`, async ({ request }) => { const res = await request.post("http://localhost:8788/api/ingest", { data: { url: vector.url } }); expect(res.status()).toBe(400); const body = await res.json(); expect(body.ok).toBe(false); expect(body.reason).toBe(vector.expectedReason); const text = await res.text(); expect(text).not.toContain(vector.forbiddenContent); });`.
    - Redirect-into-internal case: stand up a local mock server (or use a public redirector like httpbin.org/status/302?to=...) that returns 302 Location: http://169.254.169.254/. POST the redirector URL → expect 400 + ssrf-blocked-metadata or ssrf-blocked-private-ip. If a local mock is needed, use Playwright's `page.route` to intercept — but NOTE the SSRF matrix MUST run against real wrangler (NOT page.route shim) per D7-06, so prefer a public redirector OR use a dedicated test fixture under tests/e2e/ingestion/fixtures/redirect-to-metadata.html served via the wrangler pages public/ dir.
    - DNS-rebinding case: document the residual risk. The pure-Node spike can't simulate rebinding (you control the resolver); against real wrangler, the spike's cf.resolveOverride verdict (07-01) determines whether this case is "refused" or "documented residual risk". Add a test case that matches the spike outcome: if cf.resolveOverride works, assert refused; else, skip with a comment citing the documented TOCTOU residual per RESEARCH.md L756-759.

    Replace `tests/e2e/ingestion/happy-path.spec.ts` Wave-0 stub with a REAL test body. Delete all test.todo placeholders:

    - `import { test, expect } from "@playwright/test";`.
    - `test.beforeEach(async ({ page }) => { await page.goto("http://localhost:5173"); await page.evaluate(() => new Promise<void>((resolve) => { const req = indexedDB.deleteDatabase("lem-reader"); req.onsuccess = () => resolve(); req.onerror = () => resolve(); })); });` (wipe IndexedDB before each — the persistence.spec.ts L35-42 pattern).
    - `test("real URL → article opens in reader (SC#1)", async ({ page }) => { await page.goto("http://localhost:5173/#/"); await page.getByRole("textbox", { name: /url/i }).fill("<known-good publisher URL>"); await page.getByRole("button", { name: /add/i }).click(); await page.waitForURL(/#\/article\//); const headingCount = await page.getByRole("heading").count(); expect(headingCount).toBeGreaterThan(0); const paragraphCount = await page.locator("p").count(); expect(paragraphCount).toBeGreaterThan(0); });`
    - The known-good publisher URL is an open question — use a stable test fixture URL OR a real public article. If the test environment has no internet access, mock the /api/ingest response via page.route with a fixture CanonicalArticle — but PREFER the real-path test against wrangler. Document the choice in the spec header.
    - Optional but valuable: extend with a highlight round-trip step (create + reload + verify). If scope is tight, defer to manual-only verification (the round-trip anchor gate in 07-05 already covers this at unit level).
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/ingestion/ssrf-matrix.spec.ts tests/e2e/ingestion/happy-path.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "test.todo" tests/e2e/ingestion/ssrf-matrix.spec.ts` returns 0 (Wave-0 stub fully replaced).
    - `grep -c "test.todo" tests/e2e/ingestion/happy-path.spec.ts` returns 0 (Wave-0 stub fully replaced).
    - The SSRF matrix has ≥10 vector entries covering all 9 measures.
    - `grep -c "ssrf-blocked" tests/e2e/ingestion/ssrf-matrix.spec.ts` returns ≥10.
    - `grep -c "169.254.169.254" tests/e2e/ingestion/ssrf-matrix.spec.ts` returns ≥2 (direct + redirect-into-internal).
    - `grep -c "100.64" tests/e2e/ingestion/ssrf-matrix.spec.ts` returns ≥1 (CGNAT coverage).
    - The SSRF matrix asserts NO upstream body on refusal for at least the metadata case (Measure 7).
    - The happy-path spec fills the IngestControl URL input + clicks submit + waits for the ArticleView route.
    - `npx playwright test tests/e2e/ingestion/ssrf-matrix.spec.ts` exits 0 across chromium (SSRF matrix is single-engine; the engine matrix is the v1.0 reader-flow concern, not the backend's SSRF behavior).
    - `npx playwright test tests/e2e/ingestion/happy-path.spec.ts` exits 0 across chromium/firefox/webkit.
  </acceptance_criteria>

  <done>
    The SSRF regression matrix (SC#3) covers all 9 Pitfall 3 measures against real wrangler pages dev; every refusal produces a typed reason with NO upstream body leak. The happy-path e2e (SC#1) submits a real URL, lands in the reader, and proves the load-bearing invariant (ingested article reads like a fixture).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dexie v1→v3 migration snapshot (SC#5) + repo-wide dangerouslySetInnerHTML grep gate</name>
  <files>tests/e2e/ingestion/dexie-migration.spec.ts, package.json</files>
  <behavior>
    Dexie migration snapshot (SC#5):
    - Seed a v1/v2-shaped Dexie database (database name "lem-reader", version 2) with the v1.0 fixture snapshot: representative settings row ({ key: "reader-prefs", value: <ReaderSettings> }), a location row, a highlight row, a note row.
    - Close the seeded DB.
    - Open the app (page.goto http://localhost:5173/#/) — this triggers LemReaderDB construction with the new version(3) declaration, running the Dexie upgrade chain.
    - Assert the settings row is readable and parses through ReaderSettingsSchema.
    - Assert the location row is readable and addressable via the existing locationStore.
    - Assert the highlight row is readable and re-anchors (resolveQuoteSelector does not return "orphan" against the same-revision fixture).
    - Assert the note row is readable and attaches to its highlight.
    - Assert NO data loss: every seeded row appears in the post-upgrade DB.

    Repo-wide grep gate (SC#4 structural defense):
    - `npm run lint:no-danger` exits 0 today (zero matches).
    - The script is wired into the CI command sequence (or at minimum documented as a pre-merge step).
  </behavior>

  <read_first>
    - 07-RESEARCH.md §Gate 4 L975-979 (Dexie migration snapshot structure)
    - 07-VALIDATION.md §Gate 4 L100-101
    - 07-PATTERNS.md §tests/e2e/ingestion/dexie-migration.spec.ts L591-617 (the EXACT persistence.spec.ts seed mechanism)
    - tests/e2e/persistence.spec.ts (FULL FILE — the seedScrollingMode helper L71-110 + the beforeEach wipe L35-42)
    - src/persistence/db.ts (the v3 declaration from 07-02 + the v1/v2 byte-unchanged blocks)
    - package.json (the scripts section — add `lint:no-danger` after the existing `lint` entry)
    - eslint.config.js (confirm `react/no-danger: "error"` is enforced — per 07-PATTERNS.md L713 the flat-config already covers /server and /functions)
  </read_first>

  <action>
    Replace `tests/e2e/ingestion/dexie-migration.spec.ts` Wave-0 stub with a REAL test body. Delete all test.todo placeholders. Copy the persistence.spec.ts seed pattern (L71-110) verbatim in shape:

    - `import { test, expect } from "@playwright/test";`.
    - Build a `seedV1Snapshot(page)` helper that opens IndexedDB "lem-reader" at version 2 (NOT the new db.ts which would declare v3), writes representative rows to the settings/location/highlights/notes stores, and resolves on tx.oncomplete. Use the v1.0 fixture snapshot: a real ReaderSettings object (DEFAULT_SETTINGS), a LocationRecord for a known fixture id+revision, a HighlightRecord, a NoteRecord. The helper MUST construct the database at the v2 shape (the same store declarations as db.ts L102-106) so the upgrade path is exercised when the app opens.
    - `test("v1 fixture snapshot intact after v3 upgrade (SC#5)", async ({ page }) => { await seedV1Snapshot(page); await page.goto("http://localhost:5173/#/"); await page.waitForLoadState("domcontentloaded"); // Give Dexie time to upgrade await page.waitForFunction(() => "dexie-migrated" in window || true, null, { timeout: 5000 }).catch(() => {}); const settings = await page.evaluate(() => { return new Promise((resolve) => { const req = indexedDB.open("lem-reader"); req.onsuccess = () => { const db = req.result; if (!db.objectStoreNames.contains("settings")) { resolve(null); return; } const tx = db.transaction("settings", "readonly"); const store = tx.objectStore("settings"); const getReq = store.get("reader-prefs"); getReq.onsuccess = () => resolve(getReq.result); getReq.onerror = () => resolve(null); }; }); }); expect(settings).not.toBeNull(); expect(settings.key).toBe("reader-prefs"); // Similarly assert location, highlights, notes rows present });`.
    - Add a parallel read-back for each store (location, highlights, notes) and assert row counts match the seeded snapshot (no data loss).

    Add the repo-wide grep gate as a package.json script:
    - Edit package.json scripts block: insert `"lint:no-danger": "bash -c 'if grep -rn \"dangerouslySetInnerHTML\" src/ server/ functions/; then echo \"VIOLATION: dangerouslySetInnerHTML found\"; exit 1; fi'"` after the existing `"lint": "eslint ."` line. (Use bash -c with proper escaping to satisfy the cross-platform shell; on Windows-only environments use a Node-based grep — confirm target platform with the existing scripts which use npx/vite, indicating a Unix-friendly repo.)
    - Optionally add `&& npm run lint:no-danger` to the `test` script if the CI command should include it in the full gate; OR document it as a separate CI step (the project's existing CI workflow file should be checked — if absent, leave as a script-only addition).

    Run the migration spec end-to-end and confirm: the v1/v2 seeded rows are intact, the v3 upgrade did not throw, and the app loads normally.
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/ingestion/dexie-migration.spec.ts && npm run lint:no-danger</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "test.todo" tests/e2e/ingestion/dexie-migration.spec.ts` returns 0 (Wave-0 stub fully replaced).
    - `grep -c "seedV1Snapshot" tests/e2e/ingestion/dexie-migration.spec.ts` returns ≥1.
    - `grep -c "indexedDB" tests/e2e/ingestion/dexie-migration.spec.ts` returns ≥3 (seed + read-back for multiple stores).
    - `grep -c "reader-prefs" tests/e2e/ingestion/dexie-migration.spec.ts` returns ≥1.
    - `grep -c "lint:no-danger" package.json` returns 1.
    - `grep -c "dangerouslySetInnerHTML" package.json` returns ≥1 (the script itself references the literal).
    - `npm run lint:no-danger` exits 0 (zero matches in src/ server/ functions/).
    - `npx playwright test tests/e2e/ingestion/dexie-migration.spec.ts` exits 0 on chromium — every seeded v1/v2 row is intact after the v3 upgrade.
  </acceptance_criteria>

  <done>
    The Dexie v1→v3 migration snapshot (SC#5) proves every v1.0 row is intact after the upgrade. The repo-wide `dangerouslySetInnerHTML` grep gate (SC#4 structural defense) is wired as `npm run lint:no-danger` and exits 0 today.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → wrangler pages dev (:8788) | e2e exercises the real workerd runtime |
| Test runner → IndexedDB (chromium) | Dexie migration runs against real browser IndexedDB |
| Repo filesystem → grep gate | Source-tree scan for the forbidden literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-31 | Tampering | SSRF matrix misses an attack vector (regression drift) | mitigate | Matrix is table-driven over a vector corpus; adding a vector is one entry. CI runs the full matrix on every commit. |
| T-7-32 | Information Disclosure | Upstream body leaks on SSRF refusal (Measure 7 violation) | mitigate | Each matrix case asserts response.text() does NOT contain forbidden content from the upstream target. |
| T-7-33 | Tampering | Future PR re-introduces dangerouslySetInnerHTML (regression) | mitigate | `npm run lint:no-danger` exits 1 on any match; wired as a CI-runnable script independent of ESLint scope. |
| T-7-34 | Tampering | Dexie v3 upgrade silently drops v1/v2 rows (Pitfall 9 regression) | mitigate | Migration snapshot test asserts every seeded row is intact + addressable; runs in CI. |
| T-7-35 | Repudiation | SSRF matrix passes locally but fails in CI due to DNS differences | mitigate | Matrix runs against REAL wrangler pages dev (per D7-06 — NOT a Node shim); the local vs CI workerd behavior matches. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/ingestion/` exits 0 across the engine matrix.
- `npm run lint:no-danger` exits 0.
- All four phase-exit gates are GREEN:
  - SC#1 round-trip anchor (07-05 normalization.spec.ts) ✓
  - SC#3 SSRF matrix (this plan Task 1) ✓
  - SC#4 mXSS suite (07-04 mxss.spec.ts) + lint:no-danger (this plan Task 2) ✓
  - SC#5 Dexie migration snapshot (this plan Task 2) ✓
- The full phase suite `npm run test` (test:unit --run && test:e2e) exits 0.
</verification>

<success_criteria>
- SSRF regression matrix (SC#3) covers all 9 Pitfall 3 measures against real wrangler pages dev.
- Happy-path e2e (SC#1) proves the load-bearing invariant: an ingested article reads identically to a fixture.
- Dexie v1→v3 migration snapshot (SC#5) proves no data loss on upgrade.
- The repo-wide dangerouslySetInnerHTML grep gate (SC#4 structural defense) is wired and exits 0.
- All four phase-exit gates are GREEN.
- The full `npm run test` suite exits 0 — the phase is honestly done.
</success_criteria>

<output>
Create `.planning/phases/07-ingestion-substrate/07-07-SUMMARY.md` when done. The SUMMARY MUST include a "## Phase Gate Status" section recording the GREEN/RED state of each of the four phase-exit gates + the full-suite exit code (mirroring the v1.0 Phase 6 honest-suite precedent).
</output>

<artifacts_this_phase_produces>
## Artifacts this plan (07-07) produces

**Tests (replacing Wave-0 stubs):**
- `tests/e2e/ingestion/ssrf-matrix.spec.ts` — real SSRF regression matrix (SC#3) covering all 9 Pitfall 3 measures
- `tests/e2e/ingestion/happy-path.spec.ts` — real happy-path e2e (SC#1) — URL → reader
- `tests/e2e/ingestion/dexie-migration.spec.ts` — real v1→v3 Dexie migration snapshot (SC#5)

**CI gate:**
- `package.json` `lint:no-danger` script — repo-wide grep for `dangerouslySetInnerHTML` in src/ server/ functions/ (SC#4 structural defense)

---

## Phase 7 — Complete artifact inventory (all 7 plans)

This is the **single canonical list** of every symbol this phase produces. The plan-review-convergence source-grounding pass reads this section.

**New server-only deps (package.json dependencies):**
- `@mozilla/readability@0.6.0`
- `isomorphic-dompurify@3.22.0`
- `jsdom@30.0.1`
- `ip-address@10.5.0`

**New devDeps (package.json devDependencies):**
- `wrangler@4.120.1`
- `@cloudflare/vite-plugin@1.51.2`
- `@cloudflare/workers-types`
- (optional) `fake-indexeddb` — if added by 07-02 Task 2 for the v3-smoke unit test

**New configuration files:**
- `wrangler.toml` — name, compat_date="2024-09-23", compatibility_flags=["nodejs_compat"], pages_build_output_dir

**Extended configuration files:**
- `vite.config.ts` — Option A (cloudflare() plugin) OR Option B (server.proxy :8788), spike-decided
- `vitest.config.ts` — workspace with `unit` + `server` projects
- `playwright.config.ts` — `webServer` array (vite :5173 + wrangler :8788)
- `package.json` — new deps + `lint:no-danger` script

**New server modules (`/server/`):**
- `server/limits.ts` — REQUEST_TIMEOUT_MS, MAX_RESPONSE_BYTES, MAX_REDIRECTS, ALLOWED_CONTENT_TYPES, PRIVATE_RANGES, METADATA_HOSTNAMES
- `server/errors.ts` — IngestionError class
- `server/safeFetch.ts` — safeFetch(), FetchedContent interface (9 SSRF measures)
- `server/confidence.ts` — deriveConfidence(), ConfidenceResult interface
- `server/slugify.ts` — slugifyUrl()
- `server/htmlToBlocks.ts` — SANITIZE_CONFIG, sanitizeExtractedHtml(), htmlToBlocks(), extractAndNormalize()
- `server/ingest.ts` — ingest(), assertRoundTripAnchor()

**New edge function:**
- `functions/api/ingest.ts` — onRequest(context) adapter

**New client modules (`src/ingestion/`):**
- `src/ingestion/types.ts` — IngestionRequestSchema, IngestionResponseSchema, IngestionFailureReasonEnum, inferred types
- `src/ingestion/IngestionClient.ts` — ingestUrl(), ingestHtml(), IngestionError (client class), private ingest()
- `src/ingestion/LibrarySource.ts` — DexieLibrarySource class (list/open/save/has/remove), dexieLibrarySource singleton, compositeLibraryRepository
- `src/ingestion/IngestControl.tsx` — minimal URL + paste textarea control, mapReasonToCopy()

**Extended schema:**
- `src/content/schema.ts` — ArticleSourceSchema, IngestionMetaSchema, ArticleSource type, IngestionMeta type, Provenance.sourceUrl.optional(), ArticleSchema.ingestionMeta.optional()

**Extended persistence:**
- `src/persistence/db.ts` — version(3) append (source, addedAt indexes on articles); v1/v2 byte-unchanged; widened articles Table type

**Extended repository:**
- `src/content/repository.ts` — listArticles/openArticle delegate to compositeLibraryRepository (D7-02 swap)

**Extended routes:**
- `src/routes/FixtureList.tsx` — mounts <IngestControl /> above the article list

**New test files (8 Wave-0 stubs + spike, all replaced with real bodies by 07-07):**
- `tests/unit/server/spike-jsdom-workers.spec.ts` (07-01, kept)
- `tests/unit/server/safe-fetch.spec.ts` (07-03, new)
- `tests/unit/server/mxss.spec.ts` (07-04)
- `tests/unit/server/extraction.spec.ts` (07-04)
- `tests/unit/server/normalization.spec.ts` (07-05)
- `tests/unit/server/confidence.spec.ts` (07-03)
- `tests/unit/server/slugify.spec.ts` (07-03)
- `tests/e2e/ingestion/ssrf-matrix.spec.ts` (07-07)
- `tests/e2e/ingestion/happy-path.spec.ts` (07-07)
- `tests/e2e/ingestion/dexie-migration.spec.ts` (07-07)
- (optional new schema test) `tests/unit/ingestion-schema.test.ts` (07-02)

**Requirement coverage:** ING-01 (07-02, 03, 04, 05, 06, 07), ING-02 (07-02, 04, 06), ING-06 (07-03, 05, 06), ING-07 (07-01, 02, 04, 07), ING-08 (07-01, 03, 07). Every ID in at least one plan. ✓

**Four phase-exit gates:**
- SC#1 round-trip anchor — 07-05 `tests/unit/server/normalization.spec.ts` + inline `assertRoundTripAnchor` in `server/ingest.ts`
- SC#3 SSRF matrix — 07-07 `tests/e2e/ingestion/ssrf-matrix.spec.ts`
- SC#4 mXSS suite — 07-04 `tests/unit/server/mxss.spec.ts` + 07-07 `lint:no-danger` script
- SC#5 Dexie migration — 07-07 `tests/e2e/ingestion/dexie-migration.spec.ts`
</artifacts_this_phase_produces>
