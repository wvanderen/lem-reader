---
phase: 12-epub-intake
plan: 06
type: execute
wave: 5
depends_on: ["12-05"]
files_modified:
  - src/routes/ArticleView.tsx
  - src/app.css
  - tests/e2e/epub-intake.spec.ts
  - tests/e2e/a11y.spec.ts
  - tests/unit/review-filter.test.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "Epub-chapter articles show a calm context line 'Book Title · Chapter N of M' in the reader header; ordinary articles are unchanged (D12-08)"
    - "A keyboard-reachable 'Next chapter' affordance appears exactly at chapter end in BOTH modes — scrolling: after the last block in flow; paginated: rendered only while the reader is on the final page; 'Previous chapter' is reachable from chapter start symmetrically; no permanent prev/next chrome, page-turn keys untouched (D12-05)"
    - "A chapter opens, paginates, annotates, and restores location identically to every other article — the v1.0 substrate is untouched (SC#2)"
    - "Reopening a book resumes at the last-read chapter; book-level progress derives from per-chapter locations; cross-chapter navigation works end to end (SC#3)"
    - "DRM/corrupt/empty/over-cap uploads refuse calmly with zero library side effects — no epub-badged rows appear after any refusal"
  artifacts:
    - path: "src/routes/ArticleView.tsx"
      provides: "the chapter context line + end-of-chapter navigation links, epub-chapter only"
      contains: "chapter-nav"
    - path: "tests/e2e/epub-intake.spec.ts"
      provides: "SC#2 chapter-reading identity + SC#3 resume/progress/cross-chapter + refusal no-side-effect e2e"
      min_lines: 250
    - path: "tests/unit/review-filter.test.ts"
      provides: "Pitfall 8 review-panel strengthen-only case — chapter-bearing library fixture over deriveReviewSections"
      contains: "epub-chapter"
  key_links:
    - from: "src/routes/ArticleView.tsx"
      to: "src/persistence/booksStore.ts"
      via: "getBook(bookId) lookup for the context line — tolerant of a missing record (renders nothing, never crashes)"
      pattern: "getBook"
    - from: "src/routes/ArticleView.tsx"
      to: "src/ingestion/library/bookProgress.ts"
      via: "chapterOrdinal supplies the N in 'Chapter N of M'"
      pattern: "chapterOrdinal"
---

<objective>
The reader-side half of EPUB intake: the D12-08 chapter context line and the D12-05 end-of-chapter navigation affordance (calm, keyboard-reachable, mode-correct, never permanent chrome), then the phase's behavior-exit e2e — SC#2 (chapter reading identity across both modes with annotation + restore) and SC#3 (cross-chapter navigation, book-level progress, reopen-resume), plus the refusal no-side-effect gate and the a11y extension for the new surfaces.

Purpose: SC#2 is the load-bearing "a chapter IS an article" proof; D12-05's placement discipline (one affordance, exactly where needed) protects the page-turn spatial model that defines the product.
Output: Chapter chrome shipped; epub-intake.spec.ts complete; a11y coverage extended.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@src/routes/ArticleView.tsx
@src/persistence/booksStore.ts
@src/ingestion/library/bookProgress.ts
@tests/e2e/epub-intake.spec.ts
@tests/e2e/a11y.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chapter context line + end-of-chapter navigation (D12-08 + D12-05)</name>
  <files>src/routes/ArticleView.tsx, src/app.css</files>
  <read_first>src/routes/ArticleView.tsx (the header block L1408-1439 mount point; the paginated surface region L1474-1496; ArticleBody mount L1499; how current-page state is tracked for the paginated mode; the article-load effect), src/persistence/booksStore.ts (getBook), src/ingestion/library/bookProgress.ts (chapterOrdinal), src/app.css (calm link + meta styles + reduced-motion block), 12-RESEARCH.md §Pattern 8 + Open Question 2</read_first>
  <action>1. Context line (D12-08): for articles with ingestionMeta?.source === "epub-chapter" and a bookId, load the book via booksStore.getBook in the article-load effect (tolerant: a missing record renders no line — never an error state; ordinary articles take zero new code paths). Render below the existing .meta provenance paragraph: a p.meta.book-context with the literal middle-dot-separated shape "Book Title · Chapter N of M" (N = chapterOrdinal(book, article.id), M = book.chapterArticleIds.length; the separator is the U+00B7 middle dot with surrounding spaces, matching the research sketch). No book-progress indicator inside the reader (progress lives on the library row).
   2. End-of-chapter links (D12-05; planner resolution of Open Question 2 — placement AFTER the surface, not inside fragments):
   - Derive nextChapterId = book.chapterArticleIds[chapterIndex + 1] and prevChapterId = book.chapterArticleIds[chapterIndex - 1] (chapterIndex from ingestionMeta; undefined at the boundaries → no link).
   - Scrolling mode: a nav.chapter-nav AFTER the ArticleBody in the article flow — an anchor "Next chapter" (a href={`#/article/${nextChapterId}`} class="chapter-next") with the next chapter's title in a lighter span (title sourced from the loaded chapter articles or the book record's TOC labels when available; fallback to the bare "Next chapter" text). Symmetric "Previous chapter" anchor BEFORE the body (chapter start reachability).
   - Paginated mode: the same nav rendered AFTER the paginated surface element (ArticleView-owned chrome — geometrically stable, never inside page fragments), with the Next link mounted ONLY while the current page is the last page, and the Previous link ONLY while on the first page (conditional render on the existing current-page state; never permanent chrome). Plain hash-assignment navigation (the plain a href precedent — no router API).
   - Keyboard: native focusable anchors, Tab/Enter only; no shortcut registration; page-turn key handlers untouched.
   3. src/app.css — .chapter-nav styling: calm spacing above the measure, muted link color with visible focus state (the existing link focus tokens), the title span in a lighter tone; placement rules per mode; nothing animated (no transition needed — a static disclosure, trivially reduced-motion safe).</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit</automated>
  </verify>
  <acceptance_criteria>
    - ArticleView renders the context line only when ingestionMeta.source is epub-chapter AND the book record resolves; the literal shape "· Chapter" appears with the middle-dot separator; ordinary-article snapshots/tests unchanged
    - The Next chapter anchor exists with href matching the #/article/<nextId> pattern; in paginated mode it is absent on non-final pages and present on the final page (state-conditional render)
    - The Previous chapter anchor is reachable at chapter start in both modes (before the body in scrolling; first page in paginated)
    - No new keyboard listener is registered by this feature (page-turn keys untouched — the arrow-key/PageDown handlers are unmodified)
    - A missing book record renders neither context line nor nav without erroring (tolerant-lookup test or existing fallback behavior assertion)
    - npx tsc --noEmit exits 0; the full unit suite stays green
  </acceptance_criteria>
  <done>Chapters read with calm book context and exactly-one-where-needed chapter navigation in both modes; the substrate is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SC#2 + SC#3 e2e — chapter identity, resume, progress, cross-chapter nav</name>
  <files>tests/e2e/epub-intake.spec.ts</files>
  <read_first>tests/e2e/epub-intake.spec.ts (the 12-05 cases — extend, never weaken), tests/e2e/pdf-intake.spec.ts (the reading-identity pattern: open → both modes → highlight → reload → restore, with the persistence.spec tolerances), tests/e2e/persistence.spec.ts (location-restore identity assertions), src/routes/ArticleView.tsx (Task 1 state), tests/unit/server/epub-fixtures.ts</read_first>
  <action>Extend tests/e2e/epub-intake.spec.ts with the SC#2/SC#3 battery (strengthen-only — the 12-05 cases stay intact):
   1. Chapter reading identity (SC#2): upload validBookEpub3 → open chapter 2 → assert the context line "The Synthetic Book · Chapter 2 of 4" → in scrolling mode scroll to a mid-chapter offset, create a highlight → reload → the location restores within the persistence.spec tolerances AND the highlight renders at its offset → switch to paginated mode → the article paginates (page count > 1), the highlight still renders, and location restore works after another reload → switch back to scrolling (two-mode reading at chapter granularity, mode toggle + M shortcut per existing specs).
   2. Cross-chapter navigation (SC#3): in scrolling mode scroll to the very end of chapter 1 → the "Next chapter" link is keyboard-focusable at the flow end → activate it → chapter 2 opens with its context line → in paginated mode turn to the final page of a chapter → the Next link appears on that page only → activate → next chapter; Previous link reachable from chapter start (first page / flow top) and returns.
   3. Reopen-resume (SC#3): read chapter 3 partway → navigate to the library → the strip entry reads "The Synthetic Book — Chapter 3 of 4" and Resume opens chapter 3 at the saved offset → re-skim chapter 1 briefly → return → the strip now resumes chapter 1 (D12-07 last-savedAt wins, not first-unfinished).
   4. Book progress advance (SC#3): finish chapter 1 fully (offset at end / final page) → the library book hairline reflects 1/4; finish all four → the book leaves the continue strip (finished state, the FINISHED_THRESHOLD convention).
   5. Refusal no-side-effect gates: upload drmAdeptBook → the calm protected copy appears in .status → the library row count is UNCHANGED (fixtures-baseline before/after — the 11-05 pattern) → repeat for corruptNotEpub (unreadable copy) and emptyBook (no-readable-chapters copy) → upload an over-cap .epub (a File whose size exceeds EPUB_MAX_BYTES built from padded bytes) → the too-large copy appears and NO network request to /api/ingest was issued (page.on("request") guard — the picker refused before reading; the 11-04 earliest-enforcement proof).</action>
  <verify>
    <automated>npx playwright test tests/e2e/epub-intake.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The identity case proves location + highlight restore in BOTH modes for an epub-chapter with the same tolerances as persistence.spec
    - The context-line assertion pins the exact "· Chapter 2 of 4" shape
    - The paginated Next link is asserted ABSENT on a non-final page and PRESENT on the final page
    - The re-skim case asserts the strip resumes the LAST-read chapter (chapter 1 after re-skimming), not the first unfinished
    - Every refusal case asserts both the calm copy and an unchanged library row count; the over-cap case asserts zero ingest requests
    - npx playwright test tests/e2e/epub-intake.spec.ts exits 0 on all three engines
  </acceptance_criteria>
  <done>SC#2 and SC#3 proven in real browsers; every refusal is calm, typed, and side-effect-free at the surface.</done>
</task>

<task type="auto">
  <name>Task 3: a11y extension + Pitfall 8 review-panel strengthen-only case — disclosure semantics, context line, chapter nav keyboard access</name>
  <files>tests/e2e/a11y.spec.ts, tests/unit/review-filter.test.ts</files>
  <read_first>tests/e2e/a11y.spec.ts (the axe harness structure + existing view coverage), tests/e2e/library/browse-open.spec.ts (library-with-seeded-data helpers), tests/unit/server/epub-fixtures.ts, src/routes/review/reviewFilter.ts (deriveReviewSections join + tri-state classification), tests/unit/review-filter.test.ts (the fixed-corpus count assertions Pitfall 8 warns about), 12-RESEARCH.md §Pitfall 8</read_first>
  <action>Extend tests/e2e/a11y.spec.ts (strengthen-only — existing scans untouched): 1. Library-with-book scan: seed a book (upload via the fixture builder), run the existing axe analyzer over the library view collapsed AND expanded — zero new violations. 2. Keyboard walkthrough: Tab reaches the book chevron (aria-expanded toggles via keyboard), the Resume link, the chapter sub-row open links, and the Remove button in a sane order; focus-visible styling present (the existing focus-token assertions). 3. Chapter reading scan: open an epub-chapter in both modes — axe clean on the context line and the chapter-nav region; the Next/Previous links are Tab-reachable and Enter-activatable (assert navigation occurs); heading order preserved (h1 chapter title; the context line is a p, never a heading). 4. Reduced-motion: the disclosure animation is gated (assert the CSS media guard exists via the computed style or the existing reduced-motion spec's approach — visual-motion assertions stay in reduced-motion.spec.ts's domain; here assert only structural keyboard/axe properties). 5. Pitfall 8 review-panel case (the deriveReviewSections half of the pitfall — the resolveImportPlan/manifest half is covered by 12-07): extend tests/unit/review-filter.test.ts strengthen-only with a chapter-bearing library fixture — highlights on chapter articles carrying ingestionMeta source epub-chapter flow through the deriveReviewSections join and land in sections with counts that remain pinned per-article (chapters are ordinary articles to the review panel; a many-chapter book multiplies highlight rows exactly as the same count of standalone articles would — assertions added, none loosened).</action>
  <verify>
    <automated>npx vitest run tests/unit/review-filter.test.ts && npx playwright test tests/e2e/a11y.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The extended a11y spec covers library-with-book (collapsed + expanded) and chapter views in both modes with zero axe violations
    - The keyboard walkthrough asserts aria-expanded state changes on chevron activation via keyboard
    - Next/Previous chapter links are proven Tab-reachable and Enter-activatable in both modes
    - The context line is a paragraph, not a heading (heading-order preservation for the h1 chapter title)
    - review-filter.test.ts carries at least one chapter-bearing fixture case with pinned section counts; no pre-existing assertion is loosened or deleted
    - npx vitest run tests/unit/review-filter.test.ts exits 0; npx playwright test tests/e2e/a11y.spec.ts exits 0
  </acceptance_criteria>
  <done>The new book surfaces meet the project's a11y bar with automated proof, and the review panel is proven chapter-flood-safe strengthen-only; manual SR flows remain Phase 13's ACPT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted book/article rows → reader chrome | context-line + nav derive from Zod-validated reads only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Elevation/Spoofing | chapter nav as an untrusted jump target | accept | links are internal hash routes to locally-validated article ids; no external URLs involved (the renderer never emits raw HTML) |
| T-12-16 | Information Disclosure | context-line data exposure | accept | book title/ordinal are reader-local library data; no network calls added by this feature |
</threat_model>

<verification>
Unit suite green; epub-intake e2e complete (SC#1 from 12-05 + SC#2/SC#3 + refusals) on three engines; a11y extended and green.
</verification>

<success_criteria>
Chapters are indistinguishable from articles to the reading engine (proven, not assumed); cross-chapter navigation is calm and keyboard-reachable in both modes; reopen-resume and book progress work; all four refusal classes surface calmly with zero side effects.
</success_criteria>

## Artifacts this phase produces

- ArticleView chapter chrome: the .book-context context line (D12-08) + the nav.chapter-nav end-of-chapter/previous links with mode-correct conditional rendering (D12-05)
- app.css .chapter-nav + .book-context styles
- tests/e2e/epub-intake.spec.ts SC#2/SC#3 + refusal no-side-effect cases; tests/e2e/a11y.spec.ts book/chapter coverage

<output>
Create `.planning/phases/12-epub-intake/12-06-SUMMARY.md` when done
</output>
