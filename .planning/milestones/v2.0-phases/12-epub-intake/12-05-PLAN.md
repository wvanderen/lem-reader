---
phase: 12-epub-intake
plan: 05
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified:
  - src/ingestion/library/bookProgress.ts
  - src/ingestion/library/BookRow.tsx
  - src/ingestion/library/BookRemoveConfirm.tsx
  - src/ingestion/library/LibraryRow.tsx
  - src/ingestion/library/LibraryView.tsx
  - src/ingestion/library/ContinueReadingStrip.tsx
  - src/ingestion/library/libraryFilter.ts
  - src/app.css
  - tests/unit/library/book-progress.test.ts
  - tests/unit/library/book-filter.test.ts
  - tests/unit/library/book-row.test.tsx
  - tests/e2e/epub-intake.spec.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "The library shows each book as ONE expandable row: collapsed shows title/author + book progress hairline + Resume action; expanding reveals chapter sub-rows reusing the LibraryRow anatomy (D12-01); chapters never render as top-level rows"
    - "Continue-Reading shows ONE entry per in-progress book — 'BookTitle — Chapter N of M' resuming the last-read chapter; chapters never double-list (D12-02)"
    - "Book progress = chapters-finished ratio using the exported FINISHED_THRESHOLD — count(chapter locations at ≥98% of chapter text) ÷ chapterArticleIds.length (D12-03)"
    - "Resume opens the chapter whose LocationRecord has the latest savedAt within the book (D12-07)"
    - "Tags live on the Book record: tagging a book surfaces the book row in tag filtering (never 40 chapter rows); library search matches book title AND chapter titles (D12-04)"
    - "A book with skippedChapterCount > 0 discloses it calmly in the expanded grouping — '2 chapters could not be read' (D12-11)"
    - "Removing a book removes its chapters and cascades highlights/notes/locations in ONE transaction, gated by a BookRemoveConfirm clone whose Proceed onClick is the sole removeBook call site"
  artifacts:
    - path: "src/ingestion/library/bookProgress.ts"
      provides: "pure derivations — deriveBookProgress (D12-03) + resolveResumeChapterId (D12-07)"
      exports: ["deriveBookProgress", "resolveResumeChapterId"]
    - path: "src/ingestion/library/BookRow.tsx"
      provides: "the expandable book row — aria-expanded disclosure, chapter sub-list, skip disclosure, Resume action, book TagEntry, remove trigger"
      min_lines: 120
    - path: "src/ingestion/library/BookRemoveConfirm.tsx"
      provides: "the book-scoped destructive confirm (structural RemoveConfirm clone)"
      min_lines: 60
    - path: "tests/e2e/epub-intake.spec.ts"
      provides: "SC#1 e2e — upload → grouping → expand → disclosure → tag/search → remove-cascade"
      min_lines: 120
  key_links:
    - from: "src/ingestion/library/bookProgress.ts"
      to: "src/ingestion/library/ContinueReadingStrip.tsx"
      via: "imports the exported FINISHED_THRESHOLD (0.98) — never forks the constant"
      pattern: "FINISHED_THRESHOLD"
    - from: "src/ingestion/library/BookRow.tsx"
      to: "src/persistence/booksStore.ts"
      via: "chapter sub-rows + book tags + remove all flow through the store seam; removeBook fires ONLY from BookRemoveConfirm Proceed"
      pattern: "removeBook"
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "src/ingestion/library/BookRow.tsx"
      via: "standalone articles vs book groups partition — chapter articles (ingestionMeta.bookId) never render top-level"
      pattern: "bookId"
---

<objective>
The library becomes book-aware (SC#1): a pure progress/resume derivation module over existing LocationRecords (zero new measurement), the expandable BookRow with native disclosure semantics and chapter sub-rows, the book-level Continue-Reading entry, book+chapter search and book-surface tag filtering, the skip disclosure, and the book-scoped destructive confirm. The e2e epub-intake.spec.ts lands here (SC#1: upload → grouping → expand → disclose → tag/search → remove cascade).

Purpose: D12-01..D12-04 + D12-06 + D12-11 are all library-surface decisions — this plan is their whole implementation. The library's expandable grouping IS the Phase 12 TOC (D12-06).
Output: Grouped library green at unit + e2e level; chapters read/open through the untouched article surface.

Scope note (checker-reviewed): 12 files modified — one cohesive library-UI cluster (BookRow/strip/filter/confirm + LibraryView/LibraryRow widening + their unit and e2e specs travel together; splitting would sever the grouping contract from its proofs). Accepted as-is; split only if execution quality degrades.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@src/ingestion/library/LibraryView.tsx
@src/ingestion/library/LibraryRow.tsx
@src/ingestion/library/ContinueReadingStrip.tsx
@src/ingestion/library/libraryFilter.ts
@src/ingestion/library/RemoveConfirm.tsx
@src/reader/TagEntry.tsx
@src/persistence/booksStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: bookProgress derivations + BookRow + LibraryView grouping</name>
  <files>src/ingestion/library/bookProgress.ts, src/ingestion/library/BookRow.tsx, src/ingestion/library/LibraryRow.tsx, src/ingestion/library/LibraryView.tsx, src/app.css, tests/unit/library/book-progress.test.ts, tests/unit/library/book-row.test.tsx</files>
  <read_first>src/ingestion/library/ContinueReadingStrip.tsx (FINISHED_THRESHOLD export L46 + the latestByArticle fold L72-78), src/ingestion/library/LibraryRow.tsx (the row anatomy L54-99: h2 id pattern, .meta, SourceBadge, hairline, open link), src/ingestion/library/LibraryView.tsx (list render + data loading), src/persistence/locationStore.ts (LocationRecord shape + loadAllLocations), src/content/schema.ts (Book + CanonicalArticle), src/persistence/booksStore.ts, src/reader/TagEntry.tsx (props — reuse for book tags), src/app.css (library-row styles + the reduced-motion block), 12-RESEARCH.md §Pattern 7</read_first>
  <action>1. src/ingestion/library/bookProgress.ts — NEW pure module (no React, no Dexie imports; types only):
   - deriveBookProgress(book: Book, locations: LocationRecord[], textLengthOf: (articleId: string) => number | undefined): number — count chapters whose location exists with graphemeOffset >= FINISHED_THRESHOLD × textLengthOf(chapterId) (missing text length → unfinished), divided by book.chapterArticleIds.length; 0 when the list is empty. Import FINISHED_THRESHOLD from ./ContinueReadingStrip (the exported single source of truth — never fork the constant; this module lives beside it precisely to avoid a persistence→ingestion cycle).
   - resolveResumeChapterId(book: Book, locations: LocationRecord[]): string | null — the chapter id (within chapterArticleIds) whose LocationRecord has the max savedAt; null when no chapter has a location (D12-07 — last-read wins, even mid-chapter or re-skimmed earlier).
   - chapterOrdinal(book: Book, chapterId: string): number — 1-based position within chapterArticleIds (the "Chapter N" source; callers render "of M" with chapterArticleIds.length).
  2. src/ingestion/library/LibraryRow.tsx — widen with an optional headingLevel?: 2 | 3 prop (default 2) rendering the title heading element dynamically (h2 default byte-stable for existing rows; h3 inside book groups preserves heading order). No other behavior change.
  3. src/ingestion/library/BookRow.tsx — NEW: a top-level li.book-row containing article.book-card with: h2 id={`title-${book.id}`} (book title); .meta authors joined; SourceBadge (the Book variant); the book progress hairline (ratio via deriveBookProgress — reuse the ProgressHairline rendering LibraryRow uses); a Resume link (a href={`#/article/${resolveResumeChapterId(...)}`} aria-labelledby={`title-${book.id}`}) shown when a resume chapter exists and progress < 1; and a real chevron button aria-expanded={open} aria-controls={`chapters-${book.id}`} that toggles open state (row-click does NOT toggle — two gestures, two targets). The controlled region (div id=`chapters-${book.id}`) holds: the chapter ul — one li per chapter article, each rendering the LibraryRow anatomy at headingLevel 3 (SourceBadge, per-chapter hairline from that chapter's location, open link aria-labelledby the chapter's h3 id); chapters are listed from the LIVE bookId index (db.articles where bookId equals the book id, ordered to match chapterArticleIds where present — tolerant of partial imports: rows missing from the record simply don't render, rows extra to it render appended) — planner resolution for orphan tolerance; the skip disclosure p.meta when skippedChapterCount > 0 ("1 chapter could not be read" / "N chapters could not be read"); the book TagEntry (reuse src/reader/TagEntry with book.tags, persisting via setBookTags); and a calm Remove book button opening BookRemoveConfirm (Task 2).
   4. src/ingestion/library/LibraryView.tsx — partition the loaded article list: articles with ingestionMeta?.bookId are chapter members (never top-level); standalone articles render as today; book groups come from listBooks() (+ chapter rows fetched via the bookId index — extend the existing Promise.all load). Merge standalone articles and books into ONE list ordered by addedAt (the existing default-sort semantics extended to books). Route a book removeTarget state to BookRemoveConfirm (the RemoveConfirm wiring precedent).
   5. src/app.css — .book-row card styles (reuse .library-row tokens); the chevron rotate + region height transition gated behind @media (prefers-reduced-motion: no-preference); chapter sub-list spacing INSIDE the book li (the 08-05 direct-child lesson — .library-list > li counts keep working because sub-rows are nested, not siblings).
   6. tests/unit/library/book-progress.test.ts — pure cases: finished chapter at exactly 0.98 × length counts (≥ boundary); unfinished mid-chapter; ratio over 3 chapters; empty locations → 0; missing text length → unfinished; resume picks max savedAt (incl. an earlier chapter re-skimmed later winning); no locations → null; chapterOrdinal 1-based.
   7. tests/unit/library/book-row.test.tsx (RTL, the existing library component-test harness): aria-expanded toggles the region on chevron click; row-click does NOT toggle; Resume link href targets the resume chapter id; skippedChapterCount 2 renders "2 chapters could not be read"; chapter sub-rows render h3 headings; TagEntry present in the expanded region.</action>
  <verify>
    <automated>npx vitest run tests/unit/library/book-progress.test.ts tests/unit/library/book-row.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - bookProgress.ts exports deriveBookProgress, resolveResumeChapterId, chapterOrdinal and imports FINISHED_THRESHOLD from ./ContinueReadingStrip (no local 0.98 literal)
    - BookRow renders button aria-expanded toggling a region whose id matches aria-controls; the test asserts both the toggle and that row-click leaves it closed
    - LibraryRow accepts headingLevel and defaults to 2 (existing library-row tests stay green unmodified)
    - LibraryView groups by bookId — chapter articles never render as top-level li children of .library-list
    - The skip-disclosure string appears only when skippedChapterCount > 0 (test asserts absence at 0)
    - app.css contains the prefers-reduced-motion: no-preference gate around the book disclosure animation
    - npx vitest run tests/unit/library/book-progress.test.ts tests/unit/library/book-row.test.tsx exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Books group, expand, disclose, resume, and tag in the library with correct heading order and reduced-motion safety — unit + component proven.</done>
</task>

<task type="auto">
  <name>Task 2: Continue-Reading book entries + book/chapter search + tag surfacing + BookRemoveConfirm</name>
  <files>src/ingestion/library/ContinueReadingStrip.tsx, src/ingestion/library/libraryFilter.ts, src/ingestion/library/BookRemoveConfirm.tsx, src/ingestion/library/BookRow.tsx, tests/unit/library/book-filter.test.ts</files>
  <read_first>src/ingestion/library/ContinueReadingStrip.tsx (the entries fold + entry rendering), src/ingestion/library/libraryFilter.ts (haystack L76-86 + tag filter), src/ingestion/library/RemoveConfirm.tsx (the structural clone source: dialog/showModal, data-initial-focus on Cancel, close-listener routing), src/persistence/booksStore.ts (removeBook), 12-CONTEXT.md D12-02/D12-04 + the agent's-discretion cascade item, 12-PATTERNS.md §BookRow + §Shared Patterns "One Dexie transaction per user action"</read_first>
  <action>1. src/ingestion/library/ContinueReadingStrip.tsx — extend the entries derivation: alongside per-article entries, emit ONE book-level entry per book that has any chapter location and unfinished progress — label "BookTitle — Chapter N of M" (N = chapterOrdinal of the resume chapter; M = chapterArticleIds.length), linking to resolveResumeChapterId's target. Chapter articles NEVER emit their own strip entry (filter them from the standalone fold via ingestionMeta.bookId); standalone articles unchanged (D12-02).
  2. src/ingestion/library/libraryFilter.ts — restructure the pure filter to a mixed result: standalone articles (as today, excluding chapter members) PLUS book groups. The book haystack includes book title, authors, AND chapter titles (chapter titles come from the caller-supplied chapter title map — keep the function pure by parameter). Tag filtering: a selected tag matches a book via book.tags and surfaces the BOOK row only (chapters stay nested); standalone article tag behavior unchanged. Search matching a chapter title surfaces the book row, not the chapter (D12-04 — "find the essay collection containing the essay" works).
  3. src/ingestion/library/BookRemoveConfirm.tsx — NEW structural clone of RemoveConfirm (Pitfall 8 isolation — two dialogs, two call sites, no shared ConfirmDialog): native dialog + showModal, [data-initial-focus] on the Cancel button, focus restore in the close listener, Esc-originated closes routed through onCancel. Copy: title "Remove book", body shaped "Remove {book title}? Its {N} chapters and their highlights will be removed." (N = chapterArticleIds.length). The Proceed onClick is the SOLE executable removeBook call site in the codebase. Wire BookRow's Remove button → LibraryView book removeTarget state → the dialog (the 08-04 wiring precedent).
  4. tests/unit/library/book-filter.test.ts — pure-function matrix (the library-search.test.ts analog): book title match surfaces the book; book author match; chapter title match surfaces the BOOK not the chapter; tag on book surfaces book only; tag on standalone article unchanged; chapter members excluded from standalone results; empty query returns everything (books + standalone).</action>
  <verify>
    <automated>npx vitest run tests/unit/library/book-filter.test.ts tests/unit/library/book-row.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ContinueReadingStrip renders exactly one entry per in-progress book with the "Title — Chapter N of M" label shape, and zero individual chapter entries (unit-level via book-filter/component tests or a focused strip test if one exists — assert via the exported derivation if the component is not directly tested)
    - libraryFilter search over a chapter title returns the book group and not a standalone chapter row (test pinned)
    - BookRemoveConfirm.tsx exists as a structural clone; repo grep shows removeBook invoked from exactly ONE call site (its Proceed onClick)
    - The dialog's non-destructive button carries data-initial-focus
    - npx vitest run tests/unit/library/book-filter.test.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Reading resume, discovery, and destructive curation all work at book granularity with calm, honest surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: SC#1 e2e — upload → grouping → expand → disclosure → tag/search → remove cascade</name>
  <files>tests/e2e/epub-intake.spec.ts</files>
  <read_first>tests/e2e/pdf-intake.spec.ts (the upload-through-UI pattern: setInputFiles with an in-test buffer, status assertions), tests/e2e/library/browse-open.spec.ts + remove-cascade.spec.ts + search-tag-filter.spec.ts (openLibrary/wipeDatabase helpers, row-count selectors, cascade assertions), tests/unit/server/epub-fixtures.ts (import builders for setInputFiles buffers — the generator is the e2e fixture source), src/ingestion/IngestControl.tsx (the upload form selectors)</read_first>
  <action>Create tests/e2e/epub-intake.spec.ts (chromium + firefox + webkit, the existing three-engine config): build EPUB bytes in-test from the imported fixture builders and upload via setInputFiles on the ingest file input (a File created from the builder's Uint8Array). Cases:
   1. Book grouping (SC#1 core): upload validBookEpub3 → the library shows ONE book row (title from OPF) → click the chevron → exactly 4 chapter sub-rows appear (use the direct-child/nested selectors per the 08-05 lesson) → click a chapter → it opens in the reader (h1 = chapter title) → back → collapse works.
   2. Skip disclosure: upload mixedAdmissionBook → expanded grouping shows the "could not be read" note; admitted chapters open.
   3. Tag + search: tag the book via the expanded TagEntry → the tag filter surfaces the BOOK row (and no standalone chapter rows) → clear filter → search by a chapter title surfaces the book row.
   4. Continue-Reading: open chapter 2, scroll partway, return to the library → ONE book-level strip entry "BookTitle — Chapter 2 of 4" → clicking it opens chapter 2 at the saved position.
   5. Book progress: finish chapter 1 (scroll to end / navigate to last page) → the book hairline advances (ratio 1/4 class/value assertion per the existing hairline selectors).
   6. Remove cascade: upload, open a chapter, make a highlight + note, then Remove book → confirm → the book row disappears AND zero rows remain for its chapters across the library; the Dexie stores are empty of the book's ids (assert via UI absence + the existing storage-probe helper if present — the 11-05 no-side-effect assertion style).
   7. Dedupe-refuse: upload the same builder bytes again → the calm already-in-library copy appears; still exactly one book row.</action>
  <verify>
    <automated>npx playwright test tests/e2e/epub-intake.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/epub-intake.spec.ts exists and passes on chromium, firefox, and webkit (the config's default engines)
    - The grouping case asserts one book row + four chapter sub-rows after expansion and zero chapter rows at top level before expansion
    - The remove-cascade case proves highlight/note/location rows for the book's chapters are gone after confirm
    - The dedupe case asserts the already-in-library status copy and a stable row count
    - npx playwright test tests/e2e/epub-intake.spec.ts exits 0
  </acceptance_criteria>
  <done>SC#1 proven end-to-end in real browsers: expandable book grouping with honest disclosure, book-level resume entry, tag/search surfacing, and clean cascade removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted rows → library UI | books/chapters/locations re-validated on read (booksStore safeParse; ArticleSchema reads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering | corrupt book rows rendering hostile content | mitigate | BookSchema.safeParse per row on read; corrupt rows dropped calmly (the store seam); the renderer only ever renders Block JSON |
| T-12-12 | Tampering | accidental destructive cascade | mitigate | BookRemoveConfirm structural clone: data-initial-focus on Cancel, single removeBook call site, explicit confirm copy naming the chapter count |
| T-12-15 | Elevation/Spoofing | disclosure state confusion for AT users | mitigate | real button + aria-expanded + aria-controls region (native disclosure semantics); a11y.spec.ts extension lands in 12-06 |
</threat_model>

<verification>
Unit + component + filter suites green; e2e SC#1 green on three engines; existing library e2e (browse-open, remove-cascade, search-tag-filter, v1-regression) stays green — grouping changes no standalone behavior.
</verification>

<success_criteria>
D12-01..D12-04 + D12-06 + D12-11 fully implemented and browser-proven: expandable book rows with Resume + progress, single book strip entries, book-tag surfacing, book+chapter search, calm skip disclosure, and transaction-safe book removal.
</success_criteria>

## Artifacts this phase produces

- `bookProgress.ts` exports: deriveBookProgress, resolveResumeChapterId, chapterOrdinal (D12-03/D12-07 pure derivations)
- `BookRow.tsx` (expandable book row + chapter sub-list + skip disclosure + book TagEntry + remove trigger); `BookRemoveConfirm.tsx` (book-scoped destructive confirm)
- `LibraryRow` headingLevel prop (2 | 3); LibraryView book/article partition + merged addedAt ordering; ContinueReadingStrip book-level entries; libraryFilter mixed book/article results
- tests/unit/library/book-progress.test.ts, book-filter.test.ts, book-row.test.tsx; tests/e2e/epub-intake.spec.ts (created — SC#1)

<output>
Create `.planning/phases/12-epub-intake/12-05-SUMMARY.md` when done
</output>
