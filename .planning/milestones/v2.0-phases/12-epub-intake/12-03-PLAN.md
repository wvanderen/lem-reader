---
phase: 12-epub-intake
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/persistence/db.ts
  - src/persistence/booksStore.ts
  - src/ingestion/IngestionClient.ts
  - src/ingestion/IngestControl.tsx
  - tests/unit/persistence/books-store.test.ts
  - tests/unit/ingestion-client.test.ts
  - tests/unit/epub-copy.test.ts
autonomous: true
requirements: ["ING-05"]
must_haves:
  truths:
    - "Dexie v5 appends the books store + articles.bookId index with v1-v4 blocks byte-unchanged and NO .upgrade() callback (Pitfall 9)"
    - "saveBook writes book + all chapter articles in ONE Dexie transaction — a half-saved book is impossible (atomicity discipline)"
    - "removeBook cascades in one transaction: book row + every chapter's articles, highlights, notes, and location rows are all deleted (zero rows remain across all six stores)"
    - "ingestEpub sends {epub, filename?} to POST /api/ingest?format=epub and re-validates every article in the book ok-variant through ArticleSchema (client-side Zod defense-in-depth)"
    - "The .epub picker arm refuses file.size > EPUB_MAX_BYTES BEFORE any ArrayBuffer is read (earliest enforcement, the 11-04 pattern)"
    - "Book-level dedupe-refuse: has(book.id) is checked BEFORE any save; re-upload of identical bytes surfaces the calm already-in-library copy (D7-07 precedent at book level)"
    - "mapReasonToCopy carries calm DOC-06 strings for all four epub reasons, byte-pinned at the live exported surface"
  artifacts:
    - path: "src/persistence/booksStore.ts"
      provides: "the book store seam — listBooks, getBook, saveBook (one transaction), removeBook (cascade), setBookTags"
      exports: ["listBooks", "getBook", "saveBook", "removeBook", "setBookTags"]
      min_lines: 100
    - path: "src/persistence/db.ts"
      provides: "Dexie v5 append — books: id, title, *tags store + bookId index on articles"
      contains: 'version(5)'
    - path: "src/ingestion/IngestControl.tsx"
      provides: "the .epub picker arm with pre-read cap, epub save path, and four calm copy entries"
      contains: "epub-protected"
  key_links:
    - from: "src/ingestion/IngestControl.tsx"
      to: "src/persistence/booksStore.ts"
      via: "book-level has(book.id) dedupe-refuse then saveBook(book, articles) single-transaction write"
      pattern: "saveBook"
    - from: "src/ingestion/IngestionClient.ts"
      to: "src/ingestion/types.ts"
      via: "ingestEpub parses the widened book ok-variant and re-validates each article through ArticleSchema"
      pattern: "IngestionResponseSchema"
    - from: "src/persistence/db.ts"
      to: "src/content/schema.ts"
      via: "the books table is typed by Book (z.infer of BookSchema) — schema is the single source of truth"
      pattern: "Table<Book"
---

<objective>
The client half of EPUB intake: the Dexie v5 books store (additive append), the booksStore seam with one-transaction save and full cascade remove, the IngestionClient.ingestEpub wrapper with per-article re-validation, and the IngestControl .epub picker arm with the earliest cap enforcement, the book-level dedupe-refuse save path, and the four calm DOC-06 refusal strings.

Purpose: Books land in the reader's library atomically and removable without stranded annotations; the upload surface is honest before a single byte is read. Runs in parallel with 12-02 (disjoint files — this plan needs only the 12-01 schemas; the server branch it calls lands in 12-04).
Output: Persistence + client primitives green at unit level; e2e proof lands with 12-04+.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-epub-intake/12-CONTEXT.md
@.planning/phases/12-epub-intake/12-RESEARCH.md
@.planning/phases/12-epub-intake/12-PATTERNS.md
@src/persistence/db.ts
@src/persistence/locationStore.ts
@src/ingestion/LibrarySource.ts
@src/ingestion/IngestionClient.ts
@src/ingestion/IngestControl.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie v5 append + booksStore seam (save/remove transactions)</name>
  <files>src/persistence/db.ts, src/persistence/booksStore.ts, tests/unit/persistence/books-store.test.ts</files>
  <read_first>src/persistence/db.ts (the v3/v4 append-block comments + Table property annotations), src/persistence/locationStore.ts (store seam conventions: header citing decisions, import type, discriminated load result, safeParse per row, classifyStorageError), src/ingestion/LibrarySource.ts L108-151 (the remove() cascade transaction — collect-before-delete discipline), src/persistence/errors.ts, tests/unit/persistence/ (existing store test harness — fake-indexeddb setup), src/content/schema.ts (BookSchema)</read_first>
  <action>1. src/persistence/db.ts — APPEND a fifth version block; v1-v4 stay byte-unchanged (Pitfall 9): this.version(5).stores({ articles: "id, revision, source, addedAt, *tags, bookId", settings: "key", location: "[articleId+revision]", highlights: "id, [articleId+revision]", notes: "id, highlightId", books: "id, title, *tags" }) — the bookId index powers grouping reads; the *tags multi-entry index on books powers D12-04 tag filtering. NO .upgrade() callback (additive indexes + a new store that starts empty — the v3/v4 precedent). Add the books!: Table&lt;Book, string&gt; definite-assignment property annotation (the Phase 02-02 precedent) with an import type { Book } from "../content/schema".
  2. src/persistence/booksStore.ts — NEW store seam following the locationStore/settingsStore conventions (header comment citing D12-01..D12-04 + Pitfall 9/11-#3; import type only for types; discriminated {ok: true, books} | {ok: false, reason} load results; BookSchema.safeParse per row with corrupt-row drop; classifyStorageError routing). Surface:
   - listBooks(): Promise&lt;LoadResult&lt;Book[]&gt;&gt; — db.books.toArray + safeParse each.
   - getBook(id): Promise&lt;Book | null&gt;.
   - saveBook(book: Book, articles: CanonicalArticle[]): Promise&lt;void&gt; — ONE db.transaction("rw", db.books, db.articles, ...) putting the book row and every chapter article row (puts only — no Zod, no crypto, no network inside the transaction, the applyImport closure rule). Stamps book.addedAt = new Date().toISOString() only when the caller passed none.
   - removeBook(id): Promise&lt;void&gt; — ONE transaction over books + articles + highlights + notes + location: read the book row; collect its chapterArticleIds AND any articles where bookId === id (live truth — tolerant of partial imports); per chapter collect highlight ids BEFORE deleting (the LibrarySource.remove collect-before-delete discipline — in-transaction reads see pre-delete state); then delete highlights, notes (by highlightId), location (by [articleId+revision] keys), articles, and the book row. Zero rows remain across all six stores.
   - setBookTags(id, tags): Promise&lt;void&gt; — db.books.update with the validated array (D12-04 — tags live on the Book record).
   - hasBook(id): Promise&lt;boolean&gt; — the dedupe-refuse primitive IngestControl calls BEFORE any save.
  3. tests/unit/persistence/books-store.test.ts (fake-indexeddb harness, mirroring the existing store tests): saveBook + listBooks round-trip with Zod validation; a corrupt books row is dropped by listBooks (safeParse path); saveBook atomicity — inject a throwing put mid-transaction (Dexie creating-hook) and assert NEITHER the book NOR any chapter article persisted; removeBook leaves zero rows across books/articles/highlights/notes/location (seed a book + 2 chapters + a highlight + a note + a location first); setBookTags round-trip; the v4→v5 upgrade path — open a v4-shaped DB (articles without bookId index), write a legacy row, reopen at v5, the row reads back (additive index proof, the 08-02 e2e precedent at unit level).</action>
  <verify>
    <automated>npx vitest run tests/unit/persistence/books-store.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/db.ts contains this.version(5) with the books store string "id, title, *tags" and articles index string ending in bookId; the file contains no .upgrade() call added in the v5 block and the v1-v4 blocks are untouched (diff shows only additions)
    - booksStore.ts exports listBooks, getBook, saveBook, removeBook, setBookTags, hasBook; saveBook uses exactly ONE db.transaction call
    - The removeBook test asserts zero remaining rows across all six stores after seeding all five record kinds
    - The atomicity test proves a mid-transaction failure leaves no partial book
    - npx vitest run tests/unit/persistence/books-store.test.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Books persist atomically, list with Zod validation, cascade-remove cleanly, and tag at the book level; the v5 append breaks nothing.</done>
</task>

<task type="auto">
  <name>Task 2: ingestEpub client wrapper + .epub picker arm + calm copy</name>
  <files>src/ingestion/IngestionClient.ts, src/ingestion/IngestControl.tsx, tests/unit/ingestion-client.test.ts, tests/unit/epub-copy.test.ts</files>
  <read_first>src/ingestion/IngestionClient.ts (ingestPdf L109-114 + the L162-168 per-article re-validation precedent + how the fetch/parse path is structured), src/ingestion/IngestControl.tsx (the isPdf branch L201-232, bytesToBase64 L94-102, the has/save save path L145-150/L238-245, mapReasonToCopy L51-83, accept attribute + .meta copy), tests/unit/ingestion-client.test.ts, tests/unit/pdf-copy.test.ts (the byte-for-byte copy-pinning precedent), src/ingestion/types.ts (widened in 12-01), src/persistence/booksStore.ts (Task 1)</read_first>
  <action>1. src/ingestion/IngestionClient.ts — add ingestEpub(b64: string, filename?: string): POST /api/ingest with body {epub: b64, filename} AND the query string ?format=epub appended to the endpoint URL. The format query param is a COPY-ONLY hint for the middleware's pre-read 413 reason selection (planner resolution of 12-RESEARCH Pitfall 2 — enforcement stays content-length-based and body-agnostic; the authoritative reason comes from the post-read parsed shape on the server). Parse the response through the widened IngestionResponseSchema; on the book ok-variant, re-validate EVERY article through ArticleSchema.parse in a loop (client-side defense-in-depth — the network is a trust boundary) and return {book, articles, skippedCount}; refusal variants return {ok: false, reason} as the existing paths do.
  2. src/ingestion/IngestControl.tsx —
   a. Picker arm: const isEpub = /\.epub$/i.test(file.name) branching BEFORE any arrayBuffer/FileReader read: file.size > EPUB_MAX_BYTES → set the calm epub-too-large status and return without materializing bytes (the 11-04 earliest-enforcement pattern). Widen the accept attribute to ".md,.html,.pdf,.epub" and extend the picker .meta copy to mention books.
   b. Save path: on the book ok-variant — await hasBook(book.id); when true, surface the existing calm already-in-library copy and STOP (book-level dedupe-refuse, D7-07); otherwise await saveBook(book, articles) then surface a success status; when skippedCount > 0 the success copy appends the disclosure sentence shaped as "N chapters could not be read." (singular "chapter" when 1 — matching the D12-11 library wording; the .status copy and the library disclosure use the same phrasing).
   c. mapReasonToCopy — add the four cases with calm DOC-06 strings (no internal jargon): epub-protected → "This book is protected by DRM and cannot be added."; epub-unreadable → "This file could not be read as an EPUB book."; epub-empty → "No readable chapters were found in this book."; epub-too-large → "This book is too large to add." Keep mapReasonToCopy exported (the pdf-copy test precedent imports it).
  3. tests/unit/ingestion-client.test.ts — extend: ingestEpub issues POST to /api/ingest?format=epub with the {epub, filename} body (assert the URL + body via the existing fetch mock harness); a book ok-variant response parses and returns {book, articles, skippedCount}; an envelope whose articles array contains ONE malformed article rejects (per-article re-validation fires); a refusal envelope maps to {ok: false, reason: "epub-protected"}.
  4. tests/unit/epub-copy.test.ts — NEW, mirroring pdf-copy.test.ts: pin the four strings byte-for-byte (em dashes and punctuation included) against the exported mapReasonToCopy; the no-jargon guard iterates ALL 20 reasons asserting none of the internal tokens (zod, schema, base64, spine, OPF, NCX, manifest, anchor, epub-chapter as a raw id) appear in any copy string.</action>
  <verify>
    <automated>npx vitest run tests/unit/ingestion-client.test.ts tests/unit/epub-copy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - IngestionClient.ts contains ingestEpub and the string /api/ingest?format=epub; the per-article ArticleSchema.parse loop runs on the book variant
    - IngestControl.tsx contains the isEpub branch ordered BEFORE any arrayBuffer()/FileReader read, the widened accept attribute containing .epub, a hasBook(book.id) check preceding saveBook, and all four epub cases in mapReasonToCopy
    - tests/unit/epub-copy.test.ts pins "This book is protected by DRM and cannot be added." byte-for-byte and iterates all 20 reasons in the jargon guard
    - The over-cap epub path performs no fetch (assert zero mock calls in the client test when the picker cap fires — mirrored from the 11-04 picker test if it exists there, else assert the branch returns before ingestEpub)
    - npx vitest run tests/unit/ingestion-client.test.ts tests/unit/epub-copy.test.ts exits 0; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The client can send, validate, dedupe, and save a whole book atomically; every epub refusal speaks calm reader language; unit suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → IngestionClient | the /api/ingest response is untrusted until re-parsed (book ok-variant + per-article ArticleSchema) |
| file picker → client memory | untrusted .epub file metadata (name/size) and bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | DoS | oversized .epub upload | mitigate | picker cap on file.size BEFORE any read (no ArrayBuffer materialization); middleware + orchestrator layers follow in 12-04 |
| T-12-10 | Tampering | forged/poisoned ingest responses | mitigate | IngestionResponseSchema parse + per-article ArticleSchema re-validation on the network read (STATE-04 defense-in-depth) |
| T-12-11 | Tampering | corrupt persisted book rows | mitigate | BookSchema.safeParse per row on every booksStore read; corrupt rows dropped calmly (the locationStore precedent) |
| T-12-12 | Tampering | stranded annotations on book removal | mitigate | removeBook single-transaction cascade proven by the zero-rows test (Pitfall 7 of 12-RESEARCH) |
</threat_model>

<verification>
Store + client unit suites green; tsc clean; dedupe-refuse and one-transaction save observable in test assertions; four copy strings pinned.
</verification>

<success_criteria>
Dexie v5 appended additively; booksStore ships listBooks/getBook/saveBook/removeBook/setBookTags/hasBook with atomic save and full cascade remove; ingestEpub round-trips the book envelope with per-article re-validation; the .epub picker arm enforces the cap before reading and dedupe-refuses at book level; all calm copy pinned.
</success_criteria>

## Artifacts this phase produces

- Dexie `version(5)` block — books store ("id, title, *tags") + articles bookId index; `books!: Table<Book, string>` annotation
- `booksStore.ts` exports: listBooks, getBook, saveBook, removeBook, setBookTags, hasBook
- `IngestionClient.ingestEpub(b64, filename?)` — POST /api/ingest?format=epub + book ok-variant parsing + per-article re-validation
- IngestControl: `isEpub` picker branch, widened accept list, book-level dedupe-refuse save path, four mapReasonToCopy epub entries
- tests/unit/persistence/books-store.test.ts, tests/unit/epub-copy.test.ts (new); tests/unit/ingestion-client.test.ts (extended)

<output>
Create `.planning/phases/12-epub-intake/12-03-SUMMARY.md` when done
</output>
