---
phase: 13-polish-and-acceptance
plan: 10
type: execute
wave: 5
depends_on: ["13-07", "13-09"]
files_modified:
  - src/App.tsx
  - src/reader/Header.tsx
  - src/routes/ArticleView.tsx
  - src/reader/annotations/AnnotationsDrawer.tsx
  - src/app.css
  - tests/e2e/chrome/tag-popover.spec.ts
  - tests/e2e/chrome/header-geometry.spec.ts
  - tests/e2e/library/search-tag-filter.spec.ts
  - tests/e2e/portability/highlights-export.spec.ts
autonomous: true
gap_closure: true
requirements: [POLISH-03]

must_haves:
  truths:
    - "Tags are reachable from a small icon button in the top bar beside the highlights-drawer and mode-toggle controls — never inline under the article title"
    - "The tag editor opens as a light-dismiss popover (Esc + outside-click close, focus returns to the trigger) and adding/removing a tag from it persists to the article"
    - "The article-top metadata block is a compact, quiet provenance block — byline/date, book-context line, source link — with NO tag controls and NO export button in it"
    - "The per-article Export highlights action lives in the highlights drawer (highlight-scoped action in the highlight-scoped surface) and still announces its result politely"
    - "The page-1 reserve machinery still works end-to-end: the (now smaller) spot renders exactly once on page 1, coexists with page-1 content, and unmounts after the first turn"
    - "The 09-07 geometry lesson holds: no internal header scrolling at 360×640; the header-row cap rule is byte-unchanged"
  artifacts:
    - path: "src/reader/Header.tsx"
      provides: "tags-trigger icon button (TagIcon glyph) inline-start of the annotations trigger"
      contains: "tags-trigger"
    - path: "src/routes/ArticleView.tsx"
      provides: "popover surface mounting TagEntry + the compacted articleTopMeta block"
      contains: "tag-popover"
    - path: "src/reader/annotations/AnnotationsDrawer.tsx"
      provides: "Export highlights button in the drawer header row via new onExportHighlights/exportingHighlights props"
      contains: "Export highlights"
    - path: "src/App.tsx"
      provides: "tagsOpen state lifted + view-swap reset (the drawerOpen pattern)"
      contains: "tagsOpen"
    - path: "tests/e2e/chrome/tag-popover.spec.ts"
      provides: "popover open/close/light-dismiss/Esc/focus-restore/persist contract + axe on the open state"
      min_lines: 70
  key_links:
    - from: "src/reader/Header.tsx"
      to: "src/App.tsx"
      via: "tagsOpen/onToggleTags props — the same lifted-state pattern as drawerOpen/onToggleAnnotations"
      pattern: "onToggleTags"
    - from: "src/routes/ArticleView.tsx"
      to: "src/reader/TagEntry.tsx"
      via: "TagEntry mounted inside the popover surface (component file byte-unchanged — mount point moves)"
      pattern: "TagEntry"
    - from: "src/reader/annotations/AnnotationsDrawer.tsx"
      to: "src/routes/ArticleView.tsx"
      via: "onExportHighlights prop wired to ArticleView's handleExportHighlights; announcement stays in ArticleView's visually-hidden live region"
      pattern: "onExportHighlights"
    - from: "tests/e2e/portability/highlights-export.spec.ts"
      to: "src/reader/annotations/AnnotationsDrawer.tsx"
      via: "per-article export test opens the annotations drawer (header Highlights-and-notes trigger) before locating the Export highlights button — the button now lives in a showModal dialog whose closed subtree is invisible to role queries (checker blocker, revision 1)"
      pattern: "annotations-trigger"
  prohibitions:
    - statement: "src/reader/TagEntry.tsx stays byte-unchanged (phase-13 byte-stability contract; G5 moves its MOUNT POINT only — explicitly sanctioned by the gap routing)"
      status: resolved
      verification: "git diff over the plan's commits shows zero changes to src/reader/TagEntry.tsx"
    - statement: "src/reader/PageIndicator.tsx, src/persistence/settingsStore.ts, src/settings/applyTheme.ts stay byte-unchanged (byte-stability contracts)"
      status: resolved
      verification: "git diff over the plan's commits shows zero changes to those files"
    - statement: "src/pagination/fragment.ts and tests/unit/pagination/firstPageReserved.test.ts stay byte-unchanged (the firstPageReservedPx engine contract); the 09-07 header-row cap rule in app.css stays byte-unchanged"
      status: resolved
      verification: "git diff shows fragment.ts + firstPageReserved.test.ts untouched; the .article-body.paginated-surface grid cap lines are not in the diff"
    - statement: "No emoji glyphs introduced (icon policy) — the new trigger uses an inline-SVG tag glyph"
      status: resolved
      verification: "Header.tsx contains an svg tag glyph; repo emoji sweep still returns zero"
---

<objective>
Close gap G5 from the Phase 13 user review (13-UAT.md § Gaps, 13-VERIFICATION.md § Gaps Found): the user rejected the tag-adding section below the article title and the article-top metadata spot design as a whole.

This is a RECORDED USER-DIRECTION CHANGE that partially supersedes the 13-04 Option A placement (D13-13's "tags edit from a calm metadata spot at the article top"): the tag affordance becomes a small icon in the top bar NEAR the highlights-drawer + mode-toggle controls. Byline/source/export disposition is delegated to planning by the gap routing; this plan's disposition:

1. **TagEntry → top-bar popover.** A `tags-trigger` icon button in Header's `.header-controls` group, inline-start of the annotations trigger (order: [tags][annotations][mode][gear]), article-mounted only — the same gating as the annotations trigger. The editor is the byte-unchanged TagEntry mounted inside a native `popover="auto"` panel (free light-dismiss + Esc in the top layer; Baseline on all three Playwright engines), owned by ArticleView (the AnnotationsDrawer ownership pattern: App lifts the open state, Header renders the trigger, ArticleView mounts the surface). Focus restores to the trigger on close (Pitfall 1 discipline via the popover toggle event).
2. **Article-top spot → compact provenance block.** `articleTopMeta` keeps only the byline·date meta line, the epub book-context line (byte-stable `p.book-context` — asserted by a11y.spec), and the "Originally published at {domain}" source link (LIB-05 visibility). No box, no actions row, no TagEntry, no Export. The `.article-top-meta` class + measure-once reserve machinery (firstPageReservedPx) are preserved — the reserve simply gets much smaller, restoring a generous page-1 content budget at 360×640.
3. **Export highlights → the highlights drawer.** The per-article export button moves into the AnnotationsDrawer header row (highlight-scoped action in the highlight-scoped surface) via new props; the handler, disabled state, and the visually-hidden announcement live region stay in ArticleView.

The engine, its reserve parameter, and the whole-fitting escape are untouched; header-geometry.spec.ts is realigned and STRENGTHENED to the new anatomy (no tag/export controls anywhere in the article flow).

Purpose: POLISH-03 at the user's bar — slim chrome, tags beside the reader controls, a quiet booklike article opening.
Output: top-bar tag popover, compacted spot, drawer-housed export, realigned + new specs (incl. the portability highlights-export content gate, realigned to the drawer-housed button).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
@.planning/phases/13-polish-and-acceptance/13-04-SUMMARY.md
@.planning/phases/13-polish-and-acceptance/13-09-SUMMARY.md
@src/routes/ArticleView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag affordance → top-bar popover (App state + Header trigger + ArticleView surface + TagEntry move + spec realignments)</name>
  <files>src/App.tsx, src/reader/Header.tsx, src/routes/ArticleView.tsx, src/app.css, tests/e2e/library/search-tag-filter.spec.ts, tests/e2e/chrome/header-geometry.spec.ts</files>
  <read_first>
  - src/App.tsx (the drawerOpen lifting pattern at lines 148–197: state, view-swap reset effect, Header props, ArticleView props — tagsOpen mirrors it)
  - src/reader/Header.tsx (whole file — the .header-controls group, the annotations-trigger button with aria-haspopup/aria-expanded + badge, the GearIcon/HighlighterIcon anatomy the new TagIcon copies)
  - src/routes/ArticleView.tsx (the articleTopMeta block at lines 1475–1555 — TagEntry currently sits in .article-top-actions; the AnnotationsDrawer mount at line ~1888 — the ownership pattern the popover surface copies; the props interface near lines 118–197)
  - src/reader/TagEntry.tsx (interface + behavior — READ ONLY; the component is byte-frozen; it renders the fieldset with input#tag-entry-new and the Add tag button)
  - src/app.css (the .annotations-trigger rule near line 1656 — the trigger geometry to mirror; the .article-top-meta / .article-top-actions rules near lines 3005–3074 — the spot styles Task 2 simplifies and the scoped .tag-entry overrides that die with the move)
  - tests/e2e/library/search-tag-filter.spec.ts (the two ArticleView tag flows at lines ~146–155 and ~205–214 that must gain a popover-open step)
  - tests/e2e/chrome/header-geometry.spec.ts (the TagEntry count assertions at lines 101–105 and 166–167 and the scroll-mode tagEntryCount capture at line 240 — realignment targets)
  </read_first>
  <action>
  1. src/App.tsx — add `const [tagsOpen, setTagsOpen] = useState(false);` beside drawerOpen; reset it in the view-swap effect (the same effect that resets drawerOpen + annotationCount); pass `tagsOpen={tagsOpen}` + `onToggleTags={() => setTagsOpen((v) => !v)}` to Header and `tagsOpen={tagsOpen}` + `onCloseTags={() => setTagsOpen(false)}` to ArticleView (mirror the drawer prop names).

  2. src/reader/Header.tsx — add props `tagsOpen: boolean` and `onToggleTags: () => void`. Inside `.header-controls`, when `articleMounted`, render BEFORE the annotations trigger: a `button type="button" className="tags-trigger"` with `aria-label="Article tags"`, `aria-haspopup="dialog"`, `aria-expanded={tagsOpen}`, `onClick={onToggleTags}`, containing a new local `TagIcon` function component (GearIcon anatomy exactly: 20×20, viewBox 0 0 24 24, fill none, stroke currentColor, strokeWidth 1.75, round caps/joins, aria-hidden true, focusable false) using the tag-label glyph path `M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z` plus the dot circle `cx="7.5" cy="7.5" r="0.5" fill="currentColor"`.

  3. src/routes/ArticleView.tsx — accept the new `tagsOpen`/`onCloseTags` props. REMOVE `<TagEntry .../>` from `.article-top-actions` (Task 2 removes the rest of that row). Mount the popover surface near the AnnotationsDrawer mount: `<div ref={tagPopoverRef} popover="auto" role="dialog" aria-label="Article tags" className="tag-popover">` containing a short visually-hidden or legend-context intro is NOT needed — the panel wraps `<TagEntry articleId={article.id} tags={article.tags ?? []} />` directly. Drive it with an effect mirroring the AnnotationsDrawer open/close effect: when tagsOpen → capture `document.activeElement` as the trigger (Pitfall 1) and call `showPopover()` on the ref; when it closes → `hidePopover()`. Register a `toggle` event listener on the popover element: when `event.newState === "closed"`, call `onCloseTags()` and restore focus to the captured trigger (the popover's free light-dismiss/Esc path routes through the same toggle event — one close seam).

  4. src/app.css — add `.tags-trigger` mirroring the `.annotations-trigger` geometry exactly (inline-flex, centered, var(--touch) box, transparent bg, transparent border, 4px radius, var(--ink-soft), cursor pointer; a hover/`[aria-expanded="true"]` variant using var(--accent) — the annotations-trigger open-state discipline). Add `.tag-popover` (position fixed; top `calc(48px + var(--space-xs))`; inset-inline-end `var(--space-md)`; margin 0; padding `var(--space-md)`; background `var(--surface-raised)`; border 1px solid `var(--hairline)`; border-radius 4px; width `min(420px, calc(100vw - 2 * var(--space-md)))`; z-index not needed — the top layer sits above everything). Zero motion properties; comment prose avoids greppable property names.

  5. Realign tests/e2e/library/search-tag-filter.spec.ts — in BOTH ArticleView tag flows, insert before the `input#tag-entry-new` interaction: `await page.getByRole("button", { name: "Article tags" }).click();` and `await expect(page.locator(".tag-popover")).toBeVisible();` (the picker is hidden until opened — without this step the fill times out). The rest of both flows (chip assertion, library re-derivation) is unchanged.

  6. Realign tests/e2e/chrome/header-geometry.spec.ts TagEntry assertions — the render-once contract now reads: exactly ONE TagEntry instance in the document, and it lives in the popover, NEVER inside `.article-top-meta` and never in the pinned header. Concretely: `page.locator(".article-top-meta .tag-entry").count()` is 0; `page.locator(".tag-entry").count()` is 1 (the popover instance — CSS locators match closed popover DOM); the after-first-turn assertion that currently expects `.tag-entry` count 0 changes to expect `.article-top-meta .tag-entry` count 0 while `.tag-entry` total stays 1 (the popover persists across turns — it is article chrome, not page content); the scroll-mode `tagEntryCount` capture scopes the same way. Leave the Export-count assertions for Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/e2e/library/search-tag-filter.spec.ts tests/e2e/chrome/header-geometry.spec.ts tests/e2e/library/v1-regression.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/reader/Header.tsx contains a `tags-trigger` button with aria-label "Article tags", aria-haspopup="dialog", aria-expanded, rendered before the annotations trigger, gated on articleMounted, and a local `TagIcon` svg with viewBox "0 0 24 24"
  - src/App.tsx contains tagsOpen state, the view-swap reset, and both prop threads; src/routes/ArticleView.tsx contains `popover="auto"`, `role="dialog"`, `aria-label="Article tags"`, a toggle-event close seam with focus restore, and no TagEntry inside `.article-top-actions`
  - `git diff` over the task commit shows src/reader/TagEntry.tsx byte-unchanged
  - search-tag-filter.spec.ts contains the popover-open step in both flows; header-geometry.spec.ts scopes TagEntry assertions to `.article-top-meta .tag-entry` === 0 with total count 1
  - the three suites exit 0 on 3 engines; `rg -c 'autoFocus' src/routes/ArticleView.tsx` introduces no new focus-on-mount call in the popover path (TagEntry inert-at-mount carries over — its discipline prose lives in the byte-frozen file)
  </acceptance_criteria>
  <done>Tags are editable from a top-bar icon beside the highlights control via a light-dismiss popover; no tag control exists under the article title; the library tag flows prove persistence through the new surface.</done>
</task>

<task type="auto">
  <name>Task 2: Compact the article-top spot to a provenance block + move Export highlights into the drawer + strengthen header-geometry + tag-popover spec</name>
  <files>src/routes/ArticleView.tsx, src/reader/annotations/AnnotationsDrawer.tsx, src/app.css, tests/e2e/chrome/header-geometry.spec.ts, tests/e2e/chrome/tag-popover.spec.ts, tests/e2e/portability/highlights-export.spec.ts</files>
  <read_first>
  - src/routes/ArticleView.tsx (articleTopMeta at lines 1494–1555; handleExportHighlights at lines 1434–1465; the exportAnnouncement visually-hidden region at lines 1600–1613; the AnnotationsDrawer mount at lines ~1882–1895)
  - src/reader/annotations/AnnotationsDrawer.tsx (the props interface at lines 39–55; the header-row markup at lines ~125–140 — h2 + close button; the Pitfall 1 close/focus-restore effects)
  - src/app.css (the .article-top-meta / .article-top-actions rules near lines 3005–3079, including the scoped .tag-entry overrides that die with the move)
  - tests/e2e/chrome/header-geometry.spec.ts (the spot test at lines 96–211: render-once counts, reserve coexistence text-rect assertions, unmount-after-turn, remount-on-return — the assertions Task 2 realigns + strengthens)
  - tests/e2e/a11y.spec.ts (the AxeBuilder usage pattern + the `p.book-context` selector that must keep resolving)
  - tests/e2e/portability/highlights-export.spec.ts (the per-article test at lines 90–130: the direct article goto + Export-highlights role query at line 98 that MUST gain a drawer-open step; the announcement-region assertion at lines 124–129 targeting main's second visually-hidden role=status region — that assertion stays byte-intact because the live region stays in ArticleView)
  - tests/e2e/annotations/_fixtures.ts (the drawerTrigger helper at lines 296–302 — the count-suffix-tolerant /^Highlights and notes/ role-query precedent the realigned step copies)
  </read_first>
  <action>
  1. src/routes/ArticleView.tsx — compact `articleTopMeta` to the provenance block ONLY: the byline·date `.meta` paragraph, the conditional `p.meta.book-context` line (byte-stable classes — a11y.spec queries it), and the "Originally published at {domain}" source link with its visually-hidden new-tab span. DELETE the `.article-top-actions` wrapper and the Export-highlights button from the spot (the handler `handleExportHighlights`, the `exportingHighlights` state, and the exportAnnouncement region all STAY in ArticleView — only the button moves). Thread `onExportHighlights={handleExportHighlights}` and `exportingHighlights={exportingHighlights}` into the AnnotationsDrawer mount. Update the placement-contract comment: render-once page-1 mount + measured reserve unchanged; the anatomy is now provenance-only.

  2. src/reader/annotations/AnnotationsDrawer.tsx — add props `onExportHighlights: () => void` and `exportingHighlights: boolean`. In the `.annotations-drawer-header` row, between the h2 and the close button, render `<button type="button" className="article-export-highlights annotations-drawer-export" onClick={onExportHighlights} disabled={exportingHighlights}>Export highlights</button>` (reuses the generic quiet-button tokens; the second class is the drawer-scoped hook). Keep every existing drawer behavior byte-identical (close/focus discipline, entry list, jump/edit/delete paths).

  3. src/app.css — simplify the spot styles: keep a minimal `.article-top-meta` rule (meta spacing only — quiet lines above content, no card chrome) and DELETE the `.article-top-actions` rule plus the `.article-top-meta .tag-entry*` / `.article-top-actions .article-export-highlights` scoped overrides (dead with the move). Add nothing else; zero motion properties. The `.article-body.paginated-surface` header-row cap rule (09-07) stays byte-unchanged.

  4. Strengthen tests/e2e/chrome/header-geometry.spec.ts spot test to the new anatomy: exactly one `.article-top-meta` on page 1 containing ZERO buttons (strengthened — the old contract allowed the actions row); the drawer holds exactly one Export button (`page.locator(".annotations-drawer .annotations-drawer-export").count()` is 1 — use the css-scoped locator since role queries exclude closed-dialog subtrees); the spot's text-rect coexistence-with-page-1 assertions and unmount-after-turn / remount-on-return / page-count-stability assertions stay and must pass with the SMALLER reserve (do not weaken tolerances — the smaller reserve only makes them easier).

  5. Create tests/e2e/chrome/tag-popover.spec.ts (plain test(), 3-engine matrix, library open + article open helpers following the chrome-suite conventions):
  - "tags popover opens from the top bar, edits tags, light-dismisses, and restores focus": open an article; assert NO `.tag-entry` is visible before opening (popover closed); click the "Article tags" trigger; assert the popover (`role="dialog"` name "Article tags") is visible and `aria-expanded` flipped true; add a tag through `input#tag-entry-new` + the Add tag button; assert the chip appears; navigate back to the library and assert the chip/filter surfaces the tag (the search-tag-filter re-derivation bar); return to the article, reopen the popover, click the article surface outside the popover → popover hides, focus rests on the trigger (evaluate activeElement), trigger aria-expanded false; reopen and press Escape → same close + focus outcome.
  - "drawer export announces": open the annotations drawer via the header trigger; click the Export highlights button; assert the download event fires (the 09-01 acceptDownloads pattern) and the visually-hidden export announcement region carries the "Exported" phrase.
  - Include one AxeBuilder pass on the open-popover state (mirror the a11y.spec.ts invocation shape) asserting zero violations on the popover + trigger.
  - Zero fixed sleeps; every wait is expect/expect.poll.

  6. Realign tests/e2e/portability/highlights-export.spec.ts (the checker-revision blocker — the per-article PORT-03 gate would go red post-execution otherwise): in the per-article test, between the direct `#/article/{id}` goto and the Export-highlights role query at line 98, insert a drawer-open step — locate the header annotations trigger via the count-suffix-tolerant role query anchored on the Highlights-and-notes label (the drawerTrigger helper's /^Highlights and notes/ regex precedent from tests/e2e/annotations/_fixtures.ts lines 296–302; the portability suite does not import that helper, so inline the same regex shape), click it, then expect the `dialog.annotations-drawer` element to be visible (keep the generous 15s timeout on this wait — cold article mount) BEFORE locating the Export highlights button. Rationale to record in the spec's comment: the button now lives inside a showModal dialog; closed-dialog subtrees are display:none and excluded from role/visibility queries, so the button is unreachable until the drawer opens. Everything after the button lookup is UNTOUCHED: the download capture, the suggested-filename assertion, every Markdown content assertion, and the announcement-region assertion on main's second visually-hidden role=status region (the live region stays in ArticleView — the relocation moves the trigger only). The library-wide test in the same file goes through Settings and needs no change. While editing, sweep-confirm the relocation is fully covered: a repo search for the Export-highlights accessible-name role query over tests/ returns exactly the two realigned files (this spec + tests/e2e/chrome/header-geometry.spec.ts) — no third consumer exists today; if the sweep finds one, realign it in the same commit.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/tag-popover.spec.ts tests/e2e/chrome/header-geometry.spec.ts tests/e2e/chrome/dialog-centering.spec.ts tests/e2e/a11y.spec.ts tests/e2e/portability/highlights-export.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - `.article-top-actions` appears nowhere in src/ (component or CSS); `.article-top-meta` contains only meta paragraphs + the source link (zero button descendants — pinned by the strengthened header-geometry assertion)
  - AnnotationsDrawer.tsx contains the `annotations-drawer-export` button in its header row, wired via onExportHighlights/exportingHighlights props; ArticleView keeps the handler + announcement region and passes the props
  - `p.book-context` still resolves on an epub chapter (a11y.spec green — byte-stable class preserved)
  - tests/e2e/chrome/tag-popover.spec.ts exists covering open/edit/light-dismiss/Esc/focus-restore + drawer export announce + axe on open state; a hard-wait helper search over the new spec returns zero matches
  - tests/e2e/portability/highlights-export.spec.ts opens the annotations drawer before the Export-highlights role query (the dialog-visible expect precedes the button lookup) and its announcement-region assertion is byte-unchanged; the task's automated verify command includes the spec and exits 0
  - a repo search for the Export-highlights accessible-name role query over tests/ returns exactly the two realigned spec files — no un-realigned consumer remains
  - `npx playwright test tests/e2e/pagination/ tests/e2e/open-every-fixture.spec.ts` exits 0 (the smaller reserve holds the page-1 geometry contracts) and `npx vitest run tests/unit/pagination/` stays green (engine byte-frozen)
  - `git diff` over the plan's commits: src/pagination/fragment.ts, tests/unit/pagination/firstPageReserved.test.ts, src/reader/TagEntry.tsx, src/reader/PageIndicator.tsx, src/persistence/settingsStore.ts, src/settings/applyTheme.ts all byte-unchanged; the 09-07 cap lines absent from the app.css diff
  </acceptance_criteria>
  <done>The article opens with quiet provenance lines only; tags and export live beside their sibling reader controls; every byte-stability and geometry contract holds with proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | UI restructure over existing surfaces; tag writes still flow through the byte-frozen TagEntry → tagsStore path; export still uses the existing download helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10-01 | Tampering | popover surface content | mitigate | Panel renders only React children (the frozen TagEntry); no markup-string APIs; calm-voice copy only |
| T-13-10-02 | Spoofing | drawer export button | accept | Same handler + filename sanitization as the existing ArticleView export path; only the trigger's mount point changed |
| T-13-10-03 | Denial of Service | smaller page-1 reserve | mitigate | header-geometry text-rect assertions + pagination corpus green prove page-1 coexistence; the whole-fitting escape + honest typed fallback remain the degenerate outcome |
| T-13-10-04 | Elevation | popover focus handling | mitigate | toggle-event close seam restores focus to the trigger (Pitfall 1); Esc + light-dismiss are native popover behaviors, not hand-rolled |
| T-13-10-SC | Tampering | package installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- `npx playwright test tests/e2e/chrome/` — whole chrome directory (tag-popover new + header-geometry realigned + dialog-centering + back-nav + library-tidy) green, 3 engines
- `npx playwright test tests/e2e/polish/ tests/e2e/pagination/ tests/e2e/open-every-fixture.spec.ts` — the 13-09 contracts + pagination corpus hold under the restructured spot
- `npx playwright test tests/e2e/library/ tests/e2e/a11y.spec.ts tests/e2e/epub-intake.spec.ts` — library tag flows, axe, and the BookRow TagEntry (library book tags — untouched by this plan) green
- `npx playwright test tests/e2e/portability/` — the portability spine + the realigned highlights-export content gate green through the export relocation (drawer-housed button; 09-06 PORT-03 contract intact)
- `npx vitest run` + `npx tsc --noEmit` — unit leg + types clean
- Byte-freeze checks per the prohibitions block (TagEntry/PageIndicator/settingsStore/applyTheme/fragment.ts/firstPageReserved.test.ts + the 09-07 cap rule)
- `npm run test` — THE HONEST FULL-SUITE GATE, owned by this plan as the wave-5 (last) gap-closure plan: single plain invocation, exit 0 required, and the pass/fail/skip counts recorded in 13-10-SUMMARY.md (the 13-06/09-07 precedent). This is the post-gap-closure re-run proving G1–G5 fixes left the whole suite green; a red run blocks the phase regardless of targeted-suite results
</verification>

<success_criteria>
- G5 closed at the user bar: tag affordance in the top bar beside highlights/mode controls; metadata spot redesigned to a compact provenance block; export dispositioned to the drawer — with the 09-07 geometry lesson and every firstPageReservedPx contract explicitly proven unregressed
- Specs strengthened, not weakened: header-geometry pins zero-buttons-in-spot; the new tag-popover spec pins the popover contract incl. accessibility; the portability export-content gate survives the relocation realigned, not deleted or weakened
- The honest full-suite `npm run test` gate exits 0 with counts recorded — the wave-5 closure evidence for the whole gap-closure cycle
</success_criteria>

## Artifacts this phase produces

- Props `tagsOpen`/`onToggleTags` on Header; `tagsOpen`/`onCloseTags` on ArticleView (App-lifted state `tagsOpen`)
- React component `TagIcon` (local to src/reader/Header.tsx; inline-SVG tag-label glyph)
- Popover surface `.tag-popover` (div with popover="auto", role="dialog", aria-label "Article tags") in ArticleView
- CSS classes `.tags-trigger` (+ open-state variant) and `.tag-popover`; class `.annotations-drawer-export` on the relocated Export button; `.article-top-actions` deleted
- Props `onExportHighlights`/`exportingHighlights` on AnnotationsDrawer
- e2e specs: tests/e2e/chrome/tag-popover.spec.ts (new); header-geometry.spec.ts + search-tag-filter.spec.ts + portability/highlights-export.spec.ts (realigned + strengthened — the export-content gate gains the drawer-open step, keeps every content + announcement assertion)
- No new exports from byte-frozen files; no packages; no engine, schema, or store changes

<output>
Create `.planning/phases/13-polish-and-acceptance/13-10-SUMMARY.md` when done
</output>
