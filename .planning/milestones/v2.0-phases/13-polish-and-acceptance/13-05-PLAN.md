---
phase: 13-polish-and-acceptance
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md
  - tests/unit/server/pdfTimeout.spec.ts
autonomous: true
requirements: [ACPT-05]
must_haves:
  truths:
    - "The NVDA+Firefox acceptance runbook and an empty findings record sheet exist in 13-VERIFICATION.md, referencing docs/ACCEPTANCE-PROTOCOL.md exactly as documented"
    - "A reader-verifiable statement in the ledger records that ACPT-05 flips from Pending only when the user-run results land with zero blocker/major findings (D13-07)"
    - "The VoiceOver+Safari supplementary checklist covers the NEW v2.0 surfaces and is explicitly not an ACPT-05 gate (D13-05)"
    - "The Phase 11 acknowledged gap is closed: withPdfDocument's 30s timeout firing path is proven by a fake-timers unit test with zero production changes (D13-11)"
  artifacts:
    - path: ".planning/phases/13-polish-and-acceptance/13-VERIFICATION.md"
      provides: "ACPT-05 NVDA runbook + record sheet; VO supplementary checklist; findings policy"
      contains: "NVDA"
    - path: "tests/unit/server/pdfTimeout.spec.ts"
      provides: "D13-11 fake-timers timeout race test"
      min_lines: 40
  key_links:
    - from: ".planning/phases/13-polish-and-acceptance/13-VERIFICATION.md"
      to: "docs/ACCEPTANCE-PROTOCOL.md"
      via: "runbook points at the protocol AS-DOCUMENTED (six v1.0 scripted flows + five exploratory charters, D6-07 policy) — the protocol file itself stays byte-unchanged"
      pattern: "ACCEPTANCE-PROTOCOL"
    - from: "tests/unit/server/pdfTimeout.spec.ts"
      to: "server/pdfToBlocks.ts withPdfDocument"
      via: "mocked document loader + never-settling operation + vi.advanceTimersByTimeAsync past PDF_EXTRACTION_TIMEOUT_MS"
      pattern: "PDF_EXTRACTION_TIMEOUT_MS"
  prohibitions:
    - statement: "docs/ACCEPTANCE-PROTOCOL.md stays byte-unchanged (D13-04: runs as-documented, no v2.0 addendum)"
      status: resolved
      verification: "git diff --stat docs/ACCEPTANCE-PROTOCOL.md is empty for this plan's commits"
    - statement: "Zero production changes for D13-11 (the gap's own closure terms)"
      status: resolved
      verification: "git status during this plan shows no modified files under server/ or src/"
    - statement: "ACPT-05 is NOT flipped to complete by this plan (instrument-ships-now / requirement-closes-at-proof — D13-07, the 04-02/06-04 precedent)"
      status: resolved
      verification: "REQUIREMENTS.md checkbox for ACPT-05 remains unchecked after execution; the ledger states the flip condition"
---

<objective>
Prepare the ACPT-05 acceptance instrument for the user's NVDA+Firefox run (D13-07 prepare-then-run-later: this plan ships the runbook + record sheet; the user runs the protocol on Windows hardware on their own schedule; ACPT-05 flips only when results land) plus the VoiceOver+Safari supplementary checklist for the new v2.0 surfaces (D13-05), and fold the Phase 11 acknowledged gap (D13-11): a fake-timers unit test proving the 30s PDF extraction-timeout firing path — zero production changes.

Purpose: close the v1.0 A4 coverage boundary honestly and the 11-VERIFICATION acknowledged gap without pretending agent-run SR coverage.
Output: 13-VERIFICATION.md runbook/record sections; tests/unit/server/pdfTimeout.spec.ts.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13-VERIFICATION.md — NVDA runbook + record sheet + VO supplementary checklist</name>
  <files>.planning/phases/13-polish-and-acceptance/13-VERIFICATION.md</files>
  <read_first>
  - docs/ACCEPTANCE-PROTOCOL.md (THE instrument — read in full: six scripted flows, five exploratory charters, D6-05 matrix, D6-07 zero-blocker/major policy, re-run rule, role+name outcome discipline — never verbatim SR phrasing)
  - .planning/milestones/v1.0-phases/06-prototype-acceptance/06-VERIFICATION.md (the Phase 6 acceptance ledger whose findings-table shape this copies; the A4 NVDA boundary this closes; the 06-06 findings/fixes precedent incl. the NotePopover dialog promotion)
  - .planning/phases/13-polish-and-acceptance/13-CONTEXT.md (D13-04 through D13-07)
  </read_first>
  <action>
    Create .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md with a small frontmatter (phase, status: pending-user-run) and these sections: (1) "ACPT-05 — NVDA+Firefox acceptance run" — environment prerequisites (NVDA current stable + Firefox on Windows hardware), run instructions pointing at docs/ACCEPTANCE-PROTOCOL.md AS-DOCUMENTED (the six v1.0 scripted flows and five exploratory charters, executed in order, outcomes recorded as role + accessible name + state per the protocol's Pitfall 7 discipline — never verbatim SR phrasing), severity rubric reference (D6-07: zero blocker/major required to pass; confusing-but-completable = minor), an EMPTY findings record table copying the 06-VERIFICATION findings-table shape (columns: finding id, flow/charter, severity blocker|major|minor, observed role+name outcome, expected outcome, status open|fixed|deferred), and a per-flow pass/fail checklist block for the six flows + five charters. (2) A D13-07 status note: ACPT-05 remains Pending; it flips to complete only when the user-run results land in this file with zero blocker/major findings (blocker/major findings follow the fix-then-re-run policy D13-06; minors are recorded and deferred). (3) "Supplementary — VoiceOver+Safari re-run (v2.0 surfaces)" — a checklist scoped to the NEW v2.0 surfaces: library browse/search/tag filter, ingest form including calm refusal outcomes, review panel jump/curate, export/import dialogs, book groupings expand/collapse + chapter navigation — with its own empty findings table, explicitly marked NOT an ACPT-05 gate (D13-05: supplementary evidence honoring the protocol's re-run rule for material surface change; runs on the user's macOS hardware when ready; same fix-then-rerun policy). Do not modify docs/ACCEPTANCE-PROTOCOL.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md && grep -c "NVDA" .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md && git diff --stat docs/ACCEPTANCE-PROTOCOL.md | wc -l | grep -x "0" </automated>
  </verify>
  <acceptance_criteria>
    - 13-VERIFICATION.md exists and contains sections for the NVDA+Firefox run, the empty findings table with a severity column, the per-flow checklist for six flows + five charters, the D13-07 flip-condition note, and the VoiceOver supplementary checklist naming the five v2.0 surface groups
    - The file records role+name outcome discipline (contains the phrase "role" and "accessible name" in the instructions; no verbatim SR phrasing examples)
    - docs/ACCEPTANCE-PROTOCOL.md is byte-unchanged (git diff --stat empty for it)
  </acceptance_criteria>
  <done>The acceptance instrument is prepared: user-runnable NVDA runbook + record sheet + VO supplementary checklist, protocol untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D13-11 — withPdfDocument 30s timeout fake-timers spec</name>
  <files>tests/unit/server/pdfTimeout.spec.ts</files>
  <read_first>
  - server/pdfToBlocks.ts (L600-655: withPdfDocument — the Promise.race + setTimeout(PDF_EXTRACTION_TIMEOUT_MS) + typed rejection + loadingTask destroy in the outer finally; note whether withPdfDocument is exported or driven via pdfToBlocks)
  - tests/unit/server/pdf-to-blocks.spec.ts (the sibling precedent: unpdf loader mocking, IngestionError assertion helper, fixture bytes)
  - server/errors.ts (IngestionError reason field)
  - src/ingestion/types.ts (PDF_EXTRACTION_TIMEOUT_MS = 30_000)
  - tests/component/PageTurnControls.test.tsx (L64 vi.useFakeTimers precedent)
  - .planning/phases/11-pdf-intake/11-VERIFICATION.md (§ Acknowledged Gaps — the closure terms this test fulfills)
  </read_first>
  <behavior>
    - Test: with a mocked document loader resolving a minimal proxy (numPages within cap, loadingTask.destroy as a spy) and an operation promise that never settles, advancing fake timers past PDF_EXTRACTION_TIMEOUT_MS rejects with an IngestionError whose reason is "server-error" and whose message contains the exact copy "PDF extraction timed out — the document was too complex to read safely."
    - Test: loadingTask.destroy is called on the timeout path (always-destroy finally)
    - Test (control): a promptly-resolving operation completes without rejection and destroy is still called once (race wiring intact both ways)
  </behavior>
  <action>
    Create tests/unit/server/pdfTimeout.spec.ts following the tests/unit/server .spec.ts convention. Import from server/pdfToBlocks per the sibling spec's import pattern (call withPdfDocument directly if exported; otherwise drive it through the exported pdfToBlocks entry with the unpdf loader mocked so extraction hangs — mirror how pdf-to-blocks.spec.ts mocks the loader). Build a minimal stub proxy: numPages within the page cap and a loadingTask object whose destroy method is a vi.fn(). Use vi.useFakeTimers (with shouldAdvanceTime if the loader's microtasks need real ticks — the component precedent), await the loader resolution first, then vi.advanceTimersByTimeAsync(PDF_EXTRACTION_TIMEOUT_MS), and assert the three behaviors above (typed rejection with exact copy, destroy on the timeout path, control case). Restore real timers in afterEach. ZERO production changes — if the code shape resists the test, surface it rather than editing server code (the gap's closure terms are explicit).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdfTimeout.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/unit/server/pdfTimeout.spec.ts exits 0 with the three behaviors as named test cases
    - The rejection assertion checks reason "server-error" and the exact timeout copy string including the em dash
    - git status shows no modified files under server/ or src/ during this task (zero production changes)
    - The spec imports PDF_EXTRACTION_TIMEOUT_MS rather than hard-coding 30000 (no magic number drift)
  </acceptance_criteria>
  <done>The Phase 11 acknowledged 30s-timeout race gap is closed by test alone; 11-VERIFICATION acknowledged-gaps item fulfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation + test-only plan; no runtime surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Informational | Runbook leaking internal jargon into acceptance copy | mitigate | Ledger instructs role+name outcome recording per protocol discipline; calm DOC-06 voice for any reader-facing copy referenced |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; any dependency addition is out of scope |
</threat_model>

<verification>
- test -f .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md && grep -c "NVDA" (record sheet exists)
- npx vitest run tests/unit/server/pdfTimeout.spec.ts
</verification>

<success_criteria>
- ACPT-05 instrument prepared (runbook + record sheet + VO checklist); protocol byte-unchanged; flip condition recorded (requirement stays Pending until user results land)
- D13-11 timeout race proven with fake timers, zero production changes
</success_criteria>

<artifacts_produced>
- .planning/phases/13-polish-and-acceptance/13-VERIFICATION.md — new document (runbook, findings tables, checklists; no code exports)
- tests/unit/server/pdfTimeout.spec.ts — new spec
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-05-SUMMARY.md` when done
</output>
