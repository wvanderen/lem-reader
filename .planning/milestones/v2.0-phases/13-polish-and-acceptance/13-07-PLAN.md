---
phase: 13-polish-and-acceptance
plan: 07
type: execute
wave: 4
depends_on: []
files_modified:
  - src/app.css
  - src/ingestion/library/LibraryRow.tsx
  - tests/e2e/chrome/library-tidy.spec.ts
autonomous: true
gap_closure: true
requirements: [POLISH-06]

must_haves:
  truths:
    - "At wide viewports the Add-an-article section sits inside the same centered 1100px content measure as the library header, continue strip, and library list — it no longer spans edge-to-edge"
    - "At narrow viewports (360px) the add section fills the main content box exactly like its siblings — no regression"
    - "The library row remove control renders a real inline-SVG waste-bin glyph (no emoji glyph anywhere in src/)"
    - "The remove control is a quiet 44px-touch icon button whose hover conveys consequence via the destructive token"
  artifacts:
    - path: "src/app.css"
      provides: ".library-section-add measure rule + .library-row-remove quiet icon-button rule"
      contains: "library-section-add"
    - path: "src/ingestion/library/LibraryRow.tsx"
      provides: "TrashIcon inline-SVG glyph replacing the emoji glyph"
      contains: "TrashIcon"
    - path: "tests/e2e/chrome/library-tidy.spec.ts"
      provides: "strengthened measure-parity assertions for the add section (user bar, not the weaker original tidy contract)"
      contains: "library-section-add"
  key_links:
    - from: "tests/e2e/chrome/library-tidy.spec.ts"
      to: "src/app.css"
      via: "boundingBox width/center parity between .library-section-add and .library-list at 1400px"
      pattern: "library-section-add"
    - from: "src/ingestion/library/LibraryRow.tsx"
      to: "src/app.css"
      via: ".library-row-remove button styled by the new quiet icon-button rule"
      pattern: "library-row-remove"
  prohibitions:
    - statement: "No emoji-as-icon usage anywhere in src/"
      status: resolved
      verification: "repo-wide sweep for the waste-bin emoji over src/ returns zero files"
    - statement: "No motion properties in added CSS lines (A11Y-06 zero-motion discipline on the library surface)"
      status: resolved
      verification: "git diff of app.css added lines greps clean for the two CSS motion property names"
---

<objective>
Close gaps G1 + G3 from the Phase 13 user review (13-UAT.md § Gaps, 13-VERIFICATION.md § Gaps Found):

- **G1 (POLISH-06):** the Add-an-article section spans edge-to-edge instead of the shared content measure. Root cause: `.library-header`, `.continue-reading-strip`, `.library-search`, `.tag-filter`, and `.library-list` all carry the centered `max-width: 1100px` measure, but `.library-section-add` (wrapping IngestControl + the byte-stable `.status` live region) has no measure rule — its card fills the full main width.
- **G3 (icon policy / D13-12 chrome polish):** the library row remove button renders a waste-bin emoji as its glyph — the only emoji-as-icon in src/. It must become a real inline-SVG icon following the established GearIcon/HighlighterIcon anatomy.

The specs are strengthened to the user bar: library-tidy.spec.ts gains measure-parity assertions so the add section can never silently leave the shared measure again.

Purpose: the library home reads as one calm, measure-consistent surface (POLISH-06 user bar).
Output: two-line CSS measure fix + quiet icon-button rule, SVG glyph in LibraryRow, strengthened tidy spec.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
@.planning/phases/13-polish-and-acceptance/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constrain the add section to the shared 1100px measure + strengthen library-tidy parity assertions (G1)</name>
  <files>src/app.css, tests/e2e/chrome/library-tidy.spec.ts</files>
  <read_first>
  - src/app.css (the library chrome block around lines 1778–1965: .library-list / .continue-reading-strip / .library-search / .tag-filter / .library-header / .library-section rules, and the .ingest-control block around lines 749–810)
  - src/ingestion/library/LibraryView.tsx (the .library-section-add wrapper at line 236 holding IngestControl + the byte-stable .status live region)
  - tests/e2e/chrome/library-tidy.spec.ts (existing DOM-order + byte-stable-anchor assertions this task strengthens)
  - .planning/phases/13-polish-and-acceptance/13-03-SUMMARY.md (the tidy decisions: token-only spacing, margin-collapsing section wrappers, byte-stable anchors)
  </read_first>
  <action>
  In src/app.css, directly after the existing `.library-section` rule (line ~1955), add one scoped rule: `.library-section-add { max-width: 1100px; margin-inline: auto; }` with a short comment citing G1 + the sibling-measure discipline (.library-list / .library-header / .continue-reading-strip all carry the same centered 1100px measure; the add section now conforms). The wrapper stays padding/border-free so the 13-03 margin-collapsing rhythm is preserved; this is the exact centering pattern `.library-header` already uses (max-width 1100px + margin-inline auto). Do NOT touch `.ingest-control`'s own block, the side-sheet rules, or any dialog rule. Added lines contain zero CSS motion properties; comment prose must not name greppable property tokens (13-01/13-03 comment discipline).

  Strengthen tests/e2e/chrome/library-tidy.spec.ts with a new test "add section shares the library measure (G1)": set viewport 1400×900, read `boundingBox()` of `.library-section-add` and of `.library-list`, assert both widths are less than or equal to 1100 and the two boxes share the same horizontal center within 1px; then set viewport 360×640 and assert both boxes have equal width again (both fill the main content box — no narrow-viewport regression). Follow the file's existing helper/style discipline (expect.poll-free direct reads after the ready heading is visible; no fixed sleeps; 3-engine matrix inherited from the plain test() pattern).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/library-tidy.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/app.css contains a `.library-section-add` rule with `max-width: 1100px` and `margin-inline: auto`
  - `git diff src/app.css` added lines contain zero matches for the two CSS motion property names
  - library-tidy.spec.ts contains a test whose name contains "measure" asserting width ≤ 1100 and center parity between `.library-section-add` and `.library-list` at 1400×900, plus the 360×640 equal-width cell
  - `npx playwright test tests/e2e/chrome/library-tidy.spec.ts` exits 0 across chromium/firefox/webkit
  </acceptance_criteria>
  <done>The add section renders inside the shared centered measure at wide viewports and fills the content box exactly like siblings at 360px; the strengthened spec pins the parity permanently.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the LibraryRow emoji glyph with an inline-SVG trash icon + quiet icon-button styling (G3)</name>
  <files>src/ingestion/library/LibraryRow.tsx, src/app.css</files>
  <read_first>
  - src/ingestion/library/LibraryRow.tsx (the remove button block at lines 117–127; the byte-stable aria-label template)
  - src/reader/Header.tsx (GearIcon at lines 139–157 — the canonical inline-SVG icon anatomy: 20×20, viewBox 0 0 24 24, fill none, stroke currentColor, strokeWidth 1.75, round caps/joins, aria-hidden, focusable false)
  - src/app.css (the .tag-chip-remove rule around lines 1989–2010 — the quiet destructive icon-button token precedent: transparent background, transparent hairline border, 44px touch box, destructive color on hover)
  </read_first>
  <action>
  In src/ingestion/library/LibraryRow.tsx: delete the emoji span inside the `.library-row-remove` button and render a local `TrashIcon` function component instead, following the GearIcon anatomy exactly (20×20, viewBox 0 0 24 24, fill none, stroke currentColor, strokeWidth 1.75, strokeLinecap round, strokeLinejoin round, aria-hidden true, focusable false). Concrete path geometry for the glyph: lid line path `M3 6h18`; handle path `M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2`; body path `M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6`; inner line path `M10 11v6`; inner line path `M14 11v6`. The button's `type="button"`, `className="library-row-remove"`, and the aria-label template expression `Remove ${article.provenance.title} from library` stay byte-identical (dialog-centering.spec.ts and remove-cascade.spec.ts locate this button by that accessible name). Update the file-header comment note about the remove affordance without naming the old emoji character in prose.

  In src/app.css, add a `.library-row-remove` rule (the class currently has no rule at all) next to the library-row block: mirror the .tag-chip-remove discipline — transparent background, transparent border, 4px border-radius, min-height/min-width var(--touch), inline-flex centering, color var(--ink-soft), cursor pointer; a `:hover` variant shifts color AND border-color to var(--destructive) so consequence is conveyed beyond the aria-label (forced-colors-safe: shape + name carry the meaning). Zero motion properties; comment prose avoids greppable property names.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/library/remove-cascade.spec.ts tests/e2e/chrome/dialog-centering.spec.ts tests/e2e/chrome/library-tidy.spec.ts && npx vitest run tests/unit/library/</automated>
    <!-- planner-discipline-allow: 🗑 -->
  </verify>
  <acceptance_criteria>
  <!-- planner-discipline-allow: 🗑 -->
  - src/ingestion/library/LibraryRow.tsx contains `function TrashIcon(` rendering an `svg` with `viewBox="0 0 24 24"` and `focusable="false"`, mounted inside the `.library-row-remove` button
  - a repo-wide search for the waste-bin emoji over `src/` returns zero files (the icon sweep — this was the only emoji-as-icon, so zero matches proves the policy)
  - the remove button's aria-label template expression is byte-identical to `Remove ${article.provenance.title} from library`
  - src/app.css contains a `.library-row-remove` rule using `var(--touch)` and a hover variant using `var(--destructive)`
  - remove-cascade + dialog-centering + library-tidy specs exit 0 on all three engines (button accessible name unchanged)
  </acceptance_criteria>
  <done>Library rows remove articles through a real SVG icon in a quiet 44px icon button; the repo-wide emoji-as-icon sweep is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | CSS-only measure fix + presentational icon swap; no new input, data, or network path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07-01 | Tampering | LibraryRow remove button | accept | Glyph swap only; accessible name + click path byte-identical; destructive action still gated behind RemoveConfirm (Pitfall 8 single call site unchanged) |
| T-13-07-02 | Tampering | src/app.css additions | accept | Token-only authored CSS; no markup-string APIs, no new selectors reachable by content strings |
| T-13-07-SC | Tampering | package installs | accept | Zero package installs in this plan (no package.json changes) |
</threat_model>

<verification>
- `npx playwright test tests/e2e/chrome/library-tidy.spec.ts` — strengthened parity + original anchors green, 3 engines
- `npx playwright test tests/e2e/library/` — full library regression (81-cell baseline) green, remove-cascade + search-tag-filter included
- `npx playwright test tests/e2e/reduced-motion.spec.ts` — zero-motion discipline holds after CSS edits
- Icon sweep: repo-wide emoji-as-icon search over src/ returns zero files
</verification>

<success_criteria>
- G1 closed at the user bar: add section conforms to the shared content measure/inset, proven by strengthened e2e assertions
- G3 closed at the user bar: real SVG icon, zero emoji-as-icon in src/, quiet destructive icon-button styling
- No library byte-stable anchor regressed (main#main, h1, .status live region, LibraryRow link markup)
</success_criteria>

## Artifacts this phase produces

- CSS rule `.library-section-add` (scoped measure: max-width 1100px + margin-inline auto)
- CSS rules `.library-row-remove` and `.library-row-remove:hover` (quiet destructive icon-button tokens)
- React component `TrashIcon` (local to src/ingestion/library/LibraryRow.tsx; 20×20 stroke SVG waste-bin glyph)
- e2e test "add section shares the library measure (G1)" in tests/e2e/chrome/library-tidy.spec.ts
- No new exports, packages, CLI flags, or data shapes

<output>
Create `.planning/phases/13-polish-and-acceptance/13-07-SUMMARY.md` when done
</output>
