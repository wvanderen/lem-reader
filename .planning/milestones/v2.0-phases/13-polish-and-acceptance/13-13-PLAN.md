---
phase: 13-polish-and-acceptance
plan: 13
type: execute
wave: 8
depends_on: ["13-12"]
files_modified:
  - docs/ACCEPTANCE-PROTOCOL.md
  - tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts
autonomous: true
gap_closure: true
requirements: [ACPT-05]

must_haves:
  truths:
    - "ACCEPTANCE-PROTOCOL.md (v1.2) Flow C C1 instructs NVDA (Firefox) users to enable Native Selection Mode (NVDA+shift+f10, NVDA >= 2024.1) BEFORE browse-mode Shift+arrows, and the parenthetical claiming the browse-mode selection is Firefox-native is gone from the document"
    - "ACCEPTANCE-PROTOCOL.md documents the older-NVDA fallback (F7 caret browsing + focus-mode Shift+arrows) and the platform boundary: with Native Selection Mode OFF (any default NVDA), browse-mode selections exist only in NVDA's virtual buffer — the page's document selection never changes, so no page-side code can observe them (a platform boundary, not a product defect)"
    - "The e2e suite pins BOTH sides of the G8 boundary at the page-observable layer: a page-visible document selection mounts the toolbar AND fires the 'Highlight actions available.' announce (the C1 cue — previously asserted by zero automation), while a page with NO visible selection stays structurally silent (no toolbar, no cue) across a keydown-less focus move and a settle window, on chromium/firefox/webkit"
    - "ZERO production source changes ship (the page-side mount path is exonerated by the G8 diagnosis); ACPT-05 remains pending the human NVDA re-run of Flow C on protocol v1.2 per D13-06/D13-07"
  artifacts:
    - path: "docs/ACCEPTANCE-PROTOCOL.md"
      provides: "Flow C corrected for the NVDA native-selection-mode precondition + older-NVDA fallback + documented platform boundary (v1.2)"
      contains: "NVDA+shift+f10"
    - path: "tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts"
      provides: "The selection-gated mount/announce boundary pin — both sides of the G8 platform boundary at the only layer automation can see"
      min_lines: 120
  key_links:
    - from: "tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts"
      to: "tests/e2e/annotations/_fixtures.ts"
      via: "reuses wipeDatabase/openArticle/FIXTURES/findFirstBlockWithText/selectRangeInBlock wholesale (no forked harness) — the 13-12 keydownless-spec discipline"
      pattern: "selectRangeInBlock"
    - from: "tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts"
      to: "src/reader/annotations/SelectionToolbar.tsx"
      via: "asserts the toolbar-INTERNAL role=status region (L222-224) receives the announce-on-appear cue — NOT ArticleView's D5-12 CRUD announcementRegion"
      pattern: "Highlight actions available"
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: ".planning/debug/g8-toolbar-never-mounts-nvda.md"
      via: "Flow C native-selection note cites the G8 diagnosis (NVDA User Guide §Native Selection Mode + gecko_ia2.py evidence)"
      pattern: "g8-toolbar-never-mounts-nvda"
    - from: "docs/ACCEPTANCE-PROTOCOL.md"
      to: "NVDA User Guide (§Native Selection Mode)"
      via: "C1 gesture + the note encode the toggle gesture and its version history (2024.1 per-document toggle; 2026.3 persistent setting, also off by default)"
      pattern: "NVDA\\+shift\\+f10"
---

<objective>
Close UAT gap G8 (13-UAT.md, status: diagnosed): the ACPT-05 Flow C failure on the v1.1 protocol where NVDA+Firefox browse-mode Shift+arrows selection NEVER reaches the Firefox document selection (Native Selection Mode is off by default), so the selection toolbar never mounts and the "Highlight actions available." announce never fires — the page is structurally selection-blind under the documented C1 gesture.

Purpose: Make Flow C executable as written by an NVDA+Firefox tester on default-adjacent hardware — the ACPT-05 gate (D13-06/D13-07) is blocked solely on this gap.
Output: (1) ACCEPTANCE-PROTOCOL.md v1.2 — C1 native-selection-mode precondition (NVDA+shift+f10), the false Firefox-native parenthetical removed, an older-NVDA F7 caret-browsing fallback, and a documented platform boundary note; (2) a selection-gated mount/announce boundary e2e spec pinning both sides of the G8 boundary at the page-observable layer. Zero production source changes.

## Decision G8-D1 (made in planning, resolving the gap's "also consider" item)

**Chosen: protocol v1.2 (primary) + a page-observable boundary-pin spec. Rejected: protocol-only, and any production source change.** The executor MUST NOT modify anything under src/ — the page-side mount path is exonerated (3-engine e2e green; programmatic selections mount the toolbar in firefox; 13-12 shipped zero source changes; ArticleView's selectionchange listener is the only mount driver and is correctly built).

Justification (recorded here so it is not re-litigated):

1. **No page-side fix exists — by construction.** With Native Selection Mode OFF, browse-mode Shift+arrows selects only within NVDA's virtual buffer ("not within the application itself... not visible on screen" — NVDA User Guide §Native Selection Mode; NVDA source gecko_ia2.py `_setSelectionOffsets` pushes the selection to the document only under `_nativeAppSelectionMode`). The DOM never changes, so no DOM API, event, observer, or heuristic in the page can detect it. This is a platform boundary like G7's browse-mode Tab consumption — the only honest fix is the protocol instructing the correct gesture.
2. **Protocol-only was considered and rejected as insufficient.** The G7 precedent (13-12) pinned its zero-source boundary with a spec, and the G8 diagnosis exposes a worse automation hole: the "Highlight actions available." mount cue — the confirmation signal v1.1 made load-bearing at C1 and v1.2 keeps — is asserted by ZERO automation (repo-wide grep: the string exists only in SelectionToolbar.tsx and the protocol). If a future refactor breaks the announce, the next NVDA re-run fails at C1 again and gets re-diagnosed from scratch — the exact G6→G7→G8 cascade pattern. The spec closes that hole.
3. **The spec stays honest about its layer.** Playwright cannot emulate NVDA's virtual buffer (the G8 debug session states local Playwright/firefox runs add nothing for the NVDA-layer delta). What it CAN pin is the page-observable contract on both sides: document selection follows ⇒ toolbar + cue (the native-selection-ON condition); no document selection ⇒ structural silence regardless of focus moves (the native-selection-OFF condition). That pins "silence under native-selection-OFF is CORRECT page behavior, not a bug" — pointing any future silent re-run at the NVDA layer/protocol adherence instead of the page.
4. **Zero product risk.** Pointer, sighted-keyboard, Chromium, and VoiceOver paths remain byte-unchanged; no dependency changes.

## Missing-item coverage map

- Item 1 (protocol v1.2: C1 native-selection instruction + false parenthetical fix) → Task 2
- Item 2 (protocol v1.2: older-NVDA fallback + platform-boundary documentation) → Task 2 (the new note)
- Item 3 (re-run ACPT-05 Flow C on v1.2 per D13-06; residual: NVDA verbalizes the mount announce in browse mode with native selection ON) → recorded as re-run prerequisites in the SUMMARY; this plan does NOT flip ACPT-05 (13-11/13-12 precedent — it flips only when the tester's re-run lands in 13-VERIFICATION.md §1.3/§1.4 with zero blocker/major)
- "Also consider" (analogous doc/test guard) → Task 1, per decision G8-D1
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-UAT.md
@.planning/phases/13-polish-and-acceptance/13-12-SUMMARY.md
@.planning/debug/g8-toolbar-never-mounts-nvda.md
@docs/ACCEPTANCE-PROTOCOL.md
@src/routes/ArticleView.tsx
@src/reader/annotations/SelectionToolbar.tsx
@tests/e2e/annotations/toolbar-keydownless-focus.spec.ts
@tests/e2e/annotations/_fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selection-gated mount/announce boundary e2e spec (both sides of the G8 platform boundary)</name>
  <files>tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts</files>
  <behavior>
    - Test 1 (native-selection-ON equivalent): a programmatic in-block selection (the page-visible condition NVDA's native selection mode produces) mounts the toolbar AND the toolbar-internal polite status region announces the cue text "Highlight actions available." — while ArticleView's separate CRUD announcement region stays silent (region separation).
    - Test 2 (native-selection-OFF equivalent): with NO selection ever made, a keydown-less programmatic focus to the Previous page chevron (the G7-proven browse-mode Tab consequence, still possible with native selection off) plus a settle window leaves the page structurally silent on every engine: document selection collapsed, zero keydowns delivered, toolbar count 0, cue text absent from the DOM.
  </behavior>
  <action>
    Create tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts (UAT G8 "also consider" item, per decision G8-D1). Reuse the _fixtures.ts helpers wholesale (wipeDatabase, openArticle, FIXTURES, findFirstBlockWithText, selectRangeInBlock) exactly as toolbar-keydownless-focus.spec.ts does; FIXTURE = FIXTURES[0] (essay-long-form), paginated default mode, no switchMode. File header comment states the G8 provenance and the pinned contract: with NVDA Native Selection Mode OFF (the default on every NVDA since the 2024.1 per-document toggle, including 2026.3's persistent-setting release where the setting is also disabled by default), browse-mode Shift+arrows selects only within NVDA's virtual buffer and never touches the Firefox document selection (NVDA User Guide §Native Selection Mode; NVDA source gecko_ia2.py pushes the selection to the document only under _nativeAppSelectionMode; .planning/debug/g8-toolbar-never-mounts-nvda.md) — so the selection-driven toolbar lifecycle (ArticleView selectionchange listener → selectionRect/captureResult → SelectionToolbar render) is unreachable BY CONSTRUCTION: silence is correct page behavior, not a defect. This spec pins that contract at the page-observable layer (the only layer automation can see — the NVDA virtual buffer is not emulatable in Playwright); the gesture fix itself lives in ACCEPTANCE-PROTOCOL.md Flow C v1.2.

    Test 1 — "a page-visible selection mounts the toolbar and fires the announce cue (the native-selection-ON condition)": wipeDatabase → openArticle → findFirstBlockWithText(page, 24) not -1 → selectRangeInBlock(page, blockIdx, 0, 18) truthy (single-block, captureResult.ok true → buttons variant) → expect getByRole("toolbar", { name: "Highlight actions" }) visible → expect the toolbar-INTERNAL live region to contain the cue: page.getByRole("toolbar", { name: "Highlight actions" }).locator("[role='status']") toContainText(/Highlight actions available/i. CRITICAL LOCATOR FACT: the cue's region is the visually-hidden p role=status aria-live=polite rendered INSIDE SelectionToolbar (SelectionToolbar.tsx L222-224) — do NOT use the announcementRegion helper for the cue; that helper targets ArticleView's separate D5-12 CRUD announce region. Assert the separation: expect(announcementRegion(page)).not.toContainText(/Highlight actions available/i (that region announces "Highlight saved." etc. on actions only — no action occurred, so it is empty). This is the C1 mount-cue contract the protocol makes load-bearing — previously asserted by zero automation.

    Test 2 — "no page-visible selection ⇒ structural silence (the native-selection-OFF condition)": fresh openArticle (wipeDatabase first); NEVER create a selection; assert the starting state via ONE page.evaluate: window.getSelection() isCollapsed (or absent). Then, in ONE page.evaluate, install a window-scoped keydown counter with a capture-phase listener (installed AFTER all setup activity, counter starting at 0); in a SECOND page.evaluate, focus the Previous page chevron (button.page-turn-previous, aria-label "Previous page") — the keydown-less DOM-focus move NVDA browse mode produces for Tab, which remains possible with native selection off; NO page.keyboard call may occur anywhere in this test. waitForTimeout(300) for the rAF-throttled selectionchange machinery to settle, then ONE atomic evaluate returning { keydowns, isCollapsed, toolbarCount } and assert on all engines identically (no engine-keyed split — nothing engine-specific is asserted): keydowns 0, isCollapsed true, toolbarCount 0; plus expect(page.getByText("Highlight actions available")).toHaveCount(0) (the cue text exists nowhere in the DOM — the live region renders only inside a mounted toolbar). Header comment: this pins WHY the v1.2 protocol fix is a gesture instruction and not a product change — focus moves and settle time can never manufacture a selection-driven mount.

    This is a boundary-PINNING spec, not TDD: decision G8-D1 ships zero production changes — BOTH tests must be GREEN on the current build immediately; they assert today's diagnosed behavior as the documented contract. If any assertion fails on the current build, the G8 diagnosis itself is contradicted: stop and surface as Rule 4 (do NOT weaken assertions to force green).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts && [ "$(rg -c "Highlight actions available" tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts)" -ge 2 ] && rg -c "toolbar-mount-selection-gated" tests/e2e/annotations/toolbar-mount-selection-gated.spec.ts</automated>
  </verify>
  <done>New spec green on chromium/firefox/webkit with both sides of the boundary pinned: selection ⇒ toolbar + cue announce (toolbar-internal region; CRUD region silent), no selection ⇒ zero keydowns + collapsed selection + no toolbar + no cue text anywhere in the DOM; reuses _fixtures.ts helpers wholesale; zero production source files touched.</done>
</task>

<task type="auto">
  <name>Task 2: ACCEPTANCE-PROTOCOL.md Flow C NVDA native-selection-mode correction (v1.1 → v1.2)</name>
  <files>docs/ACCEPTANCE-PROTOCOL.md</files>
  <action>
    Correct Flow C per the G8 fix direction (UAT G8 items 1 + 2; decision G8-D1). Follow the doc's own versioning discipline (final section: bump the Version field when flows are edited).

    1. Header table: Version 1.1 → 1.2. Touch no other header field (the run-date/tester blanks stay blank — filled at execution).

    2. Flow C callout (the "Primary SR path" block): keep every existing sentence byte-unchanged (H/N-consumption, single-Tab reachability, G7 focus-mode-at-C2 instruction with its debug-doc reference), and APPEND the G8 precondition at its end: the focus-mode instruction at C2 has a sibling precondition one step earlier, at C1 — by default, NVDA browse-mode text selection exists only within NVDA's virtual buffer and never changes the Firefox document selection ("not within the application itself" — official NVDA User Guide, §Native Selection Mode), so the tester MUST enable Native Selection Mode (NVDA+shift+f10) BEFORE the C1 Shift+arrows selection or the toolbar cannot mount and no announce can fire; see the note below the checklist and .planning/debug/g8-toolbar-never-mounts-nvda.md (gap G8).

    3. C1 row, Keyboard sequence column: replace ONLY the NVDA sub-clause — the one currently claiming browse-mode Shift+arrows yields a native Firefox selection (the false G8 premise) — with: NVDA (Firefox): enable Native Selection Mode first (**NVDA+shift+f10**, NVDA >= 2024.1 — required, see the note below), then browse-mode **Shift+arrows**. The sighted-keyboard and VoiceOver gesture text stays byte-unchanged, as does the trailing "across several words".

    4. C1 row, Expected outcome column: keep all existing sentences (single-block rule D5-05/D5-06, SR announces the selected text, the "Highlight actions available." mount cue) and append one clause: with Native Selection Mode on, the selection is also reflected in the page itself (visible on screen).

    5. NEW note block, placed immediately AFTER the existing "Sighted keyboard/mouse convenience" note at the end of Flow C, with its own bold title ("**NVDA Native Selection Mode (required at C1):**"). Content, four short parts:
       (a) WHY — by default on every NVDA since 2024.1 (the per-document toggle; NVDA 2026.3 adds a persistent Browse Mode setting, also disabled by default), browse-mode Shift+arrows selects only within NVDA's virtual buffer: the selection is not visible on screen and the page's document selection never changes. The toolbar's lifecycle is driven by the document selection, so with native selection off the toolbar cannot appear — and NO page-side code can observe a buffer-only selection. This is a platform boundary, not a product defect (NVDA User Guide §Native Selection Mode; .planning/debug/g8-toolbar-never-mounts-nvda.md, gap G8).
       (b) WHAT TO DO — NVDA >= 2024.1: press NVDA+shift+f10 (per-document; NVDA announces the mode change) BEFORE the C1 Shift+arrows; on NVDA 2026.3+ it can be made persistent in Browse Mode settings (still off by default).
       (c) FALLBACK — older NVDA or a document where the toggle is unavailable: press F7 to enable Firefox caret browsing (confirm the prompt), then select with Shift+arrows from focus mode — the caret selection IS the document selection (page-visible), then continue at C2 as written.
       (d) RE-RUN READING AID — the C1 mount cue ("Highlight actions available.") is the confirmation the document selection followed; and keep C1 selections within a single block — a selection crossing a block boundary mounts the toolbar in a silent hint variant (no buttons, no announce).

    6. Diff discipline (the 13-11/13-12 precedent): changes confined to the header Version field and Flow C (callout + C1 row + the new note). Every other flow and section stays byte-unchanged — C2/C3 rows need NO edits (with the document selection following, the v1.1 C2 NVDA+Space-then-Tab and C3 focus-mode instructions are correct as written). Ordering: the full verify gate — including its git diff sub-commands — runs BEFORE the task commit while the edits are uncommitted; commit only after the gate passes.
  </action>
  <verify>
    <automated>[ "$(rg -c "NVDA\+shift\+f10" docs/ACCEPTANCE-PROTOCOL.md)" -ge 2 ] && rg -n "\*\*Version\*\* \| 1\.2" docs/ACCEPTANCE-PROTOCOL.md && rg -c "F7" docs/ACCEPTANCE-PROTOCOL.md && rg -c "g8-toolbar-never-mounts-nvda" docs/ACCEPTANCE-PROTOCOL.md && ! rg -q "Firefox-native selection" docs/ACCEPTANCE-PROTOCOL.md && git diff --stat && git diff docs/ACCEPTANCE-PROTOCOL.md | grep -c "^[+-]"</automated>
    <!-- planner-discipline-allow: Firefox-native selection -->
    Gate timing (13-11/13-12 precedent): run this gate BEFORE the task commit — the two git diff sub-commands must observe the uncommitted Flow C edits (post-commit, git diff is empty and grep -c returns 0, false-failing the chain). The negative grep requires the false G8 parenthetical to be gone from the document entirely (the new note describes the behavior without restoring the disproven phrase). Commit only after every sub-command passes.
  </verify>
  <done>Protocol is v1.2; C1 directs NVDA users through NVDA+shift+f10 before Shift+arrows with the disproven native-selection parenthetical gone; the new note documents why (platform boundary), the gesture, the F7 caret-browsing fallback for older NVDA, and the single-block re-run aid; NVDA+shift+f10 appears at least twice (C1 + note); the G8 debug doc is referenced; git diff confined to the Version line + Flow C.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs + test-only change; zero runtime code, zero dependencies, zero data paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13-01 | Tampering | e2e spec (test code) | accept | Test file runs in the sandboxed Playwright harness against fixtures; no production input surface |
| T-13-13-02 | Information Disclosure | ACCEPTANCE-PROTOCOL.md | accept | Public-facing manual test instructions; no secrets, credentials, or personal data |
| T-13-13-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (no package.json / lockfile changes — verified in the gates) |
</threat_model>

<verification>
1. Task gates (run per-task above): new boundary spec green on all 3 engines with the cue-text grep checks; protocol greps pass (NVDA+shift+f10 >= 2, Version 1.2, F7 present, G8 debug-doc reference present, false parenthetical absent).
2. Regression net: npx playwright test tests/e2e/annotations/ — the full annotations directory (174 prior cells + the new spec's cells) green on chromium/firefox/webkit, zero pre-existing failures introduced; in particular toolbar-keydownless-focus.spec.ts and toolbar-tab-path.spec.ts stay green (untouched).
3. Unit sanity: npm run test:unit -- --run — green (no src/ changes shipped, so any failure signals an environmental regression, not this plan).
4. Zero-product-change proof: git diff --name-only across the plan's commits shows ONLY the two files_modified paths; in particular src/ and package.json/lockfiles are byte-unchanged (decision G8-D1 compliance + T-13-13-SC).
5. Protocol diff discipline: git diff -U0 docs/ACCEPTANCE-PROTOCOL.md hunks confined to the Version header line + Flow C (callout, C1 row, new note).
6. Re-run readiness (record in the SUMMARY, UAT G8 item 3): ACPT-05 stays Pending; the tester's NVDA+Firefox re-run of Flow C on the v1.2 protocol must (a) enable Native Selection Mode (NVDA+shift+f10) before the C1 Shift+arrows, (b) confirm the residual — NVDA verbalizes the "Highlight actions available." mount announce in browse mode with native selection ON (page-side firing is now pinned by Task 1's spec; only the verbalization needs the human), and (c) complete C2/C3 as documented in v1.1 (unchanged). Results land in 13-VERIFICATION.md §1.3/§1.4; zero blocker/major across the full protocol flips ACPT-05 (D13-06/D13-07) — the 13-11/13-12 precedent: this plan does NOT flip it.
</verification>

<success_criteria>
- UAT G8's three missing items are all addressed: protocol v1.2 shipped (C1 native-selection instruction + false premise removed; fallback + boundary documented) and the re-run prerequisites recorded — the "also consider" guard answered by decision G8-D1 (Task 1).
- The C1 mount cue now has an automated substrate: the announce-on-appear contract is pinned both ways (fires on selection-driven mount; structurally silent without a page-visible selection) — the protocol expectation no longer rests on zero automation.
- The G8 platform boundary is documented in the protocol with its authoritative sources (NVDA User Guide §Native Selection Mode + the G8 debug session), so a future silent re-run points at the NVDA layer/protocol adherence instead of triggering another page-side diagnosis cascade.
- The annotations e2e directory and unit suite are green; no file outside files_modified changed; zero production source changes.
- ACPT-05 remains Pending with the re-run prerequisites recorded for the tester.
</success_criteria>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-13-SUMMARY.md` when done.
The SUMMARY must record decision G8-D1 and its justification (zero production changes are final for this gap; buffer-only selections are unobservable by construction), the boundary-spec semantics (both sides, and the layer honesty — the NVDA virtual buffer is not Playwright-emulatable), and the ACPT-05 re-run prerequisites (D13-06/D13-07 — enable native selection mode before C1; residual: announce verbalization in browse mode; flip only on the tester's 13-VERIFICATION.md results).
</output>
