---
phase: 13-polish-and-acceptance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - src/ingestion/library/LibraryView.tsx
  - tests/e2e/chrome/dialog-centering.spec.ts
  - tests/e2e/chrome/library-tidy.spec.ts
autonomous: true
requirements: [POLISH-04, POLISH-06]
must_haves:
  truths:
    - "A reader opening the note editor, wipe confirmation, library remove confirmation, or import preview sees the dialog centered in the viewport — not pinned to the top-left corner (all 3 engines)"
    - "The settings panel and annotations drawer keep their intentional side-sheet placement (unchanged)"
    - "A reader visiting the library home sees three clearly ordered regions — continue reading, then add content, then the library list — with consistent spacing"
    - "Every existing library behavior (browse/open/search/tag filter/v1 regression) keeps working byte-unchanged"
  artifacts:
    - path: "src/app.css"
      provides: "margin auto restoration on the four centered modal dialog selectors; library section spacing tokens"
      contains: "dialog.highlight-popover"
    - path: "tests/e2e/chrome/dialog-centering.spec.ts"
      provides: "boundingBox centering assertions for the four modals across 3 engines"
      min_lines: 60
    - path: "tests/e2e/chrome/library-tidy.spec.ts"
      provides: "Library section-order/visibility assertions"
      min_lines: 30
  key_links:
    - from: "src/app.css dialog.highlight-popover (and 3 sibling modal blocks)"
      to: "UA dialog:modal centering"
      via: "margin auto restores WHATWG rendering §15.3.3 margin:auto centering defeated by the current zero margin"
      pattern: "margin: auto"
    - from: "src/ingestion/library/LibraryView.tsx"
      to: "ContinueReadingStrip / IngestControl / LibrarySearch+TagFilter / library list"
      via: "DOM section ordering: continue strip before ingest control before search/filter before the ul list"
      pattern: "library-list"
  prohibitions:
    - statement: "The intentional side sheets (dialog.settings-panel ~L442, dialog.annotations-drawer ~L1699) keep their margin/anchoring rules byte-unchanged"
      status: resolved
      verification: "git diff on src/app.css shows no changed line inside the settings-panel or annotations-drawer rule blocks"
    - statement: "No JS/manual positioning is introduced for any dialog (the UA centers via margin)"
      status: resolved
      verification: "git diff shows no changes to NotePopover.tsx / WipeConfirm.tsx / ImportPreviewDialog.tsx / LibraryView dialog logic in Task 1"
    - statement: "No new CSS motion properties in this plan's app.css additions (token-only, zero-motion discipline)"
      status: resolved
      verification: "rg -c 'transition|animation' on the added diff lines returns 0 (check via git diff | rg on added lines)"
    - statement: "Byte-stable library anchors preserved: main id=main, h1 'Saved articles', the .status live region, LibraryRow markup, the hash-assignment fallbacks"
      status: resolved
      verification: "existing library specs exit 0 byte-unchanged + tidy spec asserts the anchors"
---

<objective>
Two chrome-polish items (D13-12 widened scope): restore centered placement on all four modal dialogs whose zero-margin declaration defeats the UA stylesheet's margin-auto centering (POLISH-04, D13-14 — the WHATWG-verified root cause), and give LibraryView a bounded organizational tidy — three clear sections in a calm order with consistent spacing tokens (POLISH-06, D13-16) — all within existing components, no new features.

Purpose: "Highlight/note modal always goes in top-left corner — seems like a visual bug" and "Home page needs more organization and polish. Feels haphazard."
Output: app.css dialog fixes + section structure, two chrome e2e specs, existing library specs green byte-unchanged.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-acceptance/13-CONTEXT.md
@.planning/phases/13-polish-and-acceptance/13-RESEARCH.md
@.planning/phases/13-polish-and-acceptance/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore dialog centering (4 modals) + centering spec</name>
  <files>src/app.css, tests/e2e/chrome/dialog-centering.spec.ts</files>
  <read_first>
  - src/app.css (dialog.highlight-popover ~L1475 with the centering-intent comment ~L1489; dialog.wipe-confirm ~L669; dialog.library-remove-confirm ~L2030; dialog.import-preview ~L2240; the DO-NOT-TOUCH side sheets dialog.settings-panel ~L442 and dialog.annotations-drawer ~L1699)
  - src/reader/annotations/NotePopover.tsx (how the note editor opens — highlight activation path)
  - src/reader/WipeConfirm.tsx (wipe flow trigger path)
  - src/reader/ImportPreviewDialog.tsx (import preview trigger path)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (Pattern 3 — WHATWG-verified root cause + the four-surface scope table)
  - tests/e2e/portability/_portability.ts (openSettings / bundleInput helpers for driving wipe + import-preview dialogs)
  </read_first>
  <action>
    In src/app.css change the margin declaration on exactly four modal dialog blocks — dialog.highlight-popover, dialog.wipe-confirm, dialog.library-remove-confirm, dialog.import-preview — from the zero shorthand to margin: auto, restoring the UA stylesheet's dialog:modal centering (WHATWG HTML Rendering §15.3.3: author zero-margins defeat margin:auto, resolving the over-constrained fixed box to the start edges = top-left in LTR). Keep the adjacent comments documenting centered intent accurate. Do NOT touch dialog.settings-panel or dialog.annotations-drawer (intentional margin-zero side sheets anchored to the inline end — a blanket dialog selector fix breaks them; documented anti-pattern). No JavaScript positioning changes anywhere — the fix is CSS-only. New spec tests/e2e/chrome/dialog-centering.spec.ts (create tests/e2e/chrome/), plain test() declarations: parameterize the four modals, open each through its real UI path (highlight-activate the note editor on an open article; open settings and drive the wipe-confirmation destructive screen; library-row remove confirmation; settings import preview with a valid bundle file per the _portability helpers), then locator.boundingBox() assertions per modal: the horizontal center and vertical center of the dialog box each sit within a small fixed tolerance (suggest 24px, absorbing subpixel/scrollbar variance) of the viewport's corresponding center — asserted on chromium, firefox, and webkit.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/dialog-centering.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - In src/app.css, each of the four named dialog blocks declares margin: auto (region-scoped check per block), and the settings-panel/annotations-drawer blocks are byte-unchanged in the git diff
    - The git diff for this task touches no .tsx file (CSS-only fix)
    - tests/e2e/chrome/dialog-centering.spec.ts exits 0 on chromium, firefox, and webkit with centering assertions for all four modals (both axes)
    - npx playwright test tests/e2e/reduced-motion.spec.ts exits 0 (global reduced-motion gate unaffected)
  </acceptance_criteria>
  <done>All four centered modals open centered across the matrix; side sheets untouched; fix is CSS-only.</done>
</task>

<task type="auto">
  <name>Task 2: LibraryView bounded tidy + tidy assertions</name>
  <files>src/ingestion/library/LibraryView.tsx, src/app.css, tests/e2e/chrome/library-tidy.spec.ts</files>
  <read_first>
  - src/ingestion/library/LibraryView.tsx (L191-332 current structure; L97-147 cancelled-flag load effect — unchanged; L202-210 Review-highlights button; L297-302/L321-327 hash-assignment fallbacks)
  - src/ingestion/library/ContinueReadingStrip.tsx, LibrarySearch.tsx, TagFilter.tsx, LibraryRow.tsx (siblings in the tidy; markup byte-stability)
  - tests/e2e/library/browse-open.spec.ts, tests/e2e/library/search-tag-filter.spec.ts, tests/e2e/library/v1-regression.spec.ts (the byte-unchanged regression set)
  - .planning/phases/13-polish-and-acceptance/13-RESEARCH.md (D13-16 audit: current order h1 → Review button → IngestControl → status → ContinueReadingStrip → search/tag-filter → list)
  </read_first>
  <action>
    Restructure LibraryView's markup into clearly grouped sections in this order: (1) a header row holding the h1 "Saved articles" with the Review-highlights button as a quiet control beside it; (2) a continue-reading section wrapping ContinueReadingStrip; (3) an add-content section holding IngestControl with the .status live region directly following it; (4) the library-list section holding LibrarySearch, TagFilter, and the ul.library-list. Use section/grouping elements with consistent spacing via the existing --space-* custom properties and calm visual hierarchy (secondary controls quiet); no new features, no new data loading, no copy changes beyond calm section labels IF any are needed (prefer none; the D13-16 audit names ordering/density/weight, not missing labels). Preserve byte-stable anchors exactly: main with id "main" (skip-link target), the h1 text, the .status live region with role status and aria-live polite, LibraryRow markup, and the hash-assignment fallback handlers. CSS additions in app.css are token-only spacing/layout rules for the sections — zero motion properties. New spec tests/e2e/chrome/library-tidy.spec.ts: assert DOM ordering (ContinueReadingStrip container before the ingest form before the search input before ul.library-list), the anchors present (main#main, h1, .status region), and that IngestControl's status region follows the add-content controls. Then run the three existing library specs byte-unchanged.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/chrome/library-tidy.spec.ts tests/e2e/library/</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/chrome/library-tidy.spec.ts exits 0 on chromium, firefox, and webkit asserting section order and the byte-stable anchors
    - npx playwright test tests/e2e/library/ exits 0 with zero modifications to the existing library spec files (git diff empty for tests/e2e/library/)
    - LibraryView.tsx still contains id="main", "Saved articles", className "status", and the library-list ul; the cancelled-flag load effect region is semantically unchanged (no new fetch/loading logic)
    - Added CSS lines contain no transition or animation property (git diff added-lines check returns 0 matches)
  </acceptance_criteria>
  <done>Library home reads as three calm ordered sections; zero library-behavior regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS placement + markup reordering only; no input parsing, no data-flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Repudiation/Tampering | n/a — no trust boundary crossed | accept | CSS-only dialog fix + structure-only reorg; existing destructive-action isolation (single call sites) untouched and still grep-verifiable |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; any dependency addition is out of scope |
</threat_model>

<verification>
- npx playwright test tests/e2e/chrome/dialog-centering.spec.ts tests/e2e/chrome/library-tidy.spec.ts
- Regression: npx playwright test tests/e2e/library/ and tests/e2e/reduced-motion.spec.ts
</verification>

<success_criteria>
- POLISH-04: all four centered modals pass boundingBox centering on 3 engines; side sheets byte-unchanged
- POLISH-06: tidy section order asserted; existing library specs green byte-unchanged
</success_criteria>

<artifacts_produced>
- src/app.css — modified: margin auto on dialog.highlight-popover, dialog.wipe-confirm, dialog.library-remove-confirm, dialog.import-preview; new library-section spacing rules
- src/ingestion/library/LibraryView.tsx — modified: section reordering/grouping (no new exports)
- tests/e2e/chrome/dialog-centering.spec.ts — new
- tests/e2e/chrome/library-tidy.spec.ts — new
</artifacts_produced>

<output>
Create `.planning/phases/13-polish-and-acceptance/13-03-SUMMARY.md` when done
</output>
