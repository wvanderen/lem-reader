---
phase: 10-annotation-review-panel
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/routes/ArticleView.tsx
  - src/App.tsx
  - tests/e2e/review-panel/jump-bidirectional.spec.ts
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "Deep-linking to #/article/<id>/h/<highlightId> opens the article and lands on the highlight with its <mark> focused — in BOTH reading modes (D10-03)"
    - "The /h/ suffix is stripped with history.replaceState (never a location.hash assignment) so refresh does not re-jump and the router never re-parses mid-view (Pitfall 1)"
    - "A deep link whose highlight id does not exist or does not resolve opens the article calmly — normal article view, suffix stripped, no error surface (Pitfall 4)"
    - "When a jump param is present the deep-link jump wins over the saved-location restore — the two restores never race (Pitfall 3)"
    - "Browser Back from a deep-linked article returns to #/review because the deep link was a history push (SC#2 arrival half; the panel-row loop closes in 10-06)"
  artifacts:
    - path: "src/routes/ArticleView.tsx"
      provides: "jumpHighlightId prop + on-mount readiness-gated jump effect + replaceState strip"
    - path: "src/App.tsx"
      provides: "passes jumpHighlightId from the parsed view into ArticleView"
    - path: "tests/e2e/review-panel/jump-bidirectional.spec.ts"
      provides: "Deep-link arrival/no-re-jump/calm-no-op coverage in both modes"
  key_links:
    - from: "src/App.tsx"
      to: "src/routes/ArticleView.tsx"
      via: "view swap passes jumpHighlightId={view.jumpHighlightId}"
      pattern: "jumpHighlightId"
    - from: "src/routes/ArticleView.tsx"
      to: "src/pagination/anchor.ts + src/reader/restoreLocation.ts"
      via: "on-mount jump reuses fragmentContainingOffset/turnToPage (paginated) and findScrollTarget/scrollIntoView (scrolling) — the handleNavigateBack tail, verbatim discipline"
      pattern: "fragmentContainingOffset|findScrollTarget"
---

<objective>
Ship the deep-link half of bidirectional jump (RECV-01.c/.i): ArticleView consumes the /h/<highlightId> route param, waits out the three async settles (article load, highlight resolution, first pagination commit), jumps using the existing D5-11 machinery, and silently strips the suffix with history.replaceState.

Purpose: SC#2's jump-to-location is satisfied by real browser history — the panel row pushes #/article/<id>/h/<hid>, this plan makes ArticleView honor it on mount, and the replaceState strip keeps refresh calm. Every jump mechanism already ships (handleNavigateBack); the only new engineering is the mount-time readiness gate and the restore-jump coordination (research Pitfalls 2 and 3).

Output: ArticleView jumpHighlightId prop + on-mount jump effect + suffix strip; App.tsx prop pass; deep-link e2e coverage in both reading modes.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md
@.planning/phases/10-annotation-review-panel/10-02-SUMMARY.md

Primary analog (self-extension):
@src/routes/ArticleView.tsx
@src/pagination/anchor.ts
@src/reader/restoreLocation.ts
@src/App.tsx
@tests/e2e/annotations/navigate-back.spec.ts
@tests/e2e/portability/_portability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: jumpHighlightId prop + readiness-gated on-mount jump + replaceState strip</name>
  <files>src/routes/ArticleView.tsx, src/App.tsx</files>
  <read_first>
  - src/routes/ArticleView.tsx in full sections: props interface L80–104 (extend alongside articleId/modeToggleHandlerRef/drawerOpen/onCloseDrawer/onAnnotationCountChange), location-restore effect L811–857 (cancelled-flag + rAF discipline to mirror AND coordinate with — Pitfall 3), handleNavigateBack L1015–1059 (the jump tail to reuse verbatim: fragmentContainingOffset→turnToPage | queryBlocks→findScrollTarget→scrollIntoView center | focusMark with BOTH requestAnimationFrame and setTimeout(120)), and how api.highlights entries expose record.id + resolvedPosition (unresolved = null)
  - src/App.tsx (view swap — add the prop pass jumpHighlightId={view.jumpHighlightId} to the ArticleView element; the parseHash side already ships from 10-02)
  - src/pagination/anchor.ts (fragmentContainingOffset L94–111 — exactly-once page-range contract)
  - src/reader/restoreLocation.ts (findScrollTarget L81)
  </read_first>
  <action>
  1. src/routes/ArticleView.tsx: add optional prop jumpHighlightId?: string. In App.tsx pass jumpHighlightId={view.jumpHighlightId} on the ArticleView element (one-line; nothing else in App.tsx changes).
  2. Add a dedicated on-mount jump effect (its own useEffect — do not modify handleNavigateBack). Shape: cancelled-flag effect mirroring the location-restore effect, gated on the prop being present AND the article being loaded. Readiness semantics (Pitfall 2 — three settles): (a) article truthy; (b) the highlights array has loaded — distinguish "not loaded yet" (an entry for jumpHighlightId is absent from the array entirely: keep waiting) from "loaded and unresolved" (entry exists but resolvedPosition is null: terminal — calm no-op, strip the suffix, done); (c) in paginated mode only, surfaceRef.current?.getPages() returns a non-empty array — use a bounded retry (rAF loop per research Open Question 2's recommendation, capped — e.g. stop after ~5 seconds of retries — so a never-settling pagination cannot spin forever; on cap, calm no-op + strip). In scrolling mode (c) does not apply.
  3. Once ready with a resolved position, reuse the handleNavigateBack tail EXACTLY (no forked math): paginated → fragmentContainingOffset(pages, offset, article) then surface.turnToPage(pageIdx); scrolling → queryBlocks(articleRef.current), findScrollTarget(article, blocks, offset), scrollIntoView({ block: "center" }); then the Firefox settle guard verbatim — a focusMark closure calling document.getElementById(`hl-${jumpHighlightId}`)?.focus() invoked through BOTH requestAnimationFrame and window.setTimeout(120).
  4. Strip the suffix AFTER the jump commits (and also on every terminal no-op path — unresolved, not-found, retry cap): history.replaceState(null, "", "#/article/" + articleId). NEVER strip via window.location.hash assignment — that fires hashchange, re-runs the router, and re-mounts views (Pitfall 1; no replaceState/pushState call exists in src/ today, this is the first). Mark the jump consumed in a ref so effect re-runs (article identity churn, highlight reloads) cannot re-jump within the same mount.
  5. Pitfall 3 coordination: the saved-location restore effect must not race the deep-link jump — when jumpHighlightId was provided and not yet consumed+stripped, the restore path skips (e.g. the restore effect early-returns on a jumpPending ref the jump effect owns). Deep-link wins because the reader explicitly asked for the highlight. After the jump consumes and strips, subsequent mounts (real navigation) restore normally.
  6. Pitfall 4: a jumpHighlightId that never appears in the loaded highlights (deleted in another tab, hand-typed garbage) is a calm no-op — article opens normally, suffix stripped, no error UI. Do not surface anything.
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/ArticleView.tsx contains jumpHighlightId in its props interface and a jump effect that references fragmentContainingOffset, findScrollTarget, and history.replaceState
  - The focus path uses both requestAnimationFrame and a setTimeout(120) call on the same focusMark closure (Firefox settle guard)
  - The suffix strip is history.replaceState(null, "", "#/article/" + …) — grep confirms no location.hash assignment inside the jump/strip path
  - The location-restore path is observably skipped while a jump param is pending (jumpPending ref or equivalent guard referenced in both effects)
  - src/App.tsx passes jumpHighlightId={view.jumpHighlightId} to ArticleView and nothing else changed in the file since 10-02
  - npm run build exits 0; full unit suite green (existing ArticleView-related tests unaffected — the new effect only fires when the prop is provided)
  </acceptance_criteria>
  <done>Deep-linked article URLs land on the highlight with the mark focused in both modes, strip the suffix silently, degrade calmly for unresolvable ids, and never race the saved-location restore.</done>
</task>

<task type="auto">
  <name>Task 2: Deep-link e2e — arrival, no-re-jump, calm no-op (both reading modes)</name>
  <files>tests/e2e/review-panel/jump-bidirectional.spec.ts</files>
  <read_first>
  - tests/e2e/review-panel/jump-bidirectional.spec.ts (the 10-01 sentinel to replace)
  - tests/e2e/annotations/navigate-back.spec.ts (the retry-assert toPass({ timeout: 3000 }) focus-check shape L51–59 — reuse, never fixed sleeps)
  - tests/e2e/portability/_portability.ts (BASE L32; seedRows L191; makeArticle L232; confidentHighlightOn L261 — verified-confident anchors; highlightRow L279)
  - tests/e2e/annotations/_fixtures.ts (wipeDatabase L44)
  - tests/e2e/persistence/persistence.spec.ts or the 04-06 Task 5 precedent for seeding readingMode "scrolling" via settings rows (find the existing seed pattern for the scrolling preference)
  </read_first>
  <action>
  Replace the sentinel with real coverage. Shared setup: beforeEach wipeDatabase; a helper seeds one makeArticle corpus article (2+ paragraphs of distinctive text) plus a confidentHighlightOn row via seedRows, returning the ids. Tests:
  1. Paginated arrival (default mode): page.goto(BASE + "/#/article/" + articleId + "/h/" + highlightId) → assert via the retry-assert pattern that document.activeElement is the mark (data-highlight-id attribute equals highlightId, the navigate-back.spec assertion shape) and the URL no longer contains the /h/ suffix (replaceState strip).
  2. Scrolling arrival: seed the readingMode "scrolling" settings row the same way existing persistence specs do, then the same goto + focus + stripped-URL assertions.
  3. No re-jump on refresh: after arrival assertions, page.reload() → article h1 still visible, and the mark is NOT auto-focused merely by reloading (the suffix is gone from the URL; a location restore to a saved position is acceptable — assert only that this was not a jump: e.g. focus is not on the mark immediately after reload without interaction).
  4. Calm no-op for a deleted/garbage id: goto BASE + "/#/article/" + articleId + "/h/nonexistent-id" → article h1 visible, no error region text, URL stripped to the plain article hash.
  5. Back arrives at #/review: goto BASE + "/#/review" first, then navigate to the deep link via location.hash assignment in page.evaluate (simulating the panel row push), wait for arrival, then page.goBack() → h1 "Review highlights" visible again (proves the deep link pushed history onto #/review — the SC#2 arrival half; the click-from-row loop closes in 10-06).
  Selector discipline: headings via getByRole("heading", { level: 1 }); focus via the page.evaluate activeElement read; no fixed waits — retry or expect().toBeVisible().
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts</automated>
  </verify>
  <acceptance_criteria>
  - Spec contains five tests covering paginated arrival, scrolling arrival, refresh-no-rejump, calm no-op, and back-to-#/review
  - Both reading-mode arrival tests assert mark focus via a retrying assertion (toPass or expect polling), not a fixed sleep
  - Every test asserts the URL lost the /h/ suffix after load (page.url() check)
  - npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts exits 0 across chromium/firefox/webkit (15 cells)
  </acceptance_criteria>
  <done>RECV-01.c (arrival half) and RECV-01.i are proven in real browsers across all three engines: deep links land focused, refresh stays calm, garbage ids degrade to a normal open, and history returns to #/review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash /h/<highlightId> → jump effect | Public, hand-typable param; may reference nonexistent or foreign-bundle highlight ids |
| history API | First replaceState usage in src/ — must be same-origin, template-built URLs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03a | Tampering | jumpHighlightId consumption | mitigate | Id used ONLY for Array.find over loaded highlights and getElementById("hl-" + id) prefix concat — never innerHTML, never dynamic property access; unresolvable = calm no-op (Pitfall 4) |
| T-10-03b | Tampering | replaceState URL | mitigate | URL template-built from the validated article id only; same-origin by construction; no user text enters the URL |
| T-10-03c | Denial of service (self) | bounded retry loop | mitigate | Retry cap (~5s) on the pagination-settle wait so a never-committing surface cannot spin; on cap → calm no-op + strip |
| T-10-03d | Tampering | npm installs | accept | Zero packages installed |
</threat_model>

<verification>
- npm run build + npm run test:unit -- --run — type-safe, no unit regressions
- npx playwright test tests/e2e/review-panel/jump-bidirectional.spec.ts — deep-link matrix across 3 engines
- npm run lint — no new violations
</verification>

<success_criteria>
- RECV-01.c (arrival half) + RECV-01.i proven: focused arrival in both modes, replaceState strip, calm no-op, back-to-#/review
- handleNavigateBack and the location-restore effect keep their existing behavior (existing navigate-back + persistence specs stay green — verify via targeted run then the full suite in 10-06)
- Zero new DOM-walking math: fragmentContainingOffset / findScrollTarget / turnToPage reused, not forked
</success_criteria>

## Artifacts this plan produces

- `jumpHighlightId?: string` prop on ArticleView and its App.tsx pass-through — `src/routes/ArticleView.tsx`, `src/App.tsx`
- On-mount readiness-gated jump effect (bounded rAF retry + jumpPending restore-suppression ref + terminal no-op paths) — `src/routes/ArticleView.tsx`
- First `history.replaceState` call site in src/ (silent suffix strip)
- Deep-link e2e tests (paginated arrival, scrolling arrival, refresh-no-rejump, calm no-op, back-to-review) — `tests/e2e/review-panel/jump-bidirectional.spec.ts` (strengthened from sentinel)

<output>
Create `.planning/phases/10-annotation-review-panel/10-03-SUMMARY.md` when done
</output>
