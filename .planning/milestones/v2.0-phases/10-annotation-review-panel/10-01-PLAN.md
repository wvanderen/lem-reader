---
phase: 10-annotation-review-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/portability/conflicts.ts
  - src/routes/review/reviewFilter.ts
  - tests/unit/review-filter.test.ts
  - tests/e2e/review-panel/route-entry.spec.ts
  - tests/e2e/review-panel/listing.spec.ts
  - tests/e2e/review-panel/jump-bidirectional.spec.ts
  - tests/e2e/review-panel/tri-state.spec.ts
  - tests/e2e/review-panel/curate.spec.ts
  - tests/e2e/review-panel/empty-states.spec.ts
  - .planning/phases/10-annotation-review-panel/10-VALIDATION.md
autonomous: true
requirements: [RECV-01]
must_haves:
  truths:
    - "deriveReviewSections joins every highlight with its article and note and classifies each row confident/ambiguous/orphan without dropping any highlight — absent article means orphan, row kept (D10-05, D10-13)"
    - "Filters AND together: tag (article tags), articleId, confidence; confidence=all includes ambiguous and orphan rows — never silently filtered (D10-08)"
    - "Sort date = sections by newest entry createdAt (ISO-8601 lexicographic), entries newest-first; sort article = sections by provenance.title localeCompare, entries by position.start; sort position = sections in input library order, entries by position.start"
    - "Tri-state classification runs through resolveQuoteSelectorInText with per-article memoized clusters — zero per-highlight recompute of normalizeText/graphemeClusters (D10-13)"
    - "Six review-panel e2e spec files run green as Wave-0 sentinels across chromium/firefox/webkit proving the harness wiring"
  artifacts:
    - path: "src/routes/review/reviewFilter.ts"
      provides: "Pure review-list derivation (filterLibrary twin)"
      exports: ["deriveReviewSections", "ReviewFilters", "ReviewSort", "ReviewEntry", "ReviewSection", "ReviewDerivation"]
    - path: "src/portability/conflicts.ts"
      provides: "Lifted-and-exported MemoizedArticleText (no forked twin)"
      exports: ["MemoizedArticleText"]
    - path: "tests/unit/review-filter.test.ts"
      provides: "Pure derivation coverage (filters AND, three sorts, orphan tail, tri-state join, ISO-date ordering)"
      min_lines: 120
    - path: "tests/e2e/review-panel/"
      provides: "Six Wave-0 sentinel specs (h1-visible harness proof, 04-02 precedent)"
  key_links:
    - from: "src/routes/review/reviewFilter.ts"
      to: "src/annotations/resolution.ts"
      via: "imports resolveQuoteSelectorInText — the canonical D5-02 tri-state core, never forked"
      pattern: "resolveQuoteSelectorInText"
    - from: "src/routes/review/reviewFilter.ts"
      to: "src/portability/conflicts.ts"
      via: "imports the lifted MemoizedArticleText for per-article cluster caching"
      pattern: "MemoizedArticleText"
    - from: "tests/unit/review-filter.test.ts"
      to: "src/routes/review/reviewFilter.ts"
      via: "calls deriveReviewSections on schema-constructed fixtures"
      pattern: "deriveReviewSections"
---

<objective>
Foundation for the annotation review panel (RECV-01): the pure derivation module that joins, classifies, filters, sorts, and groups every highlight+note across the library, plus the Wave-0 e2e scaffolds the rest of the phase will strengthen.

Purpose: D10-09 locks filtering/sorting to a pure, unit-testable derivation (the filterLibrary pattern); D10-13 locks tri-state re-derivation to resolveQuoteSelectorInText with per-article memoization (the Phase 9 conflicts pattern). Shipping this first means every later plan composes a tested module instead of inventing logic. The Wave-0 sentinels (h1-visible proof, the 04-02 precedent) guarantee the e2e harness wires up in all three engines before any behavior depends on it.

Output: src/routes/review/reviewFilter.ts (pure module), MemoizedArticleText lift-and-export, tests/unit/review-filter.test.ts, six sentinel specs under tests/e2e/review-panel/.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-annotation-review-panel/10-CONTEXT.md
@.planning/phases/10-annotation-review-panel/10-RESEARCH.md
@.planning/phases/10-annotation-review-panel/10-PATTERNS.md

Analog sources (verbatim excerpts in 10-PATTERNS.md):
@src/ingestion/library/libraryFilter.ts
@src/portability/conflicts.ts
@src/portability/markdown.ts
@src/annotations/resolution.ts
@tests/unit/library-search.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveReviewSections pure module + MemoizedArticleText lift-and-export + unit matrix</name>
  <files>src/portability/conflicts.ts, src/routes/review/reviewFilter.ts, tests/unit/review-filter.test.ts</files>
  <read_first>
  - src/portability/conflicts.ts (MemoizedArticleText at L193–204 and resolveHighlightStatus at L213–227 — lift the class out, do not fork it)
  - src/ingestion/library/libraryFilter.ts (the pure-derivation shape to mirror: named filter interface, plain function, no Dexie/React/IO)
  - src/portability/markdown.ts (collectHighlightEntries L98–119 join + orphan rule; HighlightEntry type L31–35; UNMATCHED_SECTION_HEADING L129; sort precedents L238–253)
  - src/annotations/resolution.ts (resolveQuoteSelectorInText L179–228 — the only resolution core; REUSE-DO-NOT-FORK header)
  - src/reader/annotations/AnnotationsDrawer.tsx (position sort key L75–79)
  - tests/unit/library-search.test.ts (plain-Node fixture style: ArticleSchema.parse-constructed makeArticle)
  - src/content/schema.ts (HighlightRecord / NoteRecord / ArticleSchema shapes)
  </read_first>
  <behavior>
  - Test: absent article (highlight.articleId = "ghost-article") → entry.status = "orphan", entry kept, lands in orphanEntries not sections (D10-05)
  - Test: confident highlight (quote matches article body once) → status "confident"
  - Test: ambiguous highlight (quote text duplicated inside one article body) → status "ambiguous"
  - Test: confidence filter "all" includes confident AND ambiguous AND orphan rows; "orphan" returns only orphanEntries
  - Test: tag filter keeps only highlights whose article carries the tag (orphans excluded when a tag filter is active — they have no article); articleId filter keeps only that article's highlights; tag+articleId+confidence AND together
  - Test: sort "date" — two articles whose newest highlights have createdAt "2026-02-01…" vs "2026-03-01…" → newer article's section first; within a section entries newest-first
  - Test: sort "article" — sections ordered by provenance.title localeCompare; within section entries ascending by highlight.position.start
  - Test: sort "position" — sections in the input articles array order; entries ascending by position.start
  - Test: note join — highlight with a NoteRecord (matching highlightId) carries note on its entry
  - Test: inputs not mutated (deep-equal snapshot before/after the call)
  </behavior>
  <action>
  1. In src/portability/conflicts.ts, lift-and-export MemoizedArticleText: add the `export` keyword to the existing class declaration (L193). Zero behavior change, zero moves — the Phase 9 internal usage stays byte-identical (per D10-13 and research Open Question 1's preferred option; do NOT mirror a twin class and do NOT touch resolveQuoteSelectorInText).
  2. Create src/routes/review/reviewFilter.ts exporting: type ReviewSort = "date" | "article" | "position"; type ConfidenceValue = "confident" | "ambiguous" | "orphan"; type ConfidenceFilter = "all" | ConfidenceValue; interface ReviewFilters { tag: string | null; articleId: string | null; confidence: ConfidenceFilter }; interface ReviewEntry { highlight: HighlightRecord; note?: NoteRecord; status: ConfidenceValue; article?: CanonicalArticle }; interface ReviewSection { key: string; article: CanonicalArticle; entries: ReviewEntry[] }; interface ReviewDerivation { sections: ReviewSection[]; orphanEntries: ReviewEntry[] }; and the single derivation function deriveReviewSections(articles, highlights, notes, filters, sort) → ReviewDerivation.
  3. deriveReviewSections implements, in order: (a) JOIN — build articleById and noteByHighlightId Maps exactly like markdown.ts collectHighlightEntries (Map.get lookups only, no dynamic property access from ids — foreign bundle ids must never reach property access); (b) CLASSIFY (D10-13) — instantiate ONE MemoizedArticleText per call; for each highlight with an article, status = resolveQuoteSelectorInText(memo.clustersFor(article), highlight.quote, article.lang, highlight.position) mapped non-string → "confident", "ambiguous"→"ambiguous", "orphan"→"orphan" (the resolveHighlightStatus mapping at conflicts.ts L213–227); absent article → "orphan", row KEPT; (c) FILTER — AND-composed: tag filter matches (article.tags ?? []).includes(filters.tag) so orphans drop out only when a tag filter is active; articleId filter matches highlight.articleId; confidence filter "all" passes everything, else entry.status === filters.confidence; (d) GROUP — entries with an article grouped under their article (sections), article-less entries collected into orphanEntries; (e) SORT — "date": sections by newest entry createdAt descending (ISO-8601 lexicographic compare), entries within section by createdAt descending; "article": sections by provenance.title localeCompare, entries by position.start ascending; "position": sections in the input articles-array order, entries by position.start ascending. Section key = article.id.
  4. Create tests/unit/review-filter.test.ts mirroring tests/unit/library-search.test.ts: plain-Node suite (no jsdom, no Dexie), a local makeArticle helper that ArticleSchema.parse-constructs articles (id/revision/lang/provenance/blocks overrides), highlights built to the HighlightRecord shape from src/content/schema.ts, notes to the NoteRecord shape. Cover every behavior listed above. Build the ambiguous case by giving an article a paragraph whose sentence appears twice (the duplicated-text knob the e2e specs will reuse per research).
  Constraint notes: no React imports, no Dexie imports, no IO in reviewFilter.ts (pure module — D10-09). Do not fork resolveQuoteSelector, graphemeClusters, or normalizeText; import them from their canonical modules.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/review-filter.test.ts && npm run test:unit -- --run tests/unit/portability/</automated>
  </verify>
  <acceptance_criteria>
  - src/routes/review/reviewFilter.ts contains `export function deriveReviewSections(` and exports ReviewFilters, ReviewSort, ReviewEntry, ReviewSection, ReviewDerivation
  - src/routes/review/reviewFilter.ts contains `resolveQuoteSelectorInText` and `MemoizedArticleText` imports; it does NOT contain a class-declared twin named MemoizedArticleText
  - src/portability/conflicts.ts contains `export class MemoizedArticleText` (lifted in place — no duplicate class remains)
  - tests/unit/review-filter.test.ts exits 0 with ≥ 10 tests covering orphan-kept, tri-state classification, AND-composed filters, all three sorts, note join, and input non-mutation
  - Existing portability unit tests still pass (the lift-and-export changed no behavior)
  </acceptance_criteria>
  <done>Pure derivation module ships with full unit matrix green; MemoizedArticleText is exported exactly once from conflicts.ts; the panel's data logic is testable without Dexie or DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 e2e sentinel scaffolds (six review-panel specs)</name>
  <files>tests/e2e/review-panel/route-entry.spec.ts, tests/e2e/review-panel/listing.spec.ts, tests/e2e/review-panel/jump-bidirectional.spec.ts, tests/e2e/review-panel/tri-state.spec.ts, tests/e2e/review-panel/curate.spec.ts, tests/e2e/review-panel/empty-states.spec.ts</files>
  <read_first>
  - tests/e2e/annotations/navigate-back.spec.ts (spec structure + beforeEach wipeDatabase discipline L19–21)
  - tests/e2e/annotations/_fixtures.ts (BASE at L33, wipeDatabase at L44)
  - tests/e2e/portability/_portability.ts (BASE at L32; seedRows/makeArticle/confidentHighlightOn/highlightRow helper names the later plans will import)
  </read_first>
  <action>
  Create the tests/e2e/review-panel/ directory with six spec files (route-entry, listing, jump-bidirectional, tri-state, curate, empty-states — names must match exactly; they are the verification-map targets RECV-01.a–.i). Each file carries ONE sentinel test in the 04-02 h1-visible style (proving the harness runs in the browser, not merely compiling): import BASE from ../annotations/_fixtures (or define the same const BASE = "http://localhost:5173" convention used by a11y.spec.ts L16), page.goto the relevant hash, and assert the currently-visible page h1. Under today's two-view router BOTH #/review and #/article/<any-id>/h/<any-id> are unknown routes that fall back to the library list — so every sentinel asserts the h1 "Saved articles" is visible after navigation. These sentinel assertions are explicitly temporary: Plans 10-02/10-03/10-04/10-05/10-06 replace each sentinel with real assertions while keeping the file, describe naming, and helper wiring (strengthen-only for shipped behavior; these files are Phase-10-native so their content may be rewritten in full). Add a header comment in each file naming the verification-map rows it owns (RECV-01.a, .b, .c+.i, .e, .f, .g respectively). Housekeeping (one pass, as files are created): backfill .planning/phases/10-annotation-review-panel/10-VALIDATION.md — set each touched row's Task ID/Plan/Wave cells (unit row → 10-01 Task 1; e2e rows → 10-01 Task 2) and flip File Exists from ❌ W0 to ✅; leave the nyquist_compliant/wave_0_complete frontmatter flags for the phase gate.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/review-panel/</automated>
  </verify>
  <acceptance_criteria>
  - tests/e2e/review-panel/ contains exactly the six named spec files, each with at least one passing test
  - npx playwright test tests/e2e/review-panel/ exits 0 across chromium, firefox, webkit (18 cells green: 6 specs × 3 engines)
  - Each file's header comment names its RECV-01 verification-map rows
  </acceptance_criteria>
  <done>Six sentinel specs prove the e2e harness reaches the app under every Phase-10 route form in all three engines, ready to be strengthened by the behavior plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported-bundle ids → derivation | HighlightRecord.id and articleId are foreign-controlled strings (imported bundles); they enter Map lookups in deriveReviewSections |
| article/note text → panel rows | Stored text (possibly from imported bundles) will render in the review surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01a | Tampering | reviewFilter.ts id handling | mitigate | Map.get lookups only (articleById/noteByHighlightId); no dynamic property access, no eval-shaped use of ids |
| T-10-01b | Tampering | stored note/highlight text | mitigate | Derivation moves text as data only; rendering plans (10-02) render it as React text children — never raw HTML (react/no-danger + lint:no-danger already enforced repo-wide) |
| T-10-01c | Tampering | npm/pip/cargo installs | accept | Phase installs zero packages (research Package Legitimacy Audit vacuous); no install tasks exist |
</threat_model>

<verification>
- npm run test:unit -- --run tests/unit/review-filter.test.ts — derivation matrix green
- npm run test:unit -- --run — no regression in existing unit suites (conflicts.ts export is behavior-neutral)
- npx playwright test tests/e2e/review-panel/ — 18 sentinel cells green
- npm run lint — no new violations
</verification>

<success_criteria>
- deriveReviewSections is pure (no Dexie/React/IO imports) and fully unit-tested including the never-drop orphan rule (D10-05) and memoized tri-state (D10-13)
- MemoizedArticleText exported from conflicts.ts exactly once; resolveQuoteSelectorInText imported, never forked
- Six Wave-0 sentinel specs green across all three engines (04-02 h1-visible precedent)
- Conflicts/export/import behavior unchanged (existing portability unit tests green)
</success_criteria>

## Artifacts this plan produces

- `deriveReviewSections` function + `ReviewFilters` / `ReviewSort` / `ConfidenceValue` / `ConfidenceFilter` / `ReviewEntry` / `ReviewSection` / `ReviewDerivation` types — new exports of `src/routes/review/reviewFilter.ts` (new file)
- `MemoizedArticleText` — newly exported class (lifted in place) from `src/portability/conflicts.ts`
- `tests/unit/review-filter.test.ts` (new file)
- `tests/e2e/review-panel/{route-entry,listing,jump-bidirectional,tri-state,curate,empty-states}.spec.ts` (six new sentinel files)

<output>
Create `.planning/phases/10-annotation-review-panel/10-01-SUMMARY.md` when done
</output>
