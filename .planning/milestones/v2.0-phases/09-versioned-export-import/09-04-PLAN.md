---
phase: 09-versioned-export-import
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/portability/ExportImportService.ts
  - tests/unit/portability/export-service.test.ts
  - tests/unit/portability/validate-bundle.test.ts
  - tests/unit/portability/atomic-import.test.ts
autonomous: true
requirements: [PORT-01, PORT-02]
must_haves:
  truths:
    - "Export produces a zip containing exactly bundle.json + manifest.json; fixture articles never serialize; fixtureIds lists the bundled fixtures the reader's records reference; preferences are always present"
    - "A bundle that is not a zip, has an unsafe entry name, is missing an entry, declares a newer schemaVersion, fails schema validation, or fails manifest recomputation is refused with a specific refusal kind — and zero writes occur (no transaction starts)"
    - "An injected mid-transaction failure rolls back every store — no partial state survives (atomicity, Pitfall 11 #3)"
    - "Page numbers and any page-derived data never appear in the bundle (SC#4 data minimization)"
  artifacts:
    - path: "src/portability/ExportImportService.ts"
      provides: "buildBundleBytes / validateBundle / applyImport — the serialize → parse/validate → atomic-apply service"
      exports: ["buildBundleBytes", "ImportRefusal", "validateBundle", "applyImport"]
    - path: "tests/unit/portability/export-service.test.ts"
      provides: "export truth (fixtures-not-serialized, fixtureIds, entries)"
    - path: "tests/unit/portability/validate-bundle.test.ts"
      provides: "all six refusal kinds"
    - path: "tests/unit/portability/atomic-import.test.ts"
      provides: "rollback proof via injected in-transaction failure"
  key_links:
    - from: "src/portability/ExportImportService.ts"
      to: "fflate"
      via: "zipSync/unzipSync + strToU8/strFromU8 — ONLY these named imports (tree-shaking discipline)"
      pattern: "from \"fflate\""
    - from: "src/portability/ExportImportService.ts"
      to: "src/portability/conflicts.ts"
      via: "applyImport consumes ResolvedImportPlan (fully-computed data; puts only)"
      pattern: "ResolvedImportPlan"
    - from: "src/portability/ExportImportService.ts"
      to: "src/persistence/db.ts"
      via: "single db.transaction(\"rw\", …) across the 5 stores — the DexieLibrarySource.remove cascade precedent extended with db.settings"
      pattern: "db\\.transaction"
---

<objective>
The PORT-01/PORT-02 service core: bundle construction from the five record sources, the full pre-write validation pipeline (unzip → Zip Slip guard on every entry → envelope peek → Zod safeParse → manifest recompute), and the atomic 5-store apply.

Purpose: This is the load-bearing safety surface of the phase — validate-before-write (Pitfall 11 #2), atomicity (Pitfall 11 #3), the decompression-bomb cap, and the forward-compat schemaVersion refusal all live here and are test-provable.
Output: src/portability/ExportImportService.ts + three unit spec files.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versioned-export-import/09-CONTEXT.md
@.planning/phases/09-versioned-export-import/09-RESEARCH.md
@.planning/phases/09-versioned-export-import/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildBundleBytes — export side</name>
  <files>src/portability/ExportImportService.ts, tests/unit/portability/export-service.test.ts</files>
  <read_first>
  - 09-RESEARCH.md §Code Examples "Export bundle construction (end-to-end shape)" (follow it)
  - src/portability/bundle.ts + src/portability/manifest.ts (Plan 09-01 output)
  - src/ingestion/LibrarySource.ts (dexieLibrarySource.list())
  - src/persistence/highlightsStore.ts + notesStore.ts + locationStore.ts + settingsStore.ts (loader signatures; loadSettings result union)
  - src/settings/defaults.ts (DEFAULT_SETTINGS export — verify name)
  - src/fixtures/index.ts (fixtures array)
  - tests/unit/ingestion-tags.test.ts (fake-indexeddb harness + sample builder)
  </read_first>
  <behavior>
  - with 2 seeded Dexie articles (one carrying provenance.sourceUrl), 1 highlight keyed to a real bundled fixture id, 1 note on that highlight, 1 location, and settings present: buildBundleBytes returns bytes that unzip (test-side unzipSync/strFromU8) to exactly the entries bundle.json and manifest.json
  - parsing bundle.json yields schemaVersion 1, articles.length 2 (the fixture article is NOT among them), the seeded article's provenance.sourceUrl present verbatim (SC#1 per-article source URLs), fixtureIds containing the fixture id, preferences equal to the stored settings
  - a recursive walk over bundle.json's keys finds no key matching /page/i (SC#4 data minimization)
  - manifest.json's blocks.articles equals the recomputed computeManifest over the parsed bundle (self-consistency)
  - when no reader-prefs row exists, preferences equals DEFAULT_SETTINGS (D9-12 always-present)
  </behavior>
  <action>
  1. Create src/portability/ExportImportService.ts — header citing D9-01..D9-05 + Pitfall 11 #2/#7. Import from fflate ONLY the named zipSync and strToU8 (unzipSync/strFromU8 join in Task 2 — the four-name allowlist is the tree-shaking discipline). Export async function buildBundleBytes(): Promise<Uint8Array>:
     - Promise.all over dexieLibrarySource.list(), loadAllHighlights(), loadAllNotes(), loadAllLocations(), and the settings load (ok ⇒ settings, else DEFAULT_SETTINGS).
     - fixtureIds: derive the Set of article ids referenced by highlights.articleId, by locations.articleId, and by notes via their owning highlight's articleId (notes reference highlights, not articles); then filter the src/fixtures array to referenced ids and map to ids. Fixtures themselves NEVER serialize (ARCHITECTURE L615).
     - Self-check: ExportBundleSchema.parse({ schemaVersion: 1, exportedAt: new Date().toISOString(), appVersion: resolveAppVersion(), articles, locations, highlights, notes, preferences, fixtureIds }) — the exporter validates its own output; a throw here is an export-side bug surfaced to the caller (UI catches and reports calmly in 09-05).
     - computeManifest(bundle), then zipSync with entries "bundle.json" (JSON.stringify(bundle, null, 2) — pretty, negligible after DEFLATE, human-debuggable) and "manifest.json" (JSON.stringify(manifest) — minified).
  2. Create tests/unit/portability/export-service.test.ts on the fake-indexeddb harness per the behavior block (test file may import unzipSync/strFromU8 from fflate — the src allowlist does not constrain tests). Use a real fixture id + a real passage of that fixture for the highlight seed so Task 2-style resolution semantics are exercised end-to-end later.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/export-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/portability/ExportImportService.ts exports buildBundleBytes; grep of src/portability for fflate imports shows only the allowed named identifiers (zipSync, unzipSync, strToU8, strFromU8 — after Task 2)
  - entries set equals {bundle.json, manifest.json}; fixture article absent from articles; fixtureIds contains the seeded fixture id; preferences always present
  - the no-page-key recursive walk assertion passes
  - npm run build exits 0
  </acceptance_criteria>
  <done>Export serializes the five record blocks plus fixtureIds into a versioned, manifest-hashed zip with fixtures excluded by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validateBundle — the six-refusal pre-write pipeline</name>
  <files>src/portability/ExportImportService.ts, tests/unit/portability/validate-bundle.test.ts</files>
  <read_first>
  - 09-RESEARCH.md §Code Examples "Import validation pipeline" (follow it) + §Security Domain rows (bomb cap, prototype-pollution-safe parsing, version skew)
  - src/portability/zipSlip.ts + manifest.ts + bundle.ts
  </read_first>
  <behavior>
  - random non-zip bytes → refusal kind not-a-zip
  - a zip containing an entry named ../../evil.sh (alongside valid entries) → refusal kind unsafe-entry naming it; same for the URL-encoded traversal form
  - a zip missing manifest.json → refusal kind missing-entry naming manifest.json
  - bundle.json with schemaVersion 2 → refusal kind newer-schema-version with bundleVersion 2 — reached WITHOUT full schema parse (peeked first)
  - a bundle.json missing a required field → refusal kind invalid with an issues array of length >= 1 whose entries are path + message strings (ALL issues, not just the first — Pitfall 11 #2)
  - a manifest whose articles hash does not match a tampered articles block → refusal kind corrupted with failedBlocks containing "articles"
  - a well-formed bundle built by buildBundleBytes → ok true with bundle + manifest
  - a zip whose entry declares an originalSize over the cap is refused (not-a-zip path or filtered — assert the function returns a refusal rather than allocating)
  </behavior>
  <action>
  1. Extend ExportImportService.ts with the ImportRefusal discriminated union exactly as specified in RESEARCH Code Examples: not-a-zip | unsafe-entry { name } | missing-entry { name } | newer-schema-version { bundleVersion } | invalid { issues: string[] } | corrupted { failedBlocks: string[] }. Export async function validateBundle(file: File): Promise<{ ok: true; bundle: ExportBundle; manifest: Manifest } | { ok: false; refusal: ImportRefusal }> implementing the pipeline IN THIS ORDER:
     - unzipSync over new Uint8Array(await file.arrayBuffer()) with the filter option capping f.originalSize at 200,000,000 bytes (decompression-bomb guard, T-9-02); any throw → not-a-zip.
     - isSafeEntryName on EVERY entry key — any failure → unsafe-entry with the name; the whole bundle is refused (SC#2 hard gate, no exceptions).
     - required entries bundle.json + manifest.json presence → missing-entry.
     - JSON.parse the bundle text and PEEK schemaVersion: a number > 1 → newer-schema-version (BEFORE full parse — the calm refusal instead of a Zod wall; RESEARCH anti-pattern).
     - ExportBundleSchema.safeParse → on failure map error.issues to strings of path-joined + message (the LIST).
     - computeManifest(parsed.data) and compare block-by-block against the claimed manifest's blocks → corrupted with the failed block names.
     - success → { ok: true, bundle, manifest }.
     Document in the header: JSON.parse is prototype-pollution-safe with Zod's z.object stripping unknown keys (including underscore-prefixed) — the parsed bundle carries only schema-known fields.
  2. Create tests/unit/portability/validate-bundle.test.ts per the behavior block, crafting each malformed input by zipSync-ing constructed entries (tests build zips in-memory; a File can be constructed via new File([bytes], "x.zip") under jsdom/Node 20). Use Node's crypto.subtle availability for manifest computation in tests.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/validate-bundle.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - all six refusal kinds asserted with their payloads; peek-before-parse ordering asserted (a schemaVersion-2 bundle with OTHER invalid fields still refuses with newer-schema-version, not invalid)
  - the issues list surfaces multiple problems at once (Pitfall 11 #2)
  - the round-trip ok case passes (bundle built by buildBundleBytes validates)
  - npx vitest run tests/unit/portability exits 0
  </acceptance_criteria>
  <done>The import side refuses every malformed or hostile bundle with a specific, calm-reportable refusal kind before any transaction can start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: applyImport — the atomic 5-store transaction</name>
  <files>src/portability/ExportImportService.ts, tests/unit/portability/atomic-import.test.ts</files>
  <read_first>
  - src/ingestion/LibrarySource.ts remove() (~L108-151) — THE transaction precedent to extend with db.settings
  - 09-RESEARCH.md Pattern 3 + Pitfall 1 (no async-non-Dexie work inside the tx)
  - src/persistence/db.ts (5 stores + PK shapes)
  - src/persistence/locationStore.ts (LocationRecord row/put shape — compound key derived from fields)
  - src/persistence/settingsStore.ts (reader-prefs row shape: key + value)
  - src/portability/conflicts.ts (ResolvedImportPlan)
  </read_first>
  <behavior>
  - applying a plan with new + keep-both-rewritten records writes them all: article/highlight/note/location rows appear, the minted highlight id exists, the rewritten note's highlightId matches the minted id, and preferences write only when applyPreferences is true (db.settings gains the reader-prefs row)
  - injected failure: register a Dexie creating hook on db.notes that throws when the note id equals a sentinel; applyImport rejects; afterwards the counts in ALL stores equal their pre-apply values (full rollback)
  - the transaction's table set includes db.settings only when plan.applyPreferences is true
  </behavior>
  <action>
  1. Extend ExportImportService.ts with export async function applyImport(plan: ResolvedImportPlan): Promise<void>:
     - tables = plan.applyPreferences ? [db.articles, db.highlights, db.notes, db.location, db.settings] : the same minus db.settings.
     - await db.transaction("rw", ...tables, async () => { … }) where the closure contains ONLY awaited db.*.put calls in this order: articles, highlights, notes, locations, then (when applyPreferences) db.settings.put({ key: "reader-prefs", value: plan.preferences }). Location rows are put with the locationStore row shape (articleId + revision fields present; Dexie derives the compound [articleId+revision] primary key — never a literal bracketed field name). NO crypto.subtle, NO Zod, NO setTimeout, NO non-Dexie await inside the closure (Pitfall 1 — a code-review gate; the plan is fully computed data from 09-03).
     - A header comment states the rule: everything async-non-Dexie completed before this function is called.
  2. Create tests/unit/portability/atomic-import.test.ts on the fake-indexeddb harness:
     - happy path per behavior block 1 (seed local conflicting rows, build bundle, detectImportPreview, resolveImportPlan with keep-both on highlight-id + applyPreferences true, applyImport, assert rows).
     - rollback proof per behavior block 2: use db.notes.hook("creating", …) to throw on the sentinel id (Dexie creating hooks fire inside the transaction; a throw rolls it back); count rows in every store before and after and assert equality.
     - remove the hook between tests (hooks persist — deregister in an afterEach to avoid cross-test bleed).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/portability/atomic-import.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - applyImport exported; the closure body contains only db.*.put calls (assertable by reading the module — the acceptance is the test-proof of rollback plus the code shape)
  - mid-write injected failure leaves ALL stores byte-identical to pre-apply counts (atomicity proven, not asserted-by-convention)
  - FK rewrite survives the write (note row's highlightId equals the minted id)
  - npm run test:unit -- --run tests/unit/portability exits 0 (all 6 unit files from 09-01..09-04)
  </acceptance_criteria>
  <done>Import applies in one Dexie transaction across all touched stores with proven all-or-nothing rollback and preferences gated by the plan flag.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 09-04)

| Symbol / Path | Kind | Notes |
|---|---|---|
| `buildBundleBytes` | async function | src/portability/ExportImportService.ts (new file) |
| `ImportRefusal` | discriminated union | 6 refusal kinds |
| `validateBundle` | async function | the pre-write pipeline (Zip Slip gate + bomb cap + peek + safeParse + manifest) |
| `applyImport` | async function | atomic 5-store transaction, puts only |
| tests/unit/portability/{export-service,validate-bundle,atomic-import}.test.ts | test files | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picked .zip file → app memory → IndexedDB | fully untrusted bytes cross here; nothing writes until every gate passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-11 | Tampering (partial import) | applyImport | mitigate | single db.transaction across touched stores; injected-failure rollback test is the proof (Pitfall 11 #3) |
| T-9-12 | Tampering + Info Disclosure (version skew) | schemaVersion | mitigate | z.literal(1) + pre-parse peek → calm newer-version refusal, no silent partial import (D9-04); peek-order test-locked |
| T-9-13 | Tampering (corruption) | manifest | mitigate | per-block SHA-256 recompute vs claimed manifest → corrupted refusal before any transaction (D9-03) |
| T-9-14 | Tampering (prototype pollution / unknown keys) | bundle parsing | mitigate | JSON.parse + z.object default unknown-key stripping (incl. underscore keys in Zod 4); literal enums reject unexpected values — documented + safeParse issues list surfaces problems loudly |
| T-9-02 | DoS (decompression bomb) | unzipSync | mitigate | fflate filter capping originalSize at 200,000,000 bytes; test-asserted refusal rather than allocation |
| T-9-15 | DoS (tx abort via foreign await) | transaction closure | mitigate | puts-only closure rule (Pitfall 1); code-review gate + header comment |
</threat_model>

<verification>
- npx vitest run tests/unit/portability exits 0 (8 unit files total across 09-01..09-04)
- npm run build exits 0
- grep -rn 'from "fflate"' src/ shows only the four allowed named imports
</verification>

<success_criteria>
- SC#1's export truth (versioned bundle, per-article source URLs inside ArticleSchema.provenance, fixtures excluded) and SC#2's validate-before-write + atomicity + Zip Slip wiring are all unit-proven
- The service surface the UI (09-05) and e2e gates (09-06) consume is complete
</success_criteria>

<output>
Create `.planning/phases/09-versioned-export-import/09-04-SUMMARY.md` when done
</output>
