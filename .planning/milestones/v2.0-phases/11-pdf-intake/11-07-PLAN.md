---
phase: 11-pdf-intake
plan: 07
type: execute
wave: 6
gap_closure: true
depends_on: ["11-06"]
files_modified:
  - server/pdfToBlocks.ts
  - tests/unit/server/pdf-to-blocks.spec.ts
  - tests/unit/server/ingest-pdf.spec.ts
  - tests/e2e/pdf-intake.spec.ts
autonomous: true
requirements: ["ING-04"]
must_haves:
  truths:
    - "pdfToBlocks on synthetic-outline.pdf returns isReaderable true with ≥3 blocks including the outline-coerced h2 headings"
    - "ingest() on synthetic-outline.pdf returns ok:true — a pdf-<hash> article whose blocks keep BOTH h2 headings, with high|low confidence (never extraction-unsupported)"
    - "Uploading synthetic-outline.pdf in the browser navigates to #/article/pdf-<id> and renders the outline bookmarks as structured h2 headings, with one PDF-badged library row"
    - "Scanned detection is NOT loosened: synthetic-scanned.pdf still refuses pdf-scanned at adapter, orchestrator, and browser level; the 11-06 calibration evidence replay stays green with PDF_THRESHOLDS byte-untouched"
    - "The middle band stays refused: a document with a minority of near-empty pages and zero text-bearing pages keeps isReaderable false even with ≥3 blocks"
  artifacts:
    - path: "server/pdfToBlocks.ts"
      provides: "the relaxed isReaderable admission algebra (sparse structured documents admit; scanned/middle-band refuse)"
      contains: "nearEmptyPages === 0"
      min_lines: 1335
    - path: "tests/unit/server/pdf-to-blocks.spec.ts"
      provides: "adapter-level isReaderable assertion for the outline fixture + the middle-band admission guard"
      min_lines: 500
    - path: "tests/unit/server/ingest-pdf.spec.ts"
      provides: "full-pipeline outline-fixture admission regression (ingest → ok:true with h2 headings)"
      min_lines: 390
    - path: "tests/e2e/pdf-intake.spec.ts"
      provides: "browser-level outline-fixture upload admission coverage"
      min_lines: 445
  key_links:
    - from: "server/pdfToBlocks.ts"
      to: "server/ingest.ts"
      via: "isReaderable feeds the orchestrator's !isReaderable refusal gate (L329) — the false negative this plan removes"
      pattern: "isReaderable"
    - from: "tests/unit/server/ingest-pdf.spec.ts"
      to: "server/ingest.ts"
      via: "ingest({pdf, filename}) over the real fixture bytes through the full 7-stage pipeline"
      pattern: "fixtureB64\\(\"synthetic-outline\\.pdf\"\\)"
    - from: "tests/e2e/pdf-intake.spec.ts"
      to: "dev-server /api/ingest + server/ingest.ts"
      via: "uploadPdf drives the real picker → POST → admission → #/article/pdf- navigation"
      pattern: "#\\/article\\/pdf-"
---

<objective>
Close the Phase-11 UAT gap (Test 2, severity major): synthetic-outline.pdf is refused `extraction-unsupported` ("Couldn't reliably read this page") because `pdfToBlocks`'s isReaderable requires `textBearingPages >= 1` while the fixture's two pages carry 6/4 real text items — below `scannedItemFloor=8` — with `nearEmptyPages=0` and `scanned:false`. Relax that one conjunct so legitimately sparse structured documents admit, and lock the behavior with the three levels of regression coverage whose absence let this bug ship (adapter asserted blocks only, never isReaderable; ingest never ran the outline fixture; e2e never uploaded it).

Purpose: UAT truth "synthetic-outline.pdf admits with outline bookmarks coerced to structured h2/h3 headings" — the ING-04 SC#1 outline-structure aspect. Diagnosis (probe-verified, .planning/debug/synthetic-outline-pdf-refusal.md): adapter assembly is CORRECT (5 blocks, h2 coercion works); only the admission conjunct produces a false negative at the orchestrator gate (server/ingest.ts L329-331, which is correct per its own contract).
Output: one-line-behavior fix in server/pdfToBlocks.ts + strengthened tests/unit/server/pdf-to-blocks.spec.ts + new full-pipeline case in tests/unit/server/ingest-pdf.spec.ts + new upload test in tests/e2e/pdf-intake.spec.ts.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pdf-intake/11-UAT.md (§Gaps — the input contract for this plan)
@.planning/debug/synthetic-outline-pdf-refusal.md (the full diagnosis: probe evidence, eliminated hypotheses, git archaeology)
@.planning/phases/11-pdf-intake/11-06-SUMMARY.md (calibration constraints — PDF_THRESHOLDS frozen by the replay pin; any threshold change requires a re-derive, out of scope here)
@server/pdfToBlocks.ts (classifyDocument L543-573, scanned/multiColumn gates L1125-1138, per-page counters L1152-1162, isReaderable L1332)
@server/ingest.ts (!isReaderable refusal gate L329-331, consumeDuplicatedTitle L148-162 + normalizeForTitleMatch, D11-09 apply L367)
@tests/unit/server/pdf-to-blocks.spec.ts (outline tests L120-124 + L375-393; tinyPdf probe helper L235-237)
@tests/unit/server/ingest-pdf.spec.ts (fixtureB64 helper, happy-path describe shape L50-92)
@tests/e2e/pdf-intake.spec.ts (fixture consts L75-78, uploadPdf L99-106, waitForOpenedArticle L126-150)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — adapter isReaderable + full-pipeline ingest regression tests for the outline fixture</name>
  <files>tests/unit/server/pdf-to-blocks.spec.ts, tests/unit/server/ingest-pdf.spec.ts</files>
  <read_first>tests/unit/server/pdf-to-blocks.spec.ts (the outline-fixture tests L120-124 and L375-393, the tinyPdf helper + serializePdf import L235-237, the single-paragraph isReaderable-false test L361-371), tests/unit/server/ingest-pdf.spec.ts (fixtureB64 + the happy-path describe L50-92), server/pdfToBlocks.ts (PDF_THRESHOLDS floors L103-114, classifyDocument L543-573, isReaderable L1332), server/ingest.ts (consumeDuplicatedTitle + normalizeForTitleMatch L111-162)</read_first>
  <behavior>
    - pdfToBlocks(fixtureBytes("synthetic-outline.pdf")).isReaderable is true; blocks.length ≥ 3; the blocks include level-2 headings "Outlined Document" and "Second Section" (the adapter-level admission this gap's missing assertion never proved)
    - ingest({ pdf: fixtureB64("synthetic-outline.pdf"), filename: "outline-notes.pdf" }) returns ok:true; article.id matches /^pdf-[0-9a-f]{12}$/; ingestionMeta.source "pdf" + origin "upload"; extractionConfidence is "high" or "low"; BOTH h2 headings survive in article.blocks (D11-09 consume does not fire — see action note)
    - Middle-band guard: a 3-page probe PDF (two pages of 4 widely-spaced short real-text lines each — above near-empty floors, below scannedItemFloor — plus one single-short-line page) assembles ≥3 blocks but isReaderable stays false (nearEmptyPages ≥ 1 and textBearingPages = 0 must both keep refusing)
    - The pre-existing cells stay green while ONLY the new outline-admission tests fail (RED proves the tests bite and the harness is not broken)
  </behavior>
  <action>1. tests/unit/server/pdf-to-blocks.spec.ts — strengthen the outline-fixture coverage in the typed-refusals describe (the L120-124 "synthetic-outline.pdf resolves" test): keep the resolves-with-blocks assertions and additionally assert result.isReaderable is true and that the heading texts "Outlined Document" and "Second Section" are present as level-2 heading blocks (the load-bearing new assertion is isReaderable — the exact field the orchestrator gate reads; heading coercion itself is already pinned at L375-393 and may simply be re-observed here).
  2. Same file — add an admission-algebra guard test (sibling of the L361-371 single-paragraph probe): build a MULTI-page probe by calling serializePdf directly with an array of buildContentStream pages (tinyPdf builds single-page only; do not fork the serializer). Page shape: two pages each carrying 4 short full-width single-column lines whose vertical gaps exceed paragraphGapRatio (each line assembles as its own paragraph block → 8 blocks total, comfortably ≥3), and a third page with a single 1-2 word line (1 real item < nearEmptyItemFloor=3 → near-empty; 1-of-3 near-empty is NOT a scanned majority, so the adapter resolves rather than throwing). Assert blocks.length ≥ 3 AND result.isReaderable is false — proving the relaxation in Task 2 cannot collapse into a blocks-count-only check. Keep lines ≥15 non-ws chars on the four-line pages so they are neither near-empty nor text-bearing (4 items < scannedItemFloor=8); full-width x=60 lines keep the pages non-columnar.
  3. tests/unit/server/ingest-pdf.spec.ts — add a describe for the full-pipeline outline admission (mirror the happy-path describe shape): call ingest over the real fixture bytes with filename "outline-notes.pdf". Assert ok:true, the pdf-<hash> id shape, source/origin, high|low confidence, and that BOTH h2 headings ("Outlined Document", "Second Section") remain in article.blocks. FILENAME IS LOAD-BEARING and must not be changed to "outlined-doc" or similar: consumeDuplicatedTitle normalizes [-_\s]+ to whitespace (D11-09), so "outlined-doc" → "outlined doc" is contained in the first heading's "outlined document" and the heading would be (correctly) consumed — "outline notes" matches neither heading in either containment direction, so both headings surviving is itself part of the proof.
  4. Commit the failing tests first (test(11-07) — RED): run the two suites, confirm exit 1 with ONLY the new outline-admission tests failing (isReaderable false / ingest extraction-unsupported), and record the failing assertion names in the working notes for the SUMMARY.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts</automated>
  </verify>
  <done>RED committed: the outline fixture's adapter-level isReaderable and full-pipeline ingest admission are asserted and demonstrably failing against the shipped formula; the middle-band guard and all pre-existing cells (including the scanned/two-column/corrupt refusals) pass. The verify command exiting 1 with only the named outline-admission tests failing is the expected RED signal per this phase's TDD convention — it is the completion evidence, not a task failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — relax the isReaderable conjunct (thresholds byte-untouched)</name>
  <files>server/pdfToBlocks.ts</files>
  <read_first>server/pdfToBlocks.ts (verdict binding L1126, per-page textBearingPages counter L1152-1162, the return object L1325-1333, PDF_THRESHOLDS L82-115), tests/unit/server/pdf-calibration/replay.spec.ts (the thresholds pin — the constraint this fix must not disturb)</read_first>
  <behavior>
    - pdfToBlocks returns isReaderable true iff blocks.length >= 3 AND (textBearingPages >= 1 OR verdict.nearEmptyPages === 0)
    - synthetic-outline.pdf flips to isReaderable true (nearEmptyPages 0, 5 blocks) → Task 1's tests go green
    - synthetic-scanned.pdf still THROWS pdf-scanned before assembly (majority-near-empty gate unchanged, runs earlier at the verdict check); synthetic-two-column.pdf still throws pdf-multi-column; the multiColumn denominator (textBearingPages) is unchanged
    - PDF_THRESHOLDS is not modified in any way — replay.spec's snapshot pin stays green without a re-derive
  </behavior>
  <action>1. Edit ONLY the isReaderable expression in the return object (~L1332): admit when blocks.length >= 3 AND (textBearingPages >= 1 OR verdict.nearEmptyPages === 0). The verdict binding from classifyDocument(strippedPages) at L1126 is already in scope and its nearEmptyPages count is computed over the same stripped pages as the local textBearingPages counter — leave the counter and every threshold untouched.
  2. Add a concise comment directly above the isReaderable line stating the admission algebra and the WHY: legitimately sparse structured documents (outline/title-page shapes) can carry zero pages reaching scannedItemFloor while NO page is near-empty; the scanned majority gate (nearEmptyPages > scannedMajorityRatio × totalPages) already refuses scanned documents BEFORE assembly, so the old conjunct double-guarded and false-refused this class; scannedItemFloor and all PDF_THRESHOLDS values stay frozen — the 11-06 calibration replay pins them, and re-tuning requires re-running the derive harness (out of scope for this gap closure).
  3. Safety envelope (encode as the comment's closing sentence, it is the review argument): nearEmptyPages === 0 means EVERY page carried at least nearEmptyItemFloor real items AND nearEmptyCharFloor non-whitespace chars — a scanned document's image-only pages are near-empty and refuse earlier via pdf-scanned; documents with a minority of near-empty pages but no text-bearing page (the middle band) remain refused, pinned by Task 1's guard test.
  4. Commit the implementation (fix(11-07) — GREEN). Do not reformat or reflow any other part of the file.</action>
  <verify>
    <automated>npx vitest run tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts tests/unit/server/pdf-calibration/replay.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/pdfToBlocks.ts's isReaderable expression references nearEmptyPages === 0 as the second disjunct arm (rg -c "verdict\.nearEmptyPages === 0" server/pdfToBlocks.ts returns 1 — the code-only form; the explanatory comment phrases the arm without the verdict.-prefixed token, so only the expression line matches)
    - The full three-suite command exits 0: Task 1's tests GREEN, the pre-existing adapter cells green (the suite that was 35/35 before this plan), the ingest-pdf suite green, replay.spec's PDF_THRESHOLDS pin green
    - npx tsc --noEmit exits 0
    - No line inside the PDF_THRESHOLDS object literal changed (the replay pin proves it mechanically)
  </acceptance_criteria>
  <done>The sparse-structured-document class admits through the adapter; scanned, multi-column, middle-band, and thin-document refusals are all provably intact; the calibration record is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: e2e outline admission + full phase-11 regression sweep</name>
  <files>tests/e2e/pdf-intake.spec.ts</files>
  <read_first>tests/e2e/pdf-intake.spec.ts (fixture consts L75-78, uploadPdf L99-106, waitForOpenedArticle L126-150, the SC#1 happy-path test's navigation + badge assertions L157-214)</read_first>
  <action>1. Add an OUTLINE_PDF const beside the other fixture consts (pdfFixture("synthetic-outline.pdf")).
  2. Add one test to the ING-04 describe: goto BASE #/ → uploadPdf(page, "outline-notes.pdf", OUTLINE_PDF) (same D11-09 filename discipline as Task 1 — "outline notes" must not fuzzy-match either heading, so both h2 headings reaching the DOM is part of the proof) → waitForURL(/#\/article\/pdf-/, 15s) → waitForOpenedArticle(page) → the level-1 heading shows "outline-notes" (filename title chain; no Info title on the fixture) → BOTH level-2 headings "Outlined Document" and "Second Section" are visible (outline coercion reaches the real reading surface — the UAT Test 2 expectation) → navigate back to #/ and assert pdfLibraryRows(page) has count 1 (the admitted row, SC#1 badge shape).
  3. Run the full pdf-intake e2e (chromium/firefox/webkit) — the same file's scanned/two-column/corrupt refusal tests and the dedupe/annotate/restore tests re-prove the fix did not loosen any refusal or identity behavior at browser level.
  4. Commit (test(11-07)). Final sweep for the SUMMARY: the three suites from Task 2's verify plus npx playwright test pdf-intake, with per-suite counts recorded honestly.</action>
  <verify>
    <automated>npx playwright test pdf-intake && npx vitest run tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts tests/unit/server/pdf-calibration/replay.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/pdf-intake.spec.ts contains an outline-fixture upload test asserting navigation to #/article/pdf-, both h2 outline headings visible, and exactly one PDF-badged library row
    - npx playwright test pdf-intake exits 0 across chromium/firefox/webkit (new test + all pre-existing pdf-intake cells, refusals included)
    - The unit sweep stays green (adapter + ingest + calibration replay)
  </acceptance_criteria>
  <done>UAT Test 2's flow is browser-proven end-to-end — the outline fixture admits with structured h2 headings — and the full phase-11 PDF surface (admissions, three refusals, dedupe, annotate/restore) is green after the fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded PDF bytes → adapter verdict | untrusted document content influences the admission decision |
| admission gate → library | only articles passing isReaderable + round-trip anchor may persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | isReaderable relaxation admitting garbage as readerable | mitigate | blocks.length >= 3 floor retained; the nearEmptyPages === 0 arm requires EVERY page to carry ≥ nearEmptyItemFloor real items AND ≥ nearEmptyCharFloor chars; the middle-band guard test (Task 1) pins the boundary; RED-before-GREEN proves the tests bite |
| T-11-17 | Tampering | silently loosened scanned/multi-column detection | mitigate | PDF_THRESHOLDS byte-untouched (replay.spec snapshot pin green in Task 2's verify); the scanned majority gate throws BEFORE assembly and is unchanged; scanned refusal re-proven at adapter (pre-existing cells), orchestrator (ingest-pdf suite), and browser (pdf-intake e2e) levels |
</threat_model>

<verification>
1. Unit: npx vitest run tests/unit/server/pdf-to-blocks.spec.ts tests/unit/server/ingest-pdf.spec.ts — exit 0 (Task 1's new admission tests + middle-band guard + all pre-existing cells including the three typed refusals).
2. Calibration integrity: npx vitest run tests/unit/server/pdf-calibration/replay.spec.ts — exit 0 (PDF_THRESHOLDS still equals the committed evidence snapshot; no re-derive needed because no threshold changed).
3. Types: npx tsc --noEmit — exit 0.
4. Browser: npx playwright test pdf-intake — exit 0 on chromium/firefox/webkit (outline admission + scanned/two-column/corrupt refusals + dedupe + annotate/restore).
</verification>

<success_criteria>
The UAT gap is closed at all three levels and nothing else moved: synthetic-outline.pdf admits through ingest() and the browser with outline-coerced h2 headings; synthetic-scanned.pdf still refuses pdf-scanned everywhere; the middle band and thin documents still refuse; the 11-06 calibration evidence replay stays green with thresholds untouched; RED→GREEN commit pairs recorded.
</success_criteria>

## Artifacts this phase produces

- server/pdfToBlocks.ts — the relaxed isReaderable admission (one expression + explanatory comment; the only production change)
- tests/unit/server/pdf-to-blocks.spec.ts — outline isReaderable assertion + multi-page middle-band admission guard
- tests/unit/server/ingest-pdf.spec.ts — outline-fixture full-pipeline admission regression
- tests/e2e/pdf-intake.spec.ts — outline-fixture upload admission test

<output>
Create `.planning/phases/11-pdf-intake/11-07-SUMMARY.md` when done
</output>
