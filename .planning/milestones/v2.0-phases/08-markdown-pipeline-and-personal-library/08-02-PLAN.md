---
phase: 08-markdown-pipeline-and-personal-library
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/persistence/db.ts
  - src/persistence/locationStore.ts
  - src/ingestion/library/tagsStore.ts
  - tests/unit/ingestion-tags.test.ts
  - tests/e2e/ingestion/dexie-migration.spec.ts
autonomous: true
requirements:
  - LIB-04
  - LIB-06
user_setup: []

must_haves:
  truths:
    - "Document tags persist as a string array on the article row and survive a round-trip through Dexie + ArticleSchema.parse"
    - "A Dexie v3 client opening the app after the v4 declaration upgrades cleanly: existing rows hydrate tags to [] via the .default() mechanism (Pitfall 9 — no .upgrade() callback)"
    - "loadAllLocations() returns every persisted LocationRecord in a single db.location.toArray() read, each row Zod-validated (STATE-04 — corrupt rows dropped)"
    - "loadAllTags() derives the distinct tag set from article rows via flatMap + Set (auto-prune is implicit — a tag no longer carried by any article disappears on next read; D8-08)"
    - "setArticleTags(id, tags) writes via db.articles.update and the change is visible on the next loadAllTags() call"
  artifacts:
    - path: "src/ingestion/library/tagsStore.ts"
      provides: "Tag read/write surface (denormalized on article row) — loadAllTags, setArticleTags"
      exports: ["loadAllTags", "setArticleTags"]
      min_lines: 40
    - path: "src/persistence/locationStore.ts"
      provides: "loadAllLocations() — single-table read for continue-reading strip derivation"
      exports: ["loadAllLocations"]
    - path: "src/persistence/db.ts"
      provides: "Dexie version(4) append — *tags multi-entry index on articles"
      contains: "version(4)"
  key_links:
    - from: "src/ingestion/library/tagsStore.ts"
      to: "src/persistence/db.ts"
      via: "db.articles.update(id, { tags }) on write; db.articles.toArray() on read"
      pattern: "db\\.articles\\.(update|toArray)"
    - from: "src/persistence/locationStore.ts"
      to: "src/persistence/db.ts"
      via: "loadAllLocations reads db.location.toArray()"
      pattern: "db\\.location\\.toArray"
---

<objective>
Ship the data-layer substrate for tags (LIB-04) and the all-locations read for continue-reading + per-row progress (LIB-06). Both are thin, additive layers over the shipped Dexie v3 schema.

Purpose: Plan 03's LibraryView consumes `loadAllTags` for the chip filter and `loadAllLocations` for the continue-reading strip + per-row hairline. Plan 04's TagEntry consumes `setArticleTags`. Neither exists yet — `locationStore.ts` currently reads one `[articleId+revision]` at a time; tags have no persistence surface at all. This plan lands both with Pitfall 9 discipline (append-only Dexie version, no `.upgrade()` callback) and STATE-04 discipline (Zod safeParse on every read, corrupt rows dropped).

Output: `tagsStore.ts` (new — loadAllTags + setArticleTags), `loadAllLocations()` added to `locationStore.ts`, Dexie `version(4)` append with `*tags` multi-entry index, a unit suite covering denormalize + auto-prune + corrupt-row drop, and the dexie-migration e2e extended with a v3→v4 additive assertion.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-CONTEXT.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-RESEARCH.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-PATTERNS.md
@src/persistence/db.ts
@src/persistence/locationStore.ts
@src/ingestion/LibrarySource.ts
@.planning/phases/08-markdown-pipeline-and-personal-library/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie version(4) append + loadAllLocations</name>
  <files>src/persistence/db.ts, src/persistence/locationStore.ts</files>
  <read_first>
    - `src/persistence/db.ts` — the shipped v1/v2/v3 declaration blocks (L91-137). Pitfall 9: v1/v2/v3 byte-unchanged; v3 is at L131-137 and adds `source` + `addedAt` indexes. The articles Table type annotation is at L68-78. The v3 block is the exact pattern to clone for v4.
    - `src/persistence/locationStore.ts` L63-88 (`loadLocation` — the single-row read pattern to mirror for `loadAllLocations`) and L101-112 (`saveLocation` — the row-shape construction)
    - `src/ingestion/LibrarySource.ts` L53-64 (`DexieLibrarySource.list()` — the safeParse-on-read pattern `tagsStore` mirrors)
    - `08-RESEARCH.md` §Pattern 2 (L375-423 — tag denormalization + Dexie v4 append skeleton) and §Pattern 4 (L443-498 — loadAllLocations contract)
    - `08-PATTERNS.md` §src/persistence/db.ts (L466-481) and §src/persistence/locationStore.ts (L485-500)
  </read_first>
  <action>
    Append `this.version(4).stores({...})` to `src/persistence/db.ts` (per Pitfall 9 — APPEND only, NEVER edit the shipped v1/v2/v3 blocks at L91-137). The v4 block re-declares every store with the v3 shape PLUS the new `*tags` multi-entry index on `articles`:
    - `articles: "id, revision, source, addedAt, *tags"` (the `*tags` syntax declares a multi-entry index over the tags array per RESEARCH §Pattern 2 L393)
    - `settings: "key"`, `location: "[articleId+revision]"`, `highlights: "id, [articleId+revision]"`, `notes: "id, highlightId"` (byte-identical to v3 — Dexie requires the full stores object at each version)
    - NO `.upgrade()` callback — additive index only; Dexie re-indexes on next open. Reference Pitfall 9 + the v3 precedent at L131-137 in the comment.
    - Reference decision D8-05 (document-tag namespace) and D8-08 (auto-prune) in the comment.

    Widen the `articles` Table type annotation in `db.ts` (L68-78): add `tags?: string[]` to the inline type object. This is a TS-only widening (runtime-unaffected; mirrors the Phase 5 highlights/notes annotation transition noted at L79-85).

    Add `loadAllLocations` to `src/persistence/locationStore.ts` (per RESEARCH §Pattern 4 L469 + Assumption A5). Signature: `export async function loadAllLocations(): Promise<LocationRecord[]>`. Implementation mirrors `loadLocation` L63-88 read discipline: `const rows = await db.location.toArray();` then per-row `LocationRecordSchema.safeParse(row)` — collect successes, drop failures silently (STATE-04 — never coerce a corrupt row). Return `LocationRecord[]`. Add a header comment citing D8-09 (continue-reading strip) + D8-10 (recently-read = opened — `savedAt` is the key).

    Run `npm run test:unit -- --run` to verify zero regressions on the existing persistence + ingestion suites (the v4 append is additive; no existing test should break).
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'version(4)' src/persistence/db.ts` returns ≥ 1 (the v4 append exists)
    - `grep -c '\\*tags' src/persistence/db.ts` returns ≥ 1 (the multi-entry index is declared)
    - `grep -c 'upgrade' src/persistence/db.ts` returns 0 in the v4 block region (NO .upgrade() callback — Pitfall 9; verify via reading the v4 block, not a global grep since older comments may mention upgrade)
    - The v1/v2/v3 declaration blocks (L91-137) are byte-unchanged (verify by reading; the edit only APPENDS a new block after L137)
    - `grep -c 'loadAllLocations' src/persistence/locationStore.ts` returns ≥ 2 (the export + a call/return inside the body)
    - `grep -c 'LocationRecordSchema.safeParse' src/persistence/locationStore.ts` returns ≥ 2 (loadLocation + loadAllLocations both validate)
    - `grep -c 'tags?:' src/persistence/db.ts` returns ≥ 1 (the articles Table type annotation is widened)
    - `npm run test:unit -- --run` exits 0 (zero regressions)
    - `npm run build` exits 0 (TypeScript type-check passes)
  </acceptance_criteria>
  <done>
    Dexie v4 is appended with the `*tags` multi-entry index and NO `.upgrade()` callback (Pitfall 9 — additive only; existing v3 rows hydrate `tags: undefined`/`[]` via the ArticleSchema `.default([])` mechanism landed in Plan 01). `loadAllLocations()` ships as a single-table read with Zod validation per row. Zero Phase 7 unit-test regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: tagsStore + unit tests + dexie-migration v4 e2e</name>
  <files>src/ingestion/library/tagsStore.ts, tests/unit/ingestion-tags.test.ts, tests/e2e/ingestion/dexie-migration.spec.ts</files>
  <read_first>
    - `src/ingestion/LibrarySource.ts` L53-64 (`DexieLibrarySource.list()` — the safeParse-on-read pattern; `tagsStore.loadAllTags` mirrors this) and L82-84 (`save()` — `db.articles.put`; `setArticleTags` uses `db.articles.update`)
    - `tests/unit/ingestion-client.test.ts` — the fake-indexeddb + Dexie test pattern to clone (Dexie.dependencies install, wipeDatabase, lazy-import, sampleArticle builder). Read the first 90 lines for the harness.
    - `tests/e2e/ingestion/dexie-migration.spec.ts` — the existing v3 assertion block (around L336-421) to mirror for v4. The seed + open + assert cycle pattern is locked.
    - `src/content/schema.ts` — `ArticleSchema.tags` field (landed in Plan 01; `z.array(z.string().min(1)).default([]).optional()`)
    - `08-RESEARCH.md` §Pattern 2 (L408-421 — tagsStore skeleton: loadAllTags via flatMap+Set, setArticleTags via db.articles.update)
    - `08-PATTERNS.md` §src/ingestion/library/tagsStore.ts (L272-294)
  </read_first>
  <action>
    Create `src/ingestion/library/tagsStore.ts` per RESEARCH §Pattern 2 L408-421. Two exports:

    `loadAllTags(): Promise<string[]>` — derives the distinct tag set from ALL article rows via the compositeLibraryRepository (or `dexieLibrarySource.list()` if simpler — both Zod-validate on read). Implementation: `const articles = await dexieLibrarySource.list();` then `const set = new Set<string>(); for (const a of articles) for (const t of a.tags ?? []) set.add(t); return [...set].sort((a, b) => a.localeCompare(b));`. Auto-prune is implicit (D8-08): a tag no longer carried by any article falls out of the Set on next read — NO cleanup write needed.

    `setArticleTags(articleId: string, tags: string[]): Promise<void>` — writes via `await db.articles.update(articleId, { tags });`. The write is idempotent (Dexie update by primary key). Validate the input shape defensively: drop empty-string tags via `tags.filter(t => t.length > 0)` before writing (mirrors the `z.string().min(1)` schema constraint).

    Import `db` from `../../persistence/db` and `dexieLibrarySource` from `../LibrarySource`. Add a header comment citing D8-05 (document-tag namespace), D8-08 (auto-prune), and Pitfall 9 (additive index — the `*tags` index landed in Task 1 enables future Dexie queries but the current implementation uses toArray() + in-memory derivation for simplicity).

    Create `tests/unit/ingestion-tags.test.ts` cloning the `tests/unit/ingestion-client.test.ts` harness (L14-90: fake-indexeddb install via `Dexie.dependencies`, `wipeDatabase` beforeEach, lazy-import of the module under test so it picks up the fake DB). Build a `sampleArticle({ tags })` helper that constructs a minimal ArticleSchema-valid row. Cover:
    - Denormalize: save an article with `tags: ["essay", "philosophy"]`; `loadAllTags()` returns both tags sorted.
    - Auto-prune (D8-08 / Pitfall 8-3): save article A with `["x"]` and article B with `["x", "y"]`; `loadAllTags()` returns `["x", "y"]`. Call `setArticleTags(B.id, [])` (B loses both tags); `loadAllTags()` returns `["x"]` (A still carries it). Call `setArticleTags(A.id, [])`; `loadAllTags()` returns `[]` (the chip strip would be empty).
    - Multi-article tag: two articles both carry `["shared"]`; `loadAllTags()` returns `["shared"]` once (Set dedupes).
    - Empty library: `loadAllTags()` on a fresh DB returns `[]`.
    - Corrupt row drop (STATE-04): manually insert a row that fails ArticleSchema.safeParse (e.g. `{ id: "bad", revision: 1, lang: "x", provenance: {}, blocks: [] }` — invalid); `loadAllTags()` does not throw and returns only tags from valid rows.
    - `setArticleTags` on a non-existent id is a no-op (Dexie update returns 0 rows updated; no throw).

    Extend `tests/e2e/ingestion/dexie-migration.spec.ts` with a v3→v4 additive assertion, mirroring the existing v3 assertion block (around L336-421). The new test: seed a v3 article row WITHOUT a `tags` field (the v3 shape), open the app (triggering the v4 upgrade), assert the row survives and `dexieLibrarySource.list()` returns it with `tags: []` (the ArticleSchema `.default([])` mechanism hydrates the absent field — Pitfall 9). Assert the `*tags` index exists (query `db.articles.where("tags").equals("anything").toArray()` returns `[]` without throwing — the index is declared). Reference decision D8-05 + Pitfall 9 in the test header comment.

    Run `npm run test:unit -- --run tests/unit/ingestion-tags.test.ts` and ensure all tests pass.
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npm run test:unit -- --run tests/unit/ingestion-tags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ingestion/library/tagsStore.ts` exports `loadAllTags` and `setArticleTags`
    - `grep -c 'new Set' src/ingestion/library/tagsStore.ts` returns ≥ 1 (the auto-prune derivation)
    - `grep -c 'db.articles.update' src/ingestion/library/tagsStore.ts` returns ≥ 1 (the write path)
    - `grep -c 'dexieLibrarySource' src/ingestion/library/tagsStore.ts` returns ≥ 1 (the read path delegates to the Zod-validated source)
    - The auto-prune unit test passes: removing the last occurrence of a tag causes `loadAllTags()` to no longer return it (Pitfall 8-3 defense)
    - The corrupt-row-drop unit test passes: `loadAllTags()` does not throw on a malformed row (STATE-04)
    - `tests/e2e/ingestion/dexie-migration.spec.ts` contains a v4 assertion block (grep for `version(4)` or `tags` in the file returns ≥ 1 in the new test region)
    - `npm run test:unit -- --run tests/unit/ingestion-tags.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    `tagsStore` ships with `loadAllTags` (Set-based auto-pruning derivation) and `setArticleTags` (idempotent Dexie update). The unit suite proves auto-prune (Pitfall 8-3), multi-article dedup, empty-library, corrupt-row drop (STATE-04), and the dexie-migration e2e proves the v3→v4 additive upgrade (Pitfall 9 — existing rows hydrate `tags: []` via the `.default()` mechanism). All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie read → tagsStore | Every row crosses this boundary; STATE-04 mandates Zod safeParse on read (corrupt rows dropped, never coerced) |
| TagEntry (Plan 04) → tagsStore → Dexie write | Tag strings cross this boundary; React escapes text children (no HTML parsing of tag values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-06 | Tampering | Tag-store row injection (corrupt tag row) | mitigate | `loadAllTags` consumes `dexieLibrarySource.list()` which Zod-validates every article row; corrupt rows dropped (STATE-04, mirrors LibrarySource.ts L57-64). |
| T-8-07 | Tampering / XSS | Tag injection (XSS via tag name) | mitigate | Tag names are stored as plain strings; React escapes text children when rendering chips. No HTML parsing of tag values (Pitfall 8 — the repo-wide lint gate applies). Defensive `tags.filter(t => t.length > 0)` in setArticleTags. |
| T-8-08 | Info Disclosure | Cross-article data leakage via stale Dexie rows | accept | `dexieLibrarySource.remove(id)` already cascades atomically (Phase 7 Plan 07-06); the tag field is on the article row, so cascade-delete removes tags too. No additional surface in Plan 02. |
| T-8-09 | Tampering | Dexie v3→v4 migration corrupts existing rows | mitigate | `version(4)` is APPEND-only with NO `.upgrade()` callback (Pitfall 9); the `*tags` index is additive. The dexie-migration e2e proves v3 rows survive with `tags: []` hydration. |
</threat_model>

<verification>
- `npm run test:unit -- --run tests/unit/ingestion-tags.test.ts` exits 0 (denormalize + auto-prune + corrupt-row drop + empty-library)
- `npm run test:unit -- --run` exits 0 (zero regressions on the full Vitest suite)
- `npm run build` exits 0 (TypeScript type-check passes)
- The dexie-migration e2e extension covers the v3→v4 additive path (verified in Plan 05's full-suite run)
</verification>

<success_criteria>
- Tags persist as `string[]` on the article row and survive ArticleSchema.parse round-trips
- `loadAllTags` auto-prunes (a tag carried by zero articles disappears — D8-08 / Pitfall 8-3)
- `loadAllLocations` returns all persisted locations in a single read, each Zod-validated
- Dexie v4 appends cleanly (Pitfall 9 — existing v3 rows hydrate `tags: []`)
- Zero Phase 7 regressions
</success_criteria>

<output>
Create `.planning/phases/08-markdown-pipeline-and-personal-library/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Plan 02 symbols:**
- `src/ingestion/library/tagsStore.ts`: `loadAllTags(): Promise<string[]>`, `setArticleTags(articleId: string, tags: string[]): Promise<void>`
- `src/persistence/locationStore.ts`: new `loadAllLocations(): Promise<LocationRecord[]>` export
- `src/persistence/db.ts`: `version(4)` block with `*tags` multi-entry index on articles; widened `articles` Table type annotation (adds `tags?: string[]`)
- `tests/unit/ingestion-tags.test.ts`: `sampleArticle` builder, denormalize/auto-prune/corrupt-row/empty-library tests
- `tests/e2e/ingestion/dexie-migration.spec.ts`: new v3→v4 additive assertion block
