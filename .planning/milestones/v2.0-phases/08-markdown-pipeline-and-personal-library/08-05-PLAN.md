---
phase: 08-markdown-pipeline-and-personal-library
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - tests/e2e/library/browse-open.spec.ts
  - tests/e2e/library/remove-cascade.spec.ts
  - tests/e2e/library/search-tag-filter.spec.ts
  - tests/e2e/library/markdown-upload.spec.ts
  - tests/e2e/library/progress-recent.spec.ts
  - tests/e2e/library/v1-regression.spec.ts
autonomous: true
requirements:
  - ING-03
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - LIB-05
  - LIB-06
user_setup: []

must_haves:
  truths:
    - "SC#1: The personal library is the default route and shows v1.0 fixtures badged source:'fixture' alongside ingested articles, with no v1.0 e2e test regressing"
    - "SC#2: Reader can open, read, and remove any article; removal cascades to highlights, notes, and position records"
    - "SC#3: Reader can search the library by title/metadata and filter by tag"
    - "SC#4: Reader can add an article by uploading a .md document with YAML front-matter recognized as metadata, normalized through the same Block-output contract as HTML"
    - "SC#5: Reader sees ingestion metadata (source URL, fetch date) with a link to the original source, plus recently-read shortcuts and positional reading-progress indicators"
    - "The full npm run test suite exits 0 across chromium, firefox, and webkit (the honest-suite discipline from PROJECT.md Key Decision #9)"
  artifacts:
    - path: "tests/e2e/library/browse-open.spec.ts"
      provides: "SC#1 + LIB-01 + LIB-05 — library is default route, fixtures badged, source link visible"
      min_lines: 40
    - path: "tests/e2e/library/remove-cascade.spec.ts"
      provides: "SC#2 + LIB-02 — cascade-remove across articles + highlights + notes + location"
      min_lines: 40
    - path: "tests/e2e/library/search-tag-filter.spec.ts"
      provides: "SC#3 + LIB-03 + LIB-04 — search by title/metadata + tag entry + filter + auto-prune"
      min_lines: 50
    - path: "tests/e2e/library/markdown-upload.spec.ts"
      provides: "SC#4 + ING-03 — .md upload with front-matter, article opens in reader"
      min_lines: 40
    - path: "tests/e2e/library/progress-recent.spec.ts"
      provides: "SC#5 + LIB-06 — per-row progress hairline + continue-reading strip + finished mark"
      min_lines: 40
    - path: "tests/e2e/library/v1-regression.spec.ts"
      provides: "SC#1 regression bar — every v1.0 fixture open + read + paginate still works through LibraryView"
      min_lines: 40
  key_links:
    - from: "tests/e2e/library/browse-open.spec.ts"
      to: "src/ingestion/library/LibraryView.tsx"
      via: "asserts the #/ default route renders <h1>Saved articles</h1> + library-list + source badges"
      pattern: "Saved articles"
    - from: "tests/e2e/library/markdown-upload.spec.ts"
      to: "src/ingestion/IngestControl.tsx"
      via: "file input setInputFiles + click submit + waitForURL #/article/<id>"
      pattern: "ingest-file"
---

<objective>
Ship the six phase-exit e2e gates that prove Phase 8's five success criteria (SC#1-5) across chromium, firefox, and webkit. These are the honest-suite gate per PROJECT.md Key Decision #9 — the executor runs the full `npm run test` itself and records pass/fail counts honestly.

Purpose: Plans 01-04 shipped the implementation; this plan ships the proof. Each SC maps to one or more e2e specs that exercise the real browser pipeline (file upload → Vite Node middleware → markdownToBlocks → ArticleSchema.parse → assertRoundTripAnchor → Dexie save → LibraryView render → ArticleView open). The v1-regression spec is the SC#1 regression bar — every v1.0 fixture must still open, paginate, annotate, and restore location through the new LibraryView surface.

Output: 6 new e2e spec files in `tests/e2e/library/`, each running across 3 engines (chromium + firefox + webkit), plus the full-suite honest run.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-CONTEXT.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-RESEARCH.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-UI-SPEC.md
@tests/e2e/ingestion/happy-path.spec.ts
@tests/e2e/open-every-fixture.spec.ts
@.planning/phases/08-markdown-pipeline-and-personal-library/08-01-SUMMARY.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-02-SUMMARY.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-03-SUMMARY.md
@.planning/phases/08-markdown-pipeline-and-personal-library/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#1 browse-open + v1-regression + SC#2 remove-cascade + SC#4 markdown-upload</name>
  <files>tests/e2e/library/browse-open.spec.ts, tests/e2e/library/v1-regression.spec.ts, tests/e2e/library/remove-cascade.spec.ts, tests/e2e/library/markdown-upload.spec.ts</files>
  <read_first>
    - `tests/e2e/ingestion/happy-path.spec.ts` — the FULL file. Copy the structure verbatim: BASE constant (L37), beforeEach (image stub + IndexedDB wipe L67-84), the fill-textarea + click + `waitForURL(#/article/<id>)` pattern (L86-119), and the `<h1>Saved articles</h1>` assertion (L93). This is the analog for every library e2e.
    - `tests/e2e/open-every-fixture.spec.ts` — the v1.0 fixture-open regression test (L65-69 row-count assertion). v1-regression.spec.ts mirrors this against LibraryView.
    - `tests/e2e/ingestion/dexie-migration.spec.ts` L374-411 — the `readRow`/`countRows` helpers for asserting Dexie state post-action. The remove-cascade spec uses these to prove highlights + notes + location are gone.
    - `08-RESEARCH.md` §Validation Architecture §Phase Requirements → Test Map (L912-927) — the locked test-per-requirement mapping
    - `08-UI-SPEC.md` §Regression Targets (L393-408) — the byte-stable elements table (h1 heading, ul/li/a structure, h2 id + aria-labelledby)
    - `08-PATTERNS.md` §tests/e2e/library/*.spec.ts (L381-388)
  </read_first>
  <action>
    Create `tests/e2e/library/browse-open.spec.ts` covering SC#1 + LIB-01 + LIB-05. Clone the `happy-path.spec.ts` harness (BASE URL, beforeEach image-stub + IndexedDB wipe). Tests:
    - The `#/` route renders `<h1>Saved articles</h1>` (byte-stable — Pitfall 8-5).
    - The library list shows one row per v1.0 fixture (assert `locator('.library-list li').count()` equals the fixture count from `src/fixtures/index.ts`).
    - Each v1.0 fixture row carries a source badge with text "Sample" (D8-02 — `locator('.source-badge').first()` has text "Sample").
    - Clicking a fixture's "Open article" link navigates to `#/article/<id>` and the ArticleView renders the article title.
    - The library list shows ingested articles (paste an HTML article via IngestControl; assert the new row appears with source badge "Pasted").
    - An article with a sourceUrl renders the source-badge as a link (LIB-05); a paste article (no sourceUrl) renders plain-text badge.

    Create `tests/e2e/library/v1-regression.spec.ts` covering the SC#1 regression bar. Clone the `open-every-fixture.spec.ts` structure. For every fixture id in the v1.0 corpus: navigate to `#/`, click the fixture's "Open article" link, assert the ArticleView heading matches the fixture title, assert the article body renders (≥1 paragraph), and assert the back button returns to `#/`. This is the explicit gate that LibraryView (a superset of FixtureList) does not regress any v1.0 fixture-open path. Reference Pitfall 8-5 in the test header.

    Create `tests/e2e/library/remove-cascade.spec.ts` covering SC#2 + LIB-02. Clone the harness + the dexie-migration readRow/countRows helpers. Tests:
    - Seed an article (via paste-HTML ingest) + a highlight + a note + a location row directly into Dexie (use `page.evaluate()` to write via the Dexie instance, mirroring dexie-migration.spec.ts seeding).
    - Navigate to `#/`, find the article's row, click the trash button (`.library-row-remove`), assert the RemoveConfirm dialog opens (`dialog.library-remove-confirm` is visible).
    - Assert the dialog body contains "Remove this article? Your highlights and notes for it will also be removed." (UI-SPEC §Copywriting L262).
    - Assert the cancel button carries `data-initial-focus` and receives focus on open (Pitfall 8 — non-destructive default).
    - Click "Remove article" (the destructive button). Assert navigation returns to `#/`. Assert the article row is gone from the library list.
    - Assert via Dexie readRow/countRows that the highlights + notes + location rows for that article id are ALL gone (cascade — D8-13).
    - Cancel path: open the dialog, click "Keep article", assert the article row is still present and no Dexie rows were deleted.

    Create `tests/e2e/library/markdown-upload.spec.ts` covering SC#4 + ING-03. Clone the harness. Tests:
    - Build a representative `.md` fixture in-test (front-matter + h1 + paragraph + bulleted list + blockquote + code block). Set the file on the `<input id="ingest-file" type="file">` via `setInputFiles` with a Buffer containing the markdown.
    - Click "Add file". Assert the `.status` region announces "Reading file…" (UI-SPEC §Copywriting).
    - Assert navigation to `#/article/<id>` where `<id>` starts with `md-` (D8-18 content-hash slug).
    - Assert the ArticleView heading matches the front-matter `title:` field (D8-17 — YAML recognized as metadata).
    - Assert the article body renders the expected block structure (heading + paragraph + list + blockquote + code).
    - Navigate back to `#/`; assert the library row shows a "Markdown" source badge (D8-02).
    - Dedupe-refuse (D7-07 + D8-18): re-upload the identical `.md` content; assert the `.status` region announces "Already in your library." and no new row appears.
    - Front-matter-absent fallback: upload a `.md` without `---` front-matter; assert the article still ingests (title fallback to a neutral default per Plan 01's server-side fallback).
    - `.html` upload variant (D8-15): upload a small `.html` file; assert it ingests and the source badge reads "HTML file".

    Run these four specs across all 3 engines: `npx playwright test tests/e2e/library/browse-open.spec.ts tests/e2e/library/v1-regression.spec.ts tests/e2e/library/remove-cascade.spec.ts tests/e2e/library/markdown-upload.spec.ts`. All must pass.
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npx playwright test tests/e2e/library/browse-open.spec.ts tests/e2e/library/v1-regression.spec.ts tests/e2e/library/remove-cascade.spec.ts tests/e2e/library/markdown-upload.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/library/browse-open.spec.ts` asserts the `<h1>Saved articles</h1>` heading + one row per fixture + "Sample" badge on fixtures + "Pasted" badge on paste articles (SC#1, LIB-01, LIB-05)
    - `tests/e2e/library/v1-regression.spec.ts` opens every v1.0 fixture through LibraryView and asserts the article renders (SC#1 regression bar)
    - `tests/e2e/library/remove-cascade.spec.ts` asserts the cascade removes article + highlights + notes + location; cancel path leaves everything intact (SC#2, LIB-02, D8-13)
    - `tests/e2e/library/markdown-upload.spec.ts` asserts .md upload → md- id → front-matter title → "Markdown" badge; dedupe-refuse; .html upload variant (SC#4, ING-03, D8-15, D8-17, D8-18)
    - The RemoveConfirm body-copy assertion matches UI-SPEC §Copywriting L262 verbatim
    - All four specs pass on chromium + firefox + webkit (the `--project` matrix runs all three by default)
  </acceptance_criteria>
  <done>
    SC#1 (library default + fixtures badged + v1.0 non-regression), SC#2 (cascade-remove with confirmation), and SC#4 (.md + .html upload with front-matter + dedupe) are proven in real browsers across all three engines. The RemoveConfirm dialog body + focus discipline (data-initial-focus on cancel) are verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: SC#3 search-tag-filter + SC#5 progress-recent + full honest-suite gate</name>
  <files>tests/e2e/library/search-tag-filter.spec.ts, tests/e2e/library/progress-recent.spec.ts</files>
  <read_first>
    - `tests/e2e/ingestion/happy-path.spec.ts` L67-84 — the beforeEach image-stub + IndexedDB wipe harness
    - `tests/e2e/persistence.spec.ts` — the location-seed + assert pattern (for progress-recent seeding a graphemeOffset and asserting the hairline fill)
    - `src/ingestion/library/libraryFilter.ts` (from Plan 03) — the filter contract (query + activeTag composition)
    - `src/ingestion/library/ContinueReadingStrip.tsx` (from Plan 03) — FINISHED_THRESHOLD = 0.98
    - `src/reader/ProgressHairline.tsx` — the fill is `transform: scaleX(ratio)` on `.progress-hairline-fill`
    - `08-RESEARCH.md` §Validation Architecture (L897-948) — the test map + sampling rate + Wave 0 gaps
    - `08-UI-SPEC.md` §LibrarySearch + §TagFilter + §ContinueReadingStrip + §Interaction Contract (the keyboard + filter behavior contracts)
  </read_first>
  <action>
    Create `tests/e2e/library/search-tag-filter.spec.ts` covering SC#3 + LIB-03 + LIB-04. Clone the harness; seed 3 articles via paste-HTML (titles "Plato Essay", "Marcus Meditations", "Seneca Letters"; one with no tags, two with overlapping tags). Tests:
    - Tag entry (D8-05): navigate to one article's ArticleView, use TagEntry to add tag "stoic"; assert the chip appears in the fieldset; navigate back to `#/`.
    - Tag-filter (D8-07): assert the TagFilter chip strip shows "stoic"; click it; assert the library list filters to only articles carrying "stoic" (`aria-pressed="true"` on the active chip; `.library-list li` count drops).
    - Click the active chip again; assert the filter clears (all articles reappear).
    - Auto-prune (D8-08): remove the tag from the last article carrying it (via TagEntry × button); navigate back to `#/`; assert the chip no longer appears in the TagFilter strip (Pitfall 8-3 defense).
    - Search (D8-06): type "plato" into the search input; assert only the "Plato Essay" row remains. Clear the query; assert all rows reappear.
    - Search by tag name: type "stoic"; assert articles carrying that tag appear (tags are first-class searchable metadata per D8-06).
    - Search + tag composition: activate a tag chip AND type a query; assert both filters apply (intersection).
    - Empty search results: type a nonexistent string; assert no rows + no crash (the library list renders zero items gracefully).

    Create `tests/e2e/library/progress-recent.spec.ts` covering SC#5 + LIB-06. Clone the harness + the persistence.spec.ts location-seed pattern. Tests:
    - Per-row progress hairline (D8-11): seed a location row for a fixture at `graphemeOffset = Math.floor(total * 0.5)`; navigate to `#/`; assert the row's `.progress-hairline-fill` has `transform: scaleX(0.5)` (approximately — use `toBeCloseTo` with precision 1). Assert the hairline is NOT rendered on a fresh article with no location (ratio 0).
    - Finished mark (D8-12): seed a location at `graphemeOffset >= total * 0.98`; navigate to `#/`; assert the row shows the `.finished-mark` element with text "Finished" and the hairline renders at scaleX(1).
    - Continue-reading strip (D8-09, D8-10): seed locations for 2 articles (one at 50%, one at 30%); navigate to `#/`; assert the `.continue-reading-strip` section renders with `<h2>Continue reading</h2>` + 2 `<li>` cards. Assert the strip is sorted most-recently-opened first (the card with the later `savedAt` appears first).
    - Finished articles leave the strip (D8-12): seed a location at `graphemeOffset >= total * 0.98` for one article; assert it does NOT appear in the continue-reading strip (filtered out by FINISHED_THRESHOLD) but DOES appear in the main library list with the finished mark.
    - Empty strip: wipe all locations; assert the `.continue-reading-strip` section is NOT rendered (null when zero unfinished articles — spare chrome).

    Run these two specs across all 3 engines: `npx playwright test tests/e2e/library/search-tag-filter.spec.ts tests/e2e/library/progress-recent.spec.ts`. All must pass.

    HONEST-SUITE GATE (PROJECT.md Key Decision #9): after both tasks' specs pass, run the FULL `npm run test` suite (Vitest unit + Playwright e2e across chromium + firefox + webkit) in ONE invocation. Record the pass/fail/skip counts honestly in the SUMMARY. The gate is GREEN only when exit code is 0 AND fail count is 0. Do NOT subset, grep, or engine-skip. If any cell fails, surface it honestly (do not omit). Reference the Phase 7 Plan 07-07 + Phase 4 Plan 04-11 precedent (the "honest-suite" discipline).
  </action>
  <verify>
    <automated>cd /Users/eggfam/dev/lem-reader && npx playwright test tests/e2e/library/search-tag-filter.spec.ts tests/e2e/library/progress-recent.spec.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/library/search-tag-filter.spec.ts` asserts: tag entry via TagEntry, tag-filter via chip toggle, auto-prune when last article loses the tag, search by title/author/domain/tag, search + tag composition, empty-results graceful handling (SC#3, LIB-03, LIB-04, D8-05..D8-08)
    - `tests/e2e/library/progress-recent.spec.ts` asserts: per-row hairline fill ratio matches seeded graphemeOffset, finished mark at >= 0.98, continue-reading strip shows most-recently-opened-first, finished articles leave the strip but stay in the list, empty strip renders null (SC#5, LIB-06, D8-09..D8-12)
    - Both specs pass on chromium + firefox + webkit
    - The FULL `npm run test` suite exits 0 (the honest-suite gate — PROJECT.md Key Decision #9)
    - The pass/fail/skip counts are recorded honestly in the SUMMARY (no subset, no grep, no engine-skip; fail count is 0)
  </acceptance_criteria>
  <done>
    SC#3 (search + tag filter + auto-prune) and SC#5 (progress hairline + continue-reading + finished mark) are proven in real browsers. The full honest-suite gate (`npm run test`) exits 0 with fail=0 across all three engines — the Phase 8 phase-exit bar is met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → Vite Node middleware → markdownToBlocks | E2e tests exercise the real browser pipeline (no mocking of the server adapter) |
| Test-seeded Dexie rows → LibraryView render | E2e tests seed real IndexedDB rows via page.evaluate to prove the read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-19 | Repudiation | False-positive verification (suite reports green but cells fail) | mitigate | The honest-suite gate runs the FULL `npm run test` in ONE invocation; the executor records pass/fail/skip counts honestly; no subset/grep/engine-skip. Fail count MUST be 0. (PROJECT.md Key Decision #9 — the Phase 4/7 precedent.) |
| T-8-20 | Tampering | E2e test masks a real regression (e.g. v1-regression.spec.ts skips a fixture) | mitigate | v1-regression.spec.ts iterates EVERY fixture id from src/fixtures/index.ts dynamically (not a hardcoded subset); the row-count assertion matches the fixture count. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/library/` passes across chromium + firefox + webkit (all 6 new specs)
- The FULL `npm run test` suite exits 0 (Vitest unit + Playwright e2e — the honest-suite gate)
- Pass/fail/skip counts recorded honestly in the SUMMARY (fail=0)
- All 5 phase success criteria (SC#1-5) proven green
</verification>

<success_criteria>
- SC#1: Library is default route; fixtures badged; no v1.0 e2e regression (v1-regression.spec.ts green)
- SC#2: Cascade-remove proven across articles + highlights + notes + location (remove-cascade.spec.ts green)
- SC#3: Search by title/metadata + tag filter + auto-prune proven (search-tag-filter.spec.ts green)
- SC#4: .md upload with front-matter + .html upload + dedupe-refuse proven (markdown-upload.spec.ts green)
- SC#5: Progress hairline + continue-reading strip + finished mark proven (progress-recent.spec.ts green)
- Full `npm run test` exits 0 across chromium + firefox + webkit (honest-suite gate)
</success_criteria>

<output>
Create `.planning/phases/08-markdown-pipeline-and-personal-library/08-05-SUMMARY.md` when done — include the honest-suite pass/fail/skip counts (fail MUST be 0)
</output>

## Artifacts this phase produces

**Plan 05 symbols:**
- `tests/e2e/library/browse-open.spec.ts`: SC#1 + LIB-01 + LIB-05 e2e (default route, fixtures badged, source link)
- `tests/e2e/library/v1-regression.spec.ts`: SC#1 regression bar (every v1.0 fixture opens through LibraryView)
- `tests/e2e/library/remove-cascade.spec.ts`: SC#2 + LIB-02 e2e (cascade-remove + confirmation + cancel path)
- `tests/e2e/library/search-tag-filter.spec.ts`: SC#3 + LIB-03 + LIB-04 e2e (tag entry, filter, auto-prune, search, composition)
- `tests/e2e/library/markdown-upload.spec.ts`: SC#4 + ING-03 e2e (.md upload, front-matter, md- id, dedupe, .html variant)
- `tests/e2e/library/progress-recent.spec.ts`: SC#5 + LIB-06 e2e (per-row hairline, continue-reading strip, finished mark, empty strip)
