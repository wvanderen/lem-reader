---
phase: 05-durable-highlights-and-notes
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - tests/e2e/annotations/capture-highlight.spec.ts
  - tests/e2e/annotations/capture-rejects.spec.ts
  - tests/e2e/annotations/keyboard-shortcuts.spec.ts
  - tests/e2e/annotations/note-create-edit.spec.ts
  - tests/e2e/annotations/drawer-view.spec.ts
  - tests/e2e/annotations/delete-confirm.spec.ts
  - tests/e2e/annotations/navigate-back.spec.ts
  - tests/e2e/annotations/survive-relayout.spec.ts
  - tests/e2e/annotations/cross-fragment-render.spec.ts
  - tests/e2e/annotations/ambiguous-orphan-surface.spec.ts
  - tests/e2e/annotations/persist-reload.spec.ts
  - tests/e2e/annotations/forced-colors-shapes.spec.ts
  - tests/e2e/annotations/_fixtures.ts
autonomous: true
requirements: [ANNO-01, ANNO-02, ANNO-03, ANNO-04, ANNO-05, ANNO-07, STATE-03]
user_setup: []

must_haves:
  truths:
    - "Full `npm run test` suite exits 0 (all 753+ prior tests + every new annotation test) across chromium/firefox/webkit — the phase gate (mirrors Plan 04-11 precedent: honest pass/fail counts, no subset/grep/engine-skip)"
    - "Reader can select text and create a highlight in BOTH reading modes across the 6-fixture corpus × theme × 3 engines (ANNO-01)"
    - "Reader can attach/edit a note, open the drawer, navigate back, and delete with confirm — proven across the corpus × mode × engines (ANNO-02/03/04)"
    - "Highlights survive repagination (font/viewport change), mode switch, and article reopen across the corpus × engines (ANNO-05/STATE-03)"
    - "Cross-browser selection parity: a highlight created in chromium re-anchors identically in firefox/webkit (whitespace-serialization independence — Pitfall 2)"
    - "Ambiguous/orphan states surface explicitly (dashed marker + flagged drawer entry + non-navigating + open-announce) — never silent reattachment (ANNO-07)"
    - "The three inline highlight states are distinguishable by SHAPE under emulated forced-colors across engines (A11Y-05)"
  artifacts:
    - path: "tests/e2e/annotations/_fixtures.ts"
      provides: "annotation e2e fixture helpers (open article, select range, assert <mark>, drive corpus × theme × mode matrix) reusing tests/e2e/pagination matrix"
    - path: "tests/e2e/annotations/capture-highlight.spec.ts"
      provides: "ANNO-01 create-highlight in both modes across corpus × engines"
    - path: "tests/e2e/annotations/capture-rejects.spec.ts"
      provides: "ANNO-01 single-block rule (D5-06) + overlap rejection (D5-13)"
    - path: "tests/e2e/annotations/keyboard-shortcuts.spec.ts"
      provides: "A11Y-01 H/N keyboard paths"
    - path: "tests/e2e/annotations/note-create-edit.spec.ts"
      provides: "ANNO-02 note attach/edit + debounced save"
    - path: "tests/e2e/annotations/drawer-view.spec.ts"
      provides: "ANNO-03 drawer reading-order list + empty-state"
    - path: "tests/e2e/annotations/delete-confirm.spec.ts"
      provides: "ANNO-03 two-step delete + non-destructive default focus"
    - path: "tests/e2e/annotations/navigate-back.spec.ts"
      provides: "ANNO-04 drawer entry → passage in BOTH modes (D5-11)"
    - path: "tests/e2e/annotations/survive-relayout.spec.ts"
      provides: "ANNO-05/STATE-03 survive repagination + mode switch + reopen"
    - path: "tests/e2e/annotations/cross-fragment-render.spec.ts"
      provides: "D5-16 split-block highlight renders on both pages"
    - path: "tests/e2e/annotations/ambiguous-orphan-surface.spec.ts"
      provides: "ANNO-07 explicit ambiguous/orphan surfacing (no silent reattach)"
    - path: "tests/e2e/annotations/persist-reload.spec.ts"
      provides: "STATE-03 highlights/notes persist across reload"
    - path: "tests/e2e/annotations/forced-colors-shapes.spec.ts"
      provides: "A11Y-05 three-shape distinction under emulated forced-colors"
  key_links:
    - from: "tests/e2e/annotations/_fixtures.ts"
      to: "tests/e2e/calibration/fixtures-matrix.ts"
      via: "reuses the 6-fixture corpus × TypographyVariant matrix (imported, not forked)"
      pattern: "fixtures-matrix|FIXTURES"
---

## Phase Goal

**As a** reader, **I want to** create, revisit, and manage local highlights and notes, **so that** they remain attached to their intended normalized text across every view change.

> This plan (05-05) is the **phase-gate validation wave**: the full Playwright e2e corpus matrix across chromium/firefox/webkit proving ANNO-01..05, ANNO-07, and STATE-03 in REAL browsers (STACK.md forbids DOM emulators for layout truth). It mirrors the Phase 4 Plan 04-11 precedent — the gate is a green `npm run test` (full suite, honest counts, no subset/grep/engine-skip). Plans 05-01..04 shipped the implementation + unit tests; this plan ships the cross-browser proof.

<objective>
Build the annotation e2e suite under `tests/e2e/annotations/` covering: capture (both modes), rejection (multi-block + overlap), keyboard (H/N), notes (create/edit/debounced save), drawer (view + empty-state + reading order), delete (two-step confirm + non-destructive focus), navigate-back (paginated + scrolling), survival (repagination + mode switch + reopen), cross-fragment render, ambiguous/orphan surfacing, persist-reload, and forced-colors shape distinction. Each spec runs across the 6-fixture corpus × theme × mode matrix × 3 engines, reusing the existing `tests/e2e/calibration/fixtures-matrix.ts` + `tests/e2e/pagination/*` matrix structure. The phase-gate command is `npm run test` (unit + e2e × 3 engines) — must exit 0.

Purpose: This is the Nyquist Dimension-8 validation (the RESEARCH §Validation Matrix). Selection capture, `<mark>` rendering, cross-fragment slicing, and forced-colors MUST run in real browsers — jsdom is not authoritative for layout (STACK.md). Unit tests (Plans 05-01/02/04) proved the pure logic; this plan proves the rendering/interaction truth and closes every requirement.
Output: 1 fixture helper + 12 e2e spec files; the green full-suite run is the phase sign-off.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durable-highlights-and-notes/05-RESEARCH.md
@.planning/phases/05-durable-highlights-and-notes/05-UI-SPEC.md
@.planning/phases/05-durable-highlights-and-notes/05-VALIDATION.md
@.planning/phases/05-durable-highlights-and-notes/05-01-SUMMARY.md
@.planning/phases/05-durable-highlights-and-notes/05-02-SUMMARY.md
@.planning/phases/05-durable-highlights-and-notes/05-03-SUMMARY.md
@.planning/phases/05-durable-highlights-and-notes/05-04-SUMMARY.md
@tests/e2e/calibration/fixtures-matrix.ts
@tests/e2e/pagination/mode-switch-anchor.spec.ts
@tests/e2e/pagination/repagination-anchor.spec.ts
@tests/e2e/forced-colors.spec.ts
@tests/e2e/pagination/last-valid-view.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotation e2e fixture helper + capture/selection/keyboard specs (ANNO-01, A11Y-01)</name>
  <files>
tests/e2e/annotations/_fixtures.ts,
tests/e2e/annotations/capture-highlight.spec.ts,
tests/e2e/annotations/capture-rejects.spec.ts,
tests/e2e/annotations/keyboard-shortcuts.spec.ts
  </files>
  <read_first>
tests/e2e/calibration/fixtures-matrix.ts (read in full — the 6-fixture corpus ids + TypographyVariant type; IMPORT these, do not fork — the Plan 04-02 precedent confirmed sharing),
tests/e2e/pagination/mode-switch-anchor.spec.ts (read in full — the matrix loop structure + per-engine + open-article + assert pattern to copy for capture-highlight.spec.ts),
tests/e2e/pagination/repagination-anchor.spec.ts (read in full — the survive-relayout pattern: change font-size/viewport, assert anchor survives; copy for survive-relayout.spec.ts in Task 3),
tests/e2e/forced-colors.spec.ts (read in full — the emulated-forced-colors pattern; copy for forced-colors-shapes.spec.ts in Task 3),
src/fixtures/index.ts (read the 6 corpus ids the matrix references — confirm they exist; tests/e2e/calibration/fixtures-matrix.ts already verified against this in Plan 04-02),
05-RESEARCH.md §Validation Architecture (Test Framework + Phase Requirements → Test Map + Validation Matrix), §Pitfall 2 (cross-browser selection parity),
05-VALIDATION.md (Wave 0 + Sampling Rate: per-task npm run test:unit, phase gate full npm run test),
05-UI-SPEC.md §Interaction 24 (capture), §Interaction 33 (H/N), §Interaction 34 (invalid hints)
  </read_first>
  <action>
Create `tests/e2e/annotations/_fixtures.ts` — shared helpers reusing `tests/e2e/calibration/fixtures-matrix.ts` (IMPORT the FIXTURES array + TypographyVariant type — do not redeclare). Helpers: `openArticle(page, fixtureId)` (navigate to `#/fixtureId`, wait for `[data-block-index]` mounted — mirrors pagination specs), `selectTextInRange(page, blockIndex, startOffset, endOffset)` (use Playwright's `page.locator('[data-block-index="N"]')` + `.textContent()` + a Range-based selection via `page.evaluate` setting `window.getSelection()` from a DOM Range — the cross-engine selection path), `assertHighlightMark(page, { highlightId?, excerptSubstring?, hasNote?, unresolved? })` (query `mark.highlight` / `mark.highlight.has-note` / `mark.highlight.unresolved` + `data-highlight-id` + aria-label contains excerpt), `driveMatrix(test, fn)` (loop corpus × theme × mode the way the pagination specs do). Export a small synthetic-article route or use a fixture with a known-duplicated passage for the ambiguous case (Task 3) — prefer reusing an existing fixture + a DEV-only injection hook if the corpus lacks a natural duplicate (mirror the `__lemDiagnosticBus` DEV-hook precedent from Plan 04-10 for test-only resolution-status injection).

Create `tests/e2e/annotations/capture-highlight.spec.ts` — ANNO-01 in BOTH modes. For each fixture in a representative subset (or full corpus per the matrix) × [paginated, scrolling] × 3 engines: open article, select a known range in a paragraph, assert the `.selection-toolbar` appears, click "Highlight" (or press H), assert a `mark.highlight` renders at the captured range with the right `data-highlight-id` + `aria-label` starts with "Highlight", assert the selection cleared, assert "Highlight saved." appeared in `.status`. Cover the eligible-block breadth (D5-07): one capture each in a paragraph, a heading, a blockquote child, a list-item child, a figure caption, a code-block source, a footnote-reference marker — assert all succeed ("if you can read it, you can highlight it"). Cover paginated-mode binding (D5-08): assert the hidden `.article-body-measurement` is NOT selectable (attempt selection there → no toolbar / no highlight).

Create `tests/e2e/annotations/capture-rejects.spec.ts` — D5-06 + D5-13. Multi-block: select across two `[data-block-index]` blocks → toolbar shows "Select within a single block to highlight it." (or no toolbar per the lifecycle); H/N do nothing. Overlap: create a highlight, then select a range intersecting it → toolbar shows "This overlaps an existing highlight." Create does not fire. Cross-page (paginated): a selection spanning a page boundary is rejected (the single-block rule + visible-fragment binding).

Create `tests/e2e/annotations/keyboard-shortcuts.spec.ts` — A11Y-01 H/N. Select via Shift+arrows, press H → highlight created, toolbar dismissed, "Highlight saved." announced. Select, press N → highlight created + popover opens with focused empty textarea. Assert H/N do nothing when the selection is collapsed or inside a form field/`[contenteditable]`/dialog (the isFormField guard). Assert Tab reaches the toolbar buttons in DOM order (fallback keyboard path). Assert H/N do not conflict with M (mode toggle).
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/annotations/capture-highlight.spec.ts tests/e2e/annotations/capture-rejects.spec.ts tests/e2e/annotations/keyboard-shortcuts.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fixtures-matrix\|FIXTURES" tests/e2e/annotations/_fixtures.ts` ≥ 1 (matrix imported, not forked).
    - `grep -c "data-block-index" tests/e2e/annotations/_fixtures.ts` ≥ 1 (selection via the 1:1 block↔element mapping).
    - `grep -c "mark.highlight" tests/e2e/annotations/capture-highlight.spec.ts` ≥ 2 (asserts the rendered mark).
    - `grep -c "Select within a single block\|overlaps an existing highlight" tests/e2e/annotations/capture-rejects.spec.ts` ≥ 2 (D5-06 + D5-13 hint copy).
    - `grep -c "press.*[\"']h[\"']\|press.*[\"']H[\"']\|press.*[\"']n[\"']\|press.*[\"']N[\"']" tests/e2e/annotations/keyboard-shortcuts.spec.ts` ≥ 2 (H + N exercised).
    - `npm run test:e2e -- tests/e2e/annotations/capture-highlight.spec.ts tests/e2e/annotations/capture-rejects.spec.ts tests/e2e/annotations/keyboard-shortcuts.spec.ts` exits 0 across chromium/firefox/webkit.
  </acceptance_criteria>
  <done>
3 capture/keyboard specs + the shared fixture helper green across 3 engines. ANNO-01 + A11Y-01 proven in real browsers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Notes + drawer + delete + navigate-back specs (ANNO-02/03/04)</name>
  <files>
tests/e2e/annotations/note-create-edit.spec.ts,
tests/e2e/annotations/drawer-view.spec.ts,
tests/e2e/annotations/delete-confirm.spec.ts,
tests/e2e/annotations/navigate-back.spec.ts
  </files>
  <read_first>
tests/e2e/annotations/_fixtures.ts (the Task 1 helper — openArticle, assertHighlightMark, driveMatrix),
tests/e2e/pagination/mode-switch-anchor.spec.ts (the navigate-back paginated assertion structure — commitTurn lands on the right page),
tests/e2e/pagination/last-valid-view.spec.ts (read for the matrix + assert pattern),
src/reader/WipeConfirm.tsx (read the two-step confirm structure the delete-confirm spec asserts — [data-initial-focus] on Keep),
05-UI-SPEC.md §Interaction 29 (popover), §Interaction 30 (drawer), §Interaction 31 (navigate-back), §Interaction 32 (delete confirm + announces)
  </read_first>
  <action>
Create `tests/e2e/annotations/note-create-edit.spec.ts` — ANNO-02. Create a highlight via N → popover opens with focused empty textarea. Type a note → assert "Note saved." appears after the debounce window (use `expect(locator).toContainText` with a waitFor; assert it does NOT fire per-keystroke). Activate the `<mark>` → popover reopens with the existing note text + `mark.highlight.has-note` modifier present. Edit the text → debounced save fires. Clear the textarea + Done → assert the `has-note` modifier is removed (empty = no note). Assert note text renders as a text child (no HTML injection surface — defensive axe scan).

Create `tests/e2e/annotations/drawer-view.spec.ts` — ANNO-03. Open the drawer via the `.annotations-trigger` button → `aria-expanded="true"`, drawer title "Highlights and notes" is the dialog accessible name. Empty-state: an article with no highlights → ".drawer-empty" card with "No highlights yet" + the body copy. Populate 3 highlights across different blocks → assert the `<ol class="drawer-list">` entries are in READING ORDER (grapheme-start ascending = document order). Assert each entry's jump button + actions row. Assert the count badge on the trigger reflects N. Close via Esc / scrim / × → focus returns to the trigger (A11Y-02). Axe-core scan on the open drawer (no focus-trap escape, correct roles).

Create `tests/e2e/annotations/delete-confirm.spec.ts` — ANNO-03/D5-12. Open popover on a highlight → click Delete → assert the confirm prompt "Delete this highlight?" replaces the body. Assert focus moved to the Keep button (`[data-initial-focus]` — non-destructive default; an accidental Enter keeps the highlight). Click Keep → returns to edit view, nothing deleted. Click Delete again → confirm → click the destructive Delete → highlight + note removed together, popover closes, `mark.highlight` unmounts, "Highlight deleted." announced. Verify the cascade (note also gone) via the drawer or a Dexie peek.

Create `tests/e2e/annotations/navigate-back.spec.ts` — ANNO-04/D5-11 in BOTH modes. Create highlights on pages 1 and 5 (paginated) + in two distant blocks (scrolling). Open drawer, click an entry → drawer closes, page turns to the target page (paginated) OR scrollIntoView centers the block (scrolling), focus moves to the target `mark.highlight` (`:focus-visible` outline). Assert the navigation lands on the CORRECT passage (the `<mark>` with the right `data-highlight-id` is focused). Assert navigating from scrolling mode does NOT force a mode switch and vice versa. Assert ambiguous/orphan entries (Task 3 seeds them) do NOT navigate (disabled jump button is a no-op).
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/annotations/note-create-edit.spec.ts tests/e2e/annotations/drawer-view.spec.ts tests/e2e/annotations/delete-confirm.spec.ts tests/e2e/annotations/navigate-back.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "has-note\|Note saved" tests/e2e/annotations/note-create-edit.spec.ts` ≥ 2.
    - `grep -c "drawer-empty\|No highlights yet\|reading order\|drawer-list" tests/e2e/annotations/drawer-view.spec.ts` ≥ 2.
    - `grep -c "data-initial-focus\|Keep\|Highlight deleted" tests/e2e/annotations/delete-confirm.spec.ts` ≥ 2 (Pitfall 8 non-destructive default + announce).
    - `grep -c "scrollIntoView\|commitTurn\|focus.*mark\|data-highlight-id" tests/e2e/annotations/navigate-back.spec.ts` ≥ 2 (D5-11 both modes).
    - `npm run test:e2e -- tests/e2e/annotations/note-create-edit.spec.ts tests/e2e/annotations/drawer-view.spec.ts tests/e2e/annotations/delete-confirm.spec.ts tests/e2e/annotations/navigate-back.spec.ts` exits 0 across chromium/firefox/webkit.
  </acceptance_criteria>
  <done>
4 management specs green across 3 engines. ANNO-02 (notes), ANNO-03 (view/delete), ANNO-04 (navigate-back both modes) proven in real browsers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Survival + cross-fragment + ambiguous/orphan + persist-reload + forced-colors specs + full npm run test phase gate</name>
  <files>
tests/e2e/annotations/survive-relayout.spec.ts,
tests/e2e/annotations/cross-fragment-render.spec.ts,
tests/e2e/annotations/ambiguous-orphan-surface.spec.ts,
tests/e2e/annotations/persist-reload.spec.ts,
tests/e2e/annotations/forced-colors-shapes.spec.ts
  </files>
  <read_first>
tests/e2e/pagination/repagination-anchor.spec.ts (read in full — the survive-repagination pattern: capture state, change font-size/viewport via page.evaluate/emulation, assert the anchor survives; copy for survive-relayout.spec.ts),
tests/e2e/pagination/mode-switch-anchor.spec.ts (read in full — the survive-mode-switch pattern; copy),
tests/e2e/forced-colors.spec.ts (read in full — the emulated-forced-colors pattern via page.emulateMedia; copy for forced-colors-shapes.spec.ts),
tests/e2e/annotations/_fixtures.ts (Task 1 helper — including any DEV-only resolution-status injection hook for the ambiguous/orphan seed),
05-RESEARCH.md §Validation Matrix (Highlight survival, Cross-fragment rendering, Forced-colors shape distinction, Cross-browser selection parity),
05-UI-SPEC.md §Interaction 26 (cross-fragment), §Interaction 28 (unresolved marker), §Color (forced-colors contract)
  </read_first>
  <action>
Create `tests/e2e/annotations/survive-relayout.spec.ts` — ANNO-05/STATE-03. Create a highlight at a known range. (a) Change font-size via the settings panel (or `page.evaluate` writing the `--font-size` custom property) → wait for repagination → assert the `mark.highlight` still renders at the SAME passage (re-resolve from the persisted offset; the grapheme offset is canonical, the page number changed but the passage didn't). (b) Switch mode (M) paginated↔scrolling → assert the highlight survives the mode switch (renders in the new mode at the same passage). (c) Reload the page → assert the highlight + its note reload from Dexie and render at the same passage (STATE-03 persistence across sessions). Cover across the corpus × engines.

Create `tests/e2e/annotations/cross-fragment-render.spec.ts` — D5-16. Find or construct a case where a highlighted block is split across a page boundary (use a viewport/typography combination that forces a known split, or the corpus's longest paragraph). Assert BOTH page fragments render a `mark.highlight` slice for the same `data-highlight-id` (no silent gap at the page turn). Turn the page → assert the mark is reachable on both pages + the popover opens from either. Assert the shared `data-highlight-id` ties the slices together.

Create `tests/e2e/annotations/ambiguous-orphan-surface.spec.ts` — ANNO-07. Seed an ambiguous highlight (a DEV-only injection hook sets a highlight's status to 'ambiguous' — mirror the `__lemDiagnosticBus` precedent; OR use a fixture with a duplicated passage if one exists). Assert: the inline render is `mark.highlight.unresolved` (dashed outline, NOT filled), the drawer entry shows the flag ("Couldn't find a unique match") + disabled jump button + enabled Delete, and the one-time ".status" announce "{N} highlight(s) couldn't be relocated." fires on open. Seed an orphan highlight (status 'orphan', e.g. via a revision mismatch simulation) → assert the "Couldn't relocate this highlight" flag + dashed marker at the stored vicinity. Assert NEITHER case silently re-attaches to a filled mark (the core ANNO-07 contract).

Create `tests/e2e/annotations/persist-reload.spec.ts` — STATE-03 (focused). Create 2 highlights + 1 note, reload the page (full `page.reload()`), assert all 3 records reload from Dexie and render correctly (the highlight at the same passage, the note attached). Assert the drawer count + entries match. Cover across engines (verifies the Dexie compound-index query + Zod boundary on real reload).

Create `tests/e2e/annotations/forced-colors-shapes.spec.ts` — A11Y-05. `page.emulateMedia({ forcedColors: 'active' })`. Create 3 highlights: one bare, one with a note, one unresolved (seeded). Assert the three inline states are distinguishable by computed style SHAPE: bare = solid `Highlight` fill; note-bearing = dotted underline (text-decoration-style: dotted survives); unresolved = dashed outline (outline-style: dashed survives). Assert none relies on color alone (the UA forced palette applies but the shapes differ). Cover across engines.

Finally run the **phase-gate command**: `npm run test` (full suite: all unit + all e2e × chromium/firefox/webkit). Record the honest pass/fail counts in the SUMMARY (mirrors Plan 04-11 anti-pattern guard: the executor runs the suite itself, records fail=0 honestly, no subset/grep/engine-skip).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "repagination\|font-size\|reload\|mode" tests/e2e/annotations/survive-relayout.spec.ts` ≥ 2 (ANNO-05 survival paths).
    - `grep -c "data-highlight-id" tests/e2e/annotations/cross-fragment-render.spec.ts` ≥ 1 (D5-16 shared id across fragments).
    - `grep -c "unresolved\|Couldn't\|couldn't be relocated\|ambiguous" tests/e2e/annotations/ambiguous-orphan-surface.spec.ts` ≥ 3 (ANNO-07 explicit surfacing).
    - `grep -c "page.reload\|reload" tests/e2e/annotations/persist-reload.spec.ts` ≥ 1 (STATE-03 reload).
    - `grep -c "forcedColors\|forced-colors\|dotted\|dashed" tests/e2e/annotations/forced-colors-shapes.spec.ts` ≥ 2 (A11Y-05 shape distinction).
    - `npm run test` (the FULL suite — unit + e2e × chromium/firefox/webkit) exits 0 with all prior 753 tests + every new annotation test passing; honest counts recorded in the SUMMARY (no subset, no --grep, no engine skip — Plan 04-11 precedent).
  </acceptance_criteria>
  <done>
5 survival/cross-fragment/ambiguous-orphan/persist/forced-colors specs green across 3 engines. Full `npm run test` exits 0 — the phase gate. ANNO-05, ANNO-07, STATE-03, A11Y-05 closed in real browsers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → real browser engines | Playwright drives chromium/firefox/webkit; the gate is honest pass/fail across all three (no engine skip) |
| Seeded ambiguous/orphan state → assertion | A DEV-only injection hook (mirrors __lemDiagnosticBus) sets resolution status for the ANNO-07 test; never shipped to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Task |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Tampering | e2e gate misreporting | mitigate | Task 3 runs the FULL `npm run test` itself; records honest fail=0; no subset/grep/engine-skip (Plan 04-11 anti-pattern guard) |
| T-05-18 | Information | DEV-only test hook leaking to prod | mitigate | The ambiguous/orphan seed hook is import.meta.env.DEV-gated (mirrors __lemDiagnosticBus); stripped from production builds |
| T-05-19 | Tampering | cross-browser selection parity drift | mitigate | capture specs run × 3 engines; Pitfall 2 (selection.toString variance) is caught if a highlight drifts across engines |
</threat_model>

<verification>
`npm run test` (full suite) exits 0. The 05-VALIDATION.md `nyquist_compliant` flag is set to `true` and `wave_0_complete` to `true` once the suite is green. The 05-VALIDATION.md Per-Task Verification Map is filled with the spec file names + green status. Manual-only verifications (screen-reader announce quality, full keyboard traversal) remain for `/gsd-verify-work` per 05-VALIDATION.md §Manual-Only.
</verification>

<success_criteria>
- Full `npm run test` suite green (753 prior + all annotation tests) across chromium/firefox/webkit — phase gate met (Plan 04-11 precedent).
- ANNO-01 (capture both modes), ANNO-02 (notes), ANNO-03 (view/delete), ANNO-04 (navigate-back), ANNO-05 (survive relayout), ANNO-07 (ambiguous/orphan explicit), STATE-03 (persist reload) all proven in real browsers.
- Cross-browser selection parity, cross-fragment render, and forced-colors shape distinction verified across engines.
- 05-VALIDATION.md updated: `nyquist_compliant: true`, Per-Task Map filled, honest counts recorded.
</success_criteria>

<output>
Create `.planning/phases/05-durable-highlights-and-notes/05-05-SUMMARY.md` when done (record the literal `npm run test` pass/fail counts per suite + per engine — the permanent record, mirroring 04-11-OUTPUT.md).
</output>

## Artifacts this phase produces (Plan 05-05)

**E2E test files (tests/e2e/annotations/):**
- `_fixtures.ts` — shared helpers (openArticle, selectTextInRange, assertHighlightMark, driveMatrix) reusing fixtures-matrix
- `capture-highlight.spec.ts`, `capture-rejects.spec.ts`, `keyboard-shortcuts.spec.ts` (ANNO-01, A11Y-01)
- `note-create-edit.spec.ts`, `drawer-view.spec.ts`, `delete-confirm.spec.ts`, `navigate-back.spec.ts` (ANNO-02/03/04)
- `survive-relayout.spec.ts`, `cross-fragment-render.spec.ts`, `ambiguous-orphan-surface.spec.ts`, `persist-reload.spec.ts`, `forced-colors-shapes.spec.ts` (ANNO-05/07, STATE-03, A11Y-05, D5-16)

**Phase-gate record:** 05-05-SUMMARY.md with the literal `npm run test` counts (per suite + per engine), exit 0.
