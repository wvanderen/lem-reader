---
phase: 06-prototype-acceptance
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/e2e/forced-colors.spec.ts
  - tests/e2e/reduced-motion.spec.ts
  - tests/e2e/reflow.spec.ts
  - tests/e2e/touch-targets.spec.ts
autonomous: true
requirements: [ACPT-03]
user_setup: []

must_haves:
  truths:
    - "The four existing edge specs (forced-colors, reduced-motion, reflow, touch-targets) each assert the shared D6-09 invariant: (a) full content reachable via keyboard in both modes, (b) no required function unreachable, (c) no overflow clips content"
    - "No edge condition is left trusting the existing spec as-is without confirming it meets the locked invariant (D6-12)"
    - "The strengthened specs remain green across chromium, firefox, webkit — no regression introduced by the audit"
  artifacts:
    - path: "tests/e2e/forced-colors.spec.ts"
      provides: "ACPT-03 forced-colors spec audited to apply the shared invariant via assertEdgeInvariant"
    - path: "tests/e2e/reduced-motion.spec.ts"
      provides: "ACPT-03 reduced-motion spec audited to apply the shared invariant via assertEdgeInvariant"
    - path: "tests/e2e/reflow.spec.ts"
      provides: "ACPT-03 reflow spec audited to apply the shared invariant (its overflow check is the (c) clause origin; now also asserts (a)/(b))"
    - path: "tests/e2e/touch-targets.spec.ts"
      provides: "ACPT-03 touch-targets spec audited to apply the shared invariant"
  key_links:
    - from: "tests/e2e/forced-colors.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant } — applied per fixture × condition"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/reduced-motion.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant }"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/reflow.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant } — (c) overflow clause origin lifted into the shared helper"
      pattern: "assertEdgeInvariant"
    - from: "tests/e2e/touch-targets.spec.ts"
      to: "tests/e2e/_edge-invariant.ts"
      via: "import { assertEdgeInvariant }"
      pattern: "assertEdgeInvariant"
---

<objective>
Audit the four existing edge-condition specs against the shared D6-09 invariant and strengthen them where they assert less than (a)/(b)/(c).

This completes the ACPT-03 vertical slice alongside Plan 06-01 (which shipped the invariant helper + the two NEW gap specs). D6-12 mandates: audit `forced-colors.spec.ts`, `reduced-motion.spec.ts`, `reflow.spec.ts`, `touch-targets.spec.ts` against the shared invariant and strengthen where weak. No edge condition is left to "trust the existing spec as-is." reflow.spec.ts is the (c) overflow-clause ORIGIN — its check was lifted into `_edge-invariant.ts` in Plan 06-01; now it consumes the helper for (a)/(b) too.

Purpose: The single accessibility bar ("the reader loses nothing") must actually hold EVERYWHERE — not six different weaker per-spec assertions. D6-09 deliberately applies one invariant to every condition so acceptance means the same thing everywhere.
Output: Four existing specs strengthened in place (no new files).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prototype-acceptance/06-CONTEXT.md
@.planning/phases/06-prototype-acceptance/06-RESEARCH.md
@.planning/phases/06-prototype-acceptance/06-PATTERNS.md

# The shared invariant helper produced by Plan 06-01 (Wave 1 dependency)
@tests/e2e/_edge-invariant.ts
# The four specs to audit
@tests/e2e/forced-colors.spec.ts
@tests/e2e/reduced-motion.spec.ts
@tests/e2e/reflow.spec.ts
@tests/e2e/touch-targets.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit forced-colors + reduced-motion specs to the shared invariant</name>
  <files>tests/e2e/forced-colors.spec.ts, tests/e2e/reduced-motion.spec.ts</files>
  <read_first>
    - tests/e2e/_edge-invariant.ts (the helper from Plan 06-01 — assertEdgeInvariant asserting (a)/(b)/(c); import it)
    - tests/e2e/forced-colors.spec.ts (the spec being audited — current emulateMedia({forcedColors:'active'}) beforeEach + what it asserts today; identify which of (a)/(b)/(c) it already covers and which are missing)
    - tests/e2e/reduced-motion.spec.ts (the spec being audited — current emulateMedia({reducedMotion:'reduce'}) + what it asserts today; identify gaps vs the invariant)
    - .planning/phases/06-prototype-acceptance/06-CONTEXT.md (D6-09 the three-clause invariant; D6-12 audit-and-strengthen)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ Shared Patterns — the helper applies uniformly)
  </read_first>
  <action>
    Audit both specs against the D6-09 invariant. For each spec: read what it currently asserts, identify which of the three clauses (a) full content reachable via keyboard in both modes, (b) no required function unreachable, (c) no overflow clips content are MISSING, and add the missing assertions by importing and calling `assertEdgeInvariant(page, { fixture, condition })` from _edge-invariant.ts. Do NOT remove existing assertions — only strengthen (D6-12).

    For forced-colors.spec.ts: the condition label passed to assertEdgeInvariant is "forced-colors"; keep the existing emulateMedia({forcedColors:'active'}) setup. Confirm meaning is not lost under forced colors (A11Y-05 substrate) — the invariant's (c) overflow + (b) required-functions-reachable clauses cover this.

    For reduced-motion.spec.ts: the condition label is "reduced-motion"; keep the existing emulateMedia({reducedMotion:'reduce'}) setup. Confirm navigation is immediate and motion-safe (A11Y-06 substrate) — the invariant's (a) keyboard-reachable-in-both-modes clause covers this.

    Where a spec already iterates only a subset of fixtures, consider widening to assert the invariant across the corpus (D6-09 applies uniformly), but preserve the spec's existing intent and keep run time reasonable. Both specs must remain green across chromium, firefox, webkit.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "forced-colors|reduced-motion" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - forced-colors.spec.ts imports and calls assertEdgeInvariant from _edge-invariant.ts (grep for assertEdgeInvariant in the file)
    - reduced-motion.spec.ts imports and calls assertEdgeInvariant from _edge-invariant.ts (grep confirms)
    - Both specs remain green: `npm run test:e2e -- --grep "forced-colors|reduced-motion"` exits 0 across chromium, firefox, webkit
    - No existing assertion is removed (strengthen-only per D6-12)
  </acceptance_criteria>
  <done>forced-colors.spec.ts and reduced-motion.spec.ts both apply the shared D6-09 invariant via assertEdgeInvariant and remain green across all 3 engines.</done>
</task>

<task type="auto">
  <name>Task 2: Audit reflow + touch-targets specs to the shared invariant</name>
  <files>tests/e2e/reflow.spec.ts, tests/e2e/touch-targets.spec.ts</files>
  <read_first>
    - tests/e2e/_edge-invariant.ts (import assertEdgeInvariant — note reflow.spec.ts is the (c) overflow-clause ORIGIN; its overflow check was lifted into the helper, so reflow.spec.ts should now ALSO assert (a)/(b) via the helper)
    - tests/e2e/reflow.spec.ts (the spec being audited — current setViewportSize({width:320}) + the body/article-body overflow check at ~L22-49 that is the (c) origin; identify whether (a)/(b) are asserted today)
    - tests/e2e/touch-targets.spec.ts (the spec being audited — current touch-target sizing + what it asserts; identify gaps vs the invariant)
    - .planning/phases/06-prototype-acceptance/06-CONTEXT.md (D6-09, D6-12)
    - .planning/phases/06-prototype-acceptance/06-PATTERNS.md (§ Shared Patterns)
  </read_first>
  <action>
    Audit both specs against the D6-09 invariant. For each: import and call `assertEdgeInvariant(page, { fixture, condition })` from _edge-invariant.ts to cover any missing clause; strengthen only, do not remove existing assertions (D6-12).

    For reflow.spec.ts: this spec is the (c) overflow-clause ORIGIN (its body/article-body scrollWidth check was lifted verbatim into _edge-invariant.ts by Plan 06-01). The audit adds the (a) full-content-reachable-in-both-modes and (b) required-functions-reachable clauses via the helper, so reflow asserts the COMPLETE invariant, not just overflow. Condition label "reflow-320". Keep the existing setViewportSize({width:320,height:800}) setup. Avoid double-asserting (c) — call the helper (which includes (c)) rather than duplicating the inline overflow check where the helper already covers it.

    For touch-targets.spec.ts: condition label "touch-targets". Keep the existing touch-target-sizing assertions (A11Y-07 substrate); add the invariant's (a)/(c) clauses via the helper where missing. Confirm required functions remain reachable via touch-sized targets.

    Both specs must remain green across chromium, firefox, webkit.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "reflow|touch-targets" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - reflow.spec.ts imports and calls assertEdgeInvariant from _edge-invariant.ts (grep confirms) — now asserts the complete invariant (a)/(b)/(c), not just overflow
    - touch-targets.spec.ts imports and calls assertEdgeInvariant from _edge-invariant.ts (grep confirms)
    - Both specs remain green: `npm run test:e2e -- --grep "reflow|touch-targets"` exits 0 across chromium, firefox, webkit
    - No existing assertion is removed (strengthen-only per D6-12)
  </acceptance_criteria>
  <done>reflow.spec.ts and touch-targets.spec.ts both apply the shared D6-09 invariant via assertEdgeInvariant and remain green across all 3 engines; reflow now asserts the complete invariant, not just overflow.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06-05)

| Artifact | Path | Symbol / Kind |
|----------|------|---------------|
| Audited forced-colors spec | `tests/e2e/forced-colors.spec.ts` | strengthened in place — applies assertEdgeInvariant |
| Audited reduced-motion spec | `tests/e2e/reduced-motion.spec.ts` | strengthened in place — applies assertEdgeInvariant |
| Audited reflow spec | `tests/e2e/reflow.spec.ts` | strengthened in place — (c) origin now also asserts (a)/(b) via the helper |
| Audited touch-targets spec | `tests/e2e/touch-targets.spec.ts` | strengthened in place — applies assertEdgeInvariant |

No new files. No production code. No new packages. All four existing specs consume the shared `_edge-invariant.ts` helper produced by Plan 06-01.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | In-place strengthening of existing test specs; no production code or new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | an edge condition silently drops content/function the existing spec did not assert | mitigate | The uniform D6-09 invariant (now applied by all four specs) closes per-spec weak-assertion gaps; every edge condition is held to the same (a)/(b)/(c) bar |
| T-06-SC | Tampering | npm package installs | accept | Plan 06-05 installs ZERO packages; no install task |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run test:e2e -- --grep "forced-colors|reduced-motion|reflow|touch-targets"` exits 0 across chromium, firefox, webkit
- All four audited specs import assertEdgeInvariant (grep confirms per file)
- The full `npm run test` suite remains green after this plan — verified at Wave merge by Plan 06-06
</verification>

<success_criteria>
- ACPT-03 is complete: all six edge conditions (high-zoom + font-failure NEW from Plan 06-01; forced-colors + reduced-motion + reflow + touch-targets audited here) assert the SAME shared invariant (D6-09/D6-12)
- No existing assertion is weakened or removed; all specs remain green across 3 engines
</success_criteria>

<output>
Create `.planning/phases/06-prototype-acceptance/06-05-SUMMARY.md` when done
</output>
