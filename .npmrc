# TypeScript 7.0.2 (locked by STACK.md) exceeds the declared peer range of
# @typescript-eslint/parser@8 (<6.1.0). No stable v9+ exists yet; the parser
# works correctly with TS 7 in practice. Pin legacy-peer-deps so installs are
# reproducible without passing the flag every time.
legacy-peer-deps=true
